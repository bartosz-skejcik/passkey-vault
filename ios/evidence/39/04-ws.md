# Phase 39, Plan 39-04 -- WebSocket push evidence

## Task 2 -- two live pushes, poll disabled, plaintext compared byte for byte

- Server: http://127.0.0.1:8621
- Simulator UDID: 34992BB7-4982-4915-92C7-C7FC987802AF
- Account: pv-39-04-ws-1787091582@example.invalid
- Item id: bde46278-209f-48ba-a13d-aa91ff974e9b
- repeatingPullDisabled = true confirmed in ios/PasskeyVault/PasskeyVault/Sync/LiveSyncProbe.swift before this run started (D-06 preflight).
- The script refuses to run when that literal is absent -- demonstrated in the plan SUMMARY (temporarily flipped to false, script exited 2, reverted).

### Timestamps

| event | UTC time |
|---|---|
| socket open (PVSYNC|event=open) | 2026-08-18T22:19:45Z |
| mutation 1 (create, literal 1) | 2026-08-18T22:19:45Z |
| frame 1 observed | 2026-08-18T22:19:45Z |
| rendered literal 1 confirmed | 2026-08-18T22:19:46Z |
| mutation 2 (edit, literal 2) | 2026-08-18T22:19:46Z |
| frame 2 observed | 2026-08-18T22:19:47Z |
| rendered literal 2 confirmed | 2026-08-18T22:19:47Z |

- Elapsed mutation-1 -> rendered-1: 1s
- Elapsed mutation-2 -> rendered-2: 1s
- Elapsed mutation-1 -> mutation-2: 1s (well under the 30s poll interval, which was DISABLED for this run -- no reader can attribute the second update to a timer)
- Frame count before mutation 2's wait: 1; after: 2 (the re-arm's own proof -- a one-shot receive would have left this unchanged)

### Literals (verbatim)

- literal 1 (create): `pv-39-04 ws push literal one 1787091333`
- literal 2 (edit): `pv-39-04 ws push literal two edited 1787091333`

### Landmines found this run (recorded as L-23/L-24, ios/IOS-SPIKE-LOG.md Sec 3 -- see the plan SUMMARY for why the numbering differs from the plan text's "L-10"/"L-11")

- **L-23**: the FIRST attempt at this run failed the socket open with a 401, traced via a local TCP sniff proxy
  to `URLComponents.queryItems` not percent-encoding `+` in the session token, which `pv-server`'s
  `axum::extract::Query` then decoded as a space. Fixed in `SyncSocket.wsURL` via
  `.percentEncodedQueryItems` with `+` excluded from the allowed character set. This run's PASS is with the fix
  applied; the pre-fix failure transcript (raw sniffed bytes, `401 {"error":"unauthorized"}`) is quoted in
  `ios/IOS-SPIKE-LOG.md` Sec 3, L-23.
- **L-24**: the re-arm guard's RED-before-green demonstration (Task 1) -- see the plan SUMMARY for the transcript.

### crates/pv-server diff gate

```
$ git diff --stat -- crates/pv-server   # before
(empty)
$ echo '' >> crates/pv-server/src/main.rs && git diff --stat -- crates/pv-server
 crates/pv-server/src/main.rs | 1 +
 1 file changed, 1 insertion(+)
$ git checkout -- crates/pv-server/src/main.rs && git diff --stat -- crates/pv-server
(empty)
```

