---
phase: 01-wasm-crypto-bridge-web-app-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - web/package.json
  - web/next.config.ts
  - web/tsconfig.json
  - web/vitest.config.ts
  - web/src/app/globals.css
  - web/src/app/layout.tsx
  - web/src/app/page.tsx
  - .gitignore
autonomous: false
requirements: [UI-01]

must_haves:
  truths:
    - "A developer running `cd web && npm run dev` gets a real Next.js 16 static-export-capable app, not a partial scaffold"
    - "globals.css defines both vault-dark (default) and vault-light DaisyUI 5 CSS-first themes using the exact OKLCH tokens from docs/UI-DESIGN.md §5"
    - "The theme's initial value is resolved before first paint (inline pre-hydration script), never a client useEffect (would cause FOUC)"
    - "The npm build pipeline is wired to scripts/build-wasm.sh via prebuild/predev so the WASM artifact from plan 01-01 is always current"
  artifacts:
    - "web/package.json — dev/build/prebuild/predev/test scripts, pinned dependency versions"
    - "web/next.config.ts — output: \"export\""
    - "web/src/app/globals.css — vault-dark + vault-light @plugin \"daisyui/theme\" blocks"
    - "web/src/app/layout.tsx — next/font DM Sans + Fuzzy Bubbles, pre-hydration theme script"
    - "web/vitest.config.ts — test runner config for plan 01-03's facade tests"
  key_links:
    - "web/package.json prebuild/predev scripts invoke ../scripts/build-wasm.sh (relative path from web/ to repo-root scripts/)"
    - "globals.css theme names (vault-dark/vault-light) match the data-theme values layout.tsx's pre-hydration script sets on <html>"
---

<objective>
Hand-author the Next.js 16 app scaffold in `web/` (static export, TypeScript, App Router) with the datafa.st `vault-dark`/`vault-light` DaisyUI 5 themes wired in, font loading via `next/font`, and a flash-free theme toggle mechanism — everything the shell and self-test UI in plan 01-03 will render into. This plan installs and configures tooling only; the actual shell components and self-test card are plan 01-03's job (kept separate so each plan stays a focused, reviewable unit).

Purpose: Stand up a real, buildable Next.js static-export app matching every locked decision in CONTEXT.md's "Build Pipeline & Layout" and "Shell UI" sections, so plan 01-03 can focus entirely on the user-visible shell and self-test wiring.
Output: A `npm run build`-able `web/` app with both themes defined, fonts loading, and vitest configured (no test files yet — those are plan 01-03's).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wasm-crypto-bridge-web-app-shell/01-CONTEXT.md
@.planning/phases/01-wasm-crypto-bridge-web-app-shell/01-RESEARCH.md
@.planning/phases/01-wasm-crypto-bridge-web-app-shell/01-UI-SPEC.md
@docs/UI-DESIGN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify npm package legitimacy before install</name>
  <action>Blocking human gate (never auto-approvable, regardless of workflow.auto_advance): pause before Task 2's `npm install` and have the developer verify the four `[SUS]`-flagged packages (`next`, `tailwindcss`, `daisyui`, `lucide-react`) against npmjs.com per the steps in `<how-to-verify>`. Proceed only on explicit "approved".</action>
  <what-built>Nothing yet — this is a pre-install gate. RESEARCH.md's Package Legitimacy Audit flags `next`, `tailwindcss`, `daisyui`, and `lucide-react` as `[SUS]` on a "too-new" recency heuristic (all had a very recent patch release), even though the audit's own download-count/source-repo cross-check assesses them as safe false positives.</what-built>
  <how-to-verify>
    Before Task 2 runs `npm install`, confirm each of these four packages independently on the npm registry:
    1. https://www.npmjs.com/package/next — expect publisher `vercel`, version `16.2.10`, weekly downloads in the tens of millions.
    2. https://www.npmjs.com/package/tailwindcss — expect publisher `tailwindlabs`, version `4.3.2`, weekly downloads over 100M.
    3. https://www.npmjs.com/package/daisyui — expect publisher `saadeghi`, version `5.6.18`, weekly downloads in the hundreds of thousands.
    4. https://www.npmjs.com/package/lucide-react — expect publisher `lucide-icons` org, version `1.24.0`, weekly downloads in the tens of millions.
    If any package's registry page shows a different publisher/org than expected, a suspiciously low download count, or a source repo that doesn't match the known project, stop and report it instead of approving.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with npm install, or describe which package looks wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Hand-author Next.js 16 scaffold — package.json, config, both DaisyUI themes, font/theme wiring</name>
  <files>web/package.json, web/next.config.ts, web/tsconfig.json, web/vitest.config.ts, web/src/app/globals.css, web/src/app/layout.tsx, web/src/app/page.tsx, .gitignore</files>
  <read_first>
    .planning/phases/01-wasm-crypto-bridge-web-app-shell/01-RESEARCH.md (Standard Stack table for exact pinned versions; Pattern 3 for the verbatim DaisyUI theme CSS block, including the vault-light completion; Recommended Project Structure)
    docs/UI-DESIGN.md §5 (source-of-truth theme token block)
    .planning/phases/01-wasm-crypto-bridge-web-app-shell/01-CONTEXT.md (Build Pipeline & Layout, Shell UI & Self-Test decisions)
    .gitignore (current contents — append, do not overwrite; note plan 01-01 already added web/src/lib/crypto/wasm/ and web/public/wasm/)
  </read_first>
  <action>
    Do NOT run `create-next-app` — it requires an empty target directory and interactive prompts, and `web/src/lib/crypto/wasm/`/`web/public/wasm/` already contain build artifacts from plan 01-01. Hand-author every file below directly into the existing `web/` directory.

    `web/package.json`: name `"web"`, private true. `scripts`: `"dev": "next dev"`, `"build": "next build"`, `"prebuild": "bash ../scripts/build-wasm.sh"`, `"predev": "bash ../scripts/build-wasm.sh"`, `"test": "vitest run"`. Dependencies pinned exactly per RESEARCH.md Standard Stack: `next@16.2.10`, `react@19.2.7`, `react-dom@19.2.7`, `daisyui@5.6.18`, `lucide-react@1.24.0`. DevDependencies: `typescript@7.0.2`, `tailwindcss@4.3.2`, `@types/node@26.1.1`, `@types/react@19.2.17`, `vitest`, `jsdom`, `@vitejs/plugin-react` (latest stable of these three — plan 01-03 needs them for facade tests).

    `web/next.config.ts`: minimal Next.js config exporting `output: "export"` (per UI-01 — static export only, no SSR/API routes, per REQUIREMENTS.md's Out of Scope).

    `web/tsconfig.json`: standard Next.js App Router TS config (strict mode, `moduleResolution: "bundler"`, JSX `preserve`) with path alias `"@/*": ["./src/*"]`.

    `web/vitest.config.ts`: `environment: "jsdom"`, React plugin registered, no test files referenced yet (plan 01-03 adds `web/src/lib/crypto/index.test.ts`).

    `web/src/app/globals.css`: `@import "tailwindcss"; @plugin "daisyui";` followed by both theme blocks verbatim from RESEARCH.md Pattern 3 / docs/UI-DESIGN.md §5 — `vault-dark` (`default: true`, `color-scheme: dark`, all `--color-*`/`--radius-*`/`--border` tokens exactly as specified: primary `oklch(65.31% 0.1637 37.22)`, accent `oklch(74.51% 0.167 183.61)`, base-100/200/300 `oklch(26.86%/24.78%/23.93% 0 0)`, base-content `oklch(89.80% 0.0017 67.80)`, success/warning/error/info per docs/UI-DESIGN.md §2 table, `--radius-box: 1rem`, `--radius-field: 0.5rem`, `--radius-selector: 1.9rem`, `--border: 1px`) and `vault-light` (`color-scheme: light`, base-100 `oklch(100% 0 0)`, base-200 AND base-300 both `oklch(98.86% 0.0017 67.80)` per UI-SPEC.md's Color section, base-content `oklch(26.86% 0 0)`; primary/secondary/accent/semantic tokens are theme-invariant OKLCH values — do not redeclare them in the light block, they inherit).

    `web/src/app/layout.tsx`: load `DM_Sans` via `next/font/google` as CSS variable `--font-sans` and `Fuzzy_Bubbles` (weight `"400"`) as `--font-hand`, both `display: "swap"`. Add an inline `<script>` (not a client `useEffect` — avoids flash-of-wrong-theme) in `<head>`, executed before paint, that reads `localStorage.getItem('pv-theme')`, falls back to `window.matchMedia('(prefers-color-scheme: light)').matches ? 'vault-light' : 'vault-dark'` if unset, and sets `document.documentElement.setAttribute('data-theme', value)`. Apply the `--font-sans`/`--font-hand` CSS variable classes to `<body>`.

    `web/src/app/page.tsx`: temporary placeholder (e.g. a single `<main>` with plain text noting the shell lands in the next plan) — plan 01-03 replaces this file's content entirely with the real shell composition; do not build shell/self-test components here.

    Append to root `.gitignore` (below the existing `web/src/lib/crypto/wasm/`/`web/public/wasm/` lines from plan 01-01, do not duplicate them): `web/node_modules/`, `web/.next/`, `web/out/` (skip any of these three that a broader existing pattern in `.gitignore` — e.g. a bare `node_modules/` or `.next/` line — already covers).
  </action>
  <verify>
    <automated>cd web && npm install && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm install` exits 0.
    - `cd web && npm run build` exits 0 and produces a `web/out/` static export directory.
    - `web/package.json` `scripts` contains exactly `dev`, `build`, `prebuild`, `predev`, `test` with the values specified in `<action>`.
    - `web/src/app/globals.css` contains both `name: "vault-dark"` and `name: "vault-light"` inside `@plugin "daisyui/theme"` blocks, and no `tailwind.config.js` file exists anywhere in `web/` (DaisyUI 5 is CSS-first only — a JS config file would be silently ignored and is itself a sign of the wrong pattern).
    - `web/next.config.ts` sets `output: "export"`.
    - `web/src/app/layout.tsx` contains no `useEffect` call that sets `data-theme` (the pre-hydration `<script>` is the only mechanism).
  </acceptance_criteria>
  <done>`npm install` and `npm run build` both succeed; both themes are defined with exact UI-DESIGN.md tokens; fonts load via `next/font`; the theme is resolved pre-paint via inline script; the WASM build is wired into the npm lifecycle via `prebuild`/`predev`.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- **Files:** `web/package.json` (new), `web/next.config.ts` (new), `web/tsconfig.json` (new), `web/vitest.config.ts` (new), `web/src/app/globals.css` (new), `web/src/app/layout.tsx` (new), `web/src/app/page.tsx` (new — temporary placeholder, replaced by plan 01-03), `.gitignore` (edited)
- **npm scripts (web/package.json):** `dev` (`next dev`), `build` (`next build`), `prebuild` (`bash ../scripts/build-wasm.sh`), `predev` (`bash ../scripts/build-wasm.sh`), `test` (`vitest run`)
- **DaisyUI theme names (web/src/app/globals.css):** `vault-dark` (default), `vault-light`
- **CSS variables (web/src/app/layout.tsx via next/font):** `--font-sans` (DM Sans), `--font-hand` (Fuzzy Bubbles)
- **localStorage key:** `pv-theme` (theme persistence — read by the pre-hydration script this plan writes; also written by plan 01-03's Sidebar toggle)

These are all newly-created symbols this phase introduces — later drift verification should not flag them as undocumented.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry ↔ local install | Supply-chain trust for `next`/`tailwindcss`/`daisyui`/`lucide-react`, all flagged `[SUS]` on a recency heuristic by RESEARCH.md's Package Legitimacy Audit |
| Font loading ↔ third-party CDN | Risk of leaking visitor IPs to an external font host if fonts are not self-hosted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-02-SC | Tampering | `npm install` of `next`/`tailwindcss`/`daisyui`/`lucide-react` | high | mitigate | Task 1 blocking-human checkpoint verifies publisher/version/downloads on npmjs.com before install runs; per protocol this checkpoint is never auto-approvable regardless of `workflow.auto_advance` |
| T-02-01 | Information Disclosure | Font loading (`layout.tsx`) | low | mitigate | `next/font/google` self-hosts font files at build time (no runtime CDN request to Google), per CONTEXT.md's locked font-loading decision — no manual `<link>` to a Google Fonts CDN anywhere in the codebase |
| T-02-02 | Tampering | `globals.css` theme configuration surface | low | accept | Placing theme tokens in `tailwind.config.js` instead of the CSS-first `@plugin` block would silently render with wrong colors, not a security misconfiguration — cosmetic-only failure mode, caught immediately by the visual checkpoint in plan 01-03 |

</threat_model>

<verification>
`cd web && npm install && npm run build` must exit 0. Manually spot-check `web/src/app/globals.css` for both theme names and `web/next.config.ts` for `output: "export"`.
</verification>

<success_criteria>
- `web/` is a real, buildable Next.js 16 static-export app.
- Both `vault-dark` and `vault-light` themes are defined with exact tokens from docs/UI-DESIGN.md.
- Theme resolves before first paint; fonts load via `next/font`.
- `prebuild`/`predev` invoke `scripts/build-wasm.sh` from plan 01-01.
</success_criteria>

<output>
Create `.planning/phases/01-wasm-crypto-bridge-web-app-shell/01-02-SUMMARY.md` when done.
</output>
