---
phase: 04-prf-unlock-login-unification
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements: [AUTH-04, AUTH-09, UI-02]

must_haves:
  truths:
    - "The full workspace test/typecheck/build suite is green with Plan 04-01/04-02's changes merged, not just each plan's own scoped tests"
    - "A fresh-browser passkey login+unlock and a reload-triggered passkey unlock have each been exercised at least once against the REAL running server + REAL (or CDP-virtual) authenticator, not just mocked unit/integration tests — closing 04-RESEARCH.md's Validation Architecture phase-gate requirement"
  artifacts: []
  key_links: []
---

## Phase Goal

**As a** user with a PRF-capable enrolled passkey, **I want to** log in and unlock my vault with a single passkey gesture, **so that** I never have to also type my master password — and when PRF isn't available I still get a clear, working password fallback instead of a dead end.

(Same restatement as 04-01/04-02-PLAN.md. This plan verifies it, end to end, against the real stack — no new implementation.)

<objective>
Close 04-RESEARCH.md's Validation Architecture phase-gate: run the full cross-crate/cross-package regression suite once everything from Plans 04-01/04-02 is merged, then exercise the two real-browser flows automated tests structurally cannot cover — a genuine `navigator.credentials.get()` gesture completing a fresh-browser passkey login-and-unlock, and a reload-triggered passkey-only unlock — against the actual running `pv-server` + web app.

Purpose: this is the plan that turns "unit/integration tests pass" into "a real person can actually do the thing the roadmap promises," matching Phase 3's own end-of-phase manual-UAT convention (03-04-PLAN.md Task 4).
Output: a green full-suite run recorded in this plan's SUMMARY, plus a completed manual/self-validated browser walkthrough (screenshots for the morning-review-flagged taste items in 04-UI-SPEC.md) confirming AUTH-04/AUTH-09/UI-02's roadmap success criteria hold in a real browser, not just in mocks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prf-unlock-login-unification/04-CONTEXT.md
@.planning/phases/04-prf-unlock-login-unification/04-UI-SPEC.md
@.planning/phases/04-prf-unlock-login-unification/04-01-SUMMARY.md
@.planning/phases/04-prf-unlock-login-unification/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-workspace regression sweep</name>
  <files>none — verification only, no files modified</files>
  <read_first>
    - .planning/phases/04-prf-unlock-login-unification/04-01-SUMMARY.md, 04-02-SUMMARY.md — confirm every acceptance criterion each plan claimed was actually independently re-verified before this task starts (do not just trust the prose; re-run the commands).
  </read_first>
  <action>
    Run, in order, and confirm each is green before proceeding to the next: `cargo test --workspace` (pv-core + pv-server + pv-wasm, all unit + integration suites including Plan 04-01's new `passkey_login`/`unlock` test files), `cargo clippy --workspace --all-targets` (zero warnings), `npm --prefix web test` (whole vitest suite including Plan 04-02's new/modified test files), `npx --prefix web tsc --noEmit`, `npm --prefix web run build` (confirms the static export still builds cleanly with the new `PasskeyUnlockButton`/`login.ts`/`prfUnavailable.ts` in the tree — this is the same regression class Phase 1's WASM-boundary grep-audit exists to catch, run it too: `grep -rn "from \"\./wasm\|from '\.\/wasm" web/src --include="*.ts" --include="*.tsx" | grep -v "lib/crypto/index.ts"` must return zero lines, confirming `lib/crypto/` remains the sole importer of the generated WASM bindings even after this phase's new files).
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault && cargo test --workspace && cargo clippy --workspace --all-targets && npm --prefix web test && npx --prefix web tsc --noEmit && npm --prefix web run build</automated>
  </verify>
  <acceptance_criteria>
    - All five commands above exit 0.
    - The WASM-choke-point grep audit returns zero matches outside `lib/crypto/index.ts`.
    - No test file was skipped/marked `.skip`/`.todo` anywhere in the diff introduced by Plans 04-01/04-02 (grep the touched test files for `.skip(` / `.todo(` / `#[ignore]` — each must return zero).
  </acceptance_criteria>
  <done>Full workspace regression suite (cargo test/clippy, npm test/tsc/build) is green with Plans 04-01/04-02 merged, and the WASM choke-point grep audit still returns zero matches outside lib/crypto/index.ts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-browser passkey login + unlock UAT (closes 04-RESEARCH.md's manual-UAT validation row)</name>
  <files>none — manual/self-validated browser verification only, no source files modified by this task</files>
  <action>
    Drive the 9-step real-browser walkthrough in `&lt;how-to-verify&gt;` below against the actual running `pv-server` + web app (not mocks). Under the project's standing overnight authorization (self-validate via Playwright/browser automation + a test account, screenshots for taste calls), resolve this checkpoint by actually performing the walkthrough yourself — via Playwright MCP browser control plus Chrome DevTools Protocol's WebAuthn virtual-authenticator domain (`WebAuthn.enable` + `WebAuthn.addVirtualAuthenticator` with `hasResidentKey: true`, `hasUserVerification: true`, and `hasPrf: true` if the CDP version in use exposes it; otherwise proceed with a non-PRF virtual authenticator for steps 3-5 and rely on step 6's deliberate non-PRF-credential path to exercise the honest-fallback branch instead) — rather than blocking for a human. Attach screenshots from step 9 to this plan's SUMMARY.
  </action>
  <what-built>
    Two new server-side WebAuthn authentication ceremony pairs (unauthenticated `passkey-login/start|finish`, session-gated `unlock/start|finish`) and the client-side wiring that makes one `navigator.credentials.get()` gesture both log in AND unlock the vault — plus a three-tier honest fallback (no WebAuthn support / PRF unavailable / genuine ceremony failure) whenever PRF can't complete the unlock half.
  </what-built>
  <how-to-verify>
    1. Run the full stack locally (`cargo run -p pv-server` in one terminal, `npm --prefix web run dev` in another — or the project's existing dev-preview workflow if already configured).
    2. Register a fresh test account, log in with the master password, unlock the vault, open Settings → Passkeys, enroll a passkey using either a real platform authenticator (Touch ID/Windows Hello) or Chrome DevTools → More tools → WebAuthn ("Enable virtual authenticator environment", check "Supports resident keys" AND "Supports user verification"; enable a PRF-capable option if your Chrome version's panel exposes one).
    3. Log out (or open a fresh incognito/private window with no session token). Confirm the login screen shows, top to bottom: the email field, then a teal "Zaloguj i odblokuj passkeyem"/"Log in and unlock with passkey" button, then a divider, then the password field — the button sits BELOW email and ABOVE password (email must be enterable before the passkey ceremony can be scoped), matching UI-DESIGN.md Screen 1 / 04-UI-SPEC.md's locked screen-layout order.
    4. Enter the test account's email, click the passkey button, complete the browser's native/virtual prompt. Confirm: (a) you land directly on `UnlockOverlay`'s ONE-CLICK "Odblokuj"/"Unlock" state (not a password form), (b) clicking that one button unlocks the vault with NO password ever typed, (c) the network tab shows no PRF bytes / raw assertion / unwrapped key in any request body.
    5. Reload the page (session still valid, vault now locked again). Confirm `UnlockOverlay` shows the SAME teal passkey button (this time with `unlock.passkeyCta` copy, not the login variant) above the password field. Click it, complete the prompt, confirm the vault unlocks directly — no session/login round trip, confirmed by checking no NEW row appears in a `sessions` count if you have DB access, or simply that no re-login screen ever appeared.
    6. Force the AUTH-09 fallback: either use an authenticator/browser without PRF support, or (faster) enroll a SECOND passkey via `register/start`+`register/finish` only, deliberately skipping the `prf-wrap` step (a non-PRF-capable credential) — repeat step 4 selecting THAT credential in the browser's chooser. Confirm you land on the password form with the `unlock.prfUnavailableExplainer` copy visible and the password field auto-focused — NOT a generic error, NOT a silent hang.
    7. Spoof zero WebAuthn support (`DevTools Console: delete window.PublicKeyCredential` before the login screen mounts, or use a browser/profile that genuinely lacks it) — confirm the passkey button is ABSENT (not disabled) and the `unlock.passkeyUnsupported` explainer text renders in its place, with the password form still fully usable below.
    8. Cancel a passkey prompt mid-ceremony (click "Cancel" in the native/DevTools dialog) — confirm the button silently returns to idle with NO error banner of any kind.
    9. Capture screenshots of steps 3-8 for 04-UI-SPEC.md's flagged "Morning review notes" (LoginForm CTA wording, tier-1/2/3 copy, divider treatment) — these were autonomous overnight taste calls, not locked decisions, and are worth a final human glance even though functionally verified here.
  </how-to-verify>
  <resume-signal>Type "approved" once every step above works as described, or describe what broke (which step, what you saw instead). Per the project's standing overnight authorization (self-validate via Playwright/browser automation + a test account, screenshots for taste calls), this checkpoint may be resolved by the executing agent itself driving the flow (Playwright MCP + Chrome DevTools Protocol virtual authenticator) rather than blocking for Bartek, with screenshots attached to the plan SUMMARY for morning review of the flagged taste items only.</resume-signal>
  <verify>
    <human-check>All 9 steps in `&lt;how-to-verify&gt;` confirmed working against the real running stack; screenshots captured for step 9's flagged taste items.</human-check>
  </verify>
  <done>Fresh-browser passkey login+unlock and reload-triggered passkey unlock both work end-to-end in a real browser; all three AUTH-09 fallback tiers plus cancellation render correctly; no PRF bytes/raw assertion appear in any real network request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real browser ↔ real running server | The one boundary automated tests (SoftPasskey, jsdom+mocked WebAuthn) cannot fully exercise — genuine browser API behavior, genuine network round trips |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Information Disclosure | Network tab inspection during manual UAT | medium | mitigate | Step 4(c) explicitly requires confirming no PRF bytes/raw assertion/unwrapped key appear in any request body during the real-browser walkthrough — the one check automated tests can only prove at the unit-mock level, not against real browser serialization |
| T-04-SC | Tampering (supply chain) | none — no installs this plan | n/a | mitigate | No new dependencies; verification-only plan |
</threat_model>

<verification>
- `cargo test --workspace`, `cargo clippy --workspace --all-targets`, `npm --prefix web test`, `npx --prefix web tsc --noEmit`, `npm --prefix web run build` all green (Task 1).
- Manual/self-validated real-browser walkthrough of all 9 steps in Task 2 confirmed working, with screenshots for the flagged taste items (Task 2).
</verification>

<success_criteria>
- ROADMAP.md Phase 4's four success criteria are each independently confirmed true against a real running stack, not inferred from unit tests alone.
- Full regression suite green.
- Screenshots captured for 04-UI-SPEC.md's morning-review-flagged items.
</success_criteria>

<output>
Create `.planning/phases/04-prf-unlock-login-unification/04-03-SUMMARY.md` when done
</output>
