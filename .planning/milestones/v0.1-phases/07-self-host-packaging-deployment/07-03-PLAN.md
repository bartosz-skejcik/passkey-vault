---
phase: 07-self-host-packaging-deployment
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - deploy/nginx.conf.example
  - deploy/Caddyfile.example
  - docker-compose.proxy-test.yml
  - scripts/verify-container.sh
  - docs/SELF-HOSTING.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]

must_haves:
  truths:
    - "Both deploy/nginx.conf.example and deploy/Caddyfile.example forward the Upgrade/Connection headers (or Caddy's automatic equivalent) needed for /api/sync/ws's WebSocket upgrade to survive a real proxy round-trip, not just a direct-to-container connection"
    - "Both reference configs strip the live session bearer token from /api/sync/ws's logged query string in their own access-log output — closing Phase 5's WR-02 gap on the reverse-proxy side, which crates/pv-server/src/main.rs's make_span doc comment explicitly names as this phase's obligation"
    - "scripts/verify-container.sh is a real, scripted pass/fail gate — not a demonstration — that builds the image, fronts it with a dockerized nginx AND a dockerized Caddy using these exact example configs (nginx fronted with a script-generated throwaway self-signed cert at the paths the example references; Caddy fronted with a test-only tls-internal-forced derivative of the example, since a real ACME challenge is unreachable in a test environment), registers a throwaway account and logs in directly against pv-server to obtain a real, percent-encoded session token (since ws_handler validates the token via session::validate_token BEFORE upgrading, and an empty sessions table rejects any placeholder token with 401), and asserts HTTPS healthz reachability (curl -k + Host header), a completed WS upgrade handshake over TLS using that real token, and the absence of the token query param in each proxy's own access log"
  artifacts:
    - path: "deploy/nginx.conf.example"
      provides: "map $http_upgrade upgrade-connection idiom + a location-scoped log_format for /api/sync/ws that logs $uri (no query string) + a listen 443 ssl server block referencing ssl_certificate/ssl_certificate_key at conventional mount paths (real certs in production, a throwaway self-signed pair for local/test verification), commented and cross-referencing main.rs's WR-02 doc comment"
    - path: "deploy/Caddyfile.example"
      provides: "reverse_proxy with automatic WS upgrade + a log { format filter { fields { uri query { delete token } } } } block (validated against a real Caddy binary), automatic real-ACME TLS by default with a documented tls internal override point for local/test verification (applied by the test overlay, not baked into this shipped file), cross-referencing the same WR-02 comment"
    - path: "scripts/verify-container.sh"
      provides: "a single documented command that generates a throwaway self-signed cert for nginx and a tls-internal-forced test Caddyfile derivative, proving HTTPS healthz + WS handshake + access-log token redaction end-to-end behind both proxies, with trap-based cleanup on success and failure"
  key_links:
    - from: "deploy/nginx.conf.example / deploy/Caddyfile.example"
      to: "crates/pv-server/src/main.rs's make_span/WR-02 doc comment"
      via: "inline comments in both example configs cross-reference WR-02 by name, per this project's established forward/back-reference doc-comment convention"
    - from: "scripts/verify-container.sh"
      to: "docker-compose.yml (Plan 07-02) + docker-compose.proxy-test.yml (this plan)"
      via: "docker compose -f docker-compose.yml -f docker-compose.proxy-test.yml up, adding nginx-test/caddy-test services fronting the existing pv-server service — never a separate, divergent compose definition"
    - from: "scripts/verify-container.sh's self-signed cert generation + tls-internal Caddyfile derivative"
      to: "deploy/nginx.conf.example's ssl_certificate paths / deploy/Caddyfile.example's default real-ACME TLS"
      via: "the script generates test-only TLS material and a test-only Caddyfile copy so both shipped reference configs stay real-deployment-realistic (real certs, real ACME) while the scripted E2E gate can still actually boot and pass both proxies in a sandboxed test environment with no public DNS/port 80/443 reachability"
---

<objective>
Ship the tested reverse-proxy half of Phase 7: a real, version-validated `deploy/nginx.conf.example` and `deploy/Caddyfile.example` that both forward the sync WebSocket's upgrade handshake and both strip the live session token from their own access logs (closing Phase 5's WR-02 gap on the proxy side, which `main.rs`'s `make_span` doc comment explicitly obligates this phase to document); and a scripted, dockerized end-to-end check (`scripts/verify-container.sh` + a `docker-compose.proxy-test.yml` overlay) that proves both configs actually work against the real packaged image from Plan 07-02, rather than merely reading as plausible.

Per 07-CONTEXT.md Area 6 (locked): both nginx AND Caddy are in scope, not either-or — self-hosters split roughly evenly between them, and ROADMAP.md's Phase 7 success criterion #3 names both by example. Per 07-RESEARCH.md: nginx has no per-query-param log redaction primitive (the practical answer is dropping the whole query string for `/api/sync/ws`'s log lines); Caddy 2.5+'s `query { delete token }` log filter is a real, purpose-built mechanism for exactly this, but its exact Caddyfile nesting is MEDIUM confidence and must be checked against a real `caddy validate` run before shipping as "tested," not just "written" (Assumption A2). A full browser-driven WebAuthn ceremony (real passkey registration/login) cannot be scripted with `curl`/`websocat` alone — it needs a browser + authenticator — so this plan's automated script proves the HTTP/WS transport-and-logging contract end-to-end, and a documented manual Playwright-UAT step (appended to `docs/SELF-HOSTING.md`) covers the full passkey-ceremony verification per this project's established UAT convention.

Purpose: ROADMAP.md's Phase 7 success criterion #3 — passkey ceremonies and the sync WebSocket both survive a real reverse proxy, not just direct-to-container.
Output: `deploy/nginx.conf.example`, `deploy/Caddyfile.example`, `docker-compose.proxy-test.yml`, `scripts/verify-container.sh`, an appended verification section in `docs/SELF-HOSTING.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-self-host-packaging-deployment/07-CONTEXT.md
@.planning/phases/07-self-host-packaging-deployment/07-RESEARCH.md
@.planning/phases/07-self-host-packaging-deployment/07-PATTERNS.md
@.planning/phases/07-self-host-packaging-deployment/07-01-SUMMARY.md
@.planning/phases/07-self-host-packaging-deployment/07-02-SUMMARY.md
@.planning/phases/05-multi-device-sync/05-02-SUMMARY.md

@crates/pv-server/src/main.rs
@crates/pv-server/tests/sync.rs
@scripts/build-wasm.sh
@docker-compose.yml
@Dockerfile
@docs/SELF-HOSTING.md

<interfaces>
<!-- Verified this session via direct file reads / 07-RESEARCH.md's cited sources — use as ground truth. -->

`crates/pv-server/src/main.rs`'s `make_span`/WR-02 doc comment (already in your context above) is the direct
textual source obligating this phase's access-log redaction deliverable — quote its intent, not its Rust code,
in both example configs' comments.

`crates/pv-server/tests/sync.rs`'s `test_server` helper establishes why a WS handshake needs a REAL socket
(`oneshot()` cannot perform an HTTP Upgrade) — the same reasoning applies one layer up here: a reverse proxy
cannot forward a fake in-process request either, so `scripts/verify-container.sh` must drive a real WS client
against a real running proxy container.

07-RESEARCH.md Pattern 4 (nginx, `map $http_upgrade $connection_upgrade` + a `sync_ws_redacted` location-scoped
`log_format`) and Pattern 5 (Caddy, `reverse_proxy` automatic WS + `log { format filter { fields { uri query {
delete token } } } }`) are the complete worked starting templates for Tasks 1/2 below — Pattern 5's exact
directive nesting is explicitly flagged MEDIUM confidence (Assumption A2) and must be corrected against a real
`caddy validate` run if it differs.

Both `nginx:1-alpine` and `caddy:2` are official Docker Hub images suitable as `docker-compose.proxy-test.yml`
services — avoids requiring a host-level nginx/Caddy install for verification (07-RESEARCH.md's Environment
Availability recommendation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `deploy/nginx.conf.example` — WS upgrade + scoped access-log token redaction</name>
  <files>deploy/nginx.conf.example</files>
  <read_first>
    07-RESEARCH.md Pattern 4 (complete worked example — the template for this task),
    crates/pv-server/src/main.rs's `make_span`/WR-02 doc comment (the exact text this file's access-log comment must cross-reference by name)
  </read_first>
  <behavior>
    - The config validates as syntactically correct nginx (see `<verify>`) — including a real, loadable
      `ssl_certificate`/`ssl_certificate_key` pair, since `nginx -t` actually opens and parses the referenced
      cert files for a `listen ... ssl` block, not just the surrounding directive syntax; a `listen 443 ssl`
      block with no cert files present fails `nginx -t` outright (`cannot load certificate ... No such file or
      directory`) and the container refuses to start — this file must never ship in a state that hits that
    - `/api/sync/ws`'s `location` block forwards `Upgrade`/`Connection: upgrade` headers plus
      `proxy_http_version 1.1` — the load-bearing WS-upgrade triple
    - `/api/sync/ws`'s `location` block logs `$request_method $uri` (path only, no query string) via its own
      `access_log`/`log_format` pair — never the default `$request`/`$request_uri`, which would include the
      live `?token=...` session bearer token
    - Every OTHER location keeps nginx's normal full-URI access log (no redaction where it isn't needed)
    - Every non-obvious directive has an inline `#` comment explaining why (07-CONTEXT.md Area 6's explicit
      requirement)
  </behavior>
  <action>
    Create `deploy/nginx.conf.example` using 07-RESEARCH.md Pattern 4 as the base. Include: a
    `map $http_upgrade $connection_upgrade { default upgrade; '' close; }` block; a
    `log_format sync_ws_redacted '$remote_addr - [$time_local] "$request_method $uri" $status';` directive with a
    comment explicitly naming `crates/pv-server/src/main.rs`'s `make_span`/WR-02 doc comment as the reason this
    exists (mirroring this project's established forward/back-reference doc-comment convention); a comment
    explaining nginx has no per-query-param redaction primitive, so this scopes to dropping the WHOLE query
    string for this one route only (07-RESEARCH.md Pitfall 3) — noting that a self-hoster who later adds a
    second query param to this specific route must re-check this file; a `server { listen 443 ssl; ... }` block
    with a placeholder `server_name vault.example.com;`, `ssl_certificate /etc/nginx/certs/fullchain.pem;`, and
    `ssl_certificate_key /etc/nginx/certs/privkey.pem;` — this file stays a TLS-terminating reference config (it
    is the shipped, real-deployment example; a bare plaintext `listen 80` fallback is deliberately NOT the
    default here, only ever an explicitly-separate test variant per this task's fix) — with a comment stating:
    for a real deployment, mount certs issued by certbot/Let's Encrypt (or equivalent) at these exact paths, that
    is the operator's own responsibility; for a syntax-only check or local/CI verification with no real domain,
    generate a throwaway self-signed pair and mount it at the same paths, e.g.
    `openssl req -x509 -nodes -newkey rsa:2048 -keyout privkey.pem -out fullchain.pem -days 1 -subj
    '/CN=vault.example.com'` (exactly what this task's own `<verify>` step below does, and what Plan 07-03's
    Task 3 `scripts/verify-container.sh` does for the full E2E run); a
    `location /api/sync/ws { access_log ... sync_ws_redacted; proxy_pass http://pv-server:8620; proxy_http_version
    1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection $connection_upgrade;
    proxy_set_header Host $host; }` block; a `location / { proxy_pass http://pv-server:8620; proxy_set_header
    Host $host; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto
    $scheme; }` block. Use `pv-server` (Plan 07-02's compose service name) as the upstream hostname, with a
    comment noting to substitute a different reachable address if running nginx outside that same Docker network.
  </action>
  <verify>
    <automated>
    mkdir -p /tmp/pv-nginx-selfcheck-certs && \
    openssl req -x509 -nodes -newkey rsa:2048 -keyout /tmp/pv-nginx-selfcheck-certs/privkey.pem \
      -out /tmp/pv-nginx-selfcheck-certs/fullchain.pem -days 1 -subj '/CN=vault.example.com' 2>&1 | tail -20 && \
    docker run --rm \
      -v "$(pwd)/deploy/nginx.conf.example:/etc/nginx/conf.d/pv.conf:ro" \
      -v /tmp/pv-nginx-selfcheck-certs:/etc/nginx/certs:ro \
      nginx:1-alpine nginx -t 2>&1 | tail -40
    </automated>
    (adjust the mount path/wrapping `nginx.conf` as needed for `nginx -t` to see a syntactically complete
    top-level config — a bare `server {}` fragment alone is not one; wrap with a minimal enclosing `http {}` if
    the file itself isn't already validated as an `include`-able fragment against `nginx:1-alpine`'s default
    `nginx.conf`. The throwaway self-signed cert generated above is disposable local-verification tooling only —
    it is never committed to the repo, only `deploy/nginx.conf.example`'s reference to its conventional mount
    path is.)
  </verify>
  <acceptance_criteria>
    - `nginx -t` (dockerized, with a throwaway self-signed cert mounted at the paths this file references)
      reports the config as syntactically valid
    - File contains the `Upgrade`/`Connection: upgrade`/`proxy_http_version 1.1` triple scoped to `/api/sync/ws`
    - File contains a `log_format`/`access_log` pair dropping the query string for `/api/sync/ws` specifically,
      with an inline comment naming `main.rs`'s WR-02 doc comment
    - File references `ssl_certificate`/`ssl_certificate_key` at documented, conventional paths
      (`/etc/nginx/certs/fullchain.pem` / `/etc/nginx/certs/privkey.pem`) with a comment describing both the
      real-deployment (certbot/Let's Encrypt) and local/test (throwaway self-signed `openssl req -x509`) paths to
      populating them — no actual cert/key material is authored/committed in this file itself
  </acceptance_criteria>
  <done>A syntactically valid, TLS-terminating nginx reference config forwards WS upgrades and strips the session token from its own access log for the sync route, fully commented, and actually starts (via a self-signed cert for testing, real certs for production) rather than refusing to boot for want of a certificate.</done>
</task>

<task type="auto">
  <name>Task 2: `deploy/Caddyfile.example` — automatic WS + validated `query { delete token }` log filter</name>
  <files>deploy/Caddyfile.example</files>
  <read_first>
    07-RESEARCH.md Pattern 5 (starting template — explicitly flagged MEDIUM confidence on exact directive nesting, Assumption A2, must be corrected against a real `caddy validate` run),
    crates/pv-server/src/main.rs's `make_span`/WR-02 doc comment (same cross-reference obligation as Task 1)
  </read_first>
  <behavior>
    - `caddy validate` (dockerized, see `<verify>`) reports the file as syntactically valid — if Pattern 5's
      exact nesting fails validation, the corrected, actually-valid syntax is used instead and the correction is
      noted in this task's summary
    - WS upgrade for `/api/sync/ws` works with zero special directives (Caddy's `reverse_proxy` auto-detects the
      `Upgrade` header) — the file's own comment says so explicitly, contrasting with Task 1's nginx file's
      extra lines
    - The `log` block's query-filter strips the session token from `/api/sync/ws`'s logged URI
    - This shipped file has NO `tls internal` (or any other non-default `tls` directive) baked in — it relies on
      Caddy's real automatic-HTTPS/ACME default, matching a real self-hoster's actual production deployment; the
      `tls internal` override needed to make Task 3's sandboxed E2E script actually boot Caddy lives only in that
      task's generated test-only derivative file, never in this one
  </behavior>
  <action>
    Create `deploy/Caddyfile.example` starting from 07-RESEARCH.md Pattern 5. Before finalizing, run
    `docker run --rm -v "$(pwd)/deploy/Caddyfile.example:/etc/caddy/Caddyfile:ro" caddy:2 caddy validate --config
    /etc/caddy/Caddyfile --adapter caddyfile` — if this reveals different current directive nesting than
    Pattern 5's example (a real possibility per Assumption A2), use the corrected, actually-valid syntax and
    document the correction in this task's summary rather than shipping unvalidated prose. Content: a
    `vault.example.com { ... }` site block that ships with NO explicit `tls` directive — Caddy's automatic HTTPS
    (real ACME/Let's Encrypt against the real `vault.example.com` a self-hoster substitutes their own domain for)
    is the correct, intentional production default and this file must stay that way; add a comment explaining
    that explicitly, and stating that Plan 07-03's Task 3 test/CI overlay separately generates a `tls internal`-
    forced DERIVATIVE copy of this file for its own scripted E2E run (since a real ACME HTTP-01/TLS-ALPN
    challenge is unreachable from a sandboxed test environment with no public DNS or port 80/443 exposure) —
    this file itself is never modified to hardcode `tls internal`, since that would ship a production-facing
    reference config that silently never gets a real, browser-trusted certificate. `reverse_proxy pv-server:8620`
    with a comment explicitly contrasting its automatic WS handling against Task 1's nginx file's explicit header
    lines; a `log { output file /var/log/caddy/access.log; format filter { wrap console; fields { uri query {
    delete token } } } }` block (or the validated-correct equivalent), with a comment cross-referencing
    `main.rs`'s WR-02 doc comment by name, mirroring Task 1's nginx comment.
  </action>
  <verify>
    <automated>docker run --rm -v "$(pwd)/deploy/Caddyfile.example:/etc/caddy/Caddyfile:ro" caddy:2 caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `caddy validate` (dockerized) reports the file as valid — the shipped syntax is the actually-validated one,
      not just Pattern 5's as-researched draft
    - The `reverse_proxy` block has an inline comment contrasting its automatic WS handling against nginx's
      explicit header lines
    - The `log` block strips the `token` query param for the sync WS route, with an inline WR-02 cross-reference
      comment
    - `grep -n "tls internal" deploy/Caddyfile.example` reports no matches — this shipped file relies on real
      automatic HTTPS/ACME by default; `tls internal` is only ever injected into Task 3's separate, generated
      test-only derivative
  </acceptance_criteria>
  <done>A `caddy validate`-clean Caddy reference config forwards WS upgrades automatically, relies on real automatic-HTTPS/ACME by default (production-realistic, no test-only TLS override baked in), and strips the session token from its own access log for the sync route, fully commented.</done>
</task>

<task type="auto">
  <name>Task 3: Dockerized E2E smoke test — container behind both proxies, healthz + WS handshake + log redaction</name>
  <files>docker-compose.proxy-test.yml, scripts/verify-container.sh, docs/SELF-HOSTING.md</files>
  <read_first>
    07-RESEARCH.md's "Validation Architecture" table (no prior container-level test infra exists in this repo — this is genuinely new),
    07-RESEARCH.md's "Environment Availability" table (recommends dockerized `caddy:2`/`nginx:1-alpine` services over requiring host-level installs),
    crates/pv-server/tests/sync.rs's `test_server` helper (the WS-handshake-needs-a-real-socket rationale, applied one layer up to a real proxy here),
    docker-compose.yml (Plan 07-02 — this overlay extends it, never replaces it)
  </read_first>
  <behavior>
    - Running `bash scripts/verify-container.sh` from a clean checkout with only Docker installed: builds the
      image; generates a throwaway self-signed cert (`openssl req -x509 ...`) for nginx and a `tls internal`-
      forced test-only derivative of `deploy/Caddyfile.example` for Caddy (neither shipped example file is
      mutated — see `<action>` below for exactly why both are necessary for the containers to boot at all);
      starts `pv-server` plus BOTH an `nginx-test` and a `caddy-test` container (nginx-test mounting Task 1's
      real `deploy/nginx.conf.example` read-only plus the generated cert; caddy-test mounting the generated
      `tls internal` derivative, NOT the raw `deploy/Caddyfile.example`, for the reason above); registers a
      throwaway account and logs in directly against `pv-server`'s own exposed port to capture a REAL,
      percent-encoded session token — with an empty `sessions` table, `ws_handler`
      (`crates/pv-server/src/routes/sync.rs`) validates any bearer token via the same `session::validate_token`
      lookup every other endpoint uses BEFORE the WebSocket upgrade happens, so no placeholder/fabricated token
      can ever reach a completed upgrade; and asserts: `curl -k` with the placeholder `Host: vault.example.com`
      header against each proxy's OWN HTTPS port returns 200 for `/healthz` (both example configs terminate TLS —
      there is no plaintext port to curl against without contradicting Task 1/2's TLS-by-default configs); a WS
      client, presenting the real captured session token, completes the `wss://.../api/sync/ws` upgrade handshake
      through each proxy without an immediate connection reset; neither proxy's own access log contains the
      literal string `token=` after that WS request
    - The script exits non-zero and names which specific assertion failed if any step fails — a real gate, not a
      demonstration
    - Deliberately breaking one config (e.g. removing the `Upgrade` header line from a scratch copy of the nginx
      config) and re-running makes the script fail specifically on the WS-handshake assertion — proving the
      check is real, not vacuous (a one-time manual sanity check at implementation time, not a permanent
      regression fixture)
    - The script cleans up its own containers AND its own generated throwaway TLS material/Caddyfile derivative
      on both success and failure exit paths (via a `trap ... EXIT`)
    - `docs/SELF-HOSTING.md` gains a short section documenting the manual Playwright-UAT steps for verifying a
      FULL browser-driven passkey registration/PRF-unlock ceremony behind a real proxy — since an actual WebAuthn
      ceremony needs a browser + authenticator and cannot be scripted with `curl`/`websocat` alone, this is
      explicitly a separate, documented manual step, not something this task's automated script claims to cover
  </behavior>
  <action>
    Both Task 1's `deploy/nginx.conf.example` and Task 2's `deploy/Caddyfile.example` are, by design, real
    TLS-terminating reference configs (nginx's explicit `listen 443 ssl` + cert paths; Caddy's automatic real
    ACME HTTPS default) — neither ships a plaintext fallback, and this task must NOT invent one just to make
    scripting easier, since that would ship an untested, less-secure variant as the "reference." That means this
    task's own test harness has two concerns to solve before either proxy container will even start successfully
    in a sandboxed test environment with no public DNS or port 80/443 reachability: nginx needs SOME cert files
    to exist at the paths it references, and Caddy needs SOME way to get a certificate that doesn't require a
    real ACME challenge. Solve both WITHOUT modifying the shipped example files. Separately — and this is about
    the WS assertion passing, not about either proxy container booting — `ws_handler` validates its `?token=`
    query param against a real `sessions` row BEFORE upgrading, so the script also needs a real, freshly-created
    session token; a fresh container's `sessions` table starts empty, so this cannot be skipped or faked (see
    step 4 below):

    Write `scripts/verify-container.sh` (bash, `set -euo pipefail`, mirroring `scripts/build-wasm.sh`'s
    header-comment style) to, in order:
    1. Create a scratch directory (e.g. `mktemp -d`), and inside it: generate a throwaway self-signed TLS pair
       via `openssl req -x509 -nodes -newkey rsa:2048 -keyout privkey.pem -out fullchain.pem -days 1 -subj
       '/CN=vault.example.com'` — this is what gets mounted into `nginx-test` at the exact
       `/etc/nginx/certs/fullchain.pem` / `/etc/nginx/certs/privkey.pem` paths Task 1's file references, letting
       `nginx -t`/nginx itself actually load a certificate without the shipped example needing any change.
    2. In that same scratch directory, write a `Caddyfile.test` derived from `deploy/Caddyfile.example` via a
       plain `sed`/`awk` transform that inserts a `tls internal` line inside the `vault.example.com { ... }`
       block (e.g. `sed '/vault.example.com {/a\    tls internal' deploy/Caddyfile.example >
       "$scratch/Caddyfile.test"`) — mount THIS generated file into `caddy-test`, never the raw
       `deploy/Caddyfile.example`. Print a clear comment/echo in the script explaining this is a test-only
       transformation of the shipped file, not a divergent hand-maintained copy, and that production deployments
       use the shipped file verbatim (real ACME).
    3. Build the image, bring up the compose overlay (below), wait for `pv-server`'s own healthcheck via a
       bounded `curl` retry loop (not a blind `sleep`).
    4. Acquire a REAL session token BEFORE any WS assertion. `ws_handler`
       (`crates/pv-server/src/routes/sync.rs`) validates the `?token=` query param via
       `super::session::validate_token(&state.db, &auth.token)` BEFORE `ws.on_upgrade(...)` runs — with an empty
       `sessions` table (a fresh container has never seen a login), ANY placeholder/fabricated token this script
       might otherwise pass gets rejected with 401, and the upgrade can never be reached. There is no way to
       script a passing WS assertion without first creating a real session row. Register a throwaway account
       directly against `pv-server`'s own exposed port (`http://127.0.0.1:8620`, per `docker-compose.yml`'s
       `ports: ["8620:8620"]` — registration/login need no reverse proxy; only the WS assertions below do) via
       `POST /api/auth/register`, using EXACTLY the payload shape `crates/pv-server/src/routes/auth.rs`'s
       `RegisterRequest` requires (confirmed by direct read): `{"email": "<fresh random, e.g.
       verify-container-$RANDOM@example.invalid>", "kdf": {"m_cost_kib": 65536, "t_cost": 3, "p_cost": 4}, "salt":
       "<base64 of >=16 random bytes>", "auth_hash": "<base64 of EXACTLY 32 random bytes — auth.rs's
       EXPECTED_AUTH_HASH_LEN; any other length is rejected as BadRequest>", "pw_wrapped_uk": "{}"}`
       (`pw_wrapped_uk` is opaque JSON `RegisterRequest`'s own doc comment confirms the server never parses, so a
       placeholder string is sufficient for this transport-only test — do not invent a shortcut or skip straight
       to a fabricated token). Then call `POST /api/auth/login` with the SAME `email` and SAME `auth_hash`
       (base64) to receive `{"session_token": "...", "pw_wrapped_uk": "..."}`; capture `session_token`.
       Percent-encode this token before interpolating it into any `?token=` query string below — a base64-STANDARD
       token can contain `+`, `/`, `=`, all reserved/meaningful in a URL query string (e.g. via
       `python3 -c 'import urllib.parse,sys; print(urllib.parse.quote(sys.argv[1]))'` or `jq -rn --arg t "$TOKEN"
       '$t|@uri'` — pick whichever needs the fewest new tools, consistent with step 5's own tool-choice
       guidance). Both proxies' WS assertions reuse this SAME captured, percent-encoded token — one throwaway
       account is sufficient for both `nginx-test` and `caddy-test`.
    5. For EACH proxy (nginx-test, caddy-test): assert `curl -kf -H "Host: vault.example.com"
       https://127.0.0.1:<proxy's host port>/healthz` returns 200 (the `-k` is required and correct here — both
       proxies present a throwaway/self-signed or Caddy-internal-CA certificate for this LOCAL verification run,
       never the real cert a production deployment would have; this is a transport/config check, not a
       certificate-trust check); drive a real WS client (e.g. `websocat --insecure`, or an inline Python/Node
       one-liner with TLS verification disabled for this same throwaway-cert reason, using whatever's already
       available in the test images or a small dedicated test-client container — pick whichever needs the fewest
       new tools) through `wss://127.0.0.1:<port>/api/sync/ws?token=<step 4's percent-encoded real session
       token>` with the same `Host` override and asserts the upgrade completes (101 response / an open socket,
       not an immediate reset, and specifically NOT a 401 — a 401 here means step 4's token didn't make it
       through, not a proxy-config failure); reads that proxy's own access log
       (`docker compose ... exec nginx-test cat /var/log/nginx/access.log` / the Caddy equivalent file path) and
       asserts it does NOT contain the literal string `token=`.
    6. Install a `trap '... cleanup (containers down + scratch dir removed) ...' EXIT` at the top so containers
       AND the generated scratch cert/Caddyfile are torn down whether the script succeeds or fails partway
       through. Print a clear PASS/FAIL line per assertion, and a final overall summary.

    Write `docker-compose.proxy-test.yml` as a compose OVERLAY (used via
    `docker compose -f docker-compose.yml -f docker-compose.proxy-test.yml up`, never replacing Plan 07-02's
    file) adding two services: `nginx-test` (`image: nginx:1-alpine`, mounting `deploy/nginx.conf.example`
    read-only PLUS a certs directory read-only at `/etc/nginx/certs` — parameterized via a compose variable/
    bind-mount path the script sets to its scratch directory, e.g. `${PV_TEST_CERTS_DIR}:/etc/nginx/certs:ro` —
    `depends_on: [pv-server]`, host port e.g. `8621:443`) and `caddy-test` (`image: caddy:2`, mounting a
    Caddyfile path parameterized the same way, e.g. `${PV_TEST_CADDYFILE}:/etc/caddy/Caddyfile:ro` — the script
    points this at its generated `Caddyfile.test`, never `deploy/Caddyfile.example` directly — `depends_on:
    [pv-server]`, host port e.g. `8622:443`), both proxying to the `pv-server` service's internal `8620`. Document
    in the compose file's comments that both example configs use a placeholder site address
    (`vault.example.com`), hence the `Host` header override approach for local testing (e.g.
    `curl -k -H "Host: vault.example.com" https://127.0.0.1:8621/healthz`) rather than rewriting the shipped
    example files — and that both TLS-related mount paths above are script-generated, disposable, test-only
    material, never committed.

    Append a **"Weryfikacja end-to-end za reverse proxy"** section to `docs/SELF-HOSTING.md` documenting: (a) the
    automated `bash scripts/verify-container.sh` command as the transport/logging-layer check, noting it exercises
    HTTPS via a throwaway self-signed cert (nginx) / Caddy's `tls internal` (Caddy), and that production
    deployments provide their own real certs/real ACME per the shipped example files' own comments; (b) a manual
    Playwright-UAT walkthrough for the full passkey ceremony — bring up `docker compose -f docker-compose.yml -f
    docker-compose.proxy-test.yml up`, open the proxied URL in a real browser, complete passkey enrollment + PRF
    unlock, confirm the sync WebSocket reconnects after a network blip — per this project's established
    Playwright-UAT convention (self-validate via Playwright + a disposable test account; screenshots for any
    taste/UX call, not required here since this is a functional check).
  </action>
  <verify>
    <automated>bash scripts/verify-container.sh 2>&1 | tail -150</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-container.sh` exits 0 with every assertion reported PASS against a correctly built
      image + both correctly configured proxies, each reached over HTTPS (`curl -k` + `Host` header), never over
      plaintext HTTP against the TLS port
    - The script registers a throwaway account and logs in directly against `pv-server`'s own exposed port
      (`POST /api/auth/register` then `POST /api/auth/login`, matching `auth.rs`'s exact request/response
      shapes) to obtain a REAL, percent-encoded session token BEFORE either proxy's WS assertion runs — an empty
      `sessions` table means `ws_handler`'s pre-upgrade `validate_token` check (`crates/pv-server/src/routes/
      sync.rs`) rejects any placeholder token with 401 unconditionally, so a real token is a precondition for the
      WS-handshake assertion to be capable of passing at all, on either proxy
    - A deliberate one-time sanity break (removing the WS-upgrade header line from a scratch copy of one config),
      run AFTER a real session token is already in hand, makes the script fail specifically on the WS-handshake
      assertion for that proxy (not a 401 from a missing/invalid token, and not a false PASS) — proving the
      break-check exercises proxy behavior, not auth
    - Neither proxy's access log contains the literal string `token=` after the script's WS assertion runs
    - The script's containers are torn down (verified via `docker ps` showing none of `pv-server`/`nginx-test`/
      `caddy-test` running) after both a successful run and a deliberately-failed run, and its generated scratch
      cert/Caddyfile derivative directory no longer exists on disk afterward
    - `deploy/nginx.conf.example` and `deploy/Caddyfile.example` themselves are byte-for-byte untouched by this
      task (`git diff --stat deploy/` shows no changes from Task 1/2's committed versions) — only script-
      generated, gitignored/scratch material bridges the gap between "real TLS reference config" and "bootable in
      a sandboxed test run"
    - `docs/SELF-HOSTING.md` documents both the scripted check and the manual Playwright-UAT passkey-ceremony
      walkthrough
  </acceptance_criteria>
  <done>A single documented command proves the sync WebSocket and healthz both survive being fronted by a real, dockerized nginx AND a real, dockerized Caddy — each reached over actual HTTPS using the exact shipped TLS-terminating reference configs (bridged to a bootable test run via a script-generated throwaway cert and a script-generated tls-internal Caddyfile derivative, never by weakening the shipped configs themselves) — with access-log token redaction verified rather than merely asserted in prose, and a documented manual path exists for verifying the full browser-driven passkey ceremony behind the same proxies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Reverse-proxy access logs (nginx/Caddy, outside `pv-server`'s own process/log sink) | Persist whatever the proxy is configured to log, independent of and after `main.rs`'s own `make_span` redaction — a proxy-side gap is invisible to any server-side control |
| `scripts/verify-container.sh`'s deliberately-broken-config sanity check | A throwaway, manually-triggered negative test — must never be left wired into a real CI path in a way that could accidentally ship a broken config as "validated" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-07-07 | Information Disclosure | `deploy/nginx.conf.example` / `deploy/Caddyfile.example` access-log configuration | high | mitigate | Both reference configs strip or drop the query string for `/api/sync/ws` specifically; `scripts/verify-container.sh` asserts the absence of `token=` in each proxy's own access log after a real WS request, not just a prose claim |
| T-07-08 | Denial of Service | Reverse-proxy config silently missing WS-upgrade headers | high | mitigate | `scripts/verify-container.sh`'s WS-handshake assertion catches a missing/incorrect `Upgrade`/`Connection` header pair at doc-authoring/verification time, rather than a self-hoster's real users discovering broken sync reconnects in production |
| T-07-09 | Spoofing / Tampering | A self-hoster copies the example `server_name`/site address (`vault.example.com`) verbatim into a real deployment without customizing `PV_RP_ID`/`PV_ORIGIN` | low | accept | Plan 07-01's `Config::validate()` already rejects an incoherent `PV_RP_ID`/`PV_ORIGIN` pair regardless of what the reverse-proxy config's `server_name` says — the proxy-level placeholder is cosmetic, the actual enforcement point is server-side and already covered |

</threat_model>

<verification>
`docker run --rm -v "$(pwd)/deploy/nginx.conf.example:..." -v <throwaway-self-signed-cert-dir>:/etc/nginx/certs:ro
nginx:1-alpine nginx -t` reports valid (a real, loadable cert must be mounted — `listen 443 ssl` fails `nginx -t`
without one). `docker run --rm -v "$(pwd)/deploy/Caddyfile.example:..." caddy:2 caddy validate --adapter
caddyfile` reports valid. `bash scripts/verify-container.sh 2>&1 | tail -150` exits 0 with every assertion PASS,
reaching both proxies over real HTTPS (`curl -k` + `Host` header against nginx's script-generated self-signed
cert and Caddy's `tls internal`-forced test derivative — never plaintext HTTP against either TLS port). Manual: a
deliberate one-time WS-header removal makes the script fail specifically on the WS assertion (documented in this
plan's summary, not a permanent regression fixture).
</verification>

<success_criteria>
Both a real, dockerized nginx and a real, dockerized Caddy — using this phase's exact, unmodified, TLS-terminating
reference configs (bridged to a bootable sandboxed test run via script-generated, disposable TLS material —
never by weakening the shipped configs to plaintext) — forward the sync WebSocket's upgrade handshake over HTTPS
and never log the live session token in their own access logs, verified by a single scripted command rather than
asserted in prose; a documented manual path exists for verifying the full browser-driven passkey ceremony behind
the same proxies (ROADMAP.md Phase 7 success criterion #3, in full).
</success_criteria>

<output>
Create `.planning/phases/07-self-host-packaging-deployment/07-03-SUMMARY.md` when done
</output>
