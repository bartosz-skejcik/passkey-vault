---
phase: 27-extension-integration-shared-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/e2e/fixtures.ts
  - extension/e2e/two-context-spike.spec.ts
autonomous: true
requirements: [EXT-07]

must_haves:
  truths:
    - "Two chromium.launchPersistentContext(\"\", ...) calls issued in the SAME worker process produce two genuinely independent browser profiles — a value written to context A's extension storage is NOT visible from context B's extension instance."
    - "extContextB/extensionIdB worker-scoped fixtures exist in extension/e2e/fixtures.ts with the identical launch-arg/headed-by-project-name shape as the existing extContext/extensionId pair, ready for later plans to import without re-deriving the launch logic."
  artifacts:
    - extension/e2e/two-context-spike.spec.ts
  key_links:
    - "extContextB's launchPersistentContext call -> same EXTENSION_PATH/headed-detection logic as extContext, never a second, drifted copy"
</must_haves>
---

<objective>
Close Open Question 2 from 27-RESEARCH.md before any plan builds the real two-extension live proof on top of an unverified assumption: prove that two `chromium.launchPersistentContext("", ...)` calls, issued in the same Playwright worker process, produce genuinely independent Chromium profiles (not a collision on the empty user-data-dir argument), and land the reusable `extContextB`/`extensionIdB` worker fixtures every later live-proof plan (27-04, 27-05, 27-06, 27-11) needs.

Purpose: this phase's own inherited obligation (STATE.md, Phase 26 blocker) is that the live two-member proof must steer the phase, not audit it at the end — but the two-extension harness itself is genuinely new infrastructure (27-RESEARCH.md Pitfall 5), not a reuse of Phase 23's two-web-context `twoSessions` fixture. Spiking the infrastructure question FIRST, cheaply, before any product-behavior test depends on it, is what lets the Wave 2 tracer's live proof be trustworthy rather than a house built on an unverified Playwright assumption.
Output: a permanent regression spec proving profile independence, plus `extContextB`/`extensionIdB` fixtures ready for reuse.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-extension-integration-shared-items/27-CONTEXT.md
@.planning/phases/27-extension-integration-shared-items/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike — prove two persistent-context profiles are genuinely independent</name>
  <files>extension/e2e/two-context-spike.spec.ts</files>
  <read_first>
    extension/e2e/fixtures.ts (full file — the existing single-extension `extContext`/`extensionId` worker fixture pattern, `launchPersistentContext("", {...})` with `args: ["--disable-extensions-except=...", "--load-extension=..."]`, headed-by-project-name detection via `workerInfo.project.name.includes("ceremony")`)
    extension/playwright.config.ts (full file — `workers: 1`, `fullyParallel: false`, the `chromium`/`chromium-ceremony` project split; a single worker CAN still launch two persistent contexts within one test file, this config does not forbid it)
  </read_first>
  <action>
    Write a new spec file that, INSIDE a single test (not via the shared worker fixtures — this is the throwaway-shaped verification, run once, kept as a permanent cheap regression), calls `chromium.launchPersistentContext("", {channel: "chromium", headless: true, args: ["--disable-extensions-except=<EXTENSION_PATH>", "--load-extension=<EXTENSION_PATH>"]})` TWICE in the same test body, resolving `EXTENSION_PATH` and the headless/args shape identically to `fixtures.ts`'s own `extContext` definition (do not diverge from that shape — a divergent spike proves nothing about the fixture it is meant to validate). Assert three independent facts: (1) each `launchPersistentContext` call resolves without throwing and returns a context whose `serviceWorkers()` (after waiting for the `serviceworker` event, mirroring `fixtures.ts`'s own `extensionId` resolution) exposes a `chrome-extension://<id>` URL; (2) the two contexts' extension ids (the `host` of that URL) are DIFFERENT strings, even though both loaded the identical `EXTENSION_PATH` build — proving two distinct profile-scoped extension instances, not one shared instance; (3) writing a literal marker value into context A's extension background via `browser.storage.local.set` (open a page against `chrome-extension://<idA>/popup.html` or dispatch through the service worker's own evaluate, whichever this codebase's existing spikes already use as the simplest path) and then reading `browser.storage.local.get` from context B's own extension instance returns undefined/absent for that key — proving the two profiles do not share `chrome.storage.local`, which is the concrete isolation property every later live-proof plan's "member A" vs "member B" separation depends on. Close both contexts in a `finally`. Do not import or extend `fixtures.ts`'s `test`/`expect` for this one spec — plain `@playwright/test`'s `test`/`expect` is correct here since this spec deliberately does NOT use the worker-scoped single-context fixture it is validating.
  </action>
  <verify>
    <automated>cd extension && npm run pretest:e2e:chrome && npx playwright test --project=chromium e2e/two-context-spike.spec.ts</automated>
  </verify>
  <done>Two distinct extension ids are observed and the storage-isolation assertion demonstrates presence-then-absence, not a vacuous "no throw" pass.</done>
  <acceptance_criteria>
    `cd extension && npm run pretest:e2e:chrome && npx playwright test --project=chromium e2e/two-context-spike.spec.ts` passes, and its output shows two distinct extension ids and a storage-isolation assertion that failed BEFORE the fix would be meaningful (i.e. asserts presence-then-absence, not merely "no throw" — a vacuous pass per 27-RESEARCH.md Pitfall 2 is not acceptable evidence here).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Land the reusable extContextB/extensionIdB worker fixtures</name>
  <files>extension/e2e/fixtures.ts</files>
  <read_first>
    extension/e2e/fixtures.ts (full file, already read for Task 1 — the exact `ExtWorkerFixtures` interface, the `ExtendFn`/`as unknown as Parameters<ExtendFn>[0]` typing workaround this project's pinned `@playwright/test@1.61.1` requires, and the ordering where `extensionId` depends on `extContext` via its own fixture-function parameter)
  </read_first>
  <action>
    Extend `ExtWorkerFixtures` with `extContextB: BrowserContext` and `extensionIdB: string`, and add matching worker-scoped fixture entries to the `base.extend<...>({...})` call — byte-identical launch shape to the existing `extContext`/`extensionId` pair (same `EXTENSION_PATH`, same `headed`-by-`workerInfo.project.name` detection, same `serviceWorkers()`/`waitForEvent("serviceworker")` resolution for the id), proven safe to launch concurrently by Task 1's spike. Keep the same `as unknown as Parameters<ExtendFn>[0]` typing workaround for the new entries. Do not touch `extensionId`'s existing implementation — `extensionIdB` is a parallel sibling depending on `extContextB`, not a refactor of the existing pair into a shared helper (a shared helper is a legitimate follow-up but out of this plan's file scope; keep the diff additive and mirror-shaped so a future reader can diff the two pairs line-for-line).
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit</automated>
  </verify>
  <done>extContextB/extensionIdB exist as worker-scoped fixtures with the identical launch shape as extContext/extensionId, and the project typechecks clean.</done>
  <acceptance_criteria>
    `cd extension && npx tsc --noEmit` passes with `extContextB`/`extensionIdB` present in `fixtures.ts`'s exported `test`'s fixture types; a throwaway one-line test file importing `{ test } from "./fixtures"` and destructuring `{ extContext, extContextB, extensionId, extensionIdB }` in a test signature type-checks (delete the throwaway file after confirming — the permanent consumer is 27-04's live-proof spec, not this plan).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test process -> two Chromium profiles | Test-infrastructure-only boundary; no untrusted input crosses it (this plan's whole scope is a local, non-production test harness) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Information Disclosure | extContextB/extensionIdB fixture | low | accept | Test-only infrastructure, never shipped in the extension build; no production code path reads these fixtures |
| T-27-02 | Tampering | Two persistent-context profiles sharing state unexpectedly | medium | mitigate | Task 1's storage-isolation assertion is the direct mitigation — it fails loud (not silently) if the two profiles turn out to share `chrome.storage.local`, which would corrupt every later live-proof plan's "member A vs member B" separation before those plans even start |

</threat_model>

<verification>
Both Task 1's spike spec and Task 2's fixture typecheck pass. `npm --prefix extension test` (unmodified vitest suite) stays green — this plan touches no production code.
</verification>

<success_criteria>
Two-extension harness feasibility is proven by a real (not assumed) Playwright run, and `extContextB`/`extensionIdB` are ready for 27-04 onward to consume without re-deriving the launch logic.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `extension/e2e/two-context-spike.spec.ts` (NEW) — permanent regression proving two-persistent-context profile independence.
- `extension/e2e/fixtures.ts` — gains `extContextB`/`extensionIdB` worker-scoped fixtures (exported via the existing `test`/`expect`).

<output>
Create `.planning/phases/27-extension-integration-shared-items/27-01-SUMMARY.md` when done
</output>
