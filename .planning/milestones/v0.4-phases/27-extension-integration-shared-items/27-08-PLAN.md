---
phase: 27-extension-integration-shared-items
plan: 08
type: execute
wave: 3
depends_on: ["27-04"]
files_modified:
  - extension/lib/i18n/dictionary.ts
  - extension/entrypoints/popup/SharedBadge.tsx
  - extension/entrypoints/popup/ItemListView.tsx
  - extension/entrypoints/popup/ItemDetailView.tsx
  - extension/entrypoints/popup/ItemListView.test.tsx
  - extension/entrypoints/popup/ItemDetailView.test.tsx
autonomous: true
requirements: [EXT-07, EXT-12]

must_haves:
  truths:
    - "A personal row's rendering is byte-identical before and after this phase — the shared badge and folder subtitle are additive to shared rows only, never a change to the personal row shape."
    - "An account with zero shared items renders the popup list exactly as it does today, with no shared-specific empty state and no 'Shared' heading."
    - "In the 'Wszystkie' list, shared rows are ordered by the existing sort key with no ownership bias (UX-2)."
    - "A shared item whose optional fields are unpopulated renders exactly as a personal one does today — existing per-field empty handling, unchanged. This phase adds no new empty state to the detail view."
    - "There is deliberately no in-panel load state in ItemDetailView: the popup can only navigate into an already-decrypted row, because pending rows are non-interactive by construction."
    - "Hidden-password field: reveal (Eye/EyeOff) and copy are omitted entirely (not merely disabled); the existing 10-dot MASK still renders, and the honesty note renders directly beneath it on every render, not just the first. Read-only vs. full-edit differ in nothing, honestly: ItemDetailView has no edit affordance for any item, personal or shared."
    - "A directly-shared item (no collectionId) renders nothing in the folder-note slot — no invented placeholder. The header badge already carries the shared fact."
    - "A very long folder name wraps to a second line inside the note rather than clipping."
    - "Item decrypted but folder name not yet resolved falls back to the item's existing per-type subtitle (username/issuer/type label) — never a blank string, never a raw collection UUID."
    - "Reuses the row's existing truncate on the subtitle span; no new CSS, no new scroll region — the D-14 single-scroll-region invariant is untouched."
    - "share.itemSharedOnCollectionNote sits in ordinary wrapping body text (no truncate), matching web's treatment of the same string."
    - statement: "A permanently-undecryptable shared row (collections-store has refreshed, the Collection Key is still unresolvable, and a previously-decrypted copy exists to anchor a visible row to) gets a distinguishable degraded treatment at list level, never rendered identically to a healthy row. A row that has NEVER once decrypted (no retained copy, no identifying plaintext at all) is dropped — this is stated here as an explicit decision, not inherited from BUG-3's default, because there is no name or content left to anchor a visible row to."
      verification: backstop
    - statement: "Icon slot renders a fixed-footprint skeleton (matching ItemIconTile's row frame so there is no layout jump on resolve) still carrying the shared badge; name/subtitle render two stacked skeleton bars; the row is non-interactive (no onClick, not a button); role=\"status\" plus the loading aria label carries the sole screen-reader announcement; neutral shimmer only, never alert-warning styling."
      verification: backstop
    - statement: "A permanently-undecryptable shared row reuses the exact alert alert-warning shape and sync.itemUndecryptableWarning copy web already ships, wired as defense-in-depth even though no live path in the extension reaches it today outside a genuine post-refresh decrypt failure — and it must never fire for the ordinary MV3-wake pending window."
      verification: backstop
  prohibitions:
    - "The extension must not silently drop a shared item a user has access to that WAS ever successfully decrypted — a retained undecryptable copy always renders with a degraded badge, never identically to a healthy row and never simply absent."
    - "sharing.sharedItemLabel must stay direction-neutral ('Shared item,' never 'Shared by you'/'Shared with you') — the popup has no per-row data path to reliably compute share direction, and a wrong claim would repeat the exact CR-02-class lie Phase 26 already found and fixed once."
    - "share.hiddenPasswordExtensionNote must never be shortened to drop any of its three claims: copy is suppressed, autofill still works, the recipient holds the key regardless."
  artifacts:
    - extension/entrypoints/popup/SharedBadge.tsx
  key_links:
    - "vault.list's new collections field (27-04) -> ItemListView/ItemDetailView's folder-name lookup, the ONLY route to a decrypted collection name a popup component may use (never a direct background/WASM import, D-05)"
</must_haves>
---

<objective>
Discharge Phase 26's carried-forward narrow-viewport obligation (D-3) and this phase's own EXT-12/UX-1/UX-2/UX-4/A-7 decisions: a shared-item corner badge reused at every icon-frame host, `ItemListView.tsx`'s folder-name subtitle and pending-decrypt skeleton row, and `ItemDetailView.tsx`'s hidden-password masking + honesty note + shared-folder note + undecryptable banner — all per 27-UI-SPEC.md's approved, checker-signed-off contract.

Task 3 implements A-7 (hidden-password enforcement is client-side interface masking only, labelled as such — never a server-side pretence).

Purpose: this is the popup's read-side visual contract for shared items. Every truth here traces to a specific UI-SPEC row (E1/E2/E3) already resolved as covered or backstop — this plan's job is to wire the evidence the backstops still require, not merely render the happy path.
Output: the reusable `SharedBadge` component, the full i18n dictionary pass, `ItemListView.tsx`'s E1/E2 treatment, `ItemDetailView.tsx`'s E3 treatment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-extension-integration-shared-items/27-CONTEXT.md
@.planning/phases/27-extension-integration-shared-items/27-UI-SPEC.md
@.planning/phases/27-extension-integration-shared-items/27-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n dictionary pass + the shared SharedBadge component</name>
  <files>extension/lib/i18n/dictionary.ts, extension/entrypoints/popup/SharedBadge.tsx</files>
  <read_first>
    extension/lib/i18n/dictionary.ts (full header + structure, already read — `DICTIONARY` spreads `COMMON_DICTIONARY` then extension-only entries; no plural machinery)
    27-UI-SPEC.md's `## Copywriting Contract` table (the full, exact PL/EN strings for `sharing.sharedItemLabel`, `sharing.sharedItemLoadingAria`, `share.itemSharedOnCollectionNote` (ported verbatim from web), `sync.itemUndecryptableWarning` (ported verbatim from web), `provider.sharedPasskeyFolderNote`, `provider.sharedPasskeyNote`, `share.hiddenPasswordExtensionNote` (its own new, NOT-web-verbatim string per the UI-SPEC's explicit rationale) — and the four "Copywriting honesty constraints" beneath it)
    web/src/lib/i18n/dictionary.ts (grep for `share.itemSharedOnCollectionNote`/`sync.itemUndecryptableWarning` to confirm the EXACT byte-identical PL/EN values this task ports, not a paraphrase)
    27-UI-SPEC.md's "Badge geometry" and "Component inventory" sections (12px diameter, `-bottom-1 -right-1` offset, `text-secondary` `Users` glyph, `ring-1 ring-base-100`, one constant size reused across every host — `ItemIconTile.tsx` itself is NOT modified, the badge wraps it externally)
  </read_first>
  <action>
    Add all 6 new keys plus the 2 verbatim-ported keys to `DICTIONARY` in `dictionary.ts`, byte-identical to the UI-SPEC's Copywriting Contract table — do not paraphrase, shorten, or reorder any clause, especially `share.hiddenPasswordExtensionNote`'s three-claim sentence (constraint 1) and `sharing.sharedItemLabel`'s direction-neutral wording (constraint 4). Create `SharedBadge.tsx`: a small component accepting `{ size?: "row" | "detail" }` (defaulting to the 12px/-bottom-1/-right-1 row geometry; the `detail` variant is the SAME 12px/-4px pair applied to `ItemDetailView`'s smaller `h-6 w-6` header tile per the UI-SPEC's "one constant badge size across every host" doctrine — do not scale the badge itself, only its host frame differs), rendering the `Users` lucide-react glyph inside a `rounded-full bg-base-100 ring-1 ring-base-100` circle, `role="img"` with `aria-label`/`title` both set to `t(locale, "sharing.sharedItemLabel")`. This component is the ONLY place the badge markup exists — 27-09/27-10 import it, never re-derive the wrapper JSX independently.
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit</automated>
  </verify>
  <done>All 6 new i18n keys plus the 2 verbatim-ported keys exist byte-identical to the UI-SPEC, and SharedBadge renders the correct direction-neutral aria-label in both locales.</done>
  <acceptance_criteria>
    `npx tsc --noEmit` clean in `extension/`; a quick render smoke test (Task 2/3's own component tests exercising `SharedBadge` transitively) confirms the badge's `aria-label` text matches `sharing.sharedItemLabel` exactly for both locales.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ItemListView.tsx — E1 badge/subtitle + E2 pending-decrypt skeleton + E1-error degraded row</name>
  <files>extension/entrypoints/popup/ItemListView.tsx, extension/entrypoints/popup/ItemListView.test.tsx</files>
  <read_first>
    extension/entrypoints/popup/ItemListView.tsx (full file, already read — the `restResults.map((item) => {...})` row-rendering block at lines 386-414, the `ItemIconTile` call site, the `items.length === 0` empty-state gate, `refetchItems()`'s `sendMessage({kind:"vault.list"})` call whose response now carries `pending`/`collections` per 27-04)
    27-UI-SPEC.md's E1 and E2 tables in full (`## UI Considerations`, sections "E1" and "E2") — every row's exact resolution text, including the E1-error backstop's own framing
    packages/pv-ui/vault/types.ts lines 126-221 (`VaultItem.isShared`/`.collectionId`/`.undecryptable`, already declared)
    extension/entrypoints/popup/autofill/OnThisPageSection.tsx lines 163-164 (the `skeleton h-12 w-full rounded-box` DaisyUI precedent this plan's pending row borrows the shimmer primitive from — cited by the UI-SPEC's own Design System table)
  </read_first>
  <action>
    Wrap `&lt;ItemIconTile item={item} /&gt;` in each "Wszystkie" row with a `relative inline-flex` container and, when `item.isShared === true`, render `&lt;SharedBadge /&gt;` from Task 1 (row variant). Branch the existing `subtitle` computation: for a shared row (`item.isShared === true &amp;&amp; item.collectionId != null`), look up the collection's name from `vault.list`'s new `collections` field (matched by `item.collectionId`) and use it as the subtitle in the SAME slot/size/color (`text-sm text-base-content/60`); if not found (not yet resolved), fall back to the EXISTING per-type subtitle computation unchanged (never a blank string, never the raw `collectionId` UUID). For a genuinely-broken row (`item.undecryptable === true`), render a small distinguishable degraded treatment INSTEAD of (or alongside) the shared badge — an `AlertTriangle` icon in the same badge-slot geometry, muted/warning-toned per the UI-SPEC's Color table's genuine-failure semantics (never the neutral shimmer) — the row stays clickable (navigating into it shows Task 3's E3 undecryptable banner in the detail view, the natural place for the full explanation). State explicitly, in a code comment at this exact spot, that a row which has NEVER successfully decrypted (no retained copy exists in `items` at all — nothing for this loop to even iterate over) is NOT rendered here; this is a deliberate, documented decision (there is no name or any plaintext to anchor a visible row to), distinct from the retained-undecryptable case above, which DOES render with the degraded treatment because it has a real prior name.

    Below the existing `restResults` rendering, append the pending-decrypt skeleton rows: read `pending` from the same `vault.list` response state already held by this component (extend the existing `items`/local state shape minimally — a sibling `pending` state array, set alongside `items` in `refetchItems()`), and render one non-interactive (`&lt;div&gt;`, not `&lt;button&gt;`) skeleton row per pending entry, sorted to the END of the "Wszystkie" section (never interleaved with resolved rows, so the resolved portion never visually reflows as pending rows resolve): `&lt;SharedBadge /&gt;`-wrapped `skeleton h-8 w-8 rounded-[8px]` icon slot (matching `ItemIconTile`'s row footprint) plus two stacked `skeleton` bars for name/subtitle, `role="status"` with `aria-label={t(locale, "sharing.sharedItemLoadingAria")}` and no visible text (mirrors `OnThisPageSection`'s own text-free skeleton precedent). Hide pending rows entirely while `trimmedQuery !== ""` (an active search cannot meaningfully match an item whose plaintext isn't known yet — a documented Claude's-discretion choice, not a UI-SPEC-mandated one; once a query is cleared, pending rows that resolved while searching simply reappear via the existing `vault.updated` broadcast → `refetchItems()` cycle). Never render `alert-warning`/any error styling on a pending row (Copywriting honesty constraint 2).
  </action>
  <verify>
    <automated>npm --prefix extension run test -- ItemListView</automated>
  </verify>
  <done>Personal rows are unchanged, shared rows carry the badge/subtitle, pending rows render as non-interactive skeletons sorted last, and retained-undecryptable rows show a degraded treatment instead of the healthy badge.</done>
  <acceptance_criteria>
    Component test (React Testing Library, mocked `sendMessage` returning a fixture with mixed personal/shared/pending/undecryptable items) asserts: (1) a personal row's DOM is unchanged (no badge, no `SharedBadge` render); (2) a shared row shows the badge with the correct `aria-label`; (3) a pending row renders `role="status"`, is non-interactive (no click handler fires a `onSelectItem` call when clicked/no button element), and sorts after every resolved row; (4) a retained-`undecryptable:true` row shows the degraded treatment, not the healthy badge. `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ItemDetailView.tsx — E3 hidden-password mask, honesty note, folder note, undecryptable banner</name>
  <files>extension/entrypoints/popup/ItemDetailView.tsx, extension/entrypoints/popup/ItemDetailView.test.tsx</files>
  <read_first>
    extension/entrypoints/popup/ItemDetailView.tsx (full file, already read — `MASK`, `REVEALABLE_FIELDS`, `displayValueFor`, the reveal/copy button JSX at lines 146-173, the header `&lt;h2&gt;` at line 133)
    web/src/components/vault/DetailPanel.tsx (grep for `hidden-password-recipient-note`/`passwordFieldHidden`/`undecryptable-item-banner`/`itemSharedOnCollectionNote` — the closest literal analog per 27-PATTERNS.md; read the exact structural placement: the hidden-password check runs BEFORE the reveal-state branch, unconditionally, so a field the user had already revealed on a previous item can never leak through)
    27-UI-SPEC.md's E3 table in full, plus the `share.hiddenPasswordExtensionNote` row in the Copywriting Contract (the extension's OWN, non-web-verbatim honesty string — suppress BOTH reveal AND copy for `hidden_password`, unlike web)
  </read_first>
  <action>
    Add a `passwordFieldHidden(key)` check mirroring web's structural placement — evaluated BEFORE `MONO_FIELDS`/`REVEALABLE_FIELDS`'s existing branches in `displayValueFor`, unconditional on every render (never gated by whether the field was previously revealed): true when `key === "password"` AND `item.accessLevel === "hidden_password"`. When true, `displayValueFor` returns `MASK` unconditionally (the fail-closed branch takes priority over the reveal-state check). In the field-row JSX, when `passwordFieldHidden("password")` is true for the password row specifically, omit BOTH the reveal (`Eye`/`EyeOff`) and copy buttons ENTIRELY (not `disabled` — literally not rendered), and render `share.hiddenPasswordExtensionNote` (Task 1's new key) directly beneath the field row, unconditionally on every render (not a one-time dismissible tip). Add the shared-folder note: when `item.collectionId != null`, resolve its name from `vault.list`'s `collections` field (same lookup Task 2 uses) and render `share.itemSharedOnCollectionNote` (interpolating `{folder}`) in ordinary wrapping body text (no `truncate`); when `item.collectionId == null` (including a directly-shared item), render NOTHING in that slot — no invented placeholder, the header badge already carries the "shared" fact. Add the header-tile badge: wrap this view's own item-name header's icon/glyph position with `SharedBadge` (`size="detail"`) when `item.isShared === true` — if this view currently renders no icon in its header (confirmed: the header is just a back button + `&lt;h2&gt;`, no `ItemIconTile`), instead render the badge as a small inline marker immediately beside the `&lt;h2&gt;` item-name text, still the SAME 12px/`-bottom-1 -right-1`-derived geometry conceptually adapted to an inline (not icon-frame-relative) placement — document this adaptation explicitly since the UI-SPEC's literal wording assumes an icon frame this view does not have. Add the undecryptable banner: when `item.undecryptable === true`, render the `alert alert-warning` `data-testid="undecryptable-item-banner"` block with `sync.itemUndecryptableWarning` (Task 1's ported key), positioned above the field list, and when this banner renders, suppress the normal field-editing/reveal affordances for that item entirely (this view already has none — confirm this is a no-op given the picker-only, no-edit-affordance-for-any-item baseline, and state that confirmation in a comment).
  </action>
  <verify>
    <automated>npm --prefix extension run test -- ItemDetailView</automated>
  </verify>
  <done>Hidden-password fields are masked with reveal/copy fully omitted and the honesty note always shown; personal/edit items are byte-unchanged; the folder note and undecryptable banner render correctly.</done>
  <acceptance_criteria>
    Component test asserts: (1) a `hidden_password`-access item's password row shows the 10-dot mask, no Eye/EyeOff button, no copy button, and the exact `share.hiddenPasswordExtensionNote` string beneath it, in both PL and EN; (2) an `edit`/`read`-access item (or personal, `accessLevel` undefined) shows the SAME reveal/copy affordances as today — zero behavior change; (3) a collection-scoped item shows `share.itemSharedOnCollectionNote` interpolated with the real folder name; (4) a direct-shared item (`collectionId` null, `sharedToMe: true`) shows nothing in that slot; (5) an `undecryptable: true` item shows the warning banner. `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Background `vault.list` response <-> popup rendering | The popup renders whatever `isShared`/`collectionId`/`accessLevel`/`undecryptable` the background reports — it never re-derives or second-guesses these values, and never claims a stronger guarantee (hidden-password) than the background's own client-side-only masking actually provides |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-19 | Information Disclosure | UI copy implying hidden-password is a cryptographic control | high | mitigate | `share.hiddenPasswordExtensionNote`'s exact, unshortened three-claim string (Task 1, honesty constraint 1) states plainly the recipient holds the key regardless — checked by Task 3's own acceptance criteria, not merely style-reviewed |
| T-27-20 | Spoofing | The badge implying a share DIRECTION the popup cannot verify | medium | mitigate | `sharing.sharedItemLabel` stays direction-neutral by construction (Task 1, honesty constraint 4) — no per-row recipient-list fetch exists in this popup, so no direction claim is ever made |
| T-27-21 | Information Disclosure | A silently-omitted shared item the user has access to | high | mitigate | Task 2's pending-row rendering (never-drop-while-collections-store-hasn't-refreshed) plus the degraded-badge treatment for a retained-undecryptable row; the one remaining drop case (never-successfully-decrypted, no retained copy) is documented as an explicit decision with a stated rationale, not a silent inheritance |

</threat_model>

<verification>
`npm --prefix extension run test -- ItemListView ItemDetailView` green (no standalone `SharedBadge.test.tsx` exists — `SharedBadge` is exercised transitively by both suites, per Task 1's own acceptance criteria). `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Every E1/E2/E3 truth and backstop from 27-UI-SPEC.md is either rendered correctly (covered) or explicitly wired with evidence (backstop) — none inherited silently.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `extension/entrypoints/popup/SharedBadge.tsx` (NEW).
- `extension/lib/i18n/dictionary.ts` — 6 new keys, 2 verbatim-ported from web.
- `extension/entrypoints/popup/ItemListView.tsx` — badge/subtitle, pending-decrypt rows, degraded-row treatment.
- `extension/entrypoints/popup/ItemDetailView.tsx` — hidden-password mask/note, shared-folder note, header badge, undecryptable banner.

<output>
Create `.planning/phases/27-extension-integration-shared-items/27-08-SUMMARY.md` when done
</output>
