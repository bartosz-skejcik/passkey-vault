---
phase: 27-extension-integration-shared-items
plan: 10
type: execute
wave: 4
depends_on: ["27-06", "27-08"]
files_modified:
  - extension/entrypoints/popup/ProviderCeremonyView.tsx
  - extension/entrypoints/popup/ProviderCeremonyView.test.tsx
  - extension/entrypoints/background/provider-ceremony.ts
autonomous: true
requirements: [EXT-09, EXT-12]

must_haves:
  truths:
    - "Candidates are resolved in the background before this view renders; no in-view load state exists today and this phase adds none. Shared candidates are resolved on the same pre-render path, not fetched lazily inside the view."
    - "Unchanged: an assertion failure still routes through the existing credentials.get failed -> { failed: true } path. This phase adds no new error surface to the ceremony, and the EXT-10 spike adds none either."
    - "Each shared candidate's KeyRound frame gains the identical corner badge E1 defines; the row grows a second muted subtitle line carrying the resolved folder name (or the folder-free note when there is no folder). A personal candidate's row is completely unchanged — single line, no badge."
    - "Shared candidate whose folder name is unavailable falls back to the folder-free note instead of the folder string. Never a raw UUID, never a blank subtitle line that changes row height for nothing."
    - "The candidate subtitle reuses the existing truncate treatment already applied to the label directly above it."
    - "One candidate -> the single-match layout (no candidate row at all; the note renders beneath the existing provider.accountLabel line in that line's own treatment). Many -> the multi-match list, personal candidates sorted before shared ones."
    - "Multi-match subtitle truncates; the single-match note wraps like the rest of that view's body copy."
    - statement: "A ceremony whose only would-be candidates are shared-but-undecryptable falls through cleanly to the platform authenticator rather than rendering an empty picker — confirmed against provider-ceremony.ts's actual fallthrough logic, not merely inferred."
      verification: backstop
  key_links:
    - "findMatchingPasskeyItems(getItems(), rpId) -> a shared-but-undecryptable item is simply ABSENT from getItems() (27-04's pending/broken row never enters the decrypted cache), so the existing 'zero matches -> fallthrough' path already covers this case with no new code — this task's job is to confirm that reasoning against the real code, not add a new check"
</must_haves>
---

<objective>
Apply the shared-item badge/subtitle contract to the provider ceremony's candidate presentations (E4), and confirm — against the real `provider-ceremony.ts` fallthrough logic, not by inference — that a ceremony with only shared-but-undecryptable would-be candidates degrades cleanly rather than rendering a broken picker.

Purpose: this is the last of the three badge-host files (`ItemListView.tsx`/`AutofillItemRow.tsx`/`TotpFillRow.tsx` in 27-08/27-09, this file completing the set) and the one E4 backstop 27-UI-SPEC.md flags as inferred-not-measured.
Output: `ProviderCeremonyView.tsx`'s badge/subtitle on both candidate presentations, and a confirmed (not merely asserted) empty-candidates fallthrough.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-extension-integration-shared-items/27-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProviderCeremonyView.tsx — badge + subtitle/note on both candidate presentations</name>
  <files>extension/entrypoints/popup/ProviderCeremonyView.tsx</files>
  <read_first>
    extension/entrypoints/popup/ProviderCeremonyView.tsx (full file, already read — the multi-match candidate row's `&lt;span className="flex h-8 w-8 shrink-0 items-center justify-center"&gt;&lt;KeyRound ... /&gt;&lt;/span&gt;` frame at lines 220-222, the single-match `provider.accountLabel` line at lines 197-201, `ProviderCredentialCandidate { itemId, label }` — 27-06 already extended this interface with `isShared?`/`folderName?`)
    27-UI-SPEC.md's E4 table in full plus its "Ordering caveat" note (personal-before-shared in the candidate list is a reasoned EXTENSION of UX-3, explicitly flagged as the planner's own discretion, not one of the four locked decisions)
    27-08's `SharedBadge.tsx` (import and reuse — never re-derive)
  </read_first>
  <action>
    Multi-match list: wrap each candidate's `KeyRound` frame in `relative inline-flex` and render `SharedBadge` (row variant) when `candidate.isShared === true`; add a second, muted subtitle line beneath the existing label (`text-sm text-base-content/70`-toned, one step down, truncated like the label above it) showing `provider.sharedPasskeyFolderNote` (interpolated `{folder}`) when `candidate.folderName` is set, else `provider.sharedPasskeyNote` (the folder-free variant) when `candidate.isShared === true` — never a raw id, never a blank line for a personal candidate (personal rows keep their current single-line shape unchanged). Order candidates personal-first-then-shared before rendering (the same stable-partition shape 27-05 uses for autofill matches — a small local partition here, not an import from `autofill-match.ts`, since this is a background-message-response array already resolved by the time this component renders, not a live `getItems()` consumer). Single-match layout: when `!multiMatch` and the sole candidate is shared, render the SAME note (`provider.sharedPasskeyFolderNote`/`provider.sharedPasskeyNote`) beneath the existing `provider.accountLabel` line, in that line's own `text-sm text-base-content/70` treatment — no candidate row exists in this layout, so no badge is rendered here (the note alone carries the "shared" signal, matching the UI-SPEC's own populated-row resolution).
  </action>
  <verify>
    <automated>npm --prefix extension run test -- ProviderCeremonyView</automated>
  </verify>
  <done>Both candidate presentations carry the correct badge/note treatment for shared candidates, personal candidates unchanged, personal-before-shared ordering in the multi-match list.</done>
  <acceptance_criteria>
    Component test asserts: a personal single-match candidate's layout is byte-unchanged; a shared single-match candidate shows the folder/folder-free note beneath `provider.accountLabel`; a multi-match list with mixed candidates orders personal before shared, badges only the shared rows, and shows the correct subtitle line for each; `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Confirm the empty-shared-candidates fallthrough against real code</name>
  <files>extension/entrypoints/background/provider-ceremony.ts</files>
  <read_first>
    extension/entrypoints/background/provider-ceremony.ts lines 661-708 (already read — `handleCredentialsGet`'s `candidates = findMatchingPasskeyItems(getItems(), rpId); if (candidates.length === 0) { return { fallthrough: true }; }` — the exact structural guarantee this task must confirm, not merely restate)
    extension/entrypoints/background/vault-store.ts (27-04's finished version — confirm that a shared-but-undecryptable passkey item, whether classified pending or genuinely broken, is NEVER present in the array `getItems()` returns; a pending item lives only in `getPendingSharedItems()`, a genuinely-broken retained item still has `undecryptable: true` but DOES exist in `getItems()` with STALE plaintext content — this second case needs its own check below)
  </read_first>
  <action>
    Read `findMatchingPasskeyItems`'s filter (`item.fields.type === "passkey" && item.fields.rpId === rpId`) against a retained `undecryptable: true` item: its STALE `fields` are the last successfully-decrypted content, which may still legitimately match `type === "passkey"`/`rpId` — meaning a stale/retained passkey candidate CAN still reach the picker today. Decide explicitly, in a short code comment at `findMatchingPasskeyItems`'s call site in `handleCredentialsGet`, whether this is acceptable (the passkey's `rawPasskeyJson` is stale but still cryptographically valid for the ceremony — an assertion signed with last-known-good key material is not itself a security problem, since the item's PLAINTEXT staleness does not affect signature validity) or whether `undecryptable: true` candidates should be filtered out of the ceremony's candidate list entirely (defensive, since presenting a candidate the popup elsewhere flags as "integrity warning" inside a security ceremony may be confusing even if cryptographically safe). If filtering out, add that one-line filter to `findMatchingPasskeyItems`'s call site (not the shared function itself, which `autofill-match.ts` also calls with different needs) and re-verify the zero-candidates fallthrough still holds for the resulting narrower set. Either way, the decision must be a stated one, backed by re-reading the actual `handleCredentialsGet` fallthrough branch — this task's own acceptance criteria is evidence of that confirmation, not a restatement of research's inference.
  </action>
  <verify>
    <automated>npm --prefix extension run test -- provider-ceremony credential-store</automated>
  </verify>
  <done>The empty-candidates fallthrough behavior for a stale/undecryptable passkey is confirmed against real code and asserted by a new test, not merely documented.</done>
  <acceptance_criteria>
    `npm --prefix extension run test -- provider-ceremony credential-store` green, including a new test asserting the exact behavior decided above (either "a stale retained passkey still reaches the picker, cryptographically safe" or "it is filtered out, fallthrough covers it") — whichever was chosen, it is asserted, not merely documented in prose.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ceremony candidate list <-> RP-facing assertion | Unchanged — whichever candidate is chosen still goes through the same `wasmGetProviderAssertion`/`persistUpdatedProviderItem` path 27-06 hardened |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-23 | Repudiation | A stale, post-revocation passkey candidate surfacing in a ceremony picker after the collection was revoked | high | mitigate | 27-04's WR-02 eviction closes the Collection Key itself; a genuinely revoked item stops decrypting on the NEXT sync merge and, per Task 2's own decision, either never enters `getItems()` again or is explicitly filtered from the ceremony's own candidate list — the live end-to-end revocation proof is 27-11's job |

</threat_model>

<verification>
`npm --prefix extension run test -- ProviderCeremonyView provider-ceremony credential-store` green.
</verification>

<success_criteria>
E4's badge/subtitle contract is complete, and the empty-candidates fallthrough claim is confirmed against real code rather than left as an inference.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `extension/entrypoints/popup/ProviderCeremonyView.tsx` — badge/note on both candidate presentations.
- `extension/entrypoints/background/provider-ceremony.ts` — the confirmed/decided stale-candidate handling at `handleCredentialsGet`'s call site.

<output>
Create `.planning/phases/27-extension-integration-shared-items/27-10-SUMMARY.md` when done
</output>
