---
phase: 27-extension-integration-shared-items
plan: 06
type: execute
wave: 3
depends_on: ["27-04", "27-02"]
files_modified:
  - extension/entrypoints/background/provider-ceremony.ts
  - extension/entrypoints/popup/ProviderCeremonyView.tsx
  - extension/e2e/dual-extension-ceremony.spec.ts
autonomous: true
requirements: [EXT-09, EXT-10]

must_haves:
  truths:
    - "A shared passkey's rawPasskeyJson decrypts via the collection path (decrypt_item_for_collection) and the resulting assertion validates at a third-party RP identically to a personal passkey's — same item-wrap mechanism, no passkey-specific special case."
    - "EXT-10's genuine live wire measurement (27-02's Task 1 in-process Rust regression is the fast permanent tier, not this one): member B's REAL browser-returned credentials.get() assertion, from this plan's headed dual-extension ceremony spec, decodes response.authenticatorData bytes 33-36 as a big-endian u32 and asserts it equals 0 — the empirical wire-level confirmation 27-CONTEXT.md §A-8 step 1 mandates, closing the loop 27-02's decision record opened."
    - statement: "A ceremony whose only would-be candidates are shared-but-undecryptable falls through cleanly to the platform authenticator rather than rendering an empty picker."
      verification: backstop
    - statement: "A provider ceremony triggered during the MV3-wake window, before Collection Keys have resolved, must not present a partial candidate list that silently omits eligible shared passkeys — it either awaits resolution or falls through, never shows an incomplete picker as if complete."
      verification: backstop
  key_links:
    - "persistUpdatedProviderItem's collection-aware re-encrypt dispatch -> the ONLY place this phase changes provider-ceremony.ts's crypto; the ephemeral matchingItemJson construction at handleCredentialsGet's read path stays User-Key-scoped and UNCHANGED, since wasmGetProviderAssertion's own Rust signature has no collection-key variant to call instead"
    - "Task 2's live headed ceremony spec's signCount==0 wire assertion -> 27-02's EXT-10 decision record, the genuine live-wire measurement half of the two-tier evidence 27-02's Task 1 (fast in-process regression) cannot supply on its own"
</must_haves>
---

<objective>
Land EXT-09 (a shared passkey works through the provider ceremony, same item-wrap mechanism as any item type), the dependent-code half of EXT-10 (the decision recorded in 27-02 stays a documented non-decision — no counter tracking is added; the one concrete code change is the write-back re-encrypt dispatch a future counter-enabling change would otherwise corrupt), and the genuine live-wire measurement EXT-10's decision record depends on: 27-CONTEXT.md §A-8 step 1's mandated live `credentials.get()` against a real RP, reading the actual `signCount` on the wire — not this phase's in-process Rust fixture, a real browser round trip.

Purpose: 27-RESEARCH.md's Pitfall 3 flags `provider-ceremony.ts`'s write-back path as re-encrypting unconditionally under the personal User Key. This plan implements the CORRECT fix location — the executor must read the crypto-boundary correction below before touching this file, because the pattern map's literal line-711 suggestion would break every shared-passkey assertion outright, not merely leave a corruption risk dormant. Task 2's live spec is also where 27-02's EXT-10 decision record's own missing evidence lands — the empirical wire-level `signCount` measurement 27-RESEARCH.md's "EXT-10 Spike Findings" section explicitly did not perform, and 27-02's own in-process fixture harness structurally cannot perform either.
Output: `persistUpdatedProviderItem`'s collection-aware re-encrypt dispatch, `ProviderCredentialCandidate` UI-metadata fields, and a live headed two-extension passkey-provider proof that also asserts EXT-10's signCount==0 wire measurement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-extension-integration-shared-items/27-CONTEXT.md
@.planning/phases/27-extension-integration-shared-items/27-RESEARCH.md
@.planning/phases/27-extension-integration-shared-items/27-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: provider-ceremony.ts — the CORRECTED collection-aware write-back fix</name>
  <files>extension/entrypoints/background/provider-ceremony.ts, extension/entrypoints/background/provider-ceremony.real-wasm.test.ts</files>
  <read_first>
    extension/entrypoints/background/provider-ceremony.ts lines 195-235 and 643-742 (already fully read this session — `persistPendingProviderItem`, `persistUpdatedProviderItem`, `handleCredentialsGet`'s `matchingItemJson` construction at line 711 and the `updatedEncryptedItemJson` handling at line 726-731)
    crates/pv-wasm/src/lib.rs lines 552-591 (`wasm_get_provider_assertion`'s Rust body, already read — it takes `uk: &WasmUserKey` and internally calls `core_decrypt_item(&uk.0, &item, ...)` on `matching_item_json`, and re-encrypts any mutated result via `core_encrypt_item(&uk.0, ...)` at line 593 — there is NO collection-key-accepting variant of this binding)
    extension/entrypoints/background/vault-store.ts (27-04's ported version — `getPendingSharedItems`, the three-source `getItems()`, `VaultItem.collectionId`)
    extension/lib/crypto/wasm-loader.ts (27-03's extended version — `encryptItemForCollection`/`decryptItem` re-exports)
    extension/entrypoints/background/provider-ceremony.test.ts lines 33-94 (the EXISTING suite this task extends for behaviors 1/3 below — `vi.mock("../../lib/crypto/wasm-loader", ...)` at line 82 mocks EVERY crypto call; behaviors 1 and 3 are pure control-flow branching, mocked crypto is admissible evidence for them, but behavior 2 below is a genuine encrypt/decrypt ROUND-TRIP claim, for which a mocked crypto call cannot detect a wrong-key/wrong-scope/wrong-AAD argument — per this phase's evidence rule, it needs real WASM)
    extension/entrypoints/background/collections-store.real-wasm.test.ts (27-03's precedent for this task's OWN new `provider-ceremony.real-wasm.test.ts` — same `beforeAll` real-WASM-loading pattern, same "mock only the wire/plumbing boundary, never the crypto" discipline)
  </read_first>
  <behavior>
    - Test (existing mocked-crypto suite, `provider-ceremony.test.ts`): for a PERSONAL item (`collectionId === null`), `persistUpdatedProviderItem`'s behavior is byte-identical to today — the ciphertext WASM returned is persisted verbatim via `updateItem`, no decrypt/re-encrypt round trip added.
    - Test (NEW `provider-ceremony.real-wasm.test.ts`, real WASM crypto — not mocked): for a COLLECTION-scoped item, given a REAL `updatedEncryptedItemJson` produced by genuinely encrypting a known plaintext under a real `WasmUserKey` (mirroring how `wasm_get_provider_assertion` constructs it — User-Key-encrypted, since that binding has no collection-aware variant), `persistUpdatedProviderItem` decrypts it with the real personal `decryptItem` (the SAME key/scope the WASM binding used to produce it) to recover the mutated plaintext, then re-encrypts it via a real `encryptItemForCollection` call using a real, locally-generated Collection Key — assert the SERVER-PERSISTED ciphertext (the argument `updateItem` was actually called with) round-trips back to the ORIGINAL known plaintext via a real `decryptItemForCollection` call, proving the re-encrypt is genuinely collection-scoped (not merely "a different function was called" — a mock cannot distinguish a correct-AAD call from a wrong-AAD one, this real round trip can).
    - Test (existing mocked-crypto suite, `provider-ceremony.test.ts`): if the item's Collection Key is not cached (`getCollectionKey` returns `undefined`) when this re-encrypt-and-persist path is reached, the function logs and returns WITHOUT persisting (fails loud/no-op, mirroring this file's own existing `catch (e) { console.error(...) }` best-effort discipline elsewhere) — it must never fall back to persisting the wrong-scoped ciphertext. This is pure control-flow (no crypto call is ever reached), so the existing mocked suite is admissible evidence for it.
  </behavior>
  <action>
    CRITICAL CORRECTION to 27-RESEARCH.md's Pitfall 3 and 27-PATTERNS.md's literal suggestion: do NOT change `handleCredentialsGet`'s `matchingItemJson` construction at line 711 (the `encryptItem(uk, chosen.fields.rawPasskeyJson, ...)` call feeding `wasmGetProviderAssertion`). That call is an EPHEMERAL, purely-internal round trip — its sole purpose is to satisfy `wasm_get_provider_assertion`'s Rust signature, which hardcodes `core_decrypt_item(&uk.0, ...)` internally with NO collection-key-accepting variant. Changing line 711 to `encryptItemForCollection` would make the WASM function's own internal decrypt FAIL for every shared passkey, breaking EXT-09 outright rather than merely leaving a corruption risk dormant. The matchingItemJson round trip is symmetric (JS encrypts with `uk`, WASM decrypts with the same `uk`) regardless of the item's real storage scope, and is never itself persisted.

    The ACTUAL fix belongs in `persistUpdatedProviderItem` (the function that writes `updatedEncryptedItemJson` to the SERVER via `updateItem`) — this is the one place a wrong-scoped ciphertext would actually corrupt a stored row. Change its signature to also accept the item's `collectionId: string | null` (threaded from `handleCredentialsGet`'s existing `chosen.item.collectionId`, already on `VaultItem` per 27-04). Inside: if `collectionId === null`, persist `updatedEncryptedItemJson` exactly as today (byte-identical existing behavior — zero change for personal items). If `collectionId !== null`, first `decryptItem(uk, updatedEncryptedItemJson, itemId, expectedRevision + 1)` (the SAME uk/item_id/revision+1 the WASM binding used internally to PRODUCE this ciphertext — see `wasm_get_provider_assertion`'s own `core_encrypt_item(&uk.0, updated_json.as_bytes(), item_id, revision + 1)` call) to recover the mutated plaintext, look up the item's cached Collection Key via `collections-store.getCollectionKey(collectionId)`, and if present, `encryptItemForCollection(ck, plaintext, collectionId, itemId, expectedRevision + 1)` to produce the correctly-scoped ciphertext, THEN persist that via `updateItem`. If the Collection Key is not cached, log and return without persisting (fail loud, never fall back to the wrong-scoped ciphertext).

    Note in a comment at this exact fix site that per the EXT-10 spike (27-02), `updatedEncryptedItemJson` is `None`/dormant for every ceremony today (no signature counter is ever set) — this dispatch fix is defense-in-depth for ANY future field-mutation write-back, not currently exercised, and must NOT be read as license to add counter tracking (the explicit anti-goal).

    Create `provider-ceremony.real-wasm.test.ts` implementing behavior 2 above with genuine, non-mocked WASM crypto (no `vi.mock` of `../../lib/crypto/wasm-loader` anywhere in this new file — mock only the wire/plumbing boundary this task's action itself does not touch, e.g. `vault-api`'s `updateItem`, exactly enough to capture the ciphertext argument it was called with for the round-trip assertion). Leave behaviors 1 and 3 in the EXISTING `provider-ceremony.test.ts` (mocked-crypto suite) unchanged in kind — they are pure control-flow assertions the mock is legitimate evidence for.
  </action>
  <reversibility rating="reversible">This is a strictly additive dispatch fix on an already-dormant code path (updatedEncryptedItemJson is always None today per 27-02's spike) — it changes nothing observable until a future field-mutation write-back exists, and can be reverted without data loss since it never runs today.</reversibility>
  <verify>
    <automated>npm --prefix extension run test -- provider-ceremony</automated>
  </verify>
  <done>persistUpdatedProviderItem re-encrypts a collection-scoped mutation under the correct Collection Key before persisting (proven with real WASM crypto, not a mock), while the personal path and the ephemeral matchingItemJson round trip stay byte-identical to today.</done>
  <acceptance_criteria>
    `npm --prefix extension run test -- provider-ceremony` passes all three behaviors — behaviors 1/3 in the existing mocked-crypto suite, behavior 2 in the new `provider-ceremony.real-wasm.test.ts` with genuine encrypt/decrypt round trips; `npx tsc --noEmit` clean; the diff does NOT touch line ~711's `matchingItemJson` construction (verify via `git diff extension/entrypoints/background/provider-ceremony.ts` before committing).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ProviderCredentialCandidate metadata + LIVE headed two-extension ceremony proof</name>
  <files>extension/entrypoints/popup/ProviderCeremonyView.tsx, extension/e2e/dual-extension-ceremony.spec.ts</files>
  <read_first>
    extension/entrypoints/popup/ProviderCeremonyView.tsx (full file, already read — the `ProviderCredentialCandidate { itemId, label }` interface used by the multi-match picker)
    extension/entrypoints/background/credential-store.ts (full file, already read — `findMatchingPasskeyItems` filters `getItems()` by `fields.type === "passkey" && fields.rpId === rpId`, zero collection/access-level branching needed per research)
    extension/e2e/fixtures.ts, extension/playwright.config.ts (the `chromium-ceremony` HEADED project — this spec MUST live there, per 27-VALIDATION.md's own Playwright-project-split rule; headless Chromium reproducibly hangs Phase-12 ceremonies on this dev machine)
    extension/e2e/dual-extension-sharing.spec.ts, extension/e2e/fixtures-account-setup.ts (27-04's fixture helpers — extend/reuse the same account-setup shape for a THIRD shared item: a passkey, created via the SAME extension-provider `credentials.create()` ceremony this file already supports, then shared to member B via a direct pv-server API call rather than driving the web app's share dialog)
  </read_first>
  <action>
    Add `isShared?: boolean`/`folderName?: string` to `ProviderCredentialCandidate` (UI-SPEC's stated data-contract prerequisite; wiring in the popup's candidate rows/badges is 27-10's job, not this task's — this task only extends the type and the background-side candidate-building code that populates it, in `provider-ceremony.ts`'s `handleCredentialsGet` candidate map, from the corresponding `VaultItem`'s own fields exactly like Task 1's dispatch reads `collectionId`).

    Create `dual-extension-ceremony.spec.ts` in the `chromium-ceremony` project: member A creates a passkey via a real `credentials.create()` provider ceremony against a local test RP page (reuse this codebase's existing Phase-12 e2e fixtures/test-RP pattern, never a novel one); the resulting item is moved/shared into a collection member B has access to via a direct API call (mirroring `fixtures-account-setup.ts`'s pattern); member B's extension then completes a real `credentials.get()` ceremony against the same test RP and the assertion is verified as genuinely valid at the RP (the RP-side verification this codebase's Phase-12 harness already performs for a personal passkey — reuse that check, not a new one). This is EXT-09's headline live proof: "a shared passkey works through the passkey provider on third-party sites, using the same item-wrap mechanism as any other item type," with member B — who never created the credential — completing the ceremony.

    EXT-10's own live wire measurement (27-CONTEXT.md §A-8 step 1, 27-02's decision record depends on it): capture the RAW `PublicKeyCredential` JSON member B's browser actually returns from this same `credentials.get()` call (the test RP page already receives it to perform its own verification above — read it from there, do not re-derive a second ceremony) and, from its `response.authenticatorData` field, base64url-decode the bytes and read the 4-byte big-endian unsigned integer at byte offset 33 (after the 32-byte rpIdHash and the 1-byte flags byte, per the WebAuthn `authenticatorData` structure — the same fixed offset 27-02 Task 1's Rust test decodes, now read from a genuine live browser assertion instead of an in-process fixture) — assert this value is literal 0. This is the empirical measurement 27-RESEARCH.md's "EXT-10 Spike Findings" explicitly did not perform and 27-02's own Rust-only harness structurally cannot perform (no browser, no `navigator.credentials`, no wire); 27-02's Task 1 test remains the permanent fast regression, this assertion is the one-time-per-run genuine live-wire confirmation.
  </action>
  <verify>
    <automated>cd extension && npm run pretest:e2e:chrome && npx playwright test --project=chromium-ceremony e2e/dual-extension-ceremony.spec.ts</automated>
  </verify>
  <done>Member B completes a real credentials.get() ceremony for a passkey member A created and shared, the RP-side verification confirms the assertion is cryptographically valid, and the assertion's real wire-level authenticatorData counter bytes decode to 0.</done>
  <acceptance_criteria>
    `cd extension && npm run pretest:e2e:chrome && npx playwright test --project=chromium-ceremony e2e/dual-extension-ceremony.spec.ts` passes headed, asserting BOTH the RP-side assertion verification succeeds for member B's ceremony (a positive cryptographic validity check, not merely "no error thrown") AND the decoded `signCount` from member B's real browser-returned `authenticatorData` bytes 33-36 equals 0 — the genuine live-wire measurement, distinct from and in addition to 27-02's in-process Rust regression.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party RP <-> shared provider passkey ceremony | Unchanged trust boundary from Phase 12 — the RP's own origin/rpId verification (`passkey-client`'s `RpIdVerifier`) is the authoritative check regardless of whether the underlying item is personal or shared |
| Server-persisted ciphertext scope | A collection item's `enc_data` must remain collection-scoped (Collection Key + collection AAD) through every write path, including this ceremony's own dormant write-back — Task 1's fix closes the one path that could have silently violated this |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-14 | Tampering | A future signature-counter-mutation write-back silently corrupting a shared passkey's ciphertext scope | high | mitigate | Task 1's `persistUpdatedProviderItem` collection-aware dispatch, landed now while the path is dormant (27-02's EXT-10 decision), so a future maintainer who enables counter tracking inherits a correct write path rather than a landmine |
| T-27-15 | Spoofing | Provider ceremony reachable from the user's own configured pv-server origin | low | accept | Already mitigated, unchanged by this plan — `isConfiguredServerOrigin`'s defense-in-depth check, verified present and untouched by Task 1's diff |
| T-27-16 | Repudiation | The provider ceremony surfacing a passkey the current member no longer has access to (post-revocation staleness) | high | mitigate | `findMatchingPasskeyItems` filters `getItems()`, which is 27-04's already-revocation-aware store (a revoked collection's items disappear once `collections-store`'s WR-02 eviction runs) — no separate check needed in this file; the end-to-end revocation proof is 27-11's job |

</threat_model>

<verification>
`npm --prefix extension run test -- provider-ceremony` green. `npx playwright test --project=chromium-ceremony e2e/dual-extension-ceremony.spec.ts` green.
</verification>

<success_criteria>
A shared passkey works through the provider ceremony end-to-end, live, for a member who did not create it; the write-back dispatch is fixed correctly without breaking the (currently load-bearing) ephemeral matchingItemJson round trip; EXT-10's genuine live-wire signCount measurement lands here, closing the evidence gap 27-02's decision record opened and 27-RESEARCH.md left explicitly undone.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `extension/entrypoints/background/provider-ceremony.ts` — `persistUpdatedProviderItem`'s collection-aware dispatch.
- `extension/entrypoints/background/provider-ceremony.real-wasm.test.ts` (NEW) — the real-crypto round-trip proof for the collection-scoped re-encrypt; the existing `provider-ceremony.test.ts` mocks `wasm-loader` entirely, so this new file is the only admissible evidence for the re-encrypt's key/scope/AAD correctness.
- `extension/entrypoints/popup/ProviderCeremonyView.tsx` — `ProviderCredentialCandidate.isShared?`/`.folderName?`.
- `extension/e2e/dual-extension-ceremony.spec.ts` (NEW) — also carries EXT-10's genuine live-wire `signCount == 0` measurement.

<output>
Create `.planning/phases/27-extension-integration-shared-items/27-06-SUMMARY.md` when done
</output>
