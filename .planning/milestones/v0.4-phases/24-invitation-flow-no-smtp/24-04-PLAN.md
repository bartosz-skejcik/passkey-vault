---
phase: 24-invitation-flow-no-smtp
plan: 04
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - crates/pv-server/tests/invitations.rs
  - crates/pv-server/src/routes/mod.rs
autonomous: true
requirements: [FAM-04, FAM-05]

must_haves:
  truths:
    - "Two genuinely concurrent redemption attempts — each on its own tokio::spawn task, released together via a shared Arc<Barrier>, against a real multi-connection (max_connections(4)) shared-cache SQLite pool, NEVER common::test_pool()'s max_connections(1) pool which would serialize the race on pool acquisition and prove nothing — BOTH presenting the objectively correct invite_proof, against the SAME single-use invite link, result in exactly one HTTP 200 and one unified failure across 20 trials — never two successes, never zero."
    - "The losing request in every trial gets the unified 404 failure, never a 500 — the test pool's connection options set the same 5-second busy_timeout pv_server::build_pool uses in production, so BEGIN IMMEDIATE's write-lock contention is absorbed by SQLite's busy handler rather than surfaced as a raw database error."
    - "invite_id alone — the correct id, but NO proof or a WRONG invite_proof — is rejected with the unified failure on BOTH fetch_metadata and accept, adversarially proven as a first-class test alongside the metadata-leak assertion (Amendment 2 — this is the test that actually closes T-24-07, not merely a design claim)."
    - "A member who already has access to a shared collection receives a live EntityType::Collection WebSocket event the moment a new member successfully joins that collection via invite — proven against a real bound server and a real WebSocket connection, not a mocked transport."
    - "A collection-scoped invite's public metadata response never contains the collection's own enc_name value or any other collection-identifying string, verified by asserting the response body does not contain the collection's real (encrypted) name as a substring."
    - "Every HTTP response this server sends carries a Referrer-Policy header, so invite_id (a path segment) cannot travel to a third-party origin via an outbound Referer header from the invite landing page."
  artifacts:
    - "crates/pv-server/src/routes/mod.rs: referrer_policy_middleware"
  key_links:
    - "The concurrency test mirrors tests/collections.rs::revoke_access_last_key_holder_guard_is_atomic_under_concurrency exactly: a fresh file:{uuid}?mode=memory&cache=shared pool per trial, tokio::spawn for each racer gated on Arc<Barrier>, tokio::join! used only to await the two JoinHandles (never raw futures directly) — 20 trials, not 5 — plus a busy_timeout the analog itself doesn't need but this task's own 500-vs-404 assertion does."
    - "The fan-out test uses common::test_server (a real bound TCP listener) and tokio_tungstenite::connect_async, mirroring tests/sync_shared.rs's established pattern — never tower::ServiceExt::oneshot, which cannot upgrade a WebSocket."
    - "The invite_id-alone adversarial test is what T-24-07's threat register row cites as evidence — a threat_model claim of 'mitigate' with no corresponding test would be exactly the gap Amendment 2 exists to close."
---

<objective>
Prove the phase's two sharpest, most failure-prone guarantees with genuinely adversarial, genuinely
concurrent tests — not proofs-by-construction, not sequential-call approximations — and close the one
remaining information-disclosure gap (Referrer-Policy) CONTEXT.md flags but does not implement.

Purpose: SC 4 ("exactly one successful join") and FAM-05 ("leaks no vault metadata") are this phase's
two claims most likely to look done while quietly being wrong; this plan is where that risk gets
retired with evidence, not assertion.
Output: a genuinely concurrent single-use proof, a real-WebSocket fan-out proof, an adversarial
metadata-shape proof, and a global Referrer-Policy header.
</objective>

## Artifacts this phase produces

- `crates/pv-server/src/routes/mod.rs` — `referrer_policy_middleware` (applied via
  `axum::middleware::from_fn`).
- `crates/pv-server/tests/invitations.rs` — `concurrent_redemption_exactly_one_wins`,
  `accept_fans_out_collection_event_to_existing_member_over_websocket`,
  `invitation_metadata_collection_scoped_never_leaks_collection_enc_name`,
  `invitation_id_alone_without_correct_proof_is_rejected_on_metadata_and_accept`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-invitation-flow-no-smtp/24-RESEARCH.md
@.planning/phases/24-invitation-flow-no-smtp/24-CONTEXT.md
@crates/pv-server/tests/collections.rs
@crates/pv-server/src/lib.rs
@crates/pv-server/tests/sync_shared.rs
@crates/pv-server/src/routes/mod.rs
@crates/pv-server/tests/invitations.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Genuinely concurrent double-redemption proof</name>
  <files>crates/pv-server/tests/invitations.rs</files>
  <read_first>
    crates/pv-server/tests/collections.rs lines 547-708
    (`revoke_access_last_key_holder_guard_is_atomic_under_concurrency` — the REQUIRED analog for this
    task's whole structure: a per-trial `file:{uuid}?mode=memory&cache=shared` pool with
    `max_connections(4)`/`min_connections(1)` because `common::test_pool()`'s bare
    `sqlite::memory:` URI is `max_connections(1)` and would serialize the race away on POOL
    ACQUISITION rather than the SQLite write lock, proving nothing; `tokio::spawn` for each racer
    gated on a shared `Arc<Barrier>`, with `tokio::join!` used ONLY to await the resulting
    `JoinHandle`s — never `tokio::join!` on the raw request futures directly, which this codebase's
    own `tests/collections.rs:559-565` doc comment explains polls both futures cooperatively from one
    task and cannot force genuine interleaving; 20 `TRIALS`, not 5); crates/pv-server/src/lib.rs lines
    55-65 (`build_pool` — sets WAL journal mode AND a 5-second `busy_timeout` via
    `SqliteConnectOptions`; `common::test_pool()` and the `collections.rs` analog do NEITHER, which is
    fine for a `max_connections(1)` pool with no real contention, but THIS task's genuinely
    multi-connection pool needs the SAME `busy_timeout` production has, or a lock-contention loser can
    surface as a raw `sqlx::Error` → `ApiError::Internal` → `500`, which is a live failure mode this
    task must pin down and assert against, not merely hope doesn't happen); crates/pv-server/tests/invitations.rs
    (Plan 24-02's existing fixture/request helpers — `req()`/`body_json()` and the by-now-established
    `pv_core::invite::{derive_invite_id, derive_invite_proof, hash_invite_proof}` "simulate the
    client" pattern for producing a real `proof_hash`/`invite_proof` pair — reuse verbatim, do not
    duplicate); .planning/phases/24-invitation-flow-no-smtp/24-RESEARCH.md's "Pitfall 5" section (the
    exact warning-sign a sequential-looking — or falsely-parallel-looking — test would trip).
  </read_first>
  <behavior>
    - Racing two different brand-new users' `accept` calls — BOTH presenting the SAME objectively
      correct `invite_proof`, released at the same instant via a shared `Barrier`, each on its own
      `tokio::spawn`ed task against a genuinely multi-connection pool — against the SAME invite_id
      produces exactly one `200 OK` and exactly one unified-failure response across 20 independent
      fresh-invite-and-fresh-database trials (not a single lucky run, and not a race that pool
      contention alone would have serialized away).
    - The losing request's response is a 4xx unified failure, NEVER a `500` — proving the
      `BEGIN IMMEDIATE` transaction's write-lock contention is absorbed by the busy handler, not
      leaked to the caller as a raw database error.
    - After each trial, `family_members` contains exactly ONE of the two racing users — never both,
      never neither.
  </behavior>
  <action>
    Add `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` (NOT the default
    single-threaded `#[tokio::test]` every other test in this file uses — this test specifically needs
    real OS-thread parallelism, not cooperative single-thread interleaving) `async fn
    concurrent_redemption_exactly_one_wins()`. Declare `const TRIALS: usize = 20;` and two counters,
    `double_wins`/`zero_wins`, both starting at `0`. Loop `0..TRIALS`; in each iteration `i`:

    Build a FRESH, genuinely multi-connection pool — never reuse `common::test_pool()` here, it is
    `max_connections(1)` by design and would serialize the race on pool acquisition, never the SQLite
    write lock, proving nothing: `let db_name = format!("invite_race_{}", uuid::Uuid::new_v4().simple());
    let db_url = format!("file:{db_name}?mode=memory&cache=shared");` then build
    `sqlx::sqlite::SqliteConnectOptions::from_str(&db_url)` (bring `std::str::FromStr` into scope)
    `.expect(...)`, chain `.busy_timeout(std::time::Duration::from_secs(5))` (mirrors
    `pv_server::build_pool`'s own production setting — WITHOUT this, a lock-contention loser can
    surface as a raw `500` instead of the application's own guarded rejection), and connect via
    `sqlx::sqlite::SqlitePoolOptions::new().max_connections(4).min_connections(1).connect_with(opts)`
    (`min_connections(1)` keeps the shared-cache in-memory DB alive for the pool's whole lifetime — it
    is dropped once its last connection closes). Run `sqlx::migrate!("./migrations").run(&pool).await`
    on this fresh pool, then `let app = test_app(pool.clone());`.

    Build one family via a fresh owner account (`invite-race-owner-{i}@example.com`), derive a fresh
    `invite_secret`/`invite_id`/`invite_proof`/`proof_hash` quadruple (the test's stand-in for the real
    client, same pattern Plan 24-02's own tests already establish in this file), and create a
    family-only invite with that `proof_hash`. Register two brand-new users
    (`invite-race-a-{i}@example.com`, `invite-race-b-{i}@example.com`).

    Build `let barrier = Arc::new(Barrier::new(2));`, clone everything each spawned task needs
    (`app`, both tokens, `invite_id`, the shared `invite_proof` string, a clone of `barrier` per task —
    mirror `collections.rs`'s exact clone-then-move pattern). Spawn `let task_a = tokio::spawn(async
    move { barrier_a.wait().await; req(&app_a, "POST", &format!("/api/invitations/{invite_id_a}/accept"),
    &racer_a_token, Some(json!({ "invite_proof": invite_proof_a }))).await.status() });` and the
    symmetric `task_b`. Await both via `let (status_a, status_b) = tokio::join!(task_a, task_b);` —
    this joins the two `JoinHandle`s (already-spawned, already-running tasks), which is NOT the
    rejected "raw-future `tokio::join!`" pattern; unwrap each with `.expect("task must not panic")`.

    Assert `usize::from(status_a == StatusCode::OK) + usize::from(status_b == StatusCode::OK) <= 1`
    (never both winning), incrementing `double_wins` if it's `2` and `zero_wins` if it's `0`. When
    exactly one won: identify `loser_status` as whichever of the two is NOT `StatusCode::OK`, and
    assert TWO things about it — `assert_ne!(loser_status, StatusCode::INTERNAL_SERVER_ERROR, "trial
    {i}: lock contention must be absorbed by the busy_timeout, never surfaced as a 500")` (the
    specific live failure mode this task's `busy_timeout` configuration exists to prevent), AND
    `assert_eq!(loser_status, StatusCode::NOT_FOUND, "trial {i}: the losing accept must render the
    same unified failure every other rejected redemption does")` (accept's actual documented
    losing-path status, per Plan 24-02's own design — not a guess). After each trial, `SELECT
    COUNT(*) FROM family_members WHERE family_id = ? AND user_id IN (?, ?)` (bound to the family id
    and both racers' user ids) and `assert_eq!(count, 1, "trial {i}: exactly one racer must have
    joined")` — the strongest possible per-trial assertion, since it fails even if response-status
    parsing were somehow fooled.

    After the loop: `assert_eq!(double_wins, 0, "{double_wins}/{TRIALS} trials had BOTH concurrent
    accepts succeed — the single-use guard is broken")` and `assert_eq!(zero_wins, 0,
    "{zero_wins}/{TRIALS} trials had NEITHER concurrent accept succeed — a valid, otherwise-eligible
    invite was wrongly rejected for both racers")`.
  </action>
  <verify>
    <automated>cargo test -p pv-server --test invitations concurrent_redemption_exactly_one_wins -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    The test passes deterministically across at least 3 consecutive full `cargo test` invocations (run
    it 3 times in a row during verification — a test that only sometimes catches the race is not
    sufficient evidence). Across all 20 trials × 3 verification runs = 60 total races: `double_wins ==
    0`, `zero_wins == 0`, no loser status is ever `500`, and `family_members` never contains both
    racers nor neither for any trial. `cargo test -p pv-server --test collections
    revoke_access_last_key_holder_guard_is_atomic_under_concurrency` (the analog) still passes
    unmodified — this task reads it as a pattern, never edits it.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real-WebSocket fan-out proof + adversarial metadata-leak + invite_id-alone rejection + Referrer-Policy</name>
  <files>crates/pv-server/tests/invitations.rs, crates/pv-server/src/routes/mod.rs</files>
  <read_first>
    crates/pv-server/tests/sync_shared.rs lines 1-230 (the real-bound-server + `tokio_tungstenite`
    pattern, `recv_ws_json`-shaped helper, `common::test_server`); crates/pv-server/src/routes/mod.rs
    (the `cors_layer()`/`build_cors_layer()` functions and their own `#[cfg(test)] mod tests`
    `probe_router`/`acao_header_for` idiom — the header-presence test this task's Referrer-Policy test
    mirrors); http crate's `header::REFERRER_POLICY` constant (re-exported via `axum::http::header`);
    .planning/phases/24-invitation-flow-no-smtp/24-CONTEXT.md's "Amendment 2" section (the exact
    property this task's new adversarial test proves).
  </read_first>
  <behavior>
    - An existing collection member with a live WebSocket connection receives one
      `EntityType::Collection` frame the instant a new member joins that collection via invite.
    - `POST /api/invitations/{id}` (metadata) for a collection-scoped invite, presenting the correct
      `invite_proof`, never returns the collection's own `enc_name` value (or any substring of it)
      anywhere in its JSON body.
    - `POST /api/invitations/{id}` and `POST /api/invitations/{id}/accept` against a REAL, otherwise-
      eligible invite id, presenting NO proof, a malformed proof, or a WRONG-but-well-formed proof, all
      render the identical unified failure a never-existed id would — invite_id by itself proves
      nothing and reveals nothing.
    - Every HTTP response — including a bare `/healthz` probe — carries `Referrer-Policy:
      strict-origin-when-cross-origin`.
  </behavior>
  <action>
    In `crates/pv-server/tests/invitations.rs`, add `async fn
    accept_fans_out_collection_event_to_existing_member_over_websocket()`: build a fixture with an
    owner, an existing member C already holding `collection_keys` access to a real shared collection
    (mirror `tests/sync_shared.rs`'s `setup_shared_fixture` shape), start the app via
    `common::test_server` (a real bound port — `tower::ServiceExt::oneshot` cannot upgrade a
    WebSocket), have C open a real `tokio_tungstenite::connect_async` connection to
    `/api/sync/ws?token=<C's token>`, then have a brand-new user D redeem a collection-scoped invite
    for that same collection, presenting the correct `invite_proof` at both the metadata fetch and
    accept. Read the next frame off C's socket (duplicate a small `recv_ws_json`-shaped helper locally
    in this file — matches this codebase's own stated precedent for per-test-binary helper duplication
    rather than exporting from `sync_shared.rs`) and assert its `entity_type == "collection"`, `id`
    equals the collection id, and `change_type == "update"`.

    Add `async fn invitation_metadata_collection_scoped_never_leaks_collection_enc_name()`: create a
    collection with a distinctive `enc_name` value (a literal test string standing in for ciphertext —
    matching `tests/membership_route_sweep.rs::create_collection`'s own `"sweep-collection-name"`
    convention), create a collection-scoped invite for it, fetch `POST /api/invitations/{id}` with no
    session but the CORRECT `invite_proof`, and assert (a) the response body's key set is still exactly
    the five documented fields (`inviter_email`, `family_name`, `inviter_fingerprint`, `collection_id`,
    `wrapped_collection_key`) and (b) `serde_json::to_string(&body)` does not contain the `enc_name`
    string anywhere as a substring.

    Add `async fn invitation_id_alone_without_correct_proof_is_rejected_on_metadata_and_accept()` — the
    Amendment 2 adversarial test the orchestrator specifically requires alongside the metadata-leak
    one: create one real, otherwise-fully-eligible invite (valid `proof_hash`, unexpired, `pending`);
    for BOTH `POST /api/invitations/{id}` and `POST /api/invitations/{id}/accept` (the latter with a
    real registered user's session, since accept always requires SOME session — a MISSING session is
    already covered by Plan 24-02's `invitation_accept_with_no_authorization_header_returns_401` test
    and correctly returns 401, a DIFFERENT and legitimate axum-level distinction from the invite's own
    validity, not something this test needs to re-prove), attempt three variants that all deserialize
    successfully (an outright-missing `invite_proof` JSON field fails deserialization before the
    handler runs at all, producing axum's own generic rejection rather than `ApiError` — a
    request-shape distinction this codebase already accepts everywhere `Json<T>` is used, not a new
    gap this test needs to cover): (a) `invite_proof: ""` (empty string — decodes to 0 bytes, an
    automatic length mismatch), (b) a WELL-FORMED but wrong 32-byte `invite_proof` (a different
    `random_bytes(32)` value, base64-encoded), (c) a malformed (non-base64, or base64 decoding to the
    wrong byte length) `invite_proof` string — and assert ALL SIX resulting responses (2 endpoints × 3
    variants) render `ApiError::NotFound`'s exact generic body, byte-identical to a request against a
    `random-unknown-id`'s response body captured in the same test. This is the test that gives
    T-24-07's `mitigate` disposition actual evidence.

    In `crates/pv-server/src/routes/mod.rs`, add `async fn referrer_policy_middleware(request:
    axum::extract::Request, next: axum::middleware::Next) -> axum::response::Response` that calls
    `next.run(request).await`, inserts `(axum::http::header::REFERRER_POLICY,
    axum::http::HeaderValue::from_static("strict-origin-when-cross-origin"))` into the response's
    headers, and returns it. Apply via `.layer(axum::middleware::from_fn(referrer_policy_middleware))`
    in `router_with_cors`, positioned so it applies to every response including the static-file
    fallback branch (check whether `.layer(cors)` already wraps the whole thing before or after the
    static-dir match — apply this new layer at the SAME point, wrapping the complete router, not just
    the `api` sub-chain, so a served `index.html`/asset also carries the header). Add a test to
    `mod.rs`'s existing `#[cfg(test)] mod tests` (reuse the `probe_router` helper's pattern) asserting
    a plain `GET /healthz` response carries the header with the exact expected value — grep the
    codebase first for any pre-existing `Referrer-Policy` setting (there is none, per this session's
    verification) so this is confirmed additive, not a duplicate.
  </action>
  <verify>
    <automated>cargo test -p pv-server --test invitations --test sync_shared -- --test-threads=2 && cargo test -p pv-server routes::mod::tests</automated>
  </verify>
  <acceptance_criteria>
    All three new `tests/invitations.rs` tests pass, including the six-way `invite_id`-alone
    adversarial assertion. The new `mod.rs` header test passes. `cargo test -p pv-server` (full suite)
    stays green — the new middleware does not break any existing CORS/response test (particularly
    `acao_header_for`-based assertions, which read a DIFFERENT header and must be unaffected).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invite landing page → third-party origin (outbound navigation/asset load) | invite_id (a public path segment) must not travel via `Referer` off-origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-05 (proof) | Elevation of Privilege | invitations::accept guarded UPDATE | high | mitigate | genuinely concurrent (multi-thread, `tokio::spawn` + `Arc<Barrier>`, against a real multi-connection shared-cache pool — never `common::test_pool()`'s single-connection pool, which would serialize the race away on pool acquisition) test proves exactly one winner across 60 total races (20 trials × 3 verification runs), with both racers presenting the correct `invite_proof` so the race is isolated to the single-use guard, and the loser's status is asserted to be `404`, never `500` |
| T-24-07 (proof) | Elevation of Privilege | invite_id known without invite_secret (fetch_metadata / accept) | medium | mitigate | **Amendment 2** — `invitation_id_alone_without_correct_proof_is_rejected_on_metadata_and_accept` proves, across 6 request variants, that a real otherwise-eligible invite is completely unreachable without the correct `invite_proof`; this is the test Plan 24-02's design-level `mitigate` claim needed and previously lacked |
| T-24-09 (proof) | Information Disclosure | invitations::fetch_metadata | high | mitigate | adversarial substring assertion against the collection's own `enc_name`, not just a key-presence check — run only with the CORRECT `invite_proof` presented, since without it the request never reaches this code path at all (see T-24-07) |
| T-24-10 | Information Disclosure | invite_id leakage via Referer header | medium | mitigate | global `Referrer-Policy: strict-origin-when-cross-origin` middleware, verified present on every response including the static-file fallback |

</threat_model>

<verification>
```
cargo test -p pv-server --test invitations
cargo test -p pv-server --test sync_shared
cargo test -p pv-server routes::mod::tests
```
Run the concurrency test 3 times in a row before considering this plan done.
</verification>

<success_criteria>
The single-use guarantee, the metadata non-leak guarantee, and the Amendment 2 proof-of-possession
guarantee (invite_id alone is provably insufficient) are all proven by tests that could actually catch
a regression, not by tests shaped to pass. Every response carries a Referrer-Policy header.
</success_criteria>

<output>
Create `.planning/phases/24-invitation-flow-no-smtp/24-04-SUMMARY.md` when done, with `## Threat Flags`
populated.
</output>
