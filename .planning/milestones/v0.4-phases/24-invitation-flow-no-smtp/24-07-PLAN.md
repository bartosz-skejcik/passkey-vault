---
phase: 24-invitation-flow-no-smtp
plan: 07
type: execute
wave: 4
depends_on: ["24-05"]
files_modified:
  - web/src/components/settings/SettingsPanel.tsx
  - web/src/components/settings/FamilyTab.tsx
  - web/src/components/settings/FamilyTab.test.tsx
autonomous: true
requirements: [FAM-04]

must_haves:
  truths:
    - "The Generate-link button shows a disabled + busy state during creation, reusing the existing submit-button busy pattern already used elsewhere in this settings drawer."
    - "Selecting the collection scope reveals a folder picker AND the honestVisibilityNote directly beneath it in the same render pass — never a two-step reveal, never shown when whole-family scope is selected."
    - "Zero folders disables the collection-scope option with folderPickerEmpty helper text — the folder <select> is never rendered broken or empty."
    - "Exactly one invite is shown after generation — the most recently created one — never a list or history, matching the explicit no-pending-invite-list boundary in CONTEXT.md."
    - "Scope defaults to whole-family and expiry defaults to 7 days, so the generate form is submittable the instant it renders, with no required field left unset."
    - "The generated link renders in a read-only, horizontally-scrolling input, never a wrapping <code> block that could widen the panel."
    - "The Copy button calls copyWithAutoClear + showCopyToast (the SAME pairing every other copyable secret in this app uses) with the field label 'Link zaproszenia'/'Invite link', not a plain non-auto-clearing toast — the link's fragment is a decryption-capable secret, the same security class as a password."
    - "The Revoke action is gated behind a DeleteConfirmDialog-shaped confirmation carrying a visible 'Unieważnij'/'Revoke' label — never icon-only."
    - "family.createCta shows a disabled + busy state during family creation."
    - "The Family tab IS the empty state for 'owner has no family yet' — detected from GET /api/families/members returning 404 — no separate loading flash before it renders."
    - "A required family-name field blocks empty submission natively, with no new validation code."
    - "A long family name is accepted by the create-family input without truncation at entry time — the ONLY place it gets truncated is the invitee's own Join heading, built in Plan 24-06, not here."
    - statement: "An invite-creation failure (network error, or the server rejecting because the caller is no longer the owner) renders as a non-silent inline error that leaves the form's already-entered scope/expiry selections intact, rather than resetting the form."
      verification: backstop
    - statement: "The folder picker renders correctly with exactly one folder and with many folders, without the panel's height blowing out in the many-folder case."
      verification: backstop
    - statement: "A folder with a very long name truncates its own <option> text rather than widening the panel."
      verification: backstop
    - statement: "The selected folder's displayed value truncates the same way the option text does, for an equally long name."
      verification: backstop
    - statement: "A family-creation 409 Conflict (another browser tab already created the singleton family) renders as a recoverable message that re-fetches membership and advances straight to the invite-generation form, rather than a dead end requiring a page reload."
      verification: backstop
  artifacts:
    - web/src/components/settings/FamilyTab.tsx
  key_links:
    - "FamilyTab calls ensureOwnIdentityKeypair (via lib/invite/crypto.ts's generateInviteLink) BEFORE generating any link — opportunistic per UI-SPEC's cross-phase gap note, so the fingerprint block in Plan 24-06's Join screen is populated for the common case, not the exception."
    - "The Copy button's accessible name comes from invite.copyLinkAria (aria-label) since the button carries no visible text — the Revoke button, by contrast, always carries a visible label."
---

<objective>
Build the owner-side "Invite someone" surface: a new Family tab inside the existing Settings drawer,
covering family bootstrap (first-run empty state), the scope/expiry invite-creation form, the
generated-link display with copy/revoke, and the two honesty-adjacent copy requirements
(`honestVisibilityNote`) this phase's owner-facing UI carries.

Purpose: FAM-04 has no owner-facing affordance without this plan — the server/crypto/client-lib work
in Plans 24-01/02/03/05 has nothing to drive it from.
Output: `FamilyTab.tsx` wired into `SettingsPanel.tsx`'s existing tab strip.
</objective>

## Artifacts this phase produces

- `web/src/components/settings/FamilyTab.tsx` (new component) — bootstrap form, scope/expiry
  invite-creation form, generated-link display with copy/revoke.
- `web/src/components/settings/SettingsPanel.tsx` — new `"family"` tab entry in `SettingsTab`, new tab
  button, new conditional render branch.
- `web/src/components/settings/FamilyTab.test.tsx` (new).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-invitation-flow-no-smtp/24-UI-SPEC.md
@.planning/phases/24-invitation-flow-no-smtp/24-PATTERNS.md
@web/src/components/settings/SettingsPanel.tsx
@web/src/components/vault/CopyToast.tsx
@web/src/components/vault/DeleteConfirmDialog.tsx
@web/src/lib/invite/crypto.ts
@web/src/lib/invite/api.ts
@web/src/lib/vault/copyToast.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Family tab — bootstrap + scope/expiry invite-creation form</name>
  <files>web/src/components/settings/SettingsPanel.tsx, web/src/components/settings/FamilyTab.tsx</files>
  <read_first>
    web/src/components/settings/SettingsPanel.tsx (the `tabClass(active)` helper, the `tabs
    tabs-bordered` strip at line ~51, the existing `passkeys`/`sessions`/`security`/`importExport` tab
    entries and their conditional-render-by-tab-id body structure — `settings.tabFamily` slots in
    alongside these with zero new visual language); web/src/lib/vault/store.ts's `useFolders()` (the
    folder-picker data source); web/src/lib/invite/api.ts (Plan 24-05's `createInvite` — this task does
    NOT call it directly; it calls `generateInviteLink` from `lib/invite/crypto.ts`, which wraps it).
  </read_first>
  <behavior>
    - No family yet (`GET /api/families/members` returns 404): renders `family.bootstrapHeading`/
      `family.bootstrapBody`/`family.nameLabel` input/`family.createCta` button; a 409 Conflict on
      submit re-fetches membership and advances to the invite form rather than showing a dead end.
    - Family exists: renders the invite-creation form directly (scope picker, expiry picker, Generate
      button) with whole-family + 7-day defaults pre-selected.
    - Selecting collection scope with zero folders disables that option with helper text; selecting it
      with folders reveals the folder `<select>` and the honesty note together.
  </behavior>
  <action>
    In `web/src/components/settings/SettingsPanel.tsx`, add a fifth tab entry: `<button
    className={tabClass(tab === "family")} onClick={() => setTab("family")}><Users className="h-4 w-4"
    aria-hidden="true" /> {t("settings.tabFamily")}</button>` inside the existing `tabs tabs-bordered`
    strip (alongside passkeys/sessions/security/importExport), and a conditional render branch `{tab
    === "family" ? <FamilyTab /> : null}` matching the existing per-tab body structure exactly. Extend
    the `SettingsTab` union type to include `"family"`.

    Create `web/src/components/settings/FamilyTab.tsx`. On mount, attempt to fetch the family roster
    (reuse whatever existing client function calls `GET /api/families/members` — check
    `web/src/lib/` for one before adding a new one) to determine bootstrap-vs-normal mode: a 404 means
    "no family yet". Bootstrap mode: `family.bootstrapHeading` (Heading), `family.bootstrapBody`
    (Body), a `family.nameLabel` labeled `input input-bordered w-full` (native `required`), a
    `family.createCta` `btn btn-primary` (disabled + busy label while the create call is in flight). On
    a `409 Conflict` response (another tab already created the singleton family — Phase 22's own
    `idx_families_singleton` DB constraint surfaces this): do not show a dead-end error — silently
    re-fetch the roster and fall through to normal mode on success (this is the E7 error backstop).

    Normal mode (family exists): render `invite.sectionHeading` (`UserPlus` icon) followed by: a
    `invite.scopeLabel` + `select select-bordered w-full` with two options
    (`invite.scopeWholeFamily`/`invite.scopeFolder`), defaulting to whole-family; when
    `invite.scopeFolder` is selected, reveal a second `select` (`invite.folderPickerLabel`, options
    populated from `useFolders()`, each `<option>` truncating a long folder name via a CSS
    `text-overflow: ellipsis`-class or native `<select>` truncation — verify the rendered `<option>`
    itself does not force the panel wider) directly followed by `invite.honestVisibilityNote` (Body
    role, rendered ONLY in this sub-state); if `useFolders()` returns an empty array, disable the
    `invite.scopeFolder` `<option>` and show `invite.folderPickerEmpty` as inline helper text instead
    of rendering the folder `<select>` at all. An `invite.expiryLabel` + `select select-bordered
    w-full` with three options (`invite.expiry1h`/`invite.expiry24h`/`invite.expiry7d`),
    `invite.expiry7d` pre-selected. An `invite.generateCta` `btn btn-primary` button — disabled + busy
    label while `generateInviteLink` (from `lib/invite/crypto.ts`) is in flight. On failure: render a
    non-dismissing inline error message BELOW the form, leaving every already-selected scope/expiry
    value exactly as the user left it (the E5 error backstop — do not reset the form's state on
    failure).
  </action>
  <verify>
    <automated>npm --prefix web run test -- FamilyTab</automated>
  </verify>
  <acceptance_criteria>
    A new `web/src/components/settings/FamilyTab.test.tsx` covers: bootstrap-mode render (mocked 404),
    bootstrap 409-then-recovers-to-normal-mode, normal-mode default selections (whole-family + 7d
    pre-selected, form immediately submittable), zero-folders disables collection scope with helper
    text, non-empty-folders reveals the picker + honesty note together, and invite-creation failure
    leaves the form's own scope/expiry state intact (assert the `<select>`'s value is unchanged after
    a mocked failed submit). All pass.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generated-invite display — link, copy, expiry, revoke</name>
  <files>web/src/components/settings/FamilyTab.tsx, web/src/components/settings/FamilyTab.test.tsx</files>
  <read_first>
    web/src/components/vault/CopyToast.tsx + web/src/lib/vault/copyToast.ts's `showCopyToast`
    (fieldLabel, durationMs); the existing `copyWithAutoClear`+`showCopyToast` PAIRED call site in
    `web/src/components/generator/GeneratorDialog.tsx` (lines ~110-113) — the exact idiom to copy,
    including `readClipboardSeconds()` for the duration; web/src/components/vault/DeleteConfirmDialog.tsx
    (the `AlertTriangle` + `btn-error` confirm pattern — `FamilyTab`'s revoke confirmation reuses this
    SHAPE inline, since `DeleteConfirmDialog` itself is typed specifically to `VaultItem`, not an
    invite).
  </read_first>
  <behavior>
    - After a successful `generateInviteLink` call, the create-form is replaced by a read-only link
      display, an `expiresAt` line, a Copy button, and a Revoke button — never both the form and the
      display at once.
    - The Copy button writes the link to the clipboard with auto-clear AND shows the shared `CopyToast`
      with field label "Link zaproszenia"/"Invite link" — the same two-call pairing every other secret
      copy in this app uses.
    - Confirming Revoke calls the revoke endpoint, then reverts the panel to the create form (step 1)
      with no history retained.
    - The Copy button's accessible name is `invite.copyLinkAria` (icon-only); the Revoke button always
      carries a visible text label.
  </behavior>
  <action>
    Extend `FamilyTab.tsx`: after `generateInviteLink` succeeds, store `{ url, expiresAt }` in local
    state and render (replacing the create form): a read-only `input input-bordered w-full font-mono`
    with `value={url}` and `readOnly`; `t("invite.expiresAt", formattedLocalDate(expiresAt))` (Label
    role — format via the browser's own `Intl.DateTimeFormat`/`toLocaleString`, no new date library);
    an icon-only Copy button (`Copy` from lucide-react) with `aria-label={t("invite.copyLinkAria")}`
    whose `onClick` reuses the EXACT existing call shape from `GeneratorDialog.tsx`:
    `const seconds = readClipboardSeconds(); copyWithAutoClear(url, seconds * 1000);
    showCopyToast(locale === "pl" ? "Link zaproszenia" : "Invite link", seconds * 1000);` (the
    field-label argument is a plain string literal passed at the call site, not a new dictionary key —
    it becomes `toast.copied`'s existing `{field}` interpolation value, per 24-UI-SPEC.md's own note
    that no new toast copy key is needed — do NOT invent a `toast.copied.field.invite`-shaped key,
    24-05 Task 3 deliberately does not create one). A `btn btn-ghost btn-error` button with the VISIBLE label
    `t("invite.revokeConfirmConfirm")` (never icon-only) that opens a confirmation modal reusing
    `DeleteConfirmDialog`'s exact visual shape inline (fixed `w-full max-w-[400px]` centered dialog,
    `AlertTriangle` in `text-error`, `invite.revokeConfirmTitle`/`invite.revokeConfirmBody`, confirm
    button `invite.revokeConfirmConfirm` as `btn-error`, cancel button reusing the existing
    `delete.cancel` key verbatim — no new cancel string). On confirm: call `revokeInvite(id)` (from
    `lib/invite/api.ts`), then clear the stored `{ url, expiresAt }` state so the panel reverts to
    Task 1's create form — no list, no history, matching CONTEXT.md's explicit boundary.

    Add tests: `generated_invite_replaces_form_with_link_display`,
    `copy_button_calls_copyWithAutoClear_then_showCopyToast_with_invite_link_field_label`,
    `copy_button_has_accessible_name_via_aria_label_not_visible_text`,
    `revoke_confirm_reverts_panel_to_create_form_with_no_history`,
    `revoke_button_always_carries_a_visible_label`.
  </action>
  <verify>
    <automated>npm --prefix web run test -- FamilyTab</automated>
  </verify>
  <acceptance_criteria>
    All new tests pass. `npm --prefix web run typecheck` passes. `npm --prefix web run test` (full
    suite) stays green — no regression in `SettingsPanel`/`GeneratorDialog`/`CopyToast` tests.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clipboard ↔ OS | the invite link's fragment is a decryption-capable secret — it must be treated with the SAME clipboard-clear discipline as a vault password, never a plain non-clearing copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-18 | Information Disclosure | FamilyTab.tsx (copy action) | medium | mitigate | uses `copyWithAutoClear` (the same auto-clearing clipboard write every password/TOTP copy in this app already uses), never a plain `navigator.clipboard.writeText` with no clear timer |
| T-24-19 | Tampering / honesty | FamilyTab.tsx (`invite.honestVisibilityNote`) | high | mitigate | rendered byte-for-byte from the dictionary; never implies the family owner loses read access to a shared folder — the opposite is stated explicitly |

</threat_model>

<verification>
```
npm --prefix web run test -- FamilyTab SettingsPanel
npm --prefix web run typecheck
npm --prefix web run test
```
</verification>

<success_criteria>
An owner can, with no family, create one, then generate a family-only or collection-scoped invite
link with a chosen expiry, copy it safely, and revoke it — all from one tab, matching the "minimal
owner-side invite UI" scope CONTEXT.md locked for this phase.
</success_criteria>

<output>
Create `.planning/phases/24-invitation-flow-no-smtp/24-07-SUMMARY.md` when done, with `## Threat Flags`
populated.
</output>
