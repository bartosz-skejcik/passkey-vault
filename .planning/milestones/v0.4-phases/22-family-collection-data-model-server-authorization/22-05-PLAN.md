---
phase: 22-family-collection-data-model-server-authorization
plan: 05
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03", "22-04"]
files_modified:
  - crates/pv-server/src/routes/mod.rs
  - crates/pv-server/tests/membership_route_sweep.rs
autonomous: true
requirements: [SEC-06, SHARE-05]

must_haves:
  truths:
    # --- Goal-backward (ROADMAP Phase 22 SC#2 — the headline deliverable) ---
    - "A route-sweep test proves no mutating family/collection/item endpoint is reachable by a caller who isn't a member of the target resource — membership_route_sweep_rejects_non_member_on_every_route iterates the SAME membership_routes() AND family_routes() tables that build the live router, AND a companion source-scan test (router_literal_routes_match_documented_allowlist) asserts every literal .route(...) call inside router_with_cors's own chain matches a documented, justified LITERAL_ROUTES_NOT_MEMBERSHIP_GATED / PRE_EXISTING_PERSONAL_SCOPE_ROUTES allowlist — so a route added outside both tables (bypassing the sweep) fails a test on the commit that adds it, closing the gap by which such a route could otherwise exist and merely go untested."
    - "Every entry in membership_routes() AND family_routes() is exercised by the sweep: a non-member caller gets 404 on every route except the documented insufficient-level cases (which get 403, since existence is not secret to a caller who provably has SOME access). Routes deliberately outside both tables (POST /api/families, the three /api/identity/* routes — all SessionUser-only, nothing to check membership against) are named in an explicit, commented SESSION_ONLY_ROUTES_NOT_SWEPT allowlist, whose cross-check now asserts BOTH that its paths are absent from the two swept tables AND that each one appears in the audited LITERAL_ROUTES_NOT_MEMBERSHIP_GATED constant — so an entry cannot be padded in to document a gap away; it must name a route the router genuinely registers, and that registration is itself accounted for."
    - "The server never calls pv_core::identity::{seal,unseal,unseal_collection_key} or pv_core::identity::unwrap_identity_secret_key or pv_core::items::{encrypt_item,decrypt_item,encrypt_item_for_collection,decrypt_item_for_collection} anywhere under crates/pv-server/src (excluding the ZK-audit test's own file, which necessarily names these strings to check for them) — verified by an automated grep gate that strips comment lines first (so Plans 22-02/22-03's mandated 'this module MUST NEVER call unseal' doc comments do not self-invalidate the gate) and matches bare identifiers as well as fully-qualified paths, not just code review (the zero-knowledge boundary this entire phase's design assumes)."
    # --- SEC-06 probe edges ---
    - "GET and mutating routes on the same resource type (e.g. GET /api/vault/collections/{id} and POST /api/vault/collections/{id}/members) both go through Membership<Collection,_> with identical resolve_access logic — the sweep test asserts both method families reject a non-member identically, closing the CVE-2026-43639 asymmetric-check class by direct proof, not by code-review assertion alone."
    - "The route-sweep test asserts a non-zero, minimum expected count of registered routes in BOTH membership_routes() and family_routes() (a cardinality tripwire on each table) — the sweep cannot vacuously pass by having nothing to sweep, and a FamilyMembership-gated route added as a stray .route() call (bypassing family_routes()) trips the family_routes() tripwire the moment someone tries to add it correctly, or is caught by the sweep's own SESSION_ONLY_ROUTES_NOT_SWEPT cross-check if it's misfiled as session-only."
    - "The order routes appear in membership_routes()/family_routes() has no effect on which routes are authorization-gated — every entry receives the identical Membership/FamilyMembership extraction regardless of position in the table (structurally true since each table is a plain Vec folded uniformly; the sweep test itself iterates each Vec in whatever order Rust yields it and still expects every entry to reject a non-member, proving no positional dependency)."
    - "Concurrent requests to DIFFERENT membership-gated routes for the same caller are each independently authorized — no shared/cached authorization context leaks from one route's check into another within the same request batch (structurally true since each Membership/FamilyMembership resolution is a fresh, independent DB query per Plan 22-01's design; the sweep test's own use of a shared axum::Router clone across many oneshot() calls in sequence is itself evidence no per-request mutable authorization state exists on AppState)."
  artifacts:
    - crates/pv-server/tests/membership_route_sweep.rs
    - crates/pv-server/src/routes/mod.rs (membership_routes() and family_routes() finalized)
  key_links:
    - "membership_routes() and family_routes() together are now the single source of truth for every family/collection/item mutating/access-sensitive route across Plans 22-01 through 22-04 — this plan adds no new production routes, only the sweep test and the cardinality tripwires."
    - "The ZK grep audit (Task 2) is the permanent, automated version of the ad-hoc grep spot-checks Plans 22-01/22-02/22-04 already ran manually — it self-excludes its own file and strips comment lines before matching, so it does not fail on itself or on the mandated 'never call unseal' doc comments in identity.rs/collections.rs."
  prohibitions:
    - statement: "MUST NOT log or trace the raw byte contents of any sealed/wrapped blob (wrapped_secret_key, sealed_key, enc_key, enc_data) at any log level across the whole crates/pv-server/src tree — a tracing::error!/tracing::warn! call site may reference an id, a table name, or an error kind, never interpolate the blob field's value."
      verification: backstop
---

<objective>
Close the phase with its headline security proof: a route-sweep test that iterates the exact
SAME two tables (`membership_routes()` and `family_routes()`) that build the live router, backed
by a companion source-scan test (`router_literal_routes_match_documented_allowlist`) asserting
every literal `.route(...)` call in the router's own chain matches a documented, justified
allowlist — so a route added outside either table fails a test on the commit that adds it, not
merely a review-level convention — plus the automated, permanent version of the zero-knowledge
grep audit every prior plan in this phase ran ad hoc.

Purpose: SEC-06 and SHARE-05 exist to guarantee uniform enforcement across the WHOLE
family/collection/item surface, and that guarantee is only real once every route built across
Plans 22-01–22-04 — across BOTH tables — is swept in one place — this plan is where that promise
becomes falsifiable.
Output: `tests/membership_route_sweep.rs`, cardinality tripwires on `membership_routes()` AND
`family_routes()`, the `router_literal_routes_match_documented_allowlist` structural backstop, and
a permanent automated zero-knowledge boundary check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-family-collection-data-model-server-authorization/22-CONTEXT.md
@.planning/phases/22-family-collection-data-model-server-authorization/22-RESEARCH.md
@.planning/phases/22-family-collection-data-model-server-authorization/22-VALIDATION.md

@crates/pv-server/src/routes/mod.rs
@crates/pv-server/tests/common/mod.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The route-sweep test (SC#2 headline deliverable) + cardinality tripwire</name>
  <files>crates/pv-server/src/routes/mod.rs, crates/pv-server/tests/membership_route_sweep.rs</files>
  <read_first>crates/pv-server/src/routes/mod.rs (the full, now-finalized `membership_routes()` AND `family_routes()` functions built incrementally across Plans 22-01/22-02/22-03/22-04 — read it in its CURRENT state, do not assume the entries described in earlier plans' actions are the final list; enumerate what is actually there in BOTH tables); .planning/phases/22-family-collection-data-model-server-authorization/22-RESEARCH.md "Pattern 4" (the exact sweep design this task implements, including its "honest limitation" callout about the count tripwire).</read_first>
  <behavior>
    - For every `(path, MethodRouter)` entry in BOTH `membership_routes()` AND `family_routes()`,
      substitute a real-but-unrelated resource id (a resource that genuinely exists in the DB,
      created by a DIFFERENT family/user than the caller) into each `{id}`/`{user_id}` path
      placeholder, fire the request with an authenticated-but-unrelated caller's bearer token, and
      assert the response is `404` — UNLESS the route is in a small, explicitly documented
      exception list of "insufficient level, not no-access" cases, which instead assert `403`.
    - For EACH entry, at least one HTTP method actually produced a real 404/403 assertion (i.e.
      was NOT skipped as `405 Method Not Allowed` for every method attempted) — an entry whose id
      substitution is wrong and 404s/405s on every method for the wrong reason must not be able to
      silently contribute zero real assertions while the outer test still passes (W2).
    - `membership_routes().len()` AND `family_routes().len()` are each asserted against a literal
      expected count in a `mod.rs` `#[cfg(test)]` unit test — the tripwire that fails loudly (wrong
      count, not silently passing a now-smaller sweep) if a future phase adds a route to either
      table without updating this assertion, or removes one without noticing.
    - `POST /api/families` and the three `/api/identity/*` routes (all `SessionUser`-only, per
      Plans 22-01/22-02 — none are `Membership`/`FamilyMembership`-gated) are named in an explicit,
      commented allowlist constant in the sweep test, documenting why each is legitimately excluded
      rather than leaving that a silent gap.
  </behavior>
  <action>
    In `mod.rs`, add a `#[cfg(test)] mod tests` assertion (in the SAME `#[cfg(test)] mod tests`
    block that already exists in this file for CORS unit tests, or a new one if that block is
    scoped elsewhere) named `membership_routes_table_has_expected_cardinality`: read the actual
    current `membership_routes().len()` AND `family_routes().len()` by running each once at the
    top of this task (do not guess either number from prior plans' descriptions — count the real
    entries in BOTH tables after reading the file's current state, since Plans
    22-01/22-02/22-03/22-04 populated them incrementally), assert `membership_routes().len() ==
    <that literal count>` AND `family_routes().len() == <that other literal count>` in the SAME
    test function, with a comment on EACH assertion stating "bump this literal AND extend
    tests/membership_route_sweep.rs's per-route id substitution when adding a new
    membership-gated (or family-gated) route" — this is the tripwire RESEARCH.md's Pattern 4
    "honest limitation" section calls for: it does not prevent a route being registered OUTSIDE
    either table via a stray literal `.route()` call elsewhere, but it does catch either table's
    own cardinality silently drifting (this closes the B2-class gap where a `FamilyMembership`-
    gated route registered as a stray `.route()` would otherwise be invisible to both the sweep
    AND any tripwire).

    Create `tests/membership_route_sweep.rs`. Set up TWO disjoint fixtures in the test: FAMILY-A
    (an owner + a `hidden_password`-level collection member, plus a personal item, a collection,
    an item share — enough real resource ids to substitute into every path shape in either table)
    and an entirely unrelated FAMILY-B-less user U (registered, logged in, belongs to NO family,
    has no keypair, no access to anything FAMILY-A owns). Add
    `const SESSION_ONLY_ROUTES_NOT_SWEPT: &[&str] = &["POST /api/families", "PUT
    /api/identity/keypair", "GET /api/identity/keypair", "POST
    /api/identity/verify/{user_id}"];` near the top of the file with a doc comment explaining why:
    these are deliberately `SessionUser`-only (Plans 22-01/22-02) — `POST /api/families` has
    nothing to check membership against yet (creating the family establishes the caller's own
    membership); the identity routes are about the caller's OWN keypair/verification record, never
    a shared resource — so neither table contains them and this sweep does not exercise them. Give
    the constant real teeth, not just documentation value: assert that none of its bare paths
    (strip the leading HTTP verb) appear as a key in either `membership_routes()` or
    `family_routes()` — if a future plan accidentally moves one of these into a swept table
    without updating this constant, or vice versa, this assertion catches the drift.

    Iterate `pv_server::routes::membership_routes()` THEN `pv_server::routes::family_routes()`
    (make BOTH functions `pub(crate)` visible enough for the integration test crate to call — if
    the current visibility is `pub(crate)` and integration tests in `tests/` cannot see
    `pub(crate)` items from the library crate, widen BOTH to `pub` with a doc comment explaining
    why: `tests/membership_route_sweep.rs` is the reason they must be externally visible, not a
    general API surface widening). For each `(path, method_router)` entry from EITHER table,
    substitute FAMILY-A's real resource id(s) into every `{...}` placeholder in `path` (write a
    small local `fn substitute(path: &str, ids: &TestIds) -> String` mapping each known
    placeholder name — `{id}`, `{user_id}` — to the right fixture id per route, since different
    routes' `{id}` means different resource kinds; build this mapping explicitly rather than
    guessing generically), fire the request as user U (unrelated, zero access) against every HTTP
    method the entry's `MethodRouter` actually serves (GET/POST/PUT/DELETE — probe by attempting
    each and skipping methods that return `405 Method Not Allowed`, which signals "this route
    doesn't serve that verb" rather than being a sweep failure), and assert `404` UNLESS the route
    is in a small `const INSUFFICIENT_LEVEL_EXCEPTIONS: &[&str]` list (paths where U genuinely
    HAS some access via a seeded fixture and the correct response is `403` — document each
    exception inline with why it's there; if no such case is deliberately seeded, this constant is
    empty and every route asserts `404`). For EACH entry, track whether at least one method
    attempt actually produced a 404/403 assertion (i.e. was NOT skipped as `405` for every method
    tried); if zero methods were exercised for an entry, panic naming that entry's path — this is
    the W2 fix: an entry whose id substitution is wrong (e.g. a typo'd fixture id causing every
    method to 404-for-the-wrong-reason or 405) must not be able to silently contribute zero real
    assertions to an otherwise-green test. Name the test function
    `membership_route_sweep_rejects_non_member_on_every_route` (exact name from
    22-VALIDATION.md). Also add a second assertion inside the same test (or a sibling test) that
    at least one GET-family and at least one mutating-family (POST/PUT/DELETE) route were BOTH
    actually exercised — i.e. the sweep did not vacuously only test one HTTP-method shape — this
    is the SEC-06 "GET/mutating symmetry" probe-edge proof.
  </action>
  <verify>
    <automated>cargo test -p pv-server --lib membership_routes_table_has_expected_cardinality 2>&1 | grep -E "test result: ok\. 1 passed" && cargo test -p pv-server --test membership_route_sweep membership_route_sweep_rejects_non_member_on_every_route 2>&1 | grep -E "test result: ok\. 1 passed"</automated>
    <!-- NOTE: these two tests live in DIFFERENT cargo test binaries — the cardinality test in the
         lib target's `#[cfg(test)] mod tests`, the sweep in `tests/membership_route_sweep.rs`.
         `cargo test` prints one `test result: ok. N passed` line PER BINARY and never an aggregate,
         so a single combined invocation grepping for `ok. 2 passed` can never match even when both
         tests pass. Split per binary, each pinned to `1 passed`. Do not recombine these. -->
  </verify>
  <done>membership_route_sweep_rejects_non_member_on_every_route and membership_routes_table_has_expected_cardinality both pass, proving uniform enforcement across every registered route in both membership_routes() and family_routes().</done>
  <acceptance_criteria>
    - Both named tests pass.
    - The sweep test iterates `pv_server::routes::membership_routes()` AND `pv_server::routes::family_routes()` directly (grep-verifiable: `grep -n "membership_routes()\|family_routes()" crates/pv-server/tests/membership_route_sweep.rs` shows real calls to both, not a hand-copied literal path list).
    - At least one route asserts `404` (no-access) and, if any `INSUFFICIENT_LEVEL_EXCEPTIONS` entries exist, at least one asserts `403` — both branches are exercised, not just one.
    - `SESSION_ONLY_ROUTES_NOT_SWEPT` names `POST /api/families` and the three `/api/identity/*` routes with inline rationale, and the test asserts none of them appear as a key in either table.
    - Every swept entry has at least one method attempt that produced a real (non-skipped) 404/403 assertion — a zero-assertion entry panics with the offending path named.
    - `cargo test -p pv-server` full crate suite remains green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Zero-knowledge boundary audit (permanent automated gate) + literal-route allowlist audit (structural SC#2 backstop) + full phase-gate run</name>
  <files>crates/pv-server/src/routes/mod.rs, crates/pv-server/tests/membership_route_sweep.rs</files>
  <read_first>crates/pv-server/src/routes/mod.rs (this plan's own Task 1 output — the file this task adds a second `#[cfg(test)]` test to); crates/pv-server/src/routes/identity.rs, crates/pv-server/src/routes/collections.rs (both already carry a manual "never call seal/unseal" doc-comment claim from Plans 22-02/22-03 — this task turns that claim into an automated, permanent check rather than a one-time spot-check); crates/pv-server/tests/membership_route_sweep.rs (this plan's own Task 1 output — the file whose `SESSION_ONLY_ROUTES_NOT_SWEPT` cross-check this task extends to assert bidirectional teeth against the new `LITERAL_ROUTES_NOT_MEMBERSHIP_GATED` constant).</read_first>
  <action>
    Add a `#[cfg(test)]` test (co-located in `mod.rs`'s existing test module, named
    `pv_server_never_calls_pv_core_seal_or_unseal_or_decrypt`) that walks every `.rs` file under
    `crates/pv-server/src/` (via `std::fs::read_dir` recursively — a small local helper, no new
    dependency needed since `walkdir` is not pinned), with TWO defenses against false
    positives/negatives a naive version does not have:

    (1) **Self-exclusion.** The walker MUST skip `crates/pv-server/src/routes/mod.rs` itself — the
    file this very test lives in. Its own assertion logic necessarily contains the forbidden
    substrings as plain `&str` literals (it has to name them to check for them), so without this
    exclusion the test always matches itself and always fails. `mod.rs`'s actual production code is
    route-table wiring only and calls no `pv_core::identity`/`pv_core::items` function directly, so
    excluding the whole file from the scan does not reduce real coverage — comment this rationale
    at the exclusion site.

    (2) **Comment-line stripping.** Before matching, skip any line whose trimmed content starts
    with `//` (covers `//`, `///`, `//!`). This is required because Plans 22-02/22-03 mandate
    module doc comments on `identity.rs`/`collections.rs` that DELIBERATELY spell out the forbidden
    call names in prose (e.g. "this module MUST NEVER call `pv_core::identity::{seal, unseal,
    unseal_collection_key}`") — without stripping comments first, the broadened bare-identifier
    matching in (b) below would fail the very doc comments those plans required as documentation of
    the constraint, which is the opposite of the intended signal.

    For each remaining (non-comment) line of each remaining (non-self) file, assert none contain:
    (a) the fully-qualified substrings `pv_core::identity::seal`, `pv_core::identity::unseal`,
    `pv_core::items::encrypt_item`, `pv_core::items::decrypt_item` (plain substring `.contains()` —
    deliberately broad so the shared `encrypt_item`/`decrypt_item` prefix also catches the
    `_for_collection` variants); AND (b) the bare identifiers `seal`, `unseal`,
    `unseal_collection_key`, `unwrap_identity_secret_key` matched as whole Rust identifiers (a
    small local `fn contains_identifier(line: &str, needle: &str) -> bool` doing manual
    word-boundary checks — the char immediately before/after each match, if any, must not be
    `[A-Za-z0-9_]`; no `regex` crate needed, none is pinned in `pv-server`'s `Cargo.toml` — this
    catches `use pv_core::identity::{seal, unseal};` grouped imports and `use pv_core::identity as
    id; id::unseal(...)` aliased calls, neither of which contain the fully-qualified substrings in
    (a)). Panic with a message naming the offending file and line if any match is found — this is
    the permanent, automated version of the ad-hoc `grep` spot-checks Plans 22-01/22-02/22-04
    already ran manually against `identity.rs`/`collections.rs` individually; this task's version
    covers the WHOLE `src/` tree (minus the one self-excluded file), including any future addition,
    not just the two files known today.

    **BLOCKER fix — make SC#2's guarantee structural, not conventional.** Add a second
    `#[cfg(test)]` test to the same test module, named `router_literal_routes_match_documented_allowlist`.
    Refactor the small local file-reading + comment-stripping step this task's ZK-boundary test
    already builds into a standalone fn (e.g. `fn non_comment_lines(path: &Path) -> Vec<String>`)
    callable from both tests — this new test calls it pointed at exactly ONE file,
    `crates/pv-server/src/routes/mod.rs` itself (a single-file read, no directory walk needed).
    From the non-comment lines of `mod.rs`, extract every literal route path registered inside
    `router_with_cors`'s body into a `HashSet<String>`. No `regex` crate — mirror this task's
    ZK-boundary test's no-new-dependency discipline.

    **CRITICAL — do NOT scan for the literal substring `.route("`.** rustfmt wraps any `.route(...)`
    call whose line exceeds the width, putting `.route(` at end-of-line and the path literal on the
    NEXT line, so `.route("` never occurs as a contiguous substring. `crates/pv-server/src/routes/mod.rs`
    ALREADY contains exactly one such wrapped registration today (`/api/extension-passkeys`, currently
    at ~line 71), and a new collection/item route with a long path plus two chained handlers is
    precisely the shape rustfmt wraps. A naive `.route("` scan therefore (a) goes red on its very
    first run, whose cheapest "fix" is deleting the allowlist entry — reintroducing the silent hole
    this test exists to close — and (b) lets any future wrapped stray route escape entirely,
    reverting SC#2 to convention.

    Instead: **join `router_with_cors`'s non-comment lines into one string before scanning**, then
    find each `.route(` occurrence and skip arbitrary whitespace/newlines before the opening `"`.
    Add two companion assertions in the same test so the scan cannot be silently out-resolved:
      - every `.route(` occurrence in the body is followed (modulo whitespace) by a string literal —
        this catches `.route(SOME_CONST, ...)`, which would otherwise register a real route while
        contributing nothing to the scanned set;
      - the body contains no `.nest(` or `.merge(` other than the two documented
        `membership_routes()` / `family_routes()` folds — so a whole sub-router cannot be attached
        below the scan's resolution.
    (`.fallback_service(serve)` at the static-file branch is NOT an API route and is expected —
    exclude it explicitly by name rather than by pattern.)

    Add two new module-level `pub const` slices in `mod.rs` — deliberately NOT `#[cfg(test)]`-gated,
    because they must be visible to the separate `tests/membership_route_sweep.rs` integration-test
    crate, and `#[cfg(test)] mod tests` items in this crate's lib target are NOT compiled into the
    build that integration tests link against (mirror the exact same `pub` visibility widening this
    plan's Task 1 already applies to `membership_routes()`/`family_routes()` for the identical
    reason):

    - `pub const LITERAL_ROUTES_NOT_MEMBERSHIP_GATED: &[&str]` — every literal `.route(...)` path
      in `router_with_cors` that predates this phase's authorization model or is deliberately
      session-scoped only (`healthz`, the `auth`/`session`/`sync` routes, and the same
      `POST /api/families` + `/api/identity/*` paths `SESSION_ONLY_ROUTES_NOT_SWEPT` names — list
      them here too, since this constant is now the complete audited set; `SESSION_ONLY_ROUTES_NOT_SWEPT`'s
      job becomes naming the subset that also needs the family/collection-adjacent doc comment, not
      being independently exhaustive). Doc-comment it: "adding an entry here requires a written
      justification for why that path is not family/collection/item-scoped — a stray mutating route
      added directly to `router_with_cors`'s chain instead of through `membership_routes()`/
      `family_routes()` trips this test on the very commit that adds it."
    - `pub const PRE_EXISTING_PERSONAL_SCOPE_ROUTES: &[&str]` — `POST /api/vault/items`,
      `GET /api/vault/items`, and every literal `folders` route already registered outside
      `membership_routes()`/`family_routes()` before this phase (enumerate the actual current
      entries by reading `mod.rs`'s current chain — do not guess the list). Doc-comment it: these
      predate Phase 22 entirely and are scoped by `SessionUser`-based ownership checks inside their
      own handlers, not by `Membership<R,M>`; listed here so the allowlist audit is complete rather
      than partial (closes the checker's specific "vault/items and folders currently unnamed" gap).

    The test asserts the scanned `HashSet` of literal route paths equals the union of
    `LITERAL_ROUTES_NOT_MEMBERSHIP_GATED` and `PRE_EXISTING_PERSONAL_SCOPE_ROUTES` exactly
    (`assert_eq!` on the two sets, panicking with the symmetric difference named on failure) — a
    route added to `router_with_cors` without a corresponding allowlist entry fails this test
    immediately, and a route removed from `router_with_cors` without removing its now-stale
    allowlist entry also fails it (both directions covered).

    Then, in `tests/membership_route_sweep.rs` (Task 1's file), extend `SESSION_ONLY_ROUTES_NOT_SWEPT`'s
    existing cross-check (which today only asserts its paths are absent from `membership_routes()`/
    `family_routes()`) to ALSO assert every one of its bare paths appears in
    `pv_server::routes::LITERAL_ROUTES_NOT_MEMBERSHIP_GATED` — this closes the "append a fictional
    exclusion to document the gap away" escape: an entry can no longer be padded into
    `SESSION_ONLY_ROUTES_NOT_SWEPT` unless the router actually registers it as a literal route AND
    that registration is itself accounted for in the audited allowlist.

    With `router_literal_routes_match_documented_allowlist` and the extended
    `SESSION_ONLY_ROUTES_NOT_SWEPT` cross-check both in place, this plan's frontmatter
    `must_haves.truths` entries under the SC#2 headline comment are now true as written (already
    updated in this plan's own frontmatter to name the mechanism precisely), and `T-22-21` in the
    threat_model below is updated to name the automated backstop instead of code review.

    **Bonus (optional — do only if it stays clean, skip if it complicates the scan):** now that
    `router_literal_routes_match_documented_allowlist` gives this file a reliable non-comment-line
    scan of `mod.rs`, narrow the ZK-boundary test's self-exclusion from "skip the whole file
    `routes/mod.rs`" down to "skip only the `#[cfg(test)] mod tests` module's own line range within
    `routes/mod.rs`" — production code in `mod.rs` (route wiring) is then genuinely scanned for
    `seal`/`unseal` calls rather than blanket-excluded, closing the documented blind spot the
    checker flagged as an info-level bonus.

    As the final acceptance step for the whole phase (not a new code artifact — a verification
    action), run the full gate: `cargo test --workspace` and
    `bash scripts/check-supply-chain.sh`, both must exit 0. Record in this task's own commit
    message (not in `.planning/REQUIREMENTS.md` — that update is the orchestrator's
    `phase.complete` step's job, not this plan's) that KEY-01/KEY-02 remain `Partial` in
    `REQUIREMENTS.md` per the documented tooling hazard (`phase.complete` auto-checks every
    requirement mapped to a phase; a human/orchestrator step must re-assert the `Partial` status
    on KEY-01/KEY-02 after this phase closes, since KEY-01/KEY-02's FULL completion also needs
    Phase 25's rewrap-only-on-removal clause).
  </action>
  <verify>
    <automated>cargo test -p pv-server --lib pv_server_never_calls_pv_core_seal_or_unseal_or_decrypt router_literal_routes_match_documented_allowlist 2>&amp;1 | grep -E "test result: ok\. 2 passed" &amp;&amp; cargo test --workspace &amp;&amp; bash scripts/check-supply-chain.sh</automated>
  </verify>
  <done>The whole-tree zero-knowledge boundary test AND the literal-route allowlist audit both pass; SESSION_ONLY_ROUTES_NOT_SWEPT's cross-check is bidirectional; cargo test --workspace and bash scripts/check-supply-chain.sh both exit 0 — the phase's full gate is green, and SC#2's guarantee is now structural, not conventional.</done>
  <acceptance_criteria>
    - The new automated ZK-boundary test passes.
    - `router_literal_routes_match_documented_allowlist` passes and asserts set equality in both directions (a route added to `router_with_cors` without a matching allowlist entry fails it; a stale allowlist entry for a route no longer registered also fails it).
    - `LITERAL_ROUTES_NOT_MEMBERSHIP_GATED` and `PRE_EXISTING_PERSONAL_SCOPE_ROUTES` are declared as plain `pub const` (not `#[cfg(test)]`-gated) — grep-verifiable: `grep -n "pub const LITERAL_ROUTES_NOT_MEMBERSHIP_GATED\|pub const PRE_EXISTING_PERSONAL_SCOPE_ROUTES" crates/pv-server/src/routes/mod.rs` shows both outside any `#[cfg(test)]` block.
    - `tests/membership_route_sweep.rs`'s `SESSION_ONLY_ROUTES_NOT_SWEPT` cross-check now asserts BOTH "absent from `membership_routes()`/`family_routes()`" AND "present in `LITERAL_ROUTES_NOT_MEMBERSHIP_GATED`" — grep-verifiable: `grep -n "LITERAL_ROUTES_NOT_MEMBERSHIP_GATED" crates/pv-server/tests/membership_route_sweep.rs` matches.
    - `cargo test --workspace` exits 0.
    - `bash scripts/check-supply-chain.sh` exits 0.
    - The ZK-boundary test genuinely scans every file under `crates/pv-server/src/` (not a fixed, hand-picked file list) other than its own single documented self-exclusion (`routes/mod.rs`, or the narrower test-module-only exclusion if the optional bonus is taken) — grep-verifiable: the test's own source contains a directory-walk, not a literal `Vec<&str>` of filenames.
    - The ZK-boundary test strips comment lines (`//`/`///`/`//!`) before matching, so `identity.rs`'s and `collections.rs`'s mandated "MUST NEVER call unseal" doc comments do not fail the test.
    - The test matches bare identifiers (e.g. `\bunseal\b`-equivalent word-boundary check), not only fully-qualified `pv_core::identity::...` substrings, so a grouped or aliased import cannot evade it.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts This Plan Produces

- **Test:** `crates/pv-server/tests/membership_route_sweep.rs` — `membership_route_sweep_rejects_non_member_on_every_route`, iterating both `membership_routes()` and `family_routes()`, plus `SESSION_ONLY_ROUTES_NOT_SWEPT` (now bidirectionally cross-checked against `LITERAL_ROUTES_NOT_MEMBERSHIP_GATED`).
- **Test:** `crates/pv-server/src/routes/mod.rs`'s `#[cfg(test)]` module — `membership_routes_table_has_expected_cardinality` (asserts both `membership_routes().len()` and `family_routes().len()`), `pv_server_never_calls_pv_core_seal_or_unseal_or_decrypt` (self-excludes `mod.rs`, strips comment lines, matches bare identifiers), `router_literal_routes_match_documented_allowlist` (scans `mod.rs`'s own literal `.route(...)` calls and asserts the set equals `LITERAL_ROUTES_NOT_MEMBERSHIP_GATED` ∪ `PRE_EXISTING_PERSONAL_SCOPE_ROUTES` exactly — the structural backstop for SC#2).
- **Constants:** `pub const LITERAL_ROUTES_NOT_MEMBERSHIP_GATED: &[&str]` and `pub const PRE_EXISTING_PERSONAL_SCOPE_ROUTES: &[&str]` in `crates/pv-server/src/routes/mod.rs` (plain `pub`, not `#[cfg(test)]`-gated — visible to `tests/membership_route_sweep.rs`'s cross-check).
- **Visibility change (if needed):** `membership_routes()` AND `family_routes()` widened from `pub(crate)` to `pub` (documented reason: the sweep integration test).
</artifacts>

<threat_model>
## Trust Boundaries — ASVS Level 1

| Boundary | Description |
|----------|--------------|
| Every membership-gated route, in aggregate | The phase's whole security promise is only real once every route from Plans 22-01–22-04, across BOTH `membership_routes()` and `family_routes()`, is swept together in one place |
| `pv-server` source tree -> `pv-core::identity`/`pv-core::items` | The zero-knowledge boundary — must remain a one-way call direction (server calls INTO pv-core for opaque storage helpers only, never for seal/unseal/encrypt/decrypt) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-21 | Elevation of Privilege | A membership-gated or family-gated route registered outside `membership_routes()`/`family_routes()` | high | mitigate | The cardinality tripwire (Task 1) catches either table silently shrinking or growing unexpectedly; `router_literal_routes_match_documented_allowlist` (Task 2) is the automated backstop for the "route added via a stray literal `.route()` call" case RESEARCH.md's Pattern 4 names as an honest limitation — it asserts every literal `.route(...)` call in `router_with_cors` matches a documented, justified `LITERAL_ROUTES_NOT_MEMBERSHIP_GATED`/`PRE_EXISTING_PERSONAL_SCOPE_ROUTES` allowlist, so an undocumented stray route fails a test on the commit that adds it — not a review-level convention; `SESSION_ONLY_ROUTES_NOT_SWEPT`'s cross-check (extended this task) catches a route silently moving between "swept" and "session-only", and now also asserts each of its entries is itself a real, audited literal route rather than a fabricated exclusion |
| T-22-22 | Information Disclosure | Zero-knowledge boundary violation (a future handler accidentally calling `unseal`, including via a grouped or aliased import) | critical | mitigate | The automated whole-tree grep test (Task 2) makes this a permanent CI-enforced gate, matching bare identifiers and import lines (not only fully-qualified call paths) and self-excluding its own file — not a one-time manual check that erodes as the codebase grows |
| T-22-23 | Information Disclosure | Sensitive blob values reaching logs/traces | medium | mitigate | This plan's authored prohibition (backstop-verified) — no `tracing::*!` call site interpolates `wrapped_secret_key`/`sealed_key`/`enc_key`/`enc_data` field values anywhere in `crates/pv-server/src` |
| T-22-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new external packages this phase — `scripts/check-supply-chain.sh` re-run here as the phase-closing confirmation that nothing drifted |
</threat_model>

<verification>
- `cargo test --workspace` green.
- `bash scripts/check-supply-chain.sh` exits 0.
- Per-binary (these tests are split across the lib target and an integration target; `cargo test`
  prints one `test result` line per binary and never an aggregate — do not recombine into one
  invocation):
  - `cargo test -p pv-server --lib pv_server_never_calls_pv_core_seal_or_unseal_or_decrypt router_literal_routes_match_documented_allowlist 2>&1 | grep -E "test result: ok\. 2 passed"` passes.
  - `cargo test -p pv-server --test membership_route_sweep membership_route_sweep_rejects_non_member_on_every_route 2>&1 | grep -E "test result: ok\. 1 passed"` passes.
</verification>

<success_criteria>
- SEC-06/SHARE-05's uniform-enforcement guarantee is proven by a single, self-maintaining sweep test rather than asserted by convention.
- The zero-knowledge boundary is a permanent automated gate covering the whole `pv-server` source tree, not a per-plan spot-check.
- The full workspace test suite and supply-chain check are green — the phase's overall gate (per 22-VALIDATION.md's Sampling Rate: "Before /gsd-verify-work: full workspace suite green and bash scripts/check-supply-chain.sh exit 0") is satisfied.
</success_criteria>

<output>
Create `.planning/phases/22-family-collection-data-model-server-authorization/22-05-SUMMARY.md` when done
</output>
