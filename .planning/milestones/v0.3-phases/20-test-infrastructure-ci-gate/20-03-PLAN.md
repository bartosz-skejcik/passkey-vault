---
phase: 20-test-infrastructure-ci-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/e2e-firefox/ff-profile-prefs.cjs
  - extension/e2e-firefox/run-core.cjs
  - extension/e2e-firefox/run-server-unlock.cjs
  - extension/e2e-firefox/probe-request-xray.cjs
  - extension/e2e-firefox/probe-provider-corruption.cjs
autonomous: true
requirements: [QA-02]

must_haves:
  truths:
    - "A headed run of the 4 harness lanes listed in the pending todo (core, server-unlock, request-xray, provider-corruption) raises zero macOS system passkey-sheet dialogs, so a fully unattended run no longer hangs waiting on a human to dismiss OS UI"
    - "Rows that deliberately assert on native-fallthrough rejection still produce their expected (rejected/honest-failure) result under the suppressed-native-UI profile — the fix changes ONLY whether the OS sheet appears, not the ceremony's outcome"
  artifacts:
    - extension/e2e-firefox/ff-profile-prefs.cjs (new shared helper, single source of the suppression prefs)
  key_links:
    - "ff-profile-prefs.cjs's exported function -> each of the 4 harness files' firefox.Options setup, called between the existing xpinstall.signatures.required preference and the Builder().build() call"
    - "This plan's local-harness-only fix -> Plan 20-04's CI workflow, which never invokes any Firefox lane (Pitfall 4: hosted Linux runners cannot raise this dialog regardless — this fix is a genuine harness-quality deliverable, not a CI blocker)"
---

<objective>
Resolve `.planning/todos/pending/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md`: rows that deliberately fall through to native WebAuthn currently trigger the macOS system passkey sheet (iCloud Keychain/Touch ID) in harness-spawned Firefox profiles, requiring a human to manually dismiss it — breaking the unattended-run contract for the exact 4 files the todo names.

Per 20-RESEARCH.md's Pitfall 4, this is confirmed a **local-harness-only** concern (hosted GitHub Actions runners are headless Linux VMs that cannot raise this dialog, and Plan 20-04's CI never invokes any Firefox probe) — the fix is a genuine QA-02 harness-quality deliverable, not a CI-blocking dependency.

Purpose: every real-Firefox harness lane in the todo's file list is truly unattended-runnable (no OS dialog can hang it), closing the pending todo with `resolves_phase: 20`.
Output: a new shared preference-injection helper (`ff-profile-prefs.cjs`) wired into the 4 named harness files; the pending todo moved to `.planning/todos/resolved/` with a resolution note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared Firefox profile pref-injection helper, wired into all 4 named harness files</name>
  <files>extension/e2e-firefox/ff-profile-prefs.cjs, extension/e2e-firefox/run-core.cjs, extension/e2e-firefox/run-server-unlock.cjs, extension/e2e-firefox/probe-request-xray.cjs, extension/e2e-firefox/probe-provider-corruption.cjs</files>
  <read_first>
    .planning/todos/pending/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md (both candidate pref approaches and the "verify pref names against the running Firefox before landing" caveat)
    extension/e2e-firefox/run-core.cjs (the `const opts = new firefox.Options(); opts.addArguments('-profile', PROFILE_DIR); opts.setPreference('extensions.webextensions.uuids', ...); opts.setPreference('xpinstall.signatures.required', false); const driver = await new Builder()...build();` block — the exact insertion point, identical across all 4 files per this session's own grep confirmation)
    extension/e2e-firefox/README.md (Firefox version reference: 152.0.6, `PV_FIREFOX_BINARY` default path)
  </read_first>
  <action>
    Create `extension/e2e-firefox/ff-profile-prefs.cjs`, a small CommonJS module exporting one function (e.g. `applyNoNativeUiPrefs(opts)`) that calls `opts.setPreference(...)` for THREE prefs, applying both candidate approaches from the todo together (belt-and-suspenders — an unrecognized pref name is silently harmless in Firefox, so combining approaches maximizes suppression reliability across version drift without risk): `security.webauthn.enable_macos_passkeys` -&gt; `false`, `security.webauth.webauthn_enable_softtoken` -&gt; `true`, `security.webauth.webauthn_enable_usbtoken` -&gt; `false`. Include a header comment citing the resolved todo path and explaining why all 3 prefs are applied together rather than picking one.

    Wire the helper into all 4 files named in the todo's frontmatter (`run-core.cjs`, `run-server-unlock.cjs`, `probe-request-xray.cjs`, `probe-provider-corruption.cjs`): `require('./ff-profile-prefs.cjs')` near the top alongside the existing `firefox`/`selenium-webdriver` requires, then call the exported function on `opts` immediately after the existing `opts.setPreference('xpinstall.signatures.required', false);` line and before the `Builder().forBrowser('firefox').setFirefoxOptions(opts).build()` call — the same insertion point in all 4 files, since each already shares this identical block shape. Do not modify `run-autofill-capture.cjs` or `probe-window-geometry.cjs` — the todo's own frontmatter scopes this fix to exactly the 4 files listed, and 20-RESEARCH.md's inventory confirms these are the only two lanes never previously flagged as reaching a native-fallthrough row.
  </action>
  <acceptance_criteria>
    - `extension/e2e-firefox/ff-profile-prefs.cjs` exists, exports exactly one function, and sets exactly the 3 named preferences
    - All 4 named harness files `require()` the helper and call it at the specified insertion point (between `xpinstall.signatures.required` and `Builder().build()`)
    - `run-autofill-capture.cjs` and `probe-window-geometry.cjs` are unmodified
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./extension/e2e-firefox/ff-profile-prefs.cjs')" &amp;&amp; for f in run-core.cjs run-server-unlock.cjs probe-request-xray.cjs probe-provider-corruption.cjs; do grep -q "ff-profile-prefs" "extension/e2e-firefox/$f" || { echo "MISSING wiring in $f"; exit 1; }; done; echo OK</automated>
  </verify>
  <done>The helper module loads without a syntax error and all 4 named files reference it; `run-autofill-capture.cjs`/`probe-window-geometry.cjs` show no diff.</done>
</task>

<task type="auto">
  <name>Task 2: Live-Firefox proof (zero OS dialogs) and todo resolution</name>
  <files>.planning/todos/resolved/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md</files>
  <read_first>
    extension/e2e-firefox/README.md's "Prerequisites" section (the combined `PV_EXTENSION_ORIGINS` command covering all 4 lanes' pinned UUIDs — the exact server-startup command this task must run)
    .planning/todos/pending/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md's acceptance line ("a full run of all four lanes headed, with zero OS dialogs, and rows that assert on native-fallthrough rejection still produce their expected results")
  </read_first>
  <action>
    Start `pv-server` locally in the background with the combined `PV_EXTENSION_ORIGINS` value documented in README.md's Prerequisites section (covering all 4 lanes' pinned UUIDs). From `extension/`, run `npm run test:e2e:firefox:core`, `npm run test:e2e:firefox:server-unlock`, `npm run test:e2e:firefox:request-xray`, and `npm run test:e2e:firefox:provider-corruption` sequentially (each rebuilds Firefox via its own `pretest:*` hook first). Confirm each run completes and exits without hanging on an OS-level dialog, and that each script's own `results-*.json` output shows native-fallthrough rows reaching their expected honest-rejection/failure state (not blocked mid-ceremony). Stop the background `pv-server` process afterward.

    Create `.planning/todos/resolved/` (new directory, mirroring `.planning/debug/resolved/`'s existing convention) and move the todo file there, appending a `## Resolution` section recording: the 3 prefs applied, the 4 runs' pass/no-dialog confirmation, and `resolves_phase: 20` (already present in frontmatter — confirm it stays intact after the move).
  </action>
  <acceptance_criteria>
    - All 4 named lanes run to completion in one sequential pass with zero OS dialog hangs
    - Native-fallthrough rows in each lane's results still show their expected honest-rejection/failure outcome (mechanism proven unchanged by the pref fix)
    - `.planning/todos/pending/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md` no longer exists; `.planning/todos/resolved/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md` exists with a `## Resolution` section
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/todos/resolved/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md &amp;&amp; ! test -f .planning/todos/pending/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md &amp;&amp; grep -q "## Resolution" .planning/todos/resolved/2026-07-20-suppress-macos-passkey-sheet-in-firefox-harness.md &amp;&amp; echo OK</automated>
  </verify>
  <done>All 4 lanes ran headed, zero OS dialogs observed, native-fallthrough rows unaffected; the todo is relocated to `.planning/todos/resolved/` with a `## Resolution` section and `resolves_phase: 20` intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Harness-spawned Firefox profile (local dev machine only) | The 3 preferences this plan sets apply only inside throwaway `.ff-profile-*` directories created by these scripts — never the developer's real Firefox profile, never a packaged/shipped artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-20-03-01 | Denial of Service (of the harness itself, not the product) | Harness-spawned Firefox profile raising an unattended OS dialog | medium | mitigate | This plan's shared pref helper suppresses native WebAuthn platform UI in the 4 named throwaway profiles, closing the exact unattended-run hang the pending todo describes |
| T-20-03-02 | Tampering | Suppressing native WebAuthn UI could mask a genuine fallthrough-detection regression if applied to the WRONG rows | low | accept | Task 2's live proof explicitly re-confirms native-fallthrough rows still reach their expected honest-rejection outcome under the new profile prefs — the fix changes only dialog visibility, never ceremony logic; this is a test-harness-only file, never shipped in the extension package |
| T-20-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new package-manager dependencies — only a new local `.cjs` helper module and 4 one-line wiring edits to existing test-harness files |
</threat_model>

<verification>
`node -e "require('./extension/e2e-firefox/ff-profile-prefs.cjs')"` loads cleanly; all 4 named harness files reference the helper at the correct insertion point; a full headed run of the 4 lanes (Task 2) completes with zero OS dialogs and unchanged native-fallthrough outcomes.
</verification>

<success_criteria>
The pending todo's acceptance bar is met exactly: "a full run of all four lanes headed, with zero OS dialogs, and rows that assert on native-fallthrough rejection still produce their expected results." Todo moved to `.planning/todos/resolved/`.
</success_criteria>

<output>
Create `.planning/phases/20-test-infrastructure-ci-gate/20-03-SUMMARY.md` when done.
</output>
