---
phase: 17-shared-component-visual-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pv-ui/tokens.css
  - extension/lib/autofill/inpage-overlay.ts
  - extension/lib/autofill/inpage-theme.test.ts
autonomous: true
requirements: [DS-04, UX-01]

must_haves:
  truths:
    - "packages/pv-ui/tokens.css declares --pv-tile-bg/--pv-tile-fg inside the EXISTING `:root, [data-theme=\"vault-dark\"]` and `[data-theme=\"vault-light\"]` blocks (never new selector blocks), with UI-SPEC's exact OKLCH/var()/color-mix() values -- verified live through inpage-theme.ts's existing `:root`->`[data-theme]` rewrite with zero code changes to that file."
    - "extension/lib/autofill/inpage-overlay.ts's `.pv-row-icon-tile` background is `var(--pv-tile-bg)` (never a bare `var(--color-base-200)`) and `.pv-row-icon` carries a NEW `color: var(--pv-tile-fg)` declaration -- both Surface A (renderFieldDropdown) and Surface B (renderFormPrompt) inherit the fix through the single shared buildIconTile()/OVERLAY_CSS choke point, with zero change to buildIconTile()'s favicon-fetch JS logic or any other selector in OVERLAY_CSS."
    - "A missing/failed favicon still falls back to the neutral per-kind glyph tile on the same light-flipped background -- the existing FAILED_FAVICON_HOSTS/onerror fallback path is untouched by this CSS-only change (UX-01 empty-state coverage)."
  artifacts:
    - "packages/pv-ui/tokens.css: +2 custom properties (--pv-tile-bg, --pv-tile-fg) inside both existing [data-theme] blocks"
    - "extension/lib/autofill/inpage-overlay.ts: .pv-row-icon-tile background swap + .pv-row-icon new color declaration"
    - "extension/lib/autofill/inpage-theme.test.ts: +2 assertions proving the new tokens survive the shadow-DOM :root->[data-theme] rewrite"
  key_links:
    - "tokens.css's token placement inside the EXISTING [data-theme] blocks -> inpage-theme.ts's regex rewrite (`tokensCss.replace(/(^|\\})(\\s*):root\\s*,/gm, \"$1$2[data-theme],\")`) -> any stamped [data-theme] carrier inside a closed shadow root -- misplacing the tokens in a NEW selector block would silently make them invisible to every in-page surface, with no build error"
  prohibitions:
    - "No React import is added to extension/lib/autofill/inpage-overlay.ts"
    - "No crypto/wasm/argon2/chacha/hkdf/derive/decrypt/prf import is added to extension/lib/autofill/inpage-overlay.ts"
    - "generate-popover.ts and save-update-toast.ts are not touched by this plan -- neither renders an icon tile today (grep-verified in RESEARCH.md/UI-SPEC.md), so DS-04's scope stays exactly the two inpage-overlay.ts declarations named above"
    - "The pre-existing NordPass-measured, Bartek-approved non-token literals in the five overlay files are left byte-unchanged by this plan: .pv-list's max-height:250px, .pv-row's height:52px/border-radius:10px, .pv-header's height:60px, .pv-field-icon's border-radius:6px, and the equivalent per-file icon-frame/panel-width dimensions in generate-popover.ts/save-update-toast.ts/mismatch-modal.ts -- DS-04 covers only a color TOKEN that already exists and has drifted, never inventing a new token surface mid-phase"
    - "The five documented box-shadow/scrim rgba() elevation literals across all five overlay files are left byte-unchanged by this plan and constitute (together with the four 999px generic-pill radii below) the FULL approved-exception list Plan 17-04's overlay-wide audit asserts against: inpage-overlay.ts's `.pv-panel` box-shadow `rgba(0, 0, 0, 0.25)`; generate-popover.ts's popover box-shadow `rgba(0, 0, 0, 0.35)`; save-update-toast.ts's toast box-shadow `rgba(0, 0, 0, 0.35)`; mismatch-modal.ts's backdrop `background: rgba(0, 0, 0, 0.7)` and its own modal box-shadow `rgba(0, 0, 0, 0.45)` -- none of these has a pv-ui token equivalent today (they are semantically shadow/scrim elevation values, not design-token colors), so none is converted to var(--...) by this or any other Phase 17 plan"
    - "The four generic-pill `border-radius: 999px` occurrences across the overlay files (generate-popover.ts's `.pv-gen-meter-track` AND `.pv-gen-meter-fill` -- the strength-meter's track/background pill and its fill pill are two separate rules, both 999px; save-update-toast.ts's badge; mismatch-modal.ts's badge) are left byte-unchanged by this plan -- pv-ui has no generic-pill-radius token today, so DS-04 does not invent one mid-phase"
---

<objective>
Fix the one real bug UX-01 names -- `.pv-row-icon-tile`'s unconditional `background: var(--color-base-200)`, which renders a DARK tile in vault-dark (the exact inconsistency web's/popup's own `TILE_BG = "bg-base-200
[[data-theme=vault-dark]_&]:bg-zinc-100"` already avoids) -- by giving `packages/pv-ui/tokens.css` two new, theme-aware custom properties and pointing the in-page overlay's CSS at them. This is DS-04's primary (RESEARCH.md: "likely only") required fix, plus the one genuinely missing declaration (`.pv-row-icon` has no `color` property at all today, so the fallback type-glyph never got the matching light-tile flip either).

**Flagged planner assumption (DS-04, edge-coverage category "unclassified"), now backed by a
documented, executable approved-exception list:** the pre-existing NordPass-measured non-token
layout literals AND the five rgba() shadow/scrim elevation literals AND the four generic-pill
`border-radius: 999px` occurrences enumerated in this plan's `must_haves.prohibitions` are
DELIBERATELY EXCLUDED from this plan's own "no hand-copied constants" fix scope -- they have no
`pv-ui` token equivalent today, and UI-SPEC.md's own DS-04 sweep explicitly confirms every other
color/radius/border value in the five overlay files already references a token. This plan's own
grep-based verification (Task 2) is therefore scoped to exactly the two `.pv-row-icon-tile`/
`.pv-row-icon` declarations this plan fixes -- the FULL five-file, phase-wide proof that no
OTHER/undocumented hand-copied literal exists anywhere across all five overlay files (DS-04's
ROADMAP success criterion 3) is Plan 17-04 Task 1's job, which runs an executable audit against the
exact allowlist this plan's `must_haves.prohibitions` documents.

Purpose: close UX-01 (light tile parity across every in-page row surface) and DS-04's one real
finding, with the token value itself sourced from `pv-ui` rather than hand-copied into the overlay
(DS-04's whole point, per CONTEXT.md's locked token-drift policy).
Output: two new tokens in `packages/pv-ui/tokens.css`; the two-line CSS fix in
`extension/lib/autofill/inpage-overlay.ts`; two new regression assertions in
`extension/lib/autofill/inpage-theme.test.ts` guarding against a future accidental deletion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-shared-component-visual-alignment/17-CONTEXT.md
@.planning/phases/17-shared-component-visual-alignment/17-RESEARCH.md
@.planning/phases/17-shared-component-visual-alignment/17-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: --pv-tile-bg/--pv-tile-fg tokens + shadow-DOM regression assertions</name>
  <files>packages/pv-ui/tokens.css, extension/lib/autofill/inpage-theme.test.ts</files>
  <read_first>
    packages/pv-ui/tokens.css (full file, 65 lines -- the exact `:root, [data-theme="vault-dark"]` and `[data-theme="vault-light"]` block bodies these two tokens are inserted into)
    extension/lib/autofill/inpage-theme.test.ts (full file, 55 lines -- the existing hand-picked-token-value assertion pattern this task's 2 new tests follow)
    17-UI-SPEC.md "## Color" section, "Tile background/foreground tokens" subsection (the exact OKLCH/var()/color-mix() values and the "why new tokens, not a reuse" rationale)
  </read_first>
  <action>
    In packages/pv-ui/tokens.css, add exactly these two declarations to the END of the existing
    `:root, [data-theme="vault-dark"] { ... }` block body (after the existing `--border: 1px;`
    line, before the block's closing brace):
    `--pv-tile-bg: oklch(96.7% 0.001 286.375);` (Tailwind v4 zinc-100)
    `--pv-tile-fg: oklch(44.2% 0.017 285.786);` (Tailwind v4 zinc-600)
    And add exactly these two declarations to the END of the existing
    `[data-theme="vault-light"] { ... }` block body (after the existing comment, before the
    block's closing brace):
    `--pv-tile-bg: var(--color-base-200);`
    `--pv-tile-fg: color-mix(in oklch, var(--color-base-content) 70%, transparent);`
    Do not create any new selector block -- both insertions go inside the two blocks that already
    exist, which is what makes inpage-theme.ts's existing `:root`->`[data-theme]` rewrite pick them
    up automatically with zero code change to that file.
    Then add 2 new `it()` blocks inside inpage-theme.test.ts's existing
    `describe("INPAGE_THEME_CSS", ...)`, following the file's own established pattern (hand-picked
    token value asserted against the real processed `INPAGE_THEME_CSS` export, never a mocked
    string): one asserting `INPAGE_THEME_CSS` contains the literal
    `--pv-tile-bg: oklch(96.7% 0.001 286.375)` (the dark-block value); one asserting the
    `[data-theme="vault-light"]` block section of `INPAGE_THEME_CSS` contains both
    `--pv-tile-bg: var(--color-base-200)` and
    `--pv-tile-fg: color-mix(in oklch, var(--color-base-content) 70%, transparent)` (extract the
    vault-light block's own text via a regex on `INPAGE_THEME_CSS`, mirroring the file's existing
    `fontRuleMatch`-extraction style, rather than a bare whole-string `.toContain`, so a future
    accidental duplicate/misplacement in the WRONG block is also caught).
  </action>
  <acceptance_criteria>
    packages/pv-ui/tokens.css's two existing blocks each gain exactly 2 new lines, in the exact
    positions specified; no other line in the file changes. inpage-theme.test.ts gains exactly 2
    new `it()` blocks inside the existing describe, both passing against the real
    `INPAGE_THEME_CSS` export (not a stub).
  </acceptance_criteria>
  <verify>
    <automated>(cd extension && npx vitest run lib/autofill/inpage-theme.test.ts)</automated>
  </verify>
  <done>packages/pv-ui/tokens.css declares --pv-tile-bg/--pv-tile-fg with UI-SPEC's exact values inside both existing [data-theme] blocks; inpage-theme.test.ts's 2 new assertions pass against the real processed CSS text.</done>
</task>

<task type="auto">
  <name>Task 2: inpage-overlay.ts's .pv-row-icon-tile / .pv-row-icon CSS fix</name>
  <files>extension/lib/autofill/inpage-overlay.ts</files>
  <read_first>
    extension/lib/autofill/inpage-overlay.ts (the OVERLAY_CSS `.pv-row-icon-tile`/`.pv-row-icon` declarations, lines ~261-273, and buildIconTile()'s favicon-fetch logic at line ~436 -- confirm this task changes ONLY the two CSS declarations, never buildIconTile()'s JS)
    17-UI-SPEC.md "## In-Page Token Contract" and "## Surface Inventory" sections (the exact diff, and confirmation that Surface A's renderFieldDropdown() and Surface B's renderFormPrompt() both route through this same buildIconTile()/.pv-row-icon-tile choke point -- one CSS fix covers both)
    17-CONTEXT.md "In-page tile appearance & token-drift policy" locked decision
  </read_first>
  <action>
    Inside OVERLAY_CSS's `.pv-row-icon-tile { ... }` rule, change the existing
    `background: var(--color-base-200);` line to `background: var(--pv-tile-bg);` -- a value SWAP
    on an already-existing declaration. Do not touch any other property in this rule
    (width/height/border-radius/overflow/flex-shrink/display/align-items/justify-content stay
    identical).
    Inside OVERLAY_CSS's `.pv-row-icon { width: 16px; height: 16px; flex-shrink: 0; }` rule, ADD a
    new `color: var(--pv-tile-fg);` declaration -- this rule currently has zero color-related
    properties at all, so this is an ADDITION, not a swap (do not confuse it with the tile
    background swap above -- they are two different kinds of diff).
    Do not touch `.pv-header`, `.pv-icon-btn:hover`, or `.pv-row:hover`'s own separate, legitimate
    `var(--color-base-200)` uses elsewhere in OVERLAY_CSS -- DS-04's scope is exactly these two
    declarations inside `.pv-row-icon-tile`/`.pv-row-icon`, confirmed by UI-SPEC.md's own sweep as
    the only bug. Do not touch buildIconTile()'s favicon-fetch logic (`${origin}/favicon.ico`,
    `referrerPolicy = "no-referrer"`) at all -- this is a pure CSS value change.
  </action>
  <acceptance_criteria>
    The `.pv-row-icon-tile` rule's background is `var(--pv-tile-bg)`, with zero remaining
    `var(--color-base-200)` reference inside that specific rule body. The `.pv-row-icon` rule
    gains a `color: var(--pv-tile-fg)` declaration. No other selector in OVERLAY_CSS changes. No
    `import` line in the file references `"react"` or any crypto/wasm keyword.
  </acceptance_criteria>
  <verify>
    <automated>node -e '
const fs = require("fs");
const p = "extension/lib/autofill/inpage-overlay.ts";
const src = fs.readFileSync(p, "utf8");
const tileMatch = src.match(/\.pv-row-icon-tile\s*\{([^}]*)\}/);
const iconMatch = src.match(/\.pv-row-icon\s*\{([^}]*)\}/);
if (!tileMatch || !iconMatch) { console.error("SELECTOR NOT FOUND"); process.exit(1); }
const tileBlock = tileMatch[1];
const iconBlock = iconMatch[1];
const cssOk = tileBlock.includes("var(--pv-tile-bg)") && !tileBlock.includes("var(--color-base-200)") && iconBlock.includes("var(--pv-tile-fg)");
const importLines = src.split("\n").filter(function (l) { return l.trim().indexOf("import") === 0; });
const reactImport = importLines.some(function (l) { return /from\s+.react./i.test(l); });
const cryptoImport = importLines.some(function (l) { return /wasm|argon2|chacha|hkdf|derive|decrypt|prf/i.test(l); });
if (!cssOk || reactImport || cryptoImport) { console.error("FAIL", JSON.stringify({cssOk: cssOk, reactImport: reactImport, cryptoImport: cryptoImport})); process.exit(1); }
console.log("OK: pv-row-icon-tile/pv-row-icon reference the new tokens; no react/crypto import introduced");
' && (cd extension && npx vitest run lib/autofill/inpage-overlay.test.ts)</automated>
  </verify>
  <done>Both Surface A (renderFieldDropdown) and Surface B (renderFormPrompt) render their icon tile background from --pv-tile-bg and their fallback glyph color from --pv-tile-fg, proven by the scoped block-extraction check; the existing inpage-overlay.test.ts suite (structural DOM assertions) passes unchanged; no React or crypto import was introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Closed shadow-DOM content script (extension/lib/autofill/inpage-overlay.ts) vs. arbitrary third-party host page | The overlay runs inside an `all_urls` content script, `attachShadow({mode:"closed"})`; this plan makes a CSS-only value change inside that boundary, touching no message-passing or credential-handling surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-17-04 | Tampering | extension/lib/autofill/inpage-overlay.ts (architectural line: imperative/crypto-free/no-React) | high | mitigate | Task 2's automated verify greps every `^import` line for `"react"` and for crypto-surface keywords (wasm/argon2/chacha/hkdf/derive/decrypt/prf), asserting zero hits -- this CSS-only plan must not be the one that quietly reintroduces React or a crypto import into this file |
| T-17-05 | Information Disclosure | extension/lib/autofill/inpage-overlay.ts's buildIconTile() favicon fetch | medium | mitigate | This plan changes ONLY the two CSS declarations named in Task 2 -- buildIconTile()'s `${origin}/favicon.ico` direct-fetch + `referrerPolicy="no-referrer"` logic is explicitly out of scope and unread-from in the diff; the zero-knowledge favicon rule (never a third-party proxy, never routed through pv-server) is preserved by simply not touching that code path |
| T-17-06 | Tampering | packages/pv-ui/tokens.css token placement | medium | mitigate | Task 1's 2 new regression assertions in inpage-theme.test.ts prove the tokens live inside the EXISTING `:root`/`[data-theme]` blocks (not a new, unrewritten selector block) by asserting against the real `INPAGE_THEME_CSS` processed output, not the raw source file |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan |
</threat_model>

<verification>
`npx vitest run lib/autofill/inpage-theme.test.ts` and `npx vitest run lib/autofill/inpage-overlay.test.ts`
both green; the scoped block-extraction script confirms the exact two-declaration diff and zero
react/crypto import introduction.
</verification>

<success_criteria>
Both Surface A and Surface B's icon tiles read their background/fallback-glyph color from
packages/pv-ui's own tokens, matching web's/popup's own vault-dark light-flip -- proven by source
assertion, ready for Plan 17-04's visual screenshot confirmation.
</success_criteria>

## Artifacts This Phase Produces (this plan's slice)

**Created:** none (this plan modifies three existing files only).

**Modified:**
- `packages/pv-ui/tokens.css` -- +2 custom properties (`--pv-tile-bg`, `--pv-tile-fg`) inside each of the two existing `[data-theme]` blocks (4 new lines total)
- `extension/lib/autofill/inpage-overlay.ts` -- `.pv-row-icon-tile`'s `background` value swapped to `var(--pv-tile-bg)`; `.pv-row-icon` gains a new `color: var(--pv-tile-fg)` declaration
- `extension/lib/autofill/inpage-theme.test.ts` -- +2 `it()` assertions guarding the new tokens against future accidental deletion/misplacement

**Deleted:** none.

<output>
Create `.planning/phases/17-shared-component-visual-alignment/17-02-SUMMARY.md` when done
</output>
