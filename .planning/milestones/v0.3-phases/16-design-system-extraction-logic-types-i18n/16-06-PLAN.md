---
phase: 16-design-system-extraction-logic-types-i18n
plan: 06
type: execute
wave: 4
depends_on: ["16-02", "16-03", "16-04", "16-05"]
files_modified: []
autonomous: true
requirements: [DS-01, DS-02]

must_haves:
  truths:
    - "No parallel duplicate implementation of any migrated module (detectCardBrand, domainFromUrl/searchItems/filterItems, sortItems/byName, copyWithAutoClear/readClipboardSeconds/clampClipboardSeconds, normalizeItemFields, the closed-over `DICTIONARY[key][locale]` t()-body shape, resolveLocale) survives outside packages/pv-ui -- verified by a repo-wide grep across web/src and extension/lib, not assumed. (Phase success criterion 3, DS-01)"
    - "Both web and extension's full test suites (`npx vitest run`), both `tsc --noEmit`, web's `next build`, and extension's `wxt build` + `wxt build -b firefox` are all green with zero regressions after all 5 migration plans land -- proving every migrated pure function's existing empty/null/edge-case input behavior is preserved unchanged end-to-end, not just per-module. (Phase success criterion 1, DS-01/DS-02)"
    - "extension's two pre-existing structural guard tests (no_other_extension_file_hard_codes_a_server_url, no_ext_scoped_prf_strings_survive) still pass after all 5 migrations -- confirms the guards' node_modules-skip scoping correctly ignored the symlinked pv-ui content throughout the phase, per CONTEXT.md's own flagged risk."
  artifacts:
    - "N/A -- this plan creates no new artifacts; it verifies the artifacts produced by Plans 16-02 through 16-05."
  key_links:
    - "This plan is the phase's single closing gate for success criterion 3 (\"no parallel duplicate implementation... verified by search, not assumed\") -- every migrated symbol from every prior plan is re-checked here in aggregate, not just per-plan."
---

<objective>
Close the phase with the aggregate proof RESEARCH.md's own primary recommendation calls for: a
repo-wide grep confirming zero surviving duplicate implementations of any migrated symbol, plus
both consumers' full build+test+typecheck chains green end-to-end. This plan creates no new
source files -- it is a pure verification gate, run only after all 4 migration plans (types,
cardBrand+clipboard, i18n, search+sort) have landed, so it can check the FULL migrated surface at
once rather than each plan re-verifying only its own slice.

This directly closes DS-01's explicit success criterion 3 ("No parallel duplicate implementation
of any migrated module remains in web/ or extension/ -- verified by search, not assumed") and
DS-01/DS-02's success criterion 1 ("both test suites pass unchanged") in their strongest, most
literal form: an aggregate check across the whole phase, not an inference from 4 separate
per-plan checks.

Purpose: the phase's closing verification gate -- prove, not assume, that the migration is
complete and behavior-neutral.
Output: a clean grep report (zero non-shim hits for every migrated symbol) and a fully green
build+test+typecheck run on both consumers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-design-system-extraction-logic-types-i18n/16-CONTEXT.md
@.planning/phases/16-design-system-extraction-logic-types-i18n/16-RESEARCH.md
@.planning/phases/16-design-system-extraction-logic-types-i18n/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: repo-wide zero-duplication grep + structural guard re-run</name>
  <files></files>
  <read_first>
    .planning/phases/16-design-system-extraction-logic-types-i18n/16-RESEARCH.md (the "Code Examples" / grep-based verification section -- the exact grep pattern this task's verify step is derived from)
    .planning/phases/16-design-system-extraction-logic-types-i18n/16-PATTERNS.md ("Grep-based zero-duplication verification" section under "Shared Patterns")
  </read_first>
  <action>
    Run the repo-wide duplication grep for every migrated symbol from Plans 16-02 through 16-05,
    expecting zero hits outside packages/pv-ui: `function detectCardBrand`,
    `function domainFromUrl`, `function sortItems`, `function copyWithAutoClear`,
    `function normalizeItemFields`, `function resolveLocale`. Also grep for the literal old
    closed-over t()-body substring `DICTIONARY[key][locale]`, WORD-BOUNDARY-ANCHORED
    (`\bDICTIONARY\[key\]\[locale\]`) so it does not false-match
    `extension/lib/i18n/autofill-dictionary.ts`'s unrelated, intentionally-unchanged
    `AUTOFILL_DICTIONARY[key][locale]` line -- across web/src, extension/lib, AND packages/pv-ui
    this exact anchored pattern must now have ZERO hits anywhere (the new generic engine uses
    `dict[key][locale]`, a different substring, confirming both consumers' `t()` wrappers now
    delegate to `tEngine` instead of re-implementing the lookup locally, while
    `autofill-dictionary.ts`'s own separate, deliberately-untouched `t()` accessor is correctly
    excluded from this check). Then re-run extension's two structural guard tests explicitly,
    confirming neither fired across the full migration.
  </action>
  <verify>
    <automated>[ -z "$(grep -rn 'function detectCardBrand\|function domainFromUrl\|function sortItems\|function copyWithAutoClear\|function normalizeItemFields\|function resolveLocale' web/src extension/lib --include='*.ts' | grep -v 'packages/pv-ui')" ] && [ -z "$(grep -rnE '\bDICTIONARY\[key\]\[locale\]' web/src extension/lib packages/pv-ui --include='*.ts')" ] && (cd extension && npx vitest run entrypoints/background/server-config.test.ts entrypoints/__tests__/no-ext-scoped-prf-strings.test.ts)</automated>
  </verify>
  <done>Zero non-shim hits for all 6 function-definition greps outside packages/pv-ui; zero hits anywhere for the old closed-over t()-body literal, correctly excluding autofill-dictionary.ts's unrelated AUTOFILL_DICTIONARY[key][locale] line (confirms both dictionary.ts files now delegate to the shared engine); both structural guard tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: full build + test suite, both consumers</name>
  <files></files>
  <read_first>
    .planning/phases/16-design-system-extraction-logic-types-i18n/16-RESEARCH.md (Validation Architecture section -- the exact "Full suite command" this task runs)
  </read_first>
  <action>
    Run each consumer's full verification chain exactly as documented in this phase's RESEARCH.md
    (web: vitest + tsc + next build; extension: vitest + tsc + wxt build + wxt build -b firefox),
    confirming zero regressions against the pre-migration baseline Plan 16-01's Task 2 established.
    Also grep every file under packages/pv-ui/{vault,i18n} and packages/pv-ui/clipboard.ts for any
    crypto-surface keyword (wasm/argon2/chacha/hkdf/derive/decrypt/prf, case-insensitive) IN AN
    IMPORT LINE ONLY (`^import` — not a whole-file keyword search, which would false-match
    cardBrand.ts's own legitimate header-comment use of the word "decrypted" describing the
    zero-knowledge boundary), aggregating the per-plan zero-knowledge checks from Plans
    16-02/16-03/16-04 into one final repo-wide pass across the whole shared package.
  </action>
  <verify>
    <automated>(cd web && npx vitest run && npx tsc --noEmit && npx next build) && (cd extension && npx vitest run && npx tsc --noEmit && npx wxt build && npx wxt build -b firefox) && [ -z "$(grep -n '^import' packages/pv-ui/vault/*.ts packages/pv-ui/i18n/*.ts packages/pv-ui/clipboard.ts | grep -riE 'wasm|argon2|chacha|hkdf|derive|decrypt|prf')" ]</automated>
  </verify>
  <done>All 4 web commands and all 4 extension commands exit 0; both packaged builds produce output with no new errors/warnings introduced by the migration; the import-line-scoped repo-wide zero-knowledge grep across the whole packages/pv-ui shared package returns empty (legitimate "decrypted" doc-comment prose does not trip this check); this closes success criteria 1-3 for Phase 16 (DS-01, DS-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| packages/pv-ui, aggregate | The single shared package consumed identically by both the web app and the extension -- this plan's final gate before the phase closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-16-11 | Tampering / Information Disclosure | packages/pv-ui (all 6 migrated modules, aggregate) | high | mitigate | Task 2's repo-wide grep across every file in packages/pv-ui/{vault,i18n} + clipboard.ts for crypto-surface keywords is the phase's final, aggregate zero-knowledge-boundary proof, superseding the 3 per-plan checks with one comprehensive pass |
| T-16-12 | Tampering (silent scope/behavior drift) | web/, extension/ (aggregate) | medium | mitigate | Both full build+test+typecheck chains green, plus the zero-duplication grep, is the phase's closing proof that the 5-plan migration introduced no behavior change anywhere it touched |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs this phase |
</threat_model>

<verification>
Both consumers' full build+test+typecheck chains are green; the repo-wide zero-duplication grep
and the aggregate zero-knowledge grep both return empty.
</verification>

<success_criteria>
Phase 16's 3 ROADMAP success criteria are all proven true in aggregate: (1) pure logic/types/i18n
live once in pv-ui with both test suites passing unchanged; (2) the shared i18n engine is
consumed identically by both surfaces; (3) no parallel duplicate implementation survives anywhere,
verified by search.
</success_criteria>

## Artifacts This Phase Produces (this plan's slice)

**Created:** none -- this plan is pure verification, no source files are created or modified.

**Modified:** none.

**Deleted:** none.

<output>
Create `.planning/phases/16-design-system-extraction-logic-types-i18n/16-06-SUMMARY.md` when done
</output>
