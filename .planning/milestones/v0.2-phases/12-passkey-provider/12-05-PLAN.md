---
phase: 12-passkey-provider
plan: 05
type: execute
wave: 4
gap_closure: true
depends_on: ["12-02", "12-03", "12-04"]
files_modified:
  - extension/entrypoints/background/provider-ceremony.ts
  - extension/entrypoints/background/provider-ceremony.test.ts
  - extension/entrypoints/content-relay.content.ts
  - extension/entrypoints/content-relay.content.test.ts
  - extension/entrypoints/page-bridge.content.ts
  - extension/entrypoints/page-bridge-firefox.ts
  - extension/entrypoints/popup/App.tsx
  - extension/entrypoints/popup/App.test.tsx
  - extension/lib/i18n/dictionary.ts
  - extension/lib/messaging/ext-protocol.ts
  - crates/pv-provider/src/credential_store.rs
  - scripts/audit-mainworld-boundary.sh
  - .planning/phases/12-passkey-provider/deferred-items.md
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04, PROV-05]

must_haves:
  truths:
    - "On credentials.create() and single-match credentials.get() with the vault UNLOCKED, the background opens the popup consent screen (ProviderCeremonyView) and waits for an explicit confirm before minting/persisting a passkey or signing an assertion — no silent-on-unlocked-vault path remains (Bartek decision A; PROV-01/PROV-02/PROV-03)."
    - "PRF eval inputs (extensions.prf.eval.first/second and evalByCredential) are base64url-encoded before the ISOLATED→background sendMessage hop, so a PRF-with-eval ceremony deserializes in WASM instead of failing on {} (CR-01, D-21, PROV-04)."
    - "credentials.get() with an omitted rpId falls back to the sender origin's host for candidate lookup, so the vault serves RPs that rely on rpId defaulting to origin (CR-02, PROV-02)."
    - "The page-side MAIN-world safety timeout is decoupled from the interactive budget (long enough for human consent/unlock), and a passkey is persisted ONLY if the ceremony is still live at confirm time — a page that already fell through never leaves an orphaned vault/server credential (CR-03)."
    - "Permissions-Policy publickey-credentials-create/get is enforced delegation-aware on Firefox (sub-frame blocked unless same-origin with top; fail-open only top-level), closing the D-20b Firefox gap (WR-01)."
  artifacts:
    - path: "extension/entrypoints/background/provider-ceremony.ts"
      provides: "consent-gated handleCredentialsCreate/handleCredentialsGet + cancellable waitForUnlock + rpId origin default"
    - path: "extension/entrypoints/content-relay.content.ts"
      provides: "PRF eval base64url encoding in encodePublicKeyOptions"
    - path: "extension/entrypoints/page-bridge.content.ts"
      provides: "decoupled interactive-vs-safety timeout; delegation-aware Permissions-Policy"

<!-- Gap-closure plan for phase 12. Closes the Opus code-review (12-REVIEW.md: 3 Critical + 4 Warning + selected Info) and the verifier's consent-gate gap (12-VERIFICATION.md, human_needed) per Bartek's decisions (2026-07-16): A = full consent on create()+every get(); WR-01 = fix delegation-aware now. -->
---

<objective>
Close phase 12's Opus code-review findings and the verifier's consent-gate gap. The headline is Bartek's decision A: make the extension a TRUE passkey provider by gating every ceremony (create + single-match get + multi-match get) behind the already-built ProviderCeremonyView consent screen — no silent-on-unlocked-vault path survives. Along the way, fix the three silent-fail-safe correctness BLOCKERs (CR-01 PRF-eval base64, CR-02 rpId origin default, CR-03 interactive timeout + orphaned-credential) that make the feature non-functional for common real ceremonies, plus WR-01/02/03/04 and the cheap high-value Info items.

Purpose: after this plan, /gsd-secure-phase (D-15) can review a provider that actually works end-to-end in a real browser and always obtains user consent — the phase goal "acts as a full passkey provider" is fully met.
Output: consent-gated provider-ceremony.ts + App.tsx, CR-01/02/03 fixes, WR-01/02/03/04, IN-01/02/04, all gates green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-passkey-provider/12-CONTEXT.md
@.planning/phases/12-passkey-provider/12-UI-SPEC.md
@.planning/phases/12-passkey-provider/12-REVIEW.md
@.planning/phases/12-passkey-provider/12-VERIFICATION.md
@.planning/phases/12-passkey-provider/12-02-SUMMARY.md
@.planning/phases/12-passkey-provider/12-03-SUMMARY.md
@.planning/phases/12-passkey-provider/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — base64url-encode PRF eval inputs on the ISOLATED→background hop</name>
  <files>extension/entrypoints/content-relay.content.ts, extension/entrypoints/content-relay.content.test.ts</files>
  <read_first>extension/entrypoints/content-relay.content.ts (encodePublicKeyOptions), extension/lib/messaging/bytes-b64.ts, 12-REVIEW.md (CR-01)</read_first>
  <behavior>
    - Test: a get() request whose publicKey.extensions.prf.eval.first (and .second) are ArrayBuffers, passed through encodePublicKeyOptions, produces base64url STRINGS for those fields (never {} after a JSON round-trip). Add an evalByCredential fixture too. Assert the encoded output survives JSON.parse(JSON.stringify(...)) with the eval values intact.
  </behavior>
  <action>
    In encodePublicKeyOptions, after copying extensions, base64url-encode extensions.prf.eval.first/second and every extensions.prf.evalByCredential[*].first/second that isBufferSource(...), exactly as 12-REVIEW.md CR-01's fix snippet shows (reuse the file's existing isBufferSource/bufferSourceToB64Url helpers). Preserve non-binary extension fields untouched. Mirror the same encoding for create() if create requests can carry prf eval inputs (they can via prf.eval on create in WebAuthn L3 — encode them the same way). Add the regression fixture to content-relay.content.test.ts.
  </action>
  <verify><automated>npm --prefix extension test -- --run content-relay.content.test.ts</automated></verify>
  <done>PRF eval fixture round-trips as base64url strings; no ArrayBuffer→{} for prf eval; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-02 — default rpId to sender origin host when the RP omits it</name>
  <files>extension/entrypoints/background/provider-ceremony.ts, extension/entrypoints/background/provider-ceremony.test.ts</files>
  <read_first>extension/entrypoints/background/provider-ceremony.ts (extractRpId, handleCredentialsGet), 12-REVIEW.md (CR-02)</read_first>
  <behavior>
    - Test: handleCredentialsGet with a request that omits rpId, senderOrigin "https://example.com", and a stored passkey item for rpId "example.com" — the candidate lookup uses "example.com" (derived from the origin host) and finds the match, instead of keying on "" and falling through.
  </behavior>
  <action>
    Where handleCredentialsGet computes the rpId for findMatchingPasskeyItems, fall back to new URL(senderOrigin).hostname when extractRpId(req.publicKey) is empty (guard the URL parse in try/catch → "" on failure), per 12-REVIEW.md CR-02. This only widens the candidate search; passkey-client still does authoritative RP-ID/origin validation during signing, so origin binding is not weakened. (create() already has a concrete rp.id from the request; leave it.)
  </action>
  <verify><automated>npm --prefix extension test -- --run provider-ceremony.test.ts</automated></verify>
  <done>Omitted-rpId get() matches a stored credential via origin host; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Decision A — consent-gate every ceremony + CR-03 timeout/orphan fix + WR-03/WR-04</name>
  <files>extension/entrypoints/background/provider-ceremony.ts, extension/entrypoints/background/provider-ceremony.test.ts, extension/entrypoints/popup/App.tsx, extension/entrypoints/popup/App.test.tsx, extension/entrypoints/page-bridge.content.ts, extension/entrypoints/page-bridge-firefox.ts, extension/lib/messaging/ext-protocol.ts</files>
  <read_first>extension/entrypoints/background/provider-ceremony.ts (handleCredentialsCreate/handleCredentialsGet, resolvePasskeyChoice, openPopupAndAwaitUnlock, waitForUnlock, PENDING_CEREMONY_KEY), extension/entrypoints/popup/App.tsx (checkPendingCeremony/refreshFromScratch, provider-ceremony ViewState), extension/entrypoints/popup/ProviderCeremonyView.tsx, extension/lib/messaging/ext-protocol.ts (provider.resolveChoice), 12-UI-SPEC.md, 12-REVIEW.md (CR-03, WR-03, WR-04), 12-04-SUMMARY.md (Scope Clarification #3)</read_first>
  <behavior>
    - Test (create, unlocked): handleCredentialsCreate with an unlocked vault now writes a pending-ceremony consent payload to chrome.storage.session (kind "create", rpId/site + prf-request flag) and AWAITS an explicit confirm before calling wasmCreateProviderCredential/persisting; on decline (or dismissal) it returns { fallthrough: true } and NEVER mints or persists a credential.
    - Test (single-match get, unlocked): handleCredentialsGet with exactly one match now writes a pending consent payload (kind "get", single candidate) and awaits confirm before wasmGetProviderAssertion; decline → { fallthrough: true }, no signature.
    - Test (multi-match get): unchanged picker flow still works (regression).
    - Test (persist-only-if-live / CR-03): if the ceremony is resolved as abandoned/declined (page already fell through), persistPendingProviderItem is NOT called and no vault/server credential is created — assert the persist path is gated on a still-live ceremony token.
    - Test (WR-03 cancellable): closing the popup while awaiting (decline signal) resolves the await, calls unsubscribe(), and removes PENDING_CEREMONY_KEY — no leaked subscription, no stale key.
    - Test (App.tsx): a pending "create" and a pending single-match "get" consent payload each mount ProviderCeremonyView in the correct state (create title / single-get body), confirm/decline report back via provider.resolveChoice, and the view takes over focus like the multi-match picker.
  </behavior>
  <action>
    IMPLEMENT DECISION A (full consent). Refactor handleCredentialsCreate and single-match handleCredentialsGet so they follow the SAME popup-consent-then-resolve pattern the multi-match picker already uses (resolvePasskeyChoice), not a silent proceed:
    1. Introduce a unified pending-consent payload shape in chrome.storage.session (extend the existing PENDING_CEREMONY object payload — do NOT keep the dead boolean, that closes WR-04): { requestId, kind: "create"|"get", rpId, site, prfRequested, prfCapable, candidates? }. For create: one implicit "new credential" affordance (no candidate list). For single get: pre-selected single candidate (no picker list, per 12-UI-SPEC). For multi get: existing candidates list.
    2. The handler writes the payload, opens the popup (browser.action.openPopup → fallback browser.windows.create width 380), and awaits a resolution via the existing provider.resolveChoice channel (extend it if needed to carry a confirm-without-itemId for create / a decline). Make the await CANCELLABLE (abort/decline path) — on decline/dismissal: unsubscribe any lock listener, remove PENDING_CEREMONY_KEY, return { fallthrough: true }. This subsumes WR-03.
    3. Only AFTER an explicit confirm: create → wasmCreateProviderCredential + persist (phase-11 write path); get → wasmGetProviderAssertion. Gate persistPendingProviderItem on the ceremony still being live at confirm time (CR-03 orphan fix): if the resolution is decline/abandon, never mint/persist.
    4. CR-03 page-side: in BOTH page-bridge.content.ts and page-bridge-firefox.ts, decouple the safety-net timeout from the interactive budget — raise RESPONSE_TIMEOUT_MS to a value well past human interaction (120000ms) and fix the stale "long enough for a popup-unlock round trip" comment. Keep a fast fallthrough ONLY for relay-error/no-relay (the relay should still fast-fail if content-relay isn't present); the 120s ceiling is the backstop for a genuinely stuck ceremony. If a lighter-weight "relay is handling this, keep waiting" ack is cheap to add, prefer it, but a 120s backstop satisfies the must_have either way — document the choice.
    5. App.tsx: extend checkPendingCeremony/refreshFromScratch to mount ProviderCeremonyView for the create and single-get payload kinds too (not only multi-match), driving the view's kind/site/candidate props from the payload; confirm/select/decline continue to report via provider.resolveChoice. ProviderCeremonyView already renders these states (12-04) — this is wiring, not new UI.
    Preserve the D-10 fresh storage.session re-check, sender-verified origin (guard.origin), and the try/catch→fallthrough discipline. Do not weaken any existing gate.
  </action>
  <verify><automated>npm --prefix extension test -- --run provider-ceremony.test.ts && npm --prefix extension test -- --run App.test.tsx</automated></verify>
  <done>create() and single-get() consent-gated end-to-end; decline never mints/persists; abandoned ceremony leaves no orphan; page-side timeout 120s; WR-03/WR-04 closed; App.tsx mounts all three ceremony kinds; tests green.</done>
</task>

<task type="auto">
  <name>Task 4: WR-01 — delegation-aware Permissions-Policy on Firefox (+ Chrome twin consistency)</name>
  <files>extension/entrypoints/page-bridge.content.ts, extension/entrypoints/page-bridge-firefox.ts</files>
  <read_first>extension/entrypoints/page-bridge-firefox.ts (isPermissionsPolicyBlocked), extension/entrypoints/page-bridge.content.ts (twin), 12-REVIEW.md (WR-01)</read_first>
  <action>
    In isPermissionsPolicyBlocked, when neither document.permissionsPolicy nor document.featurePolicy exists (Firefox), do NOT blanket-return false. Instead apply the delegation-aware default for these two policy-controlled features (browser default = "self"): if this frame is a SUB-frame (window.top !== window.self / window !== window.top) treat the feature as BLOCKED unless the frame is same-origin with the top document; only fail-open for the TOP-level frame. Keep the existing Chromium path (query the real policy) unchanged, but apply the same sub-frame conservative default there when the API is unexpectedly absent. This closes D-20b on Firefox per Bartek's decision.
  </action>
  <verify><automated>npx --prefix extension tsc --noEmit && cd extension && npx wxt build -b firefox 2>&1 | tail -5</automated></verify>
  <done>Firefox sub-frames are treated as blocked unless same-origin with top; top-level still brokers; both builds green.</done>
</task>

<task type="auto">
  <name>Task 5: WR-02 — reword prfUnavailableNote to be credential/site-scoped, not browser-blaming (D-16)</name>
  <files>extension/lib/i18n/dictionary.ts</files>
  <read_first>extension/lib/i18n/dictionary.ts (provider.prfUnavailableNote), 12-REVIEW.md (WR-02), 12-UI-SPEC.md (Copywriting Contract)</read_first>
  <action>
    Reword provider.prfUnavailableNote in PL and EN so it attributes PRF unavailability to the site's request / this passkey's capability, NOT to "this browser" (D-16: provider PRF is computed in WASM regardless of browser). Suggested PL: "Ta strona poprosiła o funkcję PRF, której ten passkey nie obsługuje." EN: "This site requested a PRF feature this passkey can't provide." Keep it consistent with the UI-SPEC's tone; the trigger stays wired to the real capability signal (unchanged).
  </action>
  <verify><automated>npm --prefix extension test -- --run ProviderCeremonyView.test.tsx</automated></verify>
  <done>Copy no longer mentions the browser; PL+EN updated; tests green.</done>
</task>

<task type="auto">
  <name>Task 6: IN-04 round-trip guard, IN-02 bundle audit, IN-01 documentation</name>
  <files>crates/pv-provider/src/credential_store.rs, scripts/audit-mainworld-boundary.sh, .planning/phases/12-passkey-provider/deferred-items.md</files>
  <read_first>crates/pv-provider/src/credential_store.rs (SerializablePasskey), scripts/audit-mainworld-boundary.sh, 12-REVIEW.md (IN-01/IN-02/IN-04)</read_first>
  <action>
    - IN-04: add a Rust test in credential_store.rs that constructs a fully-populated passkey_types::Passkey (all fields set, incl. hmac-secret/extensions) and asserts passkey_to_json → passkeys_from_json round-trips it losslessly (field-for-field), so a future passkey-rs field addition breaks CI instead of silently dropping stored data.
    - IN-02: strengthen scripts/audit-mainworld-boundary.sh to ALSO grep the emitted MAIN-world bundle(s) under extension/.output/**/ for the forbidden symbols (run a chrome+firefox build first, or grep whatever .output already exists), so the guarantee tracks what actually ships, not just the two source files' top lines. Keep the exit-0/exit-1 contract; the script must still exit 0 on the current clean tree.
    - IN-01: document the D-20(a) non-configurable-accessor ↔ D-12 coexistence tension (first-installed-PM-wins) in deferred-items.md as an accepted, secure-phase-tracked trade-off, and note the two-PM install-order case for the /gsd-secure-phase D-12 UAT checklist.
  </action>
  <verify><automated>cargo test -p pv-provider && bash scripts/audit-mainworld-boundary.sh</automated></verify>
  <done>Round-trip guard test green; audit script greps the built bundle and still exits 0; deferred-items.md documents the D-20a↔D-12 trade-off.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p pv-provider` and `cargo test -p pv-wasm` pass
- [ ] `npm --prefix extension test` full suite green (no new regressions; the pre-existing ServerConfigView unhandled-rejection may remain)
- [ ] `npx --prefix extension tsc --noEmit` clean
- [ ] `bash scripts/audit-mainworld-boundary.sh` exits 0 (now including a bundle-level check)
- [ ] `npx wxt build -b chrome` and `-b firefox` both succeed
- [ ] create() and single-match get() are consent-gated end-to-end (no silent-on-unlocked path)
- [ ] PRF-eval get()/create() round-trips (CR-01); omitted-rpId get() matches (CR-02); no orphaned credential on abandoned ceremony (CR-03)
</verification>

<success_criteria>
- Decision A implemented: every ceremony (create + single get + multi get) obtains explicit popup consent before minting/signing
- CR-01, CR-02, CR-03 fixed; WR-01, WR-02, WR-03, WR-04 fixed; IN-01/02/04 addressed
- All gates green; phase goal "full passkey provider" met, ready for /gsd-secure-phase
</success_criteria>

<output>
Create `.planning/phases/12-passkey-provider/12-05-SUMMARY.md` when done, documenting the consent-gating architecture, the CR-03 timeout/abandon design chosen, and any Rule-1 deviations.
</output>
