---
phase: 13-dual-browser-hardening
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-03", "13-05"]
files_modified: [.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md, extension/e2e/dual-browser.spec.ts]
# NOTE: non-exhaustive — divergence fixes in Task 2 touch owning-phase source files
# (Phase 9-12) that cannot be enumerated in advance; each is `git add`-ed individually
# per the <verification> section, never via `git add -A`.
autonomous: true
requirements: [XBR-01]
must_haves:
  truths:
    - "The Firefox packaged/dev build has been walked through every SC row present in 13-03's checklist (21 SCs, per ROADMAP: Phase 9=7, Phase 10=5, Phase 11=4, Phase 12=5), with results recorded in the checklist's Firefox column"
    - "D-05 (unlocked User Key lives ONLY in browser.storage.session, never storage.local) is explicitly re-verified on both browsers via storage-API placement and clear-on-lock/close — NOT via an idle-kill survival test on Firefox, which has no idle-killable background context to survive"
    - "D-08 (MAIN-world navigator.credentials patch — Chrome declarative world:'MAIN', Firefox injectScript() of page-bridge-firefox.js, per 12-03's shipped architecture) is confirmed to produce identical OUTCOMES on both browsers: patch installed before page observation (or a clean, verified fail-safe fallthrough to native on a fresh navigation), no key material in MAIN world"
    - "The ext-scoped unlock passkey (rpId = extension id) has been walked on Firefox, closing wxt.config.ts:56-64's open question, with the outcome presented per D-12 (visible-but-disabled + inline explainer, never a silent dead-end) if rejected"
    - "Every divergence found during this pass has been fixed in the owning phase's source file and re-verified on BOTH browsers before being marked resolved — never fixed and tested on only one browser"
  artifacts:
    - path: .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md
      provides: "Final all-green (or explicitly-justified-exception) SC-by-SC Chrome/Firefox matrix"
  key_links:
    - from: ".planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md"
      to: "extension/ source files touched by divergence fixes"
      via: "any FAIL row's fix is traceable to a specific file change, re-tested on both browsers before being marked resolved"
      pattern: "FAIL.*(fixed|RESOLVED)"
---

<objective>
Complete the dual-browser hardening pass: bring up a Firefox-reachable pv-server (the moz-extension origin now accepted via 13-05's `moz-extension://*` mechanism — no per-UUID capture-and-restart dance required), walk every SC row present in 13-03's checklist on Firefox (using a scriptable selenium-webdriver + geckodriver harness as the primary vehicle, since Playwright cannot load extensions into Firefox — Plan 13-03's harness is Chromium-only by design), explicitly re-verify the riskiest cross-browser invariants (D-05 session-storage discipline, D-08 MAIN-world injection parity, and the previously-unowned ext-scoped rpId-on-Firefox question), triage every divergence found, and fix each one in its owning phase's code — re-tested on BOTH browsers before being marked resolved, per the standing anti-pattern this project exists to avoid (a Chrome-only-tested fix silently re-breaking Firefox, or vice versa).

Purpose: D-01 / ROADMAP Phase 13 SC #1 and #3 — full systematic re-verification on both `wxt dev -b chrome` and `wxt dev -b firefox`, and this phase owning the *fix*, not just the finding (13-CONTEXT.md's explicit scope). Corrected count: 21 SCs total (Phase 9=7, Phase 10=5, Phase 11=4, Phase 12=5); the checklist grows to 24 rows once D-05, D-08, and the new ext-scoped rpId-on-Firefox row are included (plus an optional, nice-to-have V-04 reconfigure-server row).
Output: A fully-populated, all-green (or explicitly-justified) `13-UAT-CHECKLIST.md`, and any divergence fixes committed to the phases that own them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dual-browser-hardening/13-CONTEXT.md
@.planning/phases/13-dual-browser-hardening/13-RESEARCH.md

# This plan's baseline — the Chrome-side results and the harness built in the prior wave:
.planning/phases/13-dual-browser-hardening/13-01-SUMMARY.md
.planning/phases/13-dual-browser-hardening/13-03-SUMMARY.md
.planning/phases/13-dual-browser-hardening/13-05-SUMMARY.md
.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md
extension/e2e/dual-browser.spec.ts
extension/wxt.config.ts
extension/entrypoints/background/session-storage.ts
extension/entrypoints/page-bridge-firefox.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Firefox server bring-up + self-driven UAT walk — every 13-03 checklist row plus D-05/D-08/rpId-on-Firefox explicit checks</name>
  <files>.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md</files>
  <read_first>13-UAT-CHECKLIST.md, .planning/phases/13-dual-browser-hardening/13-01-SUMMARY.md (Firefox MV2 persistent-background target confirmed, gecko.id unchanged), .planning/phases/13-dual-browser-hardening/13-05-SUMMARY.md (the moz-extension://* server mechanism this task's step 0 relies on), 12-03-SUMMARY.md (D-08's shipped per-browser injection mechanisms)</read_first>
  <action>
    **Step 0 — Firefox-reachable pv-server bring-up (do this BEFORE any other SC; nothing server-touching
    can pass until it's done):** Start (or restart) pv-server with
    `PV_EXTENSION_ORIGINS='chrome-extension://bbpnpamaoddpkfjnohkkepbjgbjpdbfo,moz-extension://*'` —
    the scheme-scoped wildcard is accepted by the NEW mechanism 13-05 ships (bare `*` remains fatal
    per WR-07; only the `moz-extension://*` literal is special-cased). Because the server now
    accepts any well-formed `moz-extension://<uuid>` origin, there is no need to capture the running
    instance's UUID and restart the server per-run as earlier drafts assumed — one bring-up covers
    every Firefox launch for the rest of this task. Still, once the Firefox instance is running,
    read its live origin from the background console via `browser.runtime.getURL('')` and record
    the observed `moz-extension://<uuid>` value in the checklist Notes column for the Phase 9 SC#6
    row (evidence that a real request from a real per-install UUID passed through the wildcard, not
    just that the server accepted a theoretical pattern).

    Launch Firefox with a PERSISTENT profile so the moz-extension UUID and `storage.local`/
    `storage.session` state survive across the many launches this multi-SC walk requires (a fresh
    temp profile — the default for both `web-ext run` and `wxt dev -b firefox` — rotates the UUID
    and wipes storage on every relaunch, making a systematic walk impractical): run
    `npx web-ext run --source-dir .output/firefox-mv2 --keep-profile-changes --firefox-profile
    <persistent-profile-dir> --verbose` from `extension/`. The chosen profile's
    `extensions.webextensions.uuids` pref is what pins the UUID between runs; note for the record
    that a signed/installed build gets its own per-install-stable UUID by a different mechanism
    (AMO signing), which this dev-profile approach only approximates. With D-10's `moz-extension://*`
    wildcard in place, UUID stability matters less for the CORS/server-reachability half (any UUID
    origin is now accepted) — but it still matters for the storage-persistence and rpId-continuity
    checks below, so keep the persistent profile regardless.

    **Primary scriptable vehicle:** drive the walk with `selenium-webdriver` (npm, pin current `^4.x`)
    plus `geckodriver` (`>=0.35`, matching the installed Firefox 152) via
    `driver.installAddon(<path to a zip of .output/firefox-mv2>, /*temporary=*/ true)` — this is the
    canonical way to script a real Firefox instance with the extension loaded, and lets each SC
    assertion actually fail against real DOM/page state rather than a narrated pass. **Fallback:**
    `web-ext run --verbose` (as launched in Step 0) plus orchestrator-manual driving, used only where
    selenium+geckodriver genuinely cannot reach (e.g. a step requiring interaction with a native
    `about:debugging` panel) — for every fallback-driven row, capture and attach concrete falsifiable
    evidence (a screenshot plus the relevant console excerpt), never a bare `PASS` with no evidence.
    (Optional, not required: `puppeteer-core`'s WebDriver-BiDi `webExtension.install` is another
    scriptable path if selenium+geckodriver proves awkward.) `--verbose`/console streaming surfaces
    crashes automatically as you go — capture and record any such output alongside each SC's
    checklist row.

    Drive each SC row present in `13-UAT-CHECKLIST.md` (21 SCs total, per ROADMAP: Phase 9's 7,
    Phase 10's 5, Phase 11's 4, Phase 12's 5 — reference the checklist's own row list rather than a
    hardcoded number here, since it is the corrected source of truth from 13-03) against the same
    test fixtures/pages/accounts the Chrome Playwright suite uses (13-03's Task 1 defines and mines
    them from the prior-session harnesses; reuse them here rather than inventing new ones). Phase 9
    SC#6 (moz-extension CORS, verified end-to-end against a REAL Firefox request — this phase's
    headline deferred-item verification, see Step 0's Notes capture) and SC#7 (fullscreen/open-full-vault)
    each need their own Firefox row; do not silently drop either.

    For each SC, update `13-UAT-CHECKLIST.md`'s Firefox column with `PASS` or `FAIL —
    <specific reason, e.g. "PRF banner absent" or "autofill dropdown did not open on gesture">`.

    Then perform these explicit invariant/closure checks as their own checklist rows (do not skip
    these — they are the pass's highest-risk items per 13-RESEARCH.md's Anti-Patterns and Known
    Threat Patterns):

    - **D-05** (rewritten for the shipped Firefox target — MV2 with a PERSISTENT background page,
      which has no idle-kill to survive; do NOT attempt to "force the background to terminate or go
      idle 60+s" — that premise does not apply to a persistent page): with the extension unlocked,
      inspect via the background page's own devtools console (`about:debugging` → Inspect) and
      confirm (a) `await browser.storage.session.get()` holds the key envelope
      (`session-storage.ts`'s `KEY_STORAGE_KEY`), (b) `await browser.storage.local.get()` holds NO
      key material, (c) the storage area's `access_level` is unchanged at `TRUSTED_CONTEXTS` (never
      widened to content scripts — this is T-13-09's concern), (d) triggering `lockVaultSession`
      clears `storage.session`, and (e) closing the browser clears `storage.session`. Record the
      actual observed storage locations for (a)/(b). Document HONESTLY in the checklist Notes that
      Firefox's persistent background page has no idle-kill-survival analog to Chrome's — parity
      here is proven by storage-API placement and clear-on-lock/close behavior, not by a survival
      test (a persistent page combined with a leaked module-level variable would trivially "pass" an
      idle-kill test without proving anything).

    - **D-08** (per 12-03's shipped per-browser mechanisms — Chrome declarative `world:'MAIN'`,
      Firefox `injectScript()` of the unlisted `page-bridge-firefox.ts`/`.js` script from the
      ISOLATED-world content script; these are genuinely DIFFERENT mechanisms by design, not a
      single shared one): on a FRESH navigation (not a page already loaded before the extension
      re-attached) to a real third-party test page, confirm via the page's own DevTools console that
      `navigator.credentials.create.toString()` shows the RPC-shim wrapper, not `[native code]`, and
      that no key material is reachable from the MAIN world. Because `injectScript()` fires
      fire-and-forget as close to `document_start` as this mechanism allows, it can lose the race on
      a first navigation (an analog of Chromium's own bug 634381 for `world:'MAIN'`) — so the
      acceptance condition is: EITHER the shim wins the race (wrapper present) OR the patch fails
      safe to a clean native fallthrough (try/catch around `installPatch`, no half-installed state,
      no key material ever exposed) — verified freshly, never assumed from a warm/already-patched
      page. Record which of the two outcomes was actually observed.

    - **rpId-on-Firefox / ext-scoped unlock passkey** (closes the `wxt.config.ts:56-64` open
      question): attempt the extension-scoped UNLOCK passkey (rpId = extension id) on this Firefox
      instance and record whether `moz-extension://<uuid>` origins accept it. Per **D-12**, if it is
      rejected/unusable, the passkey affordance must NOT silently dead-end or hide — both the
      UnlockView unlock button and the EnrollExtPasskeyPrompt enroll offer stay VISIBLE but DISABLED,
      with an inline explainer, and password unlock remains fully functional alongside (D-06). Per
      **D-13**, the popup's explainer copy is the browser-framed 13-UI-SPEC wording (PL "Szybkie
      odblokowanie passkeyem nie jest dostępne w tej przeglądarce — użyj hasła." / EN "Fast unlock
      isn't available for this passkey on this browser — use your password.") — this replaces/aligns
      `unlock.passkeyUnsupported` and any new dead-end banners; the PROVIDER ceremony's
      `provider.prfUnavailableNote` stays untouched (WR-02 site-framed copy, D-16: provider PRF is
      WASM-computed and browser-independent — never reintroduce browser-blaming there). Record the
      observed outcome and close the `wxt.config.ts:56-64` open question explicitly in the closing
      SUMMARY.

    - **V-04 (optional, nice-to-have)**: reconfiguring the server URL while a session/host-permission
      already exists — confirm (or record as a known gap) whether the old session is invalidated and
      the old host permission is revoked, on both browsers. This row does not gate phase completion;
      mark it `PASS`, `FAIL — <reason>`, or `DEFERRED — backlog` as appropriate.
  </action>
  <verify>
    <automated>grep -c "PENDING" .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md | grep -qx '0' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `13-UAT-CHECKLIST.md` contains zero `PENDING` entries after this task (except the optional V-04
      row, which may be `DEFERRED`)
    - The Phase 9 SC#6 row's Notes cite the actual observed `moz-extension://<uuid>` origin used
    - The D-05 row documents the actual observed storage locations (session holds the key, local does
      not) and explicitly notes the absence of a Firefox idle-kill analog rather than asserting one
    - The D-08 row documents which outcome was observed (shim-wins or clean native fallthrough) on a
      fresh navigation, and names the actual per-browser mechanism (not a shared manual-injection story)
    - The rpId-on-Firefox row records the observed outcome and, if rejected, confirms the D-12
      visible-but-disabled + D-13-copy presentation was used, closing the wxt.config.ts:56-64 question
  </acceptance_criteria>
  <done>Every checklist row from 13-03 has a Firefox result (PASS or FAIL with reason); D-05, D-08, and the ext-scoped rpId-on-Firefox row are explicitly confirmed, not assumed from Firefox's more lenient lifecycle; the moz-extension server bring-up (Step 0) is recorded with an observed UUID.</done>
</task>

<task type="auto">
  <name>Task 2: Triage and fix every divergence — re-verify on BOTH browsers before marking resolved</name>
  <files>.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md</files>
  <read_first>13-UAT-CHECKLIST.md</read_first>
  <action>
    For every row with a `FAIL` in either the Chrome or Firefox column, locate and fix the bug in
    the actual owning phase's source file (Phase 9/10/11/12's code — never patch around it inside
    this phase's own harness files). After each fix, re-run BOTH the Chrome Playwright test for that
    SC (`npx playwright test --project=chromium -g "<SC description>"`) AND the Firefox re-check for
    that SC using Task 1's mechanism (selenium-webdriver + geckodriver primary, or the `web-ext run`
    + manual fallback with falsifiable evidence) — per 13-RESEARCH.md's explicit anti-pattern
    warning, a fix verified on only the browser where the bug was originally found must not be
    trusted, since Firefox's MV2-persistent background lifecycle can mask a bug that Chrome's
    stricter service-worker termination would immediately re-expose (and vice versa for any
    Firefox-specific manifest/CSP quirk).

    Update the checklist row to `RESOLVED — <what was fixed, file path, brief reason>` only after
    both browsers confirm green for that SC. If every row is already `PASS` with no `FAIL` entries
    from Task 1, state that explicitly in the SUMMARY rather than fabricating fixes.

    The automated verify below matches only cells whose content starts with `FAIL` immediately
    after a table-column pipe (`| FAIL`), not the bare substring anywhere in the row — so a
    `RESOLVED` cell's brief reason may freely narrate what used to be broken (e.g. "was failing on
    Firefox, fixed in popup.tsx") without tripping the gate. Just don't start a `Notes`/reason cell's
    text with the literal word `FAIL` once a row is resolved.

    Finally, update `.planning/phases/13-dual-browser-hardening/13-VALIDATION.md`'s frontmatter
    field `nyquist_compliant` to `true` once every row is `PASS` or `RESOLVED`.
  </action>
  <verify>
    <automated>grep -cE '\| *FAIL' .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md | grep -qx '0' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Zero rows remain `FAIL` in `13-UAT-CHECKLIST.md` — every row is `PASS` or `RESOLVED — <detail>`
    - Every `RESOLVED` row cites a specific file path and re-confirms both-browser re-testing
    - `13-VALIDATION.md` frontmatter `nyquist_compliant: true`
  </acceptance_criteria>
  <done>Every SC is green on both browsers; any divergence found has a documented, both-browser-re-verified fix; the phase's validation artifact is marked compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Chrome-verified fix ↔ Firefox behavior | A fix made and only tested on one browser is a trust boundary this phase exists to close |
| MAIN world (page) ↔ ISOLATED content script ↔ background | The passkey-provider RPC shim's injection mechanism, re-verified cross-browser here |
| Untrusted moz-extension origin ↔ pv-server CORS allowlist | D-10's `moz-extension://*` scheme-scoped wildcard, consumed (not re-decided) by this task's Step 0 server bring-up |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-13-08 | Tampering / Elevation of Privilege | Any divergence fix touching background message handlers or session storage | high | mitigate | Task 2's mandatory both-browser re-verification before marking any row `RESOLVED` — no fix is trusted from a single-browser test run |
| T-13-09 | Elevation of Privilege | `browser.storage.session`'s `access_level` accidentally widened while fixing a divergence | high | mitigate | Task 1's D-05 check explicitly confirms storage.session access stays extension-only (never exposed to content scripts) after any background-code touch, per 13-RESEARCH.md's ASVS V4 mapping |
| T-13-10 | Spoofing | MAIN-world `navigator.credentials` patch injected via genuinely DIFFERENT per-browser mechanisms (Chrome declarative `world:'MAIN'`; Firefox `injectScript()` of `page-bridge-firefox.ts` from the ISOLATED-world content script) — different mechanisms may produce different OUTCOMES, opening a gap where the RPC shim could be bypassed on one browser only | high | mitigate | Task 1's D-08 check confirms IDENTICAL OUTCOMES (not identical mechanism) on both browsers, on a fresh navigation: either the shim wins the injection race, or it fails safe to a clean native fallthrough with no key material ever exposed — verified, not assumed, per-browser |
| T-13-11 | Spoofing / Tampering | `moz-extension://*` scheme-scoped wildcard in `PV_EXTENSION_ORIGINS` (D-10, 13-05) accepted by any well-formed per-install UUID origin | medium | accept (flagged tech-debt) | CORS is not an auth boundary here (API requires bearer tokens; a hostile extension without one gains nothing) and per-profile UUID churn makes concrete-origin config hostile UX today; recorded in STATE.md Deferred Items + SELF-HOSTING.md as a stopgap to be replaced by per-install concrete-origin configuration later — this task consumes 13-05's mechanism, does not re-decide it |

</threat_model>

<verification>
- [ ] `13-UAT-CHECKLIST.md` has zero `PENDING` and zero `FAIL` rows (V-04 may be `DEFERRED`)
- [ ] D-05, D-08, and the ext-scoped rpId-on-Firefox rows are explicitly documented with observed
      evidence, not assumptions
- [ ] Phase 9 SC#6's row Notes cite the observed `moz-extension://<uuid>` origin from Step 0
- [ ] Every `RESOLVED` row's fix was re-tested on both browsers
- [ ] `13-VALIDATION.md` frontmatter `nyquist_compliant: true`
- [ ] Commit atomically per fix: `git add <specific owning-phase file path>` per divergence fix, plus a final `git add .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md .planning/phases/13-dual-browser-hardening/13-VALIDATION.md` — never `git add -A`
</verification>

<success_criteria>
- All tasks completed
- Full checklist matrix green on both browsers — 21 SCs plus D-05, D-08, and the ext-scoped
  rpId-on-Firefox row (24 rows total); the optional V-04 row does not gate this success criterion
- D-05/D-08/rpId-on-Firefox invariants explicitly re-confirmed, not assumed
- No errors or warnings introduced
</success_criteria>

<output>
Create `.planning/phases/13-dual-browser-hardening/13-04-SUMMARY.md` when done — this is the phase's final closing summary; list every divergence found and fixed, or state explicitly that none were found. Explicitly close the `wxt.config.ts:56-64` open question (ext-scoped rpId-on-Firefox outcome and its D-12/D-13 presentation) in this SUMMARY.
</output>
