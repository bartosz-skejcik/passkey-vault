---
phase: 13-dual-browser-hardening
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified: [extension/playwright.config.ts, extension/e2e/fixtures.ts, extension/e2e/dual-browser.spec.ts, extension/vitest.config.ts, extension/package.json, .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md]
autonomous: true
requirements: [XBR-01]
must_haves:
  truths:
    - "Every one of the 21 success criteria from Phases 9-12 (Phase 9's 7, Phase 10's 5, Phase 11's 4, Phase 12's 5) has an automated Playwright test case exercising it on Chromium, with honest dispositions for the handful that cannot be a clean Chromium green (see honesty dispositions below)"
    - "Running the Chrome Playwright suite produces a pass/fail result for each of the 21 SCs, recorded in the UAT checklist"
    - "The extension's pre-existing vitest suite (background + popup projects) stays green after e2e/ is introduced -- vitest must never collect Playwright specs"
  artifacts:
    - path: extension/playwright.config.ts
      provides: "Chromium-only config holding testDir/project/reporter ONLY -- persistent-context + --load-extension logic lives in e2e/fixtures.ts, not here (Playwright config cannot express launchPersistentContext)"
    - path: extension/e2e/fixtures.ts
      provides: "test.extend-based fixture exporting an extended `test` with `context` (chromium.launchPersistentContext, channel:'chromium', headless:true, --load-extension/--disable-extensions-except pointed at .output/chrome-mv3) and `extensionId` (derived from context.serviceWorkers()) fixtures, per Playwright's official chrome-extension pattern"
    - path: extension/e2e/dual-browser.spec.ts
      provides: "21 test cases covering Phase 9-12 success criteria, Chromium-only, importing the test.extend fixture from fixtures.ts"
    - path: extension/vitest.config.ts
      provides: "Both background and popup vitest projects exclude e2e/** so Playwright specs are never collected by vitest"
    - path: .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md
      provides: "SC-by-SC Chrome/Firefox pass matrix (Chrome column filled by this plan; 24 total rows)"
  key_links:
    - from: "extension/e2e/dual-browser.spec.ts"
      to: ".planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md"
      via: "Chrome test run results are transcribed into the checklist's Chrome column"
      pattern: "Chrome.*(PASS|FAIL)"
---

<objective>
Build a Playwright dual-browser test harness for the extension and author the systematic 21-SC UAT checklist (CONTEXT.md's discretion: this phase's re-verification pass must be "systematic, not spot-checked" — every SC from Phases 9-12, not just the newest). Run the full matrix against Chromium first, since Playwright's extension-loading support (`--load-extension` + persistent context) is Chromium-specific — Firefox's pass is Plan 13-04's job, using a different, web-ext-based mechanism.

**Corrected SC count (post-audit, 2026-07-17):** the real total across Phases 9-12 is **21**, not 19 — Phase 9 has SEVEN success criteria (ROADMAP.md:76-84), not five. Per-phase: Phase 9 = 7, Phase 10 = 5, Phase 11 = 4, Phase 12 = 5 (7+5+4+5=21). Every "19" in this plan has been corrected to "21"; the checklist grows from the old "21 rows (19 SCs + D-05 + D-08)" to **23 rows (21 SCs + D-05 + D-08)**, plus one more new row (ext-scoped rpId-on-Firefox, see Task 2) for a final total of **24 rows**. Use 24 consistently as "the total row count" everywhere in this plan and its outputs.

Purpose: D-01 / ROADMAP Phase 13 SC #1 — every v0.2 feature re-verified on both browsers, not spot-checked. Building this as an automated Playwright suite (rather than a purely manual click-through) gives Claude a self-driven verification vehicle consistent with this project's standing Playwright-UAT-authorization policy, and produces a regression-checkable artifact for future phases.
Output: `extension/playwright.config.ts` (testDir/project/reporter only), `extension/e2e/fixtures.ts` (the `test.extend` launchPersistentContext fixture), `extension/e2e/dual-browser.spec.ts` (21 test cases), `extension/vitest.config.ts` (e2e/** excluded from both projects), and `13-UAT-CHECKLIST.md` with the Chrome column filled in from a real run (24 rows).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dual-browser-hardening/13-CONTEXT.md
@.planning/phases/13-dual-browser-hardening/13-RESEARCH.md

# The 21 SCs this harness must cover (ROADMAP.md Phase Details — read directly, do not paraphrase
# from memory): Phase 9 SCs 1-7 (SEVEN, not five), Phase 10 SCs 1-5, Phase 11 SCs 1-4, Phase 12 SCs 1-5.
# This corrects the pre-audit "19" miscount (13-RESEARCH.md and the original version of this plan
# both undercounted Phase 9 at 5 instead of its real 7). 7+5+4+5 = 21.

# Read whatever Phases 9-12 actually produced, to know what pages/selectors/messages the tests
# drive (this plan only runs after Phases 9-12 have shipped):
.planning/phases/09-session-unlock-core-popup-sync-client/*-SUMMARY.md
.planning/phases/10-autofill-login-totp-card-identity/*-SUMMARY.md
.planning/phases/11-generate-capture/*-SUMMARY.md
.planning/phases/12-passkey-provider/*-SUMMARY.md

# --- EMBEDDED: 13-FRESHNESS-AUDIT.md's corrected SC inventory (2026-07-17 auditor notes), verbatim.
# Do NOT re-derive this list from ROADMAP.md yourself -- use it as-is; it has already been checked
# against ROADMAP.md line numbers and each SC's real automatability disposition.

# CORRECTED SC INVENTORY (feeds 13-UAT-CHECKLIST.md). Real total = 21 SCs (NOT 19). Per-phase:
# Phase 9 = 7, Phase 10 = 5, Phase 11 = 4, Phase 12 = 5. Checklist = 21 SC rows + D-05 + D-08 = 23 rows
# (+ 1 more new row this plan adds in Task 2 = 24 rows total).
#
# PHASE 9 — Session Unlock Core (7 SCs) [ROADMAP.md:76-84]:
# - P9-SC1 First-run: configure own pv-server URL; validated via /healthz; persisted; editable;
#   nothing hard-coded. AUTOMATABLE (probe-configset/probe-realconfig/verify-changeserver; selector
#   input#pv-server-url).
# - P9-SC2 Unlock from popup with master password AND with a PRF passkey where supported.
#   AUTOMATABLE (password: popup-full-flow; PRF half: CDP vAuth hasPrf:true on POPUP CDP session,
#   rpId=ext id, per probe-webauthn-rpid).
# - P9-SC3 Unlocked User Key ONLY in chrome.storage.session; usable across SW idle-kill/wake (60s+).
#   AUTOMATABLE (chrome-idle-kill/probe-wake-list). == D-05 invariant row.
# - P9-SC4 Auto-lock: key cleared after configurable idle timeout AND on browser close. AUTOMATABLE
#   (probe-autolock for idle; browser-close half via context teardown/reopen — partial/indirect).
# - P9-SC5 Browse/search/pick any item; edit on another synced client appears via REST+WS.
#   AUTOMATABLE (probe-crossclient-sync; needs API write + WS propagation).
# - P9-SC6 pv-server CORS allowlist accepts fixed extension origin (chrome-extension:// AND
#   moz-extension://), end-to-end. CHROME HALF AUTOMATABLE (PV_EXTENSION_ORIGINS=chrome-extension://
#   bbp...); moz-extension HALF DEFERRED to 13-01/13-04/13-05 (Firefox), still open per
#   deferred-items.md:25-26.
# - P9-SC7 Popup 'open full vault' opens configured server web app in new tab; popup doesn't
#   re-implement management. AUTOMATABLE (probe-sc4567; assert new-tab URL==server; web app lands
#   authed-but-locked → click [data-testid=unlock-submit] if asserting web state).
#
# PHASE 10 — Autofill (5 SCs) [ROADMAP.md:121-127]:
# - P10-SC1 Detect login form; offer fill username+password; picker on multiple matches.
#   AUTOMATABLE (probe-autofill-wave3 + e2e-fixtures).
# - P10-SC2 Live TOTP fills/copies into 2FA field. AUTOMATABLE (probe-totp; data-testid
#   autofill-totp-fill/copy).
# - P10-SC3 Card fields (number/expiry/CVV/cardholder) fill from saved card, same-origin.
#   AUTOMATABLE.
# - P10-SC4 Identity fields (name/address/email/phone) fill from saved identity. AUTOMATABLE.
# - P10-SC5 No autofill without explicit gesture; no top-level creds into cross-origin iframe —
#   adversarial fixture. AUTOMATABLE, SECURITY (e2e-fixtures/adversarial-iframe/serve.mjs ports 8791
#   origin A / 8792 origin B; probe-adversarial-iframe-10-07).
#
# PHASE 11 — Generate & Capture (4 SCs) [ROADMAP.md:151-156]:
# - P11-SC1 Signup form → offer generated password (character + passphrase modes). AUTOMATABLE
#   (probe-phase11-capture).
# - P11-SC2 After successful submit/login → prompt to save new login, attributed to correct origin.
#   AUTOMATABLE (SPA/AJAX submit heuristic).
# - P11-SC3 Password change on site with existing login → offer update (not duplicate).
#   AUTOMATABLE.
# - P11-SC4 Save/update prompts show actual origin + warn on origin mismatch (cross-origin iframe).
#   AUTOMATABLE (adversarial fixture).
#   NOTE: 11-07/08/09 theme/style parity = TASTE/visual, NOT an SC — no fabricated visual-parity
#   green.
#
# PHASE 12 — Passkey Provider (5 SCs) [ROADMAP.md:178-184]:
# - P12-SC1 navigator.credentials.create() registers ES256 passkey (passkey-rs) saved to vault.
#   AUTOMATABLE — EXTENSION is authenticator, NO CDP vAuth; drive popup ProviderCeremonyView consent
#   [data-testid=provider-confirm]; assert item saved. (12-PROVIDER-UAT.md/probe-passkey-net).
# - P12-SC2 navigator.credentials.get() logs in with a vault passkey. AUTOMATABLE — same;
#   provider-confirm + provider-credential-row-<itemId>.
# - P12-SC3 Decline/no-match → clean fallthrough to native OS authenticator, with another PM
#   extension installed. FALLTHROUGH half AUTOMATABLE via CDP vAuth (fires only after shim falls
#   through, via [data-testid=provider-decline]); 'another PM extension installed' coexistence =
#   MANUAL (deferred-items.md:19-22, D-15).
# - P12-SC4 PRF used where allowed (Chromium-first); Firefox/PRF-unavailable degrades honestly with
#   specific message. Chrome POSITIVE 'PRF used' path AUTOMATABLE; degradation COPY is Firefox-side
#   (13-04) — do NOT assert provider-ceremony banner on Chrome (provider PRF is WASM-computed,
#   D-16). Unlock-path banner can be forced on Chrome only via hasPrf:false vAuth on the UNLOCK
#   ceremony (13-02 surface).
# - P12-SC5 Security review confirms MAIN-world patch is key-free RPC shim, grep-audited. NOT A
#   PLAYWRIGHT TEST — scripts/audit-mainworld-boundary.sh + completed 12-05 review; checklist row =
#   'verified by audit script + 12-05' (a spec MAY shell out to the script and assert exit 0, which
#   is why the test-count gate below stays at 21 rather than dropping to 20).
#
# TWO EXPLICIT INVARIANT ROWS (already in plan Task 2, keep):
# - D-05: chrome.storage.session-only key survives idle-kill/wake (Chrome). Overlaps P9-SC3.
# - D-08: MAIN-world navigator.credentials patch injection behaves as designed (Chrome). Overlaps
#   P12-SC1/2/5.
#
# --- END EMBEDDED AUDITOR SC INVENTORY ---

# Mine these prior-session Playwright harnesses for real selectors/flows/gotchas BEFORE writing any
# test in this plan -- do not invent selectors (see Task 1's mining step for the rule and the
# verified selector map):
# /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/270023a3-*/scratchpad/uat
# /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/e1361adc-*/scratchpad/uat
# /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/e2d2ac4c-*/scratchpad/uat
# /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/71997f1b-*/scratchpad/uat
# /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/945c6b2d-ea30-467c-93f8-824da0230074/scratchpad/uat  (current session's merged copy)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Playwright Chromium extension-testing harness</name>
  <files>extension/playwright.config.ts, extension/e2e/fixtures.ts, extension/e2e/dual-browser.spec.ts, extension/vitest.config.ts, extension/package.json</files>
  <read_first>extension/wxt.config.ts, extension/vitest.config.ts, .planning/phases/13-dual-browser-hardening/13-01-SUMMARY.md, extension/entrypoints/popup/ProviderCeremonyView.tsx</read_first>
  <action>
    STEP 0 -- MINE THE PRIOR-SESSION HARNESSES FIRST. Before writing a single selector or test, read
    the proven Playwright harnesses left by prior UAT sessions in these directories (all confirmed to
    exist):
      /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/270023a3-*/scratchpad/uat
      /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/e1361adc-*/scratchpad/uat
      /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/e2d2ac4c-*/scratchpad/uat
      /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/71997f1b-*/scratchpad/uat
      /private/tmp/claude-501/-Users-j5on--work-projects-passkey-vault/945c6b2d-ea30-467c-93f8-824da0230074/scratchpad/uat
        (this is the CURRENT session's merged copy of all four -- check here first, it's the
        superset)
    They already encode working selectors, flows, and env setup: chrome-idle-kill.js,
    popup-full-flow.js, probe-sc4567.js (P9-SC4/5/7), probe-crossclient-sync.js (P9-SC5),
    probe-webauthn-rpid.js (ext-scoped PRF unlock), probe-autofill-wave3.js/probe-totp.js (P10),
    probe-adversarial-iframe-10-07.js (P10-SC5), probe-phase11-capture.js (P11),
    probe-passkey-net.js + 12-PROVIDER-UAT.md (P12). Do NOT invent selectors -- port these into the
    fixture model below. Verified selector map (already checked against the real source at plan-fix
    time):
      - input#pv-server-url, input[type=email], input[type=password], button[type=submit]
        (probe-sc4567.js:19-25)
      - [data-testid=provider-confirm], [data-testid=provider-decline]
        (ProviderCeremonyView.tsx:238,265)
      - [data-testid=provider-credential-row-<itemId>] (ProviderCeremonyView.tsx:209)
      - [data-testid=autofill-totp-fill-...], [data-testid=autofill-totp-copy-...]
        (TotpFillRow.tsx:158,167)
      - [data-testid=on-this-page-section] (OnThisPageSection.tsx:149)
      - [data-testid=unlock-submit] (web app, web/src/components/auth/UnlockOverlay.tsx:167,220)
    Five gotcha rules, encode as explicit test-setup rules (do not relearn these the hard way):
      1. Server origin = `http://localhost:8620`, NEVER `127.0.0.1` -- the RP ID binds to the
         hostname string. popup-full-flow.js still has the wrong 127.0.0.1 in some spots;
         probe-sc4567.js uses the correct localhost. Normalize every test to localhost.
      2. Kill zombie `serve.mjs` child processes on ports 8791/8792 before AND after the
         adversarial-iframe tests (P10-SC5 / P11-SC4) -- they leak across runs otherwise.
      3. pv-server must run with `PV_STATIC_DIR=web/out` and
         `PV_EXTENSION_ORIGINS=chrome-extension://bbpnpamaoddpkfjnohkkepbjgbjpdbfo` (the extension's
         real stable Chrome id). Test account: `uat-prf04@example.local` /
         `CorrectHorseBattery-UAT-2026!`.
      4. CDP virtual-authenticator ceremonies need a single focused tab -- multiple open tabs/pages
         racing for the CDP session cause flaky WebAuthn dispatch.
      5. Playwright's bundled Chromium is en-US -- use `data-testid` selectors, or bilingual
         (PL+EN) text-selector fallbacks. Never a PL-only text selector.

    STEP 1 -- INSTALL. Add `@playwright/test` as a devDependency, PINNED (not `latest`):
    `npm i -D @playwright/test@1.61.1`; then `npx playwright install chromium` — Firefox is
    intentionally NOT installed for Playwright here, see rationale below. Before installing, run
    `npm view @playwright/test repository.url` as an automated sanity re-confirmation —
    13-RESEARCH.md's Package Legitimacy Audit already approved this package (Microsoft-official,
    42M+ weekly downloads; version 1.61.1 confirmed current at plan-fix time); no blocking
    checkpoint required.

    STEP 2 -- KEEP VITEST GREEN. `extension/vitest.config.ts` already exists (two projects,
    background=node and popup=jsdom, vitest ^3.2.7, 45 pre-existing test files) — this task must NOT
    recreate it, only edit it. The "background" project's `exclude` currently only excludes
    `node_modules/**` and `entrypoints/popup/**`; with no `include` override it falls back to
    vitest's default `**/*.{test,spec}.?(c|m)[jt]s?(x)`, which WOULD collect
    `extension/e2e/dual-browser.spec.ts` (a `@playwright/test` file) and crash `npm test` (currently
    green). Add `"e2e/**"` to the `exclude` array of BOTH the "background" and "popup" projects (or
    a root-level `test.exclude` covering both) so vitest never collects anything under `e2e/`. After
    this edit, run `npm test` (extension's existing vitest suite) and confirm it still reports the
    full pre-existing suite green — the same test count as before this plan touched anything.

    STEP 3 -- FIXTURE-BASED HARNESS (playwright.config.ts CANNOT express --load-extension). Do NOT
    put `chromium.launchPersistentContext` or `--load-extension` args inside
    `extension/playwright.config.ts` project/use options -- this is unsatisfiable; persistent-context
    creation with extension loading must live in a custom test fixture (Playwright's official
    "Chrome extensions" pattern). Write `extension/e2e/fixtures.ts` exporting an extended `test` via
    `test.extend<{ context: BrowserContext; extensionId: string }>({ ... })`, matching the pattern
    already proven in this repo's prior-session harnesses (chrome-idle-kill.js, popup-full-flow.js):
      - `context` fixture: `chromium.launchPersistentContext('', { channel: 'chromium',
        headless: true, args: ['--disable-extensions-except=<path-to-.output/chrome-mv3>',
        '--load-extension=<path-to-.output/chrome-mv3>'] })` (confirm the actual output dir name via
        `ls extension/.output/` -- prior harnesses and 13-01 both confirm it is `chrome-mv3`, not
        `chrome-mv3-dev`).
      - `extensionId` fixture: derive from `context.serviceWorkers()[0]` (or
        `context.waitForEvent('serviceworker')` if none yet), parsing the id out of the worker's
        `.url()`.
      - Export both alongside Playwright's `expect`.
    `extension/playwright.config.ts` then holds ONLY `testDir: './e2e'`, a single `chromium` project,
    and `reporter` config -- no persistent-context logic, no `use.launchOptions`, nothing that tries
    to express `--load-extension` at the config level. Import `{ test, expect }` from `./fixtures`
    in the spec file instead of from `@playwright/test` directly.

    STEP 4 -- WRITE THE 21 TEST CASES. In `extension/e2e/dual-browser.spec.ts`, write one test case
    per success criterion from Phases 9-12 (21 total: Phase 9's 7, Phase 10's 5, Phase 11's 4, Phase
    12's 5 -- use the embedded SC inventory in this plan's `<context>` block verbatim; copy the exact
    SC wording from ROADMAP.md into each test title, do not paraphrase, so the mapping to the
    checklist is unambiguous). Group tests by phase using `test.describe()` blocks.

    WebAuthn / CDP virtual-authenticator usage -- narrow and precise, per the post-Phase-12
    architecture (the extension itself is the authenticator; a MAIN-world content-script patch
    brokers `navigator.credentials.create/get` through the popup's real consent UI -- a CDP virtual
    authenticator operates BELOW that JS API and never fires for a brokered ceremony):
      - P12-SC1 (`create()` registers a passkey) and P12-SC2 (`get()` logs in with a vault passkey):
        NO CDP virtual authenticator. Call `navigator.credentials.create()`/`get()` on a fixture
        page, then drive the REAL popup consent UI -- click `[data-testid=provider-confirm]` to
        approve, or `[data-testid=provider-credential-row-<itemId>]` to pick a credential for
        `get()` -- and assert the vault item was actually created/used. Do NOT assert a `hasPrf:false`
        vAuth forces the D-03 degradation banner on the provider ceremony: provider PRF is
        WASM-computed and capability-driven (D-16), so it never reads a browser authenticator's
        `clientExtensionResults` -- that assertion would be testing a mechanism the provider path
        doesn't use.
      - P12-SC3 fallthrough half (decline/no-match -> clean fallthrough to native OS authenticator):
        CDP virtual authenticator IS appropriate here, but only fires AFTER the shim falls through
        via `[data-testid=provider-decline]` -- it stands in for the native OS authenticator reached
        post-fallthrough, not for the brokered ceremony itself.
      - P9-SC2's ext-scoped PRF unlock half (unlocking via a PRF passkey from the popup, rpId =
        extension id): CDP virtual authenticator with `hasPrf: true`, attached to the POPUP's own CDP
        session (`popup.context().newCDPSession(popup)`), per probe-webauthn-rpid.js -- this one is a
        genuine browser WebAuthn call (the popup ceremony against the extension's own rpId), not a
        brokered provider ceremony.
      - Respect 12-07's overlay coordination + mediation: `passkeyCeremonyInFlight` suppresses the
        Phase-10 login overlay while a passkey ceremony is in flight, and passkey always wins when
        two in-page surfaces compete for attention; a provider test must NOT assert an autofill
        overlay is visible while a ceremony is in flight, and must account for conditional vs.
        immediate `get()` mediation when asserting overlay/ceremony visibility.

    Add a `"test:e2e:chrome": "playwright test --project=chromium"` script to
    `extension/package.json`.
  </action>
  <verify>
    <automated>cd extension && node -e "const fs=require('fs'); const c=fs.readFileSync('e2e/dual-browser.spec.ts','utf8'); const n=(c.match(/test\\(/g)||[]).length; if(n<21) process.exit(1); console.log('ok',n)"</automated>
    <automated>cd extension && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `extension/e2e/dual-browser.spec.ts` contains at least 21 `test(...)` cases, one per Phase 9-12 SC (Phase 9 SCs 1-7, not 1-5)
    - `extension/e2e/fixtures.ts` exports a `test.extend`-based `context` fixture (`chromium.launchPersistentContext`, `channel: 'chromium'`, `headless: true`, `--load-extension`/`--disable-extensions-except` pointed at `.output/chrome-mv3`) and an `extensionId` fixture derived from `context.serviceWorkers()`
    - `extension/playwright.config.ts` holds ONLY `testDir`/project/`reporter` config -- no `launchPersistentContext` or `--load-extension` logic (that lives in `fixtures.ts`) -- and defines exactly one `chromium` project
    - No `firefox` project exists in this Playwright config (Firefox extension loading is unsupported by Playwright — confirmed rationale documented in the file's header comment)
    - Provider create/get test cases (P12-SC1/SC2) drive the real `provider-confirm`/`provider-credential-row-<itemId>` consent UI, with NO CDP virtual authenticator attached to the brokered ceremony
    - CDP virtual-authenticator usage appears ONLY for P12-SC3's post-fallthrough half and P9-SC2's ext-scoped PRF-unlock half (on the POPUP's own CDP session)
    - `extension/vitest.config.ts`'s background and popup projects both exclude `e2e/**`, and `npm test` (extension's pre-existing vitest suite) reports the same full suite green as before this plan
    - `@playwright/test` is pinned to `1.61.1` in `package.json` (not a floating `latest`/caret-only range)
  </acceptance_criteria>
  <done>Playwright fixture-based harness exists, covers all 21 Phase 9-12 SCs as distinct test cases with correct CDP-vs-real-consent-UI boundaries, is scoped to Chromium only by design, and the pre-existing vitest suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full Chrome suite and author the 21-SC UAT checklist</name>
  <files>.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md</files>
  <read_first>extension/e2e/dual-browser.spec.ts</read_first>
  <action>
    Run `cd extension && npm run test:e2e:chrome`. For any failing test, determine whether the
    failure is a genuine Chrome-side regression (fix it in the owning phase's actual source file —
    this phase owns the fix, not just the finding, per 13-CONTEXT.md's scope) or a harness bug (fix
    the test), then re-run until the full suite is green on Chromium, subject to the HONESTY
    DISPOSITIONS below (some rows cannot be a clean Chromium green and must be marked honestly, not
    forced into a fabricated PASS).

    Author `.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md`: one row per SC (21 SC
    rows total, grouped by originating phase), columns `SC`, `Chrome`, `Firefox`, `Notes`. Fill the
    `Chrome` column from this run's actual pass/fail result (`PASS` or `FAIL — <reason>`). Leave
    `Firefox` as `PENDING` — Plan 13-04 fills it in.

    Include two extra invariant rows beyond the 21 SCs, per D-05/D-08 (already established, keep):
    "D-05: chrome.storage.session-only key survives idle-kill/wake (Chrome)" and "D-08: MAIN-world
    navigator.credentials patch injection behaves as designed (Chrome)" — both `PENDING` for the
    Firefox-equivalent columns.

    Include ONE MORE new row beyond those (21 SC rows + D-05 + D-08 + 1 = 24 total rows): "ext-scoped
    rpId-on-Firefox (closes wxt.config.ts open question)" — this row's Chrome column is `N/A
    (Firefox-only concern)`; Firefox column is `PENDING` — 13-04 fills it in (ties to the 13-04
    freshness-audit's B8 finding).

    HONESTY DISPOSITIONS (do not fabricate a green for a path not actually exercised):
    - P12-SC5 (security review confirms MAIN-world patch is a key-free RPC shim): this is NOT a
      Playwright test. Row's Chrome column = "verified by scripts/audit-mainworld-boundary.sh (exit
      0) + 12-05 security review — static audit, not a Playwright case". A spec MAY shell out to
      `scripts/audit-mainworld-boundary.sh` and assert exit 0, in which case record that instead.
    - P9-SC6 (pv-server CORS allowlist, chrome-extension:// AND moz-extension://): Chrome column =
      `PASS` for the chrome-extension origin half ONLY. Notes column: "moz-extension half deferred
      to 13-01/13-04/13-05 (Firefox) — still open per deferred-items.md:25-26".
    - P12-SC3 (decline/no-match -> clean fallthrough, with another password-manager extension
      installed): Chrome column = `PASS` for the fallthrough-to-native half (automated via CDP
      vAuth). Notes column: "the 'another PM extension installed' coexistence clause is MANUAL — see
      deferred-items.md D-15 install-order UAT, not automated here".
    - P12-SC4 (PRF used where allowed; Firefox/PRF-unavailable degrades honestly): Chrome column =
      `PASS` for the positive "PRF used" path ONLY. Notes column: "degradation copy is Firefox-side,
      verified in 13-04 — provider PRF is WASM-computed (D-16) so the banner won't fire on Chrome".
    - Also note in the checklist's preamble: Phase 11's theme/style-parity work (11-07/08/09) is
      taste/visual polish, NOT an SC — do not fabricate a visual-parity green row for it.
  </action>
  <verify>
    <automated>cd extension && npm run test:e2e:chrome -- --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `13-UAT-CHECKLIST.md` contains 24 rows (21 SCs + D-05 + D-08 + the new ext-scoped rpId-on-Firefox row)
    - Every row's Chrome column is `PASS`, `FAIL — <reason>`, the P12-SC5 audit-script disposition, or `N/A (Firefox-only concern)` for the new row — never blank
    - Every row's Firefox column is `PENDING`
    - P9-SC6, P12-SC3, and P12-SC4 rows carry the honesty-disposition Notes text above, not a blanket PASS
  </acceptance_criteria>
  <done>Chrome Playwright suite is green (or documented failures/dispositions are tracked with reasons), and the 24-row checklist exists with the Chrome column fully populated from a real run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test harness ↔ real vault/session data | Playwright drives a real extension instance; test fixtures must not touch production vault data |
| npm registry → dev machine | Installing `@playwright/test` pulls third-party browser-automation tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-13-06 | Tampering | CDP virtual WebAuthn authenticator used only in the test harness, and only for P12-SC3's post-fallthrough half + P9-SC2's ext-scoped PRF-unlock half — never attached to the brokered provider create/get ceremony | medium | mitigate | Virtual authenticator setup is scoped to `extension/e2e/dual-browser.spec.ts` only, gated behind `--project=chromium` test runs, never bundled into the packaged/production build (`.output/`) — confirmed by Task 1's config review |
| T-13-SC2 | Tampering | `npm i -D @playwright/test` (new devDependency, supply-chain surface) | high | mitigate | Pre-vetted in 13-RESEARCH.md's Package Legitimacy Audit (Approved: Microsoft-official, 42M+ weekly downloads); Task 1 re-confirms via automated `npm view` at install time — no blocking human checkpoint required per that audit's explicit discretion |
| T-13-07 | Information Disclosure | Playwright test run artifacts (screenshots, traces) accidentally capturing real vault secrets | medium | mitigate | Test suite uses a dedicated test account/vault (never a real production credential set), per this project's standing Playwright-UAT-authorized policy of self-validating with a test account |

</threat_model>

<verification>
- [ ] `npm run test:e2e:chrome` completes (green, or all failures/dispositions documented and traceable)
- [ ] `npm test` (extension's pre-existing vitest suite) still reports the full suite green — e2e/ is excluded, not collected
- [ ] `13-UAT-CHECKLIST.md` has 24 rows (21 SCs + D-05 + D-08 + ext-scoped rpId-on-Firefox) with Chrome column fully populated
- [ ] No `firefox` project exists in `playwright.config.ts`
- [ ] Commit atomically: `git add extension/playwright.config.ts extension/e2e/fixtures.ts extension/e2e/dual-browser.spec.ts extension/vitest.config.ts extension/package.json .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md` — never `git add -A`
</verification>

<success_criteria>
- All tasks completed
- Chrome Playwright suite covers all 21 Phase 9-12 SCs (Phase 9 has 7, not 5)
- 24-row UAT checklist exists with Chrome results recorded, including honest (non-fabricated) dispositions for P9-SC6, P12-SC3, P12-SC4, P12-SC5, and the new ext-scoped rpId-on-Firefox row
- Extension's pre-existing vitest suite (background + popup projects) remains green with e2e/ excluded
- No errors or warnings introduced
</success_criteria>

<output>
Create `.planning/phases/13-dual-browser-hardening/13-03-SUMMARY.md` when done — document any genuine Chrome-side divergence bugs found and fixed during this pass, with file paths and commit references.
</output>
