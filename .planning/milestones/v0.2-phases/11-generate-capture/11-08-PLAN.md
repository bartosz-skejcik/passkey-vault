---
phase: 11-generate-capture
plan: 08
type: execute
wave: 6
depends_on: ["11-07"]
files_modified:
  - extension/lib/autofill/inpage-theme.ts
  - extension/lib/autofill/inpage-theme.test.ts
  - extension/lib/autofill/inpage-mount.ts
  - extension/lib/autofill/inpage-mount.test.ts
  - extension/lib/autofill/inpage-overlay.ts
  - extension/lib/autofill/generate-popover.ts
  - extension/lib/autofill/generate-popover.test.ts
  - extension/lib/autofill/save-update-toast.ts
  - extension/lib/autofill/mismatch-modal.ts
  - extension/lib/i18n/autofill-dictionary.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every in-page surface (generate popover, save/update toast, mismatch modal, phase-10 overlay prompt window, in-field dropdown, field icon) renders from packages/pv-ui tokens in BOTH vault-light and vault-dark — no hand-rolled literal color values remain in surface CSS (D-12/D-13)."
    - "Surfaces follow the mirrored theme live: the mount stamps the resolved theme on the shadow panel root and re-stamps on chrome.storage change (11-07's resolveTheme/watchMirroredTheme)."
    - "The in-page generate popover's layout, control order, and copy mirror web/src/components/generator/GeneratorPopover.tsx one-to-one — a user who knows the web generator recognizes it instantly (Bartek: 'ma być taki sam')."
    - "The mismatch modal stays legible and SERIOUS in the light theme — warning banner keeps high-contrast error tokens (playfulness nigdy w dialogach bezpieczeństwa)."
    - "Zero behavioral change: message kinds, callbacks, gating, focus handling, and all pre-existing behavior tests stay green — this plan is presentation + theme stamping only."
  artifacts:
    - path: "extension/lib/autofill/inpage-theme.ts"
      provides: "Shadow-DOM stylesheet built from packages/pv-ui tokens.css text (vite raw/inline import) + shared font stack; single style source every surface injects"
  key_links:
    - from: "extension/lib/autofill/inpage-theme.ts"
      to: "packages/pv-ui/tokens.css"
      via: "raw CSS text import (?inline) — tokens are never re-declared by hand in the extension"
      pattern: "tokens.css"
    - from: "extension/lib/autofill/inpage-mount.ts"
      to: "extension/lib/theme/theme-mirror.ts"
      via: "mount stamps data-theme from resolveTheme(); watchMirroredTheme re-stamps live"
      pattern: "resolveTheme"
---

<objective>
Deliver the visual half of D-12/D-13: restyle every in-page surface onto the shared pv-ui tokens with live theme-following, and rebuild the generate popover layout 1:1 against the web GeneratorPopover.

Purpose: Bartek's frontend is vault-light; today every in-page surface is hard-coded dark and the generator looks nothing like the web one he asked to replicate.
Output: token-driven, theme-following surfaces; generator visually identical to the web's. Verified by packaged-build UAT screenshots in both themes (orchestrator pass).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-generate-capture/11-CONTEXT.md   # D-12 + D-13
@.planning/phases/11-generate-capture/11-07-SUMMARY.md # how tokens/mirror landed (package layout, import mechanism)
@web/src/components/generator/GeneratorPopover.tsx     # SOURCE OF TRUTH for the generator layout/controls/copy
@packages/pv-ui/tokens.css
@extension/lib/autofill/inpage-mount.ts
@extension/lib/autofill/inpage-overlay.ts

# NOTE: the review-fix round (CR-01/WR-01..05) already touched generate-popover.ts,
# mismatch-modal.ts, inpage-overlay.ts (safeRemove, shadow.activeElement focus trap).
# Read the CURRENT files — do not regress those fixes while restyling.
</context>

<tasks>

<task type="auto">
  <name>Task 1: inpage-theme.ts from package tokens + theme-stamping mount</name>
  <files>extension/lib/autofill/inpage-theme.ts, extension/lib/autofill/inpage-theme.test.ts, extension/lib/autofill/inpage-mount.ts, extension/lib/autofill/inpage-mount.test.ts</files>
  <read_first>packages/pv-ui/tokens.css, extension/lib/theme/theme-mirror.ts, extension/lib/autofill/inpage-mount.ts</read_first>
  <behavior>
    - inpage-theme.ts imports tokens.css as raw text (vite `?inline` — verify WXT supports it in content-script bundles; fallback: a tiny prebuild step or ts-embedded copy generated FROM the package file with a sync-check test that fails on drift) plus the "DM Sans", system-ui font stack, exported as one stylesheet string.
    - inpage-mount.ts: getOrCreateShadowRoot injects the theme stylesheet once, stamps `data-theme` on the panel container from resolveTheme() at mount, re-stamps via watchMirroredTheme; detaches the watcher when the mount is torn down.
    - Tests: both theme blocks present in the exported CSS; mount stamps vault-light when the mirror says so; storage flip re-stamps live.
  </behavior>
  <action>Build the shared style source first — every surface conversion in Task 2 depends on it.</action>
  <verify>
    <automated>cd extension && npx vitest run lib/autofill/inpage-theme lib/autofill/inpage-mount && npx tsc --noEmit</automated>
  </verify>
  <done>One injected stylesheet, theme-stamped mount, live re-stamping pinned by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Convert all surfaces to tokens; generator layout 1:1 with web</name>
  <files>extension/lib/autofill/inpage-overlay.ts, extension/lib/autofill/generate-popover.ts, extension/lib/autofill/generate-popover.test.ts, extension/lib/autofill/save-update-toast.ts, extension/lib/autofill/mismatch-modal.ts, extension/lib/i18n/autofill-dictionary.ts</files>
  <read_first>web/src/components/generator/GeneratorPopover.tsx (FULL read — layout, control order, labels, spacing, classes translate to token-based CSS), web/src/lib/i18n/dictionary.ts (generator copy keys — port missing ones with pl+en), extension/lib/autofill/generate-popover.ts (current, post-review-fix)</read_first>
  <behavior>
    - Every surface stylesheet swaps literal colors for var(--color-...) token references; no oklch()/hex literals outside inpage-theme.ts (grep-verifiable).
    - generate-popover.ts rebuilt visually to mirror GeneratorPopover.tsx: same control order (mode switch, preview row with regenerate/reveal, length control with live value, charset toggles / passphrase word-count+separator, strength meter if the web has one, primary apply button), same copy keys (ported to autofill-dictionary with pl+en per D-10), same radius/spacing scale. Imperative DOM stays; all existing behavior tests (generate-request round trip, click-only trigger, teardown, safeRemove) stay green.
    - Toast + modal + overlay (prompt window, dropdown, field icon) re-skinned on tokens; modal's warning banner uses error tokens at high contrast in BOTH themes.
    - Do NOT regress the review-fix round: safeRemove teardown, shadow.activeElement focus trap, trusted-origin handling stay byte-identical in behavior.
  </behavior>
  <action>
    Convert surface by surface, running each test file after conversion; finish with the full suite + both builds.
    SUMMARY must flag: packaged-build UAT with BOTH-theme screenshots of all six surfaces is required before Bartek review (orchestrator runs it).
  </action>
  <verify>
    <automated>cd extension && npx vitest run && npx tsc --noEmit && npx wxt build 2>&1 | tail -1 && npx wxt build -b firefox 2>&1 | tail -1</automated>
  </verify>
  <done>All surfaces token-driven and theme-following; generator visually 1:1 with web; zero behavioral regressions.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-11-33 | Information Disclosure | restyled surfaces | low | mitigate | Presentation-only change; masked previews (type=password), metadata-only rows, and no-secrets-in-light-DOM invariants unchanged and still pinned by existing tests. |

</threat_model>

<verification>
- [ ] `cd extension && npx vitest run` passes (all suites, incl. review-fix regression tests)
- [ ] `npx tsc --noEmit` passes; both browser builds green
- [ ] grep: no oklch(/#hex color literals in surface files outside inpage-theme.ts
- [ ] Orchestrator packaged-build UAT: 6 surfaces × 2 themes screenshots for Bartek
</verification>

<success_criteria>
- With the web app on vault-light, every in-page surface renders light with frontend tokens; toggling the web app flips them live
- Generator popover indistinguishable in layout from the web GeneratorPopover
</success_criteria>

<output>
Create `.planning/phases/11-generate-capture/11-08-SUMMARY.md` when done.
</output>
