---
phase: 11-generate-capture
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/entrypoints/background/autofill-match.ts
  - extension/entrypoints/background/autofill-match.test.ts
  - extension/entrypoints/popup/autofill/OnThisPageSection.tsx
  - extension/entrypoints/popup/autofill/OnThisPageSection.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "On a page with NO detected login form (detect ran, detected.login === false), the popup's 'Na tej stronie' section still lists LOGIN items whose origin matches the active tab — origin match alone is sufficient for logins in the POPUP channel (D-11, Bartek 2026-07-16)."
    - "Card and identity items remain detection-gated in the popup channel — they are not origin-bound, so un-gating them would surface every card on every page."
    - "TOTP items remain governed by the 10-08 policy unchanged: issuer-match AND detected.totp both required."
    - "The in-page overlay channel (autofill-frame.ts) behavior is byte-for-byte unchanged — it stays detection-gated; only handleAutofillMatch (popup) is relaxed."
  artifacts:
    - path: "extension/entrypoints/background/autofill-match.ts"
      provides: "handleAutofillMatch with the detected[kind] gate relaxed for kind === 'login' only"
    - path: "extension/entrypoints/background/autofill-match.test.ts"
      provides: "Tests pinning: login+origin+no-detection → included; card/identity+no-detection → excluded; totp policy unchanged; overlay channel untouched"
  key_links:
    - from: "extension/entrypoints/popup/autofill/OnThisPageSection.tsx"
      to: "extension/entrypoints/background/autofill-match.ts"
      via: "matches prop rendered from autofill.match response — rows now appear when detected is all-false but matches non-empty"
      pattern: "matches"
---

<objective>
Deliver Bartek's D-11 addendum (11-CONTEXT.md, 2026-07-16): the popup's "Na tej stronie" section must suggest origin-matching LOGIN items on pages without a detected login form (e.g. a dashboard after signing in). Deferred from Phase 10 (recorded in 10-VERIFICATION.md as "suggested-section origin boost for form-less pages").

Purpose: A user visiting a site they have credentials for should see those credentials suggested in the popup even when the current page happens to have no login form — matching NordPass behavior. This is a quality-of-life enhancement to the FILL-01 popup surface shipped in Phase 10 (all FILL requirements already Complete; this plan carries no new requirement ID).
Output: Relaxed login gate in `handleAutofillMatch` (popup channel only), tests pinning all four gating rules, stale popup comment fixed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-generate-capture/11-CONTEXT.md   # ADDENDUM 2026-07-16, D-11

# This plan touches PHASE 10 files, not the Phase 11 capture path. It is disjoint from
# plans 11-01..11-05 by files_modified. Read the real current state of both files before
# editing — Phase 10 round-2/round-3 fix batches landed after the original 10-06 SUMMARYs.

@extension/entrypoints/background/autofill-match.ts
@extension/entrypoints/background/frame-guard.ts
@extension/entrypoints/popup/autofill/OnThisPageSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relax the detection gate for logins in handleAutofillMatch (popup channel only)</name>
  <files>extension/entrypoints/background/autofill-match.ts, extension/entrypoints/background/autofill-match.test.ts</files>
  <read_first>extension/entrypoints/background/autofill-match.ts, extension/entrypoints/background/autofill-match.test.ts, extension/entrypoints/background/autofill-frame.ts, extension/entrypoints/background/frame-guard.ts</read_first>
  <behavior>
    - handleAutofillMatch: a LOGIN item passing `itemMatchesOrigin(item, target.origin)` is included in `matches` even when `detectResponse.detected.login === false` (detect answered, page simply has no login form).
    - handleAutofillMatch: card and identity items are still excluded when their `detected[kind]` is false (unchanged — they are not origin-bound; see frame-guard's itemMatchesOrigin treatment).
    - handleAutofillMatch: totp items still require BOTH the 10-08 issuer-match policy AND `detected.totp === true` (unchanged).
    - The `pageState: "unreachable"` path (content-relay never answered) still returns empty matches — this plan relaxes only the "detect ran, nothing login-shaped found" case, never fabricates reachability.
    - The locked path and the `restricted` path are unchanged (empty matches).
    - autofill-frame.ts (in-page overlay channel) — zero behavior change; if it shares a helper with handleAutofillMatch, the relaxation must live where ONLY the popup channel sees it, and a test must pin the overlay channel's login gating still requires detection.
  </behavior>
  <action>
    RED first: extend autofill-match.test.ts with the new cases (login+origin+detected.login:false → included;
    card+detected.card:false → excluded; identity+detected.identity:false → excluded; totp issuer-match but
    detected.totp:false → excluded; unreachable → still empty). Confirm the login case FAILS against current code.
    Then edit the match loop in handleAutofillMatch: skip the `!detectResponse.detected[kind]` continue for
    kind === "login" only. Keep the origin gate (`itemMatchesOrigin`) exactly as is. Do not touch
    buildFillValues, maskedHintFor, or any fill/totpCode handler. Verify autofill-frame.ts does not import
    the changed code path (it reuses exported pure helpers, not handleAutofillMatch itself — confirm and, if
    that assumption is wrong, restructure so the overlay keeps its gate; pin it with a test either way).
    GREEN: all new + all pre-existing tests pass.
  </action>
  <verify>
    <automated>cd extension && npx vitest run entrypoints/background/autofill-match entrypoints/background/autofill-frame</automated>
  </verify>
  <done>Login items origin-matched on form-less pages flow into popup matches; card/identity/totp gating and the overlay channel are pinned unchanged by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the stale OnThisPageSection comment and pin the form-less rendering</name>
  <files>extension/entrypoints/popup/autofill/OnThisPageSection.tsx, extension/entrypoints/popup/autofill/OnThisPageSection.test.tsx</files>
  <read_first>extension/entrypoints/popup/autofill/OnThisPageSection.tsx, extension/entrypoints/popup/ItemListView.tsx</read_first>
  <action>
    The header comment (lines ~7-9) and the `matches` prop docblock (~lines 69-70) claim the prop is a
    "popup-computed, detection-UNGATED login/origin match set" — that was the deferred intent, not reality.
    After Task 1 it becomes true, but the un-gating lives in the BACKGROUND (handleAutofillMatch), not
    popup-computed. Rewrite both comments to describe the real mechanism ("background relaxes the
    detection gate for logins as of 11-06; card/identity/totp remain gated there").
    Add one rendering test: `pageState: "ok"`, `detected` all-false, one login match in `matches` →
    the row renders (section not in empty state). No component logic change is expected — the component
    already renders purely from `matches`; if a hidden detected-based guard emerges, remove it.
    Verify the fill-failure UX on a form-less page manually in the packaged-build UAT (fill → content.fill
    finds no login fields → row's existing error toast shows) — no new UI is built for that case this plan.
  </action>
  <verify>
    <automated>cd extension && npx vitest run entrypoints/popup/autofill/OnThisPageSection && npx tsc --noEmit</automated>
  </verify>
  <done>Comments describe the real post-11-06 mechanism; a test pins that a login match renders with detected all-false.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| background service worker -> popup | `autofill.match` response — metadata only (label + maskedHint), never plaintext values. Unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-11-20 | Information Disclosure | `handleAutofillMatch` | low | mitigate | Relaxation applies ONLY to login items and ONLY behind the unchanged `itemMatchesOrigin` origin gate — no item metadata ever surfaces for a non-matching origin, restricted page, or locked vault. Cards/identities stay detection-gated precisely because they are origin-free. |
| T-11-21 | Elevation of Privilege | overlay channel (`autofill-frame.ts`) | medium | mitigate | The in-page overlay must NOT inherit the relaxed gate (a page could otherwise probe for the mere existence of credentials without presenting a form). Pinned by a dedicated test in Task 1. |

</threat_model>

<verification>
- [ ] `cd extension && npx vitest run entrypoints/background/autofill-match entrypoints/background/autofill-frame` passes
- [ ] `cd extension && npx vitest run entrypoints/popup/autofill/OnThisPageSection` passes
- [ ] `cd extension && npx tsc --noEmit` passes
</verification>

<success_criteria>
- Both tasks completed
- Popup "Na tej stronie" lists origin-matching login items on a form-less page (packaged-build UAT: open the popup on a logged-in dashboard page of a site with a saved login)
- Overlay behavior, TOTP 10-08 policy, and card/identity gating provably unchanged (tests)
</success_criteria>

<output>
Create `.planning/phases/11-generate-capture/11-06-SUMMARY.md` when done.
</output>
