---
phase: 09-session-unlock-core-popup-sync-client
plan: 07
type: execute
wave: 6
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06", "09-08"]
files_modified: []
autonomous: false
requirements: [EXT-02, EXT-03, EXT-04, EXT-05, EXT-06]

must_haves:
  truths:
    - "Every one of Phase 9's 7 ROADMAP success criteria is confirmed against a real loaded extension and a real running pv-server, not simulated or assumed"
    - "The unlocked vault survives a genuine 60+ second service-worker idle-kill/wake cycle (not a page reload, not a mocked termination)"
    - "A real chrome-extension://<id> origin request against a real running pv-server is accepted by the CORS allowlist"
  artifacts: []
  key_links: []
---

<objective>
Discharge the seven Manual-Only Verifications 09-VALIDATION.md enumerates — the ROADMAP's 7 Phase 9 success criteria, none of which have (or should have) an automated substitute per 09-RESEARCH.md Pitfall 3 and CONTEXT.md D-08/D-10's explicit "prove it against the real platform, not a mock" requirements.

Purpose: Plans 09-01 through 09-06 each carry their own automated unit/integration coverage, but SC #1 (real server reachability failure modes), #3 (real 60+s idle-kill), #5 (real cross-client sync), #6 (real cross-origin CORS request from a really-loaded extension), and #7 (real new-tab navigation) are all platform-level behaviors this environment cannot simulate faithfully — this plan is the single consolidated human verification pass that closes them, per `workflow.human_verify_mode = end-of-phase` (the same methodology Phase 8's 08-03-PLAN.md already established for SC #1/#3/#4 of that phase).
Output: A completed `09-UAT.md` (or equivalent) recording pass/fail + evidence for all 7 success criteria, plus a lightweight Chrome+Firefox sanity pass per CONTEXT.md's standing hygiene rule.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-VALIDATION.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-CONTEXT.md

# All seven prior plans' SUMMARYs — read every one before starting, since
# this plan's checkpoints assume all of them landed correctly:
@.planning/phases/09-session-unlock-core-popup-sync-client/09-01-SUMMARY.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-02-SUMMARY.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-03-SUMMARY.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-04-SUMMARY.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-05-SUMMARY.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-06-SUMMARY.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build packaged extension outputs and start a real pv-server</name>
  <files></files>
  <read_first>extension/wxt.config.ts, crates/pv-server/src/config.rs</read_first>
  <action>
  Run `cd extension && npx wxt build -b chrome && npx wxt build -b firefox` (packaged builds, per Phase 8's D-10 methodology — not `wxt dev`). Locate the output manifests. Start a real `pv-server` instance (`cargo run -p pv-server` or the existing Docker Compose setup from v0.1, whichever is already the developer's standard local workflow) with `PV_EXTENSION_ORIGINS` set to the Chrome extension's actual packaged ID (confirm the ID via `chrome://extensions` after loading, per Plan 09-03's `manifest.key`/Phase 8's ID-pinning findings) and, if Firefox's `gecko.id` is loaded too, that origin as well.
  </action>
  <verify>
    <automated>cd extension && npx wxt build -b chrome 2>&1 | tail -5 && npx wxt build -b firefox 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both packaged builds exit 0
    - A real pv-server process is running and reachable at a known local URL before Task 2 begins
  </acceptance_criteria>
  <done>Packaged Chrome and Firefox builds exist, and a real pv-server instance is running with the extension's real origin(s) in its PV_EXTENSION_ORIGINS allowlist, ready for the human-check tasks below.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: SC #1 / EXT-05 — first-run server configuration against a real server</name>
  <what-built>ServerConfigView (Plan 09-06) + server-config.ts's real-healthz validation (Plan 09-03).</what-built>
  <how-to-verify>
    1. Load the packaged Chrome build unpacked (chrome://extensions, Developer mode, Load unpacked). Open the popup on first install.
    2. Confirm the popup shows the server-configuration screen (not unlock) before any server is configured.
    3. Enter an unreachable URL (e.g. `http://localhost:1`) — confirm a clear, non-generic reachability error, and that nothing is persisted (reopen the popup — the config screen still shows, empty).
    4. Enter a URL pointing at something that returns HTTP 200 but is NOT pv-server (e.g. `http://localhost:1` swapped for any other locally running HTTP server, or a static file server) — confirm it is REJECTED (the `/healthz` body-shape check, Plan 09-03 Task 1 Test 3, must reject a same-200 non-vault response).
    5. Enter the real running pv-server's URL from Task 1 — confirm it succeeds and the popup transitions to the sign-in/unlock view.
    6. Reopen the popup — confirm the server URL is remembered (persisted in `chrome.storage.local`, survives a popup close/reopen).
  </how-to-verify>
  <resume-signal>Type "approved" once all 6 steps pass as described, or describe the specific step and failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: SC #2 / EXT-02 — sign in (fresh install, mints a token) and unlock again (existing token), with password and with the EXTENSION-SCOPED PRF passkey</name>
  <what-built>UnlockView.tsx's Sign-in and Unlock-only variants + EnrollExtPasskeyPrompt (Plan 09-06 as amended 2026-07-15) + unlock.ts's password sign-in/unlock handlers (Plan 09-04) + the extension-scoped passkey enroll/unlock chain (Plan 09-08).</what-built>
  <how-to-verify>
    AMENDED 2026-07-15 (extension-scoped PRF passkey — 09-CONTEXT AMENDMENT / Plan 09-08): the popup's PRF path uses a DEDICATED extension-scoped passkey (rpId = extension ID), never v0.1's server-registered web-RP passkeys (browser-level SecurityError from a chrome-extension:// page — empirically proven). The Sign-in variant is password-only by design (extension passkey is UNLOCK-ONLY). Steps 3-4's original web-RP PRF instructions are superseded below.
    1. On a genuinely fresh install (no prior token — confirm the popup shows the Sign-in variant with BOTH an email and a password field, and NO passkey button, since `session.status` resolves `no-session`), using an existing v0.1 account's email + master password, submit the Sign-in form (`auth.signIn.password` — mints a token, unwraps the UK in one round trip). Confirm the popup transitions directly to the item list.
    2. Immediately after this password unlock, confirm the DISCREET enrollment prompt renders at the top of the item list ("Odblokowuj szybciej passkeyem" / "Unlock faster with a passkey" — 09-UI-SPEC amendment copy). Confirm "Nie teraz"/"Not now" dismisses it without side effects (reopen popup while still unlocked — it does not reappear this unlock). Then lock + password-unlock again and this time click the enroll CTA: confirm the OS/browser passkey-creation prompt appears (rpId = the extension's own ID), a second confirmation (PRF eval get()) may follow, and the prompt reports success. Verify a row exists server-side (`GET /api/extension-passkeys` with the session's bearer token returns exactly one credential) and that the blob is opaque ciphertext.
    3. Lock the vault (auto-lock or lock affordance). Reopen the popup — Unlock-only variant renders (password field, no email) AND the "Odblokuj passkeyem" / "Unlock with passkey" button now renders (it must NOT have rendered before enrollment — that absence is part of this check). Click it: dispatches `unlock.extPrf.start`/`unlock.extPrf.finish`; confirm the OS/browser assertion prompt appears and the vault unlocks WITHOUT the password.
    4. Force a REAL service-worker idle-kill (chrome://serviceworker-internals "Stop", or 60+s genuine idle), then lock and PRF-unlock AGAIN — proving the enroll → lock → PRF unlock → idle-kill → PRF unlock loop end-to-end (local meta record + session-meta + storage.session envelope all survive independently of the SW lifetime).
    5. Fully close and reopen the browser (clears `chrome.storage.session`, including the token). Confirm the Sign-in variant renders with password only — the extension passkey correctly does NOT offer itself for sign-in (UNLOCK-ONLY, no token to authorize the blob fetch). Sign in with the password and confirm the enrollment prompt does NOT reappear (already enrolled).
    6. Honest degradation on a PRF-less authenticator (CDP virtual authenticator with hasPrf: false, or a real non-PRF key): the enroll CTA's ceremony completes but the prompt renders the "authenticator doesn't support PRF" copy (`extPasskey.enrollNoPrf`), never a silent failure or a stuck spinner, and NO server row is created. Additionally exercise the orphaned-credential path: delete the server row (or wipe the server DB row) and attempt PRF unlock — confirm the typed orphaned-passkey copy renders and the password field takes focus; after that failure the PRF button stops rendering (stale local meta cleared).
    7. "Don't ask again": on a fresh profile (or after clearing storage.local), password-unlock, check "Nie pytaj ponownie"/"Don't ask again" — confirm the prompt never reappears across lock/unlock cycles and browser restarts.
  </how-to-verify>
  <resume-signal>Type "approved" once password sign-in, enrollment, the enroll → lock → PRF unlock → idle-kill → PRF unlock loop, the UNLOCK-ONLY boundary (step 5), and both honest-degradation paths (step 6) all work end-to-end, or describe the specific step and failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: SC #3 — real 60+ second service-worker idle-kill/wake survival</name>
  <what-built>vault-session.ts's chrome.storage.session envelope + ensureHydrated() re-hydration (Plan 09-02).</what-built>
  <how-to-verify>
    1. Unlock the vault via the popup (per the prior checkpoint).
    2. Open chrome://extensions, find the loaded extension, open its background service worker's DevTools, and use the actual "stop" control to force a genuine platform-level termination — NOT a popup close, NOT a page reload, NOT disabling/re-enabling the extension. (Per 09-RESEARCH.md Pitfall 3, immediately re-testing without a real termination does not exercise this path at all.)
    3. Reopen the popup. Confirm the vault is STILL unlocked (item list renders directly, no re-prompt) — this proves the session envelope survived in chrome.storage.session and ensureHydrated() correctly re-instantiated pv-wasm and re-imported the key bytes.
    4. As a stronger variant, leave the browser genuinely idle for 60+ seconds (no interaction) to let Chrome's own idle-kill timer fire naturally, then reopen the popup and confirm the same result.
  </how-to-verify>
  <resume-signal>Type "approved" once the vault survives a REAL termination (not simulated), or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: SC #4 / EXT-03 — auto-lock idle timeout and browser-close clear</name>
  <what-built>autolock.ts's chrome.alarms-driven timer (Plan 09-02) + the popup's auto-lock select (Plan 09-06).</what-built>
  <how-to-verify>
    1. Unlock the vault, set the auto-lock select to its shortest option (5 min, or temporarily verify with a lower value if the codebase exposes one for testing — otherwise budget the real 5-minute wait).
    2. Leave the extension idle (no popup interaction) for the configured duration. Reopen the popup — confirm it shows the Unlock-only variant (password field only, no email — the bearer token minted at sign-in survived the lock per Plan 09-02's Blocker-2 fix) with the "session locked after being idle" notice (09-UI-SPEC.md's session-locked copy), and requires unlock again.
    3. Unlock again, then fully close and reopen the browser (not just the popup). Confirm the popup now shows the Sign-in variant (email + password) — chrome.storage.session's browser-restart clear (D-04) wipes BOTH the key material and the session token this time (unlike an auto-lock, which only clears the key), so a fresh sign-in (not merely an unlock) is required, per 09-CONTEXT.md.
  </how-to-verify>
  <resume-signal>Type "approved" once both the idle-timeout lock and the browser-restart clear are confirmed, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: SC #5 / EXT-04 — browse/search/pick and cross-client sync visibility</name>
  <what-built>ItemListView/ItemDetailView (Plan 09-06) + sync-client.ts/vault-store.ts (Plan 09-05).</what-built>
  <how-to-verify>
    1. With the vault unlocked in the popup, confirm the item list renders real items from the account, search filters instantly with no visible network delay, and clicking an item opens the detail pane with correct fields (password masked by default, copy-to-clipboard works).
    2. With the popup open (or the background running), open the v0.1 web app in a browser tab against the SAME pv-server, log in to the same account, and create or edit a vault item there.
    3. Within ~30 seconds (the poll fallback interval) or immediately (if the WS push fires), confirm the new/edited item appears in the extension popup's list without manually reopening the popup — proving the extension as a genuine third synced client, same as v0.1 Phase 5's two-tab proof.
  </how-to-verify>
  <resume-signal>Type "approved" once cross-client sync is confirmed working both directions, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: SC #6 / EXT-05 — real CORS allowlist proof</name>
  <what-built>build_cors_layer's PV_EXTENSION_ORIGINS allowlist (Plan 09-01).</what-built>
  <how-to-verify>
    1. With Task 1's pv-server running and PV_EXTENSION_ORIGINS set to the real loaded extension's origin, confirm every REST call the popup/background makes (already exercised by the SC #1/#2/#5 checkpoints above) succeeds — no CORS error in the background service worker's console.
    2. As a negative check, temporarily unset or misconfigure PV_EXTENSION_ORIGINS (or restart pv-server without it) and confirm the SAME extension's requests now fail with a browser-level CORS error — proving the allowlist is actually doing something, not merely coincidentally permissive.
    3. Restore the correct PV_EXTENSION_ORIGINS value before continuing.
  </how-to-verify>
  <resume-signal>Type "approved" once both the positive and negative CORS checks are confirmed, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: SC #7 / EXT-06 — "open full vault" opens the correct URL in a new tab</name>
  <what-built>ItemListView's open-full-vault control (Plan 09-06).</what-built>
  <how-to-verify>
    1. Click the "open full vault" control in the popup footer. Confirm a new browser tab opens, navigating to the exact server URL configured in the SC #1 checkpoint's step 5 (not a different origin, not a blank/error page).
    2. Confirm the v0.1 web app loads and, since the extension's session token and the web app's are independent, log in there separately (or confirm it is already logged in if the same browser profile shares a session) — this proves the "no full vault management re-implemented in the extension" boundary is intact (the popup deliberately doesn't render this UI itself).
  </how-to-verify>
  <resume-signal>Type "approved" once the correct tab opens with the correct URL, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Lightweight Chrome + Firefox sanity pass</name>
  <what-built>The whole popup, on both browser targets.</what-built>
  <how-to-verify>
    1. Repeat a shortened version of the SC #1/#2/#5/#7 checks above against the packaged Firefox build (`about:debugging#/runtime/this-firefox`, Load Temporary Add-on) — this is the lightweight hygiene check CONTEXT.md requires every phase, NOT the exhaustive Phase 13 hardening pass.
    2. Confirm zero console errors on load in both browsers' extension consoles, and that PRF's honest-degradation copy (if Firefox lacks PRF support on this test setup) renders correctly rather than silently failing.
  </how-to-verify>
  <resume-signal>Type "approved" once the Firefox sanity pass shows no blocking divergence from Chrome, or describe any found (to be triaged — full parity work is Phase 13's, not this checkpoint's, to fix).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This plan introduces no new code, hence no new trust boundary — it is the verification pass over Plans 09-01..09-06's already-registered boundaries (T-09-01..T-09-23) |

## STRIDE Threat Register

No new threats — this plan verifies the mitigations already registered in Plans 09-01 through 09-06's threat models are correctly realized in the real, running system. Any gap discovered during a checkpoint above must be filed as a follow-up fix against the owning plan's threat ID, not silently accepted here.

</threat_model>

<verification>
- [ ] All 7 ROADMAP success criteria checkpoints approved
- [ ] Lightweight Chrome + Firefox sanity pass approved
- [ ] Any issues found are filed as explicit follow-ups referencing the owning plan (09-01..09-06), not silently dropped
</verification>

<success_criteria>
- Every one of Phase 9's 7 success criteria is confirmed against real running software, not simulated
- Phase 9 is ready for `/gsd-verify-work`
</success_criteria>

<output>
Create `.planning/phases/09-session-unlock-core-popup-sync-client/09-07-SUMMARY.md` and `.planning/phases/09-session-unlock-core-popup-sync-client/09-UAT.md` recording each checkpoint's outcome, following @$HOME/.claude/gsd-core/templates/summary.md's conventions plus Phase 8's 08-03-SUMMARY.md's UAT-recording precedent.
</output>
