---
phase: 20-test-infrastructure-ci-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/package.json
  - extension/e2e-firefox/README.md
autonomous: true
requirements: [QA-02]

must_haves:
  truths:
    - "Every real-Firefox probe in extension/e2e-firefox/*.cjs has its own npm script — none is reachable only by a hand-typed `node e2e-firefox/probe-*.cjs` command"
    - "extension/e2e-firefox/README.md's \"## Running\" section documents all six lanes (core, autofill, server-unlock, window-geometry, request-xray, provider-corruption), not just the original three"
  artifacts:
    - extension/package.json (test:e2e:firefox:request-xray, test:e2e:firefox:provider-corruption, and matching pretest:* hooks)
    - extension/e2e-firefox/README.md ("## Running" section extended to 6 entries)
  key_links:
    - "extension/package.json's new test:e2e:firefox:request-xray/provider-corruption scripts -> extension/e2e-firefox/probe-request-xray.cjs / probe-provider-corruption.cjs (the two previously-orphan files)"
    - "extension/package.json's new pretest:e2e:firefox:* hooks -> `wxt build -b firefox` (mirrors the 4 existing pretest:e2e:firefox:* entries exactly)"
---

<objective>
Close QA-02's real gap, per 20-RESEARCH.md's direct-read inventory: `probe-request-xray.cjs` (Phase 14's permanent XBR-02 regression gate) and `probe-provider-corruption.cjs` (the permanent byte-level provider-response regression gate) have NO npm script at all and are undocumented in the README's "## Running" section — reachable only by a hand-typed `node e2e-firefox/probe-*.cjs` invocation. `probe-window-geometry.cjs` (Phase 18) already has an npm script but is also missing from "## Running" (only mentioned in the Prerequisites UUID table).

Purpose: every manual real-Firefox probe is wired to its own npm script AND documented as a harness lane — closing the exact "orphan probe" gap the v0.3 codebase-gaps sweep flagged.
Output: `extension/package.json` gains 2 new npm scripts + 2 matching `pretest:*` build hooks; `extension/e2e-firefox/README.md`'s "## Running" section documents all 6 lanes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-test-infrastructure-ci-gate/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire probe-request-xray.cjs and probe-provider-corruption.cjs to npm scripts</name>
  <files>extension/package.json</files>
  <read_first>
    extension/package.json (the existing `test:e2e:firefox:core`/`:autofill`/`:server-unlock`/`:window-geometry` entries and their matching `pretest:e2e:firefox:*` hooks — the exact pattern to mirror)
    extension/e2e-firefox/probe-request-xray.cjs (header comment's own "Prerequisites: identical to run-core.cjs/probe-provider-corruption.cjs ... `npm run build:firefox` already run" — confirms the pretest hook must be `wxt build -b firefox`, same as the 4 existing entries)
    extension/e2e-firefox/probe-provider-corruption.cjs (header comment's matching prerequisites note)
  </read_first>
  <action>
    Add two new npm scripts to `extension/package.json`'s `scripts` object, placed adjacent to the existing `test:e2e:firefox:*` family: `test:e2e:firefox:request-xray` running `node e2e-firefox/probe-request-xray.cjs`, and `test:e2e:firefox:provider-corruption` running `node e2e-firefox/probe-provider-corruption.cjs`. Add matching `pretest:e2e:firefox:request-xray` and `pretest:e2e:firefox:provider-corruption` entries, both running `wxt build -b firefox` — byte-for-byte the same pretest pattern already used by `pretest:e2e:firefox:core`/`:autofill`/`:server-unlock`/`:window-geometry`. Do not touch any other script in the file.
  </action>
  <acceptance_criteria>
    - `extension/package.json`'s `scripts` object contains exactly these 4 new keys: `test:e2e:firefox:request-xray`, `test:e2e:firefox:provider-corruption`, `pretest:e2e:firefox:request-xray`, `pretest:e2e:firefox:provider-corruption`
    - Values match exactly: `"node e2e-firefox/probe-request-xray.cjs"`, `"node e2e-firefox/probe-provider-corruption.cjs"`, `"wxt build -b firefox"`, `"wxt build -b firefox"`
    - JSON remains valid (parses with `node -e "require('./extension/package.json')"`)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./extension/package.json'); const need=['test:e2e:firefox:request-xray','test:e2e:firefox:provider-corruption','pretest:e2e:firefox:request-xray','pretest:e2e:firefox:provider-corruption']; const missing=need.filter(k=>!p.scripts[k]); if(missing.length) throw new Error('missing scripts: '+missing.join(', ')); console.log('OK: all 4 scripts present')"</automated>
  </verify>
  <done>`npm run test:e2e:firefox:request-xray` and `npm run test:e2e:firefox:provider-corruption` are both valid, runnable npm commands (resolvable by `npm run`, not necessarily executed here — running them requires a live pv-server, covered by Plan 20-03's live-proof task); no other script in the file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Document all 6 Firefox harness lanes in README's "## Running" section</name>
  <files>extension/e2e-firefox/README.md</files>
  <read_first>
    extension/e2e-firefox/README.md (the existing "## Running" section's 3 documented entries and their one-line comments; the "Prerequisites" section's UUID table, which already names all 6 lane files/UUIDs and is the source for each lane's one-line purpose)
  </read_first>
  <action>
    Extend the "## Running" section's fenced command block with 3 additional lines, one per undocumented lane, following the exact existing style (`npm run test:e2e:firefox:&lt;name&gt;` followed by a `#`-prefixed one-line description naming the phase/plan it originated from, matching the 3 existing lines' format): `test:e2e:firefox:window-geometry` (Plan 18-01, UX-02 window centering/sizing/self-close), `test:e2e:firefox:request-xray` (Plan 14-03, the permanent XBR-02 response-direction byte-identity regression gate), `test:e2e:firefox:provider-corruption` (the permanent provider-response byte-level regression gate, `.planning/debug/resolved/firefox-provider-corruption.md`). Do not restructure or remove the existing 3 entries or the surrounding prose paragraph about screenshots/`.ff-profile/`.
  </action>
  <acceptance_criteria>
    - The "## Running" section's command block lists all 6 `npm run test:e2e:firefox:*` lanes (core, autofill, server-unlock, window-geometry, request-xray, provider-corruption), each with a one-line description
    - No existing line in "## Running" is altered or removed
  </acceptance_criteria>
  <verify>
    <automated>awk '/^## Running/,/^## Environment variables/' extension/e2e-firefox/README.md | grep -c 'npm run test:e2e:firefox:'</automated>
  </verify>
  <done>The awk-scoped grep count above returns 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| README/npm-script documentation (no runtime code path) | This plan only adds package.json script entries and documentation prose; it does not touch any runtime request/response handling code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-20-02-01 | Information Disclosure | extension/e2e-firefox/README.md's documented default UAT credentials (`PV_UAT_EMAIL`/`PV_UAT_PASSWORD`) | low | accept | Pre-existing project convention (documented since Plan 13-04) for a throwaway self-hosted local-dev-only account; this plan adds no new credential documentation, only extends the "## Running" list of commands — never invoked from cloud CI (QA-02's explicit local/self-hosted-only scope) |
| T-20-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan introduces zero new package-manager dependencies — only 4 new `package.json` script string values (referencing files that already exist) and README prose |
</threat_model>

<verification>
`node -e "require('./extension/package.json')"` succeeds (valid JSON); the README "## Running" grep count is 6; `git diff extension/package.json` shows only the 4 new script keys added, no other line changed.
</verification>

<success_criteria>
QA-02's "documented" half is closed for `probe-request-xray.cjs`/`probe-provider-corruption.cjs`/`probe-window-geometry.cjs`: all 6 real-Firefox harness lanes have both an npm script and a "## Running" section entry — no probe is reachable only by a hand-typed command.
</success_criteria>

<output>
Create `.planning/phases/20-test-infrastructure-ci-gate/20-02-SUMMARY.md` when done.
</output>