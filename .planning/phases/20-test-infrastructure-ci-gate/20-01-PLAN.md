---
phase: 20-test-infrastructure-ci-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/pv-provider/tests/response_shape.rs
autonomous: true
requirements: [QA-04]

must_haves:
  truths:
    - "cargo test -p pv-provider passes, including the two new response_shape tests, proving every binary WebAuthn response field pv-provider emits is a base64url STRING on the wire"
    - "Temporarily disabling passkey-types' serialize_bytes_as_base64_string Cargo feature makes the new tests fail (negative-control proof performed once during this task, then reverted)"
  artifacts:
    - crates/pv-provider/tests/response_shape.rs
  key_links:
    - "response_shape.rs -> ceremony::create_provider_credential/get_provider_assertion (the functions whose serialized JSON it gates)"
    - "response_shape.rs's per-field assertions -> crates/pv-provider/Cargo.toml's passkey-types serialize_bytes_as_base64_string feature flag (the exact toggle the v0.2 XBR-02-class bug regressed)"
---

<objective>
Add a permanent Rust unit gate (QA-04) that enumerates every binary WebAuthn response field `pv-provider` emits (create AND get ceremonies) and asserts each is a base64url STRING, not a bare JSON number array — the exact regression class (`.planning/debug/resolved/firefox-provider-corruption.md`) that let the v0.2 serialization bug ship undetected through every existing `.ok`/`id`-only test.

This is complementary to, not a duplicate of, Phase 14's `tests/real_rp_verification.rs` (QA-03, cross-vendor interop via deserialize) and `probe-request-xray.cjs` (cross-realm delivery on real Firefox) — those prove the bytes round-trip correctly; this test proves the exact WIRE SHAPE with a field-by-field assertion message, so a regression fails loud and specific instead of a generic deserialize error somewhere downstream.

Purpose: Close QA-04's Rust-unit-gate half (the cross-realm half is already satisfied by Phase 14's `probe-request-xray.cjs`).
Output: `crates/pv-provider/tests/response_shape.rs` — a new Cargo integration test with two `#[test]` functions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-test-infrastructure-ci-gate/20-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QA-04 byte-shape regression test for pv-provider's WebAuthn response serialization</name>
  <files>crates/pv-provider/tests/response_shape.rs</files>
  <read_first>
    crates/pv-provider/src/lib.rs (fixture_create_request/fixture_get_request pattern and the local `base64url()` helper in `#[cfg(test)] mod tests` — these are private to lib.rs and NOT importable from an integration test; duplicate small equivalents locally in the new file, matching `tests/real_rp_verification.rs`'s own precedent of an independent fixture-owning test file)
    crates/pv-provider/src/ceremony.rs (`create_provider_credential`/`get_provider_assertion` signatures and doc comments — both are `pub fn`, reachable from `tests/`)
    crates/pv-provider/Cargo.toml (the `passkey-types = { version = "=0.5.0", features = ["serialize_bytes_as_base64_string"] }` line and its D-21 comment block — the exact feature this test guards)
    crates/pv-provider/tests/real_rp_verification.rs (existing sibling integration test — mirror its file-level doc-comment style and its own local fixture-building approach)
  </read_first>
  <behavior>
    - Test `create_response_binary_fields_are_base64url_strings`: build a create-request JSON (rp_id "example.com", `with_prf: true` so `clientExtensionResults.prf.results` may also be exercised, mirroring lib.rs's `fixture_create_request` shape), call `create_provider_credential(&request_json, "https://example.com")`, parse `credential_response_json` as `serde_json::Value`. For each of `rawId`, `response.clientDataJSON`, `response.attestationObject`, `response.authenticatorData` (all always-present `Bytes` fields on `AuthenticatorAttestationResponse`/the outer `PublicKeyCredential`), assert the JSON value `.is_string()` with a per-field panic message naming the field and warning it must be a base64url STRING not an array (the exact v0.2 regression shape), then assert `passkey_types::encoding::try_from_base64url(value.as_str().unwrap()).is_some()` to confirm it decodes as valid base64url, not just any string. `response.publicKey` is `Option<Bytes>` (`skip_serializing_if` — may be absent) — if present, apply the same two assertions; do not require its presence. Same conditionally for `clientExtensionResults.prf.results.first` (and `.second` if present) — PRF results depend on authenticator support at creation time and may legitimately be absent; assert the shape ONLY when present (explicit assumption, not silently dropped — document in a comment why presence is not asserted).
    - Test `get_response_binary_fields_are_base64url_strings`: reuse the create step's `new_passkey_json` (wrapped `[new_passkey_json]`) as `existing_credentials_json`, build a get-request JSON via a locally duplicated `fixture_get_request` equivalent, call `get_provider_assertion(&get_request_json, "https://example.com", &existing_credentials_json)`, parse `credential_response_json`. For `rawId`, `response.clientDataJSON`, `response.authenticatorData`, `response.signature` (always-present `Bytes` on `AuthenticatorAssertionResponse`/the outer `PublicKeyCredential`), apply the same is_string + try_from_base64url assertions. `response.userHandle` is `Option<Bytes>` — assert the same two checks only if present.
  </behavior>
  <action>
    Create `crates/pv-provider/tests/response_shape.rs` as a new Cargo integration test (compiled/run by `cargo test -p pv-provider`, discovered automatically — no `[[test]]` entry needed in Cargo.toml, matching `tests/real_rp_verification.rs`'s existing precedent). Open with a file-level doc comment explaining this is QA-04's dedicated field-enumerating gate, complementary to (not a duplicate of) `real_rp_verification.rs` and `probe-request-xray.cjs` — name both explicitly and summarize why each is insufficient alone (real_rp_verification only proves interop deserialize succeeds, generic-error on regression; probe-request-xray proves live-Firefox delivery, not wire shape in isolation). Implement the two behaviors above as two `#[test]` functions. Every panic message must name the exact JSON field path (e.g. `"response.attestationObject"`) and state the regression class it guards against, so a future failure is immediately diagnosable without re-deriving field names from `ceremony.rs`. Use fixture byte vectors only (e.g. `[1u8; 16]`, `[2u8; 16]`, `[3u8; 16]` matching lib.rs's own existing fixture convention) — never real/production key material.

    After the file is written, perform the negative-control proof required by CONTEXT.md's QA-04 discretion note: temporarily remove the `features = ["serialize_bytes_as_base64_string"]` clause from `crates/pv-provider/Cargo.toml` (e.g. via a scratch copy, not a committed change), run `cargo test -p pv-provider --test response_shape`, confirm BOTH new tests fail (red) with the panic messages actually firing — this proves the gate is discriminating, not vacuously green. Restore `Cargo.toml` to its original committed state (`git checkout -- crates/pv-provider/Cargo.toml` or equivalent) and re-run to confirm green. Record the red-then-green proof (exact commands + observed failure text) in this plan's SUMMARY.md — this is a one-time authorship proof, not a permanently-red test in the suite.
  </action>
  <acceptance_criteria>
    - `crates/pv-provider/tests/response_shape.rs` exists with exactly two `#[test]` functions: `create_response_binary_fields_are_base64url_strings`, `get_response_binary_fields_are_base64url_strings`
    - Every always-present Bytes field (`rawId`, `response.clientDataJSON`, `response.attestationObject`, `response.authenticatorData` for create; `rawId`, `response.clientDataJSON`, `response.authenticatorData`, `response.signature` for get) is asserted as a JSON string AND valid base64url
    - Optional Bytes fields (`response.publicKey`, `clientExtensionResults.prf.results.first`/`.second`, `response.userHandle`) are asserted when present, never required
    - `cargo test -p pv-provider --test response_shape` exits 0 against the committed Cargo.toml (feature enabled)
    - The same command exits non-zero (fails) against a Cargo.toml with `serialize_bytes_as_base64_string` removed — negative control proven and documented in SUMMARY.md, then reverted
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p pv-provider --test response_shape</automated>
  </verify>
  <done>Both new tests pass against the committed feature-enabled Cargo.toml; the negative-control red/green proof is documented in the plan's SUMMARY.md; `cargo test --workspace` (full workspace) remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| pv-provider ceremony output -> extension MAIN-world page-bridge (JS) | The JSON this test gates is the exact wire contract a browser page/RP consumes; a silent shape regression breaks every downstream consumer identically to XBR-02's blast radius |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-20-01-01 | Tampering | `crates/pv-provider`'s `passkey-types` `serialize_bytes_as_base64_string` feature flag | high | mitigate | This plan's dedicated field-enumerating unit test with an explicit negative-control proof (feature disabled -> test fails) closes the exact silent-regression class that shipped the v0.2 bug through every prior `.ok`/`id`-only assertion |
| T-20-01-02 | Tampering | `tests/response_shape.rs` itself silently passing on a partially-implemented field list | medium | mitigate | Every always-present Bytes field named in `webauthn.rs`'s `AuthenticatorAttestationResponse`/`AuthenticatorAssertionResponse` struct definitions is enumerated and asserted; optional fields are asserted-when-present rather than silently skipped, per this plan's explicit assumption note |
| T-20-01-SC | Tampering | cargo/npm/pip installs | n/a | accept | This plan introduces zero new package-manager dependencies (no `cargo add`, no npm/pip install) — only a new test file against already-vetted, already-pinned crates |
</threat_model>

<verification>
`cargo test -p pv-provider` passes in full (including the two new tests and the pre-existing `create_then_get_roundtrip`/`origin_mismatch_rejected`/`prf_capable_credential` tests and `real_rp_verification.rs`). `cargo test --workspace` remains green.
</verification>

<success_criteria>
QA-04's Rust-unit-gate half is closed: a regression of `serialize_bytes_as_base64_string` to a bare number array fails `cargo test -p pv-provider` loudly, with a per-field message, and the negative-control proof documented in SUMMARY.md confirms this discriminates correctly (not vacuously green).
</success_criteria>

<output>
Create `.planning/phases/20-test-infrastructure-ci-gate/20-01-SUMMARY.md` when done, including the negative-control red/green proof transcript.
</output>

