---
phase: 25-member-removal-suspension-re-key
plan: 08
type: execute
wave: 5
depends_on: ["25-04", "25-07"]
files_modified:
  - web/src/components/settings/FamilyTab.tsx
  - web/src/components/settings/ConfirmDialog.tsx
  - web/src/components/settings/RemoveMemberDialog.tsx
autonomous: true
requirements: [FAM-07, FAM-08, FAM-09, UX-04]

must_haves:
  truths:
    - "FamilyTab's Members section reuses the existing mode==='checking' gate — the member list and invite panel share one fetch, no separate skeleton state (E1 loading)."
    - "A member-list load failure shows family.membersLoadFailed with a retry affordance, matching the existing family.loadError/family.loadRetryCta pair's shape (E1 error)."
    - "Each member row shows email (truncate+title), a role badge, joined date, a status badge only when suspended, and family.youBadge for the caller's own row (E1 populated)."
    - "A 2-person family and a large one render the same row shape; the list scrolls within SettingsPanel's existing overflow-y-auto drawer, no new scroll container (E1 zero-one-many)."
    - "Suspend/Reinstate/Remove action icons render only when isOwner && row.user_id !== self && row.role !== 'owner' — a plain member sees a read-only roster with no action icons on any row, including their own (E1 action-visibility)."
    - "The Suspend confirmation's confirm button shows disabled + a spinner during the request (E2 loading)."
    - "member.suspendFailed renders inline in the Suspend dialog on failure — it never silently closes (E2 error)."
    - "On Suspend success the dialog closes, the row's status badge updates to family.statusSuspended, and its action icon flips from PauseCircle to PlayCircle — no toast (E2 success)."
    - "member.reinstateFailed renders on failure without leaving the status badge in a stale state (E3 error)."
    - "On Reinstate success the status badge disappears and the action icon flips back to PauseCircle immediately, with no confirmation step (E3 success)."
    - "member.removeLoadingAccess renders as a centered spinner inside the dialog shell before step 1's access list renders (E4 loading access fetch)."
    - "member.removeAccessLoadFailed fails closed — the dialog never advances to a list-less step 1; a retry re-fetches, Cancel is always available (E4 error access fetch)."
    - "member.removeAccessListEmpty renders instead of a list when the member had nothing shared with them, and the honesty warning still renders alongside it (E4 empty access list)."
    - "A populated access list shows shared-folder and individually-shared-item sub-sections, each omitted entirely when its own array is empty — never an empty heading with nothing under it (E4 populated access list)."
    - "One folder/item and many render the same row shape; the list is capped at max-h-60 overflow-y-auto inside the 400px card (E4 zero-one-many)."
    - "Step 1 to step 2 is forward-only via member.removeStep1Continue; Cancel at either step closes the whole dialog with no partial state retained — re-opening re-fetches the access list fresh (E4 step transition)."
    - "member.removeStep2Confirm is the only button that calls the removal endpoint; busy (member.removing), error (member.removeFailed, dialog stays open), and success (dialog closes, row disappears) states are all covered (E4 final action)."
    - "The suspended-member banner renders as a persistent alert alert-warning alert-soft at the top of FamilyTab, above the member list, whenever the caller's own roster row carries suspended status (E5 visibility)."
    - "An item reachable both via a shared folder and via a direct item share appears ONCE in the disclosure list, showing the higher of the two access levels — never duplicated, never omitted (UX-04 adjacency edge)."
    - { statement: "A long email address in a member row truncates with a title attribute, matching every prior email-display surface in this codebase.", verification: backstop }
    - { statement: "Role/status badges are fixed short strings and cannot overflow — only the email is variable-length.", verification: backstop }
    - { statement: "The member list can never be empty by construction — the caller is themselves always a member the moment this section renders.", verification: backstop }
    - { statement: "A realistic long email (>=40 chars) does not clip or overflow the Suspend dialog's fixed-width card in either the title (truncated) or the body (wraps normally, allowed to grow the dialog height).", verification: backstop }
    - { statement: "The Reinstate button is disabled for the duration of its request so a fast double-click cannot fire it twice.", verification: backstop }
    - { statement: "A long folder or item name inside the disclosure list truncates with a title attribute, matching every prior name-display surface in this codebase.", verification: backstop }
    - { statement: "When one folder's item names resolve and another falls back to member.removeAccessItemsUnresolvedNote, both render adjacent with no error-styled element implying the fallback folder is broken, and member.removeHonestyWarning plus Continue stay enabled.", verification: backstop }
    - { statement: "The suspended banner survives a page reload (re-derived from the same GET /api/families/members fetch every mount) and disappears on the very next fetch after reinstatement.", verification: backstop }
    - { statement: "While suspended, any previously-open shared-item detail panel referencing now-inaccessible data closes or redirects gracefully rather than rendering a broken or error-flashing panel.", verification: backstop }
    - { statement: "The disclosure list renders in a stable, specified order across re-opens of the remove dialog, so an owner re-reading it does not see items shuffle.", verification: backstop }
  artifacts:
    - web/src/components/settings/FamilyTab.tsx (extended)
    - web/src/components/settings/ConfirmDialog.tsx (extended)
    - web/src/components/settings/RemoveMemberDialog.tsx (new)
  key_links:
    - "RemoveMemberDialog imports buildMemberRemovalBatch and getCollectionItems/getCollection/unsealCollectionKey/decryptItemForCollection (Plan 25-07) directly — it does NOT wait on Phase 26's full collections browser; per 25-UI-SPEC.md's Phase-Specific Notes §4, this phase owes real item names in the normal case, full stop."
    - "ConfirmDialog's new severity prop defaults to 'error' — SessionsTab.tsx's two existing callers are unmodified, zero behavior change; only Suspend's new caller passes severity='warning'."
---

<objective>
Give the owner (and every member, read-only) a real Members list inside `FamilyTab`, with working
Suspend/Reinstate actions and the two-step Remove confirmation that discloses — with REAL item names,
not counts — exactly what the removed member could see, and states plainly that removal cannot undo
what they already saw. This is Phase 25's user-facing headline surface.

Purpose: FAM-07/FAM-08/FAM-09 need a real UI trigger to be usable at all; UX-04's honesty requirement is
non-negotiable copy that this plan is responsible for rendering correctly, with real data.
Output: `FamilyTab.tsx`'s new Members section and suspended banner, `ConfirmDialog.tsx`'s new severity
variant, and a new `RemoveMemberDialog.tsx`.
</objective>

## Artifacts this phase produces

- `web/src/components/settings/FamilyTab.tsx` (extended) — Members section (E1), suspended-member
  banner (E5), Suspend/Reinstate/Remove action wiring.
- `web/src/components/settings/ConfirmDialog.tsx` (extended) — `severity?: "error" | "warning"` prop.
- `web/src/components/settings/RemoveMemberDialog.tsx` (new) — two-step remove confirmation with real
  item-name disclosure (E4).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-member-removal-suspension-re-key/25-UI-SPEC.md
@.planning/phases/25-member-removal-suspension-re-key/25-CONTEXT.md
@web/src/components/settings/FamilyTab.tsx
@web/src/components/settings/ConfirmDialog.tsx
@web/src/components/settings/PasskeyDeleteConfirmDialog.tsx
@web/src/lib/families/api.ts
@web/src/lib/families/rekey.ts
@web/src/lib/vault/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FamilyTab Members section (E1) + suspended-member banner (E5)</name>
  <files>web/src/components/settings/FamilyTab.tsx</files>
  <read_first>
    web/src/components/settings/FamilyTab.tsx full file — the existing `mode`/`isOwner` state machine,
    `resolveOwnership`/`loadFamilyState` (the `members` array is currently fetched then discarded after
    `resolveOwnership` — this task keeps it in state); web/src/components/settings/SessionsTab.tsx's
    row shell (`session-row-*`, `flex min-h-16 items-center gap-3 rounded-box border border-base-300 px-4
    py-3` — the exact 12px-exception shell 25-UI-SPEC.md's Phase-Specific Notes §1 requires the member
    row to match byte-for-byte); web/src/lib/families/api.ts (`FamilyMemberRecord.status`, Plan 25-07).
  </read_first>
  <behavior>
    - The Members section renders using the SAME fetch `loadFamilyState` already performs — no new
      network call, no separate loading state.
    - Each row shows email/role badge/joined date/status badge (only if suspended)/youBadge (only for
      self), with action icons gated to isOwner && not-self && not-owner-role.
    - The suspended banner renders above the member list only when the caller's own row is suspended.
  </behavior>
  <action>
    In `FamilyTab.tsx`: add `const [members, setMembers] = useState&lt;FamilyMemberRecord[] | null&gt;(null)`
    and `const [selfUserId, setSelfUserId] = useState&lt;string | null&gt;(null)`; in
    `resolveOwnership`, also `setMembers(members)` and `setSelfUserId(account?.user_id ?? null)`
    (the function already resolves both `members` and `account` — just retain them instead of only
    deriving `isOwner`). Add a "Members" section (`data-testid="family-members-section"`) rendered in
    `mode === "normal"`, ABOVE the existing invite-panel content: heading `t("family.membersHeading")`
    with a `Users` icon; on member-fetch failure (an EXPLICIT re-check, not the whole-tab `mode ===
    "error"` state — the Members section can fail independently of the invite panel) show
    `t("family.membersLoadFailed")` + a retry button reusing `handleRetryLoad`; otherwise render one row
    per `members` entry using the EXACT `SessionsTab.tsx` row shell (`flex min-h-16 items-center gap-3
    rounded-box border border-base-300 px-4 py-3`): `email` (`truncate` + `title={m.email}`, Label role),
    `t(m.role === "owner" ? "family.roleOwner" : "family.roleMember")` badge (`badge badge-ghost`),
    `interpolate(t("family.joinedLabel"), { date: <browser-native-formatted m.joined_at> })`,
    `t("family.statusSuspended")` badge (`badge badge-warning badge-outline`, ONLY when `m.status ===
    "suspended"`), `t("family.youBadge")` (ONLY when `m.user_id === selfUserId`). Action icons
    (`btn btn-ghost btn-square btn-sm`, no color override): render ONLY when `isOwner && m.user_id !==
    selfUserId && m.role !== "owner"` — `PauseCircle`/`PlayCircle` (toggle by `m.status`,
    `aria-label={interpolate(t(m.status === "suspended" ? "member.reinstateAria" : "member.suspendAria"),
    { email: m.email })}`) and `UserMinus` (`aria-label={interpolate(t("member.removeAria"), { email:
    m.email })}`).

    Add the suspended-member banner (`data-testid="family-suspended-banner"`,
    `alert alert-warning alert-soft`, mirroring Phase 24 Amendment 5's exact soft-alert treatment) at the
    VERY TOP of the `mode === "normal"` render, above BOTH the Members section and the invite panel:
    renders only when `members?.find((m) => m.user_id === selfUserId)?.status === "suspended"`, showing
    `t("family.suspendedBannerTitle")` + `t("family.suspendedBannerBody")`.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/settings/FamilyTab.test.tsx</automated>
  </verify>
  <done>The Members section and suspended banner render correctly with proper action-icon visibility.</done>
  <acceptance_criteria>
    Existing `FamilyTab.test.tsx` suite stays green (no regression to bootstrap/error/invite-form
    behavior). A component-level test (added or extended in this file) asserts: a plain member sees zero
    action icons on any row; the owner sees action icons on every row except their own; a suspended
    member's row shows the status badge; the banner renders only for the caller's own suspended row.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConfirmDialog severity prop (E2 Suspend) + Reinstate wiring (E3)</name>
  <files>web/src/components/settings/ConfirmDialog.tsx, web/src/components/settings/FamilyTab.tsx</files>
  <read_first>
    web/src/components/settings/ConfirmDialog.tsx full file (77 lines — the exact `AlertTriangle`/
    `text-error`/`btn-error` treatment this task swaps for `text-warning`/`btn-warning` when
    `severity === "warning"`); web/src/components/settings/SessionsTab.tsx (ConfirmDialog's two existing
    callers — confirm neither passes a new prop, so the `"error"` default preserves their exact current
    rendering).
  </read_first>
  <behavior>
    - ConfirmDialog with no severity prop (or severity="error") renders byte-identical to today.
    - ConfirmDialog with severity="warning" renders text-warning/btn-warning instead of text-error/btn-error.
    - Suspend shows this warning-severity dialog; on confirm-success the row updates in place (status
      badge, icon flip) with no full-page reload; on failure member.suspendFailed renders inline.
    - Reinstate has NO confirmation dialog; on success the row updates immediately; on failure
      member.reinstateFailed renders (e.g. as a transient inline message near the row).
  </behavior>
  <action>
    In `ConfirmDialog.tsx`, add `severity?: "error" | "warning"` to the props type, default `"error"`.
    Swap `text-error`/`btn-error` for `text-warning`/`btn-warning` when `severity === "warning"` — the
    ONLY conditional change; every other line (structure, `data-testid`s, cancel button) stays untouched.

    In `FamilyTab.tsx`, add `suspendTarget: FamilyMemberRecord | null` state and a `Suspend` action
    handler that opens `ConfirmDialog` with `severity="warning"`, `title={interpolate(t("member.suspendConfirmTitle"),
    { email })}`, `body={interpolate(t("member.suspendConfirmBody"), { email })}`,
    `confirmLabel={t("member.suspendConfirmConfirm")}`, calling `suspendMember(userId)` from
    `families/api.ts` (Plan 25-07) `onConfirm`; on success, re-fetch (or optimistically patch) the
    `members` array's matching row's `status` and close the dialog; on failure, surface
    `t("member.suspendFailed")` — do NOT close the dialog silently (mirrors ConfirmDialog's existing
    non-silent-failure shape, since it already keeps itself open on a thrown `onConfirm`).

    Add a `Reinstate` action handler (no dialog) that calls `reinstateMember(userId)` directly from the
    row's icon `onClick`, with a per-row `reinstating` boolean disabling the button for the duration of
    the request; on success, patch the row's `status` to `"active"` in place; on failure, surface
    `t("member.reinstateFailed")` as an inline message (reuse the existing `family-load-error`-style
    `role="alert"` paragraph shape, scoped to this action, not the whole tab).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/settings/FamilyTab.test.tsx</automated>
  </verify>
  <done>Suspend uses warning-severity ConfirmDialog; Reinstate has no dialog; both update the row in place.</done>
  <acceptance_criteria>
    `SessionsTab.test.tsx`'s existing ConfirmDialog-dependent assertions stay green (zero behavior
    change for its two callers). New/extended FamilyTab tests assert: suspend confirm dialog uses
    warning styling; reinstate has no dialog and updates immediately; both failure paths surface their
    own copy without a full unmount.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RemoveMemberDialog — two-step confirm with real item-name disclosure (E4)</name>
  <files>web/src/components/settings/RemoveMemberDialog.tsx, web/src/components/settings/FamilyTab.tsx</files>
  <read_first>
    web/src/components/settings/PasskeyDeleteConfirmDialog.tsx full file (the 400px modal shell,
    non-silent-close-on-failure discipline, `blocked`-vs-`genericError` two-branch shape — this
    component's step-1-fetch-failure state mirrors that shape, with "blocked" meaning "access list fetch
    failed, Continue must not render"); web/src/lib/families/api.ts's `getMemberAccess` (Plan 25-07);
    web/src/lib/families/rekey.ts's `buildMemberRemovalBatch` (Plan 25-07); web/src/lib/vault/api.ts's
    `getCollection`/`getCollectionItems`; web/src/lib/crypto's `unsealCollectionKey`/
    `decryptItemForCollection`; `.planning/phases/25-member-removal-suspension-re-key/25-UI-SPEC.md`'s
    Phase-Specific Notes §2 and §4 (the exact access-list rendering shape and the explicit "this phase
    owes real item names in the normal case, full stop" requirement).
  </read_first>
  <behavior>
    - Step 1 fetches the target's access breakdown (getMemberAccess) and, for every reachable collection,
      the collection's real item names — resolved by unsealing that collection's CollectionKey (the
      caller is a member of every collection they can offer for removal) and decrypting each item's name
      field via decryptItemForCollection, reusing the SAME primitives buildMemberRemovalBatch does.
    - An item reachable via both a shared folder and a direct item share appears once, at the higher
      access level.
    - A per-folder name-resolution failure (network error mid-fetch, or the collection deleted between
      fetch and resolution) falls back to member.removeAccessItemsUnresolvedNote for THAT folder only,
      never disabling the rest of the dialog.
    - member.removeHonestyWarning renders directly beneath the access list, always, including the empty
      case.
    - Step 2's confirm calls buildMemberRemovalBatch then families/api.ts's removeMember.
  </behavior>
  <action>
    Create `RemoveMemberDialog.tsx` with props `{ member: FamilyMemberRecord; onClose: () => void;
    onRemoved: () => void }`. Internal state machine: `"loading-access" | "blocked" | "step1" |
    "step2" | "removing" | "error"`. On mount, fetch `getMemberAccess(member.user_id)`; on failure, set
    `"blocked"` and render `t("member.removeAccessLoadFailed")` with a retry action (mirrors
    `PasskeyDeleteConfirmDialog`'s blocked-branch shape: Cancel only, no Continue). On success, for each
    `collections` entry: `getCollection(id)` for the caller's own `sealed_key`,
    `unsealCollectionKey(callerIdentityKey, sealed_key)`, `getCollectionItems(id)`, and for each item
    `decryptItemForCollection(collectionKey, item, id, item.id, /* revision from a parsed enc_data JSON
    or 0 for the key-wrap layer per this codebase's existing item-decrypt call convention */)` to recover
    the item's plaintext name field — wrap each per-item/per-folder resolution in its own try/catch: on
    failure, record that folder as unresolved (render `interpolate(t("member.removeAccessItemsUnresolvedNote"),
    { count })` for its nested list instead of names) WITHOUT blocking the rest of the dialog. Merge
    `item_shares` entries into the same per-item map keyed by item id, taking the access level via
    `combine_access`-equivalent max-of-two-grants logic client-side (mirrors `membership.rs`'s own
    `combine_access` rank: `read=0, hidden_password=1, edit=2`) so a dual-path item appears exactly once
    at its higher level. Render step 1: `interpolate(t("member.removeStep1Title"), { email })`,
    `interpolate(t("member.removeStep1Intro"), { email })`, the merged folder/item list (each list capped
    `max-h-60 overflow-y-auto`, badges via `access.readOnly`/`access.fullEdit`/`access.hiddenPassword` —
    never the raw wire string) or `t("member.removeAccessListEmpty")` when both arrays are empty,
    `t("member.removeHonestyWarning")` ALWAYS directly beneath the list, Cancel + `t("member.removeStep1Continue")`.
    Step 2 (same card, state machine transition, not a second overlay): `interpolate(t("member.removeStep2Title"),
    { email })`, `interpolate(t("member.removeStep2Body"), { email })`, Cancel (returns to step1, keeping
    the already-fetched list in memory) + `t("member.removeStep2Confirm")` (disabled + spinner while
    `"removing"`, calling `await buildMemberRemovalBatch(member.user_id, ownUk)` then
    `removeMember(member.user_id, batch)`; on failure render `t("member.removeFailed")` inline, dialog
    stays open; on success call `onRemoved()`).

    Wire a `removeTarget` state + trigger into `FamilyTab.tsx`'s `UserMinus` action icon, mounting
    `RemoveMemberDialog` and, on `onRemoved`, removing the row from local `members` state (no full
    re-fetch required, though a re-fetch is also acceptable).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/settings/RemoveMemberDialog.test.tsx</automated>
  </verify>
  <done>The two-step dialog shows real item names, the honesty warning, and calls removal only on final confirm.</done>
  <acceptance_criteria>
    A new component test asserts: an access-fetch failure blocks progression past step 1 (no Continue
    rendered); a populated list with both a resolved and an unresolved-fallback folder renders both
    without error styling and keeps Continue enabled; an item present in both `collections`-derived and
    `item_shares` sources appears exactly once at the higher access level; the honesty warning string
    renders verbatim beneath the list in every non-blocked state, including the empty case; step 2's
    Confirm is the only element that triggers the removal call.

    **Evidentiary scope note:** this codebase's Vitest component tests mock `@/lib/crypto` wholesale
    (the same structural blind spot WR-10 documented in Phase 24). This means the real-item-name
    resolution assertions above run against a MOCKED `unsealCollectionKey`/`decryptItemForCollection` —
    they prove the component's STATE MACHINE and RENDERING logic are correct, but they are NOT
    acceptance evidence that real decryption actually resolves real item names end-to-end. That genuine
    evidence is Plan 25-07's `rekey.real-wasm.test.ts` (proves the crypto primitives themselves, no mock)
    and Plan 25-10's live e2e (proves the whole stack together, real WASM, real server). A verifier must
    not credit this component test alone as proof of UX-04's "real item names in the normal case."
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the owner's browser → the server's member_access/collection_items responses | this UI decrypts and displays server-served ciphertext; a decryption failure for one folder must not crash or block the whole dialog |
| the honesty-copy string → the owner's understanding of what re-key does | UX-04's copy is a security-relevant control, not polish — misrendering or omitting it is a defect, not a cosmetic bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-19 | Information Disclosure | RemoveMemberDialog's disclosure list | high | mitigate | only the OWNER's own already-authorized view is rendered — every item/folder name shown is something the owner already has access to (25-CONTEXT.md's explicit "discloses nothing new to them" framing); no new server data is exposed that the owner could not already decrypt via the existing collections API |
| T-25-20 | Repudiation | member.removeHonestyWarning omission or softening | high | mitigate | the string renders unconditionally beneath the access list in every non-blocked state (empty included) — a component test asserts its literal presence, closing the risk of a future refactor silently dropping it |
| T-25-21 | Denial of Service (UI) | per-folder name-resolution failure | medium | mitigate | wrapped per-folder in try/catch, degrading to the count-only fallback for that one folder without blocking Continue or the rest of the dialog |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | no new package-manager installs in this plan |

</threat_model>

<verification>
```
cd web && npx tsc --noEmit
cd web && npx vitest run src/components/settings/FamilyTab.test.tsx src/components/settings/RemoveMemberDialog.test.tsx
```
</verification>

<success_criteria>
An owner sees a real member roster with working Suspend/Reinstate, and a two-step Remove flow that
discloses real item names (not counts) and states plainly that re-key cannot undo prior exposure. A
plain member sees a read-only roster and their own suspension banner when applicable.
</success_criteria>

<output>
Create `.planning/phases/25-member-removal-suspension-re-key/25-08-SUMMARY.md` when done, with
`## Threat Flags` populated (non-optional).
</output>
