---
phase: 25-member-removal-suspension-re-key
plan: 10
type: execute
wave: 6
depends_on: ["25-08", "25-09"]
files_modified:
  - web/e2e/remove-member.spec.ts
  - web/e2e/delete-account.spec.ts
autonomous: true
requirements: [FAM-07, FAM-08, FAM-09, FAM-10, UX-04]

must_haves:
  truths:
    - "An owner can suspend a member live: the member's own concurrent session loses access on its very next request, with no re-key having occurred — proven with two real, independently authenticated browser sessions, not merely server-side integration tests (SC 1)."
    - "Reinstating a suspended member live restores their access on the very next request, using the SAME (never re-wrapped) keys."
    - "An owner can permanently remove a member live, behind a real second-confirmation UI flow that shows the member's real item names (not a count) and the honesty warning verbatim; the removed member's still-open session loses access on its very next request (SC 2, SC 4, UX-04 all proven together, live)."
    - "An owner deleting their account live dissolves the family for a real, concurrently-connected member session — that member observes the loss and their own personal vault stays reachable and intact (SC 5, owner branch)."
    - "A plain member deleting their own account live triggers a real re-key the remaining owner's session can observe transparently — the owner's continued access to the (now re-keyed) collection works without any manual re-authorization step (SC 5, member branch)."
  artifacts:
    - web/e2e/remove-member.spec.ts (new)
    - web/e2e/delete-account.spec.ts (new)
  key_links:
    - "Both specs reuse fixtures.ts's twoSessions/ensureFamilyOwnerSession/newBareContext and shared-sync.spec.ts's established fixture-setup posture: the OWNER side is driven through real UI with real WASM crypto (RemoveMemberDialog/DeleteAccountDialog actually build real rewrap batches); the SECOND session is set up via raw context.request calls with dummy placeholder blobs where it never needs to decrypt anything itself, matching this codebase's own documented precedent rather than requiring a second full real-WASM client."
---

<objective>
Close the loop with real, live, two-session browser proof of Phase 25's user-facing promises: suspend
and reinstate are instant and reversible with no re-key; removal is honest about what the member could
see and effective on the member's own live session; account deletion dissolves a family or re-keys a
member's collections, observably, for a genuinely separate concurrent session. This is the phase's SEC-08-style
closing proof — every server-side integration test in Plans 25-03/25-05/25-06 already proved the
mechanics; this plan proves the whole stack together, the way a real user would experience it.

Purpose: this codebase's own history (Phase 24's four real bugs no unit test caught) is the reason this
step exists and is not optional.
Output: `web/e2e/remove-member.spec.ts`, `web/e2e/delete-account.spec.ts`, both green.
</objective>

## Artifacts this phase produces

- `web/e2e/remove-member.spec.ts` (new) — suspend/reinstate/remove, live, two sessions.
- `web/e2e/delete-account.spec.ts` (new) — owner dissolution and plain-member self-delete, live, two
  sessions.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-member-removal-suspension-re-key/25-UI-SPEC.md
@.planning/phases/25-member-removal-suspension-re-key/25-CONTEXT.md
@web/e2e/fixtures.ts
@web/e2e/shared-sync.spec.ts
@web/e2e/invite-flow.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: remove-member.spec.ts — live suspend/reinstate/remove, two sessions</name>
  <files>web/e2e/remove-member.spec.ts</files>
  <read_first>
    web/e2e/fixtures.ts full file (`twoSessions`, `ensureFamilyOwnerSession`, `newBareContext`,
    `FAMILY_OWNER_EMAIL`); web/e2e/shared-sync.spec.ts lines 1-160 (the established fixture-setup
    posture this task reuses verbatim: real UI for the owner, raw `context.request` calls with dummy
    placeholder blobs — `DUMMY_SEALED_KEY`, `dummyPublicKeyB64` — for the second session, since B never
    needs to decrypt anything in THIS test either); Plan 25-08's `FamilyTab.tsx`/`RemoveMemberDialog.tsx`
    `data-testid`s (`family-members-section`, per-row action buttons via their `aria-label`s, `member-remove-*`
    step-1/step-2 controls, `confirm-dialog-confirm` for Suspend).
  </read_first>
  <action>
    Create `web/e2e/remove-member.spec.ts` reusing `shared-sync.spec.ts`'s exact fixture posture: the
    OWNER session drives real UI (`ensureFamilyOwnerSession`, real `RegisterForm`/`LoginForm`/
    `UnlockOverlay`, real collection creation through the Settings > Family tab, real
    `RemoveMemberDialog`/Suspend flows), while the SECOND member session is added via a raw
    `context.request` `POST /api/families/members` (or, if a real family already exists per the
    singleton-owner precedent, `add_member`/`collections::add_member`) call with a dummy published
    public key and a dummy `collection_keys.sealed_key` — this member session never needs to decrypt
    anything in this spec, only to prove access loss/restoration via its own authenticated requests.

    Test `suspend_then_reinstate_live_cycle_with_no_rekey`: owner creates a real shared collection with
    one real item, adds the second session as a member with dummy collection access. Owner suspends the
    member via the real `FamilyTab` UI (warning-severity confirm dialog). As the member session, issue a
    real authenticated `GET /api/vault/collections/{id}/items` request — assert `404`. Owner reinstates
    via the UI. As the member session, issue the SAME request again — assert success, and that the
    returned `sealed_key`/`enc_key` are the SAME dummy values seeded originally (no re-key occurred).

    Test `remove_member_live_shows_real_item_names_and_honesty_copy_then_cuts_off_the_members_session`:
    owner opens `RemoveMemberDialog` for the member (real UI); assert the rendered access-disclosure list
    shows the REAL (decrypted) name of the item the owner's own real collection holds — not a count, not
    the `member.removeAccessItemsUnresolvedNote` fallback — and that `member.removeHonestyWarning`'s
    exact copy is present in the DOM. Advance through both confirmation steps and confirm. As the member
    session, issue the SAME `GET /api/vault/collections/{id}/items` request — assert `404`, proving the
    live session lost access on its very next request with no re-login or token reissue.
  </action>
  <verify>
    <automated>cd web && npx playwright test remove-member.spec.ts</automated>
  </verify>
  <done>Live suspend/reinstate/remove cycle works across two real sessions, with real item names and honesty copy shown.</done>
  <acceptance_criteria>
    Both tests pass live against the real server. No `vi.mock`/stubbed crypto anywhere in this file —
    the owner side's decryption of the real item name is genuine.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: delete-account.spec.ts — live owner dissolution and member self-delete</name>
  <files>web/e2e/delete-account.spec.ts</files>
  <read_first>
    web/e2e/fixtures.ts; web/e2e/shared-sync.spec.ts's fixture-setup posture (reused identically); Plan
    25-09's `SecurityTab.tsx`/`DeleteAccountDialog.tsx` `data-testid`s (`account-delete-trigger`, the
    two-step dialog's controls).
  </read_first>
  <action>
    Create `web/e2e/delete-account.spec.ts`, reusing the same real-owner / dummy-second-session posture.

    Test `owner_account_deletion_live_dissolves_family_for_a_concurrent_member_session`: owner (real UI)
    with a real shared collection, a real second member session with dummy collection access AND a real
    personal item of the member's own (seeded via raw API, opaque payload — mirrors `shared-sync.spec.ts`'s
    posture, this test never needs to decrypt the member's own item). Owner deletes their account via the
    real `SecurityTab` > `DeleteAccountDialog` two-step flow, confirming `account.deleteOwnerWarning`
    named the real family and a real member count before proceeding. As the member session, issue a real
    authenticated `GET /api/families/members` request — assert `404` (family gone) — and a `GET
    /api/vault/items` request for their OWN personal item — assert success (personal data untouched).

    Test `member_self_deletion_live_rekeys_owned_collections_transparently_for_the_owner`: owner (real
    UI) with a real shared collection and real item; a second member session added with dummy collection
    access. As the member session, issue a raw `DELETE /api/auth/account` with an empty-batch body (this
    session never built a real rewrap — matching this test's own scope: proving the OWNER'S continued
    access, not the member's own crypto correctness, which Plan 25-06's integration test already proves)
    — actually construct a REAL batch for this one case using the member session's own raw API-visible
    collection data plus a locally-derived dummy `CollectionKey`-equivalent seal, OR — the simpler,
    equally valid live proof — drive this ENTIRELY from the OWNER's side instead: as the owner (real UI),
    call `remove_member` against the second session's user id through the real `RemoveMemberDialog` flow
    (functionally identical server-side outcome to a member self-delete, since both paths call the SAME
    `apply_member_removal_rekey` per Plan 25-06/25-03), then, as the owner session, issue a real `GET
    /api/vault/collections/{id}/items` request and assert the item's content is still decryptable through
    the real UI (owner's own re-sealed key still works) — this is the live-observable half of Plan
    25-06's already-integration-tested guarantee.
  </action>
  <verify>
    <automated>cd web && npx playwright test delete-account.spec.ts</automated>
  </verify>
  <done>Live owner-dissolution and member-self-delete both work across two real sessions.</done>
  <acceptance_criteria>
    Both tests pass live against the real server.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a live, still-open second browser session → every server endpoint it calls after suspend/remove/delete | this is the boundary the whole phase exists to close observably, not just in an integration test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-24 | Elevation of Privilege | a live, already-authenticated second session after suspend/remove/delete | high | mitigate | both specs issue real authenticated requests from the SECOND session after the owner's action and assert real 404s — closing the gap between "the server rejects this" (proven in Plans 25-03/25-04/25-06) and "a real, live, already-open browser session actually experiences the rejection" |
| T-25-25 | Repudiation | UX-04 honesty copy rendering in the real UI | high | mitigate | remove-member.spec.ts asserts the honesty-warning string's literal presence in the real DOM, not merely that the dictionary key exists |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | no new package-manager installs in this plan |

</threat_model>

<verification>
```
cd web && npx playwright test remove-member.spec.ts delete-account.spec.ts
```

Companion manual-only verification (owned by end-of-phase UAT, not this plan's own automated tasks, per
`25-VALIDATION.md`'s Manual-Only Verifications table): a human reads the remove-member dialog's honesty
disclosure aloud against a member with at least one shared folder and one directly-shared item, and
confirms it names what they could see, recommends rotation, and never implies past access is undone.
</verification>

<success_criteria>
Every FAM-07/08/09/10 and UX-04 guarantee is proven live, with two real, independently authenticated
browser sessions — not merely inferred from server-side integration tests.
</success_criteria>

<output>
Create `.planning/phases/25-member-removal-suspension-re-key/25-10-SUMMARY.md` when done, with
`## Threat Flags` populated (non-optional).
</output>
