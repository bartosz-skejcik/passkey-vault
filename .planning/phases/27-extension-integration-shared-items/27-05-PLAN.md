---
phase: 27-extension-integration-shared-items
plan: 05
type: execute
wave: 3
depends_on: ["27-04"]
files_modified:
  - extension/entrypoints/background/autofill-match.ts
  - extension/lib/autofill/types.ts
  - extension/entrypoints/background/autofill-match.test.ts
  - extension/e2e/dual-extension-sharing.spec.ts
autonomous: true
requirements: [EXT-07, EXT-08]

must_haves:
  truths:
    - "A shared item's TOTP code generated in the extension equals the code generated for the same item in the web app for the same time step — proving the Collection-Key decrypt path yields byte-identical secret material to the User-Key path."
    - "A shared item with no TOTP secret exposes no TOTP affordance in the popup and no TOTP fill row on the page — identical to a personal item with no TOTP secret."
    - "TOTP generation for a shared item is attempted only after that item's Collection Key has resolved; a pending-decrypt item exposes no TOTP affordance and never emits a code computed from undecrypted material."
    - "In the popup's autofill suggestions (\"Na tej stronie\"), personal matches precede shared matches, each group keeping its existing intra-group order — UX-3."
  key_links:
    - "handleAutofillMatch's matches array -> a stable partition (personal first, then shared), never a resort of the whole array (which would silently reorder items WITHIN either group, contradicting UX-3's own stated intent)"
</must_haves>
---

<objective>
Prove EXT-07/EXT-08's "identical to personal" claim for autofill and TOTP is true by CONSTRUCTION (the consumer files need zero changes because `vault-store.ts` now returns correctly-decrypted, correctly-tagged shared items — 27-RESEARCH.md's own headline architectural finding), and add the one genuine behavior change this surface needs: UX-3's personal-before-shared ordering in the "Na tej stronie" suggestions.

This plan implements A-6 (no special-casing in the TOTP code path once decryption is routed correctly — proven live, per A-6's own instruction, never by unit test alone) and UX-3 (Task 1's stable partition).

Purpose: 27-RESEARCH.md confirms `autofill-match.ts`'s `handleAutofillMatch`/`handleAutofillFill`/`buildFillValues`/`totpNow()` call sites are already scope-agnostic (`getItems()` consumers with zero type-narrowing on `collectionId`/`accessLevel`) — so EXT-07/EXT-08 require a NEGATIVE claim ("this file is unchanged") as much as a positive one. This plan proves both: the unchanged-pipeline claim via a source-level assertion, and the actual shared-item behavior via a live, positive, recipient-side observation (never a mocked-crypto unit test for the TOTP-byte-equality claim, per this phase's evidence rule).
Output: UX-3 ordering in `autofill-match.ts`, `AutofillMatch` gains `isShared?`/`folderName?`, and a live TOTP proof appended to 27-04's two-extension spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-extension-integration-shared-items/27-CONTEXT.md
@.planning/phases/27-extension-integration-shared-items/27-RESEARCH.md
@.planning/phases/27-extension-integration-shared-items/27-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UX-3 — personal-before-shared ordering in "Na tej stronie" matches</name>
  <files>extension/entrypoints/background/autofill-match.ts, extension/lib/autofill/types.ts, extension/entrypoints/background/autofill-match.test.ts</files>
  <read_first>
    extension/entrypoints/background/autofill-match.ts (full file, already read — `handleAutofillMatch`'s `for (const item of getItems())` loop building the `matches: AutofillMatch[]` array in `getItems()`'s own iteration order, which is `recomputeItems()`'s `Map`-insertion order from 27-04's three-source merge: personal, then collection-shared, then direct-shared — already roughly "personal first" by construction, but not a GUARANTEED stable partition once items interleave by id collision or future re-ordering)
    extension/lib/autofill/types.ts (full file, already read — `AutofillMatch { itemId, kind, label, maskedHint }`, no `isShared`/`folderName` yet)
    27-CONTEXT.md's UX-3 row ("Personal first, then shared, each group keeping its existing intra-group order")
  </read_first>
  <behavior>
    - Test: given a mixed array of matches where a shared item appears BEFORE a personal item in `getItems()`'s own order, the returned `matches` array places ALL personal-kind matches before ALL shared-kind matches, while personal-vs-personal and shared-vs-shared relative order is preserved exactly as `getItems()` produced it (a stable partition, not a resort).
    - Test: an all-personal or all-shared match set is returned unchanged (the partition is a no-op when there is nothing to reorder).
  </behavior>
  <action>
    Add `isShared?: boolean` and `folderName?: string` optional fields to `AutofillMatch` in `lib/autofill/types.ts`. In `autofill-match.ts`'s `handleAutofillMatch`, after building the `matches` array from the existing loop (unchanged filtering/gating logic — do not touch the `detected[kind]`/`itemMatchesOrigin` gates), apply a stable partition: personal matches (`item.isShared !== true`) first, shared matches after, each sub-list retaining its original relative order (a single pass building two arrays then concatenating them is sufficient — do not use `Array.prototype.sort` with a comparator, which is not guaranteed stable-enough for this exact "preserve intra-group order" requirement across all JS engines' historical behavior; an explicit two-array partition-then-concat is deterministic by construction). Set `isShared`/`folderName` on each pushed `AutofillMatch` from the corresponding `VaultItem`'s own `isShared`/`collectionId`-resolved folder name (folder-name resolution itself is 27-08's job via `collections-store.getCollections()`'s already-cached `name` field — this task only needs to read the name if trivially available via that store's synchronous getter; if not yet resolved, leave `folderName` undefined, matching the fallback UI-SPEC already specifies).
  </action>
  <verify>
    <automated>npm --prefix extension run test -- autofill-match</automated>
  </verify>
  <done>The autofill matches array is a stable personal-then-shared partition, intra-group order preserved.</done>
  <acceptance_criteria>
    `npm --prefix extension run test -- autofill-match` passes both behaviors; `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: LIVE proof — TOTP byte-equality for a shared item</name>
  <files>extension/e2e/dual-extension-sharing.spec.ts</files>
  <read_first>
    extension/e2e/dual-extension-sharing.spec.ts (27-04's spec — extend the SAME file's existing fixture setup rather than duplicating account/family/collection provisioning; add a second shared item, a TOTP item, to the same collection)
    web/src/lib/vault/totp.ts or the equivalent totpNow client call (grep for how web computes a TOTP code from a known secret at a known time step, to compute the EXPECTED code independently of the extension under test)
    extension/entrypoints/popup/ItemDetailView.tsx (already read — the `secret`/reveal flow is not used here; this test drives the SAME `autofill.totpCode` message path `TotpFillRow.tsx` uses, or asserts via the popup's rendered code directly if that is simpler and equally positive)
  </read_first>
  <action>
    Extend the fixture setup to also share a TOTP item (a fixed, known secret) inside the same collection member B already has edit access to. In member B's extension, trigger the SAME code path a real TOTP fill would use (either by opening the item detail view and reading the displayed code, or by dispatching the `autofill.totpCode` message directly against the background if that is more deterministic for a fixed time step) and assert the returned code equals the value independently computed from the known secret for the CURRENT time step — a real, positive byte-equality assertion, not merely "a code appeared." Also add one assertion for the "no TOTP secret -> no TOTP affordance" truth: a shared login item (already in the fixture from 27-04) with no `totp` field renders no TOTP fill row/affordance anywhere B's popup shows it.
  </action>
  <verify>
    <automated>cd extension && npm run pretest:e2e:chrome && npx playwright test --project=chromium e2e/dual-extension-sharing.spec.ts</automated>
  </verify>
  <done>Member B's extension generates a TOTP code for a shared item that byte-equals the independently-computed expected code, and no TOTP affordance appears for a shared item without a TOTP secret.</done>
  <acceptance_criteria>
    `cd extension && npm run pretest:e2e:chrome && npx playwright test --project=chromium e2e/dual-extension-sharing.spec.ts` passes, including the new TOTP byte-equality and no-affordance assertions, run twice consecutively.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Popup <-> background (`autofill.match`) | Unchanged — metadata-only match results cross this boundary, never a field value or derived secret (T-10-13, pre-existing, re-verified unmodified by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-12 | Information Disclosure | A TOTP code computed from stale/undecrypted material for a pending-decrypt shared item | medium | mitigate | `buildFillValues`/`handleAutofillTotpCode` only ever operate on items already present in `getItems()` — a pending item (27-04's `getPendingSharedItems()`) is never in that array, so there is no code path where a TOTP is computed from an item that hasn't finished decrypting |
| T-27-13 | Tampering | UX-3's partition silently reordering items WITHIN a group | low | accept | Task 1's stable-partition test explicitly asserts intra-group order preservation; a regression here is a UX annoyance (fill-list ordering), not a security boundary |

</threat_model>

<verification>
`npm --prefix extension run test -- autofill-match` green. `npx playwright test --project=chromium e2e/dual-extension-sharing.spec.ts` green twice consecutively.
</verification>

<success_criteria>
EXT-07's "reuse the fill pipeline unchanged" claim is proven both negatively (no gating-logic edits) and positively (live recipient-side fill/TOTP works); EXT-08 is proven with a genuine byte-equality assertion, not a mocked stand-in.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `extension/lib/autofill/types.ts` — `AutofillMatch.isShared?`, `AutofillMatch.folderName?`.
- `extension/entrypoints/background/autofill-match.ts` — the personal-before-shared stable partition.

<output>
Create `.planning/phases/27-extension-integration-shared-items/27-05-SUMMARY.md` when done
</output>
