---
phase: 27-extension-integration-shared-items
plan: 09
type: execute
wave: 4
depends_on: ["27-05", "27-08"]
files_modified:
  - extension/entrypoints/popup/autofill/AutofillItemRow.tsx
  - extension/entrypoints/popup/autofill/TotpFillRow.tsx
  - extension/entrypoints/popup/autofill/AutofillItemRow.test.tsx
  - extension/entrypoints/popup/autofill/TotpFillRow.test.tsx
autonomous: true
requirements: [EXT-12]

must_haves:
  truths:
    - "A personal 'Na tej stronie' row's rendering is byte-identical before and after this phase — the badge is additive to shared rows only."
    - "A shared 'Na tej stronie' row carries the identical badge geometry (12px, -bottom-1/-right-1, text-secondary Users glyph) as the 'Wszystkie' rows in ItemListView.tsx — one shape, reused, not re-derived."
</must_haves>
---

<objective>
Apply the SAME `SharedBadge` wrapper (27-08) to `AutofillItemRow.tsx`'s and `TotpFillRow.tsx`'s own existing `h-8 w-8` icon frames — the "Na tej stronie" half of E1's badge coverage, per 27-UI-SPEC.md's explicit scope note that E1 spans both `ItemListView.tsx`'s rows AND these two files' rows.

Purpose: `AutofillMatch.isShared`/`.folderName` (27-05) are already populated correctly by the time these rows render; this plan is pure presentation, consuming already-correct data with zero new logic.
Output: badge wrapper applied to both files' existing icon frames.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-extension-integration-shared-items/27-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge wrapper on AutofillItemRow.tsx and TotpFillRow.tsx</name>
  <files>extension/entrypoints/popup/autofill/AutofillItemRow.tsx, extension/entrypoints/popup/autofill/TotpFillRow.tsx</files>
  <read_first>
    extension/entrypoints/popup/autofill/AutofillItemRow.tsx (full file, already read — the `&lt;span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-[8px] bg-base-200 text-base-content/70"&gt;&lt;Icon ... /&gt;&lt;/span&gt;` icon frame at lines 83-85)
    extension/entrypoints/popup/autofill/TotpFillRow.tsx (full file, already read — the identical-shape icon frame at lines 132-134, `&lt;Timer size={18} .../&gt;`)
    extension/entrypoints/popup/SharedBadge.tsx (27-08's new shared component — import and reuse, never re-derive the wrapper JSX)
    extension/lib/autofill/types.ts (27-05's extended `AutofillMatch.isShared?`/`.folderName?`)
  </read_first>
  <action>
    In both files, wrap the existing icon-frame `&lt;span className="flex h-8 w-8 ..."&gt;` in a `relative inline-flex` container and render `&lt;SharedBadge /&gt;` (row variant) when `match.isShared === true`, importing from `27-08`'s `SharedBadge.tsx` — do not duplicate the badge's JSX/geometry inline in either file. Do not touch anything else in either component (fill/copy logic, confirm-flow, TOTP ticker — all unchanged).
  </action>
  <verify>
    <automated>npm --prefix extension run test -- AutofillItemRow TotpFillRow</automated>
  </verify>
  <done>Both "Na tej stronie" row components carry the same SharedBadge treatment as ItemListView's Wszystkie rows, personal rows unchanged.</done>
  <acceptance_criteria>
    Component tests for both files assert a personal match renders no badge and a shared match (`isShared: true`) renders `SharedBadge` with the correct `aria-label`; `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational change consuming already-validated `AutofillMatch.isShared` metadata; no new trust boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-22 | Spoofing | A miscomputed `isShared` flag misrendering a personal item as shared (or vice versa) | low | accept | The flag is computed once, upstream, in `autofill-match.ts` (27-05) from the already-decrypted `VaultItem.isShared` — this plan has no independent computation to get wrong, only a rendering branch on an already-trusted value |

</threat_model>

<verification>
`npm --prefix extension run test -- AutofillItemRow TotpFillRow` green.
</verification>

<success_criteria>
The "Na tej stronie" half of EXT-12's badge coverage is complete, reusing the exact same component as "Wszystkie".
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `extension/entrypoints/popup/autofill/AutofillItemRow.tsx`, `TotpFillRow.tsx` — `SharedBadge` wrapper applied.

<output>
Create `.planning/phases/27-extension-integration-shared-items/27-09-SUMMARY.md` when done
</output>
