---
phase: 27-extension-integration-shared-items
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pv-provider/src/ceremony.rs
  - crates/pv-provider/tests/response_shape.rs
  - .planning/PROJECT.md
files_read:
  - extension/playwright.config.ts
  - extension/e2e/fixtures.ts
autonomous: true
requirements: [EXT-10]

must_haves:
  truths:
    - "A live credentials.get() provider ceremony against a real RP is executed by 27-06's headed dual-extension ceremony spec, and the actual wire-serialized assertion response's signCount field (authenticatorData bytes 33-36, decoded from member B's own browser-returned assertion) is inspected and asserted to equal 0 — not inferred from the code read alone. This plan's Task 1 supplies the permanent fast in-process Rust regression (the empirical byte decode, run on every commit); 27-06's live spec supplies the genuine wire measurement the decision record below depends on."
    - statement: "A provider-ceremony assertion structurally cannot reach the Phase 19 SEC-04 counter-anomaly classifier (crates/pv-server/src/routes/passkeys.rs's handle_finish_auth_error, doc-commented at lines 299-314, body at 315-350): the classifier is called from exactly 3 sites — passkeys.rs:269 (prf_wrap), passkeys.rs:552 (unlock_finish), auth.rs:575 (passkey_login_finish) — every one inside pv-server's own webauthn-rs vault login/unlock ceremony verified against the passkeys table, and crates/pv-provider's own Cargo.toml has no dependency on webauthn-rs, sqlx, or pv-server at all. The two code paths cannot meet — ROADMAP SC 3's 'does not trip the classifier' clause is satisfied structurally, not by omission."
      verification: backstop
  artifacts:
    - "EXT-10 decision record: a doc comment in crates/pv-provider/src/ceremony.rs plus a Key Decisions row in .planning/PROJECT.md, committed before any dependent code change"
    - "A companion invariant test (Rust, response_shape.rs) asserting a provider-ceremony assertion always reports signCount 0"
  key_links:
    - "The EXT-10 decision record -> no per-item monotonic counter is ever introduced anywhere in this phase's dependent code (27-06's provider-ceremony.ts change stays scoped to key-routing, never counter tracking)"
    - "This plan's Task 1 in-process Rust regression -> 27-06's live headed dual-extension ceremony spec's wire-level signCount==0 assertion — the two-tier evidence (fast permanent regression + live wire measurement) EXT-10 requires, neither alone sufficient"
</must_haves>
---

<objective>
Close EXT-10 by completing 27-CONTEXT.md §A-8's full mandated spike order: this plan's Task 1 supplies the permanent in-process Rust regression against a real create-then-get ceremony (the fast, always-on decode of `signCount` from the raw wire bytes), Task 2 traces whether the Phase 19 SEC-04 counter-anomaly classifier is even reachable from a provider-ceremony assertion (it structurally is not — evidenced against the real pv-server source, not inferred) and writes the EXT-10 decision record (KEY-05 precedent: committed before any dependent code). The one remaining piece of empirical evidence — a live `credentials.get()` against a real browser and a real RP, not this crate's in-process fixture — is supplied by 27-06's headed dual-extension ceremony spec, downstream of this plan's decision record.

Purpose: EXT-10's own requirement text frames this as "no shipped product precedent exists — the starting hypothesis is server-authoritative counter state," a framing 27-CONTEXT.md §A-8 and 27-RESEARCH.md both found is likely wrong by direct code read (pv-provider never sets a signature counter) and external precedent (iCloud Keychain / Google Password Manager both report constant 0). This plan discharges §A-8's mandated order: Task 1 anchors the claim with a permanent regression, Task 2 traces step 3's classifier-reachability question against the actual pv-server source with file:line evidence and writes the decision down (steps 2 and 4) before 27-06 touches any dependent code. Step 1's genuine live-wire measurement (this Rust-only harness has no browser/wire boundary of its own) is completed downstream, live, in 27-06.
Output: EXT-10 decision record (doc comment + PROJECT.md row, including the SEC-04 classifier-reachability finding) + a companion invariant test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-extension-integration-shared-items/27-CONTEXT.md
@.planning/phases/27-extension-integration-shared-items/27-RESEARCH.md
</context>

<assumption_delta_decision>
**Detector finding:** `kind: pluralization`, term "multiple", from "a passkey shared across multiple members' concurrently active extensions" (27-CONTEXT.md/ROADMAP SC 3). The generalization: a provider passkey credential was implicitly modeled as exercised by ONE authenticator instance; it is now genuinely exercised by N concurrent member extensions.

**Decision: `promote`.** The generalized model ("a credential may be exercised by N concurrent members") becomes primary; the single-member case is a degenerate variant (N=1) of it. Rationale, established by this phase's own research and confirmed empirically by Task 1 below: `pv-provider` sets no signature counter at all (`ceremony.rs:87,155` construct `Authenticator::new(...)` with no `.make_credentials_with_signature_counter(true)` call), so promotion is realized by NOT introducing per-device counter state — the anti-goal named in 27-CONTEXT.md §A-8. A per-item monotonic counter would make two members' extensions race on a revision-guarded row and manufacture precisely the counter regression EXT-10 exists to prevent. There is no code path in this decision that distinguishes "one member" from "N members" — that IS the promotion: the concurrent-member case requires zero special-casing because the degenerate single-member case already receives the same (absent) counter treatment.

**Companion invariant test (Task 2):** an assertion produced by ANY member's extension for a shared passkey reports `signCount` 0 — added as a permanent Rust regression test so a future phase that reintroduces per-device counter tracking trips it immediately.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Live wire measurement — confirm signCount on a real provider assertion</name>
  <files>crates/pv-provider/tests/response_shape.rs</files>
  <read_first>
    crates/pv-provider/src/ceremony.rs (the full `create_provider_credential`/`get_provider_assertion` functions, already confirmed at lines 78-192 to construct `Authenticator::new(Aaguid::new_empty(), store, PvUserValidation)` with no signature-counter opt-in, and to compute `updated_passkey_json` by comparing `counter_before`/`after_pk.counter`, both `Option<u32>` staying `None`)
    crates/pv-provider/tests/response_shape.rs (existing test file — the established pattern for driving `create_provider_credential`/`get_provider_assertion` end-to-end against a fixture RP origin/request JSON without a live network RP, i.e. the existing harness already IS the "real ceremony" this task measures — read its existing `<behavior>`-contract test style before adding to it)
  </read_first>
  <action>
    Add a new test that runs a full create-then-get ceremony through `create_provider_credential`/`get_provider_assertion` exactly as `response_shape.rs`'s existing tests already do, then parses the `credential_response_json` returned by `get_provider_assertion` as raw JSON (not through any typed wrapper that might normalize an absent field) and asserts the wire-level `authenticatorData` field decodes to a signature counter of literal 0 -- WebAuthn's `authenticatorData` structure fixes the counter at a 4-byte big-endian offset (bytes 33-36 of the raw authenticator data, after the 32-byte rpIdHash and 1-byte flags), so decode the base64url `response.authenticatorData` field from the parsed JSON and read those 4 bytes directly rather than trusting any Rust-side `Option<u32>` intermediate. Record the exact assertion and its passing output as the empirical confirmation Step 1 of 27-CONTEXT.md §A-8's mandated order requires -- this is a measurement of the wire bytes, not a re-statement of the code read.
  </action>
  <verify>
    <automated>cargo test -p pv-provider response_shape</automated>
  </verify>
  <done>A new test decodes the raw authenticatorData counter bytes from a real ceremony's assertion response and asserts the value is 0.</done>
  <acceptance_criteria>
    `cargo test -p pv-provider response_shape` passes, including the new test, and its assertion message/name makes clear it decoded the raw `authenticatorData` counter bytes (not merely checked `updated_passkey_json.is_none()`, which is a WEAKER, already-known-from-code-read claim). This test is the permanent fast-regression tier only — it runs in-process against this crate's own fixture RP request JSON, never a real browser/wire round trip; the genuine live-wire measurement is 27-06's job (Task 2's own live headed dual-extension ceremony spec), not this one.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decision record + companion invariant regression test</name>
  <files>crates/pv-provider/src/ceremony.rs, .planning/PROJECT.md</files>
  <read_first>
    crates/pv-core/src/identity.rs (lines 1-17 — the KEY-05 decision-record doc-comment precedent this task mirrors: "**Zeroize gap (udokumentowane świadomie, patrz KEY-05 decision record...")
    .planning/PROJECT.md (the `## Key Decisions` table, specifically the KEY-05 row — exact table row shape: `| Decision | Rationale | Outcome |`)
    crates/pv-server/src/routes/passkeys.rs lines 299-350 (the SEC-04 counter-anomaly classifier: the doc-commented `handle_finish_auth_error`, its `matches!(e, WebauthnError::CredentialPossibleCompromise)` branch, and the `UPDATE passkeys SET counter_anomaly_at = ...` write it performs — called from exactly 3 sites, confirm each by grep: `passkeys.rs:269` inside `prf_wrap`, `passkeys.rs:552` inside `unlock_finish`, `auth.rs:575` inside `passkey_login_finish` — every one of them a vault LOGIN/unlock ceremony verified by pv-server's own `webauthn-rs` `Webauthn` instance against the `passkeys` table, never a provider-issued ITEM passkey)
    crates/pv-provider/Cargo.toml (confirms `pv-provider`'s dependency list — `passkey-authenticator`/`passkey-client`/`passkey-types`/`async-trait`/`coset`/`serde`/`thiserror`/`url` — has no dependency on `webauthn-rs`, `sqlx`, or `pv-server` itself anywhere in the graph; the provider-ceremony crate implements the AUTHENTICATOR side of a ceremony against THIRD-PARTY relying parties and structurally never calls any pv-server route handler)
  </read_first>
  <behavior>
    - Test 1: `sign_count_is_always_zero_for_a_provider_ceremony_assertion` — Task 1's raw-byte decode, promoted into a permanently-named regression test (not merely an ad-hoc assertion) that fails loud if any future change starts setting a non-zero counter.
  </behavior>
  <action>
    Add a doc comment above `get_provider_assertion` in ceremony.rs recording the EXT-10 decision: no signature-counter tracking is added for shared provider passkeys (matching the already-shipped absent-counter behavior, confirmed empirically by Task 1); cite the WebAuthn L3 6.1.1 "authenticator does not implement a counter" permitted case, the industry precedent (iCloud Keychain / Google Password Manager both report constant 0 for synced passkeys, per 27-RESEARCH.md's Secondary sources), and the explicit anti-goal (no per-item monotonic counter — two members' extensions would race on a revision-guarded row). State plainly that the requirement's own "no shipped product precedent exists" framing is factually incorrect and why. Add a matching row to PROJECT.md's `## Key Decisions` table (same `| Decision | Rationale | Outcome |` shape as the KEY-05 row) recording this as `Decided (Phase 27)`. Rename/promote Task 1's test into the permanent regression named in `<behavior>` above.

    Also trace, with file:line evidence (not inference), whether a provider-ceremony assertion can EVER reach the Phase 19 SEC-04 counter-anomaly classifier at `crates/pv-server/src/routes/passkeys.rs:299-350` (`handle_finish_auth_error`) — the mandated 27-CONTEXT.md §A-8 step 3. Confirm and record: (1) `handle_finish_auth_error` is called from exactly 3 sites — `passkeys.rs:269`, `passkeys.rs:552`, `auth.rs:575` — every one inside a vault login/unlock ceremony verified by pv-server's own `webauthn-rs` `Webauthn` instance against the `passkeys` table (the vault's own login credentials, not vault items); (2) `pv-provider`'s `Cargo.toml` has no dependency on `webauthn-rs`, `sqlx`, or `pv-server` at all — the provider-ceremony crate never calls any pv-server route handler, let alone this specific classifier. Add this finding to the SAME `ceremony.rs` doc comment and the SAME PROJECT.md Key Decisions row this task already writes — cite the file:line evidence above directly in the decision record, not merely restated prose — stating plainly that ROADMAP SC 3's "does not trip the Phase 19 (SEC-04) sign-counter anomaly classifier" clause is satisfied structurally (the two code paths cannot meet), which is what converts SC 3 from "we shipped no counter" into "we proved the classifier cannot fire."
  </action>
  <reversibility rating="reversible">A future phase can add per-device counter tracking later without undoing anything this decision writes — the decision record documents current (already-shipped) behavior rather than committing new infrastructure that a reversal would need to unwind.</reversibility>
  <verify>
    <automated>cargo test -p pv-provider sign_count_is_always_zero_for_a_provider_ceremony_assertion</automated>
  </verify>
  <done>The EXT-10 decision record (including the SEC-04 classifier-unreachability finding) exists in ceremony.rs and PROJECT.md's Key Decisions table, and the permanent regression test is green.</done>
  <acceptance_criteria>
    `cargo test -p pv-provider` green including the renamed `sign_count_is_always_zero_for_a_provider_ceremony_assertion` test; `grep -c 'EXT-10' crates/pv-provider/src/ceremony.rs` &gt;= 1; `grep -c 'passkeys.rs' crates/pv-provider/src/ceremony.rs` &gt;= 1 (the SEC-04 file:line evidence actually lands in the decision record, not merely asserted in prose elsewhere); `.planning/PROJECT.md`'s Key Decisions table contains a row whose Decision column starts with `EXT-10`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party RP <-> provider ceremony | The RP receives `signCount: 0` on every assertion for a provider-issued passkey; some RPs treat a persistently-zero counter as a signal, per-RP-policy, outside this product's control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Repudiation | Signature-counter-based clone detection assumed to protect a shared provider passkey | medium | accept | Explicitly NOT built (EXT-10 decision) — WebAuthn L3 permits `signCount == 0` as "authenticator does not implement a counter," and every major synced-passkey provider (iCloud Keychain, Google Password Manager) ships this identical behavior; documented as a deliberate, evidence-backed non-decision, not a silently-unaddressed gap |
| T-27-04 | Tampering | A future maintainer reintroduces a per-item monotonic counter to "fix" EXT-10 | high | mitigate | The decision-record doc comment states the anti-goal explicitly with rationale (race on a revision-guarded row); Task 2's regression test trips red the instant counter tracking is reintroduced, giving the future maintainer an immediate, specific signal rather than a silent regression |

</threat_model>

<verification>
`cargo test -p pv-provider` green. `.planning/PROJECT.md` and `ceremony.rs` both carry the EXT-10 decision record, committed in this plan's own commit — before 27-06 touches `provider-ceremony.ts` (the TypeScript dependent-code plan).
</verification>

<success_criteria>
EXT-10's decision record is anchored in evidence, not a code-read restatement: a permanent in-process regression (Task 1), a file:line-evidenced SEC-04 classifier-unreachability finding (Task 2), and the decision recorded per the KEY-05 precedent before any TypeScript-side dependent code lands. The one remaining evidence tier — the genuine live-wire measurement against a real browser — is completed downstream in 27-06, closing the loop this plan's decision record opens.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- EXT-10 decision-record doc comment in `crates/pv-provider/src/ceremony.rs`, including the SEC-04 classifier-unreachability finding with file:line evidence.
- New Key Decisions row in `.planning/PROJECT.md`.
- `sign_count_is_always_zero_for_a_provider_ceremony_assertion` regression test in `crates/pv-provider/tests/response_shape.rs`.

<output>
Create `.planning/phases/27-extension-integration-shared-items/27-02-SUMMARY.md` when done
</output>
