---
phase: 27-extension-integration-shared-items
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/e2e/fixtures-account-setup.ts
  - extension/e2e/dual-extension-sharing.spec.ts
  - extension/e2e/dual-extension-revocation.spec.ts
  - extension/e2e/dual-extension-access-levels.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [EXT-07, KEY-01]

must_haves:
  truths:
    - "A hidden_password-access recipient's extension still autofills the item's REAL username/password onto a real page's DOM (a positive, present, populated field-value assertion), while the popup's own detail view shows the 10-dot mask with no reveal button and no copy button for that field -- verified live by dual-extension-access-levels.spec.ts."
    - "A read-only-access recipient's real capture-confirm write attempt is refused (the toast surfaces its own error state) and the collection owner's copy of the item is genuinely unchanged afterward -- verified live by dual-extension-access-levels.spec.ts, exercising capture-handler.ts's ReadOnlyAccessError gate for the first time with live evidence."
    - "A shared login's autofill FILL EVENT (not merely the item's own display) is exercised live: the shared item's own urls field now matches a real page, and a real click on its 'Na tej stronie' Fill button populates that page's own username/password inputs with the exact shared plaintext -- verified live by dual-extension-sharing.spec.ts's extended assertion, closing the EXT-07 evidence gap 27-VERIFICATION.md flagged."
    - "REQUIREMENTS.md's KEY-01 traceability row reads Complete, matching the extension client trigger 27-04 already shipped and proved live (the row previously read Partial, contradicting the requirement's own [x] checkbox and 27-04-SUMMARY.md's requirements-completed list)."
    - "dual-extension-sharing.spec.ts's and dual-extension-revocation.spec.ts's popup sign-in no longer races a cold service-worker wake at signInAndUnlock's first interaction -- verified by N consecutive --retries=0 runs of the affected spec(s); if the flake persists despite the fix, its failure signature is recorded explicitly in the affected spec file's own header comment rather than left silently masked by playwright.config.ts's retries:2."
  artifacts:
    - path: "extension/e2e/dual-extension-access-levels.spec.ts"
      provides: "Live coverage for hidden_password autofill-without-reveal and read-only write-refusal -- the exact shape of Phase 26's own repeat gap, per 27-VERIFICATION.md's own framing."
    - path: "extension/e2e/fixtures-account-setup.ts"
      provides: "setupAccessLevelFixture() (hidden_password direct share + a read-access collection), and sharedItemName's item now carrying a real urls entry."
  key_links:
    - from: "fixtures-account-setup.ts::setupAccessLevelFixture"
      to: "extension/entrypoints/background/capture-handler.ts::ReadOnlyAccessError"
      via: "a real collection-membership access_level:\"read\" grant, exercised through a real capture.confirm round trip"
      pattern: "ReadOnlyAccessError"
    - from: "fixtures-account-setup.ts::setupAccessLevelFixture"
      to: "extension/entrypoints/popup/ItemDetailView.tsx::passwordFieldHidden"
      via: "a real item_shares access_level:\"hidden_password\" grant (POST /api/vault/items/{id}/shares)"
      pattern: "hidden_password"
---

<objective>
Close three of 27-VERIFICATION.md's remaining items, all e2e-only (no production code changes):

- Gap 3 (must fix): zero live coverage of `hidden_password` and read-only access exists anywhere in
  this phase -- both existing fixtures grant `edit` only. Add a fixture and a live spec exercising
  both access levels' actual behavioral consequences (autofill-without-reveal, write-refusal).
- Gap 4 (must fix, cheap): EXT-07's fill EVENT is never exercised live (only its display and TOTP
  halves are); REQUIREMENTS.md's KEY-01 traceability row still reads "Partial" though the extension
  trigger shipped and was proved live in 27-04. Add the missing fill assertion; reconcile the
  KEY-01 row.
- Gap 5 (flake, fix if cheap): `dual-extension-sharing.spec.ts` failed 2/6 verifier attempts, always
  at `signInAndUnlock`'s first `waitForSelector` -- a cold-service-worker-wake race, masked by
  `playwright.config.ts`'s `retries: 2`. Add a bounded service-worker-readiness wait before the
  first popup interaction in every spec sharing this duplicated helper shape.

Purpose: this is the exact repeat shape of Phase 26's own two shipped-but-broken features
(sharing worked one-way; hidden_password protected nothing while the UI claimed it did) -- both
were only ever found by a live two-session Playwright run. Phase 27 built the recipient-side READ
path's live proof early (27-04), but never extended it to the two access levels whose ENTIRE point
is restricting behavior.
Output: `setupAccessLevelFixture()`, `dual-extension-access-levels.spec.ts`, an extended
`dual-extension-sharing.spec.ts` fill assertion, a service-worker-readiness fix applied to both
existing dual-extension specs, and reconciled REQUIREMENTS.md/STATE.md bookkeeping.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-extension-integration-shared-items/27-VERIFICATION.md
@.planning/phases/27-extension-integration-shared-items/27-11-SUMMARY.md
@.planning/REQUIREMENTS.md
@extension/e2e/fixtures-account-setup.ts
@extension/e2e/dual-extension-sharing.spec.ts
@extension/e2e/dual-extension-revocation.spec.ts
@extension/entrypoints/background/capture-handler.ts
@extension/entrypoints/popup/ItemDetailView.tsx
@extension/lib/autofill/save-update-toast.ts

<interfaces>
`POST /api/vault/items/{id}/shares` (`crates/pv-server/src/routes/vault.rs::create_share`) --
body `{ recipient_user_id, sealed_key, access_level }`, `access_level` one of `"read"` / `"edit"` /
`"hidden_password"` (`membership.rs::parse_access_level`). Forbidden on a collection-scoped item
(400) -- only a PERSONAL item (`collection_id IS NULL`) may carry a direct share.

`sealItemKeyForRecipient(uk: WasmUserKey, enc_key_json: string, item_id: string, recipient_pk:
WasmIdentityPublicKey): string` (`extension/lib/crypto/wasm-loader.ts`, re-exported from
`crates/pv-wasm`) -- the direct-share counterpart of `sealCollectionKey`. `enc_key_json` is the
STRING form of the item's own `enc_key` sub-field (from `splitCombinedEncryptedItem`'s `encKey`
output), never the full combined ciphertext JSON -- mirrors
`web/src/lib/vault/store.real-wasm.test.ts`'s own proven usage (`sealItemKeyForRecipient(aliceUk,
encKey, itemId, bobPub)`).

`POST /api/vault/collections/{id}/members` (already used by `setupSharedFixture`) -- body
`{ recipient_user_id, sealed_key, access_level }`, same three-value `access_level` vocabulary.

`extension/entrypoints/background/capture-handler.ts::ReadOnlyAccessError` -- thrown by
`confirmUpdateLogin` when `target.collectionId != null && target.accessLevel !== "edit" &&
target.accessLevel !== "hidden_password"`. Only fires for a COLLECTION-scoped item -- a directly-
shared (`item_shares`) item's `collectionId` is always `null` (`decryptDirectSharedRow`), so this
gate never applies to a direct share. The read-only fixture below MUST therefore be a
collection-membership grant, not a direct item share, for this specific test to exercise the
CLIENT-side `ReadOnlyAccessError` contract 27-VERIFICATION.md names explicitly (a direct-share
"read" grant would instead be refused server-side only, a weaker and different proof).

`extension/lib/autofill/save-update-toast.ts::showError()` -- on any `capture.confirm` response
whose `status` is neither `"ok"` nor `"conflict"` (i.e. `"error"`, which is what `ReadOnlyAccessError`
maps to in `router.ts`'s `handleCaptureConfirmMessage`), the toast's `data-pv-toast-message`
element becomes visible with class `pv-toast-message-error` and `dismissBtn` is hidden -- the
positive, observable client-side signal of refusal, reachable via this spec's existing CDP
`cdpQuery(..., (_n, a) => hasAttr(a, "data-pv-toast-message"))` helper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fixtures-account-setup.ts -- expose sharedItemName's urls/id, add setupAccessLevelFixture()</name>
  <read_first>extension/e2e/fixtures-account-setup.ts (setupSharedFixture in full -- the exact create-personal-then-move / direct-add-family-member / identity-keypair patterns to mirror)</read_first>
  <files>extension/e2e/fixtures-account-setup.ts</files>
  <action>
    In `setupSharedFixture()`: change the login item's `itemPlaintext.urls` from `[]` to
    `[CAPTURE_FORM_ORIGIN]` (the SAME fixed page this file's own capture-form server already
    serves, per this file's own header comment on `CAPTURE_FORM_PORT`). Extract the currently-
    inline `"pv-e2e-shared-username"` / `"pv-e2e-shared-password"` literals into two local
    `const` variables (e.g. `sharedItemUsername`/`sharedItemPassword`) used both when building
    `itemPlaintext` and in the returned object. Add three new fields to `SharedFixtureResult`
    and to the object `setupSharedFixture` returns: `sharedItemId: string` (the existing local
    `itemId` variable, currently never returned), `sharedItemUsername: string`,
    `sharedItemPassword: string`.

    Add a new exported interface `AccessLevelFixtureResult` with fields: `memberAEmail`,
    `memberAPassword`, `memberBEmail`, `memberBPassword` (same shape as `SharedFixtureResult`'s
    own four), `hiddenPasswordItemId: string`, `hiddenPasswordItemName: string`,
    `hiddenPasswordItemUsername: string`, `hiddenPasswordItemPassword: string`,
    `readOnlyCollectionId: string`, `readOnlyItemId: string`, `readOnlyItemName: string`,
    `readOnlyItemUsername: string`, `readOnlyItemOldPassword: string`.

    Add a new exported async function `setupAccessLevelFixture(): Promise&lt;AccessLevelFixtureResult&gt;`,
    following `setupSharedFixture`'s own structure (idempotent account bring-up for owner/A/B via
    `ensureAccount`, idempotent singleton-family create + `ensureFamilyMember` for A and B, wrapped
    in the same `try`/`finally` that frees every `WasmUserKey` handle at the end):

    1. Publish B's identity keypair via the existing `ensurePublishedIdentityKeypair(b.token,
       b.uk)` (needed by both sub-fixtures below).
    2. **hidden_password (a DIRECT item share, no collection):** create a fresh personal login
       item owned by A (`randomUUID()` id, a unique name/username per call using `Date.now()`, a
       fixed password literal, `urls: [CAPTURE_FORM_ORIGIN]`) via `wasm.encryptItem(a.uk, ...)` ->
       `splitCombinedEncryptedItem` -> `POST /api/vault/items` (expect 201) -- mirror
       `setupSharedFixture`'s own item-create block exactly. Then seal that item's own `enc_key`
       (the STRING form from the split above) to B's public key via
       `wasm.sealItemKeyForRecipient(a.uk, encKey, itemId, bPublicKeyObj)` (build
       `bPublicKeyObj` via `wasm.WasmIdentityPublicKey.fromBytes(base64Decode(bPublicKeyB64))`,
       freed in a `finally`), then `POST /api/vault/items/{itemId}/shares` with
       `{ recipient_user_id: b.userId, sealed_key, access_level: "hidden_password" }` (expect 201).
    3. **read (a FRESH collection, member B joins at access_level "read"):** publish A's own
       identity keypair too (`ensurePublishedIdentityKeypair(a.token, a.uk)`, needed to seal the
       Collection Key to A's own public key exactly like `setupSharedFixture` already does), then
       mirror `setupSharedFixture`'s own collection-creation block VERBATIM (fresh
       `WasmCollectionKey.generate()`, `POST /api/vault/collections`, `POST
       /api/vault/collections/{id}/members`) with exactly one change: the `members` POST body's
       `access_level` is `"read"`, not `"edit"`. Then create ONE login item (unique name/username,
       a fixed old-password literal, `urls: [CAPTURE_FORM_ORIGIN]`) via the SAME
       create-personal-then-move-into-collection sequence `setupSharedFixture`'s login item already
       establishes (`encryptItem` at revision 1 -> `POST /api/vault/items` -> `encryptItemForCollection`
       at revision 2 -> `PUT /api/vault/items/{id}/collection`). Free the `WasmCollectionKey` handle
       in a `finally` around this whole sub-block, mirroring `setupSharedFixture`'s own disposal.
    4. Return the `AccessLevelFixtureResult` object with every field named above.
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` is clean (this file has no dedicated unit test; its correctness is proven
      by Task 3's live spec actually calling `setupAccessLevelFixture()` successfully).
    - `grep -c 'access_level: "hidden_password"' extension/e2e/fixtures-account-setup.ts` is at
      least 1; `grep -c 'access_level: "read"' extension/e2e/fixtures-account-setup.ts` is at
      least 1 (both new grants are genuinely present, not accidentally left as "edit").
  </acceptance_criteria>
  <done>setupAccessLevelFixture() provisions a real hidden_password direct share and a real
  read-access collection, both via genuine crypto and real REST calls; sharedItemName's item now
  has a real urls entry and its id/username/password are exposed.</done>
</task>

<task type="auto">
  <name>Task 2: EXT-07 live fill assertion + the service-worker-readiness (GAP 5) fix</name>
  <precondition>a local pv-server is running with PV_STATIC_DIR pointed at web/out and PV_EXTENSION_ORIGINS set (this phase's standing live-harness requirement, established by 27-04/27-06/27-11-SUMMARY.md)</precondition>
  <read_first>extension/e2e/dual-extension-sharing.spec.ts (signInAndUnlock and its two helpers, getServiceWorker, the existing headline-proof test body from the presence assertions through the TOTP block), extension/e2e/dual-extension-revocation.spec.ts (its own duplicated signInAndUnlock/ensureServerConfigured/signInWithPassword copy), extension/e2e/dual-browser.spec.ts (P10-SC1's exact live-fill drive pattern: form page -> bringToFront -> popup.reload() -> locate [data-testid="autofill-fill-${itemId}"] -> click -> waitForFunction on the input's own value -> read both fields)</read_first>
  <files>extension/e2e/dual-extension-sharing.spec.ts, extension/e2e/dual-extension-revocation.spec.ts</files>
  <action>
    GAP 5 fix, applied identically to BOTH files (the two `signInAndUnlock` copies are byte-
    identical duplicates by this codebase's own established precedent, and the diagnosed race --
    a cold MV3 service-worker wake racing the popup's own first `chrome.runtime.sendMessage` call
    inside `signInAndUnlock` -- applies equally to both, even though only the sharing spec's own
    flake was directly observed by the verifier): change `signInAndUnlock`'s signature to accept
    the owning `BrowserContext` as its first parameter (`signInAndUnlock(context, popup, email,
    password)`), and as the VERY FIRST line of its body, await the SAME service-worker resolution
    `dual-extension-sharing.spec.ts`'s own `getServiceWorker(context)` helper already performs for
    the storage audit later in that file (checks `context.serviceWorkers()` first, falls back to
    `context.waitForEvent("serviceworker", { timeout: 20000 })`) -- BEFORE the existing
    `popup.evaluate(() => chrome.runtime.sendMessage({ kind: "config.get" }))` line. In
    `dual-extension-revocation.spec.ts`, which does not yet define `getServiceWorker` at all, add
    that helper verbatim (ported from `dual-extension-sharing.spec.ts`'s own definition). Update
    every call site of `signInAndUnlock` in both files to pass the correct context
    (`extContext`/`extContextB` as appropriate). Document the fix inline at `signInAndUnlock`'s own
    doc comment: this is the diagnosed cause of 27-VERIFICATION.md's Gap 5 (2/6 verifier attempts
    failed, always at this function's own first `waitForSelector`).

    GAP 4 fix, in `dual-extension-sharing.spec.ts` only: after the existing TOTP byte-equality
    block (`expect(candidates).toContain(returnedCode)`) and BEFORE the existing "no TOTP
    affordance" detail-view click, add a new block that drives a REAL autofill fill of
    `fixture.sharedItemName`'s own item (now reachable at `CAPTURE_FORM_ORIGIN` per Task 1's
    fixture change) through the popup's own "Na tej stronie" UI, mirroring
    `dual-browser.spec.ts`'s P10-SC1 pattern exactly: open a fresh page in `extContextB`, navigate
    it to `CAPTURE_FORM_ORIGIN` (this file's own already-running `captureFormServer`, no new
    server needed), `bringToFront()` it, then `popupB.reload()` and `popupB.waitForSelector("select",
    ...)` so the popup's own active-tab resolution picks up the newly-focused page. Locate the fill
    button via `popup.locator('[data-testid="autofill-fill-${fixture.sharedItemId}"]')`, assert
    it's visible, click it, `waitForFunction` on the form page's own `#u` input's value becoming
    non-empty, then read both `#u`/`#p` values and assert they equal `fixture.sharedItemUsername`/
    `fixture.sharedItemPassword` exactly (a positive, present, populated field-value match -- the
    fill EVENT itself, not merely the item's own display, closing the specific evidence gap
    27-VERIFICATION.md names for EXT-07). Close the fill-target page afterward. The remainder of
    the existing test (the detail-view no-TOTP-secret check, the storage audit, the write-path
    proof) is unaffected and continues unchanged after this new block -- `popupB`'s subsequent
    `.getByText(fixture.sharedItemName, ...)` call still works after the `reload()` since the
    background session stays unlocked across a popup document reload.
  </action>
  <verify>
    <automated>cd extension && npm run pretest:e2e:chrome && npx playwright test --project=chromium e2e/dual-extension-sharing.spec.ts --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - `dual-extension-sharing.spec.ts` passes with `--retries=0`, run at least 3 times consecutively
      (documented in the SUMMARY), to gather genuine evidence the service-worker-wait fix closed
      the observed flake -- if any of those runs still fails at the SAME `signInAndUnlock` location
      after the fix, do not silently re-enable a masking retry: add an explicit, dated comment at
      the top of the affected spec file recording the failure signature (the exact assertion/step
      that failed and how often), so the flake is a documented, known issue rather than a silently
      re-masked one.
    - `dual-extension-revocation.spec.ts` still passes (its own live run, `--retries=0`).
    - The new fill assertion is a positive field-value match against `fixture.sharedItemUsername`/
      `fixture.sharedItemPassword`, never a mere "no error thrown" or count-only check.
  </acceptance_criteria>
  <done>Both dual-extension specs await a real service-worker readiness signal before their first
  popup interaction; the sharing spec's live proof now also drives a genuine DOM fill of a shared
  item and asserts the filled values.</done>
</task>

<task type="auto">
  <name>Task 3: live hidden_password/read-only coverage + REQUIREMENTS.md/STATE.md bookkeeping</name>
  <precondition>a local pv-server is running with PV_STATIC_DIR pointed at web/out and PV_EXTENSION_ORIGINS set (same standing requirement as Task 2)</precondition>
  <read_first>extension/e2e/dual-extension-sharing.spec.ts (the FIXED signInAndUnlock/ensureServerConfigured/getServiceWorker/cdpSession/cdpQuery/cdpClick/cdpClickAttr/waitForCdp/hasAttr helpers and captureLoginPage(), all to be ported verbatim into the new file), .planning/REQUIREMENTS.md (the KEY-01 bullet's own prose and its Traceability table row), .planning/STATE.md (the Blockers/Concerns section's existing per-phase entry format)</read_first>
  <files>extension/e2e/dual-extension-access-levels.spec.ts, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Create `extension/e2e/dual-extension-access-levels.spec.ts`, headless (`chromium` project, no
    WebAuthn ceremony), importing `setupAccessLevelFixture` from `./fixtures-account-setup`. Port
    verbatim (this codebase's own established duplication precedent, per
    `dual-extension-revocation.spec.ts`'s own header comment) the ALREADY-FIXED (Task 2)
    `signInAndUnlock`/`ensureServerConfigured`/`signInWithPassword`/`getServiceWorker` helpers, and
    the `cdpSession`/`cdpQuery`/`cdpClick`/`cdpClickAttr`/`waitForCdp`/`hasAttr`/`captureLoginPage`
    helpers from `dual-extension-sharing.spec.ts`. Stand up this file's OWN dependency-free HTTP
    form server on port 8898 (a fresh, unused port in this suite's own registry -- 8620/8791/8792/
    8895/8896/8897/8899 are all already claimed by sibling spec files) serving `captureLoginPage()`'s
    exact markup, in its own `beforeAll`/`afterAll` (mirroring `dual-extension-sharing.spec.ts`'s
    own `closeAllConnections()` teardown fix). Use this ONE page/origin for both sub-tests below
    (it has both fillable `#u`/`#p` inputs for the autofill half and a real submit handler for the
    capture half).

    Write ONE test, titled to name both concerns, accepting `{ extContext, extensionId, extContextB,
    extensionIdB }` and `test.setTimeout(180_000)`:

    1. Call `setupAccessLevelFixture()`.
    2. Sign member B into `popupB` (via the ported, already-fixed `signInAndUnlock`).
    3. **hidden_password, detail-view masking:** assert `fixture.hiddenPasswordItemName` is visible
       in the list, click into it, assert `getByTestId("hidden-password-extension-note")` is
       visible, assert there is no reveal-password button (`getByRole("button", { name: /show
       password/i })` has count 0), and assert there is no copy button scoped to the password
       field's own row (locate the password field's row container -- e.g. via the field label
       text "Password" -- and assert no `button[aria-label*="Copy"]`/copy-icon descendant exists
       within it). Navigate back to the list.
    4. **hidden_password, autofill still works:** drive a REAL fill exactly like Task 2's own new
       block (open a page at this file's own form origin, `bringToFront()`, `popupB.reload()`,
       locate `[data-testid="autofill-fill-${fixture.hiddenPasswordItemId}"]`, click, wait for the
       form's `#u` value to become non-empty, assert `#u`/`#p` equal
       `fixture.hiddenPasswordItemUsername`/`fixture.hiddenPasswordItemPassword` exactly). Close
       that page.
    5. **read-only, write refusal:** open a NEW page at this file's own form origin, `bringToFront()`,
       wait ~1s for the content-relay submit watcher to attach (mirrors
       `dual-extension-sharing.spec.ts`'s own established wait), open a CDP session, fill `#u`
       with `fixture.readOnlyItemUsername` and `#p` with a freshly-generated attempted-new-password
       literal, press Enter on the password field (a real submit event), wait for the real toast
       (`data-pv-toast` via `waitForCdp`), assert it appeared, click `data-pv-toast-confirm` via
       `cdpClickAttr`, assert the click succeeded, then wait for and assert the toast's OWN error
       signal appears (`data-pv-toast-message` becomes present via `waitForCdp`) -- a positive
       observation that the write was refused, not an absence-only guard. Close that page.
    6. **read-only, load-bearing proof:** sign member A into a fresh `popupA` (via the same ported
       `signInAndUnlock`), search for `fixture.readOnlyItemUsername` in the popup's own search box,
       click into the resulting row, reveal the password field, and assert it STILL equals
       `fixture.readOnlyItemOldPassword` exactly -- the write member B attempted never landed. This
       is the assertion that actually matters: even if the toast's error-state timing were ever
       flaky, the owner's copy staying unchanged is the real, load-bearing proof of refusal.

    In `.planning/REQUIREMENTS.md`: rewrite the KEY-01 bullet's second paragraph (currently starting
    "PARTIAL after Phase 22...") to state completion -- delivered: pv-core crypto (Phase 21), the
    full server half (Phase 22), the web client trigger (Phase 26), AND the extension client trigger
    (Phase 27, `vault-session.ts`'s `setUnlockedUserKey` calling `identity-store.ts`'s
    `publishOnUnlock(uk)` at the extension's single unlock choke point, proved live by
    `dual-extension-sharing.spec.ts`). Change the Traceability table's `KEY-01` row's Status column
    from `Partial` to `Complete`.

    In `.planning/STATE.md`'s `### Blockers/Concerns` section, append a new `[Phase 27]` entry (same
    format as the existing per-phase bullets in that section) recording, as an accepted and
    explicitly out-of-scope finding: `capture-handler.ts`'s `buildLoginFields()` (Phase 11)
    unconditionally derives an item's `name` from the submitting page's hostname on every
    capture-confirm save, new AND update alike -- for a SHARED item, one member's capture write
    silently renames the item for every other member too (confirmed live by 27-11's own write-path
    proof). Not a Phase 27 regression; recommend a future fix that preserves an existing item's
    custom name on an `'update'` capture-confirm, deriving a hostname-based name only for a
    brand-new `'new'` capture.
  </action>
  <verify>
    <automated>cd extension && npm run pretest:e2e:chrome && npx playwright test --project=chromium e2e/dual-extension-access-levels.spec.ts --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - The new spec passes with `--retries=0`.
    - Step 3's reveal/copy-absence assertions are anchored (the item's own name and the honesty
      note are asserted present FIRST), never a bare `toHaveCount(0)` with nothing else confirmed
      first (27-RESEARCH.md's own vacuous-assertion-trap discipline).
    - Step 4's and step 6's assertions are positive, present, populated field-value matches against
      known fixture literals, never mere absence-of-error checks.
    - `grep -c '"Partial"' .planning/REQUIREMENTS.md` no longer matches the KEY-01 table row
      specifically (spot-check the row text directly rather than a blind repo-wide grep, since
      other rows may legitimately still read Partial).
    - `.planning/STATE.md`'s Blockers/Concerns section contains a `[Phase 27]` entry naming
      `buildLoginFields()`.
  </acceptance_criteria>
  <done>Live, non-vacuous evidence exists for hidden_password autofill-without-reveal and
  read-only write-refusal; KEY-01's traceability row matches shipped reality; the buildLoginFields
  rename finding is recorded for a future phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-only e2e fixtures/specs | This plan touches no production code -- every file it modifies is either a test fixture (`fixtures-account-setup.ts`), a Playwright spec, or planning documentation (`REQUIREMENTS.md`/`STATE.md`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-31 | Tampering | `setupAccessLevelFixture()`'s new REST calls | low | accept | Uses the SAME fixed, deterministic test-only credentials (`MEMBER_A_EMAIL`/`MEMBER_B_EMAIL`) and the SAME real-crypto-never-dummy discipline every existing fixture in this file already follows -- no new credential material, no weakening of any server-side authorization check (the new grants exercise `access_level` values the server already validates via `parse_access_level_from_request`). |
| T-27-32 | Elevation of Privilege | the new `hidden_password`/`read` access-level fixtures | low | accept | This plan only EXERCISES the existing, already-shipped server-side and client-side access-level gates (27-07's `ReadOnlyAccessError`, 27-08's `passwordFieldHidden`) with live data -- it adds no new authorization code path and grants no access beyond what a real user could grant through the (out-of-this-phase-scope) web app's own share dialogs. |

No package-manager installs in this plan (`T-{phase}-SC` not applicable).
</threat_model>

## Artifacts this phase produces

- `extension/e2e/dual-extension-access-levels.spec.ts` (new) -- live hidden_password + read-only coverage.
- `extension/e2e/fixtures-account-setup.ts` -- `setupAccessLevelFixture()`, `SharedFixtureResult`'s new `sharedItemId`/`sharedItemUsername`/`sharedItemPassword` fields, `sharedItemName`'s item now carrying a real `urls` entry.
- `extension/e2e/dual-extension-sharing.spec.ts` -- the live EXT-07 fill-event assertion; the service-worker-readiness fix.
- `extension/e2e/dual-extension-revocation.spec.ts` -- the same service-worker-readiness fix.
- `.planning/REQUIREMENTS.md` -- KEY-01 reconciled to Complete.
- `.planning/STATE.md` -- the buildLoginFields rename finding recorded under Blockers/Concerns.

<verification>
`npm run pretest:e2e:chrome` (rebuild) before every e2e run in this plan, per this phase's own
standing convention -- a stale `.output/chrome-mv3` build has caused a false failure once before
(27-VALIDATION.md's own Playwright-project-split note). Every new/modified spec run with
`--retries=0` for genuine single-attempt evidence.
</verification>

<success_criteria>
Live, non-vacuous evidence exists for every access level SHARE-03 defines (edit already proven by
27-04/27-11; hidden_password and read-only now proven by this plan) -- closing the exact repeat
shape of Phase 26's own two shipped-but-broken features. EXT-07's fill event is exercised live.
KEY-01's bookkeeping matches shipped reality. The sign-in flake is either genuinely fixed (N
consecutive green `--retries=0` runs) or explicitly, permanently recorded as a known flake with its
failure signature -- never silently re-masked.
</success_criteria>

<output>
Create `.planning/phases/27-extension-integration-shared-items/27-14-SUMMARY.md` when done
</output>
