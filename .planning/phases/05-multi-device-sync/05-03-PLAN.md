---
phase: 05-multi-device-sync
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - web/src/lib/vault/api.ts
  - web/src/lib/vault/sync.ts
  - web/src/lib/vault/syncStatus.ts
  - web/src/lib/vault/store.ts
  - web/src/lib/vault/sync.test.ts
  - web/src/lib/vault/store.test.ts
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03]

must_haves:
  truths:
    - "The client fires a catch-up GET /api/sync pull on every WS (re)connect and on a fixed ~30s interval whenever the vault is unlocked, and stops both entirely the instant the vault locks — no background network chatter while locked"
    - "A WS drop triggers exponential-backoff reconnect (capped ~30s, jittered) rather than a fixed-interval retry storm, and a deliberate stopSync() call never gets undone by a stray trailing close event from the socket it just closed"
    - "Merging a fresh snapshot into the store replaces items/folders wholesale (so a server-side deletion is reflected simply by the deleted id's absence from the new array) without ever using a stale/freed User Key handle if a lock event raced the in-flight fetch"
  artifacts:
    - path: "web/src/lib/vault/sync.ts"
      provides: "startSync/stopSync, WS client, reconnect/backoff, poll timer"
      exports: ["startSync", "stopSync", "SyncCallbacks"]
    - path: "web/src/lib/vault/syncStatus.ts"
      provides: "connection-state singleton (connected/reconnecting/offline) + useSyncStatus() hook"
      exports: ["setSyncStatus", "getSyncStatus", "subscribeSyncStatus", "useSyncStatus"]
    - path: "web/src/lib/vault/store.ts"
      provides: "applySyncSnapshot — the single merge implementation shared by initial load and ongoing background sync"
  key_links:
    - from: "web/src/lib/vault/store.ts::subscribeLockState callback"
      to: "web/src/lib/vault/sync.ts::startSync/stopSync"
      via: "the same unlock/lock gate that already governs loadAndDecryptAll"
      pattern: "startSync|stopSync"
    - from: "web/src/lib/vault/sync.ts (internal pullOnce)"
      to: "web/src/lib/vault/api.ts::getSyncSnapshot"
      via: "GET /api/sync?since=N (Plan 05-01's endpoint)"
      pattern: "getSyncSnapshot"
    - from: "web/src/lib/vault/sync.ts (WS client)"
      to: "GET /api/sync/ws?token= (Plan 05-02's endpoint)"
      via: "browser WebSocket API, token from getSessionToken()"
      pattern: "api/sync/ws"
---

<objective>
Wire the browser-side half of SYNC-01/SYNC-02's transport onto the backend Plans 05-01/05-02 already shipped: a WS client with exponential-backoff reconnect, a fixed-interval polling fallback, and a store-level merge function that replaces `loadAndDecryptAll`'s separate list-then-decrypt calls with a single revision-aware snapshot pull — unifying "initial load" and "background sync" into one code path. Per 05-RESEARCH.md's own recommended build order (pull endpoint → WS backend → client engine → UI polish), this is the third step; Plan 05-04 layers the user-visible sync-status dot/conflict-banner/toast on top of the store state this plan produces.

Purpose: a second browser tab/device, once this plan lands, actually observes a first tab's create/edit/delete without a manual page refresh — the functional core of "stays in sync" before any UI polish exists to announce it.
Output: `sync.ts`, `syncStatus.ts`, and an `applySyncSnapshot`-unified `store.ts`, all covered by vitest with a mocked global `WebSocket`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-multi-device-sync/05-CONTEXT.md
@.planning/phases/05-multi-device-sync/05-RESEARCH.md
@.planning/phases/05-multi-device-sync/05-PATTERNS.md

@web/src/lib/vault/api.ts
@web/src/lib/vault/store.ts
@web/src/lib/vault/store.test.ts
@web/src/lib/auth/api.ts
@web/src/lib/auth/session.ts
@web/src/lib/crypto/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WS client + reconnect/backoff + poll timer + sync-status singleton</name>
  <files>web/src/lib/vault/api.ts, web/src/lib/vault/sync.ts, web/src/lib/vault/syncStatus.ts, web/src/lib/vault/sync.test.ts</files>
  <read_first>
    web/src/lib/vault/api.ts (the apiJson pattern this task extends with one more one-line wrapper),
    web/src/lib/auth/api.ts (API_BASE resolution via NEXT_PUBLIC_API_BASE_URL — sync.ts's WS URL must derive from this SAME env var, not invent a second config surface),
    web/src/lib/auth/session.ts (getSessionToken — the WS URL's ?token= param),
    web/src/lib/crypto/index.ts lines 77-127 (the lock-state module-singleton shape — lockListeners Set + notify-on-change + useSyncExternalStore hook — syncStatus.ts copies this exact shape for a 3-state string instead of a boolean),
    .planning/phases/05-multi-device-sync/05-RESEARCH.md "Pattern 4" and "Code Examples" sections (the connectWs/startSync/stopSync reference shape — this task's sync.ts is a MODIFIED version of it, deltas described below — read the reference FIRST, then apply the deltas)
  </read_first>
  <behavior>
    - startSync({getSinceRevision, onSnapshot}) opens a WS connection and starts a 30s poll timer; both ultimately call an internal pullOnce() that fetches getSyncSnapshot(getSinceRevision()) and passes the result to onSnapshot.
    - A WS onopen event resets the backoff delay to its floor and immediately fires one catch-up pullOnce().
    - A WS onmessage event (ANY message, content deliberately unparsed) triggers exactly one pullOnce(); it never reads or acts on the message body itself.
    - A WS onclose/onerror event schedules a reconnect via setTimeout at the current (jittered) backoff delay, doubling for next time, capped at 30000ms — UNLESS stopSync() already ran, in which case no reconnect is scheduled at all.
    - stopSync() closes the WS (if open), clears the poll timer and any pending reconnect timer, sets status "offline", and is idempotent.
    - getSyncSnapshot(since) calls GET /api/sync?since=<since> via the existing apiJson helper.
  </behavior>
  <action>
    In `web/src/lib/vault/api.ts`, add `export interface SyncSnapshot { revision: number; items?: ItemRow[]; folders?: FolderRow[] }` and `export function getSyncSnapshot(since: number): Promise<SyncSnapshot> { return apiJson(`/api/sync?since=${since}`); }`, following the exact `listItems`/`listFolders` one-line-wrapper convention already in this file.

    Create `web/src/lib/vault/syncStatus.ts`: copy `lib/crypto/index.ts`'s lock-state singleton shape (module-level mutable state + a `Set<() => void>` of listeners + a `useSyncExternalStore`-backed hook) for a 3-value `export type SyncStatus = "connected" | "reconnecting" | "offline"` instead of a boolean, defaulting to `"offline"`. Export `setSyncStatus(status: SyncStatus): void`, `getSyncStatus(): SyncStatus`, `subscribeSyncStatus(listener: () => void): () => void`, and `useSyncStatus(): SyncStatus` (mirroring `useIsUnlocked`'s exact `useSyncExternalStore(subscribe, getSnapshot, () => "offline")` shape, including its non-browser-fallback third argument).

    Create `web/src/lib/vault/sync.ts`: implement per 05-RESEARCH.md's Pattern 4, with these SPECIFIC deltas from that reference — do not copy it verbatim, apply these changes:
    1. Replace the single `onEvent: (ev: SyncEvent) => void` callback shape with `export interface SyncCallbacks { getSinceRevision: () => number; onSnapshot: (snapshot: SyncSnapshot) => void }` — `startSync(callbacks: SyncCallbacks): void`. The WS `onmessage` handler does NOT parse `event.data` at all; it unconditionally triggers one `pullOnce()`. This is a deliberate strengthening of SYNC-02's contract ("the client reacts with a normal pull") beyond the reference: the client has zero code path that ever inspects a WS payload's contents, which is a stronger form of the no-ciphertext-trust boundary than merely not adding a ciphertext field to the schema.
    2. `pullOnce()` is internal (not exported): it calls `getSyncSnapshot(callbacks.getSinceRevision())` and passes the result to `callbacks.onSnapshot`; track the active callbacks in a module-level `let activeCallbacks: SyncCallbacks | null = null` (set in `startSync`, cleared in `stopSync`) and make `pullOnce()` a no-op if it is `null`.
    3. Add a module-level `let intentionalStop = true` guard. `startSync` sets it to `false` before connecting; `stopSync` sets it to `true` before closing the socket. The WS `onclose` handler checks this flag FIRST: if `true`, return immediately (no reconnect scheduling — `stopSync` itself already set the final `"offline"` status). This prevents the CURRENTLY-CLOSING socket's own trailing `onclose` event (which fires asynchronously, after `stopSync` has already run) from re-arming a reconnect timer.
    4. Assign each new socket to a local `const socket = new WebSocket(...)` first, `ws = socket`, and reference `socket` (not the mutable module-level `ws` variable) inside that socket's own `onopen`/`onmessage`/`onclose`/`onerror` handlers — additionally guard `onclose` with `if (ws === socket) { ws = null; }` before proceeding, so a superseded/stale socket's late-firing events can never clobber a newer connection's state.
    5. Derive the WS URL from the SAME `process.env.NEXT_PUBLIC_API_BASE_URL` env var `lib/auth/api.ts` already reads (do not add a second config surface): if empty, build `ws(s)://<window.location.host>/api/sync/ws` using `window.location.protocol === "https:" ? "wss:" : "ws:"`; otherwise take the configured base URL and replace its leading `http` with `ws` (covers both `http→ws` and `https→wss`). Append `?token=${encodeURIComponent(token)}` using `getSessionToken()` from `@/lib/auth/session` — if no token is stored, the connect function returns without opening a socket.
    6. Backoff: start at 1000ms, double on each successive close, cap at 30000ms, apply ±25% jitter to the ACTUAL scheduled delay each time (`delay * (0.75 + Math.random() * 0.5)`) without perturbing the underlying doubling sequence itself — a discretionary hardening against reconnect-storm lockstep per 05-RESEARCH.md's Pitfall 4.
    7. `startSync` also starts a `setInterval(() => void pullOnce(), 30_000)` poll timer (unconditionally, regardless of WS state — the belt-and-suspenders fallback 05-CONTEXT.md locks in). `stopSync` clears both the poll interval and any pending reconnect `setTimeout`, closes the WS if open, and calls `setSyncStatus("offline")`.
    8. `setSyncStatus` calls: `"connected"` on WS `onopen`, `"reconnecting"` on WS `onclose` (only on the non-intentional-stop path), `"offline"` from `stopSync`.
  </action>
  <verify>
    <automated>cd web && npm test -- sync.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `sync.test.ts` covers (via `vi.stubGlobal("WebSocket", MockWebSocketClass)` and `vi.useFakeTimers()`): opening a connection calls the mocked `getSyncSnapshot` once on open; a simulated `close` event schedules a reconnect after the current backoff delay, and the delay strictly increases (bounded at 30000ms) across three successive simulated drops; `stopSync()` called immediately after `startSync()` results in NO reconnect attempt even after advancing fake timers past the scheduled delay (the intentional-stop guard is under direct test, not just code-reviewed); the poll timer independently triggers a pull at the 30s mark even with no WS activity
    - `grep -c 'intentionalStop' web/src/lib/vault/sync.ts` reports at least 2 (set in both startSync and stopSync, not just declared)
    - `npm --prefix web test -- sync.test.ts` green
  </acceptance_criteria>
  <done>sync.ts's WS client and poll fallback are both independently proven correct via vitest with a mocked WebSocket and fake timers — no real network or real pv-server process involved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: store.ts — unify initial load and background sync into applySyncSnapshot</name>
  <files>web/src/lib/vault/store.ts, web/src/lib/vault/store.test.ts</files>
  <read_first>
    web/src/lib/vault/store.ts (full file — loadAndDecryptAll, decryptItemRow/decryptFolderRow, the subscribeLockState side effect at the bottom this task extends),
    web/src/lib/vault/store.test.ts (the existing vi.mock/vi.hoisted idiom for ./api and @/lib/crypto to extend, not reinvent)
  </read_first>
  <behavior>
    - A stale snapshot (items/folders present) replaces the in-memory items/folders arrays wholesale — an item id present before but absent from the new snapshot is simply gone from the result (deletion via absence, no separate diff pass).
    - An up-to-date snapshot (no items/folders keys) leaves the current in-memory items/folders completely untouched, but still advances the tracked "last known revision" watermark.
    - Applying a snapshot after the vault has since locked (getUnlockedUserKey() now null) is a safe no-op, matching loadAndDecryptAll's existing lock-race guard.
    - Unlocking starts sync (WS + poll); locking stops sync BEFORE clearing in-memory state.
  </behavior>
  <action>
    In `web/src/lib/vault/store.ts`: add a module-level `let lastKnownRevision = 0;`. Import `getSyncSnapshot`/`SyncSnapshot` from `./api` and `startSync`/`stopSync`/`SyncCallbacks` from `./sync` (remove the now-unneeded `listItems`/`listFolders` imports if nothing else in this file still uses them). Replace `loadAndDecryptAll`'s current `Promise.all([listItems(), listFolders()])` body: it becomes `async function loadAndDecryptAll(): Promise<void> { const snapshot = await getSyncSnapshot(0); applySyncSnapshot(snapshot); }` — passing `since: 0` unconditionally means a fresh/never-synced client always receives a full snapshot (or, for a brand-new zero-item account, an up-to-date-with-no-items response, which is equally correct since there is nothing to load).

    Add `function applySyncSnapshot(snapshot: SyncSnapshot): void`: sets `lastKnownRevision = snapshot.revision` FIRST, regardless of whether `items`/`folders` are present (an up-to-date response still means the client's watermark should track the server's current value, otherwise the NEXT poll tick would immediately re-detect "stale" against a revision the client already knows about); then re-checks `getUnlockedUserKey()` and returns early if `null` (mirrors `loadAndDecryptAll`'s existing lock-race guard — a lock event may have fired while the fetch was in flight); if `snapshot.items !== undefined`, decrypt-and-replace `items` via the existing `decryptItemRow` map + `recomputeAllTags()` + `notifyListeners()` (an ABSENT `items` key means "nothing changed, do not touch the in-memory array" — this is what makes the up-to-date cheap-check path genuinely cheap on the client side too); the same conditional treatment for `snapshot.folders` via `decryptFolderRow` + `notifyFolderListeners()`. Deletion is NOT special-cased beyond this — a full snapshot replacing the entire `items` array structurally drops any id no longer present; no separate ID-diff pass belongs in this module (Plan 05-04's remote-delete-detection logic lives in `page.tsx`/a small `remoteDelete.ts` helper, comparing the ALREADY-updated `items` array against a previously-selected id — this module's job ends at "items is now the correct, current array").

    Extend the existing bottom-of-file `subscribeLockState(() => { ... })` module-level side effect: in the `isUnlocked()` branch, after `void loadAndDecryptAll()`, also call `startSync({ getSinceRevision: () => lastKnownRevision, onSnapshot: applySyncSnapshot })` — reusing the SAME `applySyncSnapshot` function as both the initial-load path and the ongoing-sync path (one merge implementation, not two). In the locked (`else`) branch, add `stopSync()` BEFORE clearing `items`/`folders` to empty arrays (stop the transport first, then clear state — avoids a race where an in-flight sync callback fires after the arrays were already cleared) and reset `lastKnownRevision = 0` alongside the existing `items = []`/`folders = []` resets.
  </action>
  <verify>
    <automated>cd web && npm test -- store.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All pre-existing `store.test.ts` cases pass — update the existing `vi.mock("./api", ...)` block's shape from `mockListItems`/`mockListFolders` to `mockGetSyncSnapshot` (a fresh/never-synced-user fixture should resolve `{ revision: 0, items: [], folders: [] }` by default in `beforeEach`, not leave a stale mock resolving a function that no longer exists), and add `vi.mock("./sync", ...)` mocking `startSync`/`stopSync`
    - New case: "merges a stale snapshot by replacing items wholesale, dropping any id absent from the new array"
    - New case: "an up-to-date snapshot (no items/folders keys) leaves the current in-memory items/folders untouched"
    - New case: "startSync/stopSync are called exactly once each across an unlock-then-lock cycle"
    - New case: "merging an unrelated item's update does not disturb a different, unmodified item's array entry"
  </acceptance_criteria>
  <done>store.ts's initial load and ongoing background sync share one merge implementation (applySyncSnapshot); sync starts/stops in lockstep with the existing unlock/lock lifecycle, with no network activity possible while locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — the browser already trusts pv-server's authenticated responses for every existing REST call; sync merely adds a new trigger path over the SAME boundary Phase 2 established.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-09 | Tampering (client-side) | `sync.ts` `onmessage` | low | mitigate | the WS payload's contents are never parsed or acted upon — any message can only ever cause a `pullOnce()`, which re-derives truth from the authenticated `GET /api/sync` response; there is no code path where WS payload bytes influence client state directly |
| T-05-10 | Denial of Service (self-inflicted) | `sync.ts` reconnect backoff | low | mitigate | ±25% jitter on the exponential backoff avoids synchronized reconnect storms across multiple tabs/devices after a shared network blip |
| T-05-11 | Elevation of Privilege | `sync.ts` token usage | low | accept | reuses the SAME `getSessionToken()` already trusted by every existing REST call (Phase 2); no new credential handling introduced, no new exposure beyond what Plan 05-02 already accepted for the WS query-param carry |

</threat_model>

<verification>
`npm --prefix web test` (whole suite) green.
</verification>

<success_criteria>
A second browser tab, once unlocked, receives and merges another tab's create/edit/delete via the real WS+poll transport (mocked in tests, wired for real usage) — proven by vitest, not just code inspection. No network activity fires while the vault is locked.
</success_criteria>

<output>
Create `.planning/phases/05-multi-device-sync/05-03-SUMMARY.md` when done
</output>
