---
phase: 30-the-living-group-family-wide-sharing
plan: 17
type: execute
wave: 7
depends_on: ["30-16", "30-05", "30-10"]
files_modified:
  - web/e2e/family-wide-sharing.spec.ts
autonomous: true
requirements: [FSH-04, FSH-05, FAM-10]

must_haves:
  truths:
    - "The shipped timing-caveat copy is checked against the MEASURED delivery bound this suite itself drove in 30-16 -- not asserted as a string-exists check, and not assumed to match intent."
    - "Leaving the family, being removed, and deleting an account each revoke family-wide access through the existing atomic re-key path -- proven with a positive was-readable anchor before revocation and the same read failing after the NEXT COMPLETED SYNC, never lock/unlock, mirroring remove-member.spec.ts's own proven pattern extended to a family-wide-flagged collection."
    - { statement: "Polish family-wide row/caveat copy (share-family-wide-timing-caveat, sharing-overview-family-wide-caveat) AND the family-rekey-notice toast's own 320px shell all wrap cleanly at real rendered width, rather than overflowing -- measured via scrollWidth<=clientWidth on the live rendered element, not assumed.", verification: backstop }
    - { statement: "A genuine, unrelated decrypt failure on real ciphertext (a deliberately corrupted/foreign collection_keys row, NOT a missing-key grant) continues rendering through the EXISTING undecryptable path and NEVER renders the pending-family-key copy -- driven live, not merely unit-asserted.", verification: backstop }
  artifacts:
    - "web/e2e/family-wide-sharing.spec.ts (SC5 measured-timing case, SC6 revocation case, both backstop falsification cases)"
  key_links:
    - "SC6's revocation case reuses remove-member.spec.ts's exact positive-then-negative pattern (assert readable, trigger removal server-side, drive one real sync cycle -- deliberately reload-free, assert the SAME read now fails), extended to a family-wide-flagged collection instead of a per-person one."
---

<objective>
Close out this phase's remaining live evidence: SC5's copy-matches-measurement bar, SC6's
positive-then-negative revocation proof for a family-wide collection, and the two structural
backstops (long-text wrap under real PL copy; a genuine decrypt failure never mistaken for a
pending grant) that no unit test alone can discharge.

Purpose: the last four unchecked boxes in this phase's own quality gate -- SC5, SC6, and the two
🧪 backstop rows from 30-UI-SPEC.md/the edge-probe table this phase's must_haves carry forward.
Output: the remaining cases in `family-wide-sharing.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-UI-SPEC.md
@web/e2e/remove-member.spec.ts
@web/e2e/family-wide-sharing.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC5 -- copy checked against measurement, not intent</name>
  <files>web/e2e/family-wide-sharing.spec.ts</files>
  <read_first>
    30-16-SUMMARY.md (once it exists) for the actual measured delivery timing 30-16's Task 3/4 drove
    (the invite-carried path's immediacy; the gap-window path's "resolves on the next real sync after
    an already-online keyholder's own unlock" bound); 30-UI-SPEC.md's Copywriting Contract for
    `share.familyWideTimingCaveat`'s exact locked pl/en strings.
  </read_first>
  <action>
    Extend `family-wide-sharing.spec.ts` with a case that, in the SAME sequence as 30-16's Task 4
    (gap-window) scenario, asserts `share-family-wide-timing-caveat` and
    `sharing-overview-family-wide-caveat`'s rendered text matches the LOCKED
    `share.familyWideTimingCaveat` string, AND asserts the sequencing this string claims actually
    happened in this same test run: the pending row was genuinely visible BEFORE the reseal-triggering
    unlock, and the content genuinely resolved only AFTER a real sync following that unlock -- never
    before it. This ties the copy assertion to the SAME live sequence 30-16 drove, rather than
    asserting the string in isolation.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/family-wide-sharing.spec.ts -g "timing copy matches measurement" 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    The case passes headed, live; it fails if the caveat string is edited to claim "instantly" for
    the gap-window case (prove this by temporarily editing the dictionary string and confirming the
    test goes red, then reverting).
  </acceptance_criteria>
  <done>The shipped copy is proven to describe the delivery bound this suite itself measured, not merely to exist.</done>
</task>

<task type="auto">
  <name>Task 2: SC6 -- positive-then-negative revocation for a family-wide collection</name>
  <files>web/e2e/family-wide-sharing.spec.ts</files>
  <read_first>
    `web/e2e/remove-member.spec.ts` in full -- its exact positive-anchor / trigger-removal /
    drive-one-sync-cycle-reload-free / negative-assert structure, and its "next completed sync, not
    lock/unlock" discipline (~line 325's own comment).
  </read_first>
  <action>
    Add three cases mirroring `remove-member.spec.ts`'s proven shape, each targeting the family-wide
    collection from 30-16: (1) member LEAVES the family -- assert the LEAVING member's own originals
    remain readable in their own account (locked decision: "leaving is not deletion"), and that every
    OTHER remaining member's read of the leaver's content, if any existed, is unaffected (family-wide
    revocation on leave affects what the LEAVER shared, per FSH-04's own wording -- assert the correct
    direction). (2) a member is REMOVED by the owner -- assert a positive "was readable" anchor on the
    removed member's OWN session before removal, trigger the real removal via the existing UI/API,
    drive ONE real sync cycle (never a reload) on the removed member's session, assert the SAME read
    now fails. (3) account DELETION (FAM-10) -- same positive-then-negative shape, triggered via the
    real `DeleteAccountDialog` flow. For case 2 or 3 (whichever session remains observable), also
    assert the quiet `family-rekey-notice` toast (30-05) appeared on a REMAINING member's session that
    still holds a re-keyed grant.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/family-wide-sharing.spec.ts -g "revocation" 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    All three cases pass headed, live, with a genuine positive read before and a genuine negative
    read after, on the next completed sync, never a reload standing in for it.
  </acceptance_criteria>
  <done>Leaving, removal, and account deletion each revoke family-wide access through the existing atomic re-key path, proven live with positive-then-negative anchors.</done>
</task>

<task type="auto">
  <name>Task 3: the two structural backstops -- long-text wrap and decrypt-failure-not-pending</name>
  <files>web/e2e/family-wide-sharing.spec.ts</files>
  <read_first>
    30-UI-SPEC.md's "long-text" row in its UI Considerations table (the 400px ShareDialog
    card/320px toast wrap requirement) and its "error G4/G5" row (the pending-vs-genuine-decrypt-
    failure correctness-critical risk) -- both explicitly named 🧪 backstop, requiring exactly the
    two falsification tests this task adds.
  </read_first>
  <action>
    Add a case that opens `ShareDialog` and `SharingOverviewPanel` in the `pl` locale, at their real
    rendered widths, and asserts (via `element.evaluate((el) => el.scrollWidth &lt;=
    el.clientWidth)`, or Playwright's own overflow-detection idiom) that
    `share-family-wide-timing-caveat` and `sharing-overview-family-wide-caveat` never overflow their
    containers. Extend the SAME case to also trigger a real `family-rekey-notice` toast (reuse Task
    2's revocation sequence, or a minimal standalone re-key trigger) and assert its own fixed-320px
    shell does not overflow either -- closing 30-UI-SPEC.md's long-text row's explicit "G8 notice"
    clause, not only the two dialog/panel caveats. Add a SECOND case that deliberately corrupts an unrelated share's ciphertext/key
    server-side (mirroring however this repo's existing `undecryptable`-triggering fixtures already
    do this, e.g. a foreign/stale `collection_keys.sealed_key` row inserted directly against the test
    DB) for an item that is NOT among any `family-wide-pending` `missing` entry, then asserts that
    item renders through the EXISTING `undecryptable-item-banner` path and NEVER renders
    `item-row-pending-family-key`/`pending-family-key-detail` -- the live falsification 30-15's own
    backstop requires, extending 30-15's unit-level regression to a genuine live decrypt failure.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/family-wide-sharing.spec.ts -g "wraps cleanly|decrypt failure" 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    Both cases pass headed, live; the decrypt-failure case fails if 30-15's discriminant is
    (hypothetically) widened to catch any decrypt exception -- prove this by temporarily broadening
    the discriminant check and confirming this test goes red, then reverting.
  </acceptance_criteria>
  <done>Both remaining 🧪 backstops from 30-UI-SPEC.md's UI Considerations table are closed with live falsification tests, not assumptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This suite's own DB-level fixture corruption (Task 3) | A deliberate, test-only corruption of a row to simulate a genuine decrypt failure -- confined to the isolated test DB the checkpoint in 30-16 already confirmed, never touching real data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-24 | Information Disclosure (false reassurance, live) | Pending copy masking a real decrypt failure | high | mitigate | Task 3's live falsification test is the closing proof for T-30-22 (30-15's threat register), exercised against genuine ciphertext corruption rather than a unit-level mock. |

</threat_model>

<verification>
`npx playwright test e2e/family-wide-sharing.spec.ts --retries=0` (full file, all cases from 30-16 and this plan)
</verification>

<success_criteria>
SC5 and SC6 are proven live. Both remaining backstops are closed with falsification tests that are
shown to actually catch the defect they guard against.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-17-SUMMARY.md` when done
</output>
