---
phase: 30-the-living-group-family-wide-sharing
plan: 12
type: execute
wave: 4
depends_on: ["30-08"]
files_modified:
  - web/src/components/vault/ShareDialog.tsx
autonomous: true
requirements: [FSH-01]

must_haves:
  truths:
    - "Selecting Cala rodzina for a single ITEM (not a folder) moves that item into the ONE per-family auto-created item_bucket collection -- lazily creating it on first use, exactly once per family, never a second bucket on a retry or a second sharer's first family-wide item share."
    - "The item variant's family-wide branch reuses the EXISTING decrypt/re-encrypt-under-destination/moveItemToCollection sequence submitFolderVariant's seed-move code already established -- no second, parallel re-encryption implementation."
    - "Finding the existing bucket (rather than creating a duplicate) is order-independent and self-healing: two different family members independently sharing their own first item family-wide both end up moving their item into the SAME bucket, NEVER two -- enforced at the DB level by idx_one_item_bucket_per_family (30-01's partial unique index), not merely by client-side list-then-create ordering. A racing second createCollection(..., 'item_bucket') call fails the constraint; that failure is handled by re-listing and using whichever bucket won, never surfaced to the user as an error."
  artifacts:
    - "web/src/components/vault/ShareDialog.tsx (findOrCreateFamilyItemBucket, submitItemVariant's family-wide branch)"
  key_links:
    - "findOrCreateFamilyItemBucket calls listCollections() first, filters to family_wide_kind === 'item_bucket', and only calls createCollection(..., 'item_bucket') when none is found. A genuine list-then-create race between two members is still POSSIBLE at the client-request level, but 30-01's idx_one_item_bucket_per_family partial unique index makes the SECOND concurrent createCollection call fail server-side (a 409-shaped constraint violation) rather than silently succeeding -- findOrCreateFamilyItemBucket treats that specific failure as 'someone else already won, re-list and use theirs,' turning a would-be race into a guaranteed single winner."
---

<objective>
Complete FSH-01's "or an item" clause: selecting "Cała rodzina" for a single item moves it into the
ONE per-family auto-created "loose family-wide items" collection, reusing the folder variant's
existing decrypt/re-encrypt/move machinery verbatim -- exactly mirroring how the existing folder
variant already auto-creates a fresh collection for a brand-new shared folder.

Purpose: RESEARCH.md's own recommendation -- "route a family-wide item share through the same
collection-scoped path by lazily auto-creating ONE per-family 'loose family-wide items' collection."
Output: `findOrCreateFamilyItemBucket`; `submitItemVariant`'s family-wide branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-RESEARCH.md
@web/src/components/vault/ShareDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: findOrCreateFamilyItemBucket + the item variant's family-wide submit branch</name>
  <files>web/src/components/vault/ShareDialog.tsx</files>
  <read_first>
    `ShareDialog.tsx`'s `submitFolderVariant` (lines 440-568, as already read in 30-08) for the exact
    decrypt/`encryptItemForCollection`/`moveItemToCollection` sequence its seed-move sub-step already
    performs (lines 522-560) -- this task's item-variant branch reuses this EXACT sequence for the
    single selected item, targeting the bucket collection instead of a newly-created folder;
    `submitItemVariant` (lines 402-425, as already read in 30-04's sibling context) -- the existing
    per-recipient `shareItemWithRecipients` direct-share path this branch does NOT use (a
    family-wide item share is collection-scoped, never a direct `item_shares` row);
    `crates/pv-server/migrations/0019_family_wide_sharing.sql` (30-01) for
    `idx_one_item_bucket_per_family`'s exact shape -- a partial unique index on
    `collections(family_id) WHERE family_wide_kind = 'item_bucket'` -- and how `collections.rs::create`
    surfaces a violation of it (mirrors the existing client-minted-id collision handling already in
    `create()`, an `ApiError::Conflict` / 409, never a raw 500).
  </read_first>
  <behavior>
    - Given `listCollections()` already contains a `family_wide_kind === "item_bucket"` collection
      the caller holds a key for: `findOrCreateFamilyItemBucket` returns its id WITHOUT calling
      `createCollection` -- assert via a spy showing `createCollection` was never invoked.
    - Given no such collection exists: `findOrCreateFamilyItemBucket` calls `createCollection(id,
      encName, sealedKeyForSelf, "item_bucket")` once, using a fixed, non-sensitive placeholder
      `encName` (this bucket is never surfaced to the user as a named folder per 30-UI-SPEC.md's own
      "not rendered as a folder row" note -- any deterministic placeholder plaintext is acceptable
      since 30-10's SharingOverviewPanel never renders this collection's own name, only its contained
      items' names).
    - Given `isFamilyWideSelected === true` on the item variant, and the bucket resolves (found or
      created): the selected item is decrypted under the caller's UK, re-encrypted under the bucket's
      Collection Key (AAD bound to the bucket's id + the item's id + the item's NEXT revision,
      mirroring `submitFolderVariant`'s seed-move AAD discipline exactly), moved via
      `moveItemToCollection`, then granted to every current active family member via the SAME
      per-recipient `addCollectionMember` loop 30-08's folder branch already established (extracted
      into a small shared helper if that keeps both call sites from duplicating the loop body, or
      duplicated verbatim if extraction would touch more of the file than this task's scope
      warrants -- either is acceptable, prefer extraction if it costs under ~15 lines of diff).
    - Given the bucket-resolution step races (two members each call `findOrCreateFamilyItemBucket`
      concurrently, both see no existing bucket, both call `createCollection(..., "item_bucket")`):
      exactly ONE `createCollection` call succeeds; the OTHER receives a 409 (30-02's
      `idx_one_item_bucket_per_family`-backed conflict) -- the losing caller catches that specific
      409, re-calls `listCollections()`, and uses the now-visible winning bucket's id instead of
      surfacing an error to the user. Assert this via a mocked `createCollection` that rejects once
      with a structural 409 then a second `listCollections()` call returning the winner's row.
  </behavior>
  <action>
    Add `async function findOrCreateFamilyItemBucket(uk: WasmUserKey, identityKey:
    Awaited&lt;ReturnType&lt;typeof ensureOwnIdentityKeypair&gt;&gt;): Promise&lt;string&gt;`: call
    `listCollections()`, find the first entry with `family_wide_kind === "item_bucket"`; if found,
    return its `id`. Otherwise mint a fresh `crypto.randomUUID()`, generate a new
    `WasmCollectionKey`, encrypt a fixed placeholder name (`JSON.stringify({name:
    "family-wide-items"})`) under it, seal to the caller's own identity key, and call
    `createCollection(id, encName, sealedKeyForSelf, "item_bucket")` inside a `try/catch`: on success,
    return the new id; on a structural 409 (the SAME `isConflictError` shape this file already uses
    elsewhere -- `idx_one_item_bucket_per_family` tripped because another member's concurrent call won
    the race), call `listCollections()` AGAIN and return the now-visible `item_bucket`-kind entry's id
    (never re-throw, never surface this as a share failure to the user -- the bucket exists, this
    caller simply didn't create it). In `submitItemVariant`, add a branch (mirroring `handleSubmit`'s
    existing `scope.kind === "item"` dispatch) that, when `isFamilyWideSelected`, calls
    `findOrCreateFamilyItemBucket`, then performs the decrypt/re-encrypt/move/grant sequence described
    in the behavior bullets above, using `getFamilyMembers()`'s current active roster (excluding the
    caller) as the grant recipient set -- the SAME recipient-resolution rule 30-08's folder branch
    uses, INCLUDING that rule's keyless-member handling: a current member with no published public
    key is OMITTED from this creation-time grant (never thrown on, never aborts the share) and
    resolves later via the same missing/lazy-reseal path once they publish a key -- reuse 30-08's
    shared filtering helper if one was extracted there, rather than re-deriving this rule.
  </action>
  <verify>
    <automated>set -o pipefail; cd web && npx vitest run src/components/vault/ShareDialog.test.tsx -t "family-wide item" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    All four behavior bullets pass, including the race-loser-recovers-via-relist case; the existing
    non-family-wide `submitItemVariant` (direct-share-to-specific-people) path remains green
    unmodified.
  </acceptance_criteria>
  <done>Selecting family-wide for a single item moves it into the one, DB-enforced, per-family bucket collection, reusing the existing folder-variant's re-encryption machinery, and grants every current active member.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Item ciphertext re-encryption under the bucket's key | Same client-side-only boundary as every other item move/share in this file -- no new server trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-20 | Denial of Service (duplicate buckets) | Concurrent first-use bucket creation race | low | mitigate | `idx_one_item_bucket_per_family` (30-01, a partial unique index on `collections(family_id) WHERE family_wide_kind='item_bucket'`) makes a second concurrent create fail server-side rather than silently succeed; `findOrCreateFamilyItemBucket` treats that specific 409 as "someone else won, re-list and use theirs," so the race resolves to exactly one bucket, never two, and never a user-visible error. |

</threat_model>

<verification>
`npx vitest run src/components/vault/ShareDialog.test.tsx`
</verification>

<success_criteria>
A bare item can be shared family-wide, landing in the one, DB-enforced, per-family bucket collection,
reusing the existing re-encryption machinery -- a concurrent first-use race resolves to exactly one
bucket, proven, not merely likely.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-12-SUMMARY.md` when done
</output>
