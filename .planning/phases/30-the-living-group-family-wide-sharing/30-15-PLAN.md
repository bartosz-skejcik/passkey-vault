---
phase: 30-the-living-group-family-wide-sharing
plan: 15
type: execute
wave: 5
depends_on: ["30-06", "30-11", "30-13"]
files_modified:
  - packages/pv-ui/vault/types.ts
  - web/src/lib/vault/store.ts
  - web/src/components/vault/ItemRow.tsx
  - web/src/components/vault/DetailPanel.tsx
  - web/src/lib/i18n/dictionary.ts
autonomous: true
requirements: [FSH-02, FSH-05]

must_haves:
  truths:
    - "A newcomer with a pending family-wide grant sees an explicit, generic, calm pending row (vault.pendingFamilyKeyItemName / vault.pendingFamilyKeyRow) merged into the flat item list -- never a failed-to-decrypt row, and never a silently absent item."
    - "A newcomer who joins when ZERO family-wide shares exist anywhere in the family gets no synthetic pending row and no grant -- familyWidePending's missing snapshot is genuinely empty, and the merge step appends nothing; the absence is correct, not a failure -- FSH-02 empty (edge-probe)."
    - "The pending row's trailing metadata slot (relative time / TOTP ring) is omitted entirely, not populated with a stale or fake value -- the ids-only discovery response carries no updated_at to show there."
    - "Once the key resolves (the next completed sync), the synthetic pending row is replaced by the real item row(s) with no transition animation and no confirming toast -- quiet arrival, matching the re-key notice's own low-fanfare tone."
    - "The pending-family-key detection is checked BEFORE item.undecryptable, using its own independently-sourced discriminant (a synthetic id derived from the discovery endpoint's missing list) -- it is never a decrypt-exception catch-all, and a genuine unrelated decrypt failure keeps rendering through the EXISTING undecryptable path unchanged."
    - { statement: "The pending copy must render ONLY for a positively-identified missing-key grant and must NEVER become a catch-all for an unexplained decrypt exception on real ciphertext.", verification: backstop }
  artifacts:
    - "packages/pv-ui/vault/types.ts (VaultItem.pendingFamilyKey discriminant, or an equivalent synthetic-row marker)"
    - "web/src/lib/vault/store.ts (merges familyWidePending.missing into synthetic pending VaultItem entries, keyed distinctly from real item ids)"
    - "web/src/components/vault/ItemRow.tsx (item-row-pending-family-key anatomy)"
    - "web/src/components/vault/DetailPanel.tsx (pending-family-key-detail note)"
  key_links:
    - "Each synthetic pending row's id is prefixed (e.g. pending-family-key:{collectionId}) so it can never collide with, or be mistaken for, a real vault_items.id -- and is excluded from delete/edit/copy actions in ItemContextMenu by construction (no real item exists to act on yet)."
  prohibitions:
    - statement: "item.undecryptable's existing meaning MUST NOT be overloaded to also mean pending-family-key -- these are two independently-checked conditions, never one flag with two meanings."
      status: "enforced by using a distinct field/synthetic-id scheme rather than reusing undecryptable."
      verification: "component test in this plan asserts a genuine undecryptable item (a prior successful decrypt exists) never renders the pending anatomy, and vice versa."
---

<objective>
Render the recipient-side half of FSH-02's honesty contract: a newcomer whose key hasn't arrived
yet sees an explicit, calm, generic pending placeholder -- one synthetic row per missing family-wide
grant, merged into the flat item list and mirrored in the detail panel -- built entirely from the
discovery endpoint's ids-only response, never from a decrypt failure.

Purpose: the inverse of DEBT-03's phantom-row defect (silently keeping a stale row) -- this is
silently DROPPING a row's existence being the failure this locked decision exists to prevent.
Output: the pending item-row and detail-panel states per 30-UI-SPEC.md's exact contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-UI-SPEC.md
@packages/pv-ui/vault/types.ts
@web/src/lib/vault/store.ts
@web/src/components/vault/ItemRow.tsx
@web/src/components/vault/DetailPanel.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: synthetic pending VaultItem rows in the item list</name>
  <files>packages/pv-ui/vault/types.ts, web/src/lib/vault/store.ts, web/src/components/vault/ItemRow.tsx</files>
  <read_first>
    `packages/pv-ui/vault/types.ts` lines 126-221 (`VaultItem`'s existing optional metadata fields --
    `undecryptable`'s own doc comment explicitly distinguishes "a prior successful decrypt exists" from
    what this task adds, which has NO prior successful decrypt ever); `web/src/lib/vault/store.ts`'s
    item-list merge path (wherever `applySyncSnapshot`/the shared-items merge builds the final
    `items` array consumers read) -- this task's synthetic rows are injected alongside real rows in
    that SAME final array, never a separate parallel list; `ItemRow.tsx` lines 90-169 (the existing
    marker-slot conditional chain from 30-11, and `ItemIconTile`'s call site at line 128) --
    30-UI-SPEC.md's "Pending-newcomer item-row state" section for the exact markup
    (`item-row-pending-family-key`, generic `Clock` icon tile, no marker-slot rendering at all for a
    pending row).
  </read_first>
  <behavior>
    - Given `familyWidePending`'s `missing` snapshot has one entry `{collection_id: "c9", kind:
      "folder"}`, and the caller has no real item/collection with that id decryptable: the merged item
      list contains exactly one synthetic row with a stable, collision-proof id (e.g.
      `pending-family-key:c9`), `fields.name` unset/generic (never derived from any real data), no
      `updatedAt`/`lastUsedAt`.
    - Given the SAME `collection_id` later disappears from `missing` (the key arrived, a real
      `collection_keys` row now exists) on a subsequent sync: the synthetic row is removed from the
      merged list on that SAME merge pass -- no stale synthetic row survives past the sync cycle that
      resolved it.
    - Given a REAL item is genuinely `undecryptable: true` (a prior successful decrypt exists, current
      merge failed) and is NOT referenced by any `missing` entry: it renders through the EXISTING
      `undecryptable-item-banner`/retained-content path, completely unaffected by this task's new
      logic -- assert this via a regression test on an existing `undecryptable` fixture.
    - `ItemRow` renders the pending anatomy (`Clock` icon in an `h-8 w-8 rounded-field bg-base-200
      text-base-content/40` tile, `vault.pendingFamilyKeyItemName`/`vault.pendingFamilyKeyRow` two-line
      text, NO marker slot content, NO trailing metadata slot) ONLY for a row carrying this task's
      pending discriminant -- checked BEFORE the existing `undecryptable`/`sharedToMe`/`isShared`
      branches, never falling through to them.
  </behavior>
  <action>
    Add a discriminant to `VaultItem` distinguishing a synthetic pending-family-key row from a real
    item -- either a dedicated `pendingFamilyKey?: boolean` field set ONLY by this task's own merge
    code, or (equivalently, pick whichever is a smaller diff against the existing merge function's
    shape) a reserved id prefix the merge function recognizes on its own synthetic output. In
    `store.ts`'s final item-list assembly, after the existing personal/shared merges, read
    `getFamilyWidePendingSnapshot().missing` (30-06) and, for each entry whose synthetic id is not
    already represented by a REAL decrypted row in the list, append a synthetic `VaultItem` with the
    pending discriminant set and no other metadata fabricated. Remove any previously-injected synthetic
    row whose backing `missing` entry is no longer present. In `ItemRow.tsx`, add a top-of-render
    branch (before `primaryText`/`subtitle` are even computed) that, when the pending discriminant is
    set, renders 30-UI-SPEC.md's exact pending anatomy and returns early -- never reaching
    `ItemIconTile`, the marker-slot conditional, or the trailing metadata slot's existing logic.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/vault/store.test.ts src/components/vault/ItemRow.test.tsx -t "pending" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>All four behavior bullets pass, including the explicit undecryptable-is-unaffected regression.</acceptance_criteria>
  <done>A pending family-wide grant renders as an honest, generic, self-resolving placeholder row, structurally distinct from a genuine decrypt failure.</done>
</task>

<task type="auto">
  <name>Task 2: pending-family-key detail-panel note</name>
  <files>web/src/components/vault/DetailPanel.tsx, web/src/lib/i18n/dictionary.ts</files>
  <read_first>
    `DetailPanel.tsx` lines 440-465 (the existing `undecryptable`/`item-shared-with-you-note`
    conditional chain -- this task's branch is checked FIRST, before either, per 30-UI-SPEC.md's own
    "check the pending-family-key condition before any decrypt-dependent rendering" requirement);
    30-UI-SPEC.md's "Pending-newcomer detail-panel state" section verbatim for the exact markup
    (`pending-family-key-detail`, `role="status" aria-live="polite"`, never `role="alert"`).
  </read_first>
  <action>
    Add a branch at the TOP of `DetailPanel`'s existing status-note conditional chain (before the
    `item.undecryptable === true` check): when the currently-selected item carries this plan's pending
    discriminant, render `&lt;div data-testid="pending-family-key-detail" role="status"
    aria-live="polite"&gt;` with two lines (`share.pendingFamilyKeyNote` /
    `share.pendingFamilyKeyNoteDetail`, `data-testid="pending-family-key-detail-explanation"` on the
    second), matching `item-shared-with-you-note`'s exact `flex flex-col gap-1 text-sm
    text-base-content/70` shell. Add `vault.pendingFamilyKeyItemName`, `vault.pendingFamilyKeyRow`,
    `share.pendingFamilyKeyNote`, `share.pendingFamilyKeyNoteDetail` to `dictionary.ts` with the exact
    pl/en copy from 30-UI-SPEC.md's Copywriting Contract.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/vault/DetailPanel.test.tsx -t "pending" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    A fixture with the pending discriminant set renders `pending-family-key-detail` with
    `role="status"` (never `role="alert"`) and BOTH text lines, and does NOT render the
    `undecryptable-item-banner` or `item-shared-with-you-note` elements even if those conditions would
    otherwise also be true on the same fixture.
  </acceptance_criteria>
  <done>The detail panel explains a pending family-wide grant calmly, checked ahead of every other status-note branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discovery-endpoint-driven UI state | The pending discriminant is sourced ENTIRELY from 30-02's ids-only discovery response -- never from a decrypt exception, which is exactly what keeps this from becoming a false-reassurance surface over a genuine security-relevant failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-22 | Information Disclosure (false reassurance) | Pending state mistaken for a decrypt-failure catch-all | high | mitigate | The pending discriminant is set ONLY from the discovery endpoint's positive `missing` list, never from a caught decrypt exception -- Task 1's regression test proves a genuine `undecryptable` item is unaffected; 30-16's e2e suite adds the live falsification proof this backstop requires. |

</threat_model>

<verification>
`npx vitest run src/lib/vault/store.test.ts src/components/vault/ItemRow.test.tsx src/components/vault/DetailPanel.test.tsx`
</verification>

<success_criteria>
A newcomer's pending grant is visible, honest, generic, and self-resolving; it is structurally
incapable of masking a genuine decrypt failure.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-15-SUMMARY.md` when done
</output>
