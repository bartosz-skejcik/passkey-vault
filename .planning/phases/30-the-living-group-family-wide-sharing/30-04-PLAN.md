---
phase: 30-the-living-group-family-wide-sharing
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - web/src/lib/families/reseal.ts
  - web/src/lib/families/reseal.real-wasm.test.ts
autonomous: true
requirements: [FSH-02, FSH-03]

must_haves:
  truths:
    - "reshareCollectionToNewMember unwraps the caller's OWN sealed Collection Key and reseals the SAME key (never a freshly-generated one) to exactly one new recipient's real published public key, proven through a real-WASM round trip -- never the mocked @/lib/crypto lane."
    - "A recipient with no published public key causes the function to throw before any network call, mirroring buildMemberRemovalBatch's T-25-16 discipline -- never a silently skipped grant."
    - "A 409 from addCollectionMember (the recipient already holds a grant, a race with another resealer) is treated as success, not an error -- the composition is idempotent by construction, matching the server's ON CONFLICT DO NOTHING idiom."
  artifacts:
    - "web/src/lib/families/reseal.ts (reshareCollectionToNewMember)"
    - "web/src/lib/families/reseal.real-wasm.test.ts"
  key_links:
    - "reshareCollectionToNewMember calls unsealCollectionKey (never rotates via WasmCollectionKey.generate()) then sealCollectionKey to the new recipient, then addCollectionMember -- the EXISTING member-add endpoint, no new server surface."
    - "The real-WASM test mirrors rekey.real-wasm.test.ts's beforeAll wiring (stub only global.fetch for the wasm binary, never @/lib/crypto itself)."
  prohibitions:
    - statement: "The server MUST NOT persist or receive a Collection Key, a private key, or plaintext anywhere on this path."
      status: "reshareCollectionToNewMember's only network call is addCollectionMember, posting the same opaque sealed_key shape the existing folder-share flow already posts."
      verification: "proven by this plan's real-WASM test (round-trips through genuine unseal/seal, never a mock) and reused by 30-14's adversarial server-side test."
---

<objective>
Build the ONE genuinely new client-side crypto composition FSH-02 needs: unwrap a Collection Key
the caller already holds, reseal the SAME key (no rotation) to exactly one new recipient, and grant
it via the existing `add_member` endpoint. RESEARCH.md names this the exact composition ORG-03/MOD-02
will reuse verbatim in Phase 31 -- this phase is its first required consumer.

Purpose: this is the lazy-reseal fallback's crypto engine -- Wave 4's reseal-trigger plan (30-12)
calls this function once per pending grant it discovers.
Output: `reshareCollectionToNewMember`, proven through a real-WASM round trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-DECISION-FSH-02.md
@web/src/lib/families/rekey.ts
@web/src/lib/families/rekey.real-wasm.test.ts
@web/src/lib/invite/crypto.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: reshareCollectionToNewMember -- unwrap-own-key, reseal-to-one-recipient, no rotation</name>
  <files>web/src/lib/families/reseal.ts</files>
  <read_first>
    `web/src/lib/families/rekey.ts` lines 1-141 in full (the module doc comment's "mirrors
    lib/invite/crypto.ts's exact shape" precedent, `buildMemberRemovalBatch`'s unwrap/seal loop and
    its T-25-16 no-silent-drop discipline -- this task's function must NOT rotate the key, unlike
    `buildMemberRemovalBatch`'s `WasmCollectionKey.generate()`); `web/src/lib/invite/crypto.ts` lines
    94-101 (the unwrap-own-sealed-key pattern, cited verbatim in RESEARCH.md's "Code Examples"
    section) and its try/finally `.free?.()` discipline on every WASM handle.
  </read_first>
  <behavior>
    - Given a collection the caller holds a decryptable `sealed_key` for, and a recipient with a
      published public key: calls `unsealCollectionKey` once, `sealCollectionKey` once (to the
      recipient's key, the SAME unwrapped Collection Key -- assert via a real-WASM round trip that
      the recipient's resulting sealed blob decrypts to the IDENTICAL key bytes the caller unwrapped,
      not a freshly-generated one), then `addCollectionMember(collectionId, recipientUserId,
      sealedKey, accessLevel)`.
    - Given a recipient with `public_key: null`: throws before calling `getCollection`/`addCollectionMember`
      at all (T-25-16 discipline) -- assert via a spy that neither is called.
    - Given `addCollectionMember` rejects with a structural 409 (mirrors `isConflictError` in
      `ShareDialog.tsx`/`rekey.ts`): resolves normally (does not throw) -- the grant already exists,
      which is exactly the state the caller wants.
    - Given the caller has `sealed_key: null` for the target collection (no access): throws before
      any `sealCollectionKey`/network call.
    - Every WASM handle (`identityKey`, unwrapped `WasmCollectionKey`, recipient
      `WasmIdentityPublicKey`) is freed in a `finally` block, mirroring `rekey.ts`'s discipline
      exactly -- assert via a spy on each handle's `.free` in a unit-level test (not the real-WASM
      test).
  </behavior>
  <action>
    Create `web/src/lib/families/reseal.ts` exporting `async function reshareCollectionToNewMember(
    collectionId: string, newRecipientUserId: string, accessLevel: string, ownUk: WasmUserKey ):
    Promise&lt;void&gt;`. Mirror `rekey.ts`'s shape: `initCrypto()`, `ensureOwnIdentityKeypair(ownUk)`,
    `getCollection(collectionId)` -&gt; throw if `sealed_key === null`, `unsealCollectionKey(identityKey,
    collectionRecord.sealed_key)`, resolve the recipient's public key via `getFamilyMembers()` (throw
    per T-25-16 if absent), `sealCollectionKey(recipientPk, ck)` -- deliberately the SAME `ck`, never
    `WasmCollectionKey.generate()` -- then `addCollectionMember(collectionId, newRecipientUserId,
    sealedKey, accessLevel)`, catching a structural 409 as success (reuse or duplicate
    `ShareDialog.tsx`'s local `isConflictError` shape, matching this codebase's established
    per-file-owns-its-own-tiny-helper convention). Free every WASM handle in a `finally` block.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/families/reseal.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    All five behavior bullets pass as vitest cases against the mocked `@/lib/crypto` lane (fast,
    logic-only); no `@/lib/crypto` import is left unmocked in this unit test file.
  </acceptance_criteria>
  <done>reshareCollectionToNewMember exists, never rotates the key, throws on a missing recipient public key before any network call, and treats a 409 as success.</done>
</task>

<task type="auto">
  <name>Task 2: real-WASM proof -- the mechanism itself, never mocked</name>
  <files>web/src/lib/families/reseal.real-wasm.test.ts</files>
  <read_first>
    `web/src/lib/families/rekey.real-wasm.test.ts` in full -- its exact `beforeAll` wiring (stubs
    only `global.fetch` for the wasm binary itself, never `@/lib/crypto`), its two-real-identity
    fixture setup, and its assertion style (decrypt the resulting sealed blob back down and compare
    bytes, not merely "did not throw").
  </read_first>
  <action>
    Create `web/src/lib/families/reseal.real-wasm.test.ts` mirroring `rekey.real-wasm.test.ts`'s
    `beforeAll` wiring exactly. Build a real Collection Key, seal it to a real "caller" identity
    keypair (simulating an existing `collection_keys` row), stub `getCollection`/`getFamilyMembers`/
    `addCollectionMember` (network layer only, never crypto) to return that sealed blob and a second
    real "recipient" identity's public key, call `reshareCollectionToNewMember`, and assert: the
    `sealedKey` argument `addCollectionMember` was called with, when unsealed with the RECIPIENT's own
    real identity secret key, decrypts to byte-identical Collection Key material as the ORIGINAL key
    the caller unwrapped -- this is the genuine SC4 real-WASM proof that the composition preserves the
    key rather than fabricating or corrupting it. Add a second real-WASM case: the caller's own
    identity key and the collection's sealed_key are read from real `pv-wasm` calls end to end (no
    intermediate mock of the unseal/seal steps themselves), matching the file-naming convention
    `*.real-wasm.test.ts` this codebase already uses to mark "this test proves the crypto, not the
    UI wiring."
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/families/reseal.real-wasm.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    The real-WASM test suite is green, exercises genuine `unsealCollectionKey`/`sealCollectionKey`
    calls (no `vi.mock("@/lib/crypto")` anywhere in this file), and its core assertion is a
    byte-for-byte key-material comparison after a real unseal on the recipient side -- not merely
    "the function did not throw."
  </acceptance_criteria>
  <done>The composition is proven to preserve the exact Collection Key end to end through real WASM, not a mock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Any current keyholder's browser -> pv-server | The client unwraps its own key and reseals it entirely locally; the server only ever receives the finished opaque blob via the existing add_member endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-10 | Information Disclosure | `reshareCollectionToNewMember` | critical | mitigate | The unwrapped Collection Key never leaves the function's own scope -- it is sealed to the recipient's public key in-memory and only the resulting opaque blob crosses the network; proven by the real-WASM test's byte-comparison assertion, not merely by code inspection. |
| T-30-11 | Tampering | Recipient public key resolution | medium | mitigate | The recipient's public key comes from `getFamilyMembers()`, the same server-attested roster every other sharing composition (`ShareDialog.tsx`, `rekey.ts`) already trusts -- no new trust source introduced. |

</threat_model>

<verification>
`npx vitest run src/lib/families/reseal.test.ts src/lib/families/reseal.real-wasm.test.ts`
</verification>

<success_criteria>
The reseal composition exists, is unit-tested against the mocked lane for logic, and is proven
through real WASM for the crypto claim -- the Non-Negotiable #2 bar this whole phase is held to.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-04-SUMMARY.md` when done
</output>
