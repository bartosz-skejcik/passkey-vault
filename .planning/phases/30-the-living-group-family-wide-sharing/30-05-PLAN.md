---
phase: 30-the-living-group-family-wide-sharing
plan: 05
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - web/src/lib/families/rekey.test.ts
  - web/src/lib/vault/collections.ts
  - web/src/components/vault/FamilyRekeyNotice.tsx
  - web/src/lib/i18n/dictionary.ts
autonomous: true
requirements: [FSH-04, FAM-10]

must_haves:
  truths:
    - "A family-wide collection flows through buildMemberRemovalBatch exactly like an ordinary shared collection, with zero special-casing -- proven by a targeted regression test, not by inspection of already-shipped code."
    - "DeleteAccountDialog's existing buildMemberRemovalBatch(selfUserId, uk) call already includes family-wide collections for free once they are ordinary collections rows -- confirmed, not re-implemented."
    - "When the collections store detects an already-known collection's sealed_key has changed on refresh (a sign a re-key just ran), it fires a quiet, singleton, non-auto-hiding toast naming no specific item -- never for a BRAND-NEW collection grant, which is not a re-key."
  artifacts:
    - "web/src/lib/families/rekey.test.ts (new: family-wide regression coverage)"
    - "web/src/lib/vault/collections.ts (sealed_key-change detection, exported subscribe hook)"
    - "web/src/components/vault/FamilyRekeyNotice.tsx (new toast component, testids family-rekey-notice / family-rekey-notice-dismiss)"
  key_links:
    - "collections.ts's refreshCollections diffs the PREVIOUS in-memory sealed_key against the NEW server value per already-known collection id (never for an id not previously present) and notifies a dedicated rekey-notice callback, distinct from the existing collections-changed listener set."
  prohibitions:
    - statement: "No per-person access override may be layered on a family-wide share in this phase."
      status: "not touched by this plan -- recorded here since rekey.ts's remaining-recipient fan-out already applies uniformly; no per-person carve-out is added."
      verification: "manual -- code review of this plan's diff."
---

<objective>
Close FAM-10/FSH-04 with proof, not new mechanism: confirm (via a targeted regression test) that a
family-wide collection needs zero special-casing to flow through v0.4's existing atomic re-key path,
and add the one genuinely new piece of UI CONTEXT.md locks -- a quiet notice telling a member their
shared content was just re-keyed because the family's membership changed.

Purpose: RESEARCH.md's own finding is that FAM-10's re-key PLUMBING is already shipped
(`DeleteAccountDialog.tsx:151-156`); this plan proves it holds for family-wide collections and adds
the honesty-owed notice CONTEXT.md's "the sharer is told, quietly" decision requires.
Output: `rekey.test.ts`'s new family-wide case; a sealed_key-change detector in `collections.ts`; the
`FamilyRekeyNotice` toast.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-UI-SPEC.md
@web/src/lib/families/rekey.ts
@web/src/lib/vault/collections.ts
@web/src/components/vault/CopyToast.tsx
@web/src/components/settings/DeleteAccountDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FAM-10 regression -- family-wide collection flows through buildMemberRemovalBatch</name>
  <files>web/src/lib/families/rekey.test.ts</files>
  <read_first>
    `web/src/lib/families/rekey.ts` lines 47-126 (`buildMemberRemovalBatch`'s own loop over
    `access.collections` -- note it reads NOTHING about `family_wide_kind`, confirming RESEARCH.md's
    "zero modification needed" finding); `web/src/components/settings/DeleteAccountDialog.tsx` lines
    132-160 (`handleFinalConfirm`'s existing `branch === "member"` call to
    `buildMemberRemovalBatch(selfUserId, uk)`).
  </read_first>
  <behavior>
    - Given `getMemberAccess` returns one collection among `access.collections` that is (per the
      fixture's own bookkeeping, not any field this function reads) a family-wide collection, and
      `getFamilyMembers`/`getCollection`/`getCollectionAccessList`/`getCollectionItems` are mocked to
      answer for it exactly like any other shared collection: `buildMemberRemovalBatch` produces a
      `CollectionRekeyBatch` entry for it, with a fresh generated key sealed to every remaining
      recipient -- identical shape to an ordinary collection's entry in the same batch.
    - Given TWO collections in `access.collections`, one ordinary and one family-wide: both appear
      in the returned batch array, in the same order `access.collections` supplied them, with no
      differing treatment.
  </behavior>
  <action>
    Create `web/src/lib/families/rekey.test.ts` (this codebase's own mocked-`@/lib/crypto` unit-test
    lane is acceptable here per RESEARCH.md's own test map -- pure batch-construction logic, no
    crypto assertion). Mock `@/lib/crypto`, `@/lib/identity/ensure`, `@/lib/vault/api`,
    `./api` the same way this codebase's other `rekey`-adjacent tests already do (mirror
    `ShareDialog.tsx`'s own test file's mock shape if one exists, or wire fresh mocks following
    `rekey.real-wasm.test.ts`'s fixture-building style but WITHOUT invoking real WASM). Assert both
    behavior bullets above.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/families/rekey.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    Both behavior bullets pass; the test file imports zero new production code (confirms
    `buildMemberRemovalBatch` needed no change, per this task's own read_first finding).
  </acceptance_criteria>
  <done>A family-wide collection is proven, by test, to flow through the existing removal/deletion re-key batch unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: quiet re-key notice -- sealed_key-change detection + toast</name>
  <files>web/src/lib/vault/collections.ts, web/src/components/vault/FamilyRekeyNotice.tsx, web/src/lib/i18n/dictionary.ts</files>
  <read_first>
    `web/src/lib/vault/collections.ts` lines 1-160 (`Collection` interface, module-private
    `collections` array, `refreshCollections`, `notifyListeners`) -- the diff must compare the
    PREVIOUS `collections` array against the freshly-fetched one BEFORE reassigning the module
    variable; `web/src/components/vault/CopyToast.tsx` in full (the exact visual shell and
    singleton-replace-not-stack behavior 30-UI-SPEC.md's Re-Key Notice Contract mandates reusing);
    30-UI-SPEC.md's "Re-Key Notice Contract" section for the exact markup/testids/copy keys
    (`family-rekey-notice`, `family-rekey-notice-dismiss`, `share.familyRekeyNotice`,
    `aria.dismissToast`).
  </read_first>
  <behavior>
    - Given a collection id already present in the in-memory store with `sealed_key: "A"`, and a
      refresh returns the SAME id with `sealed_key: "B"`: the rekey-notice callback fires exactly
      once with that collection's id.
    - Given a refresh returns a collection id NOT previously present in the store (a brand-new
      grant, e.g. this session's own reseal-trigger landing a family-wide grant): the callback does
      NOT fire -- a new grant is not a re-key.
    - Given two refreshes in quick succession both detect a changed sealed_key (two different
      collections): the toast component shows exactly one notice at a time, the second REPLACING the
      first's content, never stacking -- mirrors `CopyToast`'s own documented singleton behavior.
  </behavior>
  <action>
    In `collections.ts`, before `refreshCollections` reassigns the module-private `collections`
    array, build a `Map` of the OLD `id -&gt; sealed_key` values; after fetching the new list, for each
    NEW row whose id existed in the old map AND whose `sealed_key` differs, invoke a new exported
    `onCollectionRekeyed` callback registry (a small `Set&lt;() =&gt; void&gt;`-style subscribe function
    mirroring `subscribeCollections`'s own shape, but firing per rekey event rather than on every
    refresh). Do not fire for ids absent from the old map. Create
    `web/src/components/vault/FamilyRekeyNotice.tsx` per 30-UI-SPEC.md's Re-Key Notice Contract
    exactly (reuses `CopyToast.tsx`'s `toast toast-end toast-bottom` shell, `320px`,
    `rounded-field border border-base-300 bg-base-100 p-3 text-sm`, no auto-hide timer, singleton
    replace-not-stack state, dismiss button reusing `aria.dismissToast`). Add the new i18n key
    `share.familyRekeyNotice` (pl/en copy from 30-UI-SPEC.md's Copywriting Contract) to
    `dictionary.ts`. Mount `FamilyRekeyNotice` once near this app's existing toast-mounting root
    (wherever `CopyToast` itself is mounted), subscribing to the new `onCollectionRekeyed` registry.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/vault/collections.test.ts src/components/vault/FamilyRekeyNotice.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    All three behavior bullets pass; `FamilyRekeyNotice.tsx` renders `data-testid="family-rekey-notice"`
    with `role="status" aria-live="polite"` (never `role="alert"`) and no `warning`/`error` DaisyUI
    class anywhere in its markup.
  </acceptance_criteria>
  <done>A member whose shared content was silently re-keyed by v0.4's existing removal/leave/deletion path now sees a quiet, honest, singleton notice -- never a surprise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collections.ts refresh -> UI notice | A purely client-local detection (comparing two in-memory snapshots); no new server trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-12 | Information Disclosure | FamilyRekeyNotice copy | low | accept | Deliberately generic, names no item/collection -- a re-key batch may touch several items at once and naming one would misrepresent scope, per the locked CONTEXT.md decision and 30-UI-SPEC.md's Copywriting Contract. |

</threat_model>

<verification>
`npx vitest run src/lib/families/rekey.test.ts src/lib/vault/collections.test.ts src/components/vault/FamilyRekeyNotice.test.tsx`
</verification>

<success_criteria>
FAM-10 is proven closed by regression test, not assumed. The quiet re-key notice exists, is honest,
generic, and never fires on a brand-new grant.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-05-SUMMARY.md` when done
</output>
