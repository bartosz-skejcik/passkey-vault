---
phase: 30-the-living-group-family-wide-sharing
plan: 16
type: execute
wave: 6
depends_on: ["30-04", "30-06", "30-07", "30-08", "30-09", "30-12", "30-13", "30-15"]
files_modified:
  - web/e2e/fixtures.ts
  - web/e2e/family-wide-sharing.spec.ts
autonomous: false
requirements: [FSH-01, FSH-02, FSH-03]

must_haves:
  truths:
    - "A folder shared via the Cala rodzina row is opened and its actual decrypted content read by a SECOND real account's own browser session, asserted positively on decrypted field values -- never on row presence alone (SC2)."
    - "A THIRD real account, joining via a freshly-generated invite issued AFTER the family-wide share was created, decrypts the same real content through its own client, on its own first sync -- proving the invite-carried delivery path live (SC3)."
    - { statement: "A FOURTH real account redeems an invite generated BEFORE the family-wide share existed (the gap window), then an already-online keyholder's own subsequent app usage triggers the lazy-reseal fallback, and the fourth account's client resolves the real decrypted content on its OWN next sync -- proving the lazy-reseal path live, distinct from the invite-carried path SC3's other case already proves.", verification: backstop }
    - { statement: "FSH-02 adjacency (edge-probe): a newcomer redeeming an invite at the SAME moment a family-wide share is being created must end up with access by one path or the other, never neither. This is the identical seam Task 4's gap-window sequence exercises -- 30-DECISION-FSH-02.md's mechanism fixes an invite's family-wide payload at GENERATION time only, so a share created after generation reaches the invitee via lazy reseal regardless of how tightly redemption and share-creation are interleaved; Task 4's sequential drive of that same seam is the falsification test this backstop requires, not a separate simultaneous-click harness.", verification: backstop }
  artifacts:
    - "web/e2e/fixtures.ts (a third/fourth idempotent session helper mirroring ensureFamilyOwnerSession's shape)"
    - "web/e2e/family-wide-sharing.spec.ts (SC2, SC3-invite-carried, SC3/FSH-02-gap-window-lazy-reseal cases)"
  key_links:
    - "The gap-window case's assertion never uses a page reload to force a re-check -- it drives one real sync cycle (the existing poll interval, or a manual trigger of the already-online keyholder's own unlock/hydrate) exactly as remove-member.spec.ts's own 'deliberately reload-free' comment establishes as this suite's correct pattern."
  prohibitions:
    - statement: "This suite must run against an isolated test server, never the developer's real data/pv.db (web/playwright.config.ts:128's reuseExistingServer hazard)."
      status: "must be verified before generating live invite/redeem traffic -- carried forward from STATE.md's own recorded hazard."
      verification: "manual -- `lsof -i :8620` before the run, per STATE.md's own documented mitigation."
---

<objective>
Prove the living-group claim live: (1) every current member reads a family-wide share positively,
recipient-side, on real decrypted content; (2) a late joiner via a FRESH invite reads it immediately;
(3) a late joiner via an invite generated BEFORE the share existed reads it once ANY current
keyholder's own session next runs -- the gap-window case no existing spec covers, and this phase's
single most important live proof.

Purpose: this is SC2 and SC3's own evidence bar -- live, recipient-side, positive, on decrypted
content, never on row presence.
Output: `family-wide-sharing.spec.ts`'s SC2/SC3 cases; a third/fourth account fixture helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-RESEARCH.md
@.planning/phases/30-the-living-group-family-wide-sharing/30-UI-SPEC.md
@web/e2e/fixtures.ts
@web/e2e/invite-flow.spec.ts
@web/e2e/remove-member.spec.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: confirm isolated test server before generating live traffic</name>
  <what-built>
    Nothing yet -- this is a pre-flight environment check before this plan generates live
    invite/redeem/account traffic.
  </what-built>
  <how-to-verify>
    Run `lsof -i :8620` on the machine that will execute this plan's Playwright suite. If ANYTHING is
    already listening (a stray local `pv-server`), stop it or ensure `web/playwright.config.ts`'s
    `webServer` config spins up its own isolated instance against a throwaway DB
    (`reuseExistingServer: false`, matching STATE.md's own recorded Phase 28 hazard and mitigation) --
    never run this suite's live account-creation traffic against a developer's real `data/pv.db`.
  </how-to-verify>
  <resume-signal>Type "approved" once the server isolation is confirmed, or describe what needs fixing first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: fixtures -- a third/fourth idempotent session helper</name>
  <files>web/e2e/fixtures.ts</files>
  <read_first>
    `fixtures.ts` lines 148-211 (`FAMILY_OWNER_EMAIL`/`ensureFamilyOwnerSession`'s exact
    register-or-login-idempotent shape) and lines 96-146 (`newBareContext`/`createSession`'s
    unique-per-call email pattern) -- this task's new helper mirrors `ensureFamilyOwnerSession`'s
    shape (a FIXED, reconstructible identity any spec file's turn can re-establish) rather than
    `createSession`'s unique-per-call shape, since this plan's own multi-step scenario needs to name
    "member C" and "member D" as stable identities across several `test()` blocks in one file.
  </read_first>
  <action>
    Add `ensureFamilyMemberCSession(page: Page)`/`ensureFamilyMemberDSession(page: Page)` (or a single
    parameterized `ensureNamedFamilySession(page, email, password)` helper), following
    `ensureFamilyOwnerSession`'s exact register-then-fall-back-to-login-then-unlock structure, with
    their own fixed, uniquely-named `FAMILY_MEMBER_C_EMAIL`/`FAMILY_MEMBER_D_EMAIL` constants (mirroring
    `FAMILY_OWNER_EMAIL`'s naming convention). These accounts join the family via the real invite UI
    flow this plan's own spec drives -- this fixture only gets them to an authenticated, unlocked,
    NOT-yet-family-member state; family joining itself is this plan's spec body, not the fixture.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    The new helper(s) compile and, called twice in the same run (mirroring
    `ensureFamilyOwnerSession`'s own tolerance for "another spec file already registered this
    account"), do not throw on the second call.
  </acceptance_criteria>
  <done>Two additional stable, idempotent test identities exist for this phase's multi-account scenarios.</done>
</task>

<task type="tracer">
  <name>Task 3: SC2 (current members read) + SC3 (fresh-invite late joiner)</name>
  <files>web/e2e/family-wide-sharing.spec.ts</files>
  <read_first>
    `web/e2e/invite-flow.spec.ts` in full (the `generateInviteViaUI`/`joinAsAuthenticatedSession`/
    `registerViaInviteLink`-shaped helpers this plan reuses or closely mirrors for issuing and
    redeeming an invite through real UI, never a raw API call) and `web/e2e/shared-sync.spec.ts` (the
    two-account real-decrypt assertion shape -- reading a SECOND session's own decrypted field value,
    not merely a row's presence); 30-UI-SPEC.md's Share Dialog contract for the exact
    `share-recipient-family-wide`/`share-submit` testids this task's UI-driven share action clicks
    through.
  </read_first>
  <action>
    In `web/e2e/family-wide-sharing.spec.ts`, using `twoSessions` (owner + an already-joined member B,
    mirroring `invite-flow.spec.ts`'s existing two-account setup) plus this plan's new
    `ensureFamilyMemberCSession`: (1) SC2 -- as the owner, open the real Share entry point for a
    folder/item (locate the existing trigger this codebase already wires to `ShareDialog`'s
    `scope.kind`), check `share-recipient-family-wide`, submit; assert member B's OWN session, on its
    own next sync (poll interval, not a reload), opens the item/folder and reads the REAL decrypted
    field value (e.g. a login's password field, matching `shared-sync.spec.ts`'s own positive-assertion
    style). (2) SC3 fresh-invite case -- AFTER the family-wide share above already exists, the owner
    generates a NEW invite via the real Settings > Family UI (`generateInviteViaUI`-shaped helper);
    member C redeems it via the real `/invite/{id}#{secret}` UI flow
    (`joinAsAuthenticatedSession`/registration-through-invite, matching `invite-flow.spec.ts`'s
    established pattern); assert member C's OWN client, immediately after redemption (no reseal wait
    needed -- this is the invite-carried path), decrypts the SAME real field value SC2 asserted.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/family-wide-sharing.spec.ts -g "current members read|fresh invite" 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    Both cases pass headed, live, against an isolated test server; each assertion reads a genuine
    decrypted field value on the RECIPIENT'S OWN page object, never a row-count or presence check.
  </acceptance_criteria>
  <done>SC2 and SC3's invite-carried case are proven live, recipient-side, positive.</done>
</task>

<task type="tracer">
  <name>Task 4: the gap-window case -- invite before share, lazy reseal after</name>
  <files>web/e2e/family-wide-sharing.spec.ts</files>
  <read_first>
    `web/e2e/remove-member.spec.ts`'s "deliberately reload-free" comment (~line 325) and its
    positive-then-driven-sync-then-assert pattern -- this task's own sync-driving step must be the
    SAME shape (one real poll cycle or a deliberate unlock/hydrate re-trigger on an ALREADY-ONLINE
    keyholder's session, never a page reload standing in for it); this plan's Task 3 result (the
    family-wide share and member C's session already exist) as the starting state for this task's
    own sequence.
  </read_first>
  <action>
    Sequence: (1) the owner generates a SECOND fresh invite BEFORE creating any NEW family-wide share
    (so this invite carries the EXISTING family-wide collection's key already, per SC3's already-proven
    path -- to isolate the gap-window case cleanly, either revoke/ignore that existing carry-over or
    construct a BRAND NEW family-wide share specifically for this test, created AFTER this invite is
    generated, so the invite structurally could not have carried its key); this plan's new
    `ensureFamilyMemberDSession` redeems it -- assert member D's item list shows the
    `item-row-pending-family-key`/pending-folder-absence state (per 30-15's contract) for the new
    share, never a decrypt-failure row. (2) drive member B's (an already-online keyholder's) session
    through one real unlock/hydrate cycle (re-triggering `subscribeLockState`'s unlock branch --
    lock then unlock via the real UI, or wait one real poll interval if the session is already
    unlocked and simply idle) -- this is what fires 30-13's reseal trigger. (3) drive member D's OWN
    session through one real sync cycle (poll interval, NOT a reload). (4) assert member D's client
    now decrypts the real field value, and the synthetic pending row is gone from D's item list.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/family-wide-sharing.spec.ts -g "gap window|late joiner" 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    The gap-window case passes headed, live; the pending state is observed BEFORE the reseal trigger
    runs and the real decrypted content is observed AFTER, on the SAME session's own page object, with
    no reload anywhere in the sequence.
  </acceptance_criteria>
  <done>The lazy-reseal fallback is proven live, end to end, for the exact gap-window scenario no existing spec covers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-harness accounts <-> a real, running pv-server | Same boundary as every other live e2e spec in this repo; the isolation checkpoint above is the mitigation for this suite's own known hazard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-23 | Tampering (test data pollution) | Running against a shared/real DB | medium | mitigate | Task 1's blocking checkpoint requires confirmed server isolation before any live traffic is generated, per STATE.md's own recorded Phase 28 hazard. |

</threat_model>

<verification>
`npx playwright test e2e/family-wide-sharing.spec.ts --retries=0`
</verification>

<success_criteria>
SC2, SC3 (both delivery paths), and the FSH-02 gap-window backstop are all proven live, positive,
recipient-side, on real decrypted content.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-16-SUMMARY.md` when done
</output>
