---
phase: 30-the-living-group-family-wide-sharing
plan: 11
type: execute
wave: 4
depends_on: ["30-02", "30-05", "30-08", "30-09"]
files_modified:
  - web/src/lib/vault/collections.ts
  - web/src/components/vault/ItemRow.tsx
  - web/src/lib/i18n/dictionary.ts
autonomous: true
requirements: [FSH-01]

must_haves:
  truths:
    - "A family badge (Users icon, text-secondary, role=img, aria-label vault.familyBadgeAria) renders in ItemRow's existing marker slot IN PLACE OF AvatarStack whenever the item's own collection is family-wide and the caller is its owner -- never alongside AvatarStack, never N individual avatars for a family-wide share."
    - "The badge's boolean input is synchronously available from already-loaded collection metadata (isFamilyWideCollection(collectionId), a plain lookup against the already-fetched collections store) -- it never calls useShareRecipients and has no loading/error state of its own, by construction."
    - "After a newcomer's key arrives, a family-wide item they receive renders through the EXISTING sharedToMe/isShared marker path unchanged -- this plan does not add a family variant to that marker."
  artifacts:
    - "web/src/lib/vault/collections.ts (Collection.familyWideKind field, isFamilyWideCollection(collectionId) lookup)"
    - "web/src/components/vault/ItemRow.tsx (family badge in the marker slot)"
    - "web/src/lib/i18n/dictionary.ts (vault.familyBadgeAria)"
  key_links:
    - "ItemRow's marker-slot conditional gains a THIRD branch (family badge) ahead of the existing sharedToMe/isShared/AvatarStack chain, keyed on isFamilyWideCollection(item.collectionId) -- checked only on the OWNER side (item.sharedToMe !== true), since a family-wide item's RECIPIENT-side rendering is unchanged (VIS-02, Phase 34's job)."
---

<objective>
Give the item list a distinct, owner-side "shared with the whole family" marker -- a `Users` badge,
never N avatars -- driven entirely by already-loaded synchronous collection metadata, with no fetch
and no loading state of its own by construction.

Purpose: the locked decision "a distinct family badge in the item list, not N individual avatars ...
a five-person family rendered as five avatars is indistinguishable from five separate per-person
shares" (30-CONTEXT.md).
Output: `Collection.familyWideKind`, `isFamilyWideCollection()`, the family badge in `ItemRow.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-UI-SPEC.md
@web/src/lib/vault/collections.ts
@web/src/components/vault/ItemRow.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: isFamilyWideCollection() -- synchronous lookup, no fetch</name>
  <files>web/src/lib/vault/collections.ts</files>
  <read_first>
    `collections.ts` lines 25-34 (`Collection` interface) and lines 89-91
    (`getCollectionAccessLevel`'s exact synchronous-lookup shape -- this task's new function mirrors
    it) and `refreshCollections` (from line 130 on) where `listCollections()`'s rows are mapped into
    `Collection[]` -- this is where `family_wide_kind` (present on `CollectionRow` per 30-09) must be
    threaded into the new `familyWideKind` field.
  </read_first>
  <behavior>
    - Given the store holds a collection `{id: "c1", familyWideKind: "folder"}`:
      `isFamilyWideCollection("c1")` returns `true`.
    - Given the store holds a collection `{id: "c2", familyWideKind: null}`:
      `isFamilyWideCollection("c2")` returns `false`.
    - Given `collectionId` is `null`/`undefined`/not present in the store at all:
      `isFamilyWideCollection` returns `false` -- never throws, matching
      `getCollectionAccessLevel`'s own `?? undefined` fail-safe shape.
  </behavior>
  <action>
    Add `familyWideKind: string | null` to the `Collection` interface. In `refreshCollections`'s
    row-to-`Collection` mapping, thread `row.family_wide_kind` straight through (no transform). Add
    `export function isFamilyWideCollection(collectionId: string | null | undefined): boolean` that
    returns `collections.find((c) =&gt; c.id === collectionId)?.familyWideKind != null`.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/vault/collections.test.ts -t "familyWide" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>All three behavior bullets pass.</acceptance_criteria>
  <done>A synchronous, fail-safe, zero-fetch lookup exists for "is this collection family-wide."</done>
</task>

<task type="auto">
  <name>Task 2: the family badge in ItemRow's marker slot</name>
  <files>web/src/components/vault/ItemRow.tsx, web/src/lib/i18n/dictionary.ts</files>
  <read_first>
    `ItemRow.tsx` lines 138-169 (the existing marker-slot conditional chain in full --
    `item.sharedToMe === true ? Share2-marker : item.isShared === true ? AvatarStack : null` -- this
    task inserts a new branch BEFORE the `AvatarStack` branch, never touching the `sharedToMe` branch
    at all); 30-UI-SPEC.md's "Family badge (owner-side, replaces AvatarStack for a family-wide item)"
    section for the exact markup and the explicit "independent of recipient resolution by
    construction" requirement.
  </read_first>
  <action>
    Import `isFamilyWideCollection` from `@/lib/vault/collections`. In the marker-slot conditional,
    change the existing `item.isShared === true ? &lt;AvatarStack item={item} /&gt; : null` arm to: when
    `item.sharedToMe !== true` (owner side only -- this task never touches recipient-side rendering,
    per this plan's own third truth) AND `isFamilyWideCollection(item.collectionId)` is `true`, render
    the family badge (`&lt;span data-testid="item-row-family-badge" role="img"
    aria-label={t("vault.familyBadgeAria")} title={t("vault.familyBadgeAria")} className="inline-flex
    shrink-0 items-center text-secondary"&gt;&lt;Users size={14} aria-hidden="true" /&gt;&lt;/span&gt;`, importing
    `Users` from `lucide-react` alongside the existing `Share2`/`MoreVertical` import); otherwise fall
    through to the existing `AvatarStack` rendering unchanged. Add `vault.familyBadgeAria` to
    `dictionary.ts` (pl "Udostępnione całej rodzinie", en "Shared with the whole family").
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/vault/ItemRow.test.tsx -t "family badge" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    A fixture item whose `collectionId` resolves `isFamilyWideCollection` to `true` renders
    `item-row-family-badge` and does NOT render `AvatarStack`'s own testid; an item shared to
    specific people (not family-wide) still renders `AvatarStack` unchanged; a `sharedToMe: true` item
    still renders the existing `item-shared-with-you` marker, never the family badge, regardless of
    its collection's `familyWideKind`.
  </acceptance_criteria>
  <done>Owner-side, family-wide items render a distinct family badge instead of N avatars, with zero dependency on any recipient-list fetch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Item-list rendering | Pure client-local rendering decision from already-authorized, already-fetched metadata; no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-18 | Information Disclosure | Family badge visibility | low | accept | The badge only ever renders for items the caller already owns/has full metadata for (`item.collectionId` + the caller's own `collections` store) -- reveals nothing the caller could not already see via the existing `AvatarStack`/`sharedToMe` markers. |

</threat_model>

<verification>
`npx vitest run src/lib/vault/collections.test.ts src/components/vault/ItemRow.test.tsx`
</verification>

<success_criteria>
The family badge exists, is driven by synchronous metadata with no fetch of its own, and never
double-renders alongside AvatarStack or the sharedToMe marker.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-11-SUMMARY.md` when done
</output>
