---
phase: 30-the-living-group-family-wide-sharing
plan: 09
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - web/src/lib/vault/api.ts
autonomous: true
requirements: [FSH-01, FSH-05]

must_haves:
  truths:
    - "CollectionRow carries family_wide_kind ('folder' | 'item_bucket' | null), mirroring the server's CollectionResponse field byte-for-byte, readable from getCollection/listCollections with zero new round trips."
    - "createCollection accepts an optional familyWideKind parameter, omitted entirely from the request body when absent -- every existing call site (ordinary shared-folder creation) is byte-for-byte unchanged."
  artifacts:
    - "web/src/lib/vault/api.ts (CollectionRow.family_wide_kind, createCollection(id, encName, sealedKey, familyWideKind?))"
  key_links:
    - "This plan is the single source of the client-side wire-type contract 30-DECISION-FSH-02.md and 30-02's Rust types define -- every later client plan in this phase (30-06's discovery client excepted) reads this contract rather than re-deriving its own field names."
---

<objective>
Mirror 30-02's server-side wire additions (`CollectionResponse.family_wide_kind`,
`CreateCollectionRequest.family_wide_kind`) into the client's own `CollectionRow` type and
`createCollection()` wrapper -- the one, single place every later client plan in this phase (invite
folding, ShareDialog, SharingOverviewPanel, ItemRow badge) reads the field from, so the contract is
named exactly once, not re-derived per consumer.

Purpose: a small, mechanical, low-risk wire-contract mirror that unblocks five parallel/sequential
client plans without any one of them touching this shared file.
Output: `CollectionRow.family_wide_kind`; `createCollection`'s new optional parameter.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-DECISION-FSH-02.md
@web/src/lib/vault/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CollectionRow.family_wide_kind + createCollection's optional param</name>
  <files>web/src/lib/vault/api.ts</files>
  <read_first>
    `web/src/lib/vault/api.ts` lines 194-204 (`CollectionRow`, `getCollection`) and lines 294-312
    (`createCollection`, `listCollections`) -- the exact JSON body shape `createCollection` currently
    sends (`{id, enc_name, sealed_key}`); `30-DECISION-FSH-02.md` for the exact field name
    (`family_wide_kind`, closed set `'folder' | 'item_bucket'`, nullable).
  </read_first>
  <behavior>
    - `CollectionRow` has a new field `family_wide_kind: string | null` -- a `getCollection`/
      `listCollections` mock response omitting the field entirely still type-checks (server responses
      predating this phase's deploy could theoretically omit it during a rolling restart; treat as
      `null`-equivalent, never a required field a missing-key response would throw on).
    - `createCollection(id, encName, sealedKey)` (no 4th argument): the POSTed JSON body has NO
      `family_wide_kind` key at all -- byte-for-byte identical to today's request shape (assert via a
      spy on the underlying fetch call's body).
    - `createCollection(id, encName, sealedKey, "folder")`: the POSTed JSON body includes
      `"family_wide_kind": "folder"`.
  </behavior>
  <action>
    Add `family_wide_kind: string | null` to `CollectionRow`. Extend `createCollection`'s signature to
    `createCollection(id: string, encName: string, sealedKey: string, familyWideKind?: "folder" |
    "item_bucket"): Promise&lt;CollectionRow&gt;`, building the request body as `{ id, enc_name: encName,
    sealed_key: sealedKey, ...(familyWideKind !== undefined ? { family_wide_kind: familyWideKind } :
    {}) }` so the key is OMITTED (not sent as `null`) when the parameter is absent, matching 30-02's
    server-side `#[serde(default)]`-shaped optional-field contract exactly.
  </action>
  <verify>
    <automated>set -o pipefail; cd web && npx vitest run src/lib/vault/api.test.ts -t "createCollection" 2>&1 | tail -30 && npx tsc --noEmit -p web/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    All three behavior bullets pass; `npx tsc --noEmit` is clean (confirms every existing
    `createCollection(...)` 3-argument call site across the codebase still type-checks unmodified).
  </acceptance_criteria>
  <done>The client's wire-type contract for family_wide_kind exists, mirrors the server exactly, and is fully backward-compatible with every existing call site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client wire types | Pure type/shape mirroring; no new runtime trust boundary beyond what 30-02 already threat-modeled server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-16 | Tampering | `createCollection`'s new optional parameter | low | accept | The server independently validates `family_wide_kind` against the same closed set (30-02) -- a client sending a nonsense value is rejected server-side regardless of what this wrapper allows through at the type level. |

</threat_model>

<verification>
`npx vitest run src/lib/vault/api.test.ts`; `npx tsc --noEmit -p web/tsconfig.json`
</verification>

<success_criteria>
The client-side wire contract exists, matches the server exactly, and every existing caller is
unaffected.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-09-SUMMARY.md` when done
</output>
