---
phase: 30-the-living-group-family-wide-sharing
plan: 07
type: execute
wave: 3
depends_on: ["30-02", "30-03", "30-09"]
files_modified:
  - web/src/lib/invite/crypto.ts
  - web/src/lib/invite/api.ts
autonomous: true
requirements: [FSH-02]

must_haves:
  truths:
    - "generateInviteLink folds in a wrapped key for every family_wide_kind collection the caller currently holds a key for, in addition to whatever single explicit collection scope the invite already carries -- an invite generated while 2 family-wide collections exist carries 2 additional wrapped keys."
    - "redeemInviteFlow self-seals every family-wide key the invite's metadata carried, to the invitee's own freshly-published identity key, and submits all of them alongside the existing single-collection sealed_for_self in one accept() call."
    - "An invite generated while zero family_wide_kind collections exist behaves byte-for-byte like today -- no regression to the existing single-collection-scope or family-only invite flows."
  artifacts:
    - "web/src/lib/invite/crypto.ts (generateInviteLink/redeemInviteFlow family-wide threading)"
    - "web/src/lib/invite/api.ts (createInvite/fetchInvitePublicMetadata/redeemInvite wire-type widening)"
  key_links:
    - "generateInviteLink lists the caller's own collections via listCollections() (an existing client call, gated by the caller already holding collection_keys rows) filtered to family_wide_kind !== null -- never a new server round trip beyond what this call already makes."
---

<objective>
Fold family-wide collection wraps into the existing invite generation/redemption flow -- the
invite-time-wrap half of FSH-02's hybrid mechanism, on the client side, consuming the wire contract
30-03 built server-side.

Purpose: this is what makes "join through a fresh invite" deliver family-wide content immediately,
per 30-UI-SPEC.md's timing-caveat copy ("New family members see this right away if they join
through a fresh invite").
Output: `generateInviteLink`/`redeemInviteFlow` thread N additive family-wide wraps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-DECISION-FSH-02.md
@web/src/lib/invite/crypto.ts
@web/src/lib/invite/api.ts
@web/src/lib/vault/api.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: generateInviteLink folds in every current family-wide collection's key</name>
  <files>web/src/lib/invite/crypto.ts, web/src/lib/invite/api.ts</files>
  <read_first>
    `web/src/lib/invite/crypto.ts` lines 62-124 (`generateInviteLink` in full -- the exact point
    after `ensureOwnIdentityKeypair`/`WasmInviteChannel.fromSecret` where the existing single
    `scope.kind === "collection"` wrap happens, lines 93-101) and `web/src/lib/invite/api.ts`'s
    `createInvite` request-body shape (this task widens it to carry `family_wide_keys`, mirroring
    30-03's Rust `FamilyWideKeyEntry`); `web/src/lib/vault/api.ts`'s `listCollections()` (already
    returns `family_wide_kind` per 30-02) and `CollectionRow` type.
  </read_first>
  <behavior>
    - Given the caller currently holds keys for 2 collections with `family_wide_kind !== null` (one
      `'folder'`, one `'item_bucket'`) and generates a family-only invite (`scope.kind === "family"`):
      the resulting `createInvite` call body's `family_wide_keys` array has exactly 2 entries, each
      `{collection_id, access_level, wrapped_collection_key}` where `wrapped_collection_key` is a
      real `channel.wrapCollectionKey(...)` output (the SAME per-collection wrap primitive the
      existing `scope.kind === "collection"` branch already uses).
    - Given the caller holds zero `family_wide_kind` collections: `family_wide_keys` is `[]` -- the
      existing request shape and network call are otherwise byte-identical to pre-this-plan behavior.
    - Given `scope.kind === "collection"` (an explicit single-folder invite) AND the caller ALSO
      holds a family-wide collection: BOTH the existing singular `wrapped_collection_key` field and
      the new `family_wide_keys` array are populated in the same call -- the two mechanisms are
      independent and additive, never mutually exclusive.
    - `access_level` for each family-wide entry is the collection's OWN caller-held access level
      (from `listCollections()`'s `accessLevel` field), never hardcoded to `"read"`.
  </behavior>
  <action>
    In `generateInviteLink`, after `ensureOwnIdentityKeypair` and BEFORE the existing
    `scope.kind === "collection"` branch, call `listCollections()`, filter to entries with
    `family_wide_kind !== null` (requires `CollectionRow`/`listCollections()` to already expose
    `family_wide_kind`, added here if not already present from 30-02's server change), and for each,
    unseal via `unsealCollectionKey(identityKey, entry.sealed_key)` then `channel.wrapCollectionKey(ck)`
    -- reusing the SAME `channel` instance the existing single-collection branch uses, mirroring its
    free-in-finally discipline for every intermediate `WasmCollectionKey` handle. Build the
    `family_wide_keys` array from these results and pass it to `createInvite`'s widened body. Update
    `invite/api.ts`'s `createInvite` request type and `fetchInvitePublicMetadata`'s response type to
    carry `family_wide_keys: Array&lt;{collection_id: string; access_level: string;
    wrapped_collection_key: string}&gt;` (default `[]`), matching 30-03's server response shape
    exactly.
  </action>
  <verify>
    <automated>set -o pipefail; cd web && npx vitest run src/lib/invite/crypto.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    All four behavior bullets pass; the existing `invite/crypto.test.ts` suite's pre-existing
    single-collection and family-only cases remain green unmodified (proving no regression).
  </acceptance_criteria>
  <done>generateInviteLink carries every current family-wide collection's key, additively, alongside whatever single explicit scope the invite already had.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: redeemInviteFlow self-seals every carried family-wide key</name>
  <files>web/src/lib/invite/crypto.ts, web/src/lib/invite/api.ts</files>
  <read_first>
    `web/src/lib/invite/crypto.ts` lines 168-217 (`redeemInviteFlow` in full -- the existing
    single-collection `metadata.wrapped_collection_key !== null` branch at lines 191-203 is the
    per-entry pattern to loop) and 30-03's `AcceptInvitationRequest.family_wide_sealed_keys` shape
    this task's POST body must match exactly.
  </read_first>
  <behavior>
    - Given `metadata.family_wide_keys` has 2 entries: `redeemInviteFlow` unwraps each via
      `channel.unwrapCollectionKey(entry.wrapped_collection_key)`, self-seals each to the invitee's
      OWN freshly-published identity public key via `sealCollectionKey`, and the resulting `redeemInvite`
      call body's `family_wide_sealed_keys` array has 2 entries, each `{collection_id,
      sealed_for_self}`.
    - Given `metadata.family_wide_keys` is `[]`: `family_wide_sealed_keys` is `[]` -- the existing
      single-collection `sealed_for_self` field and network call remain byte-identical to
      pre-this-plan behavior.
    - Every intermediate `WasmCollectionKey`/`WasmIdentityPublicKey` handle from the new loop is
      freed in the SAME `finally` block the existing single-collection variables already free in --
      no leaked handle across either path.
  </behavior>
  <action>
    In `redeemInviteFlow`, after the existing single-collection `sealedForSelf` block, loop
    `metadata.family_wide_keys`: `channel.unwrapCollectionKey(entry.wrapped_collection_key)`, seal to
    `identityKey`'s own public key via `sealCollectionKey`, push `{collection_id: entry.collection_id,
    sealed_for_self: sealedBlob}` into a `familyWideSealedKeys` array; free each intermediate handle.
    Pass `familyWideSealedKeys` in `redeemInvite`'s widened request body. Update `invite/api.ts`'s
    `redeemInvite` request type to carry `family_wide_sealed_keys` (default `[]`), matching 30-03's
    server request shape exactly.
  </action>
  <verify>
    <automated>set -o pipefail; cd web && npx vitest run src/lib/invite/crypto.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    All three behavior bullets pass; the existing single-collection redemption test case remains
    green unmodified.
  </acceptance_criteria>
  <done>redeemInviteFlow self-seals and submits every family-wide key an invite carried, atomically alongside the existing single-collection grant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Invite generator's browser -> invitee's browser, via the server's opaque blob relay | Unchanged from the existing single-collection invite flow -- the server never sees an unwrapped key at any point in this widened path either. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-14 | Information Disclosure | `family_wide_keys`/`family_wide_sealed_keys` wire fields | critical | mitigate | Both are the SAME opaque `WrappedKey`-shaped blob the existing singular fields already carry -- no plaintext, no private key, ever serialized into either request. |

</threat_model>

<verification>
`npx vitest run src/lib/invite/crypto.test.ts`
</verification>

<success_criteria>
An invite generated while family-wide collections exist carries their keys; redemption self-seals
and submits them all, atomically, alongside the existing single-collection grant, with zero
regression to either existing invite path.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-07-SUMMARY.md` when done
</output>
