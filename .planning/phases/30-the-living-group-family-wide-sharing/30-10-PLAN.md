---
phase: 30-the-living-group-family-wide-sharing
plan: 10
type: execute
wave: 3
depends_on: ["30-02", "30-09"]
files_modified:
  - web/src/components/vault/SharingOverviewPanel.tsx
autonomous: true
requirements: [FSH-05]

must_haves:
  truths:
    - "The pinned family-wide block renders above sharing-overview-tabs, only when the caller has >= 1 family-wide share (folder or item_bucket), never a '0 family-wide shares' heading."
    - "The block does not render at all until BOTH the family-wide-share list AND the member count have resolved -- no half-rendered flash of an empty list with a populated count or vice versa."
    - "A partial fetch failure among the block's own data sources renders the block with whatever DID resolve rather than crashing the whole panel, mirroring this file's existing .catch(() => []) fail-safe discipline throughout."
    - "Each family-wide share's name in the list truncates rather than overflowing, matching the existing folder-row truncate pattern exactly."
    - "familyWideMemberCountError renders in place of the count on fetch failure with no retry control; the (static) timing caveat still renders regardless."
  artifacts:
    - "web/src/components/vault/SharingOverviewPanel.tsx (sharing-overview-family-wide block: wrapper, count line, caveat line, <ul> list)"
  key_links:
    - "An item_bucket-kind family-wide collection is NOT rendered as a folder row -- its contained items are fetched via getCollectionItems and decrypted (the owner always holds a key for their own bucket) and each rendered as an individual <li> with a Share2 icon, matching UI-SPEC's kind='item' branch."
  prohibitions:
    - statement: "No revoke action exists on the family-wide block -- no UserMinus button anywhere in it."
      status: "enforced by this task's markup -- the only way to change who reads a family-wide share is through the existing leave/remove/delete-account paths, unchanged elsewhere."
      verification: "manual -- component test asserts no button with a revoke-shaped testid renders inside sharing-overview-family-wide."
---

<objective>
Add the pinned family-wide block to `SharingOverviewPanel` per 30-UI-SPEC.md's exact contract --
one block (not a third tab, not a per-share row set), showing a live member count, the timing
caveat, and a flat list of what's shared family-wide (folders by name, individual bucket items by
their own decrypted name), positioned above the existing folder/person tab switcher.

Purpose: FSH-05's second required location for the timing caveat -- "the person reading the
overview later is asking exactly this question."
Output: the `sharing-overview-family-wide` block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-UI-SPEC.md
@web/src/components/vault/SharingOverviewPanel.tsx
@web/src/lib/vault/api.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: the pinned family-wide block</name>
  <files>web/src/components/vault/SharingOverviewPanel.tsx</files>
  <read_first>
    `SharingOverviewPanel.tsx` lines 180-345 (component state, the `load()` effect's existing
    `me()`/`listCollections()`/`getCollectionAccessList` calls and its `.catch(() => [])` fail-safe
    pattern throughout, and the `editableCollections`/`rawCollections` distinction -- `rawCollections`
    already carries `family_wide_kind` per 30-09) and lines 396-419 (the `&lt;aside&gt;` root and the
    `sharing-overview-tabs` element this block must render immediately above); 30-UI-SPEC.md's
    "Sharing Overview -- Family-Wide Block Contract" section verbatim for the exact markup, testids
    (`sharing-overview-family-wide`, `-count`, `-caveat`, `-list`), and the "why a dedicated block,
    not the existing By-folder tab" rationale.
  </read_first>
  <action>
    In the existing `load()` effect, derive `familyWideCollections = rawCollections.filter((c) =&gt;
    c.family_wide_kind !== null)`. For each with `family_wide_kind === "folder"`, build a
    `{id, kind: "folder", name: c.enc_name-decrypted-via-the-existing-collection-name-decrypt-path}`
    entry (reuse whatever this file/`collections.ts` already uses to decrypt a collection's `enc_name`
    -- the caller always holds a key for a folder they can see in `rawCollections` at all). For each
    with `family_wide_kind === "item_bucket"`, call `getCollectionItems(c.id)`, decrypt each item's
    name (owner always holds this bucket's key), and push one `{id: item.id, kind: "item", name:
    decryptedName}` entry per contained item (never one entry for the whole bucket). Compute the
    member count from the SAME `getFamilyMembers()`-shaped data source `share.familyWideMemberCount`'s
    ShareDialog copy uses (fetch once in this same `load()` effect; reuse the four-state
    loading/solo/populated/error discriminant). Render the block per 30-UI-SPEC.md's exact JSX
    (`data-testid="sharing-overview-family-wide"`, `Users` icon `text-secondary`, count line
    `sharing-overview-family-wide-count`, caveat line `sharing-overview-family-wide-caveat` using
    `share.familyWideTimingCaveat`, `&lt;ul data-testid="sharing-overview-family-wide-list"&gt;` with one
    `&lt;li&gt;` per entry, `Folder` icon for `kind==="folder"`/`Share2` icon for `kind==="item"`, `truncate
    text-sm` on each name span with a `title` attribute) immediately above the existing
    `sharing-overview-tabs` element, gated on `familyWideCollections.length &gt; 0` AND both the
    share-list and member-count fetches having resolved (not merely "loading" state cleared).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/vault/SharingOverviewPanel.test.tsx -t "family-wide" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    A fixture with zero family-wide collections renders no `sharing-overview-family-wide` element at
    all; a fixture with one `folder`-kind and one `item_bucket`-kind (containing 2 items) renders the
    block with exactly 3 `&lt;li&gt;` entries (1 folder + 2 items); a `getCollectionItems` rejection for
    the bucket renders the block with the folder entry present and the bucket's entries simply absent
    (not a crash); no element inside the block has a testid resembling a revoke button.
  </acceptance_criteria>
  <done>The pinned family-wide block exists, matches the locked contract, and degrades safely on partial fetch failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner's own decrypt of their own bucket items | Same trust boundary as every other client-side decrypt in this panel -- the owner already holds the relevant key; no new server trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-17 | Information Disclosure | item_bucket contents shown in the overview | low | accept | Only the CALLER's own decrypted item names render, using data the caller already holds a key for (`getCollectionItems` is `Membership&lt;Collection, RequireRead&gt;`-gated, unchanged) -- no new disclosure beyond what this panel already shows for editable collections. |

</threat_model>

<verification>
`npx vitest run src/components/vault/SharingOverviewPanel.test.tsx`
</verification>

<success_criteria>
The family-wide block exists, matches 30-UI-SPEC.md's contract exactly, and never renders a
half-resolved or empty-but-present state.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-10-SUMMARY.md` when done
</output>
