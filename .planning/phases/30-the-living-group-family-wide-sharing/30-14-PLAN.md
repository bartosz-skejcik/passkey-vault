---
phase: 30-the-living-group-family-wide-sharing
plan: 14
type: execute
wave: 3
depends_on: ["30-02", "30-03"]
files_modified:
  - crates/pv-server/tests/family_wide_sharing.rs
autonomous: true
requirements: [FSH-03]

must_haves:
  truths:
    - "An adversarial test drives the newcomer's-grant path end to end (a real invite carrying family-wide keys, redeemed by a real second account, plus a real reseal against add_member) and inspects EVERY row written and EVERY request/response body exchanged, asserting none contains a Collection Key, a private key, or plaintext -- falsification-tested by proving the test actually catches a deliberately reintroduced leak, per SC4's own bar."
    - "The discovery endpoint's JSON response, over the wire, contains no key named sealed_key, enc_name, or any field whose value is not an id/kind/user_id string."
    - "The reseal composition's add_member POST body is structurally identical in shape to an ordinary manual share's add_member call -- no new field leaks that this grant originated from a family-wide reseal rather than an explicit share."
    - "The is_family_wide_kind column and every query touching it are scoped to the caller's own resolved family_id -- a second, independent family's rows are never returned."
  artifacts:
    - "crates/pv-server/tests/family_wide_sharing.rs"
  key_links:
    - "This test simulates the CLIENT side of the reseal/invite-fold compositions directly via pv_core's real seal/unseal calls (mirroring tests/collections.rs's own established 'only tests/collections.rs calls those functions, simulating the client side' precedent) -- never a mocked crypto layer."
---

<objective>
Prove SC4's adversarial bar with a real, in-process Rust integration test: drive the newcomer's
grant through every server surface this phase adds (collection creation with `family_wide_kind`,
the invite-carried wrap path, the discovery endpoint, and a reseal via `add_member`), and inspect
every row written and every request/response body for leaked key material -- the bar this phase's
own success criteria (SC4) and REQUIREMENTS.md's Non-Negotiable #1 hold it to.

Purpose: closes the "adversarial test inspecting every row written and every request body sent"
requirement explicitly named in ROADMAP.md's SC4, on the server side (the client-side real-WASM leg
is 30-04's).
Output: `crates/pv-server/tests/family_wide_sharing.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-RESEARCH.md
@crates/pv-server/tests/collections.rs
@crates/pv-server/src/routes/collections.rs
@crates/pv-server/src/routes/families.rs
@crates/pv-server/src/routes/invitations.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: adversarial row/request-body inspection across the newcomer's-grant path</name>
  <files>crates/pv-server/tests/family_wide_sharing.rs</files>
  <read_first>
    `crates/pv-server/tests/collections.rs` for this repo's own established pattern of an
    integration test that simulates the CLIENT side directly via `pv_core::identity::{seal, unseal,
    unseal_collection_key}` (the module doc comment in `collections.rs` names this file as the ONE
    place production code forbids itself from calling those functions -- this new test file follows
    the identical simulate-the-client discipline); `families.rs`'s new `family_wide_pending` handler
    and response types (30-02); `invitations.rs`'s `create`/`accept` family-wide threading (30-03);
    `collections.rs::add_member`'s `AddMemberRequest` shape.
  </read_first>
  <action>
    Create `crates/pv-server/tests/family_wide_sharing.rs`. Seed a real family with 3 real users
    (owner, member B, member C), each with a real `user_keypairs` row built via `pv_core::identity`
    calls (mirroring `tests/collections.rs`'s own fixture style). (1) Create a `family_wide_kind:
    'folder'` collection as the owner, granting B via the real `add_member` handler; assert the
    resulting `collections`/`collection_keys` rows contain no field decodable as plaintext (the only
    non-opaque column is `family_wide_kind` itself, a plain enum string -- assert every OTHER column
    is either an id, a timestamp, or an opaque blob no test-side decrypt succeeds against without the
    real key). (2) Generate a real invite via `invitations::create` carrying this collection's
    wrapped key in `family_wide_keys`; call `invitations::accept` as C; assert the created
    `collection_keys` row for C decrypts correctly ONLY when unsealed with C's real identity secret
    key (proving the wrap is genuinely bound to C, not a copy of B's), and that the `invitations`/
    `invitation_family_wide_keys` rows read back contain no field the test can decrypt without a real
    identity secret key. (3) Call `families::family_wide_pending` as a FOURTH, freshly-added member D
    (no key yet); assert the raw JSON response body contains no key `sealed_key`/`enc_name`/any string
    field matching a base64-decodable ciphertext-shaped blob -- only `collection_id`/`kind`/
    `recipient_user_id` string ids. (4) Simulate the reseal: as B (an existing keyholder), unwrap B's
    own `collection_keys.sealed_key` via `pv_core::identity::unseal_collection_key`, reseal to D's
    real public key via `pv_core::identity::seal`, POST via the real `add_member` handler; assert the
    request body's JSON keys are byte-for-byte identical in SHAPE (key names, not values) to an
    ordinary, non-family-wide `add_member` call made earlier in the SAME test file -- proving no new
    field leaks "this came from a reseal." (5) Seed a SECOND, independent family with its own
    family-wide collection; call `family_wide_pending` as a member of the FIRST family; assert the
    response contains zero entries referencing the second family's collection id.
  </action>
  <verify>
    <automated>cd crates/pv-server && cargo test --test family_wide_sharing 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    All five numbered assertions in the action above pass as distinct `#[tokio::test]` functions (or
    clearly-separated sections of one); the test suite fails RED if any of `sealed_key`, `enc_name`, a
    raw Collection Key byte pattern, or cross-family data is deliberately reintroduced into the
    discovery response (prove this by temporarily reverting `families::family_wide_pending`'s response
    struct to include `enc_name` and confirming the test catches it, then reverting the revert).
  </acceptance_criteria>
  <done>Every row written and every request/response body exchanged during a newcomer's grant is proven, by an adversarial test, to carry no key material, private key, or plaintext, and to respect family scoping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This test itself | Acts as the adversary -- everything it inspects is data a genuinely malicious or curious operator with raw database/network access could also see. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-19 | Information Disclosure | Every row/response this phase's server surfaces write or return | critical | mitigate | This test IS the mitigation -- a falsification-tested adversarial inspection covering every new surface, not a policy statement. |

</threat_model>

<verification>
`cargo test --test family_wide_sharing -- --nocapture`
</verification>

<success_criteria>
The adversarial test exists, is falsification-tested (proven to actually catch a reintroduced leak,
not merely written to pass against correct code), and covers every new server surface this phase
adds.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-14-SUMMARY.md` when done
</output>
