---
phase: 30-the-living-group-family-wide-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/phases/30-the-living-group-family-wide-sharing/30-DECISION-FSH-02.md
  - crates/pv-server/migrations/0019_family_wide_sharing.sql
autonomous: true
requirements: [FSH-02, FSH-03]

must_haves:
  truths:
    - "The FSH-02 decision record names the chosen mechanism, names the alternatives rejected on their merits (at minimum invite-code-as-shared-secret wrapping and lazy reseal by the next member online), and states each option's user-visible caveat including whether 'automatically' can mean 'instantly'."
    - "The decision record's own commit lands before any commit that implements it — verifiable by `git log --oneline` order."
    - "Nothing the schema adds stores a Collection Key, a private key, or plaintext — `family_wide_kind` is a plain enum flag and `invitation_family_wide_keys.wrapped_collection_key` is the same opaque WrappedKey-shaped blob the existing `wrapped_collection_key` column already stores."
    - "At most one item_bucket-kind collection can exist per family, enforced at the DB level by idx_one_item_bucket_per_family (a partial unique index scoped to family_wide_kind='item_bucket') -- a folder-kind collection is correctly left unbounded per family, only the bucket is constrained."
  artifacts:
    - ".planning/phases/30-the-living-group-family-wide-sharing/30-DECISION-FSH-02.md"
    - ".planning/PROJECT.md (new Key Decisions row: FSH-02)"
    - "crates/pv-server/migrations/0019_family_wide_sharing.sql (family_wide_kind column, idx_one_item_bucket_per_family partial unique index, invitation_family_wide_keys table)"
  key_links:
    - "PROJECT.md's new FSH-02 row cites the same rationale as the dedicated decision doc — one recorded decision, two discoverable locations, never two different answers."
    - "0019's `family_wide_kind` column and `invitation_family_wide_keys` table are exactly what the decision record commits to (Path A: additive sibling table, not a widened array column on the existing singular `invitations` columns)."
  prohibitions:
    - statement: "The server MUST NOT persist or receive a Collection Key, a private key, or plaintext anywhere on the family-wide-sharing path."
      status: "enforced by schema shape in this plan (family_wide_kind is a plain CHECK-constrained TEXT flag; wrapped_collection_key columns are opaque blobs, unchanged shape from the existing 0017 migration)."
      verification: "manual — 30-02's checkpoint:decision and 30-14's adversarial test independently confirm no later plan widens this."
    - statement: "`Collection`/`Item::resolve_access` MUST NOT be widened to admit family members without a key row."
      status: "not touched by this plan — recorded here so no later plan in this phase forgets it."
      verification: "manual — re-asserted in 30-02's checkpoint:decision."
    - statement: "`FamilyTab.tsx` MUST NOT be restyled or re-laid-out — Phase 33 owns it."
      status: "no plan in this phase's `files_modified` list touches `web/src/components/settings/FamilyTab.tsx` (or wherever the shipped family tab currently lives) — recorded here as the phase-wide boundary every later plan inherits."
      verification: "manual — cross-check every plan's `files_modified` list against this path at phase seal."
    - statement: "`AvatarStack.tsx`'s VIS-01 null-on-empty defect MUST NOT be fixed here — Phase 34 owns it; the family badge is independent of it by construction."
      status: "30-11 builds the family badge as a standalone component reading synchronous collection metadata, never `AvatarStack`'s own `useShareRecipients` data flow or its early-`null` branch — no plan in this phase edits `AvatarStack.tsx`."
      verification: "manual — 30-11's own diff touches no line of `AvatarStack.tsx`."
---

<objective>
Land the FSH-02 decision record — naming the chosen family-wide key-delivery mechanism, the
alternatives rejected on their merits, and the honesty caveat about what "automatically" can and
cannot mean — as its own standalone commit, structurally BEFORE any task in this phase (or any
later plan) that depends on it. This is SC1's own evidence bar: verifiable by `git log` commit
order, not by intent. Then land the additive schema this decision requires: `collections.family_wide_kind`
and the `invitation_family_wide_keys` table, so every later plan in this phase builds on a real,
committed data model rather than a private local assumption.

Purpose: every other plan in Phase 30 depends on this plan (directly or transitively) precisely so
the commit-order guarantee holds structurally, not by discipline.
Output: `30-DECISION-FSH-02.md`, a new PROJECT.md Key Decisions row, and migration `0019`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-the-living-group-family-wide-sharing/30-CONTEXT.md
@.planning/phases/30-the-living-group-family-wide-sharing/30-RESEARCH.md
@crates/pv-server/migrations/0014_family_sharing.sql
@crates/pv-server/migrations/0017_invitations.sql
</context>

<tasks>

<task type="tracer">
  <name>Task 1: FSH-02 decision record — its own commit, before anything depends on it</name>
  <files>.planning/phases/30-the-living-group-family-wide-sharing/30-DECISION-FSH-02.md, .planning/PROJECT.md</files>
  <read_first>
    30-RESEARCH.md's "FSH-02 Mechanism — Decision Record Groundwork" section in full (the
    generalization-path analysis, the single-other-member failure case, the alternatives table);
    30-CONTEXT.md's "Claude's Discretion" block (the starting hypothesis this record confirms or
    overturns); PROJECT.md's Key Decisions table rows for KEY-05 (line 212) and EXT-10 (line 213) —
    match their shape and depth exactly (named alternatives, named rejection reasons, named residual
    limitations, `Decided (Phase N)` outcome).
  </read_first>
  <action>
    Write `30-DECISION-FSH-02.md` as a dedicated, standalone decision record (this is the file SC1's
    commit-order check targets via `git log --oneline -- <this-path>`). It must:
    (1) Name the CHOSEN mechanism precisely: a hybrid of invite-time wrap (every family-wide
    collection's key is re-wrapped into a new invite at generation time, folding into the existing
    `generateInviteLink`/`invitations::create` flow) plus lazy reseal (an unwrap-own-key /
    reseal-to-one-new-recipient composition, triggered opportunistically on any current keyholder's
    own unlock/hydrate — INCLUDING the sharer's own subsequent app usage, not scoped to "some other
    member") as the fallback for a family-wide share created after an invite was generated but before
    it was redeemed.
    (2) Name and reject, on their merits, at minimum: (a) wrapping the Collection Key into the invite
    using the invite code as the shared secret ALONE, with no lazy-reseal fallback — rejected because
    a family-wide share created after invite generation is structurally absent from that invite's
    fixed-at-INSERT-time payload for the invite's entire remaining lifetime (state this as a measured
    fact, citing `invitations.rs::create`'s one-time INSERT, never re-computed); (b) lazy reseal by
    "the next member whose client comes online" scoped to exclude the sharer — rejected as unnecessarily
    fragile: the sharer always already holds the Collection Key by construction (the existing
    multi-recipient fan-out grants every CURRENT member a key at share-creation time), so excluding
    them from the trigger set manufactures a single-point-of-failure that including them removes;
    (c) server-side re-key on every new member join — rejected as a direct FSH-03 violation (the
    server would need to hold or generate a Collection Key); (d) a snapshot-of-current-members-only
    model with no living-group semantics — rejected, already foreclosed by the product decision
    recorded in REQUIREMENTS.md's FSH header, not re-litigated here; (e) a per-family symmetric key
    shared by every member directly — rejected because it cannot be revoked from one member without
    rotating for everyone, breaking the per-recipient sealed-key model KEY-06/KEY-07 already proved
    correct.
    (3) State the user-visible caveat explicitly: invite-carried delivery is genuinely immediate
    ("automatically" MAY mean "instantly" for that specific case); lazy-reseal delivery is NOT
    instant — it arrives on the next completed sync/unlock by ANY current keyholder (the sharer
    included), and the shipped UI copy (30-UI-SPEC.md's `share.familyWideTimingCaveat`) must state
    this bound, never an unqualified "automatically"/"instantly" covering both cases.
    (4) Record the refinement this research confirmed over the starting hypothesis: the lazy-reseal
    trigger set is "any current keyholder's own session", not "another member" — this narrows the
    single-other-member failure case CONTEXT.md worried about to "every keyholder, including the
    sharer, permanently stops using the product" — an orthogonal condition, not a design defect.
    (5) Record the concrete data-model consequences this decision commits Task 2 (and every later
    plan) to, so the schema and every later implementation task can cite this record instead of
    re-deriving it: `collections.family_wide_kind` (`NULL` = ordinary collection, `'folder'` = a named
    family-wide folder, `'item_bucket'` = the one per-family auto-created collection that holds bare
    items shared family-wide — chosen as a single column over two independent booleans specifically
    because it makes "is family-wide AND which kind" a single, unambiguous read); the invite wire
    contract is widened via Path A (RESEARCH.md's own recommendation) as an ADDITIVE sibling table
    `invitation_family_wide_keys` (never a widened/repurposed existing column, per RESEARCH.md's own
    Pitfall 2) so every already-issued single-collection invite's shape is untouched; a NEW, narrow,
    ids-only discovery endpoint (`GET /api/families/family-wide-pending`) is required because
    `Collection`/`Item::resolve_access` correctly 404 a keyless caller regardless of family
    membership, and that 404 must NEVER be weakened — the record states explicitly that widening
    `resolve_access` was considered and rejected, since it would silently break the revocation
    guarantee those functions exist to provide.
    Then add ONE new row to PROJECT.md's `## Key Decisions` table (after the EXT-10 row), condensing
    the same five points into the existing `Decision | Rationale | Outcome` shape, `Outcome` =
    `Decided (Phase 30)`. Use `Edit`, not `Write`, against PROJECT.md — this is a scoped, single-row
    insertion into an existing table, never a whole-file rewrite.
  </action>
  <verify>
    <automated>set -o pipefail; git log --oneline -1 -- .planning/phases/30-the-living-group-family-wide-sharing/30-DECISION-FSH-02.md</automated>
  </verify>
  <acceptance_criteria>
    `30-DECISION-FSH-02.md` exists and names all five alternatives from RESEARCH.md's own
    "Alternatives to name and reject" table (plus the sharer-inclusion refinement) with a stated
    rejection reason for each; PROJECT.md's Key Decisions table has exactly one new row citing
    `FSH-02`; `git log --oneline -- .planning/phases/30-the-living-group-family-wide-sharing/30-DECISION-FSH-02.md`
    shows this task's commit as the ONLY commit touching that path so far in this phase.
  </acceptance_criteria>
  <done>The decision record and its PROJECT.md row are committed, alone, in this task's own commit — no other file in this task's diff.</done>
</task>

<task type="auto">
  <name>Task 2: Additive schema — `family_wide_kind` + `invitation_family_wide_keys`</name>
  <files>crates/pv-server/migrations/0019_family_wide_sharing.sql</files>
  <read_first>
    `crates/pv-server/migrations/0014_family_sharing.sql` (the `collections`/`collection_keys` table
    shapes and this schema's own additive-migration header-comment convention) and
    `crates/pv-server/migrations/0017_invitations.sql` (the `invitations` table's all-or-nothing CHECK
    constraint on `collection_id`/`access_level`/`wrapped_collection_key` — the new sibling table must
    NOT touch or repurpose these three columns, per this task's own decision record, Task 1);
    `30-DECISION-FSH-02.md` (this task's own dependency, written in Task 1) for the exact column/table
    shape it commits to.
  </read_first>
  <action>
    Create `crates/pv-server/migrations/0019_family_wide_sharing.sql` with a header comment matching
    0014/0017/0018's convention (cites FSH-01..05/FAM-10, states additive-only, cites the decision
    record by filename). Add: `ALTER TABLE collections ADD COLUMN family_wide_kind TEXT CHECK
    (family_wide_kind IN ('folder', 'item_bucket'));` — nullable, so every existing collection row
    (today's exact single-recipient-list behavior) is unchanged. Add `CREATE UNIQUE INDEX
    idx_one_item_bucket_per_family ON collections(family_id) WHERE family_wide_kind = 'item_bucket';`
    — a SQLite partial unique index, enforcing "at most one `item_bucket`-kind collection per family"
    at the DB level (never a `'folder'`-kind collection, which correctly stays unbounded per family —
    the index's `WHERE` clause scopes it to exactly the one kind that needs the constraint). This is
    what makes 30-12's "never two buckets" claim genuinely true rather than merely likely: a second
    concurrent `INSERT` from a racing member's `createCollection(..., 'item_bucket')` call now fails
    the uniqueness constraint server-side instead of silently succeeding, and 30-12's own
    `findOrCreateFamilyItemBucket` must treat that failure as "someone else just won the race, re-list
    and use theirs" rather than as a hard error. Add a new table `invitation_family_wide_keys
    (invitation_id TEXT NOT NULL REFERENCES invitations(id) ON DELETE
    CASCADE, collection_id TEXT NOT NULL REFERENCES collections(id) ON DELETE CASCADE, access_level
    TEXT NOT NULL CHECK (access_level IN ('read','edit','hidden_password')), wrapped_collection_key
    TEXT NOT NULL, PRIMARY KEY (invitation_id, collection_id))` plus `CREATE INDEX
    idx_invitation_family_wide_keys_invitation ON invitation_family_wide_keys(invitation_id);` — one
    row per family-wide collection wrapped into a given invite at generation time, mirroring
    `collection_keys`'s own composite-PK, per-relationship-row shape rather than a JSON array column.
    Do not touch `invitations`' existing singular `collection_id`/`access_level`/`wrapped_collection_key`
    columns or their CHECK constraint at all.
  </action>
  <verify>
    <automated>set -o pipefail; cd crates/pv-server && cargo test --lib membership:: 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    `cargo test --lib membership::` is green — every one of its tests calls `seeded_pool()`, which
    runs `sqlx::migrate!("./migrations").run(&pool)` against a fresh in-memory DB, so a syntax error
    or FK/CHECK-constraint defect anywhere in `0019_family_wide_sharing.sql` fails the WHOLE suite at
    pool setup, before any individual test body runs -- this is what "applies cleanly against a fresh
    DB after 0001..0018" is actually evidenced by, not asserted from. Additionally, by manual
    inspection of a temp DB migrated with `sqlx::sqlite::SqlitePoolOptions` in a throwaway
    `#[tokio::test]` (or via the `sqlite3` CLI against a copy of that same freshly-migrated file):
    `PRAGMA table_info(collections)` shows `family_wide_kind` as a nullable TEXT column; inserting a
    `family_wide_kind` value outside `('folder','item_bucket')` fails the CHECK constraint; a SECOND
    `INSERT INTO collections (..., family_wide_kind) VALUES (..., 'item_bucket')` for the SAME
    `family_id` fails `idx_one_item_bucket_per_family`'s uniqueness constraint, while a second
    `'folder'`-kind insert for the same family succeeds (the constraint is correctly scoped to
    `item_bucket` only); `invitation_family_wide_keys` exists with the composite PK and both foreign
    keys.
  </acceptance_criteria>
  <done>Migration 0019 is committed and applies cleanly on top of every existing migration with zero changes to any existing row's behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration author → SQLite schema | A schema change is a one-way door once shipped against a live self-hosted `pv.db`; additive-only discipline is the mitigation, not a runtime boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | `0019_family_wide_sharing.sql` | medium | mitigate | Additive-only (new nullable column, new sibling table); no existing column renamed/repurposed/widened — the exact discipline RESEARCH.md's Pitfall 2 names and this task follows. |
| T-30-02 | Information Disclosure | `invitation_family_wide_keys.wrapped_collection_key` | low | accept | Same opaque-blob shape as the existing `invitations.wrapped_collection_key` column this project has already reasoned about (0017's own header comment) — never unwrapped server-side, carries no more sensitivity than what's already accepted. |

</threat_model>

<verification>
`git log --oneline -- 30-DECISION-FSH-02.md` shows exactly one commit, and it is the FIRST commit
in this phase's git history. `cargo sqlx migrate run` (or an equivalent temp-DB apply) succeeds
against the full migration chain including 0019.
</verification>

<success_criteria>
The decision record is committed alone, before the schema commit, before any other plan in this
phase runs. The schema exists, additive-only, matching exactly what the decision record commits to.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-01-SUMMARY.md` when done
</output>
