---
phase: 30-the-living-group-family-wide-sharing
plan: 08
type: execute
wave: 3
depends_on: ["30-02", "30-09"]
files_modified:
  - web/src/components/vault/ShareDialog.tsx
  - web/src/lib/i18n/dictionary.ts
autonomous: true
requirements: [FSH-01, FSH-05]

must_haves:
  truths:
    - "A distinct 'Cala rodzina' row, boxed (rounded-field border border-base-300), renders pinned above the individual recipient list in both the item and folder ShareDialog variants -- FSH-01 ordering."
    - "A family of exactly one (only the sharer, no other members) shows share.familyWideMemberCountSoloOwner copy, not the interpolated n=1 form of familyWideMemberCount -- FSH-01 empty."
    - "Checking the family-wide row clears and disables every individual recipient checkbox, and vice versa -- the two modes are mutually exclusive within one dialog session, structurally preventing a person from being both individually listed and covered by the family-wide share -- FSH-01 adjacency."
    - "The timing caveat (share.familyWideTimingCaveat) renders unconditionally whenever the family-wide row is visible, not only after it is checked."
    - "The member count text is one of exactly four states -- loading / solo-owner / populated(n>=2) / error -- never a flash of 0 or a stale value across a state transition, and reads correctly at n=1 and n>=2 with no declined-noun grammar ever governed by the count (the phase-29 '1 wpisów' trap, closed by construction) -- FSH-05 boundary (edge-probe)."
    - "Member-count and timing-caveat sentences wrap (never truncate) inside the dialog card -- the same non-truncating text behavior share.seedFolderSummary's existing paragraph already relies on -- distinct from the family-wide-share NAME list in the SharingOverviewPanel block (30-10), which truncates instead."
    - { statement: "Polish family-wide row/caveat copy wraps cleanly inside the 400px ShareDialog card at every member-count state, rather than overflowing or being silently assumed to fit -- live-measured in 30-17, not merely asserted by class name here.", verification: backstop }
    - "A current family member with no published identity public key at family-wide share-creation time is OMITTED from the creation-time grant (never throws, never aborts the whole share) and surfaces via the SAME missing/lazy-reseal path a gap-window invitee already uses once they publish a key -- distinct from the individual-recipient path, where a missing key still throws before any network call (T-25-16, unchanged)."
  artifacts:
    - "web/src/components/vault/ShareDialog.tsx (isFamilyWideSelected state, family-wide row JSX, member-count fetch/state machine, submitFolderVariant's family-wide branch)"
    - "web/src/lib/i18n/dictionary.ts (share.familyWideOptionLabel, share.familyWideTimingCaveat, share.familyWideMemberCount, share.familyWideMemberCountSoloOwner, share.familyWideMemberCountLoading, share.familyWideMemberCountError)"
  key_links:
    - "The family-wide branch of submitFolderVariant creates the collection with family_wide_kind: 'folder' and seals to EVERY current active family member (from getFamilyMembers(), excluding the caller), never the UI's selectedRecipientIds set -- the checkbox selects a MODE, not a recipient list, once family-wide is chosen."
  prohibitions:
    - statement: "No per-person access override may be layered on a family-wide share in this phase."
      status: "enforced structurally by the mutual-exclusivity toggle -- there is no UI state in this dialog where an individual recipient and the family-wide row are simultaneously selected."
      verification: "component test in this plan asserts the toggle behavior directly."
    - statement: "The share dialog's final per-person layout is Phase 31's -- this phase adds the family-wide target, not the dialog's shape."
      status: "this plan inserts the family-wide row ABOVE the existing share-recipient-list block, unchanged in its own layout -- no restyling of the existing per-person rows."
      verification: "manual -- code review of this plan's diff confirms zero edits inside the existing recipient-list/access-level JSX blocks beyond the new sibling elements."
---

<objective>
Add the "Cała rodzina" recipient row to `ShareDialog` -- a distinct, pinned, boxed row above the
individual recipient list, mutually exclusive with per-person selection, carrying its own honest
member-count and timing-caveat copy -- and wire the folder variant's submit path to grant every
CURRENT active family member when it is selected. The item variant's family-wide handling (the
auto-created bucket collection) is 30-11's job, sequenced after this plan since both touch the same
file.

Purpose: this is FSH-01's UI half for the folder path -- "share with the whole family in one
action, without ticking each person."
Output: the "Cała rodzina" row per 30-UI-SPEC.md's exact anatomy; `submitFolderVariant`'s new
family-wide branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-the-living-group-family-wide-sharing/30-UI-SPEC.md
@web/src/components/vault/ShareDialog.tsx
@web/src/lib/families/api.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: the "Cała rodzina" row, member-count states, timing caveat, mutual exclusivity</name>
  <files>web/src/components/vault/ShareDialog.tsx, web/src/lib/i18n/dictionary.ts</files>
  <read_first>
    `ShareDialog.tsx` lines 239-372 (component state: `selectedRecipientIds`, `toggleRecipient`, the
    `useEffect` load sequence that already calls `getFamilyMembers()`) and lines 729-772 (the exact
    JSX insertion point -- immediately above the `data-testid="share-recipient-list"` block, in both
    variants, per 30-UI-SPEC.md's own anatomy); 30-UI-SPEC.md's "Share Dialog -- 'Cała rodzina' Row
    Contract" section verbatim (the exact markup, testids `share-recipient-family-wide` /
    `share-family-wide-member-count` / `share-family-wide-timing-caveat`, and the "member count
    INCLUDES the sharer" rule).
  </read_first>
  <action>
    Add `isFamilyWideSelected` state (boolean, default `false`) and a `familyMemberCountState`
    discriminated value (`"loading" | { count: number } | "error"`, derived from the SAME
    `getFamilyMembers()` call the dialog's existing `load()` effect already makes -- no new fetch;
    count = `(members ?? []).length + 1` to include the caller per 30-UI-SPEC.md's explicit rule, or
    `"error"` if `load()`'s existing catch branch already set `accountUnavailable`). Insert the
    "Cała rodzina" `&lt;label data-testid="share-recipient-family-wide"&gt;` row exactly per
    30-UI-SPEC.md's anatomy (checkbox `checked={isFamilyWideSelected}`, `Users` icon, `text-sm
    font-bold` label using `share.familyWideOptionLabel`, member-count span using
    `share-family-wide-member-count` rendering `familyWideMemberCountSoloOwner` when
    `familyMemberCountState.count === 1`, the interpolated `familyWideMemberCount` when `&gt;= 2`,
    `familyWideMemberCountLoading` while loading, `familyWideMemberCountError` on error) immediately
    above `share-recipient-list`, in BOTH the item and folder JSX branches. Below it, the timing
    caveat `&lt;p id="share-family-wide-caveat" data-testid="share-family-wide-timing-caveat"&gt;`
    rendering `share.familyWideTimingCaveat` UNCONDITIONALLY whenever this row is visible (not gated
    on `isFamilyWideSelected`). Wire `toggleRecipient` and a new `toggleFamilyWide` handler so
    selecting one clears+disables the other's controls (native `disabled` attribute on every
    individual checkbox when `isFamilyWideSelected` is true, and on the family-wide checkbox when
    `selectedRecipientIds.size &gt; 0`). Update `submitDisabled` to accept `isFamilyWideSelected` as an
    alternative to `selectedRecipientIds.size &gt; 0`. Add the four new i18n keys to `dictionary.ts`
    with the exact pl/en strings from 30-UI-SPEC.md's Copywriting Contract
    (`share.familyWideOptionLabel`, `share.familyWideTimingCaveat`, `share.familyWideMemberCount`,
    `share.familyWideMemberCountSoloOwner`, `share.familyWideMemberCountLoading`,
    `share.familyWideMemberCountError`).
  </action>
  <verify>
    <automated>set -o pipefail; cd web && npx vitest run src/components/vault/ShareDialog.test.tsx -t "family-wide" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    A test asserts: checking family-wide disables every `share-recipient-{id}` checkbox and clears
    `selectedRecipientIds`; checking any individual recipient disables `share-recipient-family-wide`;
    a 1-member family renders `familyWideMemberCountSoloOwner`, not an interpolated "1"; the timing
    caveat renders before the checkbox is ever checked.
  </acceptance_criteria>
  <done>The family-wide row exists, is mutually exclusive with individual selection, and its member-count/caveat copy matches the locked four-state contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: submitFolderVariant's family-wide branch -- grant every current active member</name>
  <files>web/src/components/vault/ShareDialog.tsx</files>
  <read_first>
    `ShareDialog.tsx` lines 440-568 (`submitFolderVariant` in full -- the existing
    `createCollection`/per-recipient `addCollectionMember` loop this branch must reuse, changing only
    WHICH recipients get sealed to and the `family_wide_kind` field on `createCollection`'s request);
    `handleSubmit` at lines 570-613 (where `submitFolderVariant` is invoked -- must pass
    `isFamilyWideSelected` through); `web/src/lib/vault/api.ts`'s `createCollection()` signature
    (30-09 adds its optional `familyWideKind` parameter -- this task is the first real caller).
  </read_first>
  <behavior>
    - Given `isFamilyWideSelected === true` and 3 other active family members exist, all with
      published public keys (roster from `getFamilyMembers()`): `submitFolderVariant`'s family-wide
      branch calls `createCollection` with `family_wide_kind: "folder"`, then seals and grants to all
      3 (not merely whatever was in `selectedRecipientIds`, which is empty in this mode) -- assert
      via a spy on `addCollectionMember` call count and recipient ids.
    - Given `isFamilyWideSelected === true` and ONE of those 3 members has NO published public key
      yet (`public_key: null`, e.g. they've joined the family but never unlocked the vault on any
      device): the family-wide branch does NOT throw and does NOT abort the share -- it grants the
      OTHER 2 members normally and simply OMITS the keyless member from this creation-time grant.
      This is a deliberate divergence from `assertRecipientsHavePublicKeys`/T-25-16's own
      throw-before-network discipline (which exists for the INDIVIDUAL-recipient case, where the user
      explicitly picked someone and a silent drop would defeat their explicit choice) -- a
      family-wide share's recipient set is never explicitly picked per-person, so a keyless member is
      structurally the SAME shape as a not-yet-joined member (CONTEXT.md's own "includes people who
      have not joined yet" framing extends naturally to "includes people who joined but haven't
      published a key yet"). The keyless member surfaces via `GET
      /api/families/family-wide-pending`'s `missing` list (they hold no `collection_keys` row) the
      moment they DO publish a keypair and any keyholder's session runs 30-13's reseal trigger --
      resolved by the SAME lazy-reseal mechanism the gap-window invite case already uses, no new
      machinery.
    - Given `isFamilyWideSelected === false`: behavior is byte-identical to today (the existing
      `selectedRecipientIds`-driven per-person loop, `assertRecipientsHavePublicKeys`'s
      throw-before-network discipline UNCHANGED for that path, `family_wide_kind` omitted from the
      request).
  </behavior>
  <action>
    Extend `submitFolderVariant`'s signature to accept `isFamilyWide: boolean` (or derive the
    recipient set at the `handleSubmit` call site: when `isFamilyWideSelected`, pass `(await
    getFamilyMembers()) ?? []` filtered to exclude the caller's own id, in place of `selected`, plus a
    `familyWideKind: "folder"` flag threaded into the existing `createCollection` call's request
    body). Thread `family_wide_kind` into the `createCollection` call only when this branch is active
    (existing non-family-wide creations keep omitting the field entirely, per 30-02's additive
    contract). For the family-wide recipient set specifically, filter OUT any member with
    `public_key === null`/`undefined`/`""` BEFORE the per-recipient `addCollectionMember` loop runs
    (never call `assertRecipientsHavePublicKeys` on the family-wide set -- that guard's throw
    semantics are for the individual-recipient path only, kept unchanged there). The existing
    per-recipient `addCollectionMember` loop, seed-move logic, and `SubmitOutcome` reporting are
    otherwise REUSED unchanged -- this task changes the recipient SOURCE and its keyless-member
    filter, not the granting mechanism.
  </action>
  <verify>
    <automated>set -o pipefail; cd web && npx vitest run src/components/vault/ShareDialog.test.tsx -t "family-wide folder" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    All three behavior bullets pass; the existing non-family-wide `submitFolderVariant` tests
    (including its own `assertRecipientsHavePublicKeys`-throws case) remain green unmodified.
  </acceptance_criteria>
  <done>Selecting "Cała rodzina" for a folder share creates a family_wide_kind='folder' collection and grants every current active family member, reusing the existing granting/seed-move machinery unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ShareDialog's recipient source | Same boundary as the existing per-person flow -- recipients still come from the server-attested `getFamilyMembers()` roster, never a client-fabricated list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-15 | Tampering | family-wide recipient set substitution | low | mitigate | The family-wide branch derives its recipient list from the SAME `getFamilyMembers()` call the dialog already trusts for individual selection -- no new trust source, no client-supplied recipient list bypassing server attestation. |

</threat_model>

<verification>
`npx vitest run src/components/vault/ShareDialog.test.tsx`
</verification>

<success_criteria>
The "Cała rodzina" row exists per the locked UI-SPEC contract; selecting it for a folder share
grants every current active member through the existing, unmodified granting machinery.
</success_criteria>

<output>
Create `.planning/phases/30-the-living-group-family-wide-sharing/30-08-SUMMARY.md` when done
</output>
