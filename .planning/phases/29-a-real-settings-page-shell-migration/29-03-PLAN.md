---
phase: 29-a-real-settings-page-shell-migration
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - web/src/app/page.tsx
  - web/src/app/page.test.tsx
  - web/src/components/shell/Sidebar.tsx
  - web/src/components/shell/Sidebar.test.tsx
  - web/src/components/settings/SettingsPanel.tsx
  - web/src/components/settings/SettingsPanel.test.tsx
autonomous: true
requirements: [SET-01, SET-02]
user_setup: []

must_haves:
  truths:
    - "SET-01 (ext deep-link): the shipped 0.4.0 extension's ${baseUrl}/?panel=settings link still lands its users on /settings, without a store update -- verified against the literal, unchanged href in ExtUnlockBridge.tsx."
    - "The sidebar gear is a real, middle-click/open-in-new-tab-capable link to /settings -- SET-01's own point, applied to the entry point users actually click."
    - "E6 populated: the gear icon and its aria-label are unchanged -- only the element type changes from <button onClick> to a real link."
    - "E4/E6 empty/loading/error/partial: the sidebar settings entry has no data source and therefore no such states."
    - "page.tsx no longer offers a second, parallel path to settings content (the old drawer) once this plan lands -- SettingsPanel.tsx and its mount site are both gone."
  artifacts:
    - web/src/app/page.tsx (AuthGate-based, settingsOpen/handleOpenSettings/SettingsPanel removed)
    - web/src/components/shell/Sidebar.tsx (gear as <Link href="/settings">)
  key_links:
    - "page.tsx -> AuthGate: page.tsx's own authed branch is now the shared component from Plan 29-01, not a second inline copy"
    - "page.tsx's ?panel=settings redirect -> /settings (Plan 29-01's route): the redirect target must be the real, already-built route, not a placeholder"
  prohibitions:
    - statement: "No role=\"tablist\"/role=\"tab\"/aria-selected/tabs classes may survive anywhere in the shipped app after this plan."
      status: enforced
      verification: "SettingsPanel.tsx is deleted entirely (not left as dead/unused code); grep for these strings across web/src returns zero matches outside node_modules."
    - statement: "No test may be deleted or weakened."
      status: enforced
      verification: "The Sidebar.test.tsx callback-based test becomes a real <a>/href assertion -- a stronger check (real navigation semantics), not a weaker one; SettingsPanel.test.tsx's 6 tests are removed only because the mechanism they test (tab switching) is deleted, not to hide a regression -- their behavioral intent (\"every action reachable\") is already re-proven by Plan 29-01's settings/page.test.tsx."
---

<objective>
Cut over from the old settings-drawer world to the new `/settings` route: `page.tsx` uses the shared
`AuthGate` (retiring its own duplicate inline branch), the `?panel=settings` deep link becomes a real
navigation instead of a drawer-open, the sidebar gear becomes a real link, and `SettingsPanel.tsx` --
the tab shell the UI-SPEC forbids anywhere on `/settings` -- is deleted outright.

Purpose: SET-01 (the deep-link + sidebar-gear halves of "real, linkable route"), SET-02 (no dangling
second path to the surfaces that moved).
Output: `page.tsx`/`Sidebar.tsx` using the new route; `SettingsPanel.tsx` retired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-CONTEXT.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-RESEARCH.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-PATTERNS.md
</context>

## Artifacts this phase produces

No new symbols -- this plan is a cutover/removal:
- `page.tsx` gains a `next/navigation` `useRouter` usage (redirect only, no new exported symbol)
- `Sidebar.tsx`'s `sidebar-open-settings` element changes from `<button>` to `next/link`'s `<Link>` (same testid, same aria-label -- no new identifier)
- REMOVED: `web/src/components/settings/SettingsPanel.tsx`, `web/src/components/settings/SettingsPanel.test.tsx`, `Sidebar`'s `onOpenSettings` prop, `page.tsx`'s `settingsOpen`/`handleOpenSettings`

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: page.tsx -- AuthGate wiring, real redirect, retire settingsOpen/SettingsPanel</name>
  <files>web/src/app/page.tsx, web/src/app/page.test.tsx</files>
  <read_first>web/src/app/page.tsx (full file, 452 lines -- especially lines 48-56 the PendingUrlAction type, lines 96-108 pendingUrlAction's useState initializer, lines 202-209 handleOpenSettings, lines 332-360 the authed branch being replaced, line 313 the settingsOpen render call, line 362 Sidebar's onOpenSettings prop), web/src/lib/auth/AuthGate.tsx (created in Plan 29-01 -- its exact `{ children, onRegistered }` contract), web/src/app/page.test.tsx (full file, especially lines 177-225 the panel=settings/action=new-item describe block)</read_first>
  <action>
Replace `page.tsx`'s inline `if (authed === null) return null; if (!authed) { ... }` branch (the
`AuthCard`/`LoginForm`/`RegisterForm` JSX at lines ~332-360) with `<AuthGate onRegistered={() => { if
(!isOnboardingComplete()) setShowOnboarding(true); }}>{/* existing authed-true JSX below,
unmodified */}</AuthGate>` -- import `AuthGate` from `@/lib/auth/AuthGate`, using its exact `{
children, onRegistered }` contract as built in Plan 29-01 UNCHANGED (this is settled, not a choice --
`settings/page.tsx` already consumes `AuthGate` with zero props beyond `children`, so its signature
must not grow a second prop here).

Remove `page.tsx`'s own `authed`/`setAuthed`/`mode`/`setMode` local state and the
`getSessionToken()`-reading mount effect ENTIRELY (AuthGate owns all of this independently now) --
this resolves cleanly with NO pass-through and NO new `AuthGate` prop, once traced through the
component's actual control flow: `handleInviteDone`'s `setAuthed(true)` call is the one remaining
reference to the old `authed` state, and it is DEAD once `AuthGate` is in place, not something to
preserve. Reasoning (state this in a one-line code comment at the deletion site, so a future reader
does not reintroduce it): `<AuthGate>` is inside the component's main return, reached only AFTER the
`if (invite !== null) return <InviteLandingView .../>` early return -- so while the invite view is
showing, `<AuthGate>` has never mounted at all, and its own internal `authed` state has never been
resolved. The moment `handleInviteDone` calls `setInvite(null)` (invite redemption already completed
its own inline register/login, so a valid session token now exists), the NEXT render falls through
`invite !== null` for the first time and mounts `<AuthGate>` fresh -- `AuthGate`'s own mount effect
then reads `getSessionToken()` for the first time and correctly resolves `authed=true`, independent of
anything `page.tsx` does. Delete `handleInviteDone`'s `setAuthed(true)` line outright.

Add `import { useRouter } from "next/navigation";` and `const router = useRouter();`. Replace the
`pendingUrlAction` type's `{kind: "settings"}` variant and its handling: add a SEPARATE, simple
`useEffect(() => { if (typeof window === "undefined") return; const params = new
URLSearchParams(window.location.search); if (params.get("panel") === "settings") {
router.replace("/settings"); } }, [router])` that fires once at mount, independent of the `unlocked`
gate (the destination route handles its own auth/unlock gating via AuthGate + UnlockOverlay, so this
redirect does not need to wait for anything). Narrow `PendingUrlAction` to only the `{kind: "new-item";
type: ItemType | null} | null` shape (drop the `"settings"` variant) and simplify the existing
`pendingUrlAction`-driven `useEffect` (currently handling both kinds) to only handle `new-item`.

Remove: the `settingsOpen`/`setSettingsOpen` state, `handleOpenSettings` function, the `import
SettingsPanel from "@/components/settings/SettingsPanel";` line, the `{settingsOpen ? <SettingsPanel
onClose={...}/> : null}` render call, `settingsOpen` from the `sidePanelOpen` computation
(`const sidePanelOpen = selectedItem !== null || creating || settingsOpen;` -> drop the third
disjunct), and the `onOpenSettings={handleOpenSettings}` prop passed to `<Sidebar/>` (Sidebar no
longer accepts this prop after Plan 29-03 Task 2 -- if Task 2 has not landed yet within this same
plan's execution order, temporarily leave the prop passed as `undefined`-safe via optional chaining,
since Sidebar's existing `onOpenSettings?.()` call tolerates an absent prop; Task 2 removes the prop
type entirely).

In `page.test.tsx`: per the Test Migration Map, edit ONLY the 2 "panel=settings" tests inside the
"Home (page.tsx) — panel=settings / action=new-item query params" describe block ("opens the Settings
panel on mount..." and "waits for unlock before applying a pending panel=settings param...") --
replace their `mock-settings-panel` mount assertions with a navigation assertion against the new
`useRouter` mock (add `vi.mock("next/navigation", () => ({ useRouter: () => ({ replace:
mockRouterReplace, push: vi.fn() }) }))` with a hoisted `mockRouterReplace = vi.fn()`; assert
`expect(mockRouterReplace).toHaveBeenCalledWith("/settings")`). Since the redirect no longer waits for
`unlocked`, the second test's "waits for unlock" framing is now moot -- rewrite it to assert the
redirect fires immediately regardless of lock state (or delete it if it becomes a literal duplicate of
the first, in which case state that explicitly in the SUMMARY as a legitimate simplification, not a
silent test-count reduction). Leave the 2 "action=new-item" tests in the same describe block
completely untouched, per the Test Migration Map. Leave the "does nothing when neither query param is
present" test untouched even though its `mock-settings-panel` assertion becomes vacuously true.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `page.test.tsx` passes in full; the 2 edited tests assert `mockRouterReplace` was called with `"/settings"` instead of asserting a mounted `mock-settings-panel`.
    - `grep -v -E '^\s*//' web/src/app/page.tsx | grep -c "SettingsPanel\|settingsOpen\|handleOpenSettings"` returns 0 (an explanatory comment mentioning these retired names for context is fine; a live import, state variable, or function is not).
    - `grep -n "AuthGate" web/src/app/page.tsx` shows the import and its usage.
    - Every pre-existing authed/unauthed/onboarding-wizard behavior in `page.test.tsx` outside the panel=settings tests stays green unmodified.
  </acceptance_criteria>
  <done>page.tsx uses AuthGate exclusively, the ?panel=settings redirect navigates to /settings via router.replace, and page.test.tsx is fully green.</done>
  <reversibility rating="reversible">Behavioral refactor of an existing file, fully git-revertible; no data/schema/external-service implication.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sidebar gear becomes a real link; retire SettingsPanel.tsx entirely</name>
  <files>web/src/components/shell/Sidebar.tsx, web/src/components/shell/Sidebar.test.tsx, web/src/components/settings/SettingsPanel.tsx, web/src/components/settings/SettingsPanel.test.tsx</files>
  <read_first>web/src/components/shell/Sidebar.tsx (lines 573-583, the sidebar-open-settings button; lines 75-87, the onOpenSettings prop declaration), web/src/components/shell/Sidebar.test.tsx (lines 165-169, the onOpenSettings callback test)</read_first>
  <action>
In `Sidebar.tsx`: replace the `<button type="button" data-testid="sidebar-open-settings"
aria-label={t("aria.openSettings")} onClick={() => onOpenSettings?.()}>` element with `<Link
href="/settings" data-testid="sidebar-open-settings" aria-label={t("aria.openSettings")}>` (import
`Link` from `next/link` -- same rationale as Plan 29-01's back-to-vault link: a real `<a href>` in the
DOM for middle-click/new-tab, but a client-side transition on a plain click that preserves the
in-memory unlock singleton instead of forcing a re-unlock on every settings visit), keeping the exact
same `<Settings size={16} aria-hidden="true"/>` icon and `{t("settings.title")}` label children.
Remove the `onOpenSettings` prop from `Sidebar`'s destructured props and its TypeScript prop-type
object entirely (it becomes fully dead once this change lands).

Delete `web/src/components/settings/SettingsPanel.tsx` and `web/src/components/settings/SettingsPanel.test.tsx`
outright -- not commented out, not left as an unused/unimported file. Confirm no remaining import
references it anywhere (`grep -rn "SettingsPanel" web/src` should return zero matches after this task).

In `Sidebar.test.tsx`: replace the `"calls onOpenSettings when 'Ustawienia' is clicked in the account
dropdown"` test with an assertion that `sidebar-open-settings`'s `tagName` is `"A"` and its
`getAttribute("href")` is `"/settings"` -- matching this same file's own existing pattern for other
real-element assertions (e.g. the `"every nav item... is a real interactive button"` test's `.tagName`
checks). Remove the now-unused `onOpenSettings` mock/prop from this test file if it is not referenced
elsewhere.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/shell/Sidebar.test.tsx && test ! -f src/components/settings/SettingsPanel.tsx && test ! -f src/components/settings/SettingsPanel.test.tsx && echo "confirmed removed"</automated>
  </verify>
  <acceptance_criteria>
    - `Sidebar.test.tsx` passes in full; the replaced test asserts real `<a>` semantics (`tagName === "A"`, `href === "/settings"`), not a callback invocation.
    - `web/src/components/settings/SettingsPanel.tsx` and `.test.tsx` no longer exist on disk.
    - `grep -rn "SettingsPanel" web/src | grep -v -E ':\s*//'` returns zero matches (a historical comment naming the retired component elsewhere in the codebase is fine; any import or JSX usage is not).
    - `grep -rn 'role="tablist"\|role="tab"\|aria-selected' web/src | grep -v -E ':\s*//'` returns zero matches.
  </acceptance_criteria>
  <done>Sidebar's gear is a real link, SettingsPanel.tsx/.test.tsx no longer exist, and no reference to either remains anywhere in web/src.</done>
  <reversibility rating="reversible">Deletes a fully superseded file and changes one element's semantics; git-revertible, no data/schema implication.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `?panel=settings` query value -> redirect target | The redirect destination must remain a fixed literal string, never reflect the query value itself |
| Old drawer mount path -> removal | Once `SettingsPanel.tsx` is deleted, no code path may still reference it (a dangling import would be a build failure, not a silent security gap, but is checked here for completeness) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-29-06 | Information Disclosure | `page.tsx`'s redirect effect | low | accept | The redirect target is the hardcoded literal `"/settings"`, never derived from `params.get("panel")`'s value -- no open-redirect surface exists. |
| T-29-07 | Elevation of Privilege (stale second path) | `SettingsPanel.tsx` removal | medium | mitigate | Full deletion (not a dead/unreferenced file) plus a repo-wide grep for `SettingsPanel`/`role="tablist"` in this task's own acceptance criteria closes any risk of a second, unaudited path to settings content surviving the cutover. |

No new package-manager installs in this task -- no `T-29-SC` row required.
</threat_model>

<verification>
`cd web && npx vitest run src/app/page.test.tsx src/components/shell/Sidebar.test.tsx` green, then
`cd web && npm test` (full suite -- this is the first point in the phase where the OLD settings
drawer is fully gone, so this is the most important full-suite checkpoint before the e2e fixes in
Plans 29-04/29-05), then `cd web && npm run build` (confirm the app still builds cleanly with the
route change).
</verification>

<success_criteria>
`page.tsx` has exactly one path to authenticated content (via `AuthGate`) and zero remaining
references to the old settings drawer; the sidebar gear and the `?panel=settings` deep link both land
real users on the real `/settings` route.
</success_criteria>

<output>
Create `.planning/phases/29-a-real-settings-page-shell-migration/29-03-SUMMARY.md` when done
</output>
