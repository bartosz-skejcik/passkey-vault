---
phase: 29-a-real-settings-page-shell-migration
plan: 06
type: execute
wave: 5
depends_on: ["29-01", "29-02", "29-03", "29-04", "29-05"]
files_modified:
  - web/src/components/settings/SettingsSectionData.test.tsx
  - web/src/components/settings/SettingsJumpNav.test.tsx
  - web/e2e/settings-jumpnav-labels.spec.ts
  - .planning/phases/29-a-real-settings-page-shell-migration/29-03-PLAN.md
autonomous: true
requirements: [SET-02, SET-04]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "SET-02: web/src/components/settings/SettingsSectionData.test.tsx exists and asserts that clicking settings-import-cta mounts ImportWizard and that ImportWizard's onDone unmounts it -- the verbatim successor to SettingsPanel.test.tsx's deleted 'replaces the Phase 3 placeholder with working Import/Export CTAs...' assertion (29-VERIFICATION.md gap 1)."
    - "SET-02: the same file asserts that clicking settings-export-cta mounts ExportDialog and that ExportDialog's onClose unmounts it at unit level -- previously proven only live, inside export-disclosure.spec.ts's two-session sharing e2e."
    - "SET-04: SettingsJumpNav.test.tsx's new scroll-spy adjacency tests prove Konto is the sole active jump-nav link before any IntersectionObserver entry fires, that firing an entry for a straddled section activates exactly that section's link and no other, and that a callback firing with nothing intersecting leaves the previously-active link active -- never two active links at once, never zero (29-VERIFICATION.md's behavior_unverified_items, 29-01-PLAN.md's SET-04 adjacency truth)."
    - "SET-04: the jump-nav Polish-label backstop declared in 29-01-PLAN.md ('all four PL labels render without clipping at 375px and inside the 200px desktop rail') is now backed by a genuinely re-runnable, CI-wired live Playwright spec (web/e2e/settings-jumpnav-labels.spec.ts) that measures real Chromium layout at both viewports -- not an ad-hoc, unretained measurement from a commit message."
    - "29-03-PLAN.md's two false verification claims (the unscoped role=tablist grep, and the 'already re-proven by settings/page.test.tsx' claim) are corrected in place, scoped to what is actually true, without touching SharingOverviewPanel.tsx or altering any statement/prohibition/task this plan's own diff didn't target (29-VERIFICATION.md gap 4 and gap 1's docs half)."
  artifacts:
    - web/src/components/settings/SettingsSectionData.test.tsx
    - web/e2e/settings-jumpnav-labels.spec.ts
  key_links:
    - "SettingsSectionData.test.tsx -> SettingsSectionData.tsx: mocks ImportWizard/ExportDialog exactly as SettingsSectionAccount.test.tsx already mocks DeleteAccountDialog (shallow, testid-based mount/unmount contract), asserting the same behavioral intent the deleted SettingsPanel.test.tsx assertion proved."
    - "SettingsJumpNav.test.tsx's new adjacency tests -> SettingsJumpNav.tsx's IntersectionObserver callback: captures the constructor's callback argument directly (the component exposes no other hook into activeSlug) and invokes it with synthetic { isIntersecting, target: { id } } entries."
    - "settings-jumpnav-labels.spec.ts -> web/e2e/fixtures.ts's newBareContext: a second context.addInitScript overrides fixtures.ts's own pv-locale=en default to pl, since this spec's entire point is the Polish label strings."
---

<objective>
Close the four concrete gaps 29-VERIFICATION.md found in an otherwise-passing Phase 29 verification
(score 4/5, `status: gaps_found`): one genuinely lost test assertion (SET-02, the Import CTA wiring),
one asserted-but-unproven behavior (SET-04 scroll-spy adjacency), one backstop with no retained
evidence (SET-04 jump-nav PL label fit), and one false verification claim shipped in 29-03-PLAN.md's
docs. This is a closure plan, not an improvement pass -- it touches only what 29-VERIFICATION.md
named, adds no new production behavior, and does not revisit anything already marked verified.

Purpose: restore SC2's "no test deleted or weakened" guarantee to fact, and replace two
unretained/unproven claims with real, re-runnable evidence.
Output: `SettingsSectionData.test.tsx` (new), an extended `SettingsJumpNav.test.tsx`, a new live
Playwright spec (`settings-jumpnav-labels.spec.ts`), and a corrected `29-03-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-VERIFICATION.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-01-SUMMARY.md
@web/src/components/settings/SettingsSectionData.tsx
@web/src/components/settings/SettingsJumpNav.tsx
@web/src/components/settings/SettingsSectionAccount.test.tsx
@web/e2e/settings-route.spec.ts
@web/e2e/fixtures.ts
</context>

## Artifacts this phase produces

New test files:
- `web/src/components/settings/SettingsSectionData.test.tsx` -- unit test, the verbatim successor to
  SettingsPanel.test.tsx's deleted Import/Export CTA wiring assertion.
- `web/e2e/settings-jumpnav-labels.spec.ts` -- live Playwright spec, the retained/re-runnable
  successor to the ad-hoc jump-nav PL-label measurement cited only in commit `8ef5f3e`.

Modified (not new):
- `web/src/components/settings/SettingsJumpNav.test.tsx` -- gains a second `describe` block for the
  scroll-spy adjacency invariant; its existing WR-08 `describe` block is untouched.
- `.planning/phases/29-a-real-settings-page-shell-migration/29-03-PLAN.md` -- two `verification:`
  strings and one `<acceptance_criteria>` bullet corrected in place; nothing else in that file changes.

## Debt to Record, Not Fix

29-VERIFICATION.md's Anti-Patterns table flags two pre-existing items that are explicitly OUT OF
SCOPE for this closure plan -- do not touch either. Record both in this plan's SUMMARY.md under a
"Known Debt (Not Fixed)" heading, verbatim:
1. `settings.importExportPlaceholder` (`web/src/lib/i18n/dictionary.ts:624`) is an orphaned i18n key
   with zero call sites -- pre-existing since Phase 6, not introduced by Phase 29.
2. `web/src/lib/vault/store.ts:1385-1400` -- if the shared item pipeline keeps failing with a
   non-404 error, `sharedConfirmed` never becomes true, so `hydrated` never becomes true, so
   `export-confirm` stays disabled for the rest of the session. The code comment claims a later
   background poll recovers from this; no test asserts that recovery. This is a deliberate
   fail-closed choice (better than a false zero-count), but the recovery half is prose-only --
   flag as debt for a future phase, do not add the missing test here.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SettingsSectionData.test.tsx -- the lost Import/Export CTA wiring assertion, restored</name>
  <files>web/src/components/settings/SettingsSectionData.test.tsx</files>
  <read_first>web/src/components/settings/SettingsSectionData.tsx (full file -- the two CTAs' testids `settings-import-cta`/`settings-export-cta`, the `showImportWizard`/`showExportDialog` state, and the conditional `ImportWizard`/`ExportDialog` mounts at the bottom), web/src/components/settings/SettingsSectionAccount.test.tsx (full file -- this is the mock-shape precedent to follow exactly: it shallow-mocks `./DeleteAccountDialog` as a `{ onClose }`-driven testid pair for the same reason -- the real dialog has its own dedicated test file, this file only proves the parent's wiring). Optional historical reference: `git show 1a3a2a0^:web/src/components/settings/SettingsPanel.test.tsx` shows the original deleted "replaces the Phase 3 placeholder with working Import/Export CTAs that open ImportWizard/ExportDialog" test this file succeeds -- not required reading since every assertion this task needs is specified below.</read_first>
  <action>
Create `web/src/components/settings/SettingsSectionData.test.tsx` from scratch. Import `describe`,
`expect`, `it`, `vi`, `afterEach` from `vitest` and `render`, `screen`, `fireEvent`, `cleanup` from
`@testing-library/react`.

Mock `@/lib/i18n/LocaleContext`'s `useLocale` to return `{ locale: "pl", setLocale: vi.fn(), t: (key:
string) => key }` -- identical shape to `SettingsSectionAccount.test.tsx`'s own mock.

Mock `../vault/ImportWizard` (relative from this file's own directory, matching
`SettingsSectionData.tsx`'s own import path) as a default export taking `{ onDone }: { onDone: () =>
void }`, rendering a `<div data-testid="mock-import-wizard">` containing one `<button type="button"
data-testid="mock-import-wizard-done" onClick={onDone}>done</button>`.

Mock `../vault/ExportDialog` the same way: default export taking `{ onClose }: { onClose: () => void
}`, rendering `<div data-testid="mock-export-dialog">` containing `<button type="button"
data-testid="mock-export-dialog-close" onClick={onClose}>close</button>`.

Both mocks reuse the EXACT testid strings the original, now-deleted `SettingsPanel.test.tsx` used for
this same wiring (`mock-import-wizard`, `mock-import-wizard-done`, `mock-export-dialog`,
`mock-export-dialog-close`) -- this is a deliberate, literal continuity marker, not a coincidence.
Add a comment above the mocks stating plainly that this file is the verbatim successor to
SettingsPanel.test.tsx's deleted "replaces the Phase 3 placeholder..." test (removed in commit
`1a3a2a0`), that 29-03-PLAN.md's prohibition wrongly claimed `settings/page.test.tsx` already
re-proved this intent (29-VERIFICATION.md gap 1 -- corrected by this same plan's Task 3), and that
ImportWizard/ExportDialog are shallow-mocked here for the identical reason
SettingsSectionAccount.test.tsx shallow-mocks DeleteAccountDialog: each has its own dedicated,
exhaustive test file already (`ImportWizard.test.tsx`, `ExportDialog.test.tsx`).

Import the default export from `./SettingsSectionData`. Add an `afterEach(() => cleanup())`.

Write a `describe("SettingsSectionData", ...)` block with exactly three `it`s:

1. "renders settings-section-dane with both CTAs, neither dialog mounted" -- render the component;
   assert `screen.getByTestId("settings-section-dane")`, `screen.getByTestId("settings-import-cta")`
   and `screen.getByTestId("settings-export-cta")` are each in the document; assert
   `screen.queryByTestId("mock-import-wizard")` and `screen.queryByTestId("mock-export-dialog")` are
   each NOT in the document.

2. "clicking settings-import-cta mounts ImportWizard; its onDone unmounts it" -- render; fire a click
   on `settings-import-cta`; assert `mock-import-wizard` is now in the document; fire a click on
   `mock-import-wizard-done`; assert `mock-import-wizard` is no longer in the document. This is the
   exact mount/unmount transition the deleted SettingsPanel test proved and no other test in the repo
   proves at unit level today.

3. "clicking settings-export-cta mounts ExportDialog; its onClose unmounts it" -- identical shape to
   test 2, against `settings-export-cta`/`mock-export-dialog`/`mock-export-dialog-close`. This test
   is a deliberate DUPLICATE of coverage that also exists live (`export-disclosure.spec.ts:295`) --
   that duplication is intentional: SC2's own reconciliation table already treats "proven live only"
   as a downgrade from "proven at both levels" for the sibling PasskeysTab/SessionsTab cases, and this
   task closes that same gap for the export half too, not only the import half 29-VERIFICATION.md
   named as unproven-by-anything.

State explicitly, in a comment near test 2, how this test would fail against the exact regression it
guards: if `SettingsSectionData`'s `onClick` on `settings-import-cta` were ever accidentally removed,
or `showImportWizard` never flowed into the conditional render, `screen.getByTestId("mock-import-wizard")`
would throw (element not found) immediately after the first `fireEvent.click` -- this is a real,
falsifiable assertion, not a shallow "it rendered" check.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/settings/SettingsSectionData.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vitest run src/components/settings/SettingsSectionData.test.tsx` passes with exactly 3 tests.
    - `grep -c "mock-import-wizard-done" web/src/components/settings/SettingsSectionData.test.tsx` is at least 2 (one mock definition, one `fireEvent.click` usage).
    - `grep -c "mock-export-dialog-close" web/src/components/settings/SettingsSectionData.test.tsx` is at least 2.
    - `grep -c "toBeInTheDocument" web/src/components/settings/SettingsSectionData.test.tsx` is at least 6 (proves multiple mount/unmount state assertions, not one shallow render check).
    - `grep -c "queryByTestId" web/src/components/settings/SettingsSectionData.test.tsx` is at least 3 (proves unmount/absence is actually asserted, not merely mount).
  </acceptance_criteria>
  <done>SettingsSectionData.test.tsx exists, covers both CTAs' full mount/unmount wiring at unit level, and passes -- the verbatim successor to SettingsPanel.test.tsx's deleted assertion.</done>
  <reversibility rating="reversible">New test file only; no production code touched; fully git-revertible.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SettingsJumpNav.test.tsx -- prove the scroll-spy exactly-one-active invariant</name>
  <files>web/src/components/settings/SettingsJumpNav.test.tsx</files>
  <read_first>web/src/components/settings/SettingsJumpNav.tsx (full file -- especially the `GROUPS` array's four slugs, `navItemClass`'s literal active-state class string `bg-primary/[0.08] text-primary`, and the `useEffect`'s `new IntersectionObserver((entries) => { const visible = entries.find((e) => e.isIntersecting); if (visible) setActiveSlug(visible.target.id); }, { rootMargin: "-40% 0px -55% 0px" })` construction -- this callback signature is exactly what the new tests must drive), web/src/components/settings/SettingsJumpNav.test.tsx (full file, current state -- the existing `RecordingIntersectionObserver` class and its one WR-08 test stay completely untouched; this task ADDS a second, independent `describe` block after it, never modifies the first).</read_first>
  <action>
Add a `screen` import to this file's existing `@testing-library/react` import line (currently only
`render, cleanup` are imported -- extend to `render, screen, cleanup`). Do not touch any other
existing import or the existing `RecordingIntersectionObserver` class, its `describe` block, or its
one `it`.

Below the existing WR-08 `describe` block, add a second `IntersectionObserver` mock class named
`CallbackCapturingIntersectionObserver`, implementing the `IntersectionObserver` interface the same
way `RecordingIntersectionObserver` does (`root = null`, `rootMargin = ""`, `thresholds = []`,
no-op `unobserve`/`disconnect`, `takeRecords` returning `[]`), but whose constructor stores its
`callback` argument on a `static lastCallback: IntersectionObserverCallback | null` field instead of
recording observed elements -- this is the only way to reach `SettingsJumpNav`'s internal
`activeSlug` state from outside, since the component exposes no other hook into it. `observe()` is a
no-op here too (the sections it would observe are irrelevant to this invariant -- only firing the
captured callback with synthetic entries matters).

Add a small helper function `fireIntersection(entries: Array<{ id: string; isIntersecting: boolean
}>)` that reads `CallbackCapturingIntersectionObserver.lastCallback`, throws a descriptive error if it
is `null` (the observer was never constructed -- signals the component's effect never ran), and
otherwise calls it with `entries.map((e) => ({ isIntersecting: e.isIntersecting, target: { id: e.id }
} as unknown as IntersectionObserverEntry))` and an empty-object second argument cast to
`IntersectionObserver` -- mirroring exactly the two-argument shape `SettingsJumpNav.tsx`'s own
callback destructures (`entries`, unused `observer`).

Add a second helper `activeLinkTexts(): string[]` that calls `screen.getAllByRole("link")`, filters to
elements whose `className` includes the literal active-state fragment `bg-primary/[0.08]` (the exact
string `navItemClass` emits only when `active` is true), and maps to each element's `textContent`.
Since the file's existing locale mock makes `t` return the key itself, each link's visible text is its
own translation key (`settings.groupAccount`, etc.) -- this makes `activeLinkTexts()` both identify
WHICH link is active and prove there is EXACTLY one, in a single array-equality assertion.

Add a new top-level `describe("SettingsJumpNav scroll-spy adjacency (SET-04)", ...)` block with three
`it`s. Each one starts by setting `(globalThis as any).IntersectionObserver =
CallbackCapturingIntersectionObserver;` (the file's existing `afterEach` already deletes the global
after every test, so each `it` must set it fresh) and then rendering `<SettingsJumpNav />`:

1. "Konto's link is the sole active link at scroll-top, before any IntersectionObserver entry fires"
   -- render only, no `fireIntersection` call; assert `activeLinkTexts()` deep-equals
   `["settings.groupAccount"]`. This proves the component's initial `activeSlug` state (`"konto"`)
   renders correctly with zero JS scroll-spy interaction yet.

2. "an entry marking one straddled section intersecting activates exactly that link, deactivating the
   previous one" -- call `fireIntersection([{ id: "dane", isIntersecting: true }, { id:
   "bezpieczenstwo", isIntersecting: false }])`; assert `activeLinkTexts()` deep-equals
   `["settings.groupData"]`. State in a comment that this fails against a regression where the
   previous active link (Konto) stays marked active alongside the new one, OR where both straddled
   entries end up marked active -- the real component's `entries.find((e) => e.isIntersecting)`
   picks exactly one winner by construction, and this assertion is what would catch a change that
   broke that.

3. "a callback firing with nothing intersecting leaves the previously active link active -- never
   zero mid-scroll" -- first call `fireIntersection([{ id: "dane", isIntersecting: true }])` and
   assert `activeLinkTexts()` deep-equals `["settings.groupData"]`; then call `fireIntersection([{
   id: "dane", isIntersecting: false }, { id: "bezpieczenstwo", isIntersecting: false }])` and assert
   `activeLinkTexts()` STILL deep-equals `["settings.groupData"]`. State in a comment that this fails
   against a regression where the component resets `activeSlug` to an empty/null value whenever no
   entry intersects -- the real component's `entries.find(...)` returning `undefined` and never
   calling `setActiveSlug` is exactly the behavior that SHOULD leave the previous state untouched,
   and a broken implementation that cleared it on every callback firing would make `activeLinkTexts()`
   return `[]` here, failing this assertion.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/settings/SettingsJumpNav.test.tsx 2>&1 | grep -E "4 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vitest run src/components/settings/SettingsJumpNav.test.tsx` reports 4 tests passed (the pre-existing WR-08 test plus 3 new).
    - `grep -c "RecordingIntersectionObserver" web/src/components/settings/SettingsJumpNav.test.tsx` is at least 1 -- proves the existing WR-08 class/test was not deleted or replaced.
    - `grep -c "scroll-spy adjacency" web/src/components/settings/SettingsJumpNav.test.tsx` equals 1 -- the new describe block exists exactly once.
    - `grep -c "activeLinkTexts" web/src/components/settings/SettingsJumpNav.test.tsx` is at least 4 (one definition, at least three call sites across the new its).
    - `grep -c "fireIntersection" web/src/components/settings/SettingsJumpNav.test.tsx` is at least 4 (one definition, at least three call sites).
  </acceptance_criteria>
  <done>SettingsJumpNav.test.tsx proves, with real synthetic IntersectionObserver entries, that exactly one jump-nav link is ever active -- Konto at scroll-top, the straddled section after an entry fires, and the previous section again when no entry intersects -- while leaving the pre-existing WR-08 scoping test fully intact.</done>
  <reversibility rating="reversible">Adds tests to an existing test file only; no production code touched; fully git-revertible.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Live jump-nav PL-label geometry spec, and correct 29-03-PLAN.md's two false verification claims</name>
  <files>web/e2e/settings-jumpnav-labels.spec.ts, .planning/phases/29-a-real-settings-page-shell-migration/29-03-PLAN.md</files>
  <read_first>web/e2e/settings-route.spec.ts (full file -- the registration-via-UI + client-side-`<Link>`-transition pattern this new spec reuses), web/e2e/fixtures.ts lines 1-111 (`newBareContext`, `applyE2eInitScript`'s own `pv-locale=en` seeding, `SESSION_PASSWORD`, `attachDialogGuard`/`dialogFired` -- understand exactly what `newBareContext` already sets up so this task only adds what's missing, an explicit pl override), .planning/phases/29-a-real-settings-page-shell-migration/29-03-PLAN.md lines 18-38 (the two `prohibitions` entries' `verification` strings to correct) and lines 149-186 (Task 2's `<acceptance_criteria>` bullet asserting an unscoped `role="tablist"` grep, also to correct).</read_first>
  <action>
**Part A -- new live spec, `web/e2e/settings-jumpnav-labels.spec.ts`.**

Open with a file-header comment stating: (1) this is the retained, re-runnable successor to
29-01-PLAN.md's declared backstop ("all four Polish jump-nav labels render without clipping at 375px
and inside the 200px desktop rail") -- the original evidence was an ad-hoc Playwright measurement run
once during Plan 29-01's execution and cited only in commit `8ef5f3e`'s message and a code comment,
never retained as a test; (2) the lane choice is deliberately LIVE Playwright, not a vitest/jsdom unit
test, because jsdom implements no real layout engine -- every element's `scrollWidth`/`clientWidth`/
`getBoundingClientRect()` height reads back as `0` in jsdom regardless of actual content, so a jsdom
version of this assertion would trivially pass (`0 === 0`) whether or not "Rodzina i udostępnianie"
(23 characters, the real pressure case) actually clips -- a fake backstop, worse than none. Only a
real Chromium layout produces meaningful geometry here.

Import `test, expect, newBareContext, SESSION_PASSWORD` from `./fixtures`. Write one `test(...)`
with an async `({ browser })` callback:

Call `newBareContext(browser)` to get `{ context, page, dialogFired }`. `newBareContext` internally
seeds `pv-locale=en` via its own `applyE2eInitScript` -- since this spec's entire point is the Polish
label strings, immediately call `context.addInitScript(() => { window.localStorage.setItem
("pv-locale", "pl"); })`. State in a comment that Playwright runs every context-registered
`addInitScript` in registration order before a navigating document's first real script executes, so
this second, later-registered script overwrites `pv-locale` to `"pl"` before `LocaleContext`'s own
pre-hydration read (`layout.tsx`'s inline script, read once) ever observes it -- the net effect is
`pl` for this entire session, without needing to reimplement `newBareContext`'s own registration flow.

Register a fresh account through the real UI, mirroring `settings-route.spec.ts`'s own flow exactly
but with the PL toggle-button copy: build a unique email
(``pv-e2e-jumpnav-labels-${Date.now()}-${Math.random().toString(36).slice(2, 10)}@example.test``),
`page.goto("/")`, click the button whose accessible name is the literal Polish string "Nie masz
konta? Zarejestruj się" (the `auth.toggleToRegister` pl value -- confirmed at
`web/src/lib/i18n/dictionary.ts:32-35`), fill `register-email`/`register-password`/
`register-confirm-password` (testids, locale-independent) with the email and `SESSION_PASSWORD`, click
`register-submit`, then wait for `new-item-button` to become visible.

Navigate to `/settings` via the real sidebar gear, not a cold `page.goto` -- this is deliberate: a
cold full navigation drops the in-memory unlock singleton and renders the settings shell behind
`UnlockOverlay`'s backdrop blur, which is exactly what made 29-01-SUMMARY.md's own ad-hoc screenshots
unusable (though it does not affect element geometry, per that same SUMMARY's own note). The
client-side `<Link>` transition avoids the overlay entirely, giving a clean, fully-rendered page. Click
the button whose accessible name is the literal Polish string "Konto" (the `sidebar.account` pl value,
the account-dropdown trigger `role="button"` at `Sidebar.tsx`), then click the `sidebar-open-settings`
testid, then wait for `settings-section-konto` to become visible.

Locate the jump-nav landmark via `page.getByRole("navigation", { name: "Nawigacja ustawień" })` (the
`settings.jumpNavLabel` pl value) and assert it is visible. Get its four links via
`nav.getByRole("link")` and assert `toHaveCount(4)`.

Define a local async helper `assertNoClippingAndUniformHeight(viewportLabel: string)` that calls
`links.evaluateAll((elements) => elements.map((el) => ({ text: el.textContent, scrollWidth:
el.scrollWidth, clientWidth: el.clientWidth, height: el.getBoundingClientRect().height })))`, then for
each returned metric asserts `scrollWidth === clientWidth` (a per-link message naming the link's own
text and `viewportLabel`, explaining that any mismatch means that label's content overflows its own
box -- a clipped word), and finally asserts `Math.max(...heights) - Math.min(...heights)` is less than
`1` across all four links (a message explaining that a height difference means one label wrapped to a
second line while the others stayed on one -- the exact regression this whole backstop exists to
catch). Use a sub-pixel tolerance (`< 1`, not strict equality) deliberately -- state in a comment that
this tolerates float rounding across environments while still catching a genuine multi-line wrap,
which differs by tens of pixels, not fractions of one.

Call `page.setViewportSize({ width: 1280, height: 800 })` then `await
assertNoClippingAndUniformHeight("desktop 1280px rail")`; then call `page.setViewportSize({ width:
375, height: 800 })` then `await assertNoClippingAndUniformHeight("mobile 375px pill row")`.

Finish with `expect(dialogFired(), "zero OS-level dialogs across this session").toBe(false);` and
`await context.close();`, matching every other spec in this suite's standing no-OS-dialog rule.

<!-- planner-discipline-allow: role="tablist" -->
<!-- planner-discipline-allow: role="tab" -->
<!-- planner-discipline-allow: aria-selected -->

**Part B -- correct 29-03-PLAN.md's two false verification claims (29-VERIFICATION.md gaps 4 and 1's
docs half). Edit ONLY the three exact strings named below; do not touch any other line, statement,
task, or must_have in that file, and do not touch `SharingOverviewPanel.tsx` itself anywhere.**

Edit 1 -- in the frontmatter `prohibitions` list, the first entry's `verification:` value currently
reads (verbatim, one line): `SettingsPanel.tsx is deleted entirely (not left as dead/unused code);
grep for these strings across web/src returns zero matches outside node_modules.` Replace it with a
version scoped to the settings surface plus a dated correction note: state that the grep now scopes to
`web/src/app/settings` and `web/src/components/settings` (which does return zero matches), and that
the original unscoped claim was false because `web/src/components/vault/SharingOverviewPanel.tsx`
lines 419, 422-423 and 432-433 retain these strings -- pre-existing since Phase 28, out of this plan's
scope, untouched by this cutover and untouched by this correction. Date the correction 2026-08-10 and
attribute it to 29-VERIFICATION.md gap 4, closed by this plan (29-06-PLAN.md).

Edit 2 -- the second `prohibitions` entry's `verification:` value currently ends with (verbatim):
`SettingsPanel.test.tsx's 6 tests are removed only because the mechanism they test (tab switching) is
deleted, not to hide a regression -- their behavioral intent ("every action reachable") is already
re-proven by Plan 29-01's settings/page.test.tsx.` Replace the clause after the dash: state that 5 of
the 6 removed tests have legitimate successors (per 29-VERIFICATION.md's own Test-Count Reconciliation
table), but the 6th -- "replaces the Phase 3 placeholder with working Import/Export CTAs that open
ImportWizard/ExportDialog" -- was NOT already re-proven by `settings/page.test.tsx` as originally
claimed here (that file never references either CTA testid); its verbatim successor is
`web/src/components/settings/SettingsSectionData.test.tsx`, added by this same plan (29-06-PLAN.md).
Date this correction 2026-08-10 and attribute it to 29-VERIFICATION.md gap 1.

Edit 3 -- in Task 2's `<acceptance_criteria>`, one bullet currently asserts an UNSCOPED grep for the
same three strings across all of `web/src` returning zero matches. Rescope that same grep to
`web/src/app/settings web/src/components/settings` (matching Edit 1's reasoning) and append a dated
correction note naming the exact `SharingOverviewPanel.tsx` line numbers as the reason the original,
unscoped version of this bullet was false.

For all three edits, use each target string's full surrounding context (the preceding `statement:`
line, or the preceding bullet's own distinctive text) to make the match unambiguous -- each of the
three strings is unique in the file. Do not use a whole-file rewrite; each edit must be a scoped,
surgical replacement of exactly the sentence identified above.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/settings-jumpnav-labels.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx playwright test e2e/settings-jumpnav-labels.spec.ts` passes (1 test).
    - `grep -c "evaluateAll" web/e2e/settings-jumpnav-labels.spec.ts` is at least 1.
    - `grep -c "setViewportSize" web/e2e/settings-jumpnav-labels.spec.ts` equals 2 (desktop 1280 and mobile 375).
    - `grep -c "pv-locale" web/e2e/settings-jumpnav-labels.spec.ts` is at least 1 (the pl override).
    - `grep -c "CORRECTED 2026-08-10" .planning/phases/29-a-real-settings-page-shell-migration/29-03-PLAN.md` equals 3.
    - `grep -c '^---$' .planning/phases/29-a-real-settings-page-shell-migration/29-03-PLAN.md` equals 2 (frontmatter block boundaries intact, not corrupted by the edits).
    - `git diff --stat .planning/phases/29-a-real-settings-page-shell-migration/29-03-PLAN.md` shows only the three targeted lines changed (no task body, statement, or must_have touched beyond the named verification/acceptance_criteria strings).
  </acceptance_criteria>
  <done>settings-jumpnav-labels.spec.ts exists, is CI-wired (part of the standard `npx playwright test` / `npm run test:e2e` discovery), passes against a real Chromium layout at both viewports in pl, and 29-03-PLAN.md's two false verification claims now state what is actually true, scoped correctly, with SharingOverviewPanel.tsx untouched.</done>
  <reversibility rating="reversible">New e2e spec plus a scoped doc correction to an already-shipped plan file; no production code touched; fully git-revertible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Shallow component mocks (ImportWizard/ExportDialog) -> real component contracts | A mock that silently drifts from the real component's prop shape would make Task 1's tests pass without proving anything about the real wiring |
| 29-03-PLAN.md doc edit -> rest of that file's content | A scoped-replacement edit must not alter any statement, prohibition, task body, or must_have this plan's diff doesn't name |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-29-08 | Repudiation (false-positive test) | SettingsSectionData.test.tsx / SettingsJumpNav.test.tsx new assertions | medium | mitigate | Both new suites assert real state TRANSITIONS (mount then unmount; active then re-active after a null-intersection callback), not single render-only checks; this plan's own action text states, per test, the exact regression each assertion would catch -- "a test that passes both before and after a fix is not evidence" is satisfied by construction. |
| T-29-09 | Tampering | 29-03-PLAN.md edit | medium | mitigate | Task 3's Edit calls target three exact, uniquely-identified strings only; the task's own acceptance criteria include a `git diff --stat` check confirming no unrelated line in that file changed. |

No new package-manager installs in this plan -- no `T-29-SC` row required.
</threat_model>

<verification>
`cd web && npx vitest run src/components/settings/SettingsSectionData.test.tsx
src/components/settings/SettingsJumpNav.test.tsx` green, then `cd web && npm test` (full suite --
expect 844 tests / 83 files, up from the 838/82 baseline this plan preserves and extends: +3 from
SettingsSectionData.test.tsx, +3 from SettingsJumpNav.test.tsx, +1 file), then `cd web && npx
playwright test e2e/settings-jumpnav-labels.spec.ts` green, then confirm
`.planning/phases/29-a-real-settings-page-shell-migration/29-03-PLAN.md` still parses as valid plan
frontmatter and its diff is scoped to exactly the three targeted corrections.
</verification>

<success_criteria>
All four 29-VERIFICATION.md gaps close: the Import CTA wiring has a real unit successor, the Export
CTA wiring gains one too; the scroll-spy exactly-one-active invariant is proven with synthetic
IntersectionObserver entries, not merely asserted; the jump-nav PL-label backstop is replaced by a
retained, CI-wired live spec; and 29-03-PLAN.md's two false verification claims now say what is
actually true. No test anywhere is deleted or weakened. SharingOverviewPanel.tsx and FamilyTab.tsx are
untouched.
</success_criteria>

<output>
Create `.planning/phases/29-a-real-settings-page-shell-migration/29-06-SUMMARY.md` when done
</output>
