---
phase: 29-a-real-settings-page-shell-migration
plan: 04
type: execute
wave: 3
depends_on: ["29-01", "29-03"]
files_modified:
  - web/e2e/sharing.spec.ts
  - web/e2e/invite-flow.spec.ts
  - web/e2e/remove-member.spec.ts
  - web/e2e/delete-account.spec.ts
autonomous: true
requirements: [SET-02]
user_setup: []

must_haves:
  truths:
    - "SET-02: every live e2e flow that previously reached Family & Sharing or account deletion through the old settings drawer reaches the same real content through /settings after the cutover -- no e2e regression, proven by a live run, not just a green vitest suite (REQUIREMENTS.md Non-Negotiable #2)."
    - "The delete-account trigger is found by these specs at its NEW home (Konto section), not its old one (Security tab) -- the content genuinely relocated in Plan 29-01, and the live proof must follow it there."
    - "No tab-click choreography survives in any of the four fixed helpers -- with all four /settings sections visible without interaction (SET-04), a settings-tab-family/settings-tab-security click is not merely renamed, it is removed."
  artifacts: []
  key_links:
    - "Each fixed helper -> Plan 29-01's real settings-section-{slug} testids and Plan 29-03's real <a href=\"/settings\"> sidebar link"
  prohibitions:
    - statement: "No test may be deleted or weakened."
      status: enforced
      verification: "Every existing assertion these four spec files make about family membership, invitations, member removal, and account deletion is preserved verbatim -- only the NAVIGATION path to reach the content changes, never the proof itself."
---

<objective>
This phase's own cutover (Plan 29-03) breaks four existing live e2e specs' settings-navigation
helpers: they click `sidebar-open-settings` expecting the OLD drawer+tab mechanism
(`settings-tab-family`/`settings-tab-security` clicks, `settings-close` to dismiss). That mechanism
is gone. Fix all four so the phase's own most valuable live-proof suite (family sharing, invitations,
member removal, account deletion -- REQUIREMENTS.md Non-Negotiable #2's "a green unit suite is not
evidence" applies directly here) does not silently regress.

Purpose: SET-02 (no functional regression in the LIVE proof lane, not just vitest).
Output: `openFamilyTab`/`closeSettings`/`openSecurityTab`-equivalent helpers updated across the four
files that duplicate them, per this codebase's own established "per-file-owns-its-own-tiny-helper"
convention.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix openFamilyTab/closeSettings in sharing.spec.ts, invite-flow.spec.ts, remove-member.spec.ts</name>
  <files>web/e2e/sharing.spec.ts, web/e2e/invite-flow.spec.ts, web/e2e/remove-member.spec.ts</files>
  <read_first>web/e2e/sharing.spec.ts (lines 159-181, openFamilyTab/closeSettings), web/e2e/invite-flow.spec.ts (lines 103-115, its own copy of openFamilyTab -- no closeSettings in this file per the earlier grep), web/e2e/remove-member.spec.ts (lines 150-180, its own openFamilyTab/closeSettings)</read_first>
  <action>
In each of the three files, replace `openFamilyTab(page)`'s body: drop the `settings-tab-family` click
entirely (there is no tab to click -- Rodzina i udostępnianie is already rendered, unconditionally,
inside `/settings`). New body: check `page.url().includes("/settings")` (or re-use the existing
`isVisible` idempotency-check style, now checking for `family-members-section` OR `family-bootstrap`
visibility directly) -- if not already there, click `page.getByTestId("sidebar-open-settings")` (now
a real `<Link>` per Plan 29-03, so this performs a client-side transition, NOT a full reload -- no
re-unlock step is needed afterward). Then `await Promise.race([page.getByTestId("family-bootstrap").waitFor({state:"visible"}), page.getByTestId("family-members-section").waitFor({state:"visible"})])`
exactly as today, and keep the existing `family-bootstrap` -> fill+create-family fallback logic
unchanged (that part is unaffected by the container migration).

Replace `closeSettings(page)` (in `sharing.spec.ts` and `remove-member.spec.ts` -- `invite-flow.spec.ts`
has no `closeSettings` per the grep, confirm this directly rather than assuming) with a
`returnToVault(page)` helper: click `page.getByTestId("settings-back-to-vault")`, then wait for a real
vault-only marker to reappear (`page.getByTestId("new-item-button")` -- matches this codebase's own
existing `reloadAndUnlock` helper's post-navigation wait target) rather than waiting for
`settings-panel` to detach (that testid no longer exists). Update every call site of the old
`closeSettings` name to the new `returnToVault` name across these files.
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/sharing.spec.ts e2e/invite-flow.spec.ts e2e/remove-member.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three spec files run green against a real pv-server (use an isolated instance -- confirm nothing is already listening on `localhost:8620` via `lsof -i :8620` before running, and never point at `data/pv.db`; STATE.md records a prior session accidentally wrote throwaway accounts into the real dev database this exact way).
    - `grep -rn "settings-tab-family\|settings-panel\b" web/e2e/sharing.spec.ts web/e2e/invite-flow.spec.ts web/e2e/remove-member.spec.ts` returns zero matches.
    - Every pre-existing assertion in these files about family membership, invitations, and member removal is unchanged -- only the navigation helper bodies differ.
  </acceptance_criteria>
  <done>All three spec files pass live against an isolated pv-server, with zero references to the retired tab mechanism.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix delete-account.spec.ts's openSecurityTab (delete-account relocated to Konto) + its own openFamilyTab/closeSettings</name>
  <files>web/e2e/delete-account.spec.ts</files>
  <read_first>web/e2e/delete-account.spec.ts (lines 118-160, its openFamilyTab/closeSettings/openSecurityTab helpers -- note this file has ALL THREE, unlike the other three files)</read_first>
  <action>
Apply Task 1's identical `openFamilyTab`/`returnToVault` fix to this file's own copies of those two
helpers. Additionally, fix `openSecurityTab(page)` (used solely to reach `account-delete-trigger`,
which RELOCATED from the Security tab to the Konto section in Plan 29-01 -- this is a genuine content
move the e2e spec must follow, not a rename): rename it to `openAccountSection(page)` (update every
call site), drop the `settings-tab-security` click entirely (obsolete, same reasoning as Task 1), and
change its wait target from `account-delete-trigger` under the old tab to
`page.getByTestId("account-delete-trigger").waitFor({state:"visible"})` under the new
`settings-section-konto` (the testid itself is unchanged since Plan 29-01 moved the element verbatim
-- only its container changed, so the SAME `getByTestId` call finds it at its new location once the
navigation reaches `/settings`).
  </action>
  <verify>
    <automated>cd web && npx playwright test e2e/delete-account.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `delete-account.spec.ts` runs green against an isolated pv-server instance.
    - `grep -n "openSecurityTab\|settings-tab-security" web/e2e/delete-account.spec.ts` returns zero matches; `grep -n "openAccountSection" web/e2e/delete-account.spec.ts` shows the renamed helper and its call sites.
    - Every pre-existing assertion about account deletion (both owner and plain-member paths, per this file's original scope) is unchanged.
  </acceptance_criteria>
  <done>delete-account.spec.ts passes live, reaching account-delete-trigger inside the Konto section rather than a Security tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Test-only plan -- no production code surface changes. Inherits the threat register established in
Plans 29-01/29-02/29-03; this plan adds no new production-code trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| N/A | -- | -- | -- | -- | This plan touches only `web/e2e/*.spec.ts` test files; no production code is modified. No new package-manager installs -- no `T-29-SC` row required. |
</threat_model>

<verification>
`cd web && npx playwright test e2e/sharing.spec.ts e2e/invite-flow.spec.ts e2e/remove-member.spec.ts e2e/delete-account.spec.ts` all green against a fresh, isolated pv-server instance (never the developer's `data/pv.db`).
</verification>

<success_criteria>
The four live e2e specs that exercise Family & Sharing, invitations, member removal, and account
deletion all pass against the cut-over `/settings` route, with zero remaining references to the
retired tab mechanism.
</success_criteria>

<output>
Create `.planning/phases/29-a-real-settings-page-shell-migration/29-04-SUMMARY.md` when done
</output>
