---
phase: 29-a-real-settings-page-shell-migration
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - web/src/lib/vault/store.ts
  - web/src/lib/vault/store.test.ts
  - web/src/components/vault/ExportDialog.tsx
  - web/src/components/vault/ExportDialog.test.tsx
  - web/src/lib/i18n/dictionary.ts
autonomous: false
requirements: [DEBT-02]
user_setup: []

must_haves:
  truths:
    - "n===0: the disclosure sentence is entirely absent -- never a rendered '0 wpisów' or any placeholder (E5 empty / DEBT-02 empty)."
    - "n>=1: the disclosure renders as a second <p> INSIDE the existing export-warning-banner alert, never a second/competing alert box (E5 populated/overflow/long-text)."
    - "The counted set is explicit: every VaultItem where accessLevel === 'hidden_password' as returned by getItems() -- this includes items reachable via both a collection-scoped (folder) share and a direct per-item share, since both resolve into the same VaultItem[] with accessLevel set by the existing merge logic in store.ts. This must be exercised, not assumed, by Plan 29-05's byte-level e2e proof (DEBT-02 precision)."
    - "Neither toCsv.ts nor toJson.ts gains any new masking logic -- the disclosure sentence and its count live entirely inside ExportDialog.tsx; the exporters' unconditional fields.password passthrough is unchanged."
    - "hydrated signal set/reset lifecycle: unlock -> hydrated=false immediately, then true once loadAndDecryptAll() resolves; lock -> hydrated=false."
  artifacts:
    - web/src/lib/vault/store.ts (isItemsHydrated/useItemsHydrated additions)
    - web/src/components/vault/ExportDialog.tsx (disclosure + hydration gate)
  key_links:
    - "ExportDialog.tsx -> store.ts's useItemsHydrated(): export-confirm is disabled while hydration state is unknown, never allowed to fire against an unconfirmed count"
    - "ExportDialog.tsx -> itemCapabilities.ts's isPasswordHidden(): the single established predicate for this access level, not reinvented"
  backstops:
    - statement: "The export action (export-confirm) is unavailable/disabled until getItems() is confirmed fully hydrated post-unlock; opening the export dialog against an unhydrated or partial store never presents an absent (zero-count) disclosure -- proven by a falsification test that renders against a mocked mid-hydration state, not a happy-path assertion (E5 loading/partial/error; DEBT-02 concurrency)."
      verification: backstop
    - statement: "The exact interpolated disclosure string at n=1 ('1 wpisów', matching this codebase's accepted no-plural-machinery convention, same shape as account.deleteOwnerWarning's '1 member(s)') is asserted by a held-out test (DEBT-02 boundary)."
      verification: backstop
  dismissed:
    - statement: "DEBT-02 adjacency (merge/collision semantics)."
      reason: "No merge/collision semantics apply -- the count is a plain cardinality over a set."
    - statement: "DEBT-02 ordering."
      reason: "The disclosure reports a count, not an ordered collection."
  prohibitions:
    - statement: "The exporter MUST NOT mask hidden-password values -- the locked resolution is disclose, not mask."
      status: enforced
      verification: "toCsv.ts/toJson.ts are not touched by this plan (grep diff confirms); the disclosure states what the file already contains, it does not change what the file contains."
    - statement: "No test may be deleted or weakened."
      status: enforced
      verification: "ExportDialog.test.tsx's existing 4 tests are extended, never removed; the new mock of @/lib/vault/store adds useItemsHydrated alongside the existing getItems/getFolders exports rather than replacing them."
---

<assumption_delta_decision>
The assumption-delta detector fired on ROADMAP.md's Phase 29 goal text ("whichever resolution is
chosen (mask consistently, or disclose at export time)") -- a "chosen" signal. This is NOT a runtime
configuration becoming a modeling decision: 29-CONTEXT.md's locked resolution explicitly states "No
per-export checkbox. One stated behaviour." -- the mask-vs-disclose question was a one-time,
planning-time design choice (already resolved: disclose), never a per-export or per-user parameter in
the shipped code. No promote/add-alongside modeling change applies.

- **Noun now primary:** none -- there is no new pluralized entity here.
- **Decision:** no-change.
- **Rationale:** the code has exactly one fixed behavior (disclose), enforced by the prohibition above (no checkbox); nothing in this plan introduces a constant-to-parameter transition.
</assumption_delta_decision>

<objective>
Close DEBT-02 for real: state the hidden-password disclosure honestly at export time (never mask),
and close the async hydration race that would otherwise let the disclosure silently understate real
exposure in the exact window right after unlock. The disclosure copy itself is a one-line
`interpolate()` call; the actual engineering surface is the `hydrated` signal that makes "0 items,
confirmed" distinguishable from "don't know yet."

Purpose: DEBT-02 (export honesty, both surfaces of the contradiction: the sentence and the timing gap
that could silently reopen it).
Output: `store.ts`'s new hydration signal, `ExportDialog.tsx`'s disclosure + hydration gate, and the
unit/falsification tests proving both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-CONTEXT.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-RESEARCH.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hydrated signal in store.ts -- mirrors isUnlocked's exact useSyncExternalStore shape</name>
  <files>web/src/lib/vault/store.ts, web/src/lib/vault/store.test.ts</files>
  <read_first>web/src/lib/crypto/index.ts (lines 180-208, isUnlocked/subscribeLockState/useIsUnlocked -- the exact 3-part shape to mirror: module-level let + Set of listeners + useSyncExternalStore(subscribe, getSnapshot, () => false)), web/src/lib/vault/store.ts (lines 1-20 imports, lines 1290-1339 the subscribeLockState callback -- this is the fire-and-forget hazard site), web/src/lib/vault/store.test.ts (lines 1-170, the vi.hoisted mock set + importStoreAndGetLockListener helper -- reuse this exact pattern, do not invent a second one; lines 280-329, "lock/unlock subscription behavior" describe block -- the existing act()+await Promise.resolve() x2 idiom your new tests extend)</read_first>
  <behavior>
    - Test: after `importStoreAndGetLockListener()`, before any unlock event fires, `store.isItemsHydrated()` returns `false`.
    - Test: firing the lock listener with `mockIsUnlocked` returning `true`, then awaiting the same two-`Promise.resolve()` flush the existing "populates items on unlock" test uses, `store.isItemsHydrated()` becomes `true` (loadAndDecryptAll's new `.then()` fired).
    - Test: immediately after firing the unlock listener (before the flush), `store.isItemsHydrated()` is still `false` -- the "not yet known" arm-on-every-unlock behavior.
    - Test: firing the lock listener with `mockIsUnlocked` returning `false` (a lock event) sets `store.isItemsHydrated()` back to `false`.
    - Test: `store.useItemsHydrated()` (rendered via a trivial test component or `renderHook`-equivalent already used elsewhere in this file, if any -- otherwise assert via the exported getter, matching this file's own existing convention of not testing hooks directly where a plain getter suffices) reflects the same transitions.
  </behavior>
  <action>
`useSyncExternalStore` is already imported at the top of `store.ts` from `react` -- reuse it, no new
import needed. Add a module-level `let hydrated = false;` and `const hydrationListeners = new
Set<() => void>();` alongside the file's existing singleton state. Add `function setHydrated(v:
boolean): void { hydrated = v; hydrationListeners.forEach((l) => l()); }`, `export function
isItemsHydrated(): boolean { return hydrated; }`, and `export function useItemsHydrated(): boolean {
return useSyncExternalStore((l) => { hydrationListeners.add(l); return () =>
hydrationListeners.delete(l); }, isItemsHydrated, () => false); }` -- naming is a recommendation per
29-RESEARCH.md's Assumptions Log (the load-bearing contract is "distinguish 0-confirmed from unknown,"
not these exact names; keep them unless a strong reason not to).

In the existing `subscribeLockState(() => { if (isUnlocked()) { ... } else { ... } })` callback at
`store.ts:1312-1339`: in the `isUnlocked()` (unlock) branch, call `setHydrated(false)` as the FIRST
statement (arms "not yet known" on every unlock, before any async work starts), then change the
existing `void loadAndDecryptAll();` line to `void loadAndDecryptAll().then(() => setHydrated(true));`
-- do not await it inline (this callback itself is synchronous, matching every other statement in this
branch). In the `else` (lock) branch, call `setHydrated(false)` alongside the existing
`personalItems = []`/`collectionSharedItems = []` resets.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/vault/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 behaviors above pass as new tests in `store.test.ts`, using the file's own existing `importStoreAndGetLockListener()`/`act()`/`await Promise.resolve()` idiom -- no new test-infrastructure pattern introduced.
    - The full existing `store.test.ts` suite (pre-existing tests) stays green -- the added `.then()` adds one microtask hop after `loadAndDecryptAll()` resolves, which the existing two-`Promise.resolve()` flush in "lock/unlock subscription behavior" already tolerates (confirm by running the full file, not just the new tests).
    - `grep -n "export function isItemsHydrated\|export function useItemsHydrated" web/src/lib/vault/store.ts` returns both lines.
  </acceptance_criteria>
  <done>store.test.ts passes in full (pre-existing + 5 new hydration-lifecycle tests), confirming unlock arms hydrated=false then true, lock resets to false.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExportDialog disclosure sentence + hydration gate + falsification test</name>
  <files>web/src/components/vault/ExportDialog.tsx, web/src/components/vault/ExportDialog.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <read_first>web/src/components/vault/ExportDialog.tsx (full file, 92 lines), web/src/components/vault/ExportDialog.test.tsx (full file, 89 lines -- the existing vi.hoisted mock of "@/lib/vault/store" you must extend, not replace), web/src/lib/vault/itemCapabilities.ts (lines 31-33, isPasswordHidden -- the exact predicate to reuse, never reinvent)</read_first>
  <behavior>
    - Test: `mockUseItemsHydrated` (new hoisted mock) returns `true`, `mockGetItems` returns 0 items with `accessLevel === "hidden_password"` -- `export-hidden-password-disclosure` is absent from the DOM entirely (not rendered with "0").
    - Test: same hydrated=true, but `mockGetItems` returns 1 such item -- the disclosure renders the exact interpolated string for `n=1` (assert the literal text, not just presence).
    - Test: same hydrated=true, 3 such items -- disclosure renders with `n=3`.
    - Falsification test: `mockUseItemsHydrated` returns `false` (mid-hydration/unknown state) -- `export-confirm` is disabled (or the dialog shows an explicit pending state) AND `export-hidden-password-disclosure` is absent -- assert BOTH conditions, since an absent disclosure alone is indistinguishable from the n=0 case; the disabled/pending confirm is what proves this is "unknown," not "confirmed zero."
    - Existing 4 tests (warning banner, JSON confirm, CSV confirm, cancel/backdrop) stay green with the store mock extended (not replaced).
  </behavior>
  <action>
Extend `ExportDialog.test.tsx`'s existing `vi.mock("@/lib/vault/store", ...)` (currently exporting only
`getItems`/`getFolders`) to also export a hoisted `mockUseItemsHydrated: vi.fn(() => true)` as
`useItemsHydrated`. In `ExportDialog.tsx`, import `useItemsHydrated` alongside the existing
`getFolders`/`getItems` import from `@/lib/vault/store`, and `isPasswordHidden` from
`@/lib/vault/itemCapabilities`. Compute `const hydrated = useItemsHydrated();` and
`const hiddenPasswordCount = hydrated ? getItems().filter(isPasswordHidden).length : null;` inside the
component body (call `getItems()` at render time, matching the file's existing pattern of calling it
inside `handleConfirm` -- do not memoize prematurely). Render the disclosure exactly per
29-UI-SPEC.md's DEBT-02 Export Disclosure section: inside the existing `<div className="alert
alert-warning" data-testid="export-warning-banner">`, wrap the existing `<span>{t("export.warningBody")}</span>`
and a new conditional in a `<div className="flex flex-col gap-1">`: `{hiddenPasswordCount !== null &&
hiddenPasswordCount > 0 ? (<p data-testid="export-hidden-password-disclosure">{interpolate(t("export.hiddenPasswordDisclosure"),
{ n: hiddenPasswordCount })}</p>) : null}` (import `interpolate` from the same i18n module `t` comes
from). Disable `data-testid="export-confirm"` (add `disabled={hiddenPasswordCount === null}`) so a
confirm can never fire against an unconfirmed count -- this is the correctness fix, not the sentence
alone. Do not add a loading spinner or new copy for the pending state unless trivial; the disabled
button state alone satisfies the backstop's "never presents an absent disclosure as confirmed."

Add to `web/src/lib/i18n/dictionary.ts`: `export.hiddenPasswordDisclosure` (pl "Ten eksport zawiera
hasła {n} wpisów udostępnionych Ci z ukrytym hasłem — to maskowanie działa tylko w interfejsie, nigdy
kryptograficznie.", en "This export includes the passwords for {n} items shared to you with a hidden
password — that mask is an interface-only protection, never a cryptographic one.") -- literal strings
from 29-UI-SPEC.md's Copywriting Contract, do not paraphrase.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/vault/ExportDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All behaviors above pass; the falsification test specifically asserts `export-confirm` is disabled while `useItemsHydrated` returns false -- a happy-path-only test suite would not satisfy this criterion.
    - `n===0` (hydrated, zero matching items) never renders `export-hidden-password-disclosure` in the DOM (`queryByTestId` returns null, not an element with "0" text).
    - `n===1` renders the exact literal string interpolated with 1 (grammar-imperfect but matching the accepted convention -- verified by Task 3's checkpoint, not silently assumed acceptable here).
    - `grep -n "toCsv.ts\|toJson.ts" web/src/components/vault/ExportDialog.tsx` returns nothing new beyond the existing imports (buildCsvExport/buildJsonExport calls, unchanged) -- confirms no masking logic crept into the exporters.
  </acceptance_criteria>
  <done>ExportDialog.test.tsx passes in full (4 pre-existing + n=0/n=1/n=3 + falsification tests), export-confirm is provably disabled while hydration state is unknown.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify -- n=1 Polish grammar acceptable to ship</name>
  <what-built>The DEBT-02 disclosure sentence, which at n=1 reads grammatically-imperfect Polish ("1 wpisów") matching this codebase's already-accepted no-plural-machinery convention (same shape as the recorded `account.deleteOwnerWarning` "1 member(s)" debt).</what-built>
  <how-to-verify>
    1. Run `cd web && npm run dev`, sign in to an account holding exactly one item shared to it at
       `hidden_password` access level (or seed one via the API), open Settings -> Dane -> Eksportuj
       vault.
    2. Confirm the warning banner shows the disclosure sentence with "1 wpisów" (Polish) / matching
       English text, and that this reads acceptably as shippable copy (not that it is grammatically
       perfect -- it is a known, accepted convention in this codebase).
    3. This closes both this plan's grammar backstop and Plan 29-01's jump-nav-label checkpoint's
       second half together, if you have not already confirmed the jump-nav labels separately.
  </how-to-verify>
  <resume-signal>Type "approved" if the n=1 wording is acceptable to ship, or state the preferred copy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| In-memory item store -> export file on disk | The export flow writes real plaintext (including hidden-password values) to a file the browser downloads -- this is intentional per the locked disclose-not-mask resolution, but the STATEMENT of what the file contains must never understate reality |
| Async hydration window -> disclosure computation | A narrow window right after unlock where `getItems()` can return `[]`/partial data while the app renders as unlocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-29-04 | Information Disclosure (false negative / dishonesty) | `ExportDialog.tsx` + `store.ts`'s hydration timing | high | mitigate | `hydrated` signal + `export-confirm` disabled while `hiddenPasswordCount === null`; falsification test in Task 2 proves the confirm action cannot fire against an unconfirmed count, closing the exact class of lie DEBT-02 exists to remove. |
| T-29-05 | Information Disclosure (intentional, by design) | Export file itself (`toCsv.ts`/`toJson.ts`, unchanged) | low | accept | Locked product decision (29-CONTEXT.md, carrying v0.4 decision A-6): `hidden_password` is an interface-only protection, never cryptographic -- the recipient already holds the item's Cipher Key and can read the value regardless. The disclosure sentence states this plainly at export time rather than pretending otherwise. |

No new package-manager installs in this task -- no `T-29-SC` row required.
</threat_model>

<verification>
`cd web && npx vitest run src/lib/vault/store.test.ts src/components/vault/ExportDialog.test.tsx`
green, then `cd web && npm test` (full suite, expect no unrelated regressions). The checkpoint closes
only after a human confirms the n=1 grammar is acceptable to ship.
</verification>

<success_criteria>
The export dialog states the hidden-password exposure honestly and only when the count is genuinely
known; a zero-count claim can never be made against an unhydrated store.
</success_criteria>

<output>
Create `.planning/phases/29-a-real-settings-page-shell-migration/29-02-SUMMARY.md` when done
</output>
