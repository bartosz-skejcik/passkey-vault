---
phase: 29-a-real-settings-page-shell-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/settings/page.tsx
  - web/src/lib/auth/AuthGate.tsx
  - web/src/components/settings/SettingsJumpNav.tsx
  - web/src/components/settings/SettingsSectionAccount.tsx
  - web/src/components/settings/SettingsSectionSecurity.tsx
  - web/src/components/settings/SettingsSectionData.tsx
  - web/src/components/settings/SettingsSectionFamily.tsx
  - web/src/components/settings/SecurityTab.tsx
  - web/src/lib/i18n/dictionary.ts
  - web/src/app/settings/page.test.tsx
  - web/src/components/settings/SettingsSectionAccount.test.tsx
  - web/src/components/settings/SecurityTab.test.tsx
autonomous: false
requirements: [SET-01, SET-02, SET-04]
user_setup: []

must_haves:
  truths:
    - "Page shell renders the header, jump-nav and all four <h2> group headings before any data resolves; a migrated section's own fetch failure never removes its section heading (E1 loading/error)."
    - "Page scrolls vertically only, content column capped at max-w-2xl; headings/descriptions wrap, never truncate, at any width (E1 overflow/long-text)."
    - "Jump-nav has exactly four static links (Konto, Bezpieczeństwo, Dane, Rodzina i udostępnianie), no empty/loading state, no singular/plural copy to get wrong (E2 empty/loading/populated/zero-one-many)."
    - "With IntersectionObserver unavailable or stubbed out, all four jump-nav links still navigate via native anchor href behavior; exactly one link is marked active at a time, and Konto is active at scroll-top (E2 error/partial)."
    - "Below 768px the jump-nav becomes a horizontal overflow-x-auto sticky pill row instead of the desktop vertical rail (E2 overflow)."
    - "All seven section-group states (empty/loading/error/populated/partial/zero-one-many/long-text) are carried unchanged from the already-tested PasskeysTab/SessionsTab/SecurityTab/FamilyTab -- the container migration touches none of them (E3)."
    - "Section content column is capped at max-w-2xl and never stretched wider than the migrated markup was tuned for (E3 overflow)."
    - "The back-to-vault link and the sidebar settings entry have no empty/loading/error/partial state -- both are static navigation elements with no data source (E4/E6, back-link half)."
    - "Back-to-vault link renders ArrowLeft + a fixed translator label at text-sm text-base-content/70, never accent-colored; header stacks flex-col below 768px so the link and <h1> never compete for one line at 375px (E4 populated/overflow/long-text)."
    - "SET-01: /settings is a real route -- linkable from a cold browser, survives a hard reload, browser back returns to the vault."
    - "SET-01: npm run build still emits a fully static export with no server-rendered route, proven from the built web/out/settings.html + web/out/settings.txt + web/out/settings/ directory, not from next.config.ts intent."
    - "SET-02 (empty): a group whose migrated content is empty (e.g. FamilyTab's own bootstrap-empty state) still renders its <h2> heading and its jump-nav link -- the IA never collapses because a list is empty."
    - "SET-02 (ordering): section order is fixed -- Konto -> Bezpieczeństwo -> Dane -> Rodzina i udostępnianie; within each section, migrated components keep their existing internal ordering unchanged."
    - "SET-04 (empty): all four <h2> headings render with zero interaction, regardless of their content's data state."
    - "SET-04 (ordering): jump-nav link order matches DOM section order exactly -- asserted, not assumed."
    - "SET-04 (adjacency): at a scroll position straddling two sections, scroll-spy marks exactly one link active -- the section occupying the viewport top -- never two, never zero once scrolled past the first heading."
    - "Auth gate: authenticated settings content (passkeys, sessions, family membership, delete-account trigger) never renders for a visitor with no session token -- a zero-session mount shows the login/register AuthCard, not settings content."
    - "SC5 (ROADMAP): the Family surface's 'awaiting Phase 33 redesign' status is marked in a code comment on SettingsSectionFamily.tsx -- never in rendered UI copy -- satisfying the positive marking obligation without violating the no-on-screen-WIP prohibition."
  artifacts:
    - web/src/app/settings/page.tsx
    - web/src/lib/auth/AuthGate.tsx
    - web/src/components/settings/SettingsJumpNav.tsx
    - web/src/components/settings/SettingsSectionAccount.tsx
    - web/src/components/settings/SettingsSectionSecurity.tsx
    - web/src/components/settings/SettingsSectionData.tsx
    - web/src/components/settings/SettingsSectionFamily.tsx
    - web/src/app/settings/page.test.tsx
    - web/src/components/settings/SettingsSectionAccount.test.tsx
  key_links:
    - "settings/page.tsx -> AuthGate: no settings content mounts until a session token is verified"
    - "settings/page.tsx -> SettingsJumpNav: the 4 anchors (#konto/#bezpieczenstwo/#dane/#rodzina) match the 4 section ids exactly"
    - "SettingsSectionAccount.tsx -> SecurityTab.tsx's extracted delete-account block: relocated, not duplicated -- SecurityTab no longer renders it"
  backstops:
    - statement: "All four Polish jump-nav labels render without clipping at 375px (mobile pill row) and inside the 200px desktop rail -- 'Rodzina i udostępnianie' (23 chars) is the real pressure case; neither width was chosen with the PL string in hand."
      verification: backstop
  dismissed:
    - statement: "Long-text on the four group description lines."
      reason: "Fixed, short, translator-owned copy -- no user input can lengthen them."
    - statement: "SET-02 adjacency (comparison/merge semantics between sections)."
      reason: "No comparison/merge semantics exist in a container migration -- nothing is compared for equality or merged."
  prohibitions:
    - statement: "The Family & Sharing surface MUST NOT be redesigned, restyled, or 'improved' in this phase."
      status: enforced
      verification: "SettingsSectionFamily.tsx wraps FamilyTab with zero prop/markup changes; FamilyTab.tsx itself is untouched by this plan."
    - statement: "No test may be deleted or weakened to make the suite green against the new location."
      status: enforced
      verification: "SecurityTab.test.tsx's 3 moved tests reappear verbatim (same assertions) in SettingsSectionAccount.test.tsx; the delete-account behavior they exercise remains fully asserted, just relocated."
    - statement: "The build MUST NOT gain a server-rendered route."
      status: enforced
      verification: "Task 1's verify runs a real npm run build and checks for out/settings.html/.txt/settings/ -- output:\"export\" in next.config.ts is untouched."
    - statement: "No new design tokens, colors, or type scales may be invented."
      status: enforced
      verification: "All spacing/color/typography values in this plan trace to packages/pv-ui/tokens.css or an existing Tailwind utility already cited in 29-UI-SPEC.md by file:line."
---

<objective>
Build the `/settings` route end-to-end: a real, linkable, static-export-compatible page that gates
its content on a verified session (a genuine gap today -- no shared auth-gate component exists),
presents the locked four-group information architecture with a scroll-spy jump-nav, and hosts all
four migrated surfaces (Konto, Bezpieczeństwo, Dane, Rodzina i udostępnianie) for real. This is the
phase's tracer: it proves route + auth + unlock + IA end-to-end before any other plan in this phase
touches `page.tsx`/`Sidebar.tsx` (Plan 29-03) or the export dialog (Plan 29-02).

Purpose: SET-01 (real route, static export intact), SET-02 (no functional regression -- every
migrated surface reachable), SET-04 (visible headed IA, no tab-hunting).
Output: `/settings` route + supporting components, wired and tested; `SecurityTab.tsx`'s
delete-account block relocated to Konto per the locked group definition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-CONTEXT.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-RESEARCH.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-PATTERNS.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-UI-SPEC.md
</context>

## Artifacts this phase produces

- New route: `web/src/app/settings/page.tsx`
- New components: `web/src/lib/auth/AuthGate.tsx`, `web/src/components/settings/SettingsJumpNav.tsx`, `web/src/components/settings/SettingsSectionAccount.tsx`, `web/src/components/settings/SettingsSectionSecurity.tsx`, `web/src/components/settings/SettingsSectionData.tsx`, `web/src/components/settings/SettingsSectionFamily.tsx`
- New i18n keys (`web/src/lib/i18n/dictionary.ts`): `settings.backToVault`, `settings.jumpNavLabel`, `settings.groupAccount`, `settings.groupAccountDescription`, `settings.groupSecurity`, `settings.groupSecurityDescription`, `settings.groupData`, `settings.groupDataDescription`, `settings.groupFamily`, `settings.groupFamilyDescription`
- New `data-testid` values: `settings-back-to-vault`, `settings-section-konto`, `settings-section-bezpieczenstwo`, `settings-section-dane`, `settings-section-rodzina`
- New heading ids (`tabIndex={-1}` focus targets): `konto-heading`, `bezpieczenstwo-heading`, `dane-heading`, `rodzina-heading`
- New test files: `web/src/app/settings/page.test.tsx`, `web/src/components/settings/SettingsSectionAccount.test.tsx`
- Relocated (not new): `account-delete-trigger` and its 3 tests move from `SecurityTab.tsx`/`SecurityTab.test.tsx` into `SettingsSectionAccount.tsx`/`SettingsSectionAccount.test.tsx`

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: End-to-end /settings shell -- AuthGate, header, jump-nav, all four sections wired</name>
  <files>web/src/lib/auth/AuthGate.tsx, web/src/app/settings/page.tsx, web/src/components/settings/SettingsJumpNav.tsx, web/src/components/settings/SettingsSectionAccount.tsx, web/src/components/settings/SettingsSectionSecurity.tsx, web/src/components/settings/SettingsSectionData.tsx, web/src/components/settings/SettingsSectionFamily.tsx, web/src/lib/i18n/dictionary.ts, web/src/app/settings/page.test.tsx</files>
  <read_first>web/src/app/page.tsx (lines 48-352, the authed/mode branch and its imports), web/src/app/self-test/page.tsx (the only precedent route file), web/src/components/settings/SettingsPanel.tsx (full file -- source of the Dane CTA JSX and the tab structure being replaced), web/src/components/shell/Sidebar.tsx (lines 62-73, navItemClass -- the active-state class pair to reuse verbatim), web/src/lib/crypto/index.ts (lines 180-208, isUnlocked/subscribeLockState/useIsUnlocked -- the shape UnlockOverlay/useIsUnlocked already give you, do not reimplement), web/src/components/settings/SettingsPanel.test.tsx (lines 1-55, the shallow-mock convention: mock ./FamilyTab and ../vault/ImportWizard/ExportDialog wholesale, let PasskeysTab/SessionsTab/SecurityTab render for real), web/src/app/page.test.tsx (lines 1-50, the exact mock set for @/lib/auth/session, @/lib/i18n/LocaleContext, @/lib/crypto, @/lib/idle/*), 29-UI-SPEC.md (Page Layout Contract, Migration Mapping, Accessibility Contract sections in full)</read_first>
  <action>
Create `web/src/lib/auth/AuthGate.tsx`: extract `page.tsx`'s inline `authed === null / false / true`
branch (lines ~332-360) into a standalone `"use client"` component `AuthGate({ children, onRegistered
}: { children: ReactNode; onRegistered?: () => void })`. Internal state: `useState<boolean|null>(null)`
for `authed`, `useState<"login"|"register">("login")` for `mode`, and a mount `useEffect` calling
`setAuthed(getSessionToken() !== null)` (import from `@/lib/auth/session`) -- this exact 3-state
contract (`null`=not yet resolved/render nothing, `false`=AuthCard, `true`=render children) must be
preserved verbatim. `authed===false` renders the SAME `AuthCard`/`LoginForm`/`RegisterForm` JSX
`page.tsx` currently inlines; `RegisterForm`'s `onAuthed` calls `setAuthed(true)` then
`onRegistered?.()` (the onboarding-wizard trigger stays page.tsx's own concern, passed in as a prop --
`/settings/page.tsx` passes no `onRegistered`, so a cold unauthenticated visit to `/settings` that
registers a fresh account never shows the onboarding wizard there). Do NOT wire `page.tsx` to use this
component yet -- that is Plan 29-03's job (page.tsx keeps its own inline branch working unmodified
until then); this task only creates the standalone, independently-usable component.

Create `web/src/app/settings/page.tsx`: a plain default-export function component (no `"use client"`
at this outer level, matching `self-test/page.tsx`'s shape so `next build` prerenders a static shell)
that renders `<AuthGate>` wrapping: a `"use client"` inner tree with `useIsUnlocked()` (from
`@/lib/crypto`) driving a `!unlocked ? "blur-md" : undefined` wrapper div (mirrors page.tsx's own
blur pattern), a `<header>` (`sticky top-0 z-10 border-b border-base-300 bg-base-100`) containing the
back-to-vault link (`<Link href="/" data-testid="settings-back-to-vault">` from `next/link` -- NOT a
bare `<a>`; see note below -- with `ArrowLeft` 16px + `t("settings.backToVault")`, `text-sm
text-base-content/70`) and `<h1>{t("settings.title")}</h1>` (28px/700/1.2), a
`grid grid-cols-[200px_1fr] gap-6 max-w-5xl mx-auto` wrapper (single column on mobile) containing
`<SettingsJumpNav/>` and a `<main>` stacking the four section components in locked order:
`<SettingsSectionAccount/>`, `<SettingsSectionSecurity/>`, `<SettingsSectionData/>`,
`<SettingsSectionFamily/>`, and finally `<UnlockOverlay/>` (from `@/components/auth/UnlockOverlay`,
self-contained, rendered unconditionally exactly like page.tsx does today).

Navigation-primitive note (planner-resolved gap -- neither CONTEXT.md nor RESEARCH.md addresses this):
use `next/link`'s `<Link>` for the back-to-vault link, not a bare `<a>`. `<Link>` renders an identical
`<a href="/">` in the DOM (so every literal UI-SPEC assertion -- real anchor, `getAttribute("href")`,
`getByRole("link")` -- holds unchanged), but performs a client-side SPA transition on a plain click
instead of a full browser reload. A full reload would drop this app's in-memory unlock singleton
(`currentUserKey` in `@/lib/crypto/index.ts`) exactly like the codebase's own documented
`SESSION_PASSWORD`/`reloadAndUnlock` precedent in `web/e2e/fixtures.ts` describes for other full
navigations -- forcing an unwanted re-unlock every time a user returns from Settings to the vault.
`<Link>` still falls back to a real native navigation for middle-click/ctrl-click/open-in-new-tab
(satisfying the locked "real link" intent literally), it only avoids the unnecessary reload on a plain
left-click. Apply the identical reasoning to the sidebar gear in Plan 29-03.

Create `web/src/components/settings/SettingsJumpNav.tsx`: a `"use client"` component rendering
`<nav aria-label={t("settings.jumpNavLabel")}>` with a static config array of exactly four entries --
`{slug:"konto", labelKey:"settings.groupAccount"}`, `{slug:"bezpieczenstwo",
labelKey:"settings.groupSecurity"}`, `{slug:"dane", labelKey:"settings.groupData"}`,
`{slug:"rodzina", labelKey:"settings.groupFamily"}` -- each rendered as a real `<a href="#{slug}">`.
Duplicate (do not import) a small `navItemClass(active: boolean)` helper matching
`Sidebar.tsx:62-73`'s exact two class strings (`bg-primary/[0.08] text-primary` active /
`text-base-content/70 hover:bg-base-200` inactive) verbatim, with a one-line comment noting it mirrors
Sidebar.tsx's own helper (this codebase's established per-file-tiny-helper convention -- see
`web/e2e/sharing.spec.ts`'s own comment on `openFamilyTab`). Wire an `IntersectionObserver`
(`rootMargin: "-40% 0px -55% 0px"`) over `document.querySelectorAll("section[id]")` (or refs collected
via the four section components' own `id`s) inside a `useEffect`, updating an `activeSlug` state used
only for the class toggle -- guard the observer setup so its absence never breaks navigation (the
`<a href>` elements work with zero JS). On each link's `onClick`, after the browser's native anchor
scroll, call `.focus()` on `document.getElementById(\`${slug}-heading\`)` (per the Accessibility
Contract's focus-management requirement -- every `<h2 id="{slug}-heading">` you create below carries
`tabIndex={-1}` for this to work). Desktop (`>=768px`): `sticky top-[header height] w-[200px]` vertical
list. Mobile (`<768px`): `overflow-x-auto sticky top-[header height] flex` horizontal pill row,
`rounded-selector` (1.9rem radius), each link `min-h-11` (44px tap target).

Create the four section components, each owning its OWN complete `<section id="{slug}"
aria-labelledby="{slug}-heading" data-testid="settings-section-{slug}"
className="scroll-mt-[header+jumpnav height] flex flex-col gap-4">` wrapper (per 29-PATTERNS.md's
Section wrapper contract) with an `<h2 id="{slug}-heading" tabIndex={-1}>{t("settings.group{Name}")}</h2>`
(24px/700/1.2) and an optional `<p className="text-sm text-base-content/70">{t("settings.group{Name}Description")}</p>`:
  - `SettingsSectionAccount.tsx` (slug "konto"): renders `<PasskeysTab/>` and `<SessionsTab/>` only in
    this task (unchanged imports from `./PasskeysTab`/`./SessionsTab`) -- the delete-account block
    relocates here in Task 2, not this task, so Task 1's verify does not depend on Task 2's edits.
  - `SettingsSectionSecurity.tsx` (slug "bezpieczenstwo"): thin wrap of `<SecurityTab/>`, unmodified
    import, unmodified in this task.
  - `SettingsSectionData.tsx` (slug "dane"): the exact JSX currently at `SettingsPanel.tsx:109-136`
    (the `settings-import-cta`/`settings-export-cta` block, `showImportWizard`/`showExportDialog`
    local state, `ImportWizard`/`ExportDialog` conditional renders) moved verbatim into this new file.
  - `SettingsSectionFamily.tsx` (slug "rodzina"): thin wrap of `<FamilyTab/>`, unmodified import,
    zero prop/markup changes (Family & Sharing carried across verbatim -- this phase does not touch
    `FamilyTab.tsx`'s own file at all). Per 29-CONTEXT.md's locked resolution, the "awaiting Phase 33
    redesign" marking lives in code comments/SUMMARY/ROADMAP, NEVER on the user's screen -- so add a
    file-header comment above the component (not any rendered JSX/copy) stating something equivalent
    to: "Carried across verbatim from SettingsPanel's Family tab (SET-01/SET-02, Phase 29). This
    surface is explicitly NOT redesigned here -- SET-03's redesign is Phase 33's job. Do not add
    visual/structural changes to this wrapper or to FamilyTab.tsx itself until then." This satisfies
    ROADMAP SC5's positive marking obligation without violating the no-on-screen-WIP-copy prohibition.
Add a `border-t border-base-300` divider before each section except the first (CSS `:not(:first-child)`
or an explicit prop, executor's choice).

Add to `web/src/lib/i18n/dictionary.ts` (web-only, NOT `packages/pv-ui/i18n/common.ts`, per the Phase
16 precedent): `settings.backToVault` (pl "Wróć do sejfu", en "Back to vault"),
`settings.jumpNavLabel` (pl "Nawigacja ustawień", en "Settings navigation"), `settings.groupAccount`
(pl "Konto", en "Account"), `settings.groupAccountDescription` (pl "Passkeys, sesje i urządzenia,
usuwanie konta.", en "Passkeys, sessions and devices, account deletion."), `settings.groupSecurity`
(pl "Bezpieczeństwo", en "Security"), `settings.groupSecurityDescription` (pl "Automatyczne
blokowanie i czyszczenie schowka.", en "Auto-lock and clipboard clearing."), `settings.groupData`
(pl "Dane", en "Data"), `settings.groupDataDescription` (pl "Import i eksport całego vaulta.",
en "Import and export your whole vault."), `settings.groupFamily` (pl "Rodzina i udostępnianie",
en "Family & sharing"), `settings.groupFamilyDescription` (pl "Zarządzaj rodziną i zaproszeniami.",
en "Manage your family and invitations." -- no "coming soon"/WIP wording anywhere).

Create `web/src/app/settings/page.test.tsx` combining `page.test.tsx`'s mock set (getSessionToken from
`@/lib/auth/session`, `@/lib/i18n/LocaleContext`, `@/lib/crypto`'s `useIsUnlocked` as a mutable hoisted
mock defaulting to `true`, `@/lib/idle/useIdleTimer` if pulled in transitively) with
`SettingsPanel.test.tsx`'s shallow-mock set (`./FamilyTab` -> `mock-family-tab` div, `../vault/ImportWizard`
and `../vault/ExportDialog` -> trivial divs, `@/lib/passkeys/api`'s `listPasskeys`, `@/lib/sessions/api`'s
`listSessions`) -- let `PasskeysTab`/`SessionsTab`/`SecurityTab` render for real (matching
`SettingsPanel.test.tsx`'s own precedent that this is safe). Assert: (a) with `mockGetSessionToken`
returning `null`, the page shows the login/register `AuthCard` (via a real, non-mocked-away
`getByTestId` on a login-form element) and NEVER renders `settings-section-konto` or any other
section testid -- this is the Pitfall-1 closure, a scenario this suite has never exercised before;
(b) with a session token present, all four `data-testid="settings-section-{slug}"` elements and their
`<h2>` text render with zero interaction/clicks; (c) the four `<h2>` elements appear in DOM order
Konto -> Bezpieczeństwo -> Dane -> Rodzina i udostępnianie; (d) the jump-nav landmark
(`getByRole("navigation", {name: "Nawigacja ustawień"})` or the mocked `t()` key) exists with exactly
four links whose `href`s are `#konto`/`#bezpieczenstwo`/`#dane`/`#rodzina` in that order.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/app/settings/page.test.tsx && npm run build && test -f out/settings.html && test -f out/settings.txt && test -d out/settings</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/settings/page.test.tsx` passes with all assertions above.
    - `npm run build` exits 0 and produces `web/out/settings.html`, `web/out/settings.txt`, `web/out/settings/` (mirroring the existing `out/self-test.*` triple) -- SC1's literal artifact proof.
    - `grep -rn 'role="tab"' web/src/app/settings/page.tsx web/src/components/settings/SettingsJumpNav.tsx web/src/components/settings/SettingsSection*.tsx | grep -v -E ':\s*//' | wc -l` returns 0 across all listed files (comment-only lines referencing the old tab mechanism for historical context are fine; only a live `role="tab"` attribute fails this check).
    - `SettingsSectionData.tsx` contains `settings-import-cta` and `settings-export-cta` testids unchanged (grep confirms).
    - `grep -l "Phase 33\|SET-03" web/src/components/settings/SettingsSectionFamily.tsx` finds the file (ROADMAP SC5's positive marking obligation -- the "awaiting redesign" note exists in a code comment) AND `grep -ic "coming soon\|wkrótce\|coming-soon" web/src/components/settings/SettingsSectionFamily.tsx` returns 0 (never rendered on-screen; `-i` so a capitalised "Wkrótce" at sentence start cannot slip past).
  </acceptance_criteria>
  <done>settings/page.test.tsx passes, npm run build produces the settings artifact triple, zero role="tab" occurrences exist anywhere in the new files, and SettingsSectionFamily.tsx carries its Phase 33 header comment with no on-screen WIP copy.</done>
  <reversibility rating="reversible">New, additive files plus one non-behavioral navigation-primitive choice (Link vs bare anchor) -- fully git-revertible, no data or schema implication.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Relocate delete-account from SecurityTab into Konto + move its 3 tests</name>
  <files>web/src/components/settings/SecurityTab.tsx, web/src/components/settings/SettingsSectionAccount.tsx, web/src/components/settings/SecurityTab.test.tsx, web/src/components/settings/SettingsSectionAccount.test.tsx</files>
  <read_first>web/src/components/settings/SecurityTab.tsx (full file, especially lines 34-38 and 125-146, the delete-account state/JSX/import), web/src/components/settings/SecurityTab.test.tsx (full file, the "Delete account section (E6)" describe block, lines 51-79)</read_first>
  <action>
In `SecurityTab.tsx`: remove the `deleteDialogOpen` state, the `DeleteAccountDialog` import, and the
entire delete-account `<div>` block (heading/body/`account-delete-trigger` button/conditional dialog
render, currently lines ~130-145) -- `SecurityTab` now renders only the autolock select and clipboard
duration controls, nothing else.

In `SettingsSectionAccount.tsx` (created in Task 1, currently rendering only `PasskeysTab`/`SessionsTab`):
add the removed `deleteDialogOpen` state, the `DeleteAccountDialog` import (`from
"./DeleteAccountDialog"`), and the exact same JSX block moved byte-for-byte (same `data-testid`s:
`account-delete-trigger`, same i18n keys: `account.deleteSectionHeading`, `account.deleteSectionBody`,
`account.deleteTriggerCta`) so it renders inside the Konto section, after `PasskeysTab`/`SessionsTab`.
</action>
  <verify>
    <automated>cd web && npx vitest run src/components/settings/SecurityTab.test.tsx src/components/settings/SettingsSectionAccount.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `SecurityTab.test.tsx` retains exactly 2 tests (autolock persistence, clipboard persistence) and has zero references to `account-delete-trigger` or `DeleteAccountDialog`.
    - New `SettingsSectionAccount.test.tsx` contains the 3 moved tests from `SecurityTab.test.tsx`'s former "Delete account section (E6)" describe block, with assertions unchanged (unconditional trigger render, click mounts `DeleteAccountDialog`, close unmounts it) -- verbatim behavior, relocated.
    - `SettingsSectionAccount.test.tsx` additionally asserts `PasskeysTab`/`SessionsTab` container presence (e.g. their own already-tested testids render inside `settings-section-konto`).
    - `grep -v -E '^\s*//' web/src/components/settings/SecurityTab.tsx | grep -c account-delete-trigger` returns 0 (a comment explaining the relocation is fine; a live testid/element is not).
  </acceptance_criteria>
  <done>SecurityTab.test.tsx has exactly 2 tests, SettingsSectionAccount.test.tsx carries the 3 moved delete-account tests plus Passkeys/Sessions container assertions, all green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify -- Polish jump-nav label fit at 375px and in the 200px desktop rail</name>
  <what-built>The `/settings` jump-nav (desktop 200px rail + mobile horizontal pill row) with the four locked Polish group labels, the longest of which ("Rodzina i udostępnianie", 23 characters) was flagged in 29-UI-SPEC.md as a real clipping risk since neither width was sized against the Polish strings.</what-built>
  <how-to-verify>
    1. Run `cd web && npm run dev`, sign in, visit `/settings` with locale set to `pl`.
    2. At a desktop width (>=1024px), confirm all four jump-nav labels in the left rail render on one line each, not clipped or wrapped mid-word.
    3. Resize to 375px width (or use device emulation), confirm the jump-nav becomes the horizontal pill row and all four labels remain fully readable (scroll the row horizontally if needed -- it should not truncate any label with an ellipsis).
    4. Separately, this checkpoint also carries VALIDATION.md's second manual-only item, closed together with Plan 29-02's disclosure work: confirm you accept the grammar of "1 wpisów" (or whichever count appears) once Plan 29-02 lands -- this half only needs your sign-off after 29-02 ships, note it now and re-confirm then.
  </how-to-verify>
  <resume-signal>Type "approved" once the jump-nav labels read cleanly at both widths, or describe what clips/wraps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> settings route entry | An unauthenticated visitor reaching `/settings` directly (deep link, bookmark, cold browser) must never see authenticated content |
| Client JS runtime -> in-memory unlock singleton | `useIsUnlocked()`/`currentUserKey` is the sole gate for decrypted-data-bearing components (PasskeysTab/SessionsTab/FamilyTab); a navigation path that bypasses `useIsUnlocked()` would leak nothing new (data is still fetched only after unlock) but could show a broken/loading state indefinitely |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-29-01 | Information Disclosure | `settings/page.tsx` + `AuthGate.tsx` | high | mitigate | `AuthGate` gates every child behind a verified `authed===true` session-token check (mirrors `page.tsx`'s own established 3-state contract exactly); `settings/page.test.tsx`'s zero-session-mount assertion is the regression guard -- this is Pitfall 1 from 29-RESEARCH.md, the phase's own most significant finding. |
| T-29-02 | Tampering / Spoofing | `SettingsJumpNav.tsx` anchor links | low | accept | All four jump-nav targets are hardcoded literal slugs (`#konto`/`#bezpieczenstwo`/`#dane`/`#rodzina`), never derived from any user-controlled input -- no open-redirect surface exists in this task's scope. |
| T-29-03 | Elevation of Privilege | `SettingsPanel.tsx` (old tab shell, untouched by this plan) | medium | accept (scoped) | This plan does not retire `SettingsPanel.tsx` (Plan 29-03 does) -- during this plan's own window, both surfaces coexist, each independently auth-gated (the old panel only ever mounts inside `page.tsx`'s already-authed branch). No new bypass is introduced; full retirement is tracked in 29-03. |

No new package-manager installs occur anywhere in this phase (29-RESEARCH.md's Package Legitimacy
Audit confirms) -- no `T-29-SC` supply-chain row is required.
</threat_model>

<verification>
After both code tasks: `cd web && npx vitest run src/app/settings/ src/components/settings/` green,
then `cd web && npm test` (expect the pre-existing 821-baseline count net-adjusted only by this plan's
own additions -- no unrelated regressions), then `cd web && npm run build` producing the settings
artifact triple. The checkpoint closes only after a human confirms the jump-nav label fit.
</verification>

<success_criteria>
`/settings` renders for an authed+unlocked session with all four headed sections visible without
interaction, blocks an unauthenticated visitor from seeing any settings content, survives
`npm run build` as a static artifact, and the delete-account trigger now lives under Konto with its
3 original tests intact at their new home.
</success_criteria>

<output>
Create `.planning/phases/29-a-real-settings-page-shell-migration/29-01-SUMMARY.md` when done
</output>
</content>
