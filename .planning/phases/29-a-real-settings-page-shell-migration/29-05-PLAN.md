---
phase: 29-a-real-settings-page-shell-migration
plan: 05
type: execute
wave: 4
depends_on: ["29-01", "29-02", "29-03", "29-04"]
files_modified:
  - web/e2e/export-disclosure.spec.ts
  - web/e2e/settings-route.spec.ts
autonomous: true
requirements: [DEBT-02, SET-01, SET-02]
user_setup: []

must_haves:
  truths:
    - "SC4 (DEBT-02, live evidence bar): the bytes of a real generated export file contain the actual password for a hidden_password-level item, matching what the export dialog's disclosure sentence states -- not the intent, not a unit test, the file (REQUIREMENTS.md Non-Negotiable #2)."
    - "DEBT-02 precision, exercised at the byte level: the counted/disclosed set includes an item shared to the exporting account via a collection (folder) share at hidden_password level -- not only a directly-shared item -- proving the counted set statement from Plan 29-02 against real data, not just against a mocked getItems()."
    - "SC1 (live proof, not just the build artifact): /settings reached as a first, cold navigation (a valid session, but this exact route never previously visited in this browser context) renders the settings shell with the unlock overlay -- not a 404, not a blank page."
    - "SC1: a hard page.reload() on /settings still renders the route (survives a reload) -- the static export serves the page correctly on a real reload, not only on the initial navigation."
    - "SC1: browser back after navigating from the vault to /settings returns to the vault, and the vault is usable again (proven by a real vault-only marker reappearing)."
  artifacts:
    - web/e2e/export-disclosure.spec.ts
    - web/e2e/settings-route.spec.ts
  key_links:
    - "export-disclosure.spec.ts -> Plan 29-01's /settings + Dane section + Plan 29-02's disclosure sentence and hydration gate: this is the first live run to exercise all three together"
    - "settings-route.spec.ts -> Plan 29-01's route + Plan 29-03's <Link>-based sidebar navigation: proves the reload/back-button consequences of that navigation-primitive choice live, not just asserted in prose"
  prohibitions:
    - statement: "No test may be deleted or weakened."
      status: enforced
      verification: "Both spec files in this plan are net-new -- nothing pre-existing is touched or removed."
---

<objective>
Close DEBT-02's own stated evidence bar live: generate a real export file from a real browser session
holding a real hidden_password-level share, and assert the downloaded file's bytes actually contain
what the disclosure sentence claims. This is genuinely new territory in this repo -- no existing
Playwright spec handles a browser download. This plan also closes SC1's remaining unverified claims
(reload survival, browser-back behavior) with real Playwright navigation, and runs the phase's final
full-suite + build + e2e regression sweep as the last plan in the phase's dependency graph.

This plan runs in Wave 4 (after 29-04), not Wave 3 -- its Task 3 sweep drives the same four e2e specs
Plan 29-04 repairs, and both plans target the same fixed `webServer` at `localhost:8620`; running them
in parallel would deterministically fail one leg and risks the exact `reuseExistingServer` port
collision that polluted `data/pv.db` during Plan 28-02 (recorded in STATE.md).

Purpose: DEBT-02 (SC4's literal evidence bar -- file bytes, not a unit-test-only claim); SC1 (the
reload/back-button halves of "real, linkable route" that Plan 29-01's build-artifact proof and jsdom
mount test do not exercise).
Output: two new e2e specs (export byte proof, route navigation proof); a final phase-wide verification
pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-CONTEXT.md
@.planning/phases/29-a-real-settings-page-shell-migration/29-RESEARCH.md
</context>

## Artifacts this phase produces

- New e2e spec files: `web/e2e/export-disclosure.spec.ts` (this repo's first Playwright spec exercising a real browser download event), `web/e2e/settings-route.spec.ts` (this repo's first live proof of `/settings`'s reload/back-button behavior).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: New e2e spec -- hidden_password export byte proof</name>
  <files>web/e2e/export-disclosure.spec.ts</files>
  <read_first>web/e2e/sharing.spec.ts (lines 77-235, its own local helpers -- uniqueSuffix/apiGet/tokenFor/userIdFor/ensureFamilyMembership/waitForIdentityKeyPublished/createLoginItemViaUI/shareExistingFolderWithMember(s) -- reimplement only the subset you need locally, per this file's own "per-file-owns-its-own-tiny-helper" convention, not by importing from sharing.spec.ts; lines 557-594, the createAndShare local pattern for driving ShareDialog to a hidden_password grant), web/e2e/fixtures.ts (the exported `test`/`expect`/`twoSessions`/`SESSION_PASSWORD`), 29-RESEARCH.md's "DEBT-02 export byte verification" code example (the `page.waitForEvent("download")` pattern), 29-UI-SPEC.md's DEBT-02 Export Disclosure section (the exact testids: export-format-json, export-format-csv, export-confirm, export-hidden-password-disclosure)</read_first>
  <action>
Using the `twoSessions` fixture, set up a sharer and a recipient exactly as `sharing.spec.ts` does
(`ensureFamilyMembership`, `waitForIdentityKeyPublished` for both). Create and share TWO items to the
recipient at `hidden_password` access level via the real `ShareDialog` UI: (a) one item shared
DIRECTLY (mirrors `sharing.spec.ts`'s own `createAndShare` pattern), and (b) one item that lives
inside an EXISTING shared folder the recipient has `hidden_password` access to (a collection-scoped
share) -- this second case is what proves Plan 29-02's "counted set includes both collection-scoped
and per-item shares" statement against REAL data, not just a mocked `getItems()` array; reuse
`sharing.spec.ts`'s collection-share helper pattern (`shareExistingFolderWithMember`) rather than
reinventing it.

On the RECIPIENT's session: navigate to `/settings` (click `sidebar-open-settings`, a real `<Link>`
per Plan 29-03 -- no re-unlock needed since this is a client-side transition), wait for
`settings-section-dane` to be visible, click `settings-export-cta`, wait for the export dialog, then:
`const [download] = await Promise.all([page.waitForEvent("download"), page.getByTestId("export-confirm").click()]);`
Read the downloaded file's bytes via `download.path()` + `readFileSync(..., "utf-8")` (import from
`node:fs`). Assert: (1) both shared items' real password values appear in the file bytes (proving the
export did NOT mask them, per the locked disclose-not-mask resolution); (2) before clicking confirm,
`export-hidden-password-disclosure` was visible with the interpolated count `n=2` (both items counted);
(3) `export-confirm` was NOT disabled at click time (proving hydration had genuinely completed, not
just that the test got lucky with timing -- assert `hiddenPasswordCount`'s rendered count matches 2
BEFORE the click, not merely that the download succeeded).

Cover both export formats in the SAME spirit as `ExportDialog.test.tsx`'s existing JSON/CSV split --
either two separate `test()` blocks or one test asserting both format buttons independently trigger a
correctly-byte-matching download; do not test only the default JSON format, since 29-CONTEXT.md's
locked decision requires "JSON and CSV behave identically."
  </action>
  <verify>
    <automated>cd web && grep -q 'waitForEvent("download")' e2e/export-disclosure.spec.ts && npx playwright test e2e/export-disclosure.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The cheap `grep` pre-check for the real download-event pattern (not a mocked/stubbed download) passes before the live Playwright run is attempted.
    - The spec passes against an isolated pv-server instance (confirm via `lsof -i :8620` before running; never against `data/pv.db`).
    - The downloaded file's bytes (both JSON and CSV runs) contain the real plaintext password for both the directly-shared and the collection-shared `hidden_password` item.
    - The disclosure sentence's rendered count (`n=2`) was asserted BEFORE the export click, not inferred after the fact.
  </acceptance_criteria>
  <done>export-disclosure.spec.ts passes live, with real downloaded-file bytes containing both hidden_password items' passwords across both JSON and CSV formats.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: New e2e spec -- SC1 live proof (cold navigation, reload, browser back)</name>
  <files>web/e2e/settings-route.spec.ts</files>
  <read_first>web/e2e/fixtures.ts (the exported `test`/`expect`/`SESSION_PASSWORD`, and the `reloadAndUnlock` reasoning at sharing.spec.ts:340-351 -- a full navigation drops this app's in-memory unlock singleton exactly like a real reload, which is precisely the behavior this task proves rather than assumes), web/e2e/smoke.spec.ts (a simple single-session spec to model this new file's structure on, since this test needs only one account, not twoSessions)</read_first>
  <action>
Create a single-session spec (register via the real UI, no `twoSessions` needed -- one account is
sufficient for all three assertions below).

Case 1 (cold navigation): after registering and completing the initial unlock on `/`, call
`page.goto("/settings")` directly (this browser context's FIRST visit to this specific route -- a
valid session exists, but nothing has been prefetched/hydrated for `/settings` yet). Assert the
settings shell renders: the `<h1>` (`settings.title`) and the jump-nav landmark are visible, AND
`unlock-password` (UnlockOverlay) is visible -- a fresh navigation is a full browser load, dropping
the in-memory unlock singleton exactly like `web/e2e/fixtures.ts`'s own `SESSION_PASSWORD`/
`reloadAndUnlock` precedent documents for other full navigations. Fill `unlock-password` with
`SESSION_PASSWORD` (imported from `fixtures.ts`), submit, and assert `settings-section-konto` becomes
visible -- proving the cold-URL-entry path works end-to-end, not just that a page renders.

Case 2 (reload survival): with `/settings` now unlocked and visible, call `page.reload()`. Assert the
page does NOT 404 and the same shell markers (`<h1>`, jump-nav landmark, `settings-back-to-vault`)
are visible again -- this is the static-export "survives a hard reload" claim proven live, not only
via the `npm run build` artifact check in Plan 29-01. The vault is expected to be locked again after
this reload (same singleton-drop reasoning as Case 1) -- assert `unlock-password` is visible once
more; unlock again with `SESSION_PASSWORD` to leave the session in a known state for Case 3.

Case 3 (browser back): navigate to `/` (e.g. click `settings-back-to-vault`, a real `next/link`
client-side transition per Plan 29-01/29-03 -- assert `new-item-button` visible, confirming the vault
is reachable and still unlocked, since a client-side transition does NOT drop the in-memory singleton,
unlike Cases 1/2's full navigations). Then click `sidebar-open-settings` to return to `/settings`
(again a client-side transition -- assert `settings-section-konto` visible with NO unlock prompt this
time, corroborating the `<Link>` design rationale from 29-01/29-03). Finally call `page.goBack()` and
assert `new-item-button` is visible again -- proving browser back genuinely returns to a usable vault.
  </action>
  <verify>
    <automated>cd web && grep -q "page.reload()" e2e/settings-route.spec.ts && grep -q "goBack()" e2e/settings-route.spec.ts && npx playwright test e2e/settings-route.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The cheap `grep` pre-checks for `page.reload()` and `goBack()` usage pass before the live Playwright run is attempted.
    - The spec passes against an isolated pv-server instance (confirm via `lsof -i :8620` before running; never against `data/pv.db`).
    - Case 1 proves a cold `/settings` entry (valid session, first visit to the route) renders the shell and its own unlock overlay, then unlocks through to real content.
    - Case 2 proves `page.reload()` on `/settings` re-renders the shell (not a 404) and correctly shows the unlock overlay again.
    - Case 3 proves both the client-side `<Link>` transition (no re-unlock) and a real `page.goBack()` (vault usable again) work as designed.
  </acceptance_criteria>
  <done>settings-route.spec.ts passes live, proving cold navigation, reload survival, and browser-back all behave as SC1 and the 29-01/29-03 `<Link>` design intend.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase-final verification sweep</name>
  <files></files>
  <read_first>29-VALIDATION.md's Validation Sign-Off checklist</read_first>
  <action>
Run the phase's full closing gate in sequence and record the results in this plan's SUMMARY: (1)
`cd web && npm test` -- full vitest suite, compare the resulting count against the 821-test/79-file
baseline recorded in 29-RESEARCH.md (net change should equal exactly this phase's own additions/moves,
with no unexplained drop); (2) `cd web && npm run build` -- confirm `out/settings.html`,
`out/settings.txt`, `out/settings/` all exist and the build exits 0; (3)
`cd web && npx playwright test e2e/sharing.spec.ts e2e/invite-flow.spec.ts e2e/remove-member.spec.ts e2e/delete-account.spec.ts e2e/export-disclosure.spec.ts e2e/settings-route.spec.ts e2e/smoke.spec.ts e2e/shared-sync.spec.ts`
-- the full live e2e regression across every spec this phase touched or could plausibly affect,
against a fresh isolated server instance. If any of the three legs fails, do not mark this task done
until root-caused and fixed (or, if the failure is a genuine pre-existing/out-of-scope issue, record
it explicitly rather than silently working around it).
  </action>
  <verify>
    <automated>cd web && npm test && npm run build && test -f out/settings.html</automated>
  </verify>
  <acceptance_criteria>
    - All three legs (unit suite, build artifact, live e2e regression) pass and are recorded with their actual counts/output in the SUMMARY.
    - The vitest test count delta from the 821/79 baseline is stated explicitly and matches this phase's own known additions (Plan 29-01's new tests, Plan 29-02's new tests, any test-count changes from Plans 29-03/29-04's edits) -- no silent, unexplained drop.
  </acceptance_criteria>
  <done>Full vitest suite green, npm run build produces the settings artifact triple, and the full live e2e regression across all touched specs (including the two new specs this plan adds) passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Real export file on disk | Contains real plaintext, by design (T-29-05 in Plan 29-02) -- this plan's own proof artifact, handled only within the isolated test run's temp download directory |
| Reload-triggered re-lock | A full navigation/reload drops the in-memory unlock singleton -- Task 2 proves this is consistently enforced (never silently skipped), not a new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| N/A | -- | -- | -- | -- | Test-only plan; inherits and does not extend the threat register established in Plans 29-01/29-02/29-03. No new package-manager installs -- no `T-29-SC` row required (`page.waitForEvent("download")`/`page.reload()`/`page.goBack()` are all transitive capabilities of the already-installed `@playwright/test`). |
</threat_model>

<verification>
This plan IS the phase's closing verification gate -- see Task 3. Wave 4 (after Plan 29-04) is
deliberate: Task 3 drives the same four e2e specs Plan 29-04 repairs, against the same fixed
`localhost:8620` `webServer` -- running in parallel with 29-04 would fail deterministically and risks
the `reuseExistingServer` port-collision hazard recorded in STATE.md (Plan 28-02's `data/pv.db`
pollution incident).
</verification>

<success_criteria>
DEBT-02's SC4 evidence bar is closed with a real file-byte proof; SC1's reload and browser-back claims
are closed with live navigation, not just the build artifact; the phase's full test/build/e2e surface
is green as a whole, not just plan-by-plan.
</success_criteria>

<output>
Create `.planning/phases/29-a-real-settings-page-shell-migration/29-05-SUMMARY.md` when done
</output>
