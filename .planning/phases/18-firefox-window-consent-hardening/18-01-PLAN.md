---
phase: 18-firefox-window-consent-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/lib/window-geometry.test.ts
  - extension/e2e-firefox/probe-window-geometry.cjs
  - extension/package.json
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "Given full finite current-window geometry, both the consent window (380x460) and the ceremony window (480x640) open centered over it per centeredWindowPosition()'s documented formula, verified live against a real Firefox build, not just mocks (18-UI-SPEC assertion #1)."
    - "When browser.windows.getLastFocused() returns null/undefined or a partial geometry object (e.g. missing height), centeredWindowPosition() returns {} and the window opens at the browser's default placement — never a crash, never a guessed position (18-UI-SPEC assertion #2, unit-covered)."
    - "If browser.windows.create() itself throws while opening the consent window, tryOpenFallbackWindow()'s catch swallows it and the ceremony still resolves via its existing abandon-timeout rather than hanging."
    - "If browser.windows.create() itself throws while opening the ceremony window, startServerUnlock() catches it and returns { ok: false, error: 'unknown' } with no pending record written and no orphaned alarm."
    - "Both windows open at their fixed size (380x460 / 480x640) and focused:true, unconditionally of geometry availability, verified live for size (18-UI-SPEC assertion #3)."
    - "The consent window closes (verified live: its window handle no longer exists) on both an explicit confirm and an explicit decline; the multi-match candidate list's existing max-h-52 scroll cap is unmodified by this phase's regression work (18-UI-SPEC assertion #4)."
    - "The ceremony window closes (verified live) on a successful password sign-in; a negative computed left/top (edge-positioned/multi-monitor source window) passes through centeredWindowPosition() unclamped, asserted as an exact negative integer pair, never a bound check (18-UI-SPEC assertion #6)."
    - statement: "A second concurrent startServerUnlock() call closes the prior ceremony window and overwrites its nonce (latest wins) — already unit-tested in server-unlock.test.ts; deliberately NOT re-asserted live this phase per RESEARCH.md's Open Question 1 (UX-02's success criteria are centering/sizing/self-close, not concurrency races; unit coverage already exists) — deferred, not silently omitted."
      verification: backstop
  artifacts:
    - "extension/lib/window-geometry.test.ts: new negative-position test case (assertion #6)"
    - "extension/e2e-firefox/probe-window-geometry.cjs: new live-Firefox regression probe (7 GEOM-* gates)"
    - "extension/package.json: test:e2e:firefox:window-geometry + pretest:e2e:firefox:window-geometry script entries"
  key_links:
    - "extension/e2e-firefox/probe-window-geometry.cjs -> extension/entrypoints/background/provider-ceremony.ts's tryOpenFallbackWindow() (drives the REAL browser.windows.create() fallback window directly, never a manually-opened popup tab, so the measured geometry is the genuine production call site)"
    - "extension/e2e-firefox/probe-window-geometry.cjs -> extension/entrypoints/background/server-unlock.ts's startServerUnlock() (drives the REAL ceremony window via the popup's server-ceremony-signin-button)"
  prohibitions:
    - requirement_id: UX-02
      category: safety
      statement: "MUST NOT modify the production behavior of window-geometry.ts, provider-ceremony.ts, or server-unlock.ts while formalizing their regression coverage — the new unit test case and live probe observe/verify the EXISTING centering/sizing/self-close contract only; this phase ships zero production window-lifecycle changes."
      status: unverified
      verification: flagged
      resolution: null
      reason: "UX-02 is scoped as a formalization task per 18-CONTEXT.md/18-RESEARCH.md (the behavior already landed in quick task 260720-16k) — the spec does not mechanically forbid a 'just adding tests' task from also touching the underlying window-open logic, so this is authored descriptor-less per specless-probe-fallback.md §B rather than silently assumed."

specless_probe_note: |
  UX-02 returned unclassified/unresolved from the automated edge-probe (decision/behavioral-
  contract-shaped requirement, not a simple data-shape one). Per the orchestrator's explicit
  resolution: 18-UI-SPEC.md's own "UI Considerations" table (5 covered rows + 1 backstop row,
  applied to the two window surfaces) IS treated as the authoritative edge inventory for UX-02,
  including the two intentional stay-open exceptions (failed resolveChoice send; forbidden-origin
  / ceremony-failed on the ceremony window) and the never-assert-nonnegative constraint on
  negative computed left/top (13-REVIEW-3.md IN-02). All 6 numbered assertions in 18-UI-SPEC.md's
  "Window Geometry & Lifecycle Contract" are lifted into must_haves.truths above (5 as plain
  covered truths, 1 — the double-window-open race — as the flagged backstop scalar, matching the
  UI-SPEC's own 🧪 marking). Prohibition recall (§B, two-stage adversarial pass over UX-02's text)
  surfaced ~4 raw candidates (don't clamp negative positions, don't regress self-close-on-failure,
  don't leave test pollution on the shared UAT account, don't touch production code while "just
  testing"); the first two are routine-engineering already fully owned by explicit UI-SPEC
  assertions/truths above (dropped per Stage 2 precision); the third is an already-accepted,
  documented harness quirk (README.md) not a novel omission (dropped); the fourth is a genuine
  scope-integrity risk with no mechanical spec guard, kept below descriptor-less per §B.
---

<objective>
Formalize UX-02: the Firefox consent (380x460) and ceremony (480x640) window centering, sizing,
and self-close behavior — already implemented and unit-tested (quick task 260720-16k, commit
40d1965) — gets one missing regression-coverage gap closed (negative-position pass-through) and
one genuinely new live-Firefox verification lane, so the contract cannot silently drift again.

Purpose: per D-01/UX-02, the behavior is correct today but has no live-Firefox proof and one
untested pure-function edge case; this plan closes both gaps without touching any production
window-lifecycle code (window-geometry.ts, provider-ceremony.ts, server-unlock.ts stay
byte-for-byte unchanged — see the flagged prohibition above).

Artifacts this phase produces (this plan's share, MANDATORY per downstream_consumer):
extension/e2e-firefox/probe-window-geometry.cjs (new), the
test:e2e:firefox:window-geometry / pretest:e2e:firefox:window-geometry npm script pair, and one
new unit test case in extension/lib/window-geometry.test.ts (see Task 1's exact name). The
remaining two mandatory artifacts (18-SECURITY.md sections, PROJECT.md decision entry) belong to
this phase's sibling plan (18-02-PLAN.md, XBR-03).

Output: a 7th window-geometry.test.ts case, a new permanent e2e-firefox probe lane with 7
GEOM-* gates, and a live-Firefox PASS run (results-probe-window-geometry.json + screenshots) as
UX-02's success-criterion-1 evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-firefox-window-consent-hardening/18-UI-SPEC.md
@.planning/phases/18-firefox-window-consent-hardening/18-RESEARCH.md

@extension/lib/window-geometry.ts
@extension/lib/window-geometry.test.ts
@extension/e2e-firefox/probe-request-xray.cjs
@extension/e2e-firefox/run-core.cjs
@extension/entrypoints/background/provider-ceremony.ts
@extension/entrypoints/background/server-unlock.ts
@extension/e2e-firefox/README.md
@extension/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add negative-position regression case to window-geometry.test.ts (UX-02 assertion #6)</name>
  <files>extension/lib/window-geometry.test.ts</files>
  <read_first>
    extension/lib/window-geometry.ts (centeredWindowPosition's exact formula and doc comment, lines 27-63)
    extension/lib/window-geometry.test.ts (existing 6 cases — plain describe/it, zero mocks)
    18-UI-SPEC.md's "Window Geometry & Lifecycle Contract" section, assertion #6 and its "Negative/off-screen results are intentionally NOT clamped" paragraph (13-REVIEW-3.md IN-02)
  </read_first>
  <action>
    Append one new `it(...)` case inside the existing `describe("centeredWindowPosition", ...)`
    block (after the last existing test, before the closing `});`) — do not create a new
    `describe` block. Call `centeredWindowPosition({ left: -50, top: -20, width: 300, height: 300 }, 380, 460)`
    and assert the result equals `{ left: -90, top: -100 }` exactly via `toEqual` — recompute to
    confirm before writing: left = round(-50 + (300-380)/2) = round(-90) = -90; top =
    round(-20 + (300-460)/2) = round(-100) = -100. Never assert a bound like `toBeLessThan(0)` —
    the exact negative integer pair is the point of this test. Name the test to cite both the
    source finding and the covered assertion, e.g. "passes a negative computed left/top through
    unclamped (multi-monitor edge case, 13-REVIEW-3.md IN-02, 18-UI-SPEC assertion #6)". Add a
    one-line comment above the assertion noting this is intentional, not a bug: a wider new window
    (380 or 480) centered over a current window positioned left of/above the primary display
    legitimately computes a negative left/top, and this must never be clamped to 0. Per this
    plan's flagged prohibition, do NOT touch window-geometry.ts itself — the formula is already
    correct; this task only adds coverage for an existing, correct behavior.
  </action>
  <acceptance_criteria>
    - extension/lib/window-geometry.test.ts contains exactly 7 `it(` blocks inside the single
      `describe("centeredWindowPosition"` block (6 existing + 1 new).
    - The new test's assertion is `toEqual({ left: -90, top: -100 })` for input
      `{ left: -50, top: -20, width: 300, height: 300 }, 380, 460`.
    - `git diff --stat extension/lib/window-geometry.ts` is empty (production formula untouched).
  </acceptance_criteria>
  <verify>
    <automated>cd extension && npx vitest run lib/window-geometry</automated>
  </verify>
  <done>All 7 window-geometry.test.ts cases pass; the new case's exact { left: -90, top: -100 } result is asserted via toEqual, not a bound check; window-geometry.ts diff is empty.</done>
</task>

<task type="auto">
  <name>Task 2: Create probe-window-geometry.cjs and wire its npm script pair</name>
  <files>extension/e2e-firefox/probe-window-geometry.cjs, extension/package.json</files>
  <read_first>
    extension/e2e-firefox/probe-request-xray.cjs (header-comment convention; bootstrap constants EXT_DIR/PROFILE_DIR/SHOTS/RESULTS_FILE/FIREFOX_BINARY; results/record()/shot() helpers; main().then() exit-code tail lines 539-555)
    extension/e2e-firefox/run-core.cjs lines 143-260 (openPopupTab/ensurePopup/newTabTo helpers; the P9-SC1/P9-SC2 sign-in-ceremony-window trigger via server-ceremony-signin-button + handlesBefore/handlesAfter/newHandles diff technique; P12-SC1/P12-SC2's driver.executeScript()-triggered create()/get() pattern lines 456-515) and lines 516-527, 583 (stale-popupHandle guard comments)
    extension/entrypoints/background/provider-ceremony.ts lines 249-303 (tryOpenPopup/tryOpenFallbackWindow, CONSENT_WINDOW_WIDTH=380/CONSENT_WINDOW_HEIGHT=460) and extension/entrypoints/popup/ProviderCeremonyView.tsx (data-testid="provider-confirm"/"provider-decline"/"provider-credential-row-*")
    extension/entrypoints/background/server-unlock.ts lines 76-234 (startServerUnlock, CEREMONY_WINDOW_WIDTH=480/CEREMONY_WINDOW_HEIGHT=640, getCurrentWindowGeometry)
    extension/lib/window-geometry.ts (centeredWindowPosition's exact formula, to duplicate inline)
    extension/package.json lines 17-30 (existing test:e2e:firefox:*/pretest:e2e:firefox:* script pairs)
    extension/e2e-firefox/README.md (prerequisites: PV_EXTENSION_ORIGINS shape incl. the existing moz-extension://* wildcard, npm run build:firefox precondition, EMAIL/PASSWORD defaults)
    18-UI-SPEC.md's "Window Geometry & Lifecycle Contract" section (the 6 numbered assertions, and which stay unit-only)
  </read_first>
  <action>
    Create extension/e2e-firefox/probe-window-geometry.cjs, a new permanent live-Firefox probe.
    Mirror probe-request-xray.cjs's header-comment convention (what this closes, why kept
    permanently, prerequisites) and its bootstrap-constants pattern: EXT_DIR
    (.output/firefox-mv2), PROFILE_DIR/SHOTS suffixed `-probe-window-geometry`, RESULTS_FILE
    `results-probe-window-geometry.json`, SERVER default `http://localhost:8620`, EMAIL/PASSWORD
    reusing the harness's existing shared `uat-prf04@example.local` test-account defaults (never a
    new or real account, per this plan's threat model). Use a fresh FIXED_UUID distinct from every
    other probe's — `f6a7b8c9-d0e1-4234-a567-89abcdef0123` — and FORM_PORT = 8898 (distinct from
    run-core.cjs's 8896, probe-provider-corruption.cjs's 8897, probe-request-xray.cjs's 8899).

    Reuse run-core.cjs's simpler driver.executeScript()-triggered navigator.credentials.create()/
    get() pattern (P12-SC1/P12-SC2 style) for triggering ceremonies — NOT probe-request-xray.cjs's
    genuinely-inline-`<script>`-fixture technique, since window geometry/handle-count reads are
    explicitly NOT realm-sensitive (18-UI-SPEC.md's own carve-out) and the inline-fixture
    machinery exists solely for the unrelated ArrayBuffer-instanceof cross-realm hazard.

    Add a local helper duplicating centeredWindowPosition()'s exact two-line formula
    (left = Math.round(cur.x + (cur.width - newWidth) / 2), top = Math.round(cur.y + (cur.height -
    newHeight) / 2)) with a comment pointing at extension/lib/window-geometry.ts as the single
    source of truth — note explicitly in the header comment that this is a deliberate duplication,
    caught if it ever drifts by window-geometry.test.ts, not silently. Define TOLERANCE_PX = 5 for
    comparing observed-vs-expected window POSITION only (WebDriver getRect() outer-bounds vs the
    extension's own browser.windows reading can differ slightly by WM/DPI) — use exact equality
    for width/height, never for position.

    Flow: (1) openPopupTab(), configure the server URL, submit; (2) read
    driver.manage().window().getRect() on the current popup-tab context as preCeremonyRect BEFORE
    clicking [data-testid="server-ceremony-signin-button"] — this approximates what
    getCurrentWindowGeometry() reads inside the extension at click-time, since no second OS window
    exists yet; diff getAllWindowHandles() before/after the click (mirrors run-core.cjs's own
    P9-SC2 handlesBefore/handlesAfter/newHandles technique — do NOT call ensurePopup() here, it
    would reuse the existing popup tab and miss the real window); switchTo() the new handle
    immediately and getRect() — record gate GEOM-CEREMONY-SIZE (width===480 && height===640) and
    GEOM-CEREMONY-POSITION (observed x/y within TOLERANCE_PX of the formula applied to
    preCeremonyRect); (3) fill input#pv-ext-unlock-email / input#pv-ext-unlock-password, click
    [data-testid="ext-unlock-password-submit"], poll getAllWindowHandles() up to 5s for the
    ceremony handle to disappear — record GEOM-CEREMONY-CLOSE; (4) switch back to the popup tab,
    confirm it advanced past the unlock view (now signed in and unlocked); (5) open a fresh RP tab
    via newTabTo() pointing at a minimal single-route local form server on FORM_PORT (mirror
    run-core.cjs's formServerHtml(), no CSP-strict variant needed); read getRect() on this tab as
    preConsentRect; fire navigator.credentials.create() via driver.executeScript() (rp.id:
    'localhost', a fresh random challenge/user.id, matching P12-SC1's shape) WITHOUT calling
    ensurePopup(); diff window handles before/after to capture the real fallback-window handle
    (tryOpenFallbackWindow()'s genuine window — never the popup tab from step 1, which must stay
    untouched for the rest of this task); switchTo() it, getRect() — record GEOM-CONSENT-SIZE
    (380x460) and GEOM-CONSENT-POSITION (tolerance-compared against preConsentRect's formula);
    click [data-testid="provider-confirm"]; poll for the handle to disappear — record
    GEOM-CONSENT-CLOSE-CONFIRM; (6) switch to the RP tab, refresh, fire
    navigator.credentials.get() (rpId 'localhost', matching the just-created credential, no
    allowCredentials); diff handles again for the second new fallback-window instance; switch to
    it; click [data-testid="provider-decline"] (present regardless of single/multi-match per
    ProviderCeremonyView.tsx); poll for close — record GEOM-CONSENT-CLOSE-DECLINE.

    Explicitly do NOT attempt live coverage of: assertion #2 (missing/partial geometry —
    environment-uncontrollable from outside, stays unit-only), assertion #5's non-close exceptions
    (forbidden-origin, ceremony-failed, the 120s alarm timeout — too slow/environment-specific for
    a routine probe, stays unit-only per server-unlock.test.ts), assertion #6 (pure-function-only,
    Task 1's job), and focused:true (not reliably readable via WebDriver's getRect() — already
    source-verified unchanged at both call sites via this task's own read_first). State this scope
    explicitly in the probe's header comment. Also state, in the header comment, the deliberate
    deferral of RESEARCH.md's Open Question 1 (the double-window-open/"zero-one-many" concurrent
    startServerUnlock() race, 18-UI-SPEC's backstop row): already unit-tested in
    server-unlock.test.ts; this probe does NOT add a live assertion for it since UX-02's success
    criteria are centering/sizing/self-close, not concurrency races — deferred, not silently
    omitted (mirrors this plan's own must_haves backstop entry).

    Wire two new extension/package.json script entries, inserted immediately after the existing
    test:e2e:firefox:server-unlock / pretest:e2e:firefox:server-unlock pair (same relative
    position in both the scripts block's non-pretest and pretest groups, before test:e2e:visual /
    pretest:e2e:visual): "test:e2e:firefox:window-geometry": "node e2e-firefox/probe-window-geometry.cjs"
    and "pretest:e2e:firefox:window-geometry": "wxt build -b firefox".
  </action>
  <acceptance_criteria>
    - extension/e2e-firefox/probe-window-geometry.cjs exists and is valid Node.js (node --check
      passes).
    - It defines exactly 7 gate IDs: GEOM-CEREMONY-SIZE, GEOM-CEREMONY-POSITION,
      GEOM-CEREMONY-CLOSE, GEOM-CONSENT-SIZE, GEOM-CONSENT-POSITION, GEOM-CONSENT-CLOSE-CONFIRM,
      GEOM-CONSENT-CLOSE-DECLINE.
    - extension/package.json contains both "test:e2e:firefox:window-geometry": "node e2e-firefox/probe-window-geometry.cjs"
      and "pretest:e2e:firefox:window-geometry": "wxt build -b firefox".
    - `git diff --stat extension/entrypoints/background/provider-ceremony.ts extension/entrypoints/background/server-unlock.ts extension/lib/window-geometry.ts`
      is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd extension && node --check e2e-firefox/probe-window-geometry.cjs && grep -c "test:e2e:firefox:window-geometry" package.json && grep -c "pretest:e2e:firefox:window-geometry" package.json</automated>
  </verify>
  <done>probe-window-geometry.cjs exists, is syntactically valid, defines all 7 GEOM-* gates; package.json has both new script entries; production window-lifecycle files remain untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Run probe-window-geometry.cjs live against real Firefox + pv-server (UX-02 live-verification evidence)</name>
  <files>extension/e2e-firefox/probe-window-geometry.cjs</files>
  <read_first>
    extension/e2e-firefox/probe-window-geometry.cjs (Task 2's output)
    extension/e2e-firefox/README.md (Prerequisites and Environment variables sections)
  </read_first>
  <action>
    Ensure pv-server is running locally with PV_EXTENSION_ORIGINS including the existing
    moz-extension://* wildcard already required by every other e2e-firefox lane (README.md's
    documented invocation — this probe's fresh FIXED_UUID origin needs no separate registration)
    and the shared uat-prf04@example.local account already exists on that server. Then run:
    `cd extension && npm run pretest:e2e:firefox:window-geometry && npm run test:e2e:firefox:window-geometry`.
    Capture the resulting results-probe-window-geometry.json and screenshots as the live
    verification evidence for UX-02 success criterion 1 (self-validate per the standing
    Playwright/browser-automation UAT authorization — no human checkpoint needed for this
    established headed-Firefox harness pattern, same precedent as Phase 13/14's own probe runs).
    If a selector/timing issue in the PROBE ITSELF (never production code) causes a false
    failure, fix the probe script only — provider-ceremony.ts, server-unlock.ts, and
    window-geometry.ts stay byte-for-byte unchanged this task, per this plan's flagged
    prohibition on production-behavior drift. If any of the 7 gates genuinely fails against
    correct, unmodified production code, STOP and report the failure rather than loosening the
    gate's assertion to force a pass.
  </action>
  <acceptance_criteria>
    - results-probe-window-geometry.json (or the configured PV_FF_SHOTS_DIR equivalent) exists
      and records all 7 GEOM-* gates as PASS.
    - The probe process exits 0.
    - `git diff --stat extension/entrypoints/background/provider-ceremony.ts extension/entrypoints/background/server-unlock.ts extension/lib/window-geometry.ts`
      is still empty after this task.
  </acceptance_criteria>
  <verify>
    <automated>cd extension && npm run pretest:e2e:firefox:window-geometry && npm run test:e2e:firefox:window-geometry</automated>
  </verify>
  <done>Live Firefox run against a real running pv-server produces 7/7 GEOM-* gates PASS, exit code 0; this is UX-02 success-criterion-1's live-verification evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e-firefox probe process ↔ real packaged extension build | probe-window-geometry.cjs drives the production-identical `.output/firefox-mv2` build against a real pv-server; observes only, never alters runtime behavior |
| probe process ↔ pv-server test account | Dedicated `uat-prf04@example.local` shared harness account only, never real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-18-01 | Tampering | window-geometry.test.ts / probe-window-geometry.cjs | low | accept | Test/probe files only, never bundled into the shipped `.output/*` extension build — mirrors 14-SECURITY.md's T-14-06/AR-14-02 precedent |
| T-18-02 | Information Disclosure | probe-window-geometry.cjs test credentials | low | mitigate | Reuses the harness's own existing dedicated `uat-prf04@example.local` account/password (env-var overridable), never Bartek's real credentials, never hardcoded into the shipped extension |
| T-18-03 | Tampering | window-open call sites (provider-ceremony.ts / server-unlock.ts) | high | mitigate | This plan's files_modified is scoped to test/probe/package.json only; Tasks 1-3 each carry an explicit `git diff --stat` acceptance criterion proving zero production-code drift, closing the risk by construction rather than by review alone |
| T-18-SC | Tampering | npm installs | n/a | accept | No new packages this phase — `selenium-webdriver`/`geckodriver` already pinned devDependencies (18-RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `cd extension && npx vitest run lib/window-geometry` — 7/7 cases pass.
- `cd extension && node --check e2e-firefox/probe-window-geometry.cjs` — valid syntax.
- `cd extension && npm run pretest:e2e:firefox:window-geometry && npm run test:e2e:firefox:window-geometry` — 7/7 GEOM-* gates PASS, exit 0, against a real running pv-server + real Firefox.
- `git diff --stat extension/entrypoints/background/provider-ceremony.ts extension/entrypoints/background/server-unlock.ts extension/lib/window-geometry.ts` — empty at every task boundary.
</verification>

<success_criteria>
UX-02 formalized: the negative-position regression case is unit-covered, a new permanent
live-Firefox probe proves the centering formula, fixed sizing, and self-close (confirm/decline/
success) contract against a real browser, its npm script pair matches the project's
`test:e2e:firefox:*` naming family, and zero production window-lifecycle code changed.
</success_criteria>

<output>
Create `.planning/phases/18-firefox-window-consent-hardening/18-01-SUMMARY.md` when done
</output>
