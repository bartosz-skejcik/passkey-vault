---
phase: 10-autofill-login-totp-card-identity
plan: 09
type: execute
wave: 5
depends_on: ["10-01", "10-04", "10-05"]
files_modified:
  - extension/lib/messaging/ext-protocol.ts
  - extension/entrypoints/background/autofill-frame.ts
  - extension/entrypoints/background/autofill-frame.test.ts
  - extension/entrypoints/background/router.ts
  - extension/entrypoints/background/router.test.ts
autonomous: true
requirements: [FILL-01, FILL-02, FILL-03, FILL-04]

must_haves:
  truths:
    - "A content script can request the metadata-only matches for ITS OWN frame via a new autofill.matchFrame kind — origin derived from the platform-provided sender (originFromContentSender), never from the message payload."
    - "A content script can request a fill of ITS OWN frame via autofill.fillFrame; the background re-derives the sender's origin and frameId from platform data and re-runs itemMatchesOrigin before dispatching content.fill — a content script can never drive a fill for a frame or origin other than its own."
    - "The content-frame channel is a SEPARATE onMessage listener from the popup router; the popup router's WR-01 gate (which drops all web-origin senders) is UNCHANGED, and the frame listener refuses any sender that is not a content script of THIS extension with a parseable origin."
    - "autofill.matchFrame returns ONLY metadata (itemId, kind, label, maskedHint) plus the frame origin — never a plaintext field value, never a TOTP secret."
    - "autofill.fillFrame's response is value-free (ok/reason only); plaintext flows background→content via content.fill exactly as in 10-04, never back to the caller."
  artifacts:
    - path: "extension/lib/messaging/ext-protocol.ts"
      provides: "autofill.matchFrame / autofill.fillFrame message kinds + response map entries (content-script→background direction)"
      contains: "autofill.matchFrame"
    - path: "extension/entrypoints/background/autofill-frame.ts"
      provides: "handleMatchFrame(), handleFillFrame(), and assertContentSender() — the content-frame channel handlers + their dedicated sender guard"
      contains: "assertContentSender"
    - path: "extension/entrypoints/background/autofill-frame.test.ts"
      provides: "Adversarial coverage: a web-origin sender spoofing a payload origin, a cross-origin fill attempt, the popup-sender rejection on this channel"
  key_links:
    - from: "extension/entrypoints/background/router.ts"
      to: "extension/entrypoints/background/autofill-frame.ts"
      via: "a second runtime.onMessage listener registered for the content-frame kinds, guarded by assertContentSender()"
      pattern: "assertContentSender"
---

<objective>
Add the content-relay→background channel that lets the in-page overlay (plan 10-10) know which
vault items match the frame it lives in, and fill that frame — WITHOUT ever letting a page's
content script reach cross-origin items or drive a fill for a frame it does not own. This is the
security-critical half of Bartek's in-page-affordance decision; 10-10 is pure UI on top of it.
</objective>

<context>
@.planning/phases/10-autofill-login-totp-card-identity/10-01-SUMMARY.md
@.planning/phases/10-autofill-login-totp-card-identity/10-04-SUMMARY.md
@.planning/phases/10-autofill-login-totp-card-identity/10-05-SUMMARY.md
# Read the REAL files at execution time — they are ahead of the plan interfaces:
# extension/lib/messaging/ext-protocol.ts, extension/entrypoints/background/router.ts,
# extension/entrypoints/background/frame-guard.ts, extension/entrypoints/background/autofill-match.ts
</context>

<architecture_note>
**The popup router and the content-frame channel are DELIBERATELY two separate listeners.**
`registerMessageRouter()` in router.ts drops every message whose `sender.url` is not
`chrome-extension://<own-id>/…` (the WR-01 gate) — so a content script's message never reaches
the popup `handle()` at all. Do NOT weaken that gate. Instead register a SECOND
`runtime.onMessage` listener (call it `registerAutofillFrameChannel()`) that accepts ONLY the two
new kinds and ONLY from a content-script sender of this extension. This keeps the popup-privilege
tier (session.*/vault.*) completely unreachable from any page, while giving the in-page overlay
exactly the two capabilities it needs and nothing more.

**Why this is safe** (write this reasoning into the code as comments):
- `assertContentSender(sender)` passes only when `sender.id === browser.runtime.id` AND
  `sender.tab` is defined (a real tab/content-script sender) AND `originFromContentSender(sender)`
  parses to a non-null origin. A web page cannot forge `sender.id`/`sender.tab`/`sender.origin`
  — they are platform-provided.
- `handleMatchFrame` runs the SAME `itemMatchesOrigin` gate against the SENDER'S OWN origin, so
  `evil.com`'s content script can only ever learn about items the user saved FOR `evil.com`
  (metadata only — label + masked hint, never a secret). This is the standard password-manager
  model; no cross-origin data crosses.
- `handleFillFrame` addresses the fill to `{ tabId: sender.tab.id, frameId: sender.frameId }` and
  re-verifies `itemMatchesOrigin(item, senderOrigin)` before dispatching `content.fill`. A content
  script cannot name a different frame or a different origin.
- The overlay showing available accounts on page load is NOT gesture-gated (Bartek's approved
  relaxation — it matches NordPass/1Password/Chrome); the FILL still requires the user's click,
  which is what `handleFillFrame` is driven by.
</architecture_note>

<tasks>

<task type="auto">
  <name>Task 1: Extend the contract with the content-frame kinds</name>
  <files>extension/lib/messaging/ext-protocol.ts</files>
  <read_first>extension/lib/messaging/ext-protocol.ts, extension/lib/autofill/types.ts</read_first>
  <action>
  ADD to the existing Message union + MessageResponseMap (do not restructure):
  - `{ kind: "autofill.matchFrame"; detected: DetectedFields }` → reuse `AutofillMatchResult`
    (same shape autofill.match returns: pageState/origin/detected/matches). The request carries
    the content script's OWN `detected` map (it already computed it locally via detectAll) so the
    background does not need a content.detect round-trip back into the same frame. It carries NO
    origin — the background derives it from the sender.
  - `{ kind: "autofill.fillFrame"; itemId: string; kind_: FillKind }` → the SAME response shape as
    autofill.fill (`{ ok: true } | { ok: false; reason: ... }`), value-free.
  Keep the JSON-safe-primitives-only rule (no binary types).
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit && grep -q "autofill.matchFrame" lib/messaging/ext-protocol.ts</automated>
  </verify>
  <acceptance_criteria>
    - ext-protocol.ts contains "autofill.matchFrame" and "autofill.fillFrame"
    - ext-protocol.ts still contains "autofill.match" and "session.status" (additive, not replaced)
    - `cd extension && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The one typed contract now also carries the two content-frame kinds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: autofill-frame.ts — content-frame handlers + dedicated sender guard</name>
  <files>extension/entrypoints/background/autofill-frame.ts, extension/entrypoints/background/autofill-frame.test.ts</files>
  <read_first>extension/entrypoints/background/frame-guard.ts, extension/entrypoints/background/autofill-match.ts</read_first>
  <behavior>
    - Test: assertContentSender passes for { id: ownId, tab: {id:7}, origin: "https://a.example", frameId: 0 } and refuses (a) a popup sender (no tab, extension origin), (b) a foreign extension id, (c) a sender whose origin/url neither parse.
    - Test: handleMatchFrame returns matches for items bound to the SENDER's origin only; an item bound to https://other.example is absent even if it exists in the store.
    - Test: handleMatchFrame IGNORES any origin-looking field in the payload — pass a request whose (hypothetical) payload names https://victim.example and assert the resolved origin is the sender's, not the payload's. (The request shape carries no origin field, so this is enforced by construction — assert the handler reads sender, not message.)
    - Test: handleFillFrame refuses (reason "origin-mismatch") when the item's origin does not match the sender's origin, and dispatches content.fill to { tabId: sender.tab.id, frameId: sender.frameId } when it does; the response never carries field values.
    - Test: handleFillFrame refuses when the vault is locked (reason "locked").
  </behavior>
  <action>
  Create `autofill-frame.ts`. Reuse frame-guard.ts's `originFromContentSender` and
  `itemMatchesOrigin`, and mirror autofill-match.ts's decrypt/getItems/totpNow reuse — do NOT
  duplicate that logic; import it (extract a shared helper from autofill-match.ts if needed rather
  than copy-paste). Export:
  - `assertContentSender(sender): { ok: true; origin: string; tabId: number; frameId: number } | { ok: false }`
    — the single guard. Returns the resolved origin/tabId/frameId so callers never re-derive them.
  - `handleMatchFrame(message, sender)` — guard; if refused return an empty `restricted` result.
    Otherwise assemble matches exactly like handleAutofillMatch BUT using the sender-derived origin
    and the payload-provided `detected` (own frame), gating each item on `detected[kind]` AND
    `itemMatchesOrigin(item, senderOrigin)`. Metadata only.
  - `handleFillFrame(message, sender)` — guard; ensureHydrated (locked→reason "locked"); look up
    the item; re-check `item.fields.type === message.kind_` and `itemMatchesOrigin(item, senderOrigin)`
    (mismatch→"origin-mismatch"); build the FillValues (reuse autofill-match.ts's builder, incl.
    fresh totpNow) and `browser.tabs.sendMessage(sender.tab.id, {kind:"content.fill", values}, {frameId: sender.frameId})`.
    Return `{ ok: true }` or a value-free failure reason.
  </action>
  <verify>
    <automated>cd extension && npx vitest run entrypoints/background/autofill-frame.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - autofill-frame.ts exports assertContentSender, handleMatchFrame, handleFillFrame
    - autofill-frame.ts contains no reference to a payload-supplied origin (grep: the handlers read sender, not message, for origin)
    - all autofill-frame.test.ts cases pass
  </acceptance_criteria>
  <done>The content-frame handlers exist, origin-locked to the sender's own frame, metadata-only match, value-free fill response.</done>
</task>

<task type="auto">
  <name>Task 3: Register the content-frame channel as a second listener</name>
  <files>extension/entrypoints/background/router.ts, extension/entrypoints/background/router.test.ts</files>
  <read_first>extension/entrypoints/background/router.ts, extension/entrypoints/background/autofill-frame.ts</read_first>
  <action>
  Add `registerAutofillFrameChannel()` to router.ts — a SECOND `browser.runtime.onMessage`
  listener that: returns undefined for any kind other than autofill.matchFrame/autofill.fillFrame
  (lets the popup router and content-relay's own listener handle theirs); for the two frame kinds,
  routes to handleMatchFrame/handleFillFrame with a rejection path mirroring the popup router's
  (never leaves sendResponse uncalled). Do NOT route these through the popup `handle()` — they must
  bypass the WR-01 extension-origin gate, which is exactly why they are a separate listener. Call
  `registerAutofillFrameChannel()` from background.ts's main() right after `registerMessageRouter()`.
  Add a router.test.ts case proving a content-sender autofill.matchFrame is accepted by THIS
  listener while a content-sender session.status is still refused by the popup router.
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit && npx vitest run && grep -q "registerAutofillFrameChannel" entrypoints/background/router.ts</automated>
  </verify>
  <acceptance_criteria>
    - router.ts exports/defines registerAutofillFrameChannel and background.ts calls it
    - the popup router's WR-01 gate is textually unchanged (session.*/vault.* still refused from web-origin senders)
    - `cd extension && npx tsc --noEmit` exits 0 and the full suite passes
  </acceptance_criteria>
  <done>The content-frame channel is live as a separate, strictly-guarded listener; the popup tier is untouched.</done>
</task>

</tasks>

<verification>
- [ ] `cd extension && npx tsc --noEmit` passes
- [ ] `cd extension && npx vitest run` passes
- [ ] `grep -n "autofill.matchFrame" extension/entrypoints/background/router.ts` shows it routed through the NEW listener, not the popup handle()
</verification>

<output>
Create `.planning/phases/10-autofill-login-totp-card-identity/10-09-SUMMARY.md` per the template
(including the coverage: block for FILL-01..04). Record the real ext-protocol.ts/router.ts shapes
as found and any divergence from this plan's assumptions.
</output>
