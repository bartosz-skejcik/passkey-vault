---
phase: 32-putting-things-into-shared-folders
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pv-server/src/routes/vault.rs
  - crates/pv-provider/src/ceremony.rs
autonomous: true
requirements: [DEBT-04]

must_haves:
  truths:
    - "cargo clippy --workspace --all-targets -- -D warnings exits 0 workspace-wide, not just for vault.rs's named 19 explicit_auto_deref sites."
    - "No behavior changes anywhere in this plan — cargo test --workspace --no-fail-fast passes identically before and after."
  artifacts:
    - "vault.rs's 19 explicit_auto_deref call sites (all calls into this file's own tx-taking helper functions) use &mut tx, not &mut *tx"
    - "ceremony.rs's EXT-10 doc comment (lines ~137-170) no longer contains a line-initial '1)' token that rustdoc's Markdown parser misreads as an ordered-list marker"
  key_links:
    - "This plan runs alone in wave 1, before 32-01 (wave 2): 32-01's every live-e2e verify run triggers playwright.config.ts's own `cargo build --release -p pv-server` from the live tree, which would race a concurrent edit to vault.rs if the two plans ran in parallel (32-PLAN-CHECK.md B-5) — see this plan's objective section below for the full reasoning"
---

<objective>
Close DEBT-04: `cargo clippy --workspace --all-targets -- -D warnings` must exit 0. Independently re-measured by the plan-check pass: exactly 19 `explicit_auto_deref` errors in `vault.rs` (all in this file's own tx-taking helper call sites) plus 6 `doc_lazy_continuation` errors in `crates/pv-provider/src/ceremony.rs` (an accidental Markdown ordered-list marker at line 153) plus 3 "could not compile" summary lines — 28 error lines total, confirmed complete (re-run with both lints `-A`'d: zero errors/warnings workspace-wide, so nothing else hides behind the failing lib build).

**Scope note (2026-08-19, product reversal):** this plan previously also carried a fix scoping `move_item`'s `item_shares` DELETE to `item_bucket` destinations only, per 32-CONTEXT.md Area 2's (then-locked) decision that pre-existing per-item shares should survive an ordinary-folder move. `32-PLAN-CHECK.md`'s B-1 finding disproved the decision's own premise: a surviving `item_shares` row does not restore read access (the item is re-encrypted under the destination collection's key, which that recipient never holds) — it only preserves `Membership<Item, RequireEdit>`, and `vault::update` has no collection guard while `vault::delete` is the same `RequireEdit` gate, so the surviving grant is purely destructive (overwrite-or-delete-what-you-cannot-see) with zero access benefit. Bartek reversed the decision (32-CONTEXT.md Area 2, "DECISION REVERSED 2026-08-19"). **`move_item`'s DELETE is therefore untouched by this phase** — it stays exactly as shipped, unconditional on every move into any collection, and `crates/pv-server/tests/sync_shared.rs:1117`'s existing test needs no retarget and is not touched by this plan. This plan is DEBT-04-only.

**Wave placement (32-PLAN-CHECK.md B-5):** runs ALONE in wave 1, strictly before 32-01 (wave 2). `web/playwright.config.ts` builds `pv-server` from the live tree (`cargo build --manifest-path {repo}/Cargo.toml --release -p pv-server`) for every one of 32-01's live verify runs — running this plan's `vault.rs` edit concurrently with that build would race a half-applied clippy sweep against a live compile, exactly the "no repo builds while an agent writes" hazard this repo has already logged an incident for. Serializing costs little: this plan is small, Rust-only, and purely mechanical.

Purpose: closes DEBT-04 (open since Phase 24, WINDOWS #1 and #3).
Output: a fully clean, workspace-wide `cargo clippy --all-targets -- -D warnings` run, zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-putting-things-into-shared-folders/32-CONTEXT.md
@.planning/phases/32-putting-things-into-shared-folders/32-RESEARCH.md
@.planning/phases/32-putting-things-into-shared-folders/32-PLAN-CHECK.md

@crates/pv-server/src/routes/vault.rs
@crates/pv-provider/src/ceremony.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-04 — cargo clippy --workspace --all-targets -- -D warnings exits 0</name>
  <files>crates/pv-server/src/routes/vault.rs, crates/pv-provider/src/ceremony.rs</files>
  <action>
  Two independent, purely-mechanical fixes (confirmed twice now — by RESEARCH.md and independently re-measured by 32-PLAN-CHECK.md — 25 real errors: 19 in vault.rs, 6 in ceremony.rs, plus 3 "could not compile" summary lines; with both lints `-A`'d, the workspace is completely clean, so no other finding is hiding behind the failing lib build):

  **vault.rs (19 `explicit_auto_deref` errors):** every flagged site is a call INTO this file's own helper functions (`resolve_recipients`, `resolve_collection_members`, `bump_collection_revision`, `bump_recipients_vault_revision`, `bump_direct_share_revision`, `claim_item_bucket_edit_in_tx` — all declared `tx: &mut sqlx::SqliteConnection`) where the caller currently writes `&mut *tx`; clippy's own suggested fix is `&mut tx`. This is NOT every `&mut *tx` in the file — 28 other occurrences pass `&mut *tx` directly to `sqlx::query(...).fetch_optional/execute/fetch_one(...)` (an `impl Executor`-generic method), which clippy does not flag; leave those untouched. Run `cargo clippy -p pv-server --all-targets -- -D warnings` and apply exactly its 19 suggested `&mut *tx` -> `&mut tx` replacements, nothing else in this file.

  **ceremony.rs (6 `doc_lazy_continuation` errors, lines 154-159):** the doc comment at lines 137-170 (the EXT-10 decision record) contains, at line 153, the substring "(EXT-10 Task\n/// 1) confirms this empirically..." — rustdoc's Markdown parser reads the line-initial `1)` as an ordered-list marker (CommonMark permits `)` as a list delimiter), turning lines 154-159 into unindented "continuation" lines of that accidental list item, which clippy's `doc_lazy_continuation` lint rejects. Reflow this one paragraph (content unchanged, meaning unchanged) so `1)` is never the first token of a doc-comment line — e.g. keep "(EXT-10 Task 1)" together on one line and rewrap the remaining sentence normally. Do not add indentation to "fix" it as a real list — there is no real list here, the fix is removing the accidental trigger.
  </action>
  <verify>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <done>`cargo clippy --workspace --all-targets -- -D warnings` exits 0, workspace-wide, and `cargo test --workspace --no-fail-fast` passes identically to before this task (zero behavior change).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced or touched — this plan is a pure lint/style fix with zero behavior change. `move_item`'s DELETE (the one substantive server-side authorization surface this phase originally planned to touch here) is explicitly untouched, per the product reversal recorded above.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-07 | N/A (mechanical) | `vault.rs`/`ceremony.rs` clippy fixes | — | mitigate | Zero behavior change, verified by `cargo test --workspace --no-fail-fast` passing identically before and after — not merely by clippy's own exit code. |

</threat_model>

<verification>
- `cargo clippy --workspace --all-targets -- -D warnings` — exits 0.
- `cargo test --workspace --no-fail-fast` — identical pass/fail outcome to the pre-task baseline (zero behavior change).
</verification>

<success_criteria>
SC5 (DEBT-04) is met workspace-wide, with zero behavior change anywhere in the server.
</success_criteria>

<output>
Create `.planning/phases/32-putting-things-into-shared-folders/32-03-SUMMARY.md` when done
</output>
