---
phase: 32-putting-things-into-shared-folders
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pv-server/src/routes/vault.rs
  - crates/pv-server/tests/sync_shared.rs
  - crates/pv-provider/src/ceremony.rs
autonomous: true
requirements: [ORG-01, ORG-02, DEBT-04]

must_haves:
  truths:
    - "Pre-existing per-item shares (item_shares) survive a move of an item into an ORDINARY shared folder — the recipient's direct grant still resolves access via combine_access afterward."
    - "Pre-existing per-item shares are still dropped when the destination is a family-wide item_bucket, preserving the contributor-escalation guard that DELETE originally existed for."
    - "cargo clippy --workspace --all-targets -- -D warnings exits 0 workspace-wide, not just for vault.rs's named 19 explicit_auto_deref sites."
  artifacts:
    - "crates/pv-server/src/routes/vault.rs's move_item scopes its item_shares DELETE to dest_is_item_bucket only"
    - "crates/pv-server/tests/sync_shared.rs has a regression test for BOTH branches (item_bucket: dropped; ordinary collection: survives)"
  key_links:
    - "The DELETE's condition reuses the same dest_is_item_bucket boolean move_item already computes and threads through to the post-move claim (vault.rs:1020-1034, :1117-1120) — no new computation, no new query"
---

<objective>
Reconcile `vault.rs::move_item`'s shipped behavior with 32-CONTEXT.md's locked Area 2 decision — pre-existing `item_shares` survive a move into an ORDINARY shared folder — which the code currently contradicts by deleting `item_shares` unconditionally on every move into any collection. Also close DEBT-04: `cargo clippy --workspace --all-targets -- -D warnings` must exit 0, which requires fixing both the 19 named `vault.rs` `explicit_auto_deref` errors and 6 unnamed `pv-provider/src/ceremony.rs` `doc_lazy_continuation` errors.

This is a change to shipped, tested server behavior — 32-CONTEXT.md's own text: "it needs its own regression test and an explicit note in the SUMMARY, not a quiet edit." This plan runs Rust-only, independent of the client-side destination-control work in 32-01/32-02 (no shared files, no shared runtime dependency — a personal item with no direct share behaves identically before and after this fix, which is the scenario 32-01's own tracer exercises).

Purpose: closes the CONTEXT.md-vs-code contradiction 32-RESEARCH.md's Pitfall 1 identifies, and DEBT-04 (open since Phase 24, WINDOWS #1/#3).
Output: `move_item`'s item_shares DELETE scoped correctly, a regression test on both branches, and a fully clean workspace-wide clippy run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-putting-things-into-shared-folders/32-CONTEXT.md
@.planning/phases/32-putting-things-into-shared-folders/32-RESEARCH.md

@crates/pv-server/src/routes/vault.rs
@crates/pv-server/tests/sync_shared.rs
@crates/pv-server/src/routes/membership.rs
@crates/pv-provider/src/ceremony.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: scope the item_shares DELETE to item_bucket destinations only, regression test on both branches</name>
  <reversibility rating="reversible">32-CONTEXT.md's Area 2 already locked this decision (with its own recorded correction, 2026-08-19) — the door is already walked through. Undo cost is one condition and its two tests.</reversibility>
  <files>crates/pv-server/src/routes/vault.rs, crates/pv-server/tests/sync_shared.rs</files>
  <action>
  Per 32-CONTEXT.md Area 2's locked decision (and its correction: the code was found to unconditionally delete, contradicting the decision, not confirming it) and 32-RESEARCH.md Pitfall 1's recommended fix:

  In `move_item` (`vault.rs`), the DELETE at what is currently `if req.new_collection_id.is_some() { sqlx::query("DELETE FROM item_shares WHERE item_id = ?")... }` (around line 1193-1207) must be scoped to `if dest_is_item_bucket {`  — `dest_is_item_bucket` is already computed earlier in this same function (around line 1020-1034) and already threaded through to the post-move `claim_item_bucket_edit_in_tx` call (around line 1117-1120); reuse that exact boolean, do not recompute. Update the DELETE's own surrounding doc comment (the "BL-01" comment directly above it) to state the new scope precisely: the escalation-prevention rationale (WR-10/HI-03, a contributor moving items between differently-leveled item_buckets) applies only to item_bucket destinations; for an ordinary shared folder, `Item::resolve_access`'s existing `combine_access(collection_access, item_share_access)` (`membership.rs:386`) already does the right thing and requires no change — do not touch that function.

  Do NOT touch `bump_direct_share_revision`'s own unconditional call (line ~1192, runs before the now-conditional DELETE) — it stays unconditional and correct regardless of destination kind: `pull_shared_direct` filters `WHERE ... collection_id IS NULL` (`sync.rs:401-402`), so a direct-share recipient's "direct" bucket view loses this item on ANY move into ANY collection whether or not the `item_shares` row itself survives — the counter bump is about that visibility change, not about the row's existence.

  **Regression test, both branches** (in `crates/pv-server/tests/sync_shared.rs`):
  - **Retarget the existing `share_then_move_into_collection_bumps_recipients_direct_revision_and_revokes_their_access`** (currently ~line 1117): today it moves into an ORDINARY collection and asserts the recipient's post-move `PUT`/`DELETE` on the item both return 404 ("item_shares row must be gone"). With this fix, that assertion is now WRONG for an ordinary collection — the row survives, `combine_access` resolves the recipient to `Edit` via the surviving `item_shares` row alone (they hold no `collection_keys` row, so `collection_access` is `None`, and `combine_access(None, Some(Edit)) = Some(Edit)`), so the post-move `PUT` must now assert **200 OK**, not 404. Retarget this test's destination collection to a real `item_bucket` (`POST /api/vault/collections` with `family_wide_kind: "item_bucket"`, `family_wide_access_level: "edit"` — see `collections.rs:48-121` for the exact request fields; `family_wide_sharing.rs` has existing precedent for constructing one in a test) so its ORIGINAL intent (item_shares dropped, recipient's stale grant no longer resolves — WR-10's escalation-prevention rationale) is preserved and still asserts 404, honestly against the branch it now actually tests.
  - **Add a new test** for the ORDINARY-collection case this file no longer covers after the retarget above: grant a direct `item_shares` row on a personal item, move it into an ordinary (non-family-wide) collection the recipient is NOT a member of, and assert the recipient's `PUT` on the item now succeeds (200) via the surviving `item_shares` row — proving 32-CONTEXT.md's Area 2 decision literally. Cross-check `bump_direct_share_revision`'s counter STILL bumps on this branch too (mirrors the retargeted test's existing assertion — the recipient's "direct" view still changes even though the row survives, per the doc-comment update above).
  </action>
  <verify>
    <automated>cargo test -p pv-server --test sync_shared -- item_shares --nocapture</automated>
  </verify>
  <done>Both branches (item_bucket: dropped, recipient loses access; ordinary collection: survives, recipient's access resolves via combine_access) are asserted and pass.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-04 — cargo clippy --workspace --all-targets -- -D warnings exits 0</name>
  <files>crates/pv-server/src/routes/vault.rs, crates/pv-provider/src/ceremony.rs</files>
  <action>
  Two independent, purely-mechanical fixes (confirmed by running the exact command this session — 25 real errors: 19 in vault.rs, 6 in ceremony.rs, plus 3 "could not compile" summary lines):

  **vault.rs (19 `explicit_auto_deref` errors):** every flagged site is a call INTO this file's own helper functions (`resolve_recipients`, `resolve_collection_members`, `bump_collection_revision`, `bump_recipients_vault_revision`, `bump_direct_share_revision`, `claim_item_bucket_edit_in_tx` — all declared `tx: &mut sqlx::SqliteConnection`) where the caller currently writes `&mut *tx`; clippy's own suggested fix is `&mut tx`. This is NOT every `&mut *tx` in the file — 28 other occurrences pass `&mut *tx` directly to `sqlx::query(...).fetch_optional/execute/fetch_one(...)` (an `impl Executor`-generic method), which clippy does not flag; leave those untouched. Fix only the sites clippy names — run `cargo clippy -p pv-server --all-targets -- -D warnings` after Task 1's edit lands (Task 1's own condition-swap does not touch any `&mut *tx` call site, so the flagged line count is unaffected by task order) and apply exactly its 19 suggested `&mut *tx` → `&mut tx` replacements.

  **ceremony.rs (6 `doc_lazy_continuation` errors, lines 154-159):** the doc comment at lines 137-170 (the EXT-10 decision record) contains, at line 153, the substring "(EXT-10 Task\n/// 1) confirms this empirically..." — rustdoc's Markdown parser reads the line-initial `1)` as an ordered-list marker (CommonMark permits `)` as a list delimiter), turning lines 154-159 into unindented "continuation" lines of that accidental list item, which clippy's `doc_lazy_continuation` lint rejects. Reflow this one paragraph (content unchanged, meaning unchanged) so `1)` is never the first token of a doc-comment line — e.g. keep "(EXT-10 Task 1)" together on one line and rewrap the remaining sentence normally. Do not add indentation to "fix" it as a real list — there is no real list here, the fix is removing the accidental trigger.
  </action>
  <verify>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <done>`cargo clippy --workspace --all-targets -- -D warnings` exits 0, workspace-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `vault.rs::move_item`'s DELETE condition vs `membership.rs::Item::resolve_access`'s `combine_access` | The access-resolution consequence of whether a direct grant row survives a scope move. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Elevation of Privilege | `move_item`'s item_shares DELETE, scoped incorrectly | high | mitigate | The condition reuses the EXACT `dest_is_item_bucket` boolean the pre-existing `claim_item_bucket_edit_in_tx` escalation guard already relies on (`vault.rs:1020-1034`) — never a fresh, independently-fallible re-derivation. A regression test on the item_bucket branch (Task 1) proves the WR-10/HI-03 escalation-prevention behavior still holds; reverting the scope (deleting unconditionally, or not at all) is what the two-branch test set is built to catch. |
| T-32-06 | Information Disclosure | A direct `item_shares` recipient (not a collection member) retains authorization but no read path once the item is collection-scoped | low | accept | Pre-existing, out-of-scope per 32-RESEARCH.md's own recommendation: `pull_shared_direct` filters `collection_id IS NULL` (`sync.rs:401-402`), so this grant is authorization-only, functionally inert until/unless the recipient is also added to the folder. Fixing it touches `pull_shared_direct`'s query shape, a strictly bigger surface than this phase's scope-move path; 32-CONTEXT.md's own deferred-ideas list already declines "collapsing per-item shares into folder membership." |
| T-32-07 | N/A (DEBT-04, mechanical) | `vault.rs`/`ceremony.rs` clippy fixes | — | mitigate | Pure lint/style, zero behavior change — verified by the full `cargo test --workspace` suite passing unchanged after both fixes, not merely by clippy's own exit code. |

</threat_model>

<verification>
- `cargo test -p pv-server --test sync_shared -- item_shares --nocapture` — both branches pass.
- `cargo test --workspace --no-fail-fast` — no regression from either task.
- `cargo clippy --workspace --all-targets -- -D warnings` — exits 0.
</verification>

<success_criteria>
SC5 (DEBT-04) is met workspace-wide. The item_shares-survival decision from 32-CONTEXT.md Area 2 is implemented and independently regression-tested on both branches, with the WR-10/HI-03 escalation guard the DELETE originally protected still intact and tested for item_bucket destinations.
</success_criteria>

<output>
Create `.planning/phases/32-putting-things-into-shared-folders/32-03-SUMMARY.md` when done
</output>
