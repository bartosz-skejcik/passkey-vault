---
phase: 32-putting-things-into-shared-folders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/vault/store.ts
  - web/src/components/vault/ItemForm.tsx
  - web/src/components/vault/DetailPanel.tsx
  - web/src/lib/i18n/dictionary.ts
  - web/e2e/sharing.spec.ts
  - web/src/lib/vault/moveVaultItem.real-wasm.test.ts
autonomous: true
requirements: [ORG-01, ORG-02, ORG-04]

must_haves:
  truths:
    - "In the item editor, selecting an existing shared folder as the destination (on create or on edit) results in the item's server-side collection_id matching that folder, and the destination survives save, a page reload, and a sync round trip."
    - "A second real account holding edit or read access to the destination folder reads the moved item's actual decrypted field values in a live run (recipient-side, real crypto)."
    - "Creating an item with a shared folder pre-selected never leaves it silently stranded in personal scope: if the move half of the two-call sequence fails after the create half succeeds, the user is told honestly and a retry moves (never re-creates) the same item."
    - "Family-wide item_bucket collections never appear as a selectable destination, and a folder the caller cannot write to (read or hidden_password) appears but is disabled, with the reason."
  artifacts:
    - "web/src/lib/vault/store.ts exports moveVaultItem(id, rawFields, currentRevision, newCollectionId)"
    - "web/src/components/vault/ItemForm.tsx's item-folder-select renders 'Moje foldery' and 'Udostępnione foldery' optgroups alongside the existing 'Bez folderu' option"
    - "web/src/lib/vault/moveVaultItem.real-wasm.test.ts proves the destination-key dispatch with genuine WASM crypto, no mocked @/lib/crypto"
  key_links:
    - "ItemForm.tsx's handleSubmit dispatches to moveVaultItem (not updateVaultItem) exactly when the selected destination differs from the item's current collectionId"
    - "DetailPanel.tsx passes item.collectionId into ItemForm as the new currentCollectionId prop so edit mode knows the item's current scope"
    - "moveVaultItem calls moveItemToCollection (web/src/lib/vault/api.ts), the existing wire wrapper for PUT /api/vault/items/{id}/collection — vault.rs::move_item is unchanged by this plan"
---

<objective>
Make an existing shared folder a selectable destination in the item editor (create AND edit), wired to real re-encryption under the destination scope's key, proven end-to-end with a real second account reading the moved item's plaintext — closing the verified gap that `ItemForm.tsx` has no `collectionId` concept at all today.

This is the phase's tracer: it wires client store to UI to the existing server route (`vault.rs::move_item`, unchanged) to real persisted, recipient-readable state, for ONE full path. DEBT-04's clippy debt and the `item_shares`-survival fix (32-CONTEXT.md Area 2's correction) are independent Rust-only concerns handled by 32-03, running in parallel.

Purpose: closes ORG-01 (destination control, same mental model as a personal folder), the core of ORG-02 (re-encrypt under destination key, refuse on unavailable key), and the mechanism ORG-04 (move back out) reuses symmetrically.
Output: `moveVaultItem` in the client store, a restructured `item-folder-select` with three groups, a proven live create+edit round trip with a real recipient read, and a real-WASM proof of the crypto dispatch across all directions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-putting-things-into-shared-folders/32-CONTEXT.md
@.planning/phases/32-putting-things-into-shared-folders/32-RESEARCH.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-VERIFICATION.md

@web/src/components/vault/ItemForm.tsx
@web/src/components/vault/DetailPanel.tsx
@web/src/lib/vault/store.ts
@web/src/lib/vault/collections.ts
@web/src/lib/vault/api.ts
@web/src/components/vault/CollectionPicker.tsx
@web/src/lib/vault/store.real-wasm.test.ts
@web/e2e/sharing.spec.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: moveVaultItem + a real destination control, wired end-to-end (create and edit)</name>
  <reversibility rating="reversible">A client-side helper + a UI control behind the existing item-folder-select id; swapping the encoding scheme or the dispatch shape later touches only this file pair.</reversibility>
  <files>web/src/lib/vault/store.ts, web/src/components/vault/ItemForm.tsx, web/src/components/vault/DetailPanel.tsx, web/src/lib/i18n/dictionary.ts, web/e2e/sharing.spec.ts</files>
  <action>
  Per 32-CONTEXT.md Area 1 and Area 2, and RESEARCH.md's Pattern 1/Q1 generalization (with one deliberate departure noted below):

  1. store.ts — add moveVaultItem(id: string, rawFields: ItemFields, currentRevision: number, newCollectionId: string | null): Promise&lt;VaultItem&gt;.
  Import moveItemToCollection from ./api (not currently imported here). Reuse this file's own existing recombineEncryptedItem/splitCombinedEncryptedItem (already module-private here — do not import ShareDialog.tsx's copy), and the already-imported encryptItem/encryptItemForCollection.

  Departure from RESEARCH.md's literal "decrypt source, encrypt dest" Pattern 1: ItemForm already holds the item's up-to-date plaintext in React state (that is what the form edits), so moveVaultItem takes that plaintext directly and only dispatches on the DESTINATION side — normalize rawFields (normalizeItemFields, mirrors updateVaultItem), JSON.stringify it, and encrypt with encryptItem(uk, plaintext, id, newRevision) when newCollectionId === null, or encryptItemForCollection(ck, plaintext, newCollectionId, id, newRevision) (via getCollectionKey(newCollectionId), throwing CollectionKeyUnavailableError(newCollectionId) when undefined, mirroring updateVaultItem's existing discipline) otherwise. newRevision = currentRevision + 1 (move_item bumps unconditionally, same AAD-revision discipline updateVaultItem already uses). This is strictly safer than a decrypt-old/re-encrypt-old sequence: it correctly captures a content edit made in the SAME save as a destination change, instead of silently discarding it. Reuse the identical existingBeforeSave?.undecryptable / directSharedItems.some guards updateVaultItem already has, for the same reasons.

  Call moveItemToCollection(id, newCollectionId, encKey, encData, currentRevision). On failure: mirror updateVaultItem's exact 409 handling (isConflictError → loadAndDecryptAll() → throw RevisionConflictError from details?.last_editor_email). Add a new duck-typed isForbiddenError helper (status === 403, same shape as the existing isConflictError/isNotFoundError) and, on a 403, throw CollectionKeyUnavailableError(newCollectionId ?? "personal") — this is the TOCTOU refusal path ORG-02 requires (destination access revoked between the client's stale useCollections() view and submit; vault.rs::move_item Gate 2 refuses server-side before any write — no server change needed, see 32-RESEARCH.md Pitfall 2). A 403 can only occur when newCollectionId !== null (Gate 2 only runs on a non-null destination), so this branch is unreachable on a move-out.

  On success, build the updated VaultItem the same way updateVaultItem's tail does (carry forward lastUsedAt; collectionId: newCollectionId; accessLevel: newCollectionId === null ? undefined : getCollectionAccessLevel(newCollectionId), mirroring decryptItemRow's dispatch; isShared: newCollectionId !== null ? true : (existing?.isShared ?? false) as a best-effort optimistic value, corrected on the next background snapshot — same "carried forward, corrected next snapshot" discipline the existing isShared/lastEditorEmail carry-forward comment already documents). Write through replaceItemInSources(id, updated) (already handles relocation between the three source arrays — no special-casing needed, confirmed by reading its own doc comment).

  2. ItemForm.tsx — restructure renderFolderBlock()'s &lt;select id="item-folder-select"&gt;.
  New state: const [destinationCollectionId, setDestinationCollectionId] = useState&lt;string | null&gt;(currentCollectionId ?? null) and const [createdItemState, setCreatedItemState] = useState&lt;{ id: string; revision: number } | null&gt;(null). New prop on the component: currentCollectionId?: string | null (default null).

  Guard: if the item's CURRENT collection is an item_bucket (useCollections().find(c =&gt; c.id === currentCollectionId)?.familyWideKind === "item_bucket"), render the EXISTING, unmodified personal-folder-only select (no new optgroups, no destination state) — family-wide item_bucket editing has its own surface (Phase 30/31's dialogs) and is explicitly out of this phase's scope; excluding it from the destination LIST (below) does not by itself handle an item whose CURRENT scope already IS a bucket, since such a collection would never appear as an option to reselect. This guard is this plan's own finding, not in 32-RESEARCH.md — flagged for the record.

  Otherwise, keep the existing bare &lt;option value=""&gt;{t("item.noFolder")}&lt;/option&gt; unchanged (least change from the shipped structure; "Bez folderu" is not literally wrapped in its own &lt;optgroup&gt; — Claude's discretion per CONTEXT.md's own grant over optgroup mechanics), then add &lt;optgroup label={t("item.myFoldersGroup")}&gt; wrapping the existing folderOptions.map(...) unchanged, then &lt;optgroup label={t("item.sharedFoldersGroup")}&gt; wrapping useCollections().filter(c =&gt; c.familyWideKind !== "item_bucket") (mirrors CollectionPicker.tsx:70-77's inline filter — not extracted, matching that file's own un-extracted precedent), split into writable (accessLevel === "edit") rendered first as plain &lt;option value={`collection:${c.id}`}&gt;{c.name}&lt;/option&gt;, then read-only (accessLevel !== "edit", covers read/hidden_password/null) rendered disabled, label built via interpolate(t("item.folderReadOnlyOption"), { name: c.name }) (new key, inline reason per CONTEXT.md's locked "shown but disabled, with the reason").

  The &lt;select&gt;'s value is destinationCollectionId !== null ? `collection:${destinationCollectionId}` : (fields.folderId ?? ""). onChange: if e.target.value.startsWith("collection:"), setDestinationCollectionId(e.target.value.slice(11)) and clear fields.folderId to null; else setDestinationCollectionId(null) and set fields.folderId to e.target.value || null (existing behavior). handleCreateFolder's success path must also call setDestinationCollectionId(null) (a brand-new personal folder always clears any prior shared-destination selection).

  3. ItemForm.tsx — handleSubmit's dispatch.
  Edit mode: if destinationCollectionId !== (currentCollectionId ?? null), call moveVaultItem(itemId, cleaned, currentRevision, destinationCollectionId); else the existing updateVaultItem(itemId, cleaned, currentRevision) call, unchanged (RESEARCH.md's Anti-Pattern: never route an unchanged destination through the move helper).
  Create mode (retry-safe two-call sequence, this plan's answer to the prompt's "what happens if the move half fails after the create half succeeded" concern): if createdItemState === null, call createVaultItem(cleaned) and store { id, revision } in createdItemState; else reuse the already-tracked createdItemState (never call createVaultItem twice for the same submission attempt — the item already exists). If destinationCollectionId !== null, call moveVaultItem(created.id, cleaned, created.revision, destinationCollectionId) inside its own try/catch: on failure, setSubmitError(t("error.itemCreatedButMoveFailed")), do NOT call onCreated() (the item exists but is not yet where the user asked — the form stays open so a second Save click retries ONLY the move, via the createdItemState !== null branch above, never a duplicate create). Only call onCreated() once the full sequence (create, and move if requested) has actually succeeded.

  Also widen the edit-mode error surfacing: in DetailPanel.tsx's ItemForm's onError, add else if (err instanceof CollectionKeyUnavailableError) { setSaveError("moveRefused") } before the final generic else — widen saveError's type to null | "generic" | "notEditable" | "moveRefused" and render t("error.itemMoveAccessLost") for that case. This string must NOT invite a retry (SC3/Phase 31's established rule — retrying cannot succeed until access is restored); error.itemCreatedButMoveFailed (create-mode) is a genuinely different case where retry DOES work by design (per the retry-safe dispatch above), so its copy MAY say "try again."

  4. DetailPanel.tsx. Pass currentCollectionId={item.collectionId ?? null} on the existing &lt;ItemForm mode="edit" .../&gt; call (~line 595-601).

  5. dictionary.ts — new keys (place near item.noFolder/item.folderLabel and near error.itemSaveFailed): item.myFoldersGroup ("Moje foldery"/"My folders"), item.sharedFoldersGroup ("Udostępnione foldery"/"Shared folders"), item.folderReadOnlyOption (interpolated, "{name} (tylko do odczytu)"/"{name} (read-only)"), error.itemMoveAccessLost (honest, no retry invitation — e.g. "Nie masz już dostępu do zapisu w tym folderze. Zmiana nie została zapisana."/"You no longer have write access to this folder. The change was not saved."), error.itemCreatedButMoveFailed (retry IS safe here — e.g. "Item został zapisany, ale nie udało się przenieść go do wybranego folderu. Spróbuj ponownie."/"The item was saved, but moving it to the selected folder failed. Try again.").

  6. web/e2e/sharing.spec.ts — one new live, two-session test proving SC1 (edit-mode round trip) and SC2 (recipient read) together.
  Reuse this file's existing helpers (twoSessions, ensureFamilyMembership, waitForIdentityKeyPublished, newBareContext/ensureFamilyOwnerSession, createLoginItemViaUI, shareExistingFolderWithMember, reloadAndUnlock, newIdAfter/listItemIds/listFolderIds/listCollectionIds, assertRecipientDecrypts — note assertRecipientDecrypts is currently defined further down this same file; either hoist it above this new test or add a small local equivalent, matching this file's own per-file-owns-its-own-tiny-helper convention). Add one new local helper moveItemToDestinationViaEditor(page, itemId, destinationCollectionId): open the item row, click detail-panel-edit, selectOption(`collection:${destinationCollectionId}`) on item-folder-select, click item-form-submit, wait for edit mode to close (mirrors the shape of this file's existing moveItemToFolder, which drives the UNRELATED context-menu-move personal-folder mechanism — do not reuse or modify that helper, it is out of this phase's scope).

  Flow: owner creates a login item; owner creates a personal folder, shares it with a family member at "edit" via shareExistingFolderWithMember (yields a real destination collection id); owner opens the item in edit mode and moves it into the destination via the new helper; reload the OWNER's page and assert (via the API, GET /api/vault/items) the item's collection_id now equals the destination — SC1's "survives save AND reload" half. Then, on the MEMBER's session (reloadAndUnlock first, since collections.ts has no live-update subscription for a brand-new membership — this file's own established header note), assert the member's own session reads the item's real decrypted name/password via assertRecipientDecrypts — SC2's positive recipient-side read, live.
  </action>
  <verify>
    <automated>cd web && npm run build && npx playwright test e2e/sharing.spec.ts -g "moved via the item editor" --retries=0</automated>
  </verify>
  <done>The new destination optgroup select renders and dispatches correctly for edit mode; a real second account, in a live run, reads the moved item's actual decrypted content after the owner moves it via the item editor and reloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: real-WASM proof of moveVaultItem's destination-key dispatch, all directions</name>
  <precondition>public/wasm/pv_wasm_bg.wasm exists (built by npm run build's prebuild step / scripts/build-wasm.sh)</precondition>
  <files>web/src/lib/vault/moveVaultItem.real-wasm.test.ts</files>
  <behavior>
    Mirrors store.real-wasm.test.ts's existing "encrypt dispatch" describe block (lines 277-427) exactly in setup shape (mock ./api's getSyncSnapshot/listCollections/moveItemToCollection only, never @/lib/crypto; real beforeAll WASM-loading stub, byte-identical to store.real-wasm.test.ts:80-97), generalized to moveVaultItem instead of updateVaultItem:
    - Test 1 (personal to collection): a personal item's plaintext, moved to a real WasmCollectionKey-backed destination, decrypts correctly via decryptItemForCollection under the DESTINATION key and AAD (collection_id/item_id/revision+1) — and FAILS to decrypt under a second, different collection's key (negative check: AEAD auth failure, proving the key genuinely differs per destination, not merely per call).
    - Test 2 (collection to personal, i.e. ORG-04's move-out mechanism): a collection-scoped item, moved to newCollectionId: null, decrypts via decryptItem under the caller's own UserKey.
    - Test 3 (collection to a different collection): moves between two distinct WasmCollectionKeys, decrypts under the NEW one, fails under the OLD one.
    - Test 4: getCollectionKey(newCollectionId) returning undefined (destination never cached/unsealed) throws CollectionKeyUnavailableError before any network call (moveItemToCollection mock never invoked) — the client-detectable half of ORG-02's refusal path (distinct from the live TOCTOU/403 half Task 1's e2e proves).
  </behavior>
  <action>Create the new real-WASM test file per the behavior above, following store.real-wasm.test.ts's exact conventions (imports, beforeAll WASM stub, vi.hoisted mocks scoped to ./api only).</action>
  <verify>
    <automated>cd web && npx vitest run src/lib/vault/moveVaultItem.real-wasm.test.ts</automated>
  </verify>
  <done>All four cases pass against genuine wasm-bindgen crypto with zero @/lib/crypto mocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (ItemForm's destination select) to pv-server PUT /api/vault/items/{id}/collection | User-chosen destination id crosses into a network request; the UI's disabled/excluded options are presentation only, not the authorization boundary. |
| Browser's useCollections() cache vs the server's live authorization state | The client's destination list can be stale relative to a concurrent revoke (TOCTOU). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | ItemForm.tsx's destination select vs moveItemToCollection PUT | medium | mitigate | Already mitigated server-side, unmodified by this task: require_collection_edit (Gate 2, vault.rs:1020-1034) re-validates independently of any client-side disabled/excluded option — a crafted request naming a collection the caller lacks edit on is refused regardless of what the UI showed. |
| T-32-02 | Information Disclosure | Destination select's stale useCollections() cache vs a concurrently revoked grant | medium | mitigate | Gate 2 runs entirely before tx.begin() (vault.rs:1053) — a stale client encrypting and sending a move is refused with zero write; proven live by Plan 32-04's dedicated SC3 test. |
| T-32-03 | Information Disclosure | Moving an item into a shared folder with no confirmation step | low | accept | 32-CONTEXT.md Area 2's explicit, recorded tradeoff: no inline note, no confirm dialog — "the Udostępnione foldery group label is itself the signal." Re-opening this is a decision, not a gap (per CONTEXT.md's own text). |
| T-32-04 | Repudiation | Create-then-move two-call sequence, move half failing after create half succeeds | high | mitigate | Task 1's retry-safe dispatch: the item is never reported as saved-into-the-folder on a partial failure (onCreated() withheld), the honest error.itemCreatedButMoveFailed copy names what happened, and a retry reuses the already-created id/revision (never a duplicate createVaultItem call, never a lost edit). |

</threat_model>

<verification>
- cd web && npx vitest run src/lib/vault/moveVaultItem.real-wasm.test.ts — all four dispatch/refusal cases pass, real WASM.
- cd web && npm run build && npx playwright test e2e/sharing.spec.ts -g "moved via the item editor" --retries=0 — live, two real sessions, recipient-side read.
- cd web && npm run compile (after npm run build has already populated packages/pv-ui/node_modules via the command above) exits 0.
</verification>

<success_criteria>
SC1 (edit-mode half) and SC2 are both proven live, non-vacuously, by a single real two-session Playwright run. moveVaultItem's full direction coverage is proven against genuine WASM crypto with no mocked @/lib/crypto.
</success_criteria>

<output>
Create `.planning/phases/32-putting-things-into-shared-folders/32-01-SUMMARY.md` when done
</output>
