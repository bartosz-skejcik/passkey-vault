---
phase: 32-putting-things-into-shared-folders
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - web/src/components/vault/ItemForm.test.tsx
  - web/e2e/sharing.spec.ts
autonomous: true
requirements: [ORG-01]

must_haves:
  truths:
    - "The destination select's optgroup structure, the item_bucket exclusion, and the disabled-with-reason rendering for read-only shared folders are each proven by a dedicated unit test, not just eyeballed."
    - "The retry-safe create-then-move dispatch (from 32-01 Task 1) is proven by a dedicated unit test: a partial failure never calls createVaultItem twice."
    - "Creating an item directly in an existing shared folder (SC1's create-mode half) is proven live: the item ends up genuinely collection-scoped, never stranded in personal scope, after a real save and reload."
  artifacts:
    - "web/src/components/vault/ItemForm.test.tsx has new describe blocks for the destination optgroup and the create-then-move dispatch"
    - "web/e2e/sharing.spec.ts has a new live create-mode test"
  key_links:
    - "The retry-safety unit test mocks createVaultItem/moveVaultItem directly (this is a control-flow claim, not a crypto claim — a mocked-crypto unit test IS legitimate evidence here, unlike SC2's recipient-side claim)"
---

<objective>
Prove, with dedicated tests, the destination-control mechanics and the create-mode two-call sequence 32-01 built: the optgroup rendering (empty/one/many, disabled-with-reason, item_bucket exclusion) via mocked-store unit tests, the retry-safe partial-failure dispatch via a mocked-store unit test (a legitimate control-flow claim, not a crypto claim), and SC1's create-mode half (create directly in a shared folder, never stranded) via one live single-session Playwright test.

No new production code — this plan is pure verification of what 32-01 already shipped. This is the "each plan still leaves the tree in an independently verifiable state" guarantee applied literally: a green run of this plan's tests is evidence about 32-01's mechanism, not a mechanism this plan itself introduces.

Purpose: closes the remaining test-coverage gaps 32-RESEARCH.md's Wave 0 Gaps section names for ORG-01's create-mode half and the destination control's own rendering contract.
Output: two new unit-test describe blocks in `ItemForm.test.tsx`, one new live e2e test in `sharing.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-putting-things-into-shared-folders/32-CONTEXT.md
@.planning/phases/32-putting-things-into-shared-folders/32-RESEARCH.md
@.planning/phases/32-putting-things-into-shared-folders/32-01-SUMMARY.md

@web/src/components/vault/ItemForm.tsx
@web/src/components/vault/ItemForm.test.tsx
@web/e2e/sharing.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: unit tests for the destination optgroup and the retry-safe create-then-move dispatch</name>
  <files>web/src/components/vault/ItemForm.test.tsx</files>
  <behavior>
    Extends this file's existing `vi.mock("@/lib/vault/store", ...)`/`vi.mock("@/lib/vault/collections", ...)` shape (add a `useCollections` mock alongside the existing `useFolders`/`useAllTags` mocks, and a `moveVaultItem` mock alongside `createVaultItem`/`updateVaultItem`). Mocked crypto/store throughout — legitimate evidence for these specific claims (rendering structure and call-order/call-count), NOT for SC2's crypto claim (already proven with real WASM in 32-01 Task 2 and live in 32-01 Task 1).

    Describe "destination optgroup":
    - Zero shared collections: no "Udostępnione foldery" `<optgroup>` renders (or renders empty — pick one, assert it), personal-folder behavior unchanged from today.
    - One writable (`accessLevel: "edit"`) + one read-only (`accessLevel: "read"`) + one `hidden_password` shared collection: the writable one is a plain, enabled `<option>`; both non-edit ones render `disabled` with the read-only reason visible in the option's text content.
    - An `item_bucket`-kind collection in the mocked `useCollections()` list never appears as any `<option>`, writable or disabled.
    - Selecting a shared-folder `<option>` (`collection:{id}` value) then submitting calls `createVaultItem` followed by `moveVaultItem` with that collection id — never the reverse order, never both simultaneously via `Promise.all`.

    Describe "create-then-move retry safety":
    - `createVaultItem` resolves, `moveVaultItem` rejects: assert `onCreated` is NOT called, the honest `error.itemCreatedButMoveFailed`-keyed error text renders, and `createVaultItem` was called exactly once.
    - Clicking Save again (same rendered form, no remount): assert `createVaultItem` is STILL called exactly once total (not twice) while `moveVaultItem` is now called a second time — proving the tracked created-item id/revision is reused, never a duplicate create.
  </behavior>
  <action>Add both describe blocks per the behavior above, following this file's existing `vi.hoisted`/`vi.mock` conventions exactly (see the top of the file for the established shape).</action>
  <verify>
    <automated>cd web && npx vitest run src/components/vault/ItemForm.test.tsx</automated>
  </verify>
  <done>Both describe blocks pass; reverting 32-01's retry-safety branch (deleting the createdItemState reuse and always calling createVaultItem) makes the second new test fail on the call-count assertion.</done>
</task>

<task type="auto">
  <name>Task 2: live e2e — creating an item directly in an existing shared folder (SC1 create-mode)</name>
  <files>web/e2e/sharing.spec.ts</files>
  <action>
  Single-session (owner only — the recipient-read half of ORG-02/SC2 is already proven live by 32-01's own test against the SAME `moveVaultItem` mechanism; this test only needs to prove the CREATE-mode two-call sequence itself lands correctly, not re-prove the crypto).

  Flow: owner creates a personal folder and shares it with a fresh family member at `"edit"` (`shareExistingFolderWithMember`) to get a real destination collection id — the family membership itself can be a single-member `ensureFamilyMembership` call, the member's own session is never opened in this test. Click "new-item-button", pick a type (`type-tile-login`), fill `item-name`/`item-password`, select the shared folder in `item-folder-select` (`collection:{destinationId}` value) BEFORE the first Save click (proving the destination choice is honored on CREATE, not just edit), submit, wait for the form to close. Assert via `listItemIds`/a direct `GET /api/vault/items` read that the new item's `collection_id` equals the destination — never `null` (the "never stranded in personal scope" claim, driven, not assumed). Reload the owner's page and re-assert the same, proving persistence across a reload (SC1's create-mode "survives save and reload" half — the sync round trip and recipient read are already proven for the mechanism by 32-01's own edit-mode test, per this plan's objective).
  </action>
  <verify>
    <automated>cd web && npm run build && npx playwright test e2e/sharing.spec.ts -g "created directly in an existing shared folder" --retries=0</automated>
  </verify>
  <done>The live test passes: the newly created item's collection_id is the shared destination, immediately after save and again after a reload — never personal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — this plan adds tests only, exercising the boundaries 32-01 already established.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-08 | Information Disclosure | A disabled `<option>` that is visually styled but not actually `disabled` in the DOM | low | mitigate | Task 1's unit test asserts the DOM `disabled` attribute directly (not just a CSS class), so a click-through on a "read-only" option — which the server's Gate 2 would refuse anyway (403) — cannot pass this test silently; catches a confusing-but-not-exploitable regression before it reaches a live run. |

</threat_model>

<verification>
- `cd web && npx vitest run src/components/vault/ItemForm.test.tsx` — all new describe blocks pass.
- `cd web && npm run build && npx playwright test e2e/sharing.spec.ts -g "created directly in an existing shared folder" --retries=0` — passes live.
- `cd web && npm run compile` exits 0 (run after `npm run build` above).
</verification>

<success_criteria>
SC1's create-mode half is proven live and never lands the item stranded in personal scope. The destination control's rendering contract (optgroups, disabled-with-reason, item_bucket exclusion) and the retry-safe partial-failure dispatch are each falsification-provable by a dedicated unit test.
</success_criteria>

<output>
Create `.planning/phases/32-putting-things-into-shared-folders/32-02-SUMMARY.md` when done
</output>
