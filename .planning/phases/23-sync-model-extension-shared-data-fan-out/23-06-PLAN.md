---
phase: 23-sync-model-extension-shared-data-fan-out
plan: 06
type: execute
wave: 4
depends_on: ["23-04", "23-05"]
files_modified:
  - web/e2e/shared-sync.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [SYNC-04, SYNC-06, SEC-08]

must_haves:
  truths:
    - "A live Playwright spec (2 real, independent browser sessions) proves SYNC-04's headline: a shared item edited by one member becomes visible to another with access, observed through the owner's own real UI (DetailPanel) reacting to the co-editor's raw-API-driven edit."
    - "The same or a sibling live Playwright spec proves SYNC-06/SC3: the owner's conflict banner attributes the concurrent edit to the co-editor's email, not a generic message."
    - "web/'s new Playwright suite is wired into .github/workflows/ci.yml as a real, blocking job — not a manual-only convention and not continue-on-error — since SEC-08 exists specifically to convert the v0.2->v0.3 \"green CI missed 7 bug classes\" lesson into standing automation, and an optional/soft gate would not close that gap."
    - "Every WebAuthn-adjacent step in the new spec (there are none — both sessions authenticate password-only) raises zero OS-level dialogs, consistent with the harness built in Plan 23-04."
  artifacts:
    - path: "web/e2e/shared-sync.spec.ts"
      provides: "revision fan-out + conflict attribution live proofs, matching 23-RESEARCH.md's Validation Architecture test names"
    - path: ".github/workflows/ci.yml"
      provides: "a new web-e2e job running npm run test:e2e in web/, gated the same as the existing web job"
  key_links:
    - from: "web/e2e/shared-sync.spec.ts"
      to: "crates/pv-server/src/routes/collections.rs + vault.rs (via raw HTTP fixture setup)"
      via: "context.request calls to POST /api/families, PUT /api/identity/keypair, POST /api/vault/collections, POST /api/vault/collections/{id}/members, PUT /api/vault/items/{id}/collection — all with opaque dummy blob values, never real crypto, mirroring the Rust integration tests' own fixture posture"
      pattern: "context.request"
    - from: ".github/workflows/ci.yml::web-e2e"
      to: "web/package.json::test:e2e"
      via: "npm run test:e2e --workspace or working-directory: web"
      pattern: "test:e2e"

  prohibitions:
    - statement: "MUST NOT wire the new Playwright suite into CI as a soft/continue-on-error/manual-only gate — SEC-08's entire product intent is closing the exact gap a green-but-incomplete CI left open across v0.2/v0.3, so a non-blocking job would silently defeat the requirement it exists to satisfy."
      category: values
---

<objective>
Write the standing suite's first real specs, proving SYNC-04 and SYNC-06/SC3 live with 2 genuinely
independent, concurrently authenticated browser sessions against the real server stack built in
Plans 23-01 through 23-05, then wire the suite into CI as a real, blocking gate (Open Question 3,
decided: add it — `.github/workflows/ci.yml` today runs no Playwright suite at all, not even the
extension's own 21+-SC suite; SEC-08's whole rationale is the v0.2->v0.3 lesson that green CI missed
7 bug classes only visible live, so leaving this suite manual-only would reproduce the exact gap it
exists to close).

Concrete, crypto-free fixture design (documented rationale — deviates from "drive everything through
real crypto" since B, the second family member, has no client-side identity-keypair generation this
phase, per Plan 23-05's own scope boundary): both specs use REAL UI for the owner's own
encrypt/decrypt operations (their own item, their own key — genuinely valid ciphertext) and RAW
authenticated HTTP requests (`context.request`, bearing each session's own real bearer token read
from `localStorage`) for every fixture-setup call the web app has no UI for yet (create family, add
member, publish a dummy identity keypair, create a direct item share) — mirroring the Rust
integration tests' own posture of opaque, never-validated blob values for `sealed_key`/`enc_key`/
`enc_data` fields the server never decodes. Neither spec requires B to decrypt anything: SYNC-04's
proof reads ONLY revision numbers via `GET /api/sync/shared` (metadata, no ciphertext); SYNC-06's
proof reads ONLY the 409 conflict's `last_editor_email` field (plain JSON, no ciphertext) — both are
genuinely observable without the identity-keypair machinery Phase 26 owns.

Purpose: close SEC-08 as "stood up with the sync phase, not deferred" and give SYNC-04/SYNC-06 their
live, 2-real-session proof — the milestone's direct application of the recorded v0.2->v0.3 lesson.
Output: `web/e2e/shared-sync.spec.ts` with two passing live specs; a new blocking `web-e2e` CI job.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-sync-model-extension-shared-data-fan-out/23-CONTEXT.md
@.planning/phases/23-sync-model-extension-shared-data-fan-out/23-RESEARCH.md
@web/e2e/fixtures.ts
@web/e2e/smoke.spec.ts
@web/src/lib/vault/api.ts
@web/src/components/vault/DetailPanel.tsx
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shared-sync.spec.ts — revision fan-out + conflict attribution, live</name>
  <files>web/e2e/shared-sync.spec.ts</files>
  <read_first>
    web/e2e/fixtures.ts (Plan 23-04's twoSessions fixture — reuse it verbatim, do not build a second
    two-session bring-up mechanism);
    crates/pv-server/src/routes/vault.rs (CreateItemRequest/UpdateItemRequest/CreateItemShareRequest
    field names — id/enc_key/enc_data, expected_revision, recipient_user_id/sealed_key/access_level);
    crates/pv-server/src/routes/collections.rs and families.rs (POST /api/families body shape;
    POST /api/families/members body shape — {user_id});
    crates/pv-server/src/routes/identity.rs (PUT /api/identity/keypair's expected body shape for a
    dummy publish);
    web/src/components/auth/RegisterForm.tsx and the vault's "Add Item"/ItemForm entry points (real
    UI selectors the spec drives for the owner's own item create/edit).
  </read_first>
  <behavior>
    - Test "revision fan-out": owner A creates a family, adds member B, B publishes a dummy identity
      keypair, A creates a login item through the real UI, A shares it directly with B
      (access_level: "read") via a raw request. B's own `GET /api/sync/shared` (raw request, B's own
      bearer token) reflects the item's current revision under the `direct` bucket. A then edits the
      item through the real UI (bumping its revision). B's own subsequent `GET /api/sync/shared`
      reflects the NEW revision — proving the fan-out reached a second real, independently
      authenticated session with zero decryption required on B's side.
    - Test "conflict attribution": owner A creates a second item, shares it with B at
      access_level: "edit". A opens the item in DetailPanel (real UI) and enters edit mode (capturing
      baseline revision). B performs a raw authenticated PUT to the SAME item with a stale-safe
      revision number that the server accepts (bumping the item's revision and setting
      `last_editor_user_id` to B). A then submits their own edit through the real UI's Save action,
      still holding their now-stale baseline revision — the server responds 409 with B's email —
      and A's `live-edit-conflict-banner` (or the reactive `revision-conflict-banner`, whichever the
      existing save-flow triggers, per DetailPanel.tsx's own dual-banner distinction) visibly renders
      B's real email address in its text content.
    - Neither test ever triggers a real WebAuthn ceremony (both sessions are password-only per
      RegisterForm's own flow) and neither asserts on any decrypted content originating from B's raw
      requests.
  </behavior>
  <action>
    Create `web/e2e/shared-sync.spec.ts` importing `test`/`expect` from `./fixtures`. Implement the
    two tests per the `<behavior>` block, naming them so `npx playwright test -g "revision fan-out"`
    and `npx playwright test -g "conflict attribution"` each match exactly one test (matching
    23-RESEARCH.md's own Validation Architecture table naming). Every raw fixture-setup call uses
    `context.request` with an `Authorization: Bearer <token>` header, where `<token>` is read via
    `page.evaluate(() => localStorage.getItem("pv-session-token"))` for the relevant session. Every
    opaque field this test supplies (`enc_key`, `enc_data` for B's raw edit; `sealed_key` for the
    identity keypair and the item share) is a fixed placeholder string — never a real WASM-encrypted
    value — matching the server's own opaque-blob treatment of these columns.
  </action>
  <verify>
    <automated>cd web && npx playwright test shared-sync.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx playwright test -g "revision fan-out"` exits 0.
    - `cd web && npx playwright test -g "conflict attribution"` exits 0.
    - The conflict-attribution test's final assertion checks that the rendered banner's text content
      contains member B's actual registered email address (not a placeholder/generic string).
    - Neither test registers a `page.on("dialog", ...)` handler that ever fires (zero OS-level
      prompts raised during either flow).
  </acceptance_criteria>
  <done>Both live specs pass against the real Playwright-booted server, proving SYNC-04's fan-out and
  SYNC-06/SC3's attribution with 2 genuinely independent, concurrently authenticated sessions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire web-e2e into CI as a real, blocking job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (the existing `web` job's exact steps — checkout, rust toolchain w/
    wasm32-unknown-unknown target, node setup w/ cache-dependency-path for web/pv-ui, build-wasm.sh,
    pv-ui/web npm ci — to mirror for the new job's setup phase).
  </read_first>
  <action>
    Add a new job named `web-e2e` to `.github/workflows/ci.yml`, sibling to the existing `web` job,
    reusing the SAME checkout/rust-toolchain/node-setup/build-wasm/npm-ci steps that job already runs,
    then adds: install Playwright's browser binaries (`npx playwright install --with-deps chromium`,
    working-directory `web`), and run `npm run test:e2e` (working-directory `web`) — Playwright's own
    `webServer` config (Plan 23-04) handles building `pv-server` and `web`'s static export and booting
    the real server against a throwaway SQLite DB, so no separate manual build/boot step is needed in
    the workflow itself. This job runs unconditionally on every push/PR (the same `on:` triggers as
    every other job in this file) and is NOT marked `continue-on-error` — a red result here fails the
    workflow, matching every other job in this file and honoring SEC-08's "not deferred, not manual"
    intent.
  </action>
  <verify>
    <automated>grep -c 'web-e2e' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a `web-e2e:` job definition.
    - The new job's steps include a `npm run test:e2e` invocation with `working-directory: web`.
    - The new job has no `continue-on-error: true` key anywhere in its definition.
    - `grep -c 'web-e2e' .github/workflows/ci.yml` returns at least 1.
  </acceptance_criteria>
  <done>The standing shared-sync Playwright suite runs on every push/PR as a real, blocking CI job —
  SEC-08's "not deferred to a later hardening phase" intent is now enforced by the workflow itself,
  not just by developer discipline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> real multi-session server stack | The e2e job boots a real pv-server + real Chromium instances driving two real accounts on every push/PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-16 | Tampering | supply-chain (`@playwright/test`, Chromium download in CI) | high | mitigate | `@playwright/test` is the SAME already-vetted pin `extension/package.json` uses in this same repo's own CI (RESEARCH.md's Package Legitimacy Audit); `npx playwright install` downloads Playwright's own signed Chromium build, the identical mechanism the extension job already runs today. |
| T-23-17 | Denial of Service | new blocking CI job | medium | accept | A flaky/slow e2e job could block merges; accepted per the explicit prohibition against a soft/non-blocking gate — flakiness is addressed by fixing the test, not by weakening the gate. |
</threat_model>

<verification>
`cd web && npx playwright test shared-sync.spec.ts` exits 0 locally; `.github/workflows/ci.yml`
contains the new blocking `web-e2e` job.
</verification>

<success_criteria>
SYNC-04 and SYNC-06/SC3 are proven live with 2 real, independently authenticated browser sessions,
and that proof runs automatically, as a blocking gate, on every push and PR.
</success_criteria>

<output>
Create `.planning/phases/23-sync-model-extension-shared-data-fan-out/23-06-SUMMARY.md` when done
</output>
