---
phase: 31-the-share-dialog-per-person-access-existing-destinations
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - web/src/components/vault/ShareDialog.tsx
  - web/src/components/vault/ShareDialog.test.tsx
  - web/src/components/vault/ShareDialog.real-wasm.test.ts
  - web/src/lib/i18n/dictionary.ts
  - web/e2e/sharing.spec.ts
autonomous: true
requirements: [MOD-01, MOD-02, ORG-03]

must_haves:
  truths:
    - "The share dialog renders a destination selector above the person list for the folder scope; choosing an existing shared folder populates every row with that person's real current level for THAT destination."
    - "A submission targeting an existing destination adds/edits recipients without creating a new collection — the collection count is unchanged and every resulting membership row carries the chosen destination's id."
    - "A person newly added to an existing destination can decrypt items already in it, proven through real (unmocked) crypto."
  artifacts:
    - web/src/components/vault/ShareDialog.tsx (destination selector, folder-branch update/revoke reachability)
    - web/src/components/vault/ShareDialog.real-wasm.test.ts (ORG-03 existing-destination case)
    - web/e2e/sharing.spec.ts (SC1, SC2, live)
  key_links:
    - "Destination selector's existing-folder list -> useCollections() filtered to accessLevel === 'edit' && familyWideKind !== 'item_bucket' (mirrors SharingOverviewPanel.tsx:315's own predicate)"
    - "reconcileRow's grant branch, existing destination -> reshareCollectionToNewMember (Phase 30, real-WASM-proven); update -> updateCollectionAccess (31-01); revoke -> revokeCollectionAccess (existing, SHARE-06)"
---

<objective>
Add the destination selector 31-02 deliberately deferred (MOD-02, ORG-03): "Nowy folder…" or one of the caller's own edit-held, non-`item_bucket` existing shared folders, rendered above the row list for the folder scope only. Choosing an existing destination fetches its real access list and re-seeds every row's `currentLevel` from it (Pitfall 3's re-fetch-on-switch rule) — this is what makes the folder branch of `reconcileRow`'s update and revoke dispatch (written but unreachable in 31-02) actually fire for the first time, and it is the capability ORG-03's real-WASM proof and SC1/SC2's live proofs depend on, so all three land together in this plan.

Purpose: MOD-02 (existing-destination targeting, no new collection), ORG-03 (v0.4's WINDOWS #13, a new member decrypts pre-existing content), MOD-01/SC1 (per-person levels proven against real server state).
Output: a working destination selector; folder-scope grant/update/revoke all reachable and dispatch-count-proven; ORG-03/SC3 real-WASM proof; SC1/SC2 live e2e proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-CONTEXT.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-RESEARCH.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-UI-SPEC.md

@web/src/components/vault/ShareDialog.tsx
@web/src/lib/families/reseal.ts
@web/src/lib/vault/api.ts
@web/src/lib/vault/collections.ts
@web/src/components/vault/CollectionPicker.tsx
@web/e2e/sharing.spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Destination selector — target an existing shared folder, with dispatch-count proof</name>
  <files>web/src/components/vault/ShareDialog.tsx, web/src/components/vault/ShareDialog.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <behavior>
    - Destination selector, folder scope only: "Nowy folder…" plus every collection from useCollections() where accessLevel === "edit" && familyWideKind !== "item_bucket".
    - Switching destinations re-fetches getCollectionAccessList(destinationId) and re-renders every row from that destination's real current state (never carries a pending value over from the previous destination).
    - For a row transitioning (read -> edit) against an EXISTING destination: exactly one updateCollectionAccess call and zero revokeCollectionAccess/addCollectionMember/reshareCollectionToNewMember calls for that userId (dispatch-count assertion, closing Blocker 7 for the collection branch).
  </behavior>
  <action>
    Add a destination selector (inline JSX, matching this file's single-component-owns-its-tree convention) rendered ONLY when `scope.kind === "folder"`, per 31-UI-SPEC.md's Destination Selector Contract: a `&lt;select&gt;` with two `&lt;optgroup&gt;`s ("Utwórz nowy" -> `"Nowy folder…"` value `"new"`; "Istniejące foldery" -> every collection from `useCollections()` filtered to `c.accessLevel === "edit" && c.familyWideKind !== "item_bucket"`, the SAME predicate `SharingOverviewPanel.tsx:315` already uses — never `CollectionPicker` reused unfiltered, since it has no access-level filter and would let a read-only member pick a destination they cannot grant on). Add `destinationId: string | null` state (`null` = mint new, matching the existing `folderName`/seed-folder markup's meaning). Each existing-folder `&lt;option&gt;` carries `title={c.name}` for long-name truncation.

    Wire destination changes: on switch, fetch `getCollectionAccessList(destinationId)` and re-seed EVERY row's `currentLevel` from the result (rows for members with no grant on this destination get `currentLevel: null`), resetting every row's `pendingLevel` to match its freshly-fetched `currentLevel` — never carry a pending value from the previous destination forward (Pitfall 3: a stale `currentLevel` dispatched as an "update" against a destination it was never granted on produces a wrong 404, not a grant). Show a loading sub-state for the row region specifically while this fetch resolves (the destination selector itself stays interactive).

    Extend the folder-scope grant branch (built in 31-02) to dispatch by whether a destination is selected: `destinationId === null` (mint new) keeps 31-02's per-row `addCollectionMember` loop; `destinationId !== null` (existing) calls `reshareCollectionToNewMember(destinationId, userId, pendingLevel, ownUk)` (the Phase 30 composition — unwrap-own-key, reseal-to-recipient, already real-WASM-proven, never `WasmCollectionKey.generate()`). Wire the update branch (`(currentLevel, pendingLevel)` both non-null and different) to `updateCollectionAccess(destinationId, userId, pendingLevel)` and the revoke branch (`(currentLevel, "none")`) to `revokeCollectionAccess(destinationId, userId)` — both are now reachable for the first time against a real existing destination.

    Add a unit test (mocking `updateCollectionAccess`/`revokeCollectionAccess`/`addCollectionMember`/`reshareCollectionToNewMember`) proving the dispatch-count property for the collection branch (Blocker 7): seed a row against an existing destination with `currentLevel: "read"`, change its `pendingLevel` to `"edit"`, submit, and assert `updateCollectionAccess` was called EXACTLY ONCE for that userId and the other three functions were called ZERO times for it.

    **Falsification (non-optional, plan-check iteration 2 / Blocker 3).** Together with 31-02's item-branch twin, this assertion is the ONLY evidence that a level edit is atomic rather than a revoke-then-re-add — 31-06-T2 cites it instead of re-deriving the claim at the e2e layer, where call shape is genuinely unobservable. That citation is worthless unless this test has been seen to fail. So: temporarily replace the update branch's single `updateCollectionAccess` call with a `revokeCollectionAccess` + `addCollectionMember` pair, re-run this test alone, confirm it goes RED on the count assertion, record the exact observed output in the SUMMARY, restore, and confirm green.

    Add dictionary keys verbatim: `share.destinationLabel`, `share.destinationNewFolderOption`, `share.destinationNewGroupLabel`, `share.destinationExistingGroupLabel`.
  </action>
  <verify>
    <automated>cd web && npm run compile && npm test && npm run build && npx playwright test e2e/sharing.spec.ts e2e/shared-sync.spec.ts e2e/export-disclosure.spec.ts e2e/family-wide-sharing.spec.ts --retries=0</automated>
  </verify>
  <done>The destination selector renders, filtered correctly; switching destinations re-fetches and re-seeds every row; the update/revoke branches are proven reachable and dispatch-correct against an existing destination via the dispatch-count test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ORG-03/SC3 real-WASM proof — a new recipient on an existing destination decrypts pre-existing items</name>
  <files>web/src/components/vault/ShareDialog.real-wasm.test.ts</files>
  <behavior>
    - Real (unmocked) crypto: seed a collection with a real item encrypted under its real CollectionKey via the actual production composition, then reshare to a new recipient via ShareDialog's own exported grant-dispatch function, and assert the new recipient's own identity key can unwrap the collection's sealed_key and decrypt the pre-existing item's real ciphertext back to the original plaintext.
  </behavior>
  <action>
    Extend `ShareDialog.real-wasm.test.ts` with a case proving SC3/ORG-03: construct two real, locally-generated identity keypairs (owner + new recipient, mirroring `reseal.real-wasm.test.ts`'s own fixture shape); the owner generates a real `WasmCollectionKey`, encrypts a real item under it, and reshares the SAME (never regenerated) key to the new recipient by calling the ACTUAL production dispatch this plan's Task 1 wired to `reshareCollectionToNewMember` — never a re-implementation of the composition. Assert the new recipient's own identity key genuinely unwraps the collection's `sealed_key` and that decrypting the pre-existing item's ciphertext under the unwrapped key reproduces the exact original plaintext.

    Falsification (mandatory, record exact output in the SUMMARY): temporarily make the grant branch reseal a FRESHLY GENERATED key instead of the unwrapped existing one, re-run this test, confirm it fails with a decrypt error (the new recipient's unwrapped key no longer matches the item's actual encryption key), record the exact failure output, then restore and re-run to confirm green.
  </action>
  <verify>
    <automated>cd web && npm run compile && npm test</automated>
  </verify>
  <done>A real-WASM test proves a new recipient added to an existing destination decrypts an item that existed in it BEFORE they joined, exercising the dialog's real production dispatch code; the falsification record shows the test genuinely fails when the composition is broken.</done>
</task>

<task type="auto">
  <name>Task 3: Live e2e — SC1 (per-person levels) and SC2 (existing destination, no new collection)</name>
  <files>web/e2e/sharing.spec.ts</files>
  <action>
    Add a live e2e test proving SC1: create a real shared folder, add two REAL recipients in ONE dialog submission each set to a DIFFERENT level via their own row select, then call `GET /api/vault/collections/{id}/access` and assert EACH recipient's server-stored `access_level` matches their own row's chosen value exactly.

    Add a live e2e test proving SC2: capture `listCollectionIds` BEFORE opening the dialog against an ALREADY-EXISTING shared folder (selected via the destination selector's "Istniejące foldery" group), submit a share naming a real recipient, then assert `listCollectionIds` AFTER is IDENTICAL in count AND that the newly-created `collection_keys` row's `collection_id` equals the PRE-CHOSEN destination id, never a freshly minted one.
  </action>
  <verify>
    <automated>cd web && npm run build && npx playwright test e2e/sharing.spec.ts e2e/shared-sync.spec.ts e2e/export-disclosure.spec.ts e2e/family-wide-sharing.spec.ts --retries=0</automated>
  </verify>
  <done>SC1's per-recipient level assertion and SC2's collection-count/destination-id assertions both pass against a real running server, alongside every pre-existing test in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client-side destination filter -> server-side authorization | The destination selector's `access_level === "edit"` filter is advisory UX only; the real boundary is the server's `RequireEdit`/`may_grant_access_level` checks `add_member`/`update_access` (31-01) already enforce. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-09 | Elevation of Privilege | Destination selector's client-side filter | medium | mitigate | Filter mirrors `SharingOverviewPanel.tsx:315`'s already-proven predicate, but is defense-in-depth only — server-side `RequireEdit`/`may_grant_access_level` (31-01, existing `add_member`) remain the real enforcement point regardless of a client filter bug. |
| T-31-10 | Tampering (stale state) | Destination-switch row re-seed | medium | mitigate | Every row is re-fetched and re-seeded from the NEW destination's real access list on switch — a pending value from a previous destination is never carried forward, closing the class of bug where a row dispatches an "update" against a destination it was never granted on. |
| T-31-11 | Information Disclosure | `reshareCollectionToNewMember`'s reuse | low | accept | Already real-WASM-proven in Phase 30 (T-25-16 no-public-key throw-before-network, never a fresh key generation) — this plan is a new call site of already-audited crypto, not new crypto. |

Package Legitimacy Audit: not applicable — no new dependencies.
</threat_model>

<verification>
`npm run compile` clean; `npm test` green including both dispatch-count assertions and the real-WASM extension; `npm run build` and `npx playwright test e2e/sharing.spec.ts e2e/shared-sync.spec.ts e2e/export-disclosure.spec.ts e2e/family-wide-sharing.spec.ts --retries=0` green including SC1/SC2 and every pre-existing test in the file.
</verification>

<success_criteria>
Folder-scope sharing against an existing destination works end to end (grant, update, revoke all dispatch-proven), a real-WASM test proves the newly-added recipient decrypts pre-existing content, and live e2e proves SC1/SC2 against real server state.
</success_criteria>

<output>
Create `.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-03-SUMMARY.md` when done
</output>
