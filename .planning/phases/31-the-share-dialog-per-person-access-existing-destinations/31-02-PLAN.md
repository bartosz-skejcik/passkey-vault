---
phase: 31-the-share-dialog-per-person-access-existing-destinations
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - web/src/components/vault/ShareDialog.tsx
  - web/src/components/vault/ShareDialog.test.tsx
  - web/src/components/vault/ShareDialog.real-wasm.test.ts
  - web/src/lib/i18n/dictionary.ts
autonomous: true
requirements: [MOD-01, MOD-02, ORG-03]

must_haves:
  truths:
    - "The share dialog renders a destination selector above the person list for the folder scope; choosing an existing shared folder populates every row with that person's real current level for THAT destination."
    - "A submission targeting an existing destination adds/edits recipients without creating a new collection — the collection count is unchanged and every resulting membership row carries the chosen destination's id."
    - "A person newly added to an existing destination can decrypt items already in it, proven through real (unmocked) crypto."
  artifacts:
    - web/src/components/vault/ShareDialog.tsx (destination selector, Row Anatomy, reconcileRow dispatcher)
    - web/src/components/vault/ShareDialog.real-wasm.test.ts (ORG-03 existing-destination case)
    - web/src/lib/i18n/dictionary.ts (share.destination*, access.none, share.rowCurrentlyLabel, share.rowNoPublishedKey)
  key_links:
    - "Destination selector's existing-folder list -> useCollections() filtered to access_level === 'edit' && family_wide_kind !== 'item_bucket' (mirrors SharingOverviewPanel.tsx:315's own predicate)"
    - "reconcileRow's grant branch (currentLevel === null) -> reshareCollectionToNewMember (existing destination) / addCollectionMember (mint-new destination)"
    - "reconcileRow's update branch (currentLevel !== pendingLevel, both non-null) -> updateCollectionAccess (31-01)"
    - "reconcileRow's revoke branch (pendingLevel === 'none', currentLevel !== null) -> revokeCollectionAccess (existing, SHARE-06)"
---

<objective>
Replace `ShareDialog.tsx`'s per-person UI — today one shared `accessLevel` radio group applied to a checkbox-selected recipient set — with the product owner's design: a destination selector above the list (folder scope only), and one standing row per family member whose own `<select>` shows and edits their real current level in place, per CONTEXT.md Area 1/Area 2's locked decisions. Wire the destination selector to an EXISTING shared folder as a genuine new destination (MOD-02), and build the per-row grant/update/revoke/no-op dispatcher that makes MOD-01's "one submission naming two people at two different levels" true. This plan covers the FOLDER scope only (both "mint a new folder" and "target an existing one"); item-scope wiring is Wave 4's job (31-04). "Cała rodzina" stays its own single-control mode, structurally unchanged, per CONTEXT.md's explicit instruction — this plan adapts its mutual-exclusivity logic to key off "any row has a non-none pending level" rather than deleting or duplicating it.

Purpose: MOD-01 (per-person rows), MOD-02 (existing-destination targeting), ORG-03 (v0.4's WINDOWS #13 — adding a member to a collection that already exists).
Output: a working destination selector + row-based per-person UI wired end to end for the folder scope, real-WASM-proven for ORG-03/SC3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-CONTEXT.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-RESEARCH.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-UI-SPEC.md

@web/src/components/vault/ShareDialog.tsx
@web/src/lib/families/reseal.ts
@web/src/lib/families/accessLevel.ts
@web/src/lib/vault/api.ts
@web/src/lib/vault/collections.ts
@web/src/components/vault/CollectionPicker.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Destination selector + Row Anatomy + reconcileRow dispatcher (folder scope)</name>
  <files>web/src/components/vault/ShareDialog.tsx, web/src/components/vault/ShareDialog.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <behavior>
    - reconcileRow(currentLevel, pendingLevel) dispatch: (null, "read"|"edit"|"hidden_password") -> grant; (X, "none") where X !== null -> revoke; (X, Y) both non-null and X !== Y -> update; (X, X) -> no network call.
    - Destination selector, folder scope only: options are "Nowy folder…" plus every collection from useCollections() where accessLevel === "edit" && familyWideKind !== "item_bucket"; switching destinations re-fetches and re-renders every row from that destination's real current state.
    - Row Anatomy: pendingLevel initializes to currentLevel ?? "none" (never a neutral default); a row whose person has no published public key is disabled and locked to "none".
  </behavior>
  <action>
    Add a destination selector (inline JSX within ShareDialog's own render, matching the file's established single-component-owns-its-tree convention — do not extract a separate file unless the render genuinely becomes unwieldy) rendered ONLY when `scope.kind === "folder"`, per 31-UI-SPEC.md's Destination Selector Contract markup: a `&lt;select&gt;` with two `&lt;optgroup&gt;`s ("Utwórz nowy" holding a single "Nowy folder…" option value `"new"`, "Istniejące foldery" holding every collection from `useCollections()` filtered to `c.accessLevel === "edit" && c.familyWideKind !== "item_bucket"` — the SAME predicate `SharingOverviewPanel.tsx:315` already uses, not `CollectionPicker` reused unfiltered since that component has no access-level filter and would let a read-only member pick a destination they cannot grant on). Add `destinationId: string | null` state (`null` = mint new, matching the existing `folderName`/seed-folder markup's meaning). Each `&lt;option&gt;` for an existing folder carries `title={c.name}` for long-name truncation, mirroring `CollectionPicker.tsx`'s own discharged pattern.

    Replace the per-person checkbox list + shared `ACCESS_LEVEL_VALUES` radio group (ShareDialog.tsx:1307-1344) with a per-row model for the NON-family-wide branch only (the "Cała rodzina" boxed row and its own single radio-equivalent stay completely unchanged — see below). Introduce `rows: { userId: string; email: string; currentLevel: AccessLevelValue | null; pendingLevel: AccessLevelValue | "none"; suspended: boolean; publicKey: string | null }[]` state, populated by fetching `getFamilyMembers()` (for identity/public-key/suspended) AND — when `destinationId !== null` — `getCollectionAccessList(destinationId)` (for each row's real `currentLevel`); when `destinationId === null` (minting a new folder) every row's `currentLevel` is `null` (nothing exists yet). Row order: existing-access rows first (grouped, per CONTEXT's "readable at a glance" ask), alphabetical by email within each group — a resolved discretion call per 31-UI-SPEC.md, not left arbitrary. Render per 31-UI-SPEC.md's Row Anatomy markup exactly: `UserCheck` icon (secondary color) when `currentLevel !== null`, the `share.rowCurrentlyLabel` subtitle interpolating `accessLevelKey(currentLevel)` when non-null, `UserMinus` icon (error color) when `pendingLevel === "none" && currentLevel !== null`, `share.rowNoPublishedKey` text + a disabled `&lt;select&gt;` locked to `"none"` when `publicKey === null`, and the row's own `&lt;select&gt;` (`select select-bordered select-sm w-40 shrink-0`) with four options: `access.none` (NEW key), `access.readOnly`, `access.fullEdit`, `access.hiddenPassword` — reusing the three shipped strings VERBATIM per MOD-03, never reworded.

    Write a `reconcileRow` function (module-level, matching this file's existing `grantCollectionToRecipients`/`shareItemWithRecipients` export-for-testability convention) implementing the exact three-way dispatch RESEARCH.md's Pattern 1 specifies: `(null, pendingLevel)` where pendingLevel !== "none" -> grant (below); `(currentLevel, "none")` where currentLevel !== null -> revoke via `revokeCollectionAccess(collectionId, userId)`; `(currentLevel, pendingLevel)` both non-null and different -> update via `updateCollectionAccess(collectionId, userId, pendingLevel)` (the 31-01 wrapper); `pendingLevel === currentLevel` (including both `"none"`/`null`) -> no network call at all. For the GRANT branch: when `destinationId !== null` (existing destination) call `reshareCollectionToNewMember(destinationId, userId, pendingLevel, ownUk)` (the Phase 30 composition — unwrap-own-key, reseal-to-recipient, already real-WASM-proven, never call `WasmCollectionKey.generate()` here); when minting a new folder, grant via the SAME per-recipient `addCollectionMember` call the existing `grantCollectionToRecipients` helper makes, but with EACH row's OWN `pendingLevel` rather than one shared `level` argument — do not widen `grantCollectionToRecipients`'s existing signature (family-wide callers still need its current one-level-for-all shape, per CONTEXT's "unchanged from Phase 30" instruction); write the per-row grant loop as new code in the per-person submit path instead.

    Wire `reconcileRow` into BOTH `submitFolderVariant`'s mint-new path (every row dispatches as grant-or-noop, since `currentLevel` is always `null` there) and a NEW existing-destination submit path (fetch-then-three-way-dispatch per row) — both replace the OLD `selectedRecipientIds`/`accessLevel`-driven submit logic for the per-person (non-family-wide) branch specifically. Preserve the seed-move-from-personal-folder logic (`seedFolder`) completely unchanged; it applies only to the mint-new path.

    Adapt (do not delete) the family-wide mutual-exclusivity logic: `toggleFamilyWide`'s existing disable condition (`selectedRecipientIds.size > 0`) becomes "any row's `pendingLevel !== "none"`"; selecting the family-wide checkbox resets every row's `pendingLevel` back to its own `currentLevel` (mirroring the old `toggleRecipient`'s "picking anyone clears family-wide" symmetry, now reversed). The "Cała rodzina" boxed row, its `Users` icon, its member-count text, its timing caveat, and its OWN single access-level control (kept as the existing radio group, or reused verbatim) stay 100% unchanged — this plan touches only the per-person branch.

    Add new dictionary keys verbatim per 31-UI-SPEC.md's Copywriting Contract: `share.destinationLabel`, `share.destinationNewFolderOption`, `share.destinationNewGroupLabel`, `share.destinationExistingGroupLabel`, `access.none`, `share.rowCurrentlyLabel`, `share.rowNoPublishedKey` (PL/EN as specified in the UI-SPEC table — copy the exact strings, do not retype the Polish diacritics from memory).

    Update `ShareDialog.test.tsx`'s per-person-mode tests: every assertion that clicks a `share-recipient-{userId}` checkbox and a `share-access-level-{value}` radio for the non-family-wide path must become a `share-recipient-row-select-{userId}` value change instead — keep every family-wide-mode test (checkbox path for "Cała rodzina") completely unchanged, since that markup is untouched.
  </action>
  <verify>
    <automated>cd web && npm run compile && npm test -- ShareDialog</automated>
  </verify>
  <done>Destination selector renders for folder scope, filtered to edit-held non-item_bucket collections; rows show real current levels pre-filled; reconcileRow's three branches (grant/update/revoke/no-op) are wired into both mint-new and existing-destination submit paths; family-wide mode is unchanged and still mutually exclusive with per-person rows; npm test -- ShareDialog is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ORG-03/SC3 real-WASM proof — a new recipient on an existing destination decrypts pre-existing items</name>
  <files>web/src/components/vault/ShareDialog.real-wasm.test.ts</files>
  <behavior>
    - Real (unmocked) crypto: seed a collection with a real item encrypted under its real CollectionKey via the actual production composition, then reshare to a new recipient via ShareDialog's own exported dispatch function, and assert the new recipient's own identity key can unwrap the collection's sealed_key and decrypt the pre-existing item's real ciphertext back to the original plaintext.
  </behavior>
  <action>
    Extend `ShareDialog.real-wasm.test.ts` (the established real-WASM escape hatch this codebase uses for crypto-adjacent claims — mocked-crypto unit tests are NOT evidence per REQUIREMENTS.md's Non-Negotiable 2) with a case proving SC3/ORG-03: construct two real, locally-generated identity keypairs (owner + new recipient, mirroring `reseal.real-wasm.test.ts`'s own established fixture shape); the owner generates a real `WasmCollectionKey`, encrypts a real item under it (`encryptItemForCollection`), and reshares the SAME (never regenerated) key to the new recipient by calling `reconcileRow`/the grant path this plan just exported from `ShareDialog.tsx` — exercise the ACTUAL production function, never a re-implementation of its composition (mirrors this file's own stated discipline for `shareItemWithRecipients`, which exists specifically so `ShareDialog.real-wasm.test.ts` proves the real sequence, not a parallel one that could silently drift). Assert the new recipient's own identity key genuinely unwraps the collection's `sealed_key` (`unsealCollectionKey`) and that decrypting the pre-existing item's ciphertext under the unwrapped key reproduces the exact original plaintext.

    Falsification (mandatory, record exact output in the SUMMARY): temporarily make the grant branch reseal a FRESHLY GENERATED key instead of the unwrapped existing one (i.e., call `WasmCollectionKey.generate()` where `reshareCollectionToNewMember`/the equivalent inline logic should reuse the unwrapped key), re-run this test, confirm it fails with a decrypt error (the new recipient's unwrapped key no longer matches the item's actual encryption key), record the exact failure output, then restore the correct behavior and re-run to confirm green.
  </action>
  <verify>
    <automated>cd web && npm run compile && npm test -- ShareDialog.real-wasm</automated>
  </verify>
  <done>A real-WASM test proves a new recipient added to an existing destination decrypts an item that existed in it BEFORE they joined, exercising the dialog's real production dispatch code; the falsification record shows the test genuinely fails when the composition is broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client-side destination filter -> server-side authorization | The destination selector's `access_level === "edit"` filter is advisory UX only; the real boundary is the server's `RequireEdit`/`may_grant_access_level` checks the existing `add_member`/new `update_access` routes already enforce. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-06 | Elevation of Privilege | Destination selector's client-side filter | medium | mitigate | Filter mirrors `SharingOverviewPanel.tsx:315`'s already-proven predicate (`access_level === "edit" && family_wide_kind !== "item_bucket"`), but the client filter is defense-in-depth only — the server-side `RequireEdit`/`may_grant_access_level` bounds (31-01, existing `add_member`) are the real enforcement point regardless of a client filter bug. |
| T-31-07 | Tampering / Repudiation | `reconcileRow`'s grant/update/revoke dispatch | medium | mitigate | Every dispatch decision derives from the SAME `rows` state the UI renders (`row.currentLevel`/`row.pendingLevel`) — no separate, divergent computation of "what will be sent" vs "what is shown," closing the class of bug where the summary/UI says one thing and the network call does another. |
| T-31-08 | Information Disclosure | `reshareCollectionToNewMember`'s reuse for the grant branch | low | accept | Already real-WASM-proven in Phase 30 (T-25-16 no-public-key throw-before-network, never a fresh key generation) — this plan is a new call site of already-audited crypto, not new crypto. |

Package Legitimacy Audit: not applicable — no new dependencies.
</threat_model>

<verification>
`npm run compile` clean; `npm test -- ShareDialog` and `npm test -- ShareDialog.real-wasm` both green; the destination selector and per-row select markup exist with the exact `data-testid`s 31-UI-SPEC.md's inventory names.
</verification>

<success_criteria>
Folder-scope per-person sharing renders a destination selector and per-row access-level controls, dispatches grant/update/revoke/no-op correctly by comparing current vs. pending level, and a real-WASM test proves a newly-added recipient on an existing destination decrypts content that predates their access.
</success_criteria>

<output>
Create `.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-02-SUMMARY.md` when done
</output>
