---
phase: 31-the-share-dialog-per-person-access-existing-destinations
plan: 04
type: execute
wave: 4
depends_on: ["31-03"]
files_modified:
  - web/src/components/vault/ShareDialog.tsx
  - web/src/components/vault/ShareDialog.test.tsx
  - web/src/lib/i18n/dictionary.ts
autonomous: true
requirements: [MOD-01, MOD-03]

must_haves:
  truths:
    - "A bare item's per-person shares support the exact same grant/update/revoke row model a folder's do — MOD-01 is not folder-only."
    - "On a repeat share by an already-hidden-password-acked account, the always-visible inline note states plainly that hidden-password is an interface protection and never a cryptographic one — no hover, no second click."
    - "The dialog scrolls as a single region; Cancel/Save stay reachable without scrolling past the row list at any row count."
  artifacts:
    - web/src/components/vault/ShareDialog.tsx (item-scope dispatch, revised hidden-password copy, scroll/footer split)
  key_links:
    - "Item-scope reconcileRow grant -> createItemShare; update -> updateItemShare (31-01); revoke -> revokeItemShare"
    - "share.hiddenPasswordInlineNote's revised text -> echoes share.hiddenPasswordDisclosureBody's own established 'nie kryptograficznie'/'not cryptographically' phrasing verbatim, not new vocabulary"
---

<objective>
Extend the per-row grant/update/revoke dispatcher 31-02 built for the folder scope to the bare-item scope (MOD-01 applies to both — SC1's "two people at two different levels, one submission" names no scope restriction), select the correct submit CTA text for an "editing an existing access picture" destination vs. a genuinely fresh share, and close the MOD-03/SC4 gap the UI checker's blocker 2 found: the always-visible hidden-password inline note — the ONLY copy an already-acked account ever sees again — must state directly that hidden-password is an interface protection and never a cryptographic one, not merely imply it. Also lands the UI-SPEC's required scroll/footer restructure so a long member list never traps the hidden-password note or the pending-revocations summary behind an undiscoverable inner scroller.

Purpose: MOD-01 (item scope completes the row model), MOD-03/SC4 (the honesty bar on a repeat share, the actual gap the checker found).
Output: item-scope per-row sharing works identically to folder-scope; the hidden-password disclosure states the fact directly on every occasion after the first; the dialog is scrollable-as-one-region with a pinned footer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-CONTEXT.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-RESEARCH.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-UI-SPEC.md

@web/src/components/vault/ShareDialog.tsx
@web/src/lib/vault/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Item-scope per-row dispatch + CTA text selection</name>
  <files>web/src/components/vault/ShareDialog.tsx, web/src/components/vault/ShareDialog.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <behavior>
    - For scope.kind === "item" and NOT family-wide: rows seed currentLevel from listItemShares(item.id); reconcileRow's grant branch calls createItemShare, update calls updateItemShare, revoke calls revokeItemShare — same three-way dispatch shape as the folder scope, different target functions.
    - CTA text: share.ctaSaveAccess when destination.kind === "existing" (folder) OR (item scope AND ≥1 row's CURRENT level is non-null); otherwise the existing share.ctaFolder/share.ctaItem.
  </behavior>
  <action>
    For `scope.kind === "item"` (bare item, not family-wide — the family-wide item_bucket path via `submitItemFamilyWide` stays completely untouched, it has no per-row concept), render the SAME Row Anatomy 31-02 built for the folder scope (per-person rows, no destination selector — 31-UI-SPEC.md's Destination Selector Contract explicitly renders it only for `scope.kind === "folder"`), seeding each row's `currentLevel` from `listItemShares(item.id)` (the existing `vault/api.ts::listItemShares` wrapper) instead of `getCollectionAccessList`. Write the item-scope `reconcileRow` dispatch using the SAME three-way shape 31-02 established (grant/update/revoke/no-op by comparing currentLevel to pendingLevel), targeting `createItemShare` (grant — the existing `shareItemWithRecipients` composition, called per-row with that row's OWN `pendingLevel` rather than one shared `accessLevel` for the whole selected set), `updateItemShare` (update — the 31-01 wrapper), `revokeItemShare` (revoke — existing, SHARE-06). Do not touch `submitItemFamilyWide`/`findOrCreateFamilyItemBucket` at all; the family-wide item toggle keeps its own single-level control exactly as shipped.

    Add `share.ctaSaveAccess` (NEW key, exact PL "Zapisz dostęp"/EN "Save access" per 31-UI-SPEC.md's Copywriting Contract) and select it over the existing `share.ctaFolder`/`share.ctaItem` when `destination.kind === "existing"` (folder scope, an existing destination is selected) OR (item scope AND at least one row's CURRENT — not pending — level is non-null, meaning the item already has ≥1 recipient among the standing rows). This is the honest distinction 31-UI-SPEC.md's Color section states: editing an already-shared destination's access picture is not the same act as sharing it for the first time.

    Update `ShareDialog.test.tsx` for the item-scope row-based tests (replacing the old item-scope checkbox+radio assertions with row-select assertions, mirroring 31-02's folder-scope test updates) and add CTA-selection test cases for all four combinations (fresh folder, existing folder, fresh item, item with existing recipients).
  </action>
  <verify>
    <automated>cd web && npm run compile && npm test -- ShareDialog</automated>
  </verify>
  <done>Item-scope per-person sharing dispatches grant/update/revoke identically to folder scope; the family-wide item path is unchanged; the CTA text correctly distinguishes a fresh share from editing an existing access picture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hidden-password honesty revision (MOD-03/SC4) + scroll/footer restructure</name>
  <files>web/src/components/vault/ShareDialog.tsx, web/src/components/vault/ShareDialog.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <behavior>
    - The one-time blocking modal fires the FIRST time ANY row's select becomes hidden_password (not the old single global radio) — never per-row, per CONTEXT's "text flooding" rejection.
    - On a REPEAT share by an already-acked account (localStorage flag pre-set), the always-visible inline note states directly that hidden-password is an interface protection and never a cryptographic one, with no hover and no second click.
  </behavior>
  <action>
    Revise the hidden-password one-time blocking-modal trigger: it fires the FIRST time any row's `&lt;select&gt;` value becomes `hidden_password` (was: the single global radio group becoming `hidden_password`) — check this on every row-level `onChange`, never per-row repeatedly once already shown this session. Revise `share.hiddenPasswordInlineNote` IN PLACE (same key — per 31-UI-SPEC.md this is a strengthening, not the "softened/reworded" change the Phase 26 honesty-string comment forbids) to the exact PL/EN text 31-UI-SPEC.md's Copywriting Contract specifies: PL "Ukryte tylko w interfejsie, nie kryptograficznie — {recipient} nadal ma dostęp do klucza i technicznie może odzyskać hasło.", EN "Hidden in the interface only, not cryptographically — {recipient} still has key access and can technically recover the password." — echoing `share.hiddenPasswordDisclosureBody`'s own established "nie kryptograficznie"/"not cryptographically" and "technicznie może odzyskać hasło"/"can technically recover the password" phrasing verbatim, never inventing new vocabulary for the same fact. Re-scope the note's subject/multi-subject fallback logic (`hiddenPasswordNoteSubject`, currently derived from `selectedRecipientIds`) from "selected recipients" to "rows currently at `hidden_password`" — render the note ONCE, below the row list, the moment ≥1 row is at that level (never per-row).

    Restructure the dialog's outer shell per 31-UI-SPEC.md's Scale & Scroll Contract: the card itself becomes the single scroll container (`max-h-[85vh] flex flex-col`, an inner `overflow-y-auto p-6` body holding title/destination selector/"Cała rodzina" row/row list/hidden-password note/pending-revocations summary/error slots), with the Cancel/Save footer moved OUTSIDE that scroll region into a `shrink-0 border-t border-base-300 p-4` sibling — no nested scroll containers (the OLD `max-h-48 overflow-y-auto` flat checkbox list is removed entirely; the row list is now part of the SAME single scrolling document flow). This guarantees the footer and the honesty notes beneath the row list are reachable by the SAME scroll gesture, at any row count.

    Add a new unit test proving MOD-03/SC4 specifically on a REPEAT share: pre-set the per-account `localStorage` hidden-password-ack flag (mirroring the existing ack-flow tests' own setup), select `hidden_password` on a row (the one-time modal must NOT reappear), and assert the rendered inline note's text contains the "not cryptographically"/"can technically recover" phrasing directly — the exact gap 31-UI-SPEC.md records the checker found unproven (the old wording only implied it).
  </action>
  <verify>
    <automated>cd web && npm run compile && npm test -- ShareDialog</automated>
    <human-check>Open the share dialog at 375px and at desktop width with a row set to hidden_password; confirm the revised PL `share.hiddenPasswordInlineNote` string wraps cleanly without clipping and without pushing the footer off-card, per 31-VALIDATION.md's Manual-Only Verifications table.</human-check>
  </verify>
  <done>The hidden-password one-time modal fires on any row's transition to that level; the always-visible inline note states the interface-only/not-cryptographic fact directly on a repeat share; the dialog scrolls as one region with a pinned, always-reachable footer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan is UI/copy/layout work against already-audited item-share routes (create_share/update_share/revoke_share, RequireEdit-gated); no new server surface or authorization decision is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-12 | Elevation of Privilege | Item-scope reconcileRow dispatching against a collection-scoped item | low | accept | Structurally unreachable: item-scope per-row rendering only applies to `scope.kind === "item"` in the non-family-wide branch, which the codebase's own `ShareDialogScope` type only ever constructs for a bare personal item (`DetailPanel.tsx`/`ItemContextMenu.tsx`'s entry points); the family-wide item path (which DOES route through a collection) stays on its own mutually-exclusive toggle with no per-row rows. Server-side, `create_share`/`update_share` additionally 400 on a collection-scoped item regardless (WR-10, pre-existing). |
| T-31-13 | Information Disclosure | Hidden-password inline note's honesty claim | medium | mitigate | The revised copy is verified against `share.hiddenPasswordDisclosureBody`'s own established phrasing (verbatim echo, not new vocabulary) and pinned by a dedicated unit test asserting the exact "not cryptographically"/"can technically recover" substrings render on a REPEAT share — the specific case that was previously unproven. |

Package Legitimacy Audit: not applicable — no new dependencies.
</threat_model>

<verification>
`npm run compile` clean; `npm test -- ShareDialog` green including the new MOD-03/SC4 repeat-share test and the CTA-selection cases; manual PL-width check performed per 31-VALIDATION.md.
</verification>

<success_criteria>
Item-scope sharing has full per-row parity with folder-scope; the hidden-password disclosure is honest on every occasion, not just the first; the dialog never traps content in an undiscoverable inner scroll region.
</success_criteria>

<output>
Create `.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-04-SUMMARY.md` when done
</output>
