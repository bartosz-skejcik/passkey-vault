---
phase: 31-the-share-dialog-per-person-access-existing-destinations
plan: 04
type: execute
wave: 4
depends_on: ["31-03"]
files_modified:
  - web/src/components/vault/ShareDialog.tsx
  - web/src/components/vault/ShareDialog.test.tsx
  - web/src/lib/i18n/dictionary.ts
  - web/e2e/sharing.spec.ts
autonomous: true
requirements: [MOD-01]

must_haves:
  truths:
    - "Before saving, a user is told plainly who is about to lose access and how many — the summary is present exactly when a real revocation is queued, never otherwise."
    - "Setting a member with existing access to 'Brak dostępu' and saving revokes it, and that member's own client loses the ability to decrypt the shared content on its next completed sync — proven live with a positive read before and a failing read after."
  artifacts:
    - web/src/components/vault/ShareDialog.tsx (pending-revocations summary)
    - web/e2e/sharing.spec.ts (the sixth proof obligation, live, two sessions)
  key_links:
    - "The pending-revocations summary text -> derived from the SAME rows state reconcileRow dispatches from (31-02/31-03), never a separate computation"
    - "Revoke dispatch (already reachable since 31-03) -> revokeCollectionAccess, whose server-side commit bumps the revoked recipient's own vault_revision -> their next GET /api/sync detects the change"
---

<objective>
Land the pending-revocations honesty summary — the UI half of the sixth proof obligation CONTEXT.md records deliberately beyond the ROADMAP's five recorded success criteria — and prove that obligation live: setting a member with existing access to "Brak dostępu" and saving revokes it, and that member's own client loses the ability to decrypt on the next completed sync. This is a first-class criterion, not an extra; it gets its own live, two-session, positive-then-negative proof, mirroring the pattern `family-wide-sharing.spec.ts` already established for the family-wide revocation surface. The revocation MECHANISM itself (reconcileRow's `(X, "none")` branch dispatching `revokeCollectionAccess`) is already reachable as of 31-03 — this plan adds only the honesty summary UI and the live proof.

Purpose: the sixth proof obligation (revocation honesty + mechanism).
Output: a working pending-revocations summary with genuine falsification coverage, and a live two-session positive/negative revocation proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-CONTEXT.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-RESEARCH.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-UI-SPEC.md

@web/src/components/vault/ShareDialog.tsx
@web/e2e/sharing.spec.ts
@web/e2e/family-wide-sharing.spec.ts
@web/src/lib/vault/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pending-revocations honesty summary</name>
  <files>web/src/components/vault/ShareDialog.tsx, web/src/components/vault/ShareDialog.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <behavior>
    - Renders only when ≥1 row's pendingLevel === "none" while its currentLevel is not null (a real queued revocation) — absent otherwise, including when the pending set is pure-addition/pure-edit.
    - {count}/{names} derive from the exact same `rows` array reconcileRow dispatches from — never a second, independently-computed list.
  </behavior>
  <action>
    Add `share.pendingRevocationsSummary` (NEW key, PL/EN exact strings from 31-UI-SPEC.md's Copywriting Contract, interpolating `{count}` and a comma-joined `{names}` list mirroring `share.partialShareFailed`'s existing `.join(", ")` convention). Render it below the row list, above the footer, `role="status" aria-live="polite"`, `data-testid="share-pending-revocations-summary"`, computed by filtering the SAME `rows` state for `pendingLevel === "none" && currentLevel !== null`. Per 31-UI-SPEC.md's "Revocation confirmation" contract: do NOT open `RevokeShareDialog` or any second confirm step — the row's own `UserMinus` icon (already wired in 31-02) plus this summary together carry the honesty weight, without a second click. `RevokeShareDialog` itself stays untouched.

    Update `ShareDialog.test.tsx`: add cases proving the summary is ABSENT when the pending set is pure-addition/pure-edit, PRESENT with the correct count/name list when ≥1 row is queued for revocation, and that the submit button's own label text does NOT change when the pending set contains a revocation.

    Falsification (mandatory, record exact output in the SUMMARY — this closes Blocker 6 for this task): the "absent when pure-addition" assertion is an absence assertion and is exactly the shape that can go vacuous. Falsify it by temporarily rendering the summary UNCONDITIONALLY (removing its render-guard), re-run the "absent when pure-addition" test, confirm it goes from pass to fail (the summary now renders when it should not), record the exact failure output, then restore the correct guard and re-run to confirm green. Separately, falsify the "present with correct count" case by temporarily hard-coding `{count}` to a wrong value, confirm that test fails on the interpolated text, restore.
  </action>
  <verify>
    <automated>cd web && npm run compile && npm test</automated>
  </verify>
  <done>The pending-revocations summary renders exactly when a real revocation is queued, states who and how many, is aria-live polite, never opens a second confirmation, and both its presence and absence assertions are falsification-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Live e2e — the sixth proof obligation, positive-then-negative revocation</name>
  <files>web/e2e/sharing.spec.ts</files>
  <action>
    Add a new live e2e test using `twoSessions` (owner + a real member), mirroring `family-wide-sharing.spec.ts`'s own established positive-anchor/negative-anchor shape (that file's `revocation: a member REMOVED by the owner...` test, lines ~1209-1307, is the direct precedent to follow, not re-derive independently). This file does not import that spec's helpers — write a small, local `assertRecipientDecrypts`-equivalent in `sharing.spec.ts` itself (asserting the item's decrypted name/password renders on the member's own page), per this codebase's own established per-file-owns-its-own-tiny-helper convention (already used throughout this file), rather than cross-importing from `family-wide-sharing.spec.ts`.

    1. Owner creates a real shared folder, shares it with the member at `read` via the redesigned dialog (destination selector defaults to "Nowy folder…", the member's row set to `access.readOnly`).
    2. **Before the positive anchor**, `relockAndUnlock` (or this file's own reload-and-unlock helper) the member's session — `family-wide-sharing.spec.ts:1260-1268` documents directly that `refreshCollectionsNow()` fires only on the sharer's own submit, an unlock transition, or the pending/reseal path, never a passive session's ambient poll; without this step the member's session may never discover the brand-new collection at all, and the positive anchor would be untestable rather than merely slow (Warning 2's fix). The member's own session then reads the shared item for real (positive "was readable" anchor — assert the item's decrypted name/password renders, not merely that a row exists).
    3. Owner reopens `ShareDialog` against the SAME existing folder (via the destination selector's "Istniejące foldery" group), sets the member's row to `access.none` ("Brak dostępu"), and saves — assert `share-pending-revocations-summary` is visible and names this member BEFORE Save is clicked, querying it while `share-dialog` is still mounted (never after `waitFor({ state: "detached" })`).
    4. On the member's OWN still-open session (no reload this time — the next completed sync, mirroring `family-wide-sharing.spec.ts`'s proven `item-row-${id}` `toHaveCount(0, { timeout: 60000 })` pattern), assert the item disappears — negative anchor.

    Falsification (mandatory, record exact output in the SUMMARY): temporarily make the revoke dispatch branch a no-op instead of calling `revokeCollectionAccess`, re-run this test, confirm the negative-anchor assertion times out / fails, record the exact failure output, then restore the correct dispatch and re-run to confirm green.
  </action>
  <verify>
    <automated>cd web && npm run build && npx playwright test e2e/sharing.spec.ts --retries=0</automated>
  </verify>
  <done>A real second session's positive read before revocation (after a deliberate relock/unlock to guarantee discovery) and failing read after (next completed sync, no reload) both pass; the pending-revocations summary is asserted visible and correctly named before Save; the falsification record shows the test genuinely detects a broken revoke dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Owner's revoke action -> revoked member's own client | The server's DELETE on `collection_keys` is the authoritative fact; the revoked member's client must lose access through its OWN normal, already-authorized sync channel, never through a push naming them their own removal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-12 | Elevation of Privilege | Revocation dispatch against an `item_bucket` destination | medium | mitigate | Structurally unreachable — the destination selector (31-03) already excludes `item_bucket` collections, and the server's existing `collections::revoke_access` unconditionally refuses (403) on one regardless, per quick task 260812-01e. |
| T-31-13 | Repudiation | Pending-revocations summary text vs. actual dispatch | medium | mitigate | The summary's `{count}`/`{names}` and the dispatcher's revoke set are computed from the IDENTICAL `rows` state — falsification-proven this plan (Task 1). |
| T-31-14 | Information Disclosure | The revoked member's own WS channel | low | accept | `collections::revoke_access` (existing, unchanged) deliberately publishes no event naming the just-revoked member their own removal — inherited, already-audited SHARE-06 behavior. |

Package Legitimacy Audit: not applicable — no new dependencies.
</threat_model>

<verification>
`npm run compile` clean; `npm test` green with the pending-revocations summary cases (both falsification-proven); `npx playwright test e2e/sharing.spec.ts --retries=0` green including the sixth-obligation test and every pre-existing test in the file.
</verification>

<success_criteria>
Revoking a member through the redesigned dialog is honestly disclosed before Save and genuinely removes their ability to decrypt on their own next completed sync, proven live with a real second session that reliably discovers the share first.
</success_criteria>

<output>
Create `.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-04-SUMMARY.md` when done
</output>
