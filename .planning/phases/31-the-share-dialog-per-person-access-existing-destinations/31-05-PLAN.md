---
phase: 31-the-share-dialog-per-person-access-existing-destinations
plan: 05
type: execute
wave: 5
depends_on: ["31-04"]
files_modified:
  - web/src/components/vault/ShareDialog.tsx
  - web/src/components/vault/ShareDialog.test.tsx
  - web/src/lib/i18n/dictionary.ts
autonomous: true
requirements: [MOD-01, MOD-03]

must_haves:
  truths:
    - "Editing an already-shared destination's access picture is honestly distinguished, in the submit CTA, from sharing something for the first time."
    - "On a repeat share by an already-hidden-password-acked account, the always-visible inline note states plainly that hidden-password is an interface protection and never a cryptographic one — no hover, no second click."
  artifacts:
    - web/src/components/vault/ShareDialog.tsx (CTA selection, revised hidden-password copy)
  key_links:
    - "share.hiddenPasswordInlineNote's revised text -> echoes share.hiddenPasswordDisclosureBody's own established 'nie kryptograficznie'/'not cryptographically' phrasing verbatim, not new vocabulary"
    - "CTA selection -> destination.kind === 'existing' (folder) OR (item scope AND ≥1 row's CURRENT level is non-null)"
---

<objective>
Close the two remaining copy-level gaps now that the dialog's mechanism is fully working (rows for both scopes since 31-02, destination selection since 31-03, revocation since 31-04): select the correct submit CTA text for "editing an existing access picture" vs. a genuinely fresh share, and revise the hidden-password inline note's wording to state directly — not merely imply — that hidden-password is an interface protection and never a cryptographic one. This is MOD-03/SC4's own gap, per the UI checker's blocker 2: on a REPEAT share by an already-acked account, the always-visible inline note is the ONLY copy that account ever sees again, and the old wording only implied the fact.

The hidden-password disclosure's TRIGGER and SUBJECT (fires on any row, subject derived from rows at that level) were already re-wired in 31-02 alongside the row model itself, so MOD-03 has been true throughout the migration — this plan only strengthens the note's TEXT.

Purpose: MOD-03/SC4 (the honesty bar's exact wording), MOD-01 (CTA distinction is part of the row model reading honestly as "the access picture", per CONTEXT.md's own framing).
Output: the submit CTA correctly distinguishes editing from fresh sharing; the hidden-password inline note states the fact directly on every occasion after the first.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-CONTEXT.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-UI-SPEC.md
@.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-VALIDATION.md

@web/src/components/vault/ShareDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Submit CTA text selection</name>
  <files>web/src/components/vault/ShareDialog.tsx, web/src/components/vault/ShareDialog.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <behavior>
    - share.ctaSaveAccess is selected when destination.kind === "existing" (folder scope, an existing destination is selected) OR (item scope AND ≥1 row's CURRENT — not pending — level is non-null); otherwise the existing share.ctaFolder/share.ctaItem.
  </behavior>
  <action>
    Add `share.ctaSaveAccess` (NEW key, exact PL "Zapisz dostęp"/EN "Save access" per 31-UI-SPEC.md's Copywriting Contract). Select it over the existing `share.ctaFolder`/`share.ctaItem` when `destinationId !== null` (folder scope, an existing destination is selected via 31-03's selector) OR (`scope.kind === "item"` AND at least one row's CURRENT level is non-null, meaning the item already has ≥1 recipient among the standing rows fetched via `listItemShares` since 31-02). Add unit test cases for all four combinations: fresh folder (mint-new, no existing recipients) -> `ctaFolder`; existing folder destination -> `ctaSaveAccess`; fresh item (no existing shares) -> `ctaItem`; item with ≥1 existing recipient row -> `ctaSaveAccess`.

    Falsification (mandatory, record exact output in the SUMMARY): temporarily invert the existing-folder condition (`destinationId === null` instead of `!== null`), re-run the "existing folder destination -> ctaSaveAccess" test, confirm it fails (wrong CTA text rendered), record the exact failure output, then restore and re-run to confirm green.
  </action>
  <verify>
    <automated>cd web && npm run compile && npm test</automated>
  </verify>
  <done>The submit CTA correctly distinguishes a fresh share from editing an existing access picture, for both scopes, with a falsification-proven test covering all four cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hidden-password inline note — the honesty-wording revision (MOD-03/SC4)</name>
  <files>web/src/components/vault/ShareDialog.tsx, web/src/components/vault/ShareDialog.test.tsx</files>
  <behavior>
    - On a REPEAT share by an already-acked account (localStorage flag pre-set, one-time modal does NOT reappear), the always-visible inline note's rendered text contains the "not cryptographically"/"can technically recover" phrasing directly.
  </behavior>
  <action>
    Revise `share.hiddenPasswordInlineNote` IN PLACE (same key — per 31-UI-SPEC.md this is a strengthening, not the "softened/reworded" change the Phase 26 honesty-string comment forbids) to the exact PL/EN text 31-UI-SPEC.md's Copywriting Contract specifies: PL "Ukryte tylko w interfejsie, nie kryptograficznie — {recipient} nadal ma dostęp do klucza i technicznie może odzyskać hasło.", EN "Hidden in the interface only, not cryptographically — {recipient} still has key access and can technically recover the password." — echoing `share.hiddenPasswordDisclosureBody`'s own established "nie kryptograficznie"/"not cryptographically" and "technicznie może odzyskać hasło"/"can technically recover the password" phrasing verbatim, never inventing new vocabulary for the same fact. The trigger/subject-derivation logic (any row at `hidden_password`, subject from rows) is UNCHANGED from 31-02 — this task touches only the string's own text.

    Add a new unit test proving MOD-03/SC4 specifically on a REPEAT share: pre-set the per-account `localStorage` hidden-password-ack flag (mirroring the existing ack-flow tests' own setup), select `hidden_password` on a row (the one-time modal must NOT reappear), and assert the rendered inline note's text contains the "not cryptographically"/"can technically recover" phrasing directly — the exact gap 31-UI-SPEC.md records the checker found unproven (the old wording only implied it).

    Falsification (mandatory, record exact output in the SUMMARY — closes Blocker 6 for this task): temporarily revert `share.hiddenPasswordInlineNote` to its PRE-this-task wording (the old "still has key access" phrasing without "not cryptographically"/"can technically recover"), re-run the new repeat-share test, confirm it fails (the substring assertion no longer matches), record the exact failure output, then restore the revised wording and re-run to confirm green.
  </action>
  <verify>
    <automated>cd web && npm run compile && npm test</automated>
    <human-check>Open the share dialog at 375px and at desktop width with a row set to hidden_password; confirm the revised PL `share.hiddenPasswordInlineNote` string wraps cleanly without clipping and without pushing the footer off-card, per 31-VALIDATION.md's Manual-Only Verifications table.</human-check>
  </verify>
  <done>The always-visible inline note states the interface-only/not-cryptographic fact directly on a repeat share, proven by a falsification-checked unit test; the manual PL-width check is performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure copy/CTA-selection work against an already-working, already-audited dispatch mechanism (31-02/31-03); no new server surface or authorization decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-15 | Information Disclosure | Hidden-password inline note's honesty claim | high | mitigate | The revised copy is verified against `share.hiddenPasswordDisclosureBody`'s own established phrasing (verbatim echo, not new vocabulary) and pinned by a falsification-checked unit test asserting the exact "not cryptographically"/"can technically recover" substrings render on a REPEAT share — the specific case that was previously unproven. |

Package Legitimacy Audit: not applicable — no new dependencies.
</threat_model>

<verification>
`npm run compile` clean; `npm test` green including the new MOD-03/SC4 repeat-share test and the CTA-selection cases, both falsification-proven; manual PL-width check performed.
</verification>

<success_criteria>
The submit CTA honestly distinguishes editing from fresh sharing; the hidden-password disclosure is honest in its exact wording on every occasion, not just the first.
</success_criteria>

<output>
Create `.planning/phases/31-the-share-dialog-per-person-access-existing-destinations/31-05-SUMMARY.md` when done
</output>
