---
phase: 12-passkey-provider
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - extension/lib/vault/types.ts
  - extension/entrypoints/background/credential-store.ts
  - extension/entrypoints/background/credential-store.test.ts
  - extension/entrypoints/background/provider-ceremony.ts
  - extension/entrypoints/background/provider-ceremony.test.ts
  - extension/entrypoints/background/router.ts
  - extension/entrypoints/background/router.test.ts
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]

must_haves:
  truths:
    - "Given the vault is unlocked and holds a matching passkey item for the RP, a credentials.get message produces a signed assertion response without re-deriving or re-storing key material outside chrome.storage.session (D-09)."
    - "Given the vault is locked when a ceremony message arrives, the background opens the popup for unlock before proceeding, never silently failing the ceremony (D-09)."
    - "Given no matching credential or a user decline, the handler returns an explicit fallthrough response, never throwing or hanging (D-11, PROV-03)."
    - "Given the RP's create() request includes the prf extension, the response honestly reports PRF capability or a specific unavailable reason — never silently omitted (D-13/D-14, PROV-04)."
    - "Every message handler re-checks chrome.storage.session on each invocation rather than trusting in-memory state that might predate an idle-kill (D-10)."
  artifacts:
    - path: "extension/entrypoints/background/provider-ceremony.ts"
      provides: "handleCredentialsCreate/handleCredentialsGet pure orchestration functions"
    - path: "extension/entrypoints/background/credential-store.ts"
      provides: "findMatchingPasskeyItems — eager decrypt-and-filter vault query for a given RP"
    - path: "extension/entrypoints/background/router.ts"
      provides: "credentials.create/credentials.get message dispatch with sender-verified origin"
  key_links:
    - from: "extension/entrypoints/background/router.ts"
      to: "extension/entrypoints/background/provider-ceremony.ts"
      via: "onMessage dispatch for kind === 'credentials.create'/'credentials.get', passing sender-verified origin (not payload-reported origin)"
      pattern: "handleCredentials(Create|Get)"
    - from: "extension/entrypoints/background/provider-ceremony.ts"
      to: "crates/pv-wasm/src/lib.rs"
      via: "calls wasmCreateProviderCredential/wasmGetProviderAssertion (Plan 12-01) after confirming the vault is unlocked"
      pattern: "wasm(Create|Get)Provider"
---

<objective>
Implement the background service worker's provider-ceremony orchestration: dispatch `credentials.create`/`credentials.get` messages to handlers that check/await vault unlock (D-09), re-check session state fresh on every call (D-10), call Plan 12-01's WASM bindings, detect PRF capability honestly (D-13/D-14), and decide fallthrough vs. respond (D-11/PROV-03) — never throwing uncaught.

Purpose: This is the "server" tier of the extension's passkey provider — the sole place passkey-rs/pv-wasm are invoked (D-05) and the sole place the unlocked User Key is read from `chrome.storage.session`.
Output: `provider-ceremony.ts`, `credential-store.ts`, and an extended `router.ts` with full Vitest coverage of the decision logic (locked/unlocked, match/no-match, PRF capable/not, genuine failure).

**Cross-phase dependency note:** Phases 8-11 (extension bootstrap, session core, autofill messaging, generate/capture) had not executed at the time this plan was written — file paths below (`extension/entrypoints/background/router.ts`, the vault item-types module location, the messaging convention, the sync/CRUD client name) are RESEARCH.md's best-grounded proposal, not confirmed fact. Before writing any code, read the actual Phase 8/9/10 SUMMARY.md files (they will exist by the time this plan executes, since Phase 12 depends on Phase 11 completing) and adjust file paths/import names to match reality — do not create a parallel/duplicate router, messaging layer, or vault-types module if an equivalent already exists under a different name.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-passkey-provider/12-CONTEXT.md
@.planning/phases/12-passkey-provider/12-RESEARCH.md
@.planning/phases/12-passkey-provider/12-PATTERNS.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-CONTEXT.md
@.planning/phases/10-autofill-login-totp-card-identity/10-CONTEXT.md
@web/src/lib/vault/types.ts
@web/src/lib/passkeys/login.ts
@web/src/lib/passkeys/errors.ts

# Read the actual Phase 8-11 SUMMARY.md files at execution time (not available at plan-write time):
.planning/phases/08-extension-bootstrap-wasm-in-background-spike/*-SUMMARY.md
.planning/phases/09-session-unlock-core-popup-sync-client/*-SUMMARY.md
.planning/phases/10-autofill-login-totp-card-identity/*-SUMMARY.md
.planning/phases/11-generate-capture/*-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend vault item model + credential-store query helper</name>
  <files>extension/lib/vault/types.ts, extension/entrypoints/background/credential-store.ts, extension/entrypoints/background/credential-store.test.ts</files>
  <read_first>web/src/lib/vault/types.ts, .planning/phases/12-passkey-provider/12-PATTERNS.md (Vault item model extension section)</read_first>
  <action>
    Verify the extension's actual vault item-types module path against Phase 9/10's SUMMARY.md (expected `extension/lib/vault/types.ts` mirroring `web/src/lib/vault/types.ts` — if Phase 9/10 instead re-exports the web app's types via a shared package, extend THAT file; do not create a duplicate divergent copy).

    Add `"passkey"` to the `ItemType` union and a `PasskeyFields` interface: `type: "passkey"`, `rpId: string`, `credentialId: string` (base64url), plus whatever plaintext fields `pv-provider`'s `Passkey` JSON actually serializes to once Plan 12-01 Task 3 exists (re-verify field names against the real `new_passkey_json`/`encrypted_item_json` shape rather than inventing a `wrappedPrivateKey` string field — the private key material is already protected at the `EncryptedItem` level by `encrypt_item`, not by a second per-field wrap). Add `PasskeyFields` to the `ItemFields` union.

    Implement `findMatchingPasskeyItems(items: VaultItem[], rpId: string): { item: VaultItem; fields: PasskeyFields }[]` in `credential-store.ts` — a single eager decrypt-and-filter pass over the already-decrypted-in-memory vault item cache (verify the actual store accessor per Phase 9's conventions), filtering `fields.type === 'passkey' && fields.rpId === rpId`. This resolves 12-RESEARCH.md Open Question #1 in favor of eager filtering (appropriate for a personal/family-scale vault, not enterprise scale — no lazy per-credential-ID fetch needed).
  </action>
  <verify>
    <automated>npm --prefix extension test -- --run credential-store.test.ts</automated>
  </verify>
  <done>Filtering returns the correct subset against a 3-item fixture (2 passkey items for different RPs + 1 login item); no item is decrypted more than once per call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: provider-ceremony.ts — handleCredentialsCreate/handleCredentialsGet</name>
  <files>extension/entrypoints/background/provider-ceremony.ts, extension/entrypoints/background/provider-ceremony.test.ts</files>
  <read_first>web/src/lib/passkeys/login.ts, web/src/lib/passkeys/errors.ts, .planning/phases/12-passkey-provider/12-PATTERNS.md (background orchestration section), .planning/phases/12-passkey-provider/12-UI-SPEC.md (Phase-Specific Notes: "Where the ceremony consent UI lives")</read_first>
  <behavior>
    - Test: vault locked (`chrome.storage.session` mocked empty) — handler calls the popup-open mechanism (mock `browser.action.openPopup`, falling back to mock `browser.windows.create`) and does NOT call the WASM binding until an unlock-resolved signal arrives (D-09).
    - Test: vault unlocked, zero matching passkey items for the RP on `credentials.get` — handler returns `{ fallthrough: true }` WITHOUT calling `wasmGetProviderAssertion` and WITHOUT throwing (D-11/PROV-03).
    - Test: vault unlocked, exactly one matching item — handler calls the (mocked) `wasmGetProviderAssertion` and returns `{ fallthrough: false, credentialResponseJson }`.
    - Test: `credentials.create` request with `extensions.prf` present, mocked as Chromium-capable — response includes `{ prfCapable: true }`; same request mocked as Firefox/PRF-unsupported — response includes `{ prfCapable: false, prfUnavailableReason: <non-empty string> }` — the field is never omitted silently (D-13/D-14, PROV-04).
    - Test: a thrown exception from the (mocked) WASM call is caught and converted to `{ fallthrough: false, failed: true }` — surfaced, not swallowed, distinguishing genuine failure from decline/no-match (mirrors `login.ts`'s three-way cancel/failure/unavailable branching per 12-PATTERNS.md's Shared Patterns).
    - Test: every handler re-reads `chrome.storage.session` at the start of its own invocation rather than reading a module-level cached flag (D-10) — assert the mock storage getter is called at least once per handler invocation, even on a second call in the same test run.
  </behavior>
  <action>
    Implement `handleCredentialsCreate(req: CreateRpcRequest, senderOrigin: string): Promise<CreateRpcResponse>` and `handleCredentialsGet(req: GetRpcRequest, senderOrigin: string): Promise<GetRpcResponse>` as pure async functions (no framework/UI state — 12-PATTERNS.md's "pure orchestration + onStep callback" convention; an `onStep` callback is optional here since there's no popup-driven UI step sequence to report beyond open/resolve).

    Re-check `chrome.storage.session` for the unlocked User Key handle at the START of every call (D-10 — never trust a module-level "already unlocked" flag that might predate an idle-kill). If locked: attempt `browser.action.openPopup()` first; if unavailable/rejected, fall back to `browser.windows.create({ type: 'popup', width: 360, url: 'popup.html#/provider-ceremony' })` per 12-UI-SPEC.md's exact mechanism; await the popup's unlock-resolved signal (verify the actual signal mechanism against Phase 9's unlock-flow SUMMARY) before proceeding — never fail the ceremony outright while locked (D-09).

    On `credentials.get`: call `findMatchingPasskeyItems` (Task 1) scoped to `req.rpId`; zero matches -> `{ fallthrough: true }` immediately, no WASM call. One match -> call `wasmGetProviderAssertion` (Plan 12-01) with that item's `id`/`revision`, return `{ fallthrough: false, credentialResponseJson }`. Multiple matches -> signal a picker state for the popup UI (Plan 12-04) to render, awaiting the user's selection before calling `wasmGetProviderAssertion` with the chosen item.

    On `credentials.create`: call `wasmCreateProviderCredential` (Plan 12-01), then wrap the returned `encrypted_item_json` via `wasmWrapEphemeralProviderCred` (Plan 12-01) with a fresh `crypto.getRandomValues(new Uint8Array(32))` seed into a `pendingProviderItems` map in `chrome.storage.session`, respond to the page immediately with `credential_response_json` + `prfCapable`/`prfUnavailableReason`, then asynchronously persist via the existing vault CRUD/sync client (verify actual client name/shape against Phase 9's SUMMARY) and clear the pending entry once sync confirms. On next wake (per D-10), re-check for any still-pending entries in `chrome.storage.session` and retry their sync.

    PRF capability detection: check `req.publicKey.extensions?.prf` presence AND a runtime/browser capability signal (verify exact detection mechanism against 12-RESEARCH.md Pitfall 2 — `clientExtensionResults.prf.enabled` is create-time-only per the spec) — when present but unsupported, ALWAYS include a specific `prfUnavailableReason` string, never omit the field.

    Every exported handler wraps its body in try/catch; any exception -> `{ fallthrough: false, failed: true }`, never an uncaught throw or a hung promise (D-11).
  </action>
  <verify>
    <automated>npm --prefix extension test -- --run provider-ceremony.test.ts</automated>
  </verify>
  <done>All behavior-listed tests pass; grep confirms no exported handler lacks a top-level try/catch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: router.ts — dispatch credentials.create/get with sender-verified origin</name>
  <files>extension/entrypoints/background/router.ts, extension/entrypoints/background/router.test.ts</files>
  <read_first>.planning/phases/12-passkey-provider/12-RESEARCH.md (Security Domain, ASVS V4 row)</read_first>
  <behavior>
    - Test: a message with `kind: 'credentials.create'` and `sender.tab`/`sender.origin` present — dispatched to `handleCredentialsCreate` with the origin argument taken from `sender.origin` (or `sender.tab.url`'s parsed origin, whichever the actual `runtime.onMessage` sender shape provides), NOT from the payload's self-reported `origin` field — construct a test fixture where these deliberately differ and assert the SENDER's origin is what's used (ASVS V4).
    - Test: an unrecognized `kind` value falls through to the existing (Phase 9/10) dispatch chain unmodified — no regression against whatever message kinds Phase 9/10 already registered.
  </behavior>
  <action>
    Verify the actual background message-dispatch entrypoint/pattern established by Phase 9/10 (file name may not literally be `router.ts` — locate the real one and extend it there; do not create a parallel dispatcher). Add `credentials.create`/`credentials.get` cases that validate `sender.tab`/`sender.frameId`/origin from the `runtime.onMessage` sender object BEFORE dispatching to `provider-ceremony.ts` (Task 2), passing the sender-verified origin, not the payload's self-reported one.
  </action>
  <verify>
    <automated>npm --prefix extension test -- --run router.test.ts</automated>
  </verify>
  <done>New message kinds dispatch correctly using sender-verified origin; existing Phase 9/10 message kinds unaffected (regression test passes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ISOLATED content script <-> background (runtime.sendMessage) | Any content script (this extension's own, or in principle a compromised one) can send a `runtime.sendMessage` — background must verify sender, not trust payload |
| background <-> chrome.storage.session | Only the background reads/writes the unlocked User Key handle; must always re-check freshness after a possible idle-kill |
| background <-> pv-wasm (Plan 12-01) | Background is the sole caller of the WASM bindings that touch key material (D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Spoofing | router.ts message dispatch | high | mitigate | Origin/frameId taken from browser-verified `sender` object, never the payload's self-reported fields — tested explicitly in Task 3 |
| T-12-06 | Information Disclosure | provider-ceremony.ts session handling | high | mitigate | D-10 fresh re-check of `chrome.storage.session` on every handler invocation, tested explicitly in Task 2 |
| T-12-07 | Denial of Service (of ceremony) | provider-ceremony.ts | medium | mitigate | Top-level try/catch in every handler converts any failure to an explicit `{failed: true}` response, never a hang — tested in Task 2 |
| T-12-08 | Tampering | chrome.storage.session `pendingProviderItems` | low | accept | Storage is extension-internal (not page-reachable); contents are already ciphertext (ephemeral-wrapped `EncryptedItem`), so tampering only causes a decrypt failure on resume, never disclosure |

</threat_model>

<verification>
- [ ] `npm --prefix extension test -- --run credential-store.test.ts` passes
- [ ] `npm --prefix extension test -- --run provider-ceremony.test.ts` passes
- [ ] `npm --prefix extension test -- --run router.test.ts` passes
- [ ] `npm --prefix extension test` (full suite) has no regressions against Phase 9/10's existing tests
</verification>

<success_criteria>
- All tasks completed
- Locked-vault, no-match, multi-match, PRF-capable/unavailable, and genuine-failure paths all covered by passing tests
- No handler throws uncaught or hangs the page's promise
</success_criteria>

<output>
Create `.planning/phases/12-passkey-provider/12-02-SUMMARY.md` when done, noting any file-path/convention deviations found against the actual Phase 8-11 SUMMARY.md files.
</output>
