---
phase: 12-passkey-provider
plan: 04
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - extension/entrypoints/popup/ProviderCeremonyView.tsx
  - extension/entrypoints/popup/ProviderCeremonyView.test.tsx
  - extension/lib/i18n/dictionary.ts
  - extension/entrypoints/popup/App.tsx
  - extension/entrypoints/popup/App.test.tsx
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]

must_haves:
  truths:
    - "When background signals a pending ceremony, the popup (or a dedicated small window) mounts ProviderCeremonyView and takes over focus, regardless of what the user was previously viewing (UI-SPEC 'Where the ceremony consent UI lives')."
    - "A single-match get() ceremony pre-selects that credential with no picker list; a multi-match ceremony shows the 56px credential-choice list with the confirm CTA disabled until a selection is made."
    - "Dismissing the popup/window without confirming or explicitly declining is treated as an explicit decline by the background, never a hang (D-11)."
    - "The PRF-capable/PRF-unavailable note lines render only when the RP's own request included the prf extension, driven by the real capability signal background reports from the passkey-rs ceremony (never a browser check — D-16), using the exact Copywriting Contract strings in PL and EN, and are never conflated with Phase 3/4's vault-unlock PRF feature."
  artifacts:
    - path: "extension/entrypoints/popup/ProviderCeremonyView.tsx"
      provides: "Popup-hosted passkey ceremony consent screen per 12-UI-SPEC.md"
    - path: "extension/lib/i18n/dictionary.ts"
      provides: "provider.* PL/EN copy entries from 12-UI-SPEC.md's Copywriting Contract"
      contains: "provider.createTitle"
  key_links:
    - from: "extension/entrypoints/popup/App.tsx"
      to: "extension/entrypoints/popup/ProviderCeremonyView.tsx"
      via: "pending-ceremony chrome.storage.session flag read in App.tsx init sets ViewState to { kind: 'provider-ceremony' }, taking over any prior view"
      pattern: "provider-ceremony"
    - from: "extension/entrypoints/popup/ProviderCeremonyView.tsx"
      to: "extension/entrypoints/background/provider-ceremony.ts"
      via: "confirm/decline/selection messages sent back through the same background<->popup channel Plan 12-02 defines"
      pattern: "credentials\\.(create|get)"
---

<objective>
Build the popup-hosted passkey ceremony consent screen (`ProviderCeremonyView`) exactly per 12-UI-SPEC.md: single teal confirm CTA, ghost "use something else" fallback, multi-match credential picker, PRF note lines scoped to the RP's own request, and dismissal-as-decline wiring — never in-page, never a second competing accent color.

Purpose: This is the ONLY user-visible surface for the entire passkey-provider feature (12-UI-SPEC.md's explicit non-goal: no in-page indicator this phase). Getting the "pending ceremony always wins focus" and "dismissal = decline" mechanics right is what keeps PROV-03's "never dead-end the ceremony" promise on the human side of the flow.
Output: `ProviderCeremonyView.tsx`, extended i18n dictionary, `App.tsx` ViewState takeover wiring.

**Cross-phase dependency note (resolved 2026-07-16, freshness audit):** Phases 9–11 are built. The popup shell has NO router and NO hash routing — views live FLAT in `extension/entrypoints/popup/` (UnlockView.tsx, ItemListView.tsx, ItemDetailView.tsx, ServerConfigView.tsx, ...) and routing is a `ViewState` discriminated union in `App.tsx` (init at ~App.tsx:39-72). The extension-local i18n module EXISTS at `extension/lib/i18n/dictionary.ts` (`resolveLocale()` + `t(locale, key)`) — add `provider.*` keys there; do NOT duplicate a new i18n module from web. Popup width is `w-[380px]` (D-14; only the item-list view pins `h-[600px]`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-passkey-provider/12-CONTEXT.md
@.planning/phases/12-passkey-provider/12-UI-SPEC.md
@.planning/phases/12-passkey-provider/12-PATTERNS.md
@extension/entrypoints/popup/App.tsx
@extension/lib/i18n/dictionary.ts

# Real popup-shell / width / i18n conventions from phases 9-11:
@.planning/phases/09-session-unlock-core-popup-sync-client/09-06-SUMMARY.md
@.planning/phases/10-autofill-login-totp-card-identity/10-06-SUMMARY.md
@.planning/phases/11-generate-capture/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProviderCeremonyView — core layout, states, single/multi-match</name>
  <files>extension/entrypoints/popup/ProviderCeremonyView.tsx, extension/entrypoints/popup/ProviderCeremonyView.test.tsx</files>
  <read_first>.planning/phases/12-passkey-provider/12-UI-SPEC.md (Spacing Scale, Color, Multi-match credential list, Busy and error states sections)</read_first>
  <behavior>
    - Test: ceremony kind "create", single account — renders title=`provider.createTitle`, body=`provider.createBody` interpolated with `{site}`, primary CTA=`provider.createCta` (enabled, `btn btn-accent`, `KeyRound` icon), ghost fallback=`provider.useOther` (always visible/enabled, `btn btn-ghost`, no icon, no teal).
    - Test: ceremony kind "get", exactly 1 match — renders `provider.signinBodySingle` (no picker list rendered at all), CTA enabled immediately with that credential pre-selected.
    - Test: ceremony kind "get", 3 matches — renders exactly 3 credential-choice rows (56px height, 32px `KeyRound` icon slot + username Label text + radio-style selection affordance), CTA disabled until a row is selected, then enabled once one is.
    - Test: busy state — CTA becomes disabled, label swaps to `provider.createBusy`/`provider.signinBusy`, leading icon becomes `Fingerprint` + `Loader2` with `animate-spin`.
    - Test: genuine failure — `provider.failed` renders as a plain `text-sm text-error` line directly below the button (no icon, no banner), CTA returns to enabled/idle.
    - Test: no coral (`btn-accent` or any coral/`primary` DaisyUI class) appears anywhere on the ghost fallback button or elsewhere in this view; no favicon `<img>`; no empty-state screen rendered for a zero-match scenario (this view is never even mounted in that case per Plan 12-02's silent-fallthrough decision — assert the component doesn't render a "no matches" variant at all, i.e. no such prop/state exists).
  </behavior>
  <action>
    Build per 12-UI-SPEC.md exactly: DaisyUI `btn btn-accent` full-width primary CTA with `KeyRound` icon (teal `#00CDB7`), `btn btn-ghost` for `provider.useOther` (no icon, no fill color), hostname header as plain Label text next to a neutral `Globe` icon (no favicon fetch), 56px credential-choice rows only when there is more than one match (single match: no list, pre-selected, `provider.signinBodySingle` copy), `Fingerprint`+`Loader2 animate-spin` busy composition matching the existing icon-offset grammar of `extension/entrypoints/popup/EnrollExtPasskeyPrompt.tsx` (Phase 9's extension-ported equivalent of `EnrollPasskeyDialog.tsx`/`PasskeyUnlockButton` — verify exact class names there), plain `text-sm text-error` failure line, no destructive-action styling, no coral anywhere in this view (12-UI-SPEC.md Color section: "Not used anywhere in the ceremony view").
  </action>
  <verify>
    <automated>npm --prefix extension test -- --run ProviderCeremonyView.test.tsx</automated>
  </verify>
  <done>All behavior-listed tests pass; visual spot-check confirms the `w-[380px]` popup canvas (D-14 — 380, not the UI-SPEC's assumed 360; natural height, not pinned), 24px outer padding, 56px credential rows per 12-UI-SPEC.md's Spacing Scale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PRF notes, dismissal-as-decline, i18n entries</name>
  <files>extension/entrypoints/popup/ProviderCeremonyView.tsx, extension/lib/i18n/dictionary.ts</files>
  <read_first>.planning/phases/12-passkey-provider/12-UI-SPEC.md (Copywriting Contract table, "PRF handling in this screen" Phase-Specific Note, "Popup dismissal = implicit decline" Phase-Specific Note)</read_first>
  <behavior>
    - Test matrix: {create, get} x {RP request included `prf` extension: yes/no} x {background-reported capability signal: capable/unavailable} — a note (`provider.prfCapableNote` on create + prf-requested + capable, `provider.prfUnavailableNote` on prf-requested + capability signal says unavailable) renders ONLY when the RP's own request included `prf`; when the RP's request did NOT include `prf`, neither note renders, regardless of ceremony kind or capability.
    - Test (D-16): the note choice is driven purely by the capability prop background passes (sourced from the passkey-rs ceremony's `clientExtensionResults.prf.enabled` / HmacSecretConfig support) — assert the component has NO browser/user-agent detection (no `navigator.userAgent`, no Firefox check) and renders identically for the same props on any browser.
    - Test: simulated popup/window close while a ceremony is pending (mock the close/unmount lifecycle hook) sends an explicit decline message to background before/as the popup closes.
  </behavior>
  <action>
    Add every `provider.*` Copywriting Contract row (12-UI-SPEC.md's full table) verbatim in PL and EN to `extension/lib/i18n/dictionary.ts` — the existing extension i18n module (`resolveLocale()` + `t(locale, key)`); do NOT create a parallel i18n module. Render `provider.prfCapableNote` only when ceremony kind is "create" AND the RP's request included the `prf` extension AND background reports the ceremony as PRF-capable; render `provider.prfUnavailableNote` when the RP requested `prf` but background's capability signal says it can't be provided; render neither when the RP's request didn't include `prf` at all. The capability signal is REAL (D-16): it originates from the passkey-rs soft-authenticator ceremony in background (`clientExtensionResults.prf.enabled` / missing HmacSecretConfig support) — the extension IS the authenticator in provider role, so provider-PRF works identically on Chrome and Firefox and this view must never sniff the browser to decide the note. This is scoped to the RP's OWN PRF request, never Phase 3/4's vault-unlock PRF feature (12-UI-SPEC.md Review Note #5 — do not conflate the two; that vault-unlock PRF stays browser-limited and untouched, and this view's copy must not imply anything about the user's own vault-unlock capability). Add a dismissal handler (verify the correct WXT/popup-lifecycle hook — `window.onbeforeunload`, `chrome.windows.onRemoved`, or component-unmount-while-pending, whichever the actual popup shell exposes) that sends an explicit decline message to background as the popup closes, so background's fallthrough logic (Plan 12-02) always receives a terminal signal instead of inferring "popup just closed" ambiguously (D-11).
  </action>
  <verify>
    <automated>npm --prefix extension test -- --run ProviderCeremonyView.test.tsx</automated>
  </verify>
  <done>PRF-note visibility matrix passes (every no-prf-request combination renders no note; note choice among the prf-requested combinations follows the background capability signal, never a browser check); dismissal reliably sends an explicit decline message; i18n entries match the Copywriting Contract table exactly in both locales (spot-check `provider.createTitle`, `provider.prfUnavailableNote`, `provider.useOther`).</done>
</task>

<task type="auto">
  <name>Task 3: App.tsx ViewState takeover-mount on background-signaled ceremony</name>
  <files>extension/entrypoints/popup/App.tsx, extension/entrypoints/popup/App.test.tsx</files>
  <read_first>.planning/phases/12-passkey-provider/12-UI-SPEC.md (Mechanism steps under "Where the ceremony consent UI lives"); extension/entrypoints/popup/App.tsx (ViewState discriminated union + init flow at ~lines 39-72)</read_first>
  <action>
    The popup has NO router and NO hash routing — routing is the `ViewState` discriminated union in `App.tsx`. Extend `ViewState` with a `{ kind: "provider-ceremony"; ... }` variant carrying the pending-ceremony payload/reference. In App.tsx's init (~lines 39-72), read the pending-ceremony flag from `chrome.storage.session` (the signal Plan 12-02's background writes) and, if present, set the view to `{ kind: "provider-ceremony", ... }` immediately, replacing whatever view the user was on (list/search/settings) — per 12-UI-SPEC.md's "a pending ceremony always wins focus" rule. When the ceremony resolves (confirm, decline, or error), return to the prior view if the popup was already open before the ceremony began, or close the window if it was opened solely for this ceremony (the `browser.windows.create` path from Plan 12-02 — that window is created at width 380 to match the `w-[380px]` popup canvas; the ceremony view keeps natural height, not pinned). Tests go in `App.test.tsx` (or `ProviderCeremonyView.test.tsx` where component-local) — there is no popup-router.test.tsx.
  </action>
  <verify>
    <automated>npm --prefix extension test -- --run App.test.tsx</automated>
  </verify>
  <done>Pending-ceremony `chrome.storage.session` flag read in App.tsx init forces the `provider-ceremony` ViewState on mount, overriding any other initial view; resolving the ceremony returns to the prior view or closes the window per the two-path rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party page <-> popup (browser-chrome-owned surface) | The popup is intentionally the ONLY place this consent UI renders, since a page cannot draw over or read from browser chrome (unlike an in-page overlay) |
| popup <-> background | Confirm/decline/selection messages must reach background reliably, including on ambiguous dismissal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-14 | Spoofing | third-party page attempting to fake a "Passkey Vault" consent UI in-page to phish confirm clicks | high | mitigate | Structural: the ceremony consent screen never renders in-page, only in the browser-chrome-owned popup a page cannot draw over or read from (12-UI-SPEC.md Review Note #1) — no additional code-level mitigation needed in this plan since the architecture already prevents the vector |
| T-12-15 | Information Disclosure | ProviderCeremonyView rendering | critical | mitigate | 12-UI-SPEC.md's zero-knowledge note: no `ui-monospace`, no raw-value display anywhere in this screen — enforced by this plan's task-level review, no secret-shaped prop is ever passed to this component |
| T-12-16 | Denial of Service (of ceremony) | user closes popup without an explicit decision | medium | mitigate | Dismissal-as-decline wiring (Task 2) — page's promise always resolves via an explicit fallthrough signal, never hangs |

</threat_model>

<verification>
- [ ] `npm --prefix extension test -- --run ProviderCeremonyView.test.tsx` passes
- [ ] `npm --prefix extension test -- --run App.test.tsx` passes (provider-ceremony ViewState takeover)
- [ ] No browser/user-agent sniffing anywhere in the PRF-note logic (D-16 — capability signal only)
- [ ] Visual spot-check against 12-UI-SPEC.md's Spacing/Color/Typography sections (canvas `w-[380px]` per D-14, not 360)
</verification>

<success_criteria>
- All tasks completed
- No coral anywhere in this view; no favicon fetch; no empty-state screen for zero-match
- PRF notes scoped to the RP's own prf request and driven by the real passkey-rs capability signal (D-16, never browser detection); dismissal always signals decline
- Takeover implemented as a `ViewState` variant in App.tsx + `chrome.storage.session` flag — no router file, no hash routes introduced
</success_criteria>

<output>
Create `.planning/phases/12-passkey-provider/12-04-SUMMARY.md` when done.
</output>
