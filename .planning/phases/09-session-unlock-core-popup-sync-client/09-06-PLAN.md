---
phase: 09-session-unlock-core-popup-sync-client
plan: 06
type: execute
wave: 4
depends_on: ["09-04", "09-05"]
files_modified:
  - extension/package.json
  - extension/wxt.config.ts
  - extension/vitest.config.ts
  - extension/lib/i18n/dictionary.ts
  - extension/lib/messaging/ext-protocol.ts
  - extension/entrypoints/background/router.ts
  - extension/entrypoints/popup/main.tsx
  - extension/entrypoints/popup/App.tsx
  - extension/entrypoints/popup/ServerConfigView.tsx
  - extension/entrypoints/popup/UnlockView.tsx
  - extension/entrypoints/popup/ItemListView.tsx
  - extension/entrypoints/popup/ItemDetailView.tsx
  - extension/entrypoints/popup/App.test.tsx
  - extension/entrypoints/popup/UnlockView.test.tsx
  - extension/entrypoints/popup/ItemListView.test.tsx
autonomous: true
requirements: [EXT-02, EXT-03, EXT-04, EXT-06]

must_haves:
  truths:
    - "The popup renders exactly one of: loading, server-config-needed, sign-in/unlock, item-list+search, item-detail — per 09-UI-SPEC.md's single-view state machine, never two at once"
    - "The popup is built with React via @wxt-dev/module-react, reusing the web app's exact Tailwind v4 + DaisyUI 5 (vault-dark/vault-light) theme and lucide-react — added to extension/ THIS phase because Phase 8 scaffolded a vanilla-TS popup, not the React shell D-11 anticipated; pv-wasm is still reused unchanged and stays background-only (D-05/D-11)"
    - "The popup never imports pv-wasm/pv-core or a wasm-loader module — every crypto/decrypt/unlock action is a sendMessage call to the background (D-05), verified by a standing grep gate"
    - "A 'fullscreen / open full vault' control opens the configured server's web app in a new browser tab, deriving the URL solely from server-config.ts's stored value via a config.get message — never a hard-coded origin (EXT-06)"
  artifacts:
    - path: "extension/entrypoints/popup/App.tsx"
      provides: "top-level view-state switch (loading | server-config | unlock | list | detail)"
      contains: "session.status"
    - path: "extension/entrypoints/popup/UnlockView.tsx"
      provides: "password form + PRF button, thin message-dispatch layer only — Sign-in variant (auth.signIn.*, mints a token) and Unlock-only variant (unlock.*, existing token)"
      contains: "auth.signIn.password"
    - path: "extension/entrypoints/popup/ItemListView.tsx"
      provides: "search bar + item rows + auto-lock footer select + open-full-vault control"
      contains: "searchItems"
  key_links:
    - from: "extension/entrypoints/popup/UnlockView.tsx"
      to: "extension/entrypoints/background/unlock.ts (Plan 09-04)"
      via: "Sign-in variant: sendMessage({kind:'auth.signIn.password'|'auth.signIn.prf.start'|'auth.signIn.prf.finish'}); Unlock-only variant: sendMessage({kind:'unlock.password'|'unlock.prf.start'|'unlock.prf.finish'}) — PRF's navigator.credentials.get() runs here, in the popup's DOM context, for both variants"
      pattern: "auth.signIn.password"
    - from: "extension/entrypoints/popup/ItemListView.tsx"
      to: "extension/entrypoints/background/vault-store.ts (Plan 09-05)"
      via: "sendMessage({kind:'vault.list'}) on mount and on a 'vault.updated' push broadcast"
      pattern: "vault.list"
    - from: "extension/entrypoints/popup/ServerConfigView.tsx"
      to: "extension/entrypoints/background/server-config.ts (Plan 09-03)"
      via: "sendMessage({kind:'config.get'|'config.set'}) — router.ts wires these to server-config.ts's readServerConfig/configureServer"
      pattern: "config.set"
---

<objective>
Build the popup — the extension's first real user-facing surface: first-run server configuration, unlock (password + PRF), browse/search/pick vault items, minimal item detail, the auto-lock control, and the "open full vault" action — entirely as a thin message-dispatch layer over Plans 09-02/09-03/09-04/09-05's background subsystems, per 09-UI-SPEC.md's binding design contract.

Purpose: This is where EXT-02/03/04/06 become an actual usable feature rather than four independent background subsystems. 09-UI-SPEC.md is the authoritative, binding UI contract for every view built here — tokens, spacing, copy (PL+EN), and component vocabulary are not renegotiated in this plan, only implemented.
Output: A React+DaisyUI popup (`@wxt-dev/module-react` added since Phase 8's actual popup is vanilla TS, confirmed by 08-03-PLAN.md — RESEARCH.md's Assumption A2 "no framework" branch holds), reusing `web/`'s exact Tailwind v4 + DaisyUI 5 theme and `lucide-react` icon set, wired end-to-end to the background via `ext-protocol.ts`'s typed message contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-CONTEXT.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-UI-SPEC.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-PATTERNS.md

# Prior-plan output this plan directly consumes:
@.planning/phases/09-session-unlock-core-popup-sync-client/09-02-SUMMARY.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-03-SUMMARY.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-04-SUMMARY.md
@.planning/phases/09-session-unlock-core-popup-sync-client/09-05-SUMMARY.md

# Phase 8's actual popup scaffold — read before touching anything under
# entrypoints/popup/: Phase 8's popup (08-03-PLAN.md) is a vanilla
# index.html + main.ts debug harness, explicitly throwaway. This plan
# REPLACES it with the real popup, not extends it — confirm the exact
# current files before deleting/rewriting.
@.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-03-PLAN.md

# v0.1 patterns this plan ports into React components:
@web/src/components/auth/UnlockOverlay.tsx
@web/src/components/auth/PasskeyUnlockButton.tsx
@web/src/components/vault/ItemList.tsx
@web/src/components/vault/DetailPanel.tsx
@web/src/lib/i18n/dictionary.ts
@web/package.json
</context>

<interfaces>
<!-- Message contract this plan's components dispatch through exclusively -->

From extension/lib/messaging/ext-protocol.ts (Plan 09-02, extended by 09-04/09-05, extended again here):
```typescript
export async function sendMessage<K extends Message["kind"]>(message: MessageOf<K>): Promise<MessageResponseMap[K]>;
// existing kinds: session.status, session.setAutoLockMinutes,
// unlock.password, unlock.prf.start, unlock.prf.finish (UNLOCK-ONLY —
// requires an existing token; UnlockView's Unlock-only variant dispatches
// these, per session.status returning "locked"),
// auth.signIn.password, auth.signIn.prf.start, auth.signIn.prf.finish
// (SIGN-IN — mints a token, no prior session required; UnlockView's
// Sign-in variant dispatches these, per session.status returning
// "no-session" — see Plan 09-04's Blocker-1 fix, which added this pair),
// vault.list, vault.updated (broadcast)
// NEW this plan: config.get, config.set
```

From extension/lib/passkeys/prf.ts (Plan 09-04, pure, safe for popup import):
```typescript
export function buildPrfExtensions(prfSalts: Record<string, string>): { prf: { evalByCredential: Record<string, { first: BufferSource }> } };
export function extractPrfBytes(assertion: PublicKeyCredential): ArrayBuffer | undefined;
export function stripPrfFromCredentialJson(assertion: PublicKeyCredential): unknown;
```

From extension/lib/vault/search.ts (Plan 09-05, pure):
```typescript
export function searchItems(items: VaultItem[], query: string): VaultItem[];
export function filterItems(items: VaultItem[], filter: VaultFilter): VaultItem[];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: React/DaisyUI scaffold, i18n dictionary, config.get/config.set wiring</name>
  <files>extension/package.json, extension/wxt.config.ts, extension/vitest.config.ts, extension/lib/i18n/dictionary.ts, extension/lib/messaging/ext-protocol.ts, extension/entrypoints/background/router.ts</files>
  <read_first>web/package.json, .planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-03-PLAN.md, extension/entrypoints/background/server-config.ts, extension/entrypoints/background/router.ts</read_first>
  <action>
  Add `@wxt-dev/module-react` (npm registry-verified `1.2.2` per 09-RESEARCH.md's Package Legitimacy Audit — already OK-verdict, no checkpoint needed) plus `react@19.2.7`/`react-dom@19.2.7`/`daisyui@5.6.18`/`lucide-react@1.24.0`/`tailwindcss@4.3.2`/`@tailwindcss/postcss@4.3.2` to `extension/package.json`, matching `web/package.json`'s exact pinned versions (09-UI-SPEC.md: "reused unchanged, not a parallel theme"). Add `@vitejs/plugin-react`/`jsdom` as devDependencies, matching `web/package.json`'s pins.

  Edit `extension/wxt.config.ts` to add `modules: ["@wxt-dev/module-react"]` (additive to existing manifest/CSP/gecko.id/optional_host_permissions fields from Phase 8 + Plan 09-03 — do not remove or restructure those).

  Edit `extension/vitest.config.ts` (Phase 8's existing file — extend, do not recreate, per 09-VALIDATION.md's Wave 0 correction) to add `environment: "jsdom"` for popup component tests and the `@vitejs/plugin-react` Vite plugin — confirm whether Plan 09-02's `environment: "node"` setting needs to become per-test-file-scoped (Vitest's `// @vitest-environment jsdom` docblock override) rather than a single global environment, since background tests (Plans 09-02/09-04/09-05) need `"node"` and popup tests need `"jsdom"` in the same project — check current Vitest docs for the supported per-file override syntax at execution time (Context7/vitest.dev) rather than assuming.

  Create `extension/lib/i18n/dictionary.ts`, following `web/src/lib/i18n/dictionary.ts`'s exact `DICTIONARY`/`interpolate()` structural pattern (same `Locale = "pl" | "en"` type, same `{pl, en}` entry shape). Populate ONLY the keys 09-UI-SPEC.md's Copywriting Contract table lists for this phase (unlock.submit, unlock.passkeyCta, unlock.passkeyLoginCta, the PRF-unavailable tiers, empty-state/error/loading copy, auto-lock label, session-locked notice, plus new `config.*`/`vault.openFullVault` keys this task adds for the server-config screen and the "open full vault" button — author PL+EN pairs matching the design contract's plain, founder-voice-free security-surface tone) — reuse the verbatim strings 09-UI-SPEC.md marks "reused verbatim from 04-UI-SPEC.md/13-UI-SPEC.md" exactly, do not paraphrase.

  Extend `ext-protocol.ts`'s `Message`/`MessageResponseMap` with `"config.get"` (returns `{ baseUrl: string } | null`) and `"config.set"` (payload `{ rawUrl: string }`, returns `{ ok: true } | { ok: false; error: "invalid-url" | "unreachable" }`). Edit `router.ts` to add both cases, delegating directly to `server-config.ts`'s `readServerConfig`/`configureServer` (Plan 09-03) — catch `InvalidServerUrlError`/`ServerUnreachableError` and map to the typed error response rather than letting them reject the message channel.
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - extension/package.json contains "@wxt-dev/module-react", "react", "daisyui", "lucide-react"
    - extension/lib/i18n/dictionary.ts exports "DICTIONARY" with at least "unlock.submit" and "unlock.passkeyCta" keys
    - extension/entrypoints/background/router.ts contains "config.get" and "config.set"
    - full extension/ test suite still passes (background tests unaffected by the jsdom addition)
  </acceptance_criteria>
  <done>extension/ has a working React+DaisyUI+Tailwind toolchain matching web/'s exact pins, an i18n dictionary covering this phase's copy, and config.get/config.set wired into the router ahead of any popup component needing them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App.tsx shell, ServerConfigView, UnlockView</name>
  <files>extension/entrypoints/popup/main.tsx, extension/entrypoints/popup/App.tsx, extension/entrypoints/popup/ServerConfigView.tsx, extension/entrypoints/popup/UnlockView.tsx, extension/entrypoints/popup/App.test.tsx, extension/entrypoints/popup/UnlockView.test.tsx</files>
  <read_first>web/src/components/auth/LoginForm.tsx, web/src/components/auth/UnlockOverlay.tsx, web/src/components/auth/PasskeyUnlockButton.tsx, .planning/phases/09-session-unlock-core-popup-sync-client/09-UI-SPEC.md (Popup shell, Unlock/Sign-in view sections)</read_first>
  <behavior>
    - Test 1 (App.tsx): a mocked `sendMessage({kind:"config.get"})` resolving `null` renders `ServerConfigView` (first-run gate takes priority over everything else — no unlock UI can render without a configured server).
    - Test 2 (App.tsx): config resolved + mocked `sendMessage({kind:"session.status"})` resolving `{kind:"no-session"}` or `{kind:"locked",...}` renders `UnlockView`; resolving `{kind:"unlocked",...}` renders the item-list view (Task 3).
    - Test 3 (UnlockView.tsx, Unlock-only variant — `session.status` returned `locked`): submitting the password form calls `sendMessage({kind:"unlock.password", passwordBytes})` exactly once (no `email` field rendered in this variant), and clears the password field's local state after either outcome (never leaves plaintext sitting in a stale controlled-input across a re-render past submission).
    - Test 3b (UnlockView.tsx, Sign-in variant — `session.status` returned `no-session`): the form renders an email field alongside the password field; submitting calls `sendMessage({kind:"auth.signIn.password", email, passwordBytes})` exactly once — NOT `unlock.password` (that kind would fail server-side with no token to authorize against); same password-field-clearing discipline as Test 3.
    - Test 4 (UnlockView.tsx, Unlock-only variant): clicking the PRF button, with `window.PublicKeyCredential` mocked present — calls `sendMessage({kind:"unlock.prf.start"})`, then (given a mocked resolved challenge) calls a mocked `navigator.credentials.get()`, then `sendMessage({kind:"unlock.prf.finish", ...})` with `stripPrfFromCredentialJson`'d JSON and extracted PRF bytes — confirms the ceremony sequence without any of these three steps touching pv-wasm/wasm-loader (grep-checked in acceptance criteria).
    - Test 4b (UnlockView.tsx, Sign-in variant): the SAME PRF ceremony sequence, but dispatches `sendMessage({kind:"auth.signIn.prf.start", email})` then `sendMessage({kind:"auth.signIn.prf.finish", stateId, email, credentialJson, prfBytes})` — the "Log in and unlock with passkey" gesture (09-UI-SPEC.md's `unlock.passkeyLoginCta`) is this variant's PRF button, never `unlock.prf.*`.
    - Test 5 (UnlockView.tsx): with `window.PublicKeyCredential === undefined`, the PRF button does not render at all in EITHER variant — a Tier-1 explainer line renders in its place (09-UI-SPEC.md's absent-vs-disabled rule).
  </behavior>
  <action>
  Create `extension/entrypoints/popup/main.tsx` — the `@wxt-dev/module-react` entrypoint, mounting `App` into the popup's root (WXT's generated `index.html`, confirm exact structure against the module's own scaffolding conventions at execution time).

  Create `extension/entrypoints/popup/App.tsx` implementing 09-UI-SPEC.md's exact state machine: `loading` (initial, spinner + "Ładowanie sejfu…"/"Loading your vault…") → `config.get` resolves `null` → `ServerConfigView` (first-run) → `config.get` resolves a value → `session.status` message → `no-session`/`locked` → `UnlockView` → `unlocked` → item-list view (Task 3). Exactly one view renders at a time, per the UI-SPEC's "popup is single-view, no tabs" rule.

  Create `extension/entrypoints/popup/ServerConfigView.tsx`: an `input input-bordered` for the server URL, a coral `btn btn-primary` submit calling `sendMessage({kind:"config.set", rawUrl})`, a `loading loading-spinner` while the message is in flight, an `alert alert-error` rendering the typed error (`invalid-url` vs. `unreachable`, distinct copy per 09-UI-SPEC.md's vocabulary — author these two lines in `dictionary.ts` if not already covered by Task 1's copy pass). On success, transitions `App.tsx`'s state to re-check `session.status`. This view is NOT named in 09-UI-SPEC.md (that document predates EXT-05) — build it strictly within the document's existing token/component vocabulary (`input input-bordered`, `btn btn-primary`, `alert alert-error`, `loading loading-spinner` — the exact classes 09-UI-SPEC.md's Design System table already licenses for this phase) and flag it in the SUMMARY for UI-checker review, per the orchestrator's explicit instruction.

  Create `extension/entrypoints/popup/UnlockView.tsx`, adapting `LoginForm.tsx` (Sign-in variant) + `UnlockOverlay.tsx` + `PasskeyUnlockButton.tsx`'s exact layout (password field + PRF button + divider, per 09-UI-SPEC.md's Unlock/Sign-in view section) into the popup's narrower viewport. Every crypto-adjacent action becomes a `sendMessage` call — this component NEVER imports `@/lib/crypto`, `pv-wasm`, or a wasm-loader module (D-05), and the ONE crypto-shaped thing it DOES do — call `navigator.credentials.get()` — passes its result through `extension/lib/passkeys/prf.ts`'s pure `stripPrfFromCredentialJson`/`extractPrfBytes` (Plan 09-04) before ever calling `sendMessage`. Implement both variants, switching on whether `session.status` returned `no-session` (Sign-in) vs. `locked` (Unlock-only) — this determines BOTH which fields render AND which message kinds are dispatched (Plan 09-04's Blocker-1 fix added the `auth.signIn.*` pair specifically for this variant; do not dispatch `unlock.*` from the Sign-in variant or vice versa, per Tests 3/3b/4/4b):
  - **Sign-in variant** (`no-session`): email + password fields, PRF-login-CTA copy (`unlock.passkeyLoginCta`, "Log in and unlock with passkey"). Password submit → `sendMessage({kind:"auth.signIn.password", email, passwordBytes})`. PRF click → `sendMessage({kind:"auth.signIn.prf.start", email})` → ceremony → `sendMessage({kind:"auth.signIn.prf.finish", stateId, email, credentialJson, prfBytes})`. On a `{prfUnavailable:true}` response, per Plan 09-04's documented scope decision (no partial session persisted), fall back to showing/focusing the password field — do NOT assume a token now exists.
  - **Unlock-only variant** (`locked`): password field only (no email — the account is already known), PRF-unlock-CTA copy (`unlock.passkeyCta`), session-locked notice line when `wasAutoLocked` is `true` (from `session.status`'s response). Password submit → `sendMessage({kind:"unlock.password", passwordBytes})` (no `email` argument — router.ts routes this to the SAME `handleUnlockPassword` function, unlock-only branch). PRF click → `sendMessage({kind:"unlock.prf.start"})` → ceremony → `sendMessage({kind:"unlock.prf.finish", ...})`.

  RED: write `App.test.tsx` (2 cases) and `UnlockView.test.tsx` (5 cases: 3, 3b, 4, 4b, 5) first, using `@testing-library/react` + jsdom, mocking `ext-protocol.ts`'s `sendMessage` and `navigator.credentials.get`. GREEN: implement.
  </action>
  <verify>
    <automated>cd extension && npx vitest run App.test.tsx UnlockView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - extension/entrypoints/popup/UnlockView.tsx does NOT import from "pv-wasm", "wasm-loader", or any "@/lib/crypto"-equivalent path
    - extension/entrypoints/popup/App.tsx renders ServerConfigView before ever calling session.status when config.get resolves null
    - extension/entrypoints/popup/UnlockView.tsx contains "auth.signIn.password", "auth.signIn.prf.start", "auth.signIn.prf.finish" (Sign-in variant) AND "unlock.password", "unlock.prf.start", "unlock.prf.finish" (Unlock-only variant) — both message families dispatched, not just one
    - all 7 behaviors pass (App.tsx Tests 1-2, UnlockView.tsx Tests 3/3b/4/4b/5)
  </acceptance_criteria>
  <done>App.tsx correctly gates through server-config → unlock → unlocked in that order; UnlockView.tsx implements both the Sign-in variant (auth.signIn.* — mints a token for a fresh install) and the Unlock-only variant (unlock.* — existing token) as a pure message-dispatch layer, with the PRF ceremony's DOM-bound navigator.credentials.get() call living here and nowhere else.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ItemListView, ItemDetailView, auto-lock footer, "open full vault"</name>
  <files>extension/entrypoints/popup/ItemListView.tsx, extension/entrypoints/popup/ItemDetailView.tsx, extension/entrypoints/popup/ItemListView.test.tsx</files>
  <read_first>web/src/components/vault/ItemList.tsx, web/src/components/vault/DetailPanel.tsx, .planning/phases/09-session-unlock-core-popup-sync-client/09-UI-SPEC.md (Item List + Search, Item Detail, Auto-lock control sections)</read_first>
  <behavior>
    - Test 1: on mount, `ItemListView` calls `sendMessage({kind:"vault.list"})` once and renders the returned items through `filterItems`/`searchItems` (Plan 09-05's `search.ts`) as the search input changes — no additional `sendMessage` call per keystroke (client-side filter, per 09-UI-SPEC.md's "no debounce, no network-call-per-keystroke" rule).
    - Test 2: a `runtime.onMessage`/broadcast listener receiving `{kind:"vault.updated"}` (Plan 09-05's lock-driven broadcast) triggers a fresh `vault.list` re-fetch and re-renders in place — no toast, matching 09-UI-SPEC.md's "sync-driven updates re-render in place" rule.
    - Test 3: zero items in the vault renders the "vault empty so far" copy; a non-empty search with zero matches renders the reused `Brak wyników dla „{query}"` line — distinct empty states, per 09-UI-SPEC.md.
    - Test 4: the auto-lock `select select-bordered select-sm` control's `onChange` calls `sendMessage({kind:"session.setAutoLockMinutes", minutes})` immediately (no confirm dialog, per the design contract).
    - Test 5: the "open full vault" control calls `sendMessage({kind:"config.get"})` then `browser.tabs.create({url: baseUrl})` — verify it is NEVER called with a hard-coded string literal (mock `browser.tabs.create` and assert the URL argument came from the mocked `config.get` response, not a constant in the component).
  </behavior>
  <action>
  Create `extension/entrypoints/popup/ItemListView.tsx` per 09-UI-SPEC.md's "Item List + Search" section: sticky `input input-bordered` search bar with a leading `Search` icon (`aria-hidden`), 48px-min-height item rows (item-type icon — `KeyRound` teal/muted, `CreditCard`, `IdCard`, `StickyNote` — + item name (Body) + username/subtext (Label, muted)), tapping a row transitions to `ItemDetailView`. Below the list, a footer row holding the auto-lock `select` (options `[5,15,30,60]`, matching `extension/entrypoints/background/autolock.ts`'s `AUTOLOCK_OPTIONS`, Plan 09-02) and the "open full vault" control (`ExternalLink` lucide icon — chosen per the orchestrator's instruction since 09-UI-SPEC.md's icon list doesn't name one; justified as the standard "leaves this surface" glyph, carrying no new semantic weight beyond "opens elsewhere," flag in the SUMMARY for UI-checker review). Register a `browser.runtime.onMessage` listener for the `"vault.updated"` broadcast (Plan 09-05) that re-triggers the `vault.list` fetch.

  Create `extension/entrypoints/popup/ItemDetailView.tsx` per 09-UI-SPEC.md's "Item Detail view" section: `ChevronLeft` back button (40px hit area, `aria-label` per the accessibility contract), item name (Heading) + type badge, field rows (label + value, `font-mono` for password/TOTP/card-number-shaped values, `Copy`/`Check` flash-confirm icon buttons, `Eye`/`EyeOff` reveal toggle defaulting masked for password fields) — sourced from the SAME item object already present in `ItemListView`'s fetched array (no separate `vault.getItem` message needed — the popup already holds the full decrypted list client-side once unlocked). No edit/delete/deep-link controls (CONTEXT.md's locked out-of-scope boundary, reaffirmed by 09-UI-SPEC.md).

  BINDING (Bartek, 2026-07-15 — resolves UI-SPEC Review Question 4 as an OVERRIDE of the earlier discretionary wording): for passkey-type items, `ItemDetailView` MUST render read-only "RP ID" and "last used" field rows. When the synced item payload carries these fields, show the values (last-used formatted per the web app's date convention); when absent, render a muted "—" placeholder row — never silently omit the rows. This is a guaranteed deliverable of this plan, and the surface must be ready for Phase 12's provider, which starts writing these fields.

  RED: write `ItemListView.test.tsx` for the 5 behaviors above (mocking `sendMessage`, `browser.runtime.onMessage`, `browser.tabs.create`). GREEN: implement both components.
  </action>
  <verify>
    <automated>cd extension && npx vitest run ItemListView.test.tsx && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - extension/entrypoints/popup/ItemListView.tsx contains "searchItems" and "filterItems"
    - extension/entrypoints/popup/ItemListView.tsx contains "tabs.create" and does NOT contain a literal "http://" or "https://" string (the URL must come from config.get's response, not a constant — enforces EXT-06's "solely derived from the configured server URL" requirement)
    - `grep -rlnE "https?://" extension/entrypoints/popup/App.tsx extension/entrypoints/popup/ServerConfigView.tsx extension/entrypoints/popup/UnlockView.tsx extension/entrypoints/popup/ItemListView.tsx extension/entrypoints/popup/ItemDetailView.tsx` returns nothing — the no-hard-coded-URL invariant (EXT-06) applies to EVERY popup component this plan creates, not just `ItemListView.tsx`; a URL literal in any of them (e.g. an accidental fallback in `ServerConfigView.tsx`'s placeholder text, or a stray dev-time constant in `App.tsx`) is just as much a violation
    - Plan 09-03's Task 2 invariant test (`no_other_extension_file_hard_codes_a_server_url`, widened to `*.tsx` per that plan's own revision) must also pass at this point — run `cd extension && npx vitest run server-config.test.ts` as part of this task's full-suite verify, since it now covers the popup files this plan just created
    - all 5 ItemListView.test.tsx behaviors pass
    - full extension/ test suite + tsc pass
  </acceptance_criteria>
  <done>ItemListView renders the browse/search/pick surface with live sync-driven updates and the auto-lock/open-full-vault footer controls; ItemDetailView renders a minimal, picker-only detail pane with masked-by-default secrets — both strictly within 09-UI-SPEC.md's token vocabulary, with EXT-06's URL sourced exclusively from server-config.ts via config.get.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Popup (React, DOM-privileged) ↔ background (crypto-privileged) | Same trust tier this phase, but D-05's discipline is enforced here by construction (no crypto import) rather than by runtime sandboxing |
| `config.get`'s resolved URL → `browser.tabs.create` (EXT-06) | The final consumer of Plan 09-03's scheme-restricted, healthz-validated URL — this plan must not reintroduce a second, unvalidated path to a tab-opening call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-13 | Elevation of Privilege | Popup accidentally importing `pv-wasm`/crypto directly | high | mitigate | Grep-enforced in Task 2/3 acceptance criteria (`UnlockView.tsx` explicitly checked); D-05's single-choke-point discipline holds by construction, matching the same pattern Phase 8's debug popup already established |
| T-09-20 | Tampering | "Open full vault" button constructing its own URL instead of reading `config.get`'s response | medium | mitigate | Task 3's acceptance criteria greps for a hard-coded `http(s)://` literal in `ItemListView.tsx` and fails the build if found — the URL must originate from `server-config.ts` (Plan 09-03) exclusively, which already scheme-restricts and healthz-validates it |
| T-09-21 | Information Disclosure | Item Detail's masked password field defaulting to visible | medium | mitigate | `Eye`/`EyeOff` reveal toggle defaults to masked state on every mount — no persisted "last reveal state," matching the web app's existing item-detail security posture |

</threat_model>

<verification>
- [ ] `cd extension && npx tsc --noEmit` passes
- [ ] `cd extension && npx vitest run` passes (full suite, including background tests unaffected)
- [ ] `grep -rn "pv-wasm\|wasm-loader\|@/lib/crypto" extension/entrypoints/popup/` returns nothing
- [ ] `grep -rlnE "https?://" extension/entrypoints/popup/App.tsx extension/entrypoints/popup/ServerConfigView.tsx extension/entrypoints/popup/UnlockView.tsx extension/entrypoints/popup/ItemListView.tsx extension/entrypoints/popup/ItemDetailView.tsx` returns nothing (widened from `ItemListView.tsx`-only — every popup component is in scope for EXT-06's invariant)
- [ ] `cd extension && npx vitest run server-config.test.ts` passes (Plan 09-03's widened `*.tsx`-inclusive invariant test now also walks this plan's popup files)
</verification>

<success_criteria>
- All tasks completed, all automated verifications pass
- Popup implements 09-UI-SPEC.md's full state machine and every named view
- D-05 (no crypto in popup) holds by construction, grep-verified
- EXT-06's "open full vault" URL is provably sourced only from server-config.ts
</success_criteria>

<output>
Create `.planning/phases/09-session-unlock-core-popup-sync-client/09-06-SUMMARY.md` when done, following @$HOME/.claude/gsd-core/templates/summary.md exactly (including the `coverage:` block for EXT-02/EXT-03/EXT-04/EXT-06, and flagging ServerConfigView's icon/copy choices plus the ExternalLink icon choice for UI-checker review, per the orchestrator's explicit instruction).
</output>
