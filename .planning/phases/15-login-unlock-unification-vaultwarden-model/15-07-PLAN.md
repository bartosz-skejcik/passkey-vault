---
phase: 15-login-unlock-unification-vaultwarden-model
plan: 07
type: execute
wave: 3
depends_on: ["15-01", "15-03", "15-05"]
files_modified:
  - extension/e2e/dual-browser.spec.ts
  - extension/e2e/fixtures.ts
  - extension/e2e-firefox/run-core.cjs
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "The entire 21-SC Playwright suite's sign-in setup path (worker-scoped beforeAll + per-test ensureVaultReady recovery) establishes its session by driving the server-origin ceremony window, never a popup password/email field."
    - "P9-SC1/P9-SC2 assert the NEW popup layout (no email/password field in the no-session view; password-first + promoted passkey button in the locked view) instead of the retired dual-model."
    - "The Firefox manual harness's initial sign-in step (run-core.cjs) delegates to the same ceremony-window-driving technique run-server-unlock.cjs already proves on real Firefox."
  artifacts: []
  key_links: []
  prohibitions: []

specless_probe_note: |
  AUTH-01/02/03/04 all returned unclassified/unresolved from the edge-probe. must_haves.truths
  above are derived from RESEARCH.md's "e2e Impact" section (the single highest-impact e2e
  change identified this phase) and the phase's own sequencing guidance ("e2e rework is
  substantial ... plan it as first-class tasks, not an afterthought"), not from an edge-taxonomy
  classification.
---

<objective>
Rework the e2e blast radius RESEARCH.md flagged as this phase's single highest-impact change:
the ENTIRE 21-SC Playwright suite's setup path signs in via a popup password/email field that
no longer exists after Plan 15-03. Drive the new server-origin ceremony window instead, reusing
`run-server-unlock.cjs`'s already-proven window-handle-driving technique (Selenium) and the
equivalent Playwright idiom (`context.waitForEvent("page")`).

This plan runs in Wave 3, parallel with Plan 15-04 (no file overlap) — both depend on Plan
15-03's UI landing and Plan 15-01's proven ceremony-window password path; e2e correctness does
not depend on Plan 15-04's dead-code deletion.

Purpose: keep the e2e suites convertible and green through the AUTH refactor; consolidate the
phase's full closing gate (vitest, tsc, both builds, mainworld-boundary audit, chromium +
chromium-ceremony Playwright, real-Firefox core + server-unlock lanes, the AUTH-04 two-server
scenario) into one verification checkpoint.
Output: reworked `dual-browser.spec.ts`/`fixtures.ts`/`run-core.cjs`; a human-verified green
phase-close gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-login-unlock-unification-vaultwarden-model/15-RESEARCH.md
@extension/e2e/dual-browser.spec.ts
@extension/e2e/fixtures.ts
@extension/e2e-firefox/run-core.cjs
@extension/e2e-firefox/run-server-unlock.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright dual-browser.spec.ts / fixtures.ts rework</name>
  <files>extension/e2e/dual-browser.spec.ts, extension/e2e/fixtures.ts</files>
  <read_first>
    extension/e2e/dual-browser.spec.ts lines 1-260 (SERVER/EMAIL/PASSWORD constants; ensureServerConfigured/signInWithPassword/ensureVaultReady at lines 181-253)
    extension/e2e/dual-browser.spec.ts lines 426-511 (P9-SC1/P9-SC2 test bodies)
    extension/e2e-firefox/run-server-unlock.cjs lines 300-320, 440-450 (the proven getAllWindowHandles-before/after-click -> switchTo(newWindow) -> drive -> switchTo back pattern; Playwright's equivalent is `context.waitForEvent("page")`)
    15-01-SUMMARY.md and 15-03-SUMMARY.md (confirm the exact data-testid/id values landed: `[data-testid="server-ceremony-signin-button"]` in the popup, `input#pv-ext-unlock-email`/`input#pv-ext-unlock-password`/`[data-testid="ext-unlock-password-submit"]` in the ceremony window)
  </read_first>
  <action>
    Rewrite `signInWithPassword()` (dual-browser.spec.ts:189-195): click `popup.locator('[data-testid="server-ceremony-signin-button"]')`; await the new window via `const [ceremonyPage] = await Promise.all([popup.context().waitForEvent("page"), <the click above>])`; on `ceremonyPage`, fill `input#pv-ext-unlock-email` with `EMAIL` and `input#pv-ext-unlock-password` with `PASSWORD`; click `[data-testid="ext-unlock-password-submit"]`; wait for either the ceremony window to self-close (`await ceremonyPage.waitForEvent("close", {timeout: 15000}).catch(() => {})`) or the popup's own view to advance past the unlock screen (`await popup.waitForSelector('select, button:has-text("Create a passkey")', {timeout: 20000})` — narrow this selector since "Create a passkey" no longer exists post-Plan-15-04; use `await popup.waitForSelector('select', {timeout: 20000})` instead, matching the item-list view's own established selector).

    Rework `ensureVaultReady()` (dual-browser.spec.ts:211-253): replace the `popup.waitForSelector('input[type="password"], select')` + conditional `signInWithPassword()` call with a check against the NEW signed-out hero — `if (await popup.locator('[data-testid="server-ceremony-signin-button"]').count()) { await signInWithPassword(); }` (no-session case) else if `await popup.locator('input[type="password"]').count()` (locked-with-existing-session case), fill the popup's OWN password field directly and submit — this path is UNCHANGED, `unlock.password` still dispatches exactly as before, AUTH-01 does not touch it.

    Rework P9-SC1 (dual-browser.spec.ts:431-443): after config submit, assert the popup now shows the NEW hero — `await expect(popup.locator('[data-testid="server-ceremony-signin-button"]')).toBeVisible({timeout: 20000})` and `await expect(popup.locator('input[type="password"]')).toHaveCount(0)` — instead of the old `input[type="password"]` wait. The `config.get` "editable later" assertion is unchanged.

    Rework P9-SC2 (dual-browser.spec.ts:445-489): DELETE the entire ext-scoped-PRF CDP-virtual-authenticator enrollment+unlock sub-flow (the `WebAuthn.addVirtualAuthenticator`/`Create a passkey` click/force-lock/`Unlock with passkey` click sequence — that feature no longer exists post-Plan-15-04). Replace this SC's body with: (a) call the reworked `signInWithPassword()` (proves AUTH-01's full window-driven sign-in end-to-end with a real password ceremony); (b) a genuine lock -> reload -> password-unlock round trip — clear the key envelope via `chrome.storage.session.remove("pv-uk-envelope")` + force-terminate the service worker (the EXISTING CDP `ServiceWorker.stopAllWorkers` block, unchanged), reload, assert the NEW password-first locked view renders (`input[type="password"]` visible, `[data-testid="server-ceremony-unlock-button"]` visible with a class containing `btn-accent`), then unlock via the popup's own password field. Reword the test's own title (currently "... and with a PRF passkey where the browser supports it") to describe the new scenario — e.g. "P9-SC2: user signs in via the server-origin ceremony window (password), and unlocks the popup with the master password after a lock".
  </action>
  <verify>
    <automated>cd extension && npm run test:e2e:chrome</automated>
  </verify>
  <done>P9-SC1/P9-SC2 pass against the new UI; `grep -n 'input\[type="email"\]' extension/e2e/dual-browser.spec.ts` shows matches only against `ceremonyPage`/window-scoped locators, never `popup.locator(...)`; the full chromium Playwright project (21 SCs minus P9-SC2's reworded scope) passes.</done>
</task>

<task type="auto">
  <name>Task 2: Firefox manual harness (run-core.cjs) rework</name>
  <files>extension/e2e-firefox/run-core.cjs</files>
  <read_first>
    extension/e2e-firefox/run-core.cjs lines 180-245 (the P9-SC1 config block, the P9-SC2 password-half+sign-in block, and the "rpId-on-Firefox / ext-scoped passkey" block being deleted)
    extension/e2e-firefox/run-server-unlock.cjs lines 260-460 (the PROVEN window-handle-driving pattern for this exact scenario: getAllWindowHandles before/after click, driver.switchTo().window(ceremonyHandle), fill fields, driver.switchTo().window(popupHandle))
  </read_first>
  <action>
    Rework the "P9-SC1: server config" block (run-core.cjs ~195-212): after submitting the server URL, assert the NEW post-config view is the signed-out hero — `tryFind(driver, '[data-testid="server-ceremony-signin-button"]', 15000)` — instead of waiting for `input[type="password"]`.

    Rework the "P9-SC2 (password half) + sign-in" block (run-core.cjs ~214-223): click the hero button; capture `handlesBefore`/`handlesAfter`/`newHandles` exactly as `run-server-unlock.cjs` already does at its own signin-mode step (lines 440-450); `driver.switchTo().window(newHandles[0])`; fill `input[type="email"]` and the new `input#pv-ext-unlock-password` on that window; click the new password-submit control (locate by its `data-testid="ext-unlock-password-submit"`); wait for the ceremony window to self-close OR `driver.switchTo().window(popupHandle)` and wait for the popup to advance past the unlock view (`tryFind(driver, 'select', 20000)`).

    DELETE the "rpId-on-Firefox / ext-scoped passkey" block (run-core.cjs ~225-245) entirely — the feature it probed (`extPasskey.enroll.start`, D-12's disabled-button explainer) no longer exists post-Plan-15-04; this coverage class is now fully superseded by `run-server-unlock.cjs`'s own unchanged server-origin-ceremony assertions (P13-06/P13-07 rows), which this phase does not touch.

    Leave the "D-05: storage.session vs storage.local" block and everything after it UNCHANGED — unaffected by this phase's changes.
  </action>
  <verify>
    <automated>cd extension && node -c e2e-firefox/run-core.cjs</automated>
  </verify>
  <done>Script is syntactically valid (node -c passes); the rpId-on-Firefox block is fully removed; the P9-SC1/SC2 blocks target the new selectors. Live-Firefox execution is verified in Task 3's checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-close gate — real-browser e2e + AUTH-04 two-server live proof</name>
  <files>none — verification only, no source files modified by this task</files>
  <action>
    Run the full phase-close gate below against the real running stack (both browsers, a
    second live `pv-server` instance). Under the project's standing overnight authorization
    (self-validate via Playwright/browser automation + a test account, screenshots for taste
    calls), this checkpoint may be resolved by the executing agent itself driving the flow
    rather than blocking for Bartek, with results/screenshots attached to this plan's SUMMARY.
  </action>
  <what-built>The full phase-close gate for Phase 15: vitest/tsc/both builds already gated per-plan; this checkpoint covers the parts that need a real browser/real second server — chromium + chromium-ceremony Playwright (Task 1's rework), the reworked Firefox manual harness (Task 2's rework, run via real geckodriver), and a LIVE two-server AUTH-04 reconfigure scenario exercising Plan 15-05's confirm dialog end-to-end.</what-built>
  <how-to-verify>
    1. `cd extension && npm test` — expect the new baseline (674 minus the ~26 wholesale-deleted-file cases minus App/UnlockView/router replaced cases plus this phase's new cases; see each plan's own SUMMARY for its stated delta) — zero failures.
    2. `cd extension && npx tsc --noEmit && npm run build:chrome && npm run build:firefox` — all clean.
    3. `cd extension && npm run test:e2e:chrome` (both the `chromium` and `chromium-ceremony` Playwright projects) — all green, including this plan's reworked P9-SC1/SC2.
    4. Start a real Firefox + geckodriver session and run `node extension/e2e-firefox/run-core.cjs` — confirm every `record(...)` line prints `PASS` or `OBSERVED`, zero `FAIL`.
    5. Run `node extension/e2e-firefox/run-server-unlock.cjs` (unchanged this phase) — confirm it still passes, proving Firefox window centering/self-close (quick-260720-16k) has not regressed now that the ceremony window is the UNIVERSAL sign-in entry point on Firefox, not just the D-12 fallback case.
    6. AUTH-04 two-server live proof: start a SECOND `pv-server` instance on a different port/DB, e.g. `PV_ADDR=127.0.0.1:8621 PV_DB_URL=sqlite://data/pv2.db PV_DEV_CORS=1 cargo run -p pv-server` alongside the existing `:8620` instance. In the extension popup, open server settings while signed into `:8620`, reconfigure to `http://127.0.0.1:8621`, confirm the new dialog (Plan 15-05) appears, confirm the switch, and verify: (a) the popup ends up signed into `:8621`; (b) `:8620`'s `sessions` table no longer contains the old token (`sqlite3 data/pv.db "SELECT * FROM sessions;"` shows it gone); (c) no stray `browser.permissions` grant remains for `:8620`'s origin (check via the extension's `chrome://extensions` details page, "Site access", or `browser.permissions.getAll()` in the popup's dev console).
    7. `cargo test --workspace` — confirm untouched (this phase makes no server-side code changes).
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 checks pass, or describe which check failed and its output.</resume-signal>
  <verify>
    <human-check>All 7 steps in `&lt;how-to-verify&gt;` confirmed passing against the real running stack (both browsers + two live pv-server instances).</human-check>
  </verify>
  <done>vitest/tsc/both builds clean; chromium + chromium-ceremony Playwright green; real-Firefox run-core.cjs + run-server-unlock.cjs green; AUTH-04's two-server reconfigure scenario proven live (old session revoked server-side, old permission revoked, new server fully functional); cargo test --workspace unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test harness -> real browser -> real pv-server(s) | Live infrastructure, no new production trust boundary — this plan verifies existing boundaries hold, introduces none |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-15-12 | Repudiation (silent regression) | e2e coverage gap | medium | mitigate | This plan is the explicit closer for RESEARCH's flagged "highest-impact e2e change" — a missed rework here would leave AUTH-01/02/04 unverified in the ONLY lane that exercises real browser/real WebAuthn/real cross-server behavior |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs this plan |
</threat_model>

<verification>
Task 1 + Task 2's automated commands green; Task 3's checkpoint is the phase's actual closing gate.
</verification>

<success_criteria>
The 21-SC Playwright suite and both Firefox manual-harness scripts pass against the Wave 2/3
UI and message-protocol changes; the AUTH-04 two-server scenario is proven live, not just
mocked.
</success_criteria>

## Artifacts This Phase Produces (this plan's slice)

**Created:** none new production files — this plan is test-infrastructure rework only.

**Modified:** `signInWithPassword()`/`ensureVaultReady()` (dual-browser.spec.ts), P9-SC1/P9-SC2
test bodies, `run-core.cjs`'s initial sign-in block.

**Deleted:** the ext-scoped-PRF CDP-virtual-authenticator sub-flow inside P9-SC2
(dual-browser.spec.ts); the "rpId-on-Firefox / ext-scoped passkey" probe block (run-core.cjs).

<output>
Create `.planning/phases/15-login-unlock-unification-vaultwarden-model/15-07-SUMMARY.md` when done
</output>
