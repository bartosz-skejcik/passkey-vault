---
phase: 15-login-unlock-unification-vaultwarden-model
plan: 04
type: execute
wave: 3
depends_on: ["15-01", "15-03"]
files_modified:
  - extension/entrypoints/popup/EnrollExtPasskeyPrompt.tsx
  - extension/entrypoints/popup/EnrollExtPasskeyPrompt.test.tsx
  - extension/entrypoints/background/ext-passkey.ts
  - extension/entrypoints/background/ext-passkey.test.ts
  - extension/lib/passkeys/prf.ts
  - extension/lib/passkeys/ext-prf.ts
  - extension/lib/passkeys/ext-prf.test.ts
  - extension/lib/passkeys/prf-capability.ts
  - extension/lib/passkeys/prf-capability.test.ts
  - extension/entrypoints/background/router.ts
  - extension/entrypoints/background/router.test.ts
  - extension/lib/messaging/ext-protocol.ts
autonomous: true
requirements: [AUTH-01, AUTH-03]

must_haves:
  truths:
    - "The extension-scoped PRF unlock path (RP ID = extension id) is hard-removed — no code path can reach navigator.credentials.get()/create() with rpId=browser.runtime.id anymore (AUTH-03 success criterion #3)."
    - "The popup never dispatches auth.signIn.password again — the router no longer recognizes it as a popup-facing kind (AUTH-01, RESEARCH's recommendation)."
    - "The WR-01 assertPopupSender() gate at router.ts's handle() is byte-for-byte unchanged by this deletion pass (CONTEXT.md's explicit non-negotiable)."
  artifacts: []
  key_links: []
  prohibitions:
    - statement: "MUST NOT modify, reorder, or remove any line inside router.ts's WR-01 assertPopupSender() gate block while deleting the surrounding ext-scoped kinds"
      verification: test

assumption_delta_decision: |
  PROMOTE (one line): the assumption-delta scanner fired on pluralization language ("a second
  server") in ROADMAP's AUTH-04 success criterion — that is AUTH-04's two-server VERIFICATION
  infrastructure (Plan 15-07), not a second competing login/unlock mechanism. This phase's
  actual historical dual-mechanism (popup password sign-in + ext-scoped PRF vs. the
  server-origin ceremony) is resolved by PROMOTING the server-origin ceremony window to the
  sole sign-in/unlock mechanism on both browsers — exactly what this plan's deletion pass
  completes.

specless_probe_note: |
  AUTH-01/AUTH-03 returned unclassified/unresolved from the edge-probe. must_haves.truths above
  are derived from ROADMAP.md's Phase 15 success criteria #1 and #3 + 15-CONTEXT.md's AUTH-03
  decision text ("HARD removal, no migration UI ... delete the ext-scoped enrollment prompt
  ... the D-12/D-13 disabled-button-with-explainer machinery"), not from an edge-taxonomy
  classification.
---

<objective>
Complete AUTH-03's hard removal: delete every ext-scoped-PRF file (zero importers remain
outside the dying surface, per RESEARCH's grep-verified inventory, now that Plan 15-03 has
already stopped calling any of them from the popup UI), and narrow `router.ts`/`ext-protocol.ts`
to drop the corresponding message kinds and the now-dead `SessionStatus` fields
(`extPasskeyEnrolled`/`extPasskeyPromptSuppressed`).

Sequenced AFTER Plan 15-03 (not parallel with it) specifically to avoid a circular
type-coupling problem: `SessionStatus`'s two ext-scoped fields cannot be safely removed from
the TYPE in the same wave a UI consumer still constructs object literals against the WIDER
type — by the time this plan runs, `App.tsx`/`UnlockView.tsx` no longer read those fields at
all (Plan 15-03), so removing them from the type here cannot break a live consumer.

This is a removal-only plan — no new capability, no new UI. `crates/pv-server`'s
`extension_passkeys` routes are explicitly NOT touched (RESEARCH's recommendation: the CRUD
surface costs nothing to keep, deleting it would be pure churn requiring its own migration;
writes simply stop once the client-side enrollment UI no longer exists).

Purpose: physically remove the dead ext-scoped-PRF surface and the popup's own
auth.signIn.password dispatch path.
Output: 9 files deleted, router.ts/ext-protocol.ts narrowed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-login-unlock-unification-vaultwarden-model/15-CONTEXT.md
@.planning/phases/15-login-unlock-unification-vaultwarden-model/15-RESEARCH.md
@extension/entrypoints/background/router.ts
@extension/lib/messaging/ext-protocol.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete standalone ext-scoped-PRF files</name>
  <files>extension/entrypoints/popup/EnrollExtPasskeyPrompt.tsx, extension/entrypoints/popup/EnrollExtPasskeyPrompt.test.tsx, extension/entrypoints/background/ext-passkey.ts, extension/entrypoints/background/ext-passkey.test.ts, extension/lib/passkeys/prf.ts, extension/lib/passkeys/ext-prf.ts, extension/lib/passkeys/ext-prf.test.ts, extension/lib/passkeys/prf-capability.ts, extension/lib/passkeys/prf-capability.test.ts</files>
  <read_first>
    .planning/phases/15-login-unlock-unification-vaultwarden-model/15-RESEARCH.md section "DELETE WHOLESALE" (the grep-verified importer table — every file here is imported ONLY by other files also being deleted this task)
    15-03-SUMMARY.md (confirm App.tsx/UnlockView.tsx no longer import any of these 9 files, per Plan 15-03's own deletion of those call sites)
  </read_first>
  <action>
    Before deleting, run `grep -rl "ext-passkey\|lib/passkeys/prf\b\|lib/passkeys/ext-prf\|lib/passkeys/prf-capability\|EnrollExtPasskeyPrompt" extension/entrypoints extension/lib --include='*.ts' --include='*.tsx'` and confirm the ONLY matches are the 9 files themselves (each importing its own sibling test file, or a same-family import already scheduled for deletion in this same task). If any OTHER live file still imports one of these 9, STOP and investigate before deleting — do not delete a file something still needs. Then delete all 9 files listed in this task's `<files>` field.
  </action>
  <verify>
    <automated>cd extension && test -z "$(rg -l 'ext-passkey|EnrollExtPasskeyPrompt|lib/passkeys/prf|lib/passkeys/ext-prf|lib/passkeys/prf-capability' entrypoints lib --type-add 'tsx:*.tsx' --type ts --type tsx)"</automated>
  </verify>
  <done>All 9 files removed from the working tree; the verify command above asserts empty output (no remaining references) rather than swallowing a nonzero exit; `npx tsc --noEmit` still passes ONCE Task 2's router.ts/ext-protocol.ts import cleanup lands (both tasks land together before this plan's final gate — see plan-level verification).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: router.ts + ext-protocol.ts surgery</name>
  <files>extension/entrypoints/background/router.ts, extension/entrypoints/background/router.test.ts, extension/lib/messaging/ext-protocol.ts</files>
  <read_first>
    extension/entrypoints/background/router.ts — the line numbers below are PRE-15-05: Plan 15-05 (wave 2, this plan's own dependency at wave 3) inserts a `signOutVaultSession` import, a new `handleConfigProbe()` function, and new `isProtocolMessage()`/`handle()`-switch cases into this SAME file before this plan runs, shifting every downstream line number. Locate each block by symbol/pattern, NOT by these numbers: the ext-passkey.ts import block (originally ~77-123, search for `handleExtEnrollStart`), `isProtocolMessage()` (originally ~463-502), the `handle()` switch (originally ~526-587), `getSessionStatus()` (originally ~590-625)
    extension/lib/messaging/ext-protocol.ts — same pre-15-05 caveat (15-05 adds `config.probe`/`session.signOut` entries to this file first); locate by symbol name, not line number: `SessionStatus` + the `ExtEnrollStartResult`/`ExtUnlockResult` type-only import (originally ~99-124), the `Message` union's dying members (originally ~126-163), `MessageResponseMap`'s dying entries (originally ~356-371)
  </read_first>
  <action>
    In router.ts: remove the entire `ext-passkey.ts` import block (`handleExtEnrollStart, handleExtEnrollFinish, handleExtPrfUnlockStart, handleExtPrfUnlockFinish, hasEnrolledExtPasskey, readExtPasskeyPromptSuppressed, setExtPasskeyPromptSuppressed`, originally lines 107-115 pre-15-05 — locate by matching this exact import specifier list rather than the line numbers, since Plan 15-05's wave-2 edits to this same file shift them by the time this plan runs). Remove from `isProtocolMessage()`'s return expression: `kind === "extPasskey.enroll.start"`, `"extPasskey.enroll.finish"`, `"extPasskey.suppressPrompt"`, `"unlock.extPrf.start"`, `"unlock.extPrf.finish"`, AND `kind === "auth.signIn.password"` (per RESEARCH's recommendation — the underlying `handleUnlockPassword` survives as an internal-only target called from Plan 15-01's `completeServerUnlock` password branch, not from a popup-dispatched message kind). Remove the corresponding 6 `case` arms from `handle()`'s switch: `"extPasskey.enroll.start"`, `"extPasskey.enroll.finish"`, `"extPasskey.suppressPrompt"`, `"unlock.extPrf.start"`, `"unlock.extPrf.finish"`, `"auth.signIn.password"`. In `getSessionStatus()`: remove the `hasEnrolledExtPasskey()`/`readExtPasskeyPromptSuppressed()` calls from the `Promise.all([...])` (keep only `ensureHydrated()`), and remove `extPasskeyEnrolled`/`extPasskeyPromptSuppressed` from both the `locked` and `unlocked` returned object shapes. LEAVE the WR-01 `assertPopupSender()` gate (the `if (... startsWith("session.") ... !assertPopupSender(sender)) { return {ok:false, error:"forbidden-sender"} }` block) completely untouched — zero line changes inside that specific `if` block, verified by this task's own diff review before commit.

    In ext-protocol.ts: remove `extPasskey.enroll.start`/`extPasskey.enroll.finish`/`extPasskey.suppressPrompt`/`unlock.extPrf.start`/`unlock.extPrf.finish` from the `Message` union and their `MessageResponseMap` entries; remove `auth.signIn.password` from both the `Message` union and `MessageResponseMap`; remove `extPasskeyEnrolled`/`extPasskeyPromptSuppressed` from `SessionStatus`'s `locked` and `unlocked` variants; remove the now-dead `import type { ExtEnrollStartResult, ExtUnlockResult } from "../../entrypoints/background/ext-passkey"` (the module no longer exists after Task 1).

    Update router.test.ts: remove every test case exercising the 6 deleted kinds; for every `SessionStatus`-shaped fixture object this file constructs (grep the file for `extPasskeyEnrolled`/`extPasskeyPromptSuppressed` to find each one), drop both fields.
  </action>
  <behavior>
    - isProtocolMessage() returns false for {kind:"extPasskey.enroll.start"} and for {kind:"auth.signIn.password", email:"a@b.c", passwordB64:"x"}
    - getSessionStatus()'s returned locked/unlocked shapes have no extPasskeyEnrolled/extPasskeyPromptSuppressed keys (assert via a `not.toHaveProperty` or explicit Object.keys check)
    - unlock.password (unchanged), config.get/config.set (unchanged), unlock.serverCeremony.start (unchanged) all still dispatch correctly — no collateral regression
  </behavior>
  <verify>
    <automated>cd extension && npx vitest run entrypoints/background/router.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>3 behavior cases green; WR-01 gate block diff-reviewed as zero-changed; ext-protocol.ts's Message/MessageResponseMap/SessionStatus carry none of the 6 removed Message kinds or 2 removed SessionStatus fields; tsc clean across the whole extension workspace (confirms Task 1's deletions left no dangling importer).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Popup -> background (WR-01 gate) | The ONLY security-relevant surface this plan touches adjacently — must remain byte-identical |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-15-08 | Elevation of Privilege | router.ts WR-01 gate | high | mitigate | Explicit "leave untouched" instruction + a diff-review acceptance criterion before commit; no logic inside the gate's own `if` block is edited by either task |
| T-15-09 | Information Disclosure | ext-protocol.ts SessionStatus | low | accept | Removing extPasskeyEnrolled/extPasskeyPromptSuppressed REDUCES the response surface (net-positive, not a new exposure) |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs this plan |
</threat_model>

<verification>
`cd extension && npx vitest run && npx tsc --noEmit` fully green after BOTH tasks land (Task 1's deletion + Task 2's import cleanup are interdependent for a clean tsc pass — verify together, not task-by-task in isolation, even though each task's own automated command is listed separately above for granular feedback during execution).
</verification>

<success_criteria>
Zero remaining ext-scoped-PRF files or message kinds in the codebase; `auth.signIn.password` no longer reachable from the popup; WR-01 gate byte-identical; full vitest+tsc green.
</success_criteria>

## Artifacts This Phase Produces (this plan's slice)

**Deleted:**
- `extension/entrypoints/popup/EnrollExtPasskeyPrompt.tsx` + `.test.tsx`
- `extension/entrypoints/background/ext-passkey.ts` + `.test.ts`
- `extension/lib/passkeys/prf.ts`
- `extension/lib/passkeys/ext-prf.ts` + `.test.ts`
- `extension/lib/passkeys/prf-capability.ts` + `.test.ts`
- Message kinds: `extPasskey.enroll.start`, `extPasskey.enroll.finish`, `extPasskey.suppressPrompt`, `unlock.extPrf.start`, `unlock.extPrf.finish`, `auth.signIn.password`
- `SessionStatus` fields: `extPasskeyEnrolled`, `extPasskeyPromptSuppressed` (both `locked` and `unlocked` variants)

**Created:** none this plan.

<output>
Create `.planning/phases/15-login-unlock-unification-vaultwarden-model/15-04-SUMMARY.md` when done
</output>
