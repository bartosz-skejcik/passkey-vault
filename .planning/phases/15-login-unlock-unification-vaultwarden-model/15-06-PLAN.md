---
phase: 15-login-unlock-unification-vaultwarden-model
plan: 06
type: execute
wave: 4
depends_on: ["15-03", "15-04"]
files_modified:
  - extension/lib/i18n/dictionary.ts
  - extension/entrypoints/__tests__/no-ext-scoped-prf-strings.test.ts
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "No extPasskey.*/extPrf/ext-passkey/ext-prf/prf-capability string literal survives anywhere under extension/entrypoints/** or extension/lib/**, proven by a permanent structural test, not assumed from the deletion pass alone (AUTH-03: 'verified by search, not assumed')."
  artifacts:
    - "extension/entrypoints/__tests__/no-ext-scoped-prf-strings.test.ts: permanent regression guard"
  key_links: []
  prohibitions: []

specless_probe_note: |
  AUTH-03 returned unclassified/unresolved from the edge-probe. The must_haves.truths above is
  derived from ROADMAP.md's Phase 15 success criterion #3 ("removed, or explicitly documented
  as retired") and this project's own scope_reduction_prohibition doctrine ("verified by
  search, not assumed"), not from an edge-taxonomy classification. This plan is the closing
  proof for AUTH-03, run only after Plan 15-03 and Plan 15-04 have removed every live usage.
---

<objective>
Close out AUTH-03 with two final, low-risk steps that were deliberately deferred past Plan
15-04's deletion pass: (1) purge the now-dead dictionary keys once BOTH consuming plans
(15-03's UI rewrite, 15-04's file deletion) have removed every live reference — dictionary key
deletion must happen no earlier than removal of the LAST usage, and those usages were split
across two plans in two different waves, so this cleanup could not safely land any earlier; and
(2) add a permanent, structural, grep-based vitest guard (mirroring
`server-config.test.ts`'s own `no_other_extension_file_hard_codes_a_server_url` pattern) proving
no ext-scoped-PRF string survives anywhere in the codebase — RESEARCH.md's own flagged Wave 0
gap, and the mechanical proof AUTH-03's "verified by search, not assumed" clause demands.

Purpose: leave zero dead dictionary entries and a permanent regression guard against the
ext-scoped-PRF surface ever silently creeping back.
Output: pruned `dictionary.ts`; a new structural guard test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-login-unlock-unification-vaultwarden-model/15-UI-SPEC.md
@.planning/phases/15-login-unlock-unification-vaultwarden-model/15-RESEARCH.md
@extension/lib/i18n/dictionary.ts
@extension/entrypoints/background/server-config.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge dead dictionary keys</name>
  <files>extension/lib/i18n/dictionary.ts</files>
  <read_first>
    extension/lib/i18n/dictionary.ts (full — the DICTIONARY object)
    .planning/phases/15-login-unlock-unification-vaultwarden-model/15-UI-SPEC.md's Copywriting Contract section, "Dictionary keys retired this phase" paragraph (the authoritative retirement list)
  </read_first>
  <action>
    For EACH key below, grep-verify zero remaining references to the key STRING (not just the
    object-literal entry) under `extension/entrypoints/**` and `extension/lib/**` before
    deleting it from `DICTIONARY` — if any live reference remains, STOP and report which file
    still uses it rather than deleting blindly: the entire `extPasskey.*` block (`promptTitle`,
    `promptBody`, `promptCta`, `promptSkip`, `promptDontAskAgain`, `enrollDone`, `enrollNoPrf`,
    `enrollFailed`, `unlockOrphaned`, `serverPathPointer`); `unlock.passkeyLoginCta`;
    `unlock.passkeyBusy`; `unlock.passkeyFailed`; `unlock.passkeyUnsupported`;
    `unlock.serverCeremonyCta`; `unlock.serverCeremonySigninCta`; `auth.emailLabel` (the
    popup's own sign-in email field was removed in Plan 15-03); `auth.wrongCredentials`
    (RESEARCH.md already grep-confirmed this dead in `extension/` — re-verify at execution
    time, since intervening plans may have changed things).

    Also verify (idempotent check, Plan 15-03's Task 1 should have already handled this): the
    `unlock.serverCeremonySigninFailed` entry's PL/EN values no longer contain the stale
    "or use your password" / "albo użyj hasła" clause. If they still do, fix them now: PL "Nie
    udało się zalogować przez stronę serwera. Spróbuj ponownie." / EN "Couldn't sign in via
    your server. Try again."

    Document, in this plan's own SUMMARY.md (never as product code, per RESEARCH's explicit
    recommendation), the one-time manual server-side cleanup command for Bartek's own dev
    database: `sqlite3 data/pv.db "DELETE FROM extension_passkeys;"` — server-side routes and
    the `extension_passkeys` table are deliberately NOT touched by this phase.
  </action>
  <verify>
    <automated>cd extension && grep -c '"extPasskey\.' lib/i18n/dictionary.ts | grep -qx 0 && npx tsc --noEmit</automated>
  </verify>
  <done>Every listed key removed from `DICTIONARY`; `npx tsc --noEmit` clean (proves no remaining `t(locale, "...")` call site references a now-deleted key — that would be a type error against the narrowed `DICTIONARY` key union); the stale-copy check confirmed already-fixed or fixed now; the manual cleanup command documented in SUMMARY.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural guard test — AUTH-03 closure proof</name>
  <files>extension/entrypoints/__tests__/no-ext-scoped-prf-strings.test.ts</files>
  <read_first>
    extension/entrypoints/background/server-config.test.ts lines 252-320 (the exact `describe("no_other_extension_file_hard_codes_a_server_url", ...)` walk/skipDirs/allowedFiles/pattern-match structure to mirror — readdirSync/readFileSync/statSync recursive walk, self-exclusion via an `allowedFiles` Set)
  </read_first>
  <action>
    Create `extension/entrypoints/__tests__/no-ext-scoped-prf-strings.test.ts`, mirroring
    `server-config.test.ts`'s structural-walk pattern closely but with two deliberate
    differences from that precedent: (1) this guard DOES scan `*.test.ts`/`*.test.tsx` files
    too (unlike the URL-literal guard, which excludes test files as legitimate mock-fixture
    territory) — a leftover `extPasskey.enroll.start` string inside a stale test fixture is
    exactly the kind of orphan this guard exists to catch; (2) the skip-dirs set still excludes
    `node_modules`/`.output`/`.wxt`/`dist` but does NOT need to exclude `e2e` the way the
    URL-literal guard does, since a leftover ext-scoped-PRF string inside e2e/ IS meaningful
    signal here (Plan 15-07 is expected to have already removed it). Walk both
    `extension/entrypoints/` and `extension/lib/` (not the whole `extension/` root — scope this
    guard to production+test source, mirroring the two directories RESEARCH.md's own inventory
    scoped its deletion table to). Assert that none of these literal substrings appear anywhere
    in the walked files: `"extPasskey."`, `"extPrf"`, `"ext-passkey"`, `"ext-prf"`,
    `"prf-capability"` — self-excluding this new test file itself via an `allowedFiles`-style
    Set, matching the precedent file's own self-exclusion pattern.
  </action>
  <behavior>
    - The walk finds zero occurrences of any of the 5 substrings across extension/entrypoints/** and extension/lib/** (excluding this test file itself)
  </behavior>
  <verify>
    <automated>cd extension && npx vitest run entrypoints/__tests__/no-ext-scoped-prf-strings.test.ts</automated>
  </verify>
  <done>New guard test exists, passes, and is committed as PERMANENT regression coverage (not a throwaway one-off check) — a future PR that accidentally reintroduces any ext-scoped-PRF string anywhere in the scanned directories will fail this test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — this plan touches only i18n data and adds a static-analysis test; no runtime trust
boundary is crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-15-13 | Tampering (silent regression) | dictionary.ts / any future PR | low | mitigate | Task 2's permanent structural guard fails any future PR that reintroduces an ext-scoped-PRF string, closing AUTH-03 as a durable invariant rather than a one-time cleanup |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs this plan |
</threat_model>

<verification>
`cd extension && npx vitest run && npx tsc --noEmit` fully green.
</verification>

<success_criteria>
Dictionary carries no dead ext-scoped-PRF keys; a permanent grep-based test proves and will
continue to prove the removal is complete.
</success_criteria>

## Artifacts This Phase Produces (this plan's slice)

**Created:**
- `extension/entrypoints/__tests__/no-ext-scoped-prf-strings.test.ts` (new, permanent)

**Deleted (dictionary keys):**
- `extPasskey.promptTitle`, `extPasskey.promptBody`, `extPasskey.promptCta`,
  `extPasskey.promptSkip`, `extPasskey.promptDontAskAgain`, `extPasskey.enrollDone`,
  `extPasskey.enrollNoPrf`, `extPasskey.enrollFailed`, `extPasskey.unlockOrphaned`,
  `extPasskey.serverPathPointer`, `unlock.passkeyLoginCta`, `unlock.passkeyBusy`,
  `unlock.passkeyFailed`, `unlock.passkeyUnsupported`, `unlock.serverCeremonyCta`,
  `unlock.serverCeremonySigninCta`, `auth.emailLabel`, `auth.wrongCredentials`

<output>
Create `.planning/phases/15-login-unlock-unification-vaultwarden-model/15-06-SUMMARY.md` when done
</output>
