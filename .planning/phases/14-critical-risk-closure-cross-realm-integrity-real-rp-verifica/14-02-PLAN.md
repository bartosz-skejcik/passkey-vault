---
phase: 14-critical-risk-closure-cross-realm-integrity-real-rp-verifica
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/entrypoints/page-bridge-firefox.ts
  - extension/entrypoints/content-relay.content.ts
  - .planning/debug/firefox-request-xray-hole.md
autonomous: true
requirements: [XBR-02]

must_haves:
  truths:
    - "A live-Firefox differential probe explains (or honestly documents inability to explain) the standalone-probe-vs-real-flow discrepancy from the debug doc's 2026-07-20T00:30:00Z/01:00:00Z evidence entries, and the evidence is recorded BEFORE any fix code is written"
    - "page-bridge-firefox.ts's shapeCredential() re-materializes every response-direction binary field (rawId, response.clientDataJSON, response.attestationObject/authenticatorData/signature, optional response.userHandle, optional clientExtensionResults.prf.results.first/.second) as a genuine MAIN-world-native ArrayBuffer, decoded from credentialJson's base64url string form"
    - "Any decode failure in the new re-materialization logic falls through to broker()'s existing native-fallthrough catch, never throwing uncaught into the RP page's promise chain"
    - "content-relay.content.ts's D-21 header comment is updated in the same commit to reflect the new Firefox-only MAIN-world exception, per CONTEXT.md's explicit instruction not to let a stale comment veto the correct fix"
    - "page-bridge.content.ts (Chrome) is unmodified — research found no equivalent Xray hazard on Chrome's MAIN<->ISOLATED hop"
    - "validation/nonce/origin/consent logic (handleProviderPageMessage's D-03/ASVS V5 gates, the nonce ledger, postAck) is byte-for-byte unchanged — SECURED constraint"
  artifacts:
    - path: "extension/entrypoints/page-bridge-firefox.ts"
      provides: "MAIN-world b64UrlToArrayBuffer() + re-materializing shapeCredential() — the Firefox response-direction Xray fix"
      contains: "b64UrlToArrayBuffer"
    - path: ".planning/debug/firefox-request-xray-hole.md"
      provides: "Git-tracked differential-probe evidence entry root-causing (or documenting) the response-direction discrepancy, recorded before the fix"
  key_links:
    - from: "extension/entrypoints/page-bridge-firefox.ts"
      to: "extension/entrypoints/content-relay.content.ts"
      via: "credentialJson (the base64url-string form respondToPage() already produces via JSON.parse(response.credentialResponseJson)) is the shared contract both files independently decode — once in ISOLATED world (existing, unchanged), now also once in MAIN world (new)"
      pattern: "credentialJson"
    - from: "extension/entrypoints/page-bridge-firefox.ts (broker())"
      to: "extension/entrypoints/page-bridge-firefox.ts (shapeCredential())"
      via: "broker()'s existing outer try/catch (lines 251-276) already wraps the shapeCredential() call site, providing fail-safe native fallthrough for the new decode logic without adding a redundant inner try/catch"
      pattern: "shapeCredential"
---

<objective>
Root-cause (empirically, on real Firefox, before writing any fix code) and then fix the RESPONSE-direction Firefox cross-realm Xray hole: `credential.rawId` and — by the same mechanism — every other binary field `decodeCredentialResponseJson` produces (`response.clientDataJSON`/`attestationObject`/`authenticatorData`/`signature`/`userHandle`, PRF `results.first`/`.second`) arrives in the RP page's own MAIN-world realm with `instanceof ArrayBuffer === false`, even though the underlying bytes are intact. A real RP library that branches on `instanceof` (e.g. `webauthn-json`-style helpers) may treat a genuinely valid credential as malformed on Firefox.

Purpose: the debug doc's own evidence contains an UNEXPLAINED discrepancy — a minimal standalone probe found the response direction clean (`instanceof: true`), but the real end-to-end product flow found the opposite (`instanceof: false`). The phase's own success bar ("verified live, not inferred") requires this discrepancy be explained, not paved over, before landing a fix.

Output: `page-bridge-firefox.ts` gains a MAIN-world re-materialization fix (preferred path (a) per CONTEXT.md — no SECURED/D-21 conflict is expected per RESEARCH.md, but Task 1's live evidence is the actual gate); `content-relay.content.ts`'s D-21 ownership comment is corrected; `.planning/debug/firefox-request-xray-hole.md` becomes git-tracked for the first time and carries a new dated Evidence entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/firefox-request-xray-hole.md
@extension/entrypoints/page-bridge-firefox.ts
@extension/entrypoints/content-relay.content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-Firefox differential root-cause probe (diagnostic only, no fix code)</name>
  <files>.planning/debug/firefox-request-xray-hole.md</files>
  <read_first>
    .planning/debug/firefox-request-xray-hole.md (full doc — especially the 2026-07-20T00:30:00Z evidence entry's standalone-probe methodology and the 2026-07-20T01:00:00Z entry's real-flow finding and its "NOT YET explained" implication)
    extension/entrypoints/content-relay.content.ts, lines 675-816 (postToPage, postAck, respondToPage, handleProviderPageMessage — the REAL message sequence: postAck() fires BEFORE the terminal postToPage() credential message, both on the same nonce/window)
    extension/entrypoints/content-relay.content.ts, lines 434-501 (isCrossRealmArrayBuffer/isBufferSource/bufferSourceToB64Url/b64UrlToArrayBuffer — the request-direction fix's own detection/construction pattern, for methodology parity)
    extension/e2e-firefox/probe-request-xray.cjs, lines 1-235 (harness conventions to mirror for realism: geckodriver Builder/firefox.Options, fixed profile UUID, CSP-strict fixture server, record()/shot() helpers — reuse the CONVENTIONS; do not commit any change to this file in this task)
  </read_first>
  <action>
    Build a throwaway, uncommitted differential-probe harness (temporary script(s) and/or a standalone scratch WebExtension, kept OUTSIDE git tracking — mirror the debug doc's own 2026-07-20T00:30:00Z "scratchpad xray-probe-ext/" technique, and reuse probe-request-xray.cjs's geckodriver/profile/CSP-fixture conventions for realism) that drives real Firefox 152.x and isolates RESEARCH.md's Open Question 1 candidate variables ONE AT A TIME:

    (a) Whether posting an ack-shaped `{source:'pv-content-relay', nonce, kind:'ack'}` message on the SAME nonce/window immediately BEFORE the credential-shaped response message (mirroring content-relay.content.ts's real two-message postAck()-then-postToPage() sequence) changes the SECOND message's ArrayBuffer field's `instanceof` result on the receiving (page) side, versus sending only the single credential message with no preceding ack.

    (b) Whether driving the REAL, unmodified `.output/firefox-mv2` build end-to-end (a real ceremony through the actual `respondToPage()` -> `decodeCredentialResponseJson()` -> `postToPage()` chain) produces a DIFFERENT `instanceof` result than the standalone harness's own bare construction of the same field, using the SAME byte values for direct comparison.

    (c) Whether the credential envelope's sibling-field count (matching the real 5-6-field response shape: `kind`/`credential`/`credentialJson`/`prfCapable`/`prfUnavailableReason`) versus a minimal 1-field envelope changes the outcome — this confirms or overturns the debug doc's own prior claim that envelope shape was "ruled out" as the variable.

    For each variant, record `instanceof ArrayBuffer`, `Object.prototype.toString.call(...)`, and a `new Uint8Array(...)`-based byte-read on the page-realm side. Append ONE new Evidence entry to `.planning/debug/firefox-request-xray-hole.md` (mirroring the doc's existing entry structure: `timestamp:`/`checked:`/`found:`/`implication:`), timestamped later than `2026-07-20T01:00:00Z`, documenting: the per-variant results, which variable (if any) flips the outcome, an explicit conclusion about whether the standalone-vs-real-flow discrepancy is now explained, and — the gate Task 2 depends on — an explicit statement of whether anything found conflicts with the SECURED constraints or the D-21 encode/decode-ownership boundary. Per CONTEXT.md's fix-preference order: no conflict found means fix path (a) (MAIN-world re-materialization) proceeds in Task 2 exactly as specified there; a genuine conflict found means Task 2 must use path (b) (documented contract-equivalent) instead.

    This task's own commit must `git add .planning/debug/firefox-request-xray-hole.md` explicitly — the file currently exists on disk but is untracked (confirmed via `git status`). This is the phase's first commit touching the topic, satisfying CONTEXT.md's record-hygiene requirement ("the doc must never again exist only untracked") at the earliest possible point rather than deferring it to the phase-closing plan.
  </action>
  <acceptance_criteria>
    - `.planning/debug/firefox-request-xray-hole.md` contains a new `- timestamp:` Evidence entry with a value later than `2026-07-20T01:00:00Z`.
    - That entry's `found:`/`implication:` text addresses all three candidate variables (a)/(b)/(c) above, each with a concrete recorded result (never "TBD"/"unknown").
    - That entry contains an explicit sentence stating whether fix path (a) is clear to proceed, or whether path (b) is required instead.
    - `git ls-files .planning/debug/firefox-request-xray-hole.md` returns the path (the doc is now tracked).
    - `git status --porcelain -- extension/` shows no changes to any tracked file under `extension/` (this task is diagnostic-only; the throwaway harness lives outside git tracking).
  </acceptance_criteria>
  <verify>
    <automated>git ls-files .planning/debug/firefox-request-xray-hole.md | grep -q firefox-request-xray-hole.md</automated>
  </verify>
  <done>A dated Evidence entry records real-Firefox differential-probe results for all three candidate variables, root-causing (or honestly documenting the limits of root-causing) the response-direction discrepancy, and gates Task 2's fix-path choice — BEFORE any production file changes.</done>
</task>

<task type="auto">
  <name>Task 2: MAIN-world response-direction re-materialization fix</name>
  <files>extension/entrypoints/page-bridge-firefox.ts, extension/entrypoints/content-relay.content.ts</files>
  <read_first>
    extension/entrypoints/page-bridge-firefox.ts (full file — current shapeCredential() at lines 230-241; broker()'s outer try/catch at lines 251-276 that already wraps the shapeCredential() call site)
    extension/entrypoints/content-relay.content.ts, lines 434-501 (isCrossRealmArrayBuffer/isBufferSource/bufferSourceToB64Url/b64UrlToArrayBuffer — the exact decode logic to mirror, executing against THIS file's own atob/Uint8Array/ArrayBuffer globals instead)
    extension/entrypoints/content-relay.content.ts, lines 600-658 (RESPONSE_BINARY_FIELDS = ["clientDataJSON","attestationObject","authenticatorData","signature","publicKey"] and decodeCredentialResponseJson()'s exact field list and typeof-string guards — the field list and guard style to mirror)
    extension/entrypoints/content-relay.content.ts, lines 359-383 (the D-21 header comment block claiming "page-bridge itself stays completely free of any encoding logic" and "page-bridge -- which never runs a base64 decoder of its own" — both sentences become inaccurate once this task lands and must be amended in the same commit)
    extension/entrypoints/page-bridge.content.ts (Chrome twin — confirm it needs no change; research found no Xray hazard on Chrome's MAIN<->ISOLATED hop)
    scripts/audit-mainworld-boundary.sh (confirms the FORBIDDEN regex is `pv-wasm|passkey-(authenticator|client|types)|lib/crypto|lib/vault` — native atob/ArrayBuffer/Uint8Array usage is not restricted by this gate)
    .planning/debug/firefox-request-xray-hole.md (Task 1's new Evidence entry — read its explicit path-(a)-vs-(b) determination before implementing)
  </read_first>
  <action>
    If Task 1's evidence found no SECURED/D-21 conflict (the expected outcome per RESEARCH.md's own analysis): implement fix path (a). Add a local `b64UrlToArrayBuffer(b64url: string): ArrayBuffer` function to page-bridge-firefox.ts, copying content-relay.content.ts's own implementation verbatim but executing against this file's own native `atob`/`Uint8Array`/`ArrayBuffer` globals (MAIN-world realm) — this addition uses only native browser globals, so it structurally cannot trip `audit-mainworld-boundary.sh`'s FORBIDDEN-import regex.

    Rewrite `shapeCredential(credential, credentialJson)` to re-materialize every response-direction binary field from `credentialJson`'s already-available base64url STRING fields (never from `credential`'s ISOLATED-decoded ArrayBuffer values, whose realm identity is what's broken) using the new MAIN-world `b64UrlToArrayBuffer`: top-level `rawId`; `response.clientDataJSON`; `response.attestationObject` (create) or `response.authenticatorData` + `response.signature` (get) — mirror content-relay.content.ts's `RESPONSE_BINARY_FIELDS` list exactly, plus optional `response.userHandle` and optional `clientExtensionResults.prf.results.first`/`.second`. Only decode a field when `credentialJson`'s corresponding value is a `string` (mirrors `decodeCredentialResponseJson`'s own `typeof === "string"` guard, so a field the ceremony never set stays untouched). Build a new object with these freshly re-materialized fields layered over the existing `{...cred, getClientExtensionResults, toJSON}` shape, so any field `credentialJson` does not cover (e.g. `id`, `type`) still falls back to `credential`'s existing value — this fix is additive, not a full-object replacement. Do not add a new try/catch inside `shapeCredential` itself: `broker()`'s existing outer try/catch already wraps the `shapeCredential(...)` call site and falls through to `original(options)` on any thrown error, which already satisfies the fail-safe discipline this new decode logic needs (matches the IN-01/Pitfall-5 precedent without a redundant inner guard).

    In content-relay.content.ts, amend the D-21 header comment block (the two sentences identified in read_first) to note the new Firefox-only exception: page-bridge-firefox.ts's `shapeCredential()` now ALSO re-decodes response-direction binary fields from `credentialJson` in MAIN world, for Xray realm-safety — cite `.planning/debug/resolved/firefox-request-xray-hole.md` by path in the amended comment. State clearly that content-relay.content.ts remains the sole owner of the REQUEST-direction encode and its own unchanged ISOLATED-world response decode (`decodeCredentialResponseJson` itself is not modified — it is still needed and correct for its existing purpose).

    Do not modify extension/entrypoints/page-bridge.content.ts (Chrome) — Plan 14-03's phase-closing gate suite re-runs `chromium-ceremony` 5/5 as the regression proof this omission is safe.

    If Task 1's evidence instead found a genuine SECURED/D-21 conflict (the documented fallback trigger): implement path (b) instead — leave `shapeCredential()`'s field-handling unchanged, and instead write down the documented contract-equivalent guarantee (`Object.prototype.toString.call` brand + byte-intact via `new Uint8Array(...)` + `toJSON()`) explicitly in this file's header comment and in the debug doc's Resolution section (Plan 14-03 handles the doc's final Resolution write-up, but this task must record the path-(b) determination and its exact guarantees so that plan can cite them precisely).
  </action>
  <acceptance_criteria>
    - `grep -c "^export " extension/entrypoints/page-bridge-firefox.ts` returns exactly 1 (only the pre-existing `isPermissionsPolicyBlocked` export — D-02 "no new import surface" preserved).
    - `bash scripts/audit-mainworld-boundary.sh` exits 0.
    - `cd extension && npx tsc --noEmit` is clean.
    - `cd extension && npm run build:firefox` succeeds and produces `.output/firefox-mv2/page-bridge-firefox.js`.
    - content-relay.content.ts's D-21 header comment block (lines ~359-383) contains a new reference to `.planning/debug/resolved/firefox-request-xray-hole.md`.
    - `git diff --stat extension/entrypoints/page-bridge.content.ts` is empty (Chrome file untouched), unless Task 1's evidence explicitly required a shared-file change (in which case note the deviation and why in the SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/audit-mainworld-boundary.sh</automated>
  </verify>
  <done>page-bridge-firefox.ts's shapeCredential() re-materializes response-direction binary fields as genuine MAIN-world ArrayBuffers; audit-mainworld-boundary.sh stays green; Chrome's twin file is untouched; the D-21 comment reflects reality.</done>
</task>

</tasks>

<flagged_assumptions>
- **XBR-02's edge-probe row returned category `unclassified`, probe "unclassified — review manually", status `unresolved`.** This requirement's actual edge-shape (cross-realm JS object-identity integrity, not a CRUD data boundary) falls outside the edge-probe's closed shape taxonomy (boundary/precision/type/etc.) — it is comprehensively covered instead by this phase's own dedicated goal-backward `must_haves` above (realm-correctness for every response-direction binary field, byte-level round-trip, live-Firefox differential-probe evidence) and by Plan 14-03's upgraded `probe-request-xray.cjs` hard assertions. Recorded here per the no-silent-drop rule rather than auto-dismissed.
</flagged_assumptions>

## Artifacts this phase produces (this plan's share)

- `extension/entrypoints/page-bridge-firefox.ts` — new local `b64UrlToArrayBuffer()` helper; rewritten `shapeCredential()` that re-materializes response-direction binary fields in MAIN world.
- `extension/entrypoints/content-relay.content.ts` — amended D-21 ownership header comment (no new symbols/logic).
- `.planning/debug/firefox-request-xray-hole.md` — new, git-tracked Evidence entry (Task 1) documenting the differential-probe root-cause investigation.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| RP page (MAIN world, untrusted) <-> page-bridge-firefox.ts (MAIN world, key-free) | The new `credentialJson` re-decode reads data the page itself already receives — no new secret crosses this boundary |
| page-bridge-firefox.ts <-> content-relay.content.ts (ISOLATED world) | Unchanged this task — postMessage envelope shape, nonce, origin discipline (D-03/ASVS V5) are byte-for-byte preserved (SECURED) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering / Denial of Service | page-bridge-firefox.ts new b64UrlToArrayBuffer/shapeCredential re-materialization | medium | mitigate | A malformed/spoofed credentialJson field causing `new Uint8Array(...)`/`atob(...)` to throw is caught by broker()'s existing outer try/catch (unchanged), which falls through to native `original(options)` — never wedges the ceremony or propagates a new error type to the page (mirrors the already-accepted IN-01 residual-risk posture for the request direction) |
| T-14-04 | Information Disclosure | credentialJson fields re-decoded in MAIN world | low | accept | These are the SAME public credential-response fields (`rawId`, `clientDataJSON`, `attestationObject`, etc.) the page already receives as the return value of `navigator.credentials.create()/get()` per spec — re-decoding them in the same realm the page already has full access to discloses nothing new |
| T-14-05 | Elevation of Privilege | handleProviderPageMessage validation gates | n/a (not touched) | mitigate | SECURED constraint: validation/nonce/origin/consent logic is explicitly out of scope for this task; acceptance criteria include a diff check confirming no changes outside the two named fix files |
| T-14-SC | Tampering | npm/cargo installs | n/a | accept | No new package introduced by this plan (native browser globals only) |

</threat_model>

<verification>
`bash scripts/audit-mainworld-boundary.sh` exits 0. `cd extension && npx tsc --noEmit` clean. `cd extension && npm run build:chrome && npm run build:firefox` both succeed. `git diff --stat extension/entrypoints/page-bridge.content.ts` empty. `.planning/debug/firefox-request-xray-hole.md` git-tracked with a new dated Evidence entry.
</verification>

<success_criteria>
The response-direction discrepancy is explained (or its investigation limits honestly recorded) by live-Firefox evidence gathered BEFORE the fix; the fix re-materializes every response-direction binary field as a genuine MAIN-world ArrayBuffer without touching SECURED validation/nonce/origin/consent logic or regressing Chrome.
</success_criteria>

<output>
Create `.planning/phases/14-critical-risk-closure-cross-realm-integrity-real-rp-verifica/14-02-SUMMARY.md` when done
</output>
