---
phase: 14-critical-risk-closure-cross-realm-integrity-real-rp-verifica
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - extension/entrypoints/__tests__/page-bridge-firefox.test.ts
  - extension/e2e-firefox/probe-request-xray.cjs
  - .planning/debug/firefox-request-xray-hole.md
  - .planning/debug/resolved/firefox-request-xray-hole.md
  - .planning/STATE.md
autonomous: true
requirements: [XBR-02]

must_haves:
  truths:
    - "A deterministic jsdom test (page-bridge-firefox.test.ts) proves the response-direction MAIN-world re-materialization fix using the SAME cross-realm-iframe technique already proven for the request direction"
    - "probe-request-xray.cjs's XRAY-CREATE/XRAY-GET rows hard-assert response-direction realm identity for every binary field on a REAL Firefox run, and both PASS"
    - "The debug doc is git-tracked, its Resolution section covers the response direction, its status is resolved, and it lives at .planning/debug/resolved/"
    - "STATE.md's OPEN blocker and Deferred Items row for this issue are flipped to resolved"
    - "The full phase gate suite (vitest, tsc, both builds, audit-mainworld-boundary.sh, run-core.cjs, run-server-unlock.cjs, probe-request-xray.cjs, chromium-ceremony, cargo test --workspace) is green"
  artifacts:
    - path: "extension/entrypoints/__tests__/page-bridge-firefox.test.ts"
      provides: "Deterministic jsdom regression coverage for the MAIN-world response-direction re-materialization fix"
      contains: "crossRealmArrayBuffer"
    - path: "extension/e2e-firefox/probe-request-xray.cjs"
      provides: "Upgraded live-Firefox hard-assertion of response-direction realm+byte identity (no longer an unasserted, known-failing caveat)"
      contains: "IsArrayBuffer"
    - path: ".planning/debug/resolved/firefox-request-xray-hole.md"
      provides: "Final resolved, git-tracked closure record for XBR-02"
  key_links:
    - from: "extension/entrypoints/__tests__/page-bridge-firefox.test.ts"
      to: "extension/entrypoints/page-bridge-firefox.ts"
      via: "imports pageBridgeFirefoxDefinition, exercises installPatch()/shapeCredential() through the real navigator.credentials.get()/create() promise flow"
      pattern: "page-bridge-firefox"
    - from: ".planning/STATE.md"
      to: ".planning/debug/resolved/firefox-request-xray-hole.md"
      via: "Deferred Items table row + Blockers/Concerns entry mirror the doc's new resolved status and path"
      pattern: "resolved"
---

<objective>
Close out XBR-02: add deterministic jsdom regression coverage for the MAIN-world response-direction fix (Plan 14-02), upgrade `probe-request-xray.cjs` from an unasserted diagnostic capture into a hard PASS/FAIL gate on real Firefox, then confirm the FULL phase gate suite (both requirements, both browsers) is green and perform record hygiene on `.planning/debug/firefox-request-xray-hole.md`.

Purpose: a fix without a permanent regression gate can silently regress; this plan turns Plan 14-02's evidence-driven fix into two independent, permanent proofs (CI-safe jsdom + live-Firefox), then closes the loop on the tracking doc CONTEXT.md required never be lost again.

Output: `extension/entrypoints/__tests__/page-bridge-firefox.test.ts` (new), an upgraded `extension/e2e-firefox/probe-request-xray.cjs`, `.planning/debug/resolved/firefox-request-xray-hole.md` (moved, resolved), and `.planning/STATE.md` mirroring the closure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/firefox-request-xray-hole.md
@extension/entrypoints/page-bridge-firefox.ts
@extension/entrypoints/__tests__/page-bridge.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: page-bridge-firefox.test.ts — jsdom regression coverage</name>
  <files>extension/entrypoints/__tests__/page-bridge-firefox.test.ts</files>
  <read_first>
    extension/entrypoints/__tests__/page-bridge.test.ts (full file — Chrome twin's established pattern: the `// @vitest-environment jsdom` header, `installFakeCredentialsContainer()`/`spyOnOutgoingPostMessage()`/`dispatchRelayResponse()`/`lastRequestEnvelope()` helpers, `beforeEach`/`afterEach` DOM-marker cleanup, and the "credential success" describe block at lines 352-388 — the exact end-to-end technique to mirror)
    extension/entrypoints/__tests__/content-relay.test.ts, lines 611-671 (crossRealmArrayBuffer() hidden-iframe helper and its first test's structure — the technique for constructing a genuinely cross-realm ArrayBuffer inside jsdom, to reuse verbatim)
    extension/entrypoints/page-bridge-firefox.ts (Plan 14-02 Task 2's FIXED file — read the new shapeCredential()/b64UrlToArrayBuffer to know exactly which fields it re-materializes and from which credentialJson keys)
  </read_first>
  <action>
    Create extension/entrypoints/__tests__/page-bridge-firefox.test.ts, mirroring page-bridge.test.ts's structure verbatim: the same `// @vitest-environment jsdom` header, the same `installFakeCredentialsContainer()`/`spyOnOutgoingPostMessage()`/`dispatchRelayResponse()`/`lastRequestEnvelope()` helpers (duplicated into this new file — each test file in this codebase owns its own copy, never cross-imports another test file's helpers), the same `beforeEach`/`afterEach` DOM-marker cleanup — but importing `pageBridgeFirefoxDefinition, { isPermissionsPolicyBlocked }` from `../page-bridge-firefox` (its default export is `{ main: () => void }` per `defineUnlistedScript`'s runtime shape — cast identically to how page-bridge.test.ts casts `pageBridgeDefinition`, since `defineUnlistedScript(fn)` returns `{ main: fn }`).

    Add a `describe("response-direction MAIN-world re-materialization (Firefox Xray hole fix)", ...)` block that: (1) copies content-relay.test.ts's `crossRealmArrayBuffer(bytes: number[])` hidden-iframe helper verbatim into this new file; (2) dispatches a `kind: "credential"` response via `dispatchRelayResponse()` where `credential.rawId` and `credential.response.clientDataJSON` are cross-realm `ArrayBuffer`s built via `crossRealmArrayBuffer()`, and `credentialJson.rawId`/`credentialJson.response.clientDataJSON` are the MATCHING base64url STRING encodings of the exact same byte arrays (`Buffer.from(bytes).toString("base64url")`); (3) awaits the `navigator.credentials.get()` promise the dispatched response resolves and asserts the RESOLVED credential's `rawId instanceof ArrayBuffer` is `true` in THIS test file's own realm (proving MAIN-world re-materialization actually reconstructed the value — a merely-passed-through cross-realm value would fail this exact assertion, the same signature the debug doc's own real-Firefox evidence used) AND that `new Uint8Array(result.rawId)` reads the exact original bytes, then repeats both assertions for `result.response.clientDataJSON`; (4) adds one control test confirming a field `credentialJson` does not cover (e.g. `id`/`type`) still passes through unchanged from `credential`, proving the fix is additive to the existing spread, not a full-object replacement; (5) adds one control test using a `create()` flow (not `get()`) with `credentialJson.response.attestationObject` set, asserting `result.response.attestationObject instanceof ArrayBuffer` is `true` — covers the create()-only field the get()-focused tests above do not exercise.
  </action>
  <acceptance_criteria>
    - `cd extension && npx vitest run --reporter=dot entrypoints/__tests__/page-bridge-firefox.test.ts` passes, with at least 4 test cases in the new describe block.
    - `grep -c "crossRealmArrayBuffer" extension/entrypoints/__tests__/page-bridge-firefox.test.ts` >= 1 (the cross-realm technique is actually used, not a same-realm-only control file).
    - `grep -c "instanceof ArrayBuffer" extension/entrypoints/__tests__/page-bridge-firefox.test.ts` >= 2 (both `rawId` and `response.clientDataJSON`/`attestationObject` are asserted, not just one field).
    - Full extension suite: `cd extension && npx vitest run` reports a total >= 651 + this task's new test count, with 0 unexpected failures.
  </acceptance_criteria>
  <verify>
    <automated>cd extension && npx vitest run --reporter=dot entrypoints/__tests__/page-bridge-firefox.test.ts</automated>
  </verify>
  <done>A new jsdom test file deterministically proves the MAIN-world re-materialization fix for rawId, clientDataJSON, and attestationObject, using the same cross-realm-iframe technique already proven for the request direction.</done>
</task>

<task type="auto">
  <name>Task 2: probe-request-xray.cjs — hard-gate the response-direction assertions</name>
  <files>extension/e2e-firefox/probe-request-xray.cjs</files>
  <read_first>
    extension/e2e-firefox/probe-request-xray.cjs (full file — header comment lines 1-70; create() executeScript+record block lines 236-296; get() executeScript+record block lines 298-359)
    extension/entrypoints/page-bridge-firefox.ts (Plan 14-02 Task 2's fixed shapeCredential() — confirms exactly which fields/instanceof to assert)
    .planning/debug/firefox-request-xray-hole.md (Plan 14-02 Task 1's evidence entry)
  </read_first>
  <action>
    Update this file's header comment (the block currently documenting the response-direction check as an unasserted, expected-to-fail gap pending a follow-up investigation) to state plainly that the response-direction realm contract is now fixed and hard-gated, citing `.planning/debug/resolved/firefox-request-xray-hole.md` by its new path.

    In the create() `driver.executeScript()` block, extend the existing capture (currently only `rawIdIsArrayBuffer`/`rawIdToStringTag`/`rawIdCtorName`/`rawIdIsView`) to ALSO compute the SAME battery — `instanceof ArrayBuffer`, `Object.prototype.toString.call(...)`, `.constructor.name`, `ArrayBuffer.isView(...)` — for `cred.response.clientDataJSON` and `cred.response.attestationObject`, storing them on `window.__pv_xray_create` as `clientDataJSONIsArrayBuffer`/`clientDataJSONToStringTag`/`attestationObjectIsArrayBuffer`/`attestationObjectToStringTag` (mirror the existing `rawId*` naming convention exactly). Mirror this for the get() block's `window.__pv_xray_get`: add `clientDataJSONIsArrayBuffer`/`authenticatorDataIsArrayBuffer`/`signatureIsArrayBuffer` (+ matching `*ToStringTag`) captures.

    Change the `record("XRAY-CREATE", ...)`/`record("XRAY-GET", ...)` PASS/FAIL logic (currently gating ONLY on `challengeMatches`, the request-direction byte-identity check) to ALSO require every one of these new `*IsArrayBuffer` fields to be `true` before recording PASS — a run where ANY response-direction field's `instanceof ArrayBuffer` is `false` now records FAIL, with a `notes` string naming the specific failing field(s), never a silent pass with an unasserted caveat.

    Then run the live probe end to end: `cd extension && npm run build:firefox`; confirm `pv-server` is reachable on `:8620`; then `node e2e-firefox/probe-request-xray.cjs` (from `extension/`). Confirm the resulting `e2e-firefox/.ff-screenshots-probe-request-xray/results-probe-request-xray.json` shows both `XRAY-CREATE` and `XRAY-GET` as `PASS`.
  </action>
  <acceptance_criteria>
    - `grep -c "IsArrayBuffer" extension/e2e-firefox/probe-request-xray.cjs` >= 8 (rawId + clientDataJSON + attestationObject for create; rawId + clientDataJSON + authenticatorData + signature for get — capture assignment plus PASS/FAIL gate reference for each).
    - `grep -c "resolved/firefox-request-xray-hole.md" extension/e2e-firefox/probe-request-xray.cjs` >= 1 (header comment cites the resolved doc's new location).
    - Live run produces `results-probe-request-xray.json` with `"XRAY-CREATE"` and `"XRAY-GET"` both showing `"status": "PASS"`.
  </acceptance_criteria>
  <verify>
    <automated>cd extension && npm run build:firefox && node e2e-firefox/probe-request-xray.cjs</automated>
  </verify>
  <done>probe-request-xray.cjs hard-asserts response-direction realm identity for every binary field on both create() and get(), and a live Firefox run confirms both rows PASS.</done>
</task>

<task type="auto">
  <name>Task 3: Full gate suite confirmation + record hygiene</name>
  <files>.planning/debug/firefox-request-xray-hole.md, .planning/debug/resolved/firefox-request-xray-hole.md, .planning/STATE.md</files>
  <read_first>
    .planning/debug/firefox-request-xray-hole.md (current frontmatter `status:` field and full `## Resolution` section structure — root_cause/fix/verification/files_changed keys)
    .planning/debug/resolved/firefox-injection-csp-blocked.md (repo convention precedent for a resolved doc's final shape and location)
    .planning/STATE.md (`### Blockers/Concerns` section — the existing `**RESOLVED 2026-07-17**` bullet's style, two lines above the bullet documenting this issue; `## Deferred Items` table — the `uat | Phase 07 container/proxy E2E...` row's `resolved` Status value, to mirror)
  </read_first>
  <action>
    Run the full phase-closing gate suite and record each command's actual output for the SUMMARY: `cd extension && npx vitest run` (baseline 651 + Task 1's new page-bridge-firefox.test.ts cases, 0 unexpected failures — the 1 known pre-existing unrelated ServerConfigView.tsx rejection, if still present on `main`, is the only allowed pre-existing red); `cd extension && npx tsc --noEmit`; `cd extension && npm run build:chrome && npm run build:firefox`; `bash scripts/audit-mainworld-boundary.sh` (exit 0); `node extension/e2e-firefox/run-core.cjs` (17 PASS + 1 OBSERVED, 0 FAIL); `node extension/e2e-firefox/run-server-unlock.cjs` (15 PASS / 2 INFO / 0 FAIL); `node extension/e2e-firefox/probe-request-xray.cjs` (all rows PASS — re-confirms Task 2's upgrade still holds); `cd extension && npx playwright test --project=chromium-ceremony` (5/5, headed — the Chrome regression proof CONTEXT.md requires); `cargo test --workspace` (green, includes Plan 14-01's new `real_rp_verification` test).

    Only after every gate above is confirmed green: `git mv .planning/debug/firefox-request-xray-hole.md .planning/debug/resolved/firefox-request-xray-hole.md`. Edit the moved doc's frontmatter `status:` field from `awaiting_human_verify` to `resolved`. In its `## Resolution` section, add a new subsection for the RESPONSE direction (mirroring the existing REQUEST-direction `root_cause:`/`fix:`/`verification:`/`files_changed:` structure) citing: Plan 14-02 Task 1's differential-probe root-cause evidence entry, Plan 14-02 Task 2's exact fix, this plan's Task 1 jsdom regression test and Task 2 upgraded live-Firefox probe as verification, and Plan 14-01's new `cargo test -p pv-provider --test real_rp_verification` as the independent QA-03 closure evidence this same doc's "byte-level proof" bar also cites. Explicitly preserve the existing `awaiting_human_verify` text's honesty: add a note that Bartek's live github.com retest of the ORIGINAL request-direction fix remains open at his leisure (per CONTEXT.md — never mark it done on his behalf), and that this phase's automated evidence (webauthn-rs round-trip + upgraded probe + jsdom tests) is the in-repo substitute closure evidence for the response direction specifically, not a claim the live retest happened.

    In `.planning/STATE.md`'s `### Blockers/Concerns` section, replace the bullet documenting this issue as an open, Phase-14-targeted item with a new bullet in the SAME style as the existing `**RESOLVED 2026-07-17**` entry nearby (bold `**RESOLVED <execution-date>**:` prefix, then 2-4 sentences summarizing root cause, fix, and gate evidence, noting the doc's new path under `.planning/debug/resolved/`).

    In `.planning/STATE.md`'s `## Deferred Items` table, change the Firefox-Xray-hole row's Status column value from its current targeted-but-open value to `resolved` — matching the value already used by the Phase-07 container/proxy E2E row in the same table.
  </action>
  <acceptance_criteria>
    - `git log --oneline -- .planning/debug/resolved/firefox-request-xray-hole.md` shows at least 1 commit.
    - `test -f .planning/debug/firefox-request-xray-hole.md` exits 1 (old path gone); `test -f .planning/debug/resolved/firefox-request-xray-hole.md` exits 0.
    - The moved doc's frontmatter contains `status: resolved`.
    - `grep -c "awaiting_human_verify" .planning/debug/resolved/firefox-request-xray-hole.md` >= 1 (the honest open-item note is preserved, not deleted).
    - `.planning/STATE.md`'s Deferred Items table row for the Firefox Xray hole contains `resolved` in its Status column.
    - `.planning/STATE.md`'s Blockers/Concerns section contains a new bullet beginning `- **RESOLVED` that references `firefox-request-xray-hole`.
    - All 9 gate commands above were actually run in this task, with their real output recorded in this plan's SUMMARY.md (not asserted from memory).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <done>The full phase gate suite is confirmed green; the debug doc is git-tracked, resolved, and moved; STATE.md mirrors the closure while honestly preserving the still-open human-verification note.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `extension/entrypoints/__tests__/page-bridge-firefox.test.ts` (new file) — deterministic jsdom regression coverage for the response-direction fix (new `describe("response-direction MAIN-world re-materialization (Firefox Xray hole fix)", ...)` block, >= 4 test cases, reused `crossRealmArrayBuffer` technique).
- `extension/e2e-firefox/probe-request-xray.cjs` — new `*IsArrayBuffer`/`*ToStringTag` capture fields and hard PASS/FAIL gating on `XRAY-CREATE`/`XRAY-GET`.
- `.planning/debug/resolved/firefox-request-xray-hole.md` — final resolved location (moved from `.planning/debug/firefox-request-xray-hole.md`), new RESPONSE-direction Resolution subsection.
- `.planning/STATE.md` — updated Blockers/Concerns bullet and Deferred Items row (both flipped to resolved).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| jsdom test realm ↔ hidden-iframe realm | Test-only boundary reproducing the real Firefox Xray hazard deterministically; no production trust boundary crossed |
| Live-Firefox probe ↔ real extension build | The probe drives the REAL, built `.output/firefox-mv2` artifact — its own trust boundary (RP page vs. extension) is identical to production, unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06 | Tampering | page-bridge-firefox.test.ts / probe-request-xray.cjs (test-only code) | low | accept | Test/harness code only, never shipped to the extension bundle; does not affect production trust boundaries |
| T-14-07 | Tampering | any throwaway differential-probe artifact from Plan 14-02 Task 1 | low | mitigate | This plan's own acceptance criteria confirm no uncommitted scratch harness was accidentally committed (Plan 14-02's own `git status --porcelain` check already gates this; this plan's `files_modified` list is the sole source of truth for what should be tracked) |
| T-14-SC | Tampering | npm/cargo installs | n/a | accept | No new package introduced by this plan |

</threat_model>

<verification>
Task 1: `cd extension && npx vitest run` — full suite green (>= 651 baseline + new tests). Task 2: live `node e2e-firefox/probe-request-xray.cjs` run — `XRAY-CREATE`/`XRAY-GET` both PASS. Task 3 (full phase gate suite, all must be green): `cd extension && npx vitest run`; `cd extension && npx tsc --noEmit`; `cd extension && npm run build:chrome && npm run build:firefox`; `bash scripts/audit-mainworld-boundary.sh`; `node extension/e2e-firefox/run-core.cjs` (17 PASS + 1 OBSERVED); `node extension/e2e-firefox/run-server-unlock.cjs` (15 PASS/2 INFO/0 FAIL); `node extension/e2e-firefox/probe-request-xray.cjs` (all PASS); `cd extension && npx playwright test --project=chromium-ceremony` (5/5, headed); `cargo test --workspace` (green).
</verification>

<success_criteria>
XBR-02 fully closed with byte-level, live-verified proof on both browsers; QA-03 (Plan 14-01) independently confirmed green in the same gate pass; the debug doc is git-tracked, resolved, and relocated; STATE.md mirrors the closure while the still-open human-verification note (Bartek's real github.com retest) stays honestly recorded, not fabricated as done.
</success_criteria>

<output>
Create `.planning/phases/14-critical-risk-closure-cross-realm-integrity-real-rp-verifica/14-03-SUMMARY.md` when done
</output>
