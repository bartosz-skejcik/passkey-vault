---
phase: 17-shared-component-visual-alignment
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - packages/pv-ui/components/ItemIconTile.tsx
  - web/src/components/vault/ItemIconTile.tsx
  - extension/entrypoints/popup/ItemIconTile.tsx
autonomous: true
requirements: [DS-03]

must_haves:
  truths:
    - "packages/pv-ui/components/ItemIconTile.tsx is the sole implementation containing `function ItemIconTile`/`export default function ItemIconTile` anywhere in the repo -- web's and the popup's own copies are both re-export/wrapper shims, verified by grep, not assumed. (DS-03, ROADMAP Phase 17 success criterion 1: 'no second implementation remains')"
    - "web/src/components/vault/ItemRow.test.tsx and extension/entrypoints/popup/ItemIconTile.test.tsx both pass with ZERO test-file edits -- proving the shim is behavior-neutral: favicon-first tile, card-brand short-circuit, type-glyph fallback, and both `variant` sizes all render identically to before the promotion."
    - "The favicon `<img>`'s direct-to-hostname `src` and `referrerPolicy=\"no-referrer\"` are preserved byte-for-byte in the promoted component -- never a third-party favicon proxy, never routed through pv-server (zero-knowledge invariant, this repo's CLAUDE.md Constraints)."
  artifacts:
    - "packages/pv-ui/components/ItemIconTile.tsx (new, ~185 LOC -- the promoted superset, variant: \"row\" | \"header\")"
    - "web/src/components/vault/ItemIconTile.tsx (re-export shim, 1 line + header comment)"
    - "extension/entrypoints/popup/ItemIconTile.tsx (thin wrapper shim pinning variant=\"row\")"
  key_links:
    - "packages/pv-ui/components/ItemIconTile.tsx's imports of pv-ui/vault/types, pv-ui/vault/search, pv-ui/vault/cardBrand -> the Phase-16-canonical modules directly, never through either consumer's own shim -- confirms DS-03 builds on DS-01's completed foundation rather than re-introducing an indirection layer"
    - "web/src/components/vault/ItemRow.tsx, DetailPanel.tsx, and extension/entrypoints/popup/ItemListView.tsx's existing `import ItemIconTile from \"./ItemIconTile\"` lines -> unchanged, zero-churn -- all 3 importers keep resolving to the exact same relative path, which now points at a shim instead of the full implementation"
  prohibitions:
    - "TILE_BG/TILE_FG are never converted to var(--pv-tile-bg)/var(--pv-tile-fg) inside the promoted .tsx component -- they stay Tailwind arbitrary-variant strings, per UI-SPEC.md's explicit anti-pattern (this differs from Plan 17-02's in-page CSS fix, which is a separate, token-based consumption path for a component-free surface)"
    - "No crypto/wasm/argon2/chacha/hkdf/derive/decrypt/prf import appears anywhere in packages/pv-ui/components/ItemIconTile.tsx"
    - "The favicon fetch is never routed through pv-server or any third-party favicon-proxy domain (google/gstatic/s2/duckduckgo/proxy)"
---

<assumption_delta_decision>
Signal: the assumption-delta scanner detected pluralization language ("no second implementation
remains", ROADMAP Phase 17 success criterion 1) around this plan's shared-component generalization.
Decision: promote. packages/pv-ui/components/ItemIconTile.tsx becomes the primary implementation
(the promoted superset, `variant: "row" | "header"`, per web's own pre-existing shape); both
web/src/components/vault/ItemIconTile.tsx and extension/entrypoints/popup/ItemIconTile.tsx demote
to thin shims (re-export / variant="row" wrapper respectively), per CONTEXT.md's own locked
architecture discretion (Claude's Discretion, "Promote web's ItemIconTile.tsx... Both import sites
become shims/imports of the shared component"). No second full implementation survives outside
packages/pv-ui/components/ after this plan.
</assumption_delta_decision>

<objective>
Promote `web/src/components/vault/ItemIconTile.tsx` -- already the superset implementation
(`variant: "row" | "header"`) -- to `packages/pv-ui/components/ItemIconTile.tsx`, folding in the
extension's two small deltas (`FAVICON_URL_PREFIX` const, defensive `Array.isArray(urls)` guard),
then collapse both consumers' own copies into thin shims. This is DS-03: the flagship proof that
`pv-ui`'s React-sharing pipeline (unblocked by Plan 17-01's peer-dependency infra) works end-to-end
on a real, already-shipped component -- not a toy. Both consumers' existing test suites
(`ItemRow.test.tsx`, `ItemIconTile.test.tsx`) exercise the exact same favicon/card-brand/fallback
behavior today; this plan's whole correctness bar is that neither test file needs a single edit to
keep passing after the promotion.

**Flagged planner assumption (DS-03, edge-coverage category "unclassified"):** this plan relies on
Plan 17-01 having already proven, live, that a real .tsx importing both `react` and `lucide-react`
resolves to a single, non-duplicated instance and generates real Tailwind CSS in both consumers --
this plan's own component promotion is the FIRST real (non-smoke) exercise of that infrastructure,
and its own green test/build gate is additional confirmation of the same guarantee for the actual
production component, not merely the throwaway probe.

Purpose: prove the pv-ui React-sharing pipeline end-to-end on the smallest real component before any
future component (ItemRow, DetailPanel, dialogs -- explicitly deferred, research "Phase D") attempts
the same promotion.
Output: one canonical `ItemIconTile` implementation in `packages/pv-ui/components/`; both prior
implementations become zero-behavior-change shims.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-shared-component-visual-alignment/17-CONTEXT.md
@.planning/phases/17-shared-component-visual-alignment/17-RESEARCH.md
@.planning/phases/17-shared-component-visual-alignment/17-PATTERNS.md
@.planning/phases/17-shared-component-visual-alignment/17-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create packages/pv-ui/components/ItemIconTile.tsx (promotion + extension deltas)</name>
  <files>packages/pv-ui/components/ItemIconTile.tsx</files>
  <read_first>
    web/src/components/vault/ItemIconTile.tsx (full file, 179 lines -- the exact promotion source, the superset with variant/CardBrandTile)
    extension/entrypoints/popup/ItemIconTile.tsx (full file, 182 lines -- the two deltas to fold in: FAVICON_URL_PREFIX const at line 33, the Array.isArray(urls) guard at line 55)
    packages/pv-ui/vault/types.ts (VaultItem/ItemType/LoginFields/PasskeyFields/CardFields shapes -- confirms the exact fields this component reads)
    packages/pv-ui/vault/search.ts (domainFromUrl's exact export signature)
    packages/pv-ui/vault/cardBrand.ts (detectCardBrand/CardBrand's exact export signature)
    17-UI-SPEC.md "## Component Contract -- ItemIconTile (DS-03)" table (the authoritative target shape)
  </read_first>
  <behavior>
    - Rendering a login/passkey VaultItem with a resolvable hostname/rpId renders a direct
      `<img src="https://{hostname}/favicon.ico">` with `referrerPolicy="no-referrer"`, never a
      third-party proxy.
    - Rendering a card VaultItem whose number resolves to a known brand (visa/mastercard/amex/discover)
      renders that brand's CardBrandTile instead of any favicon/type-glyph.
    - Rendering any other item type, or a login/passkey with no resolvable hostname, or a card with
      no detected brand, falls back to the neutral per-ItemType glyph tile (TYPE_ICON map).
    - A favicon `<img>` firing `onError` permanently caches the hostname in the module-level
      FAILED_FAVICON_HOSTS set and swaps to the type-glyph tile without re-attempting the broken URL
      on re-render.
    - `variant="header"` renders the smaller h-6/w-6/rounded-[6px]/14px-icon frame; the default
      `variant="row"` (or omitted) renders h-8/w-8/rounded-[8px]/18px-icon.
    - A login item whose `fields.urls` is missing/non-array (legacy/un-normalized shape) resolves to
      "no hostname" rather than throwing.
  </behavior>
  <action>
    Create packages/pv-ui/components/ItemIconTile.tsx as a near-verbatim promotion of web's
    ItemIconTile.tsx (179 LOC), with exactly these changes from the web source:
    - Change the three type/logic imports to `pv-ui/vault/types`, `pv-ui/vault/search`,
      `pv-ui/vault/cardBrand` (bare pv-ui subpaths, per CONTEXT.md's discretion -- NOT through
      either consumer's own shim, and NOT the web source's `@/lib/vault/*` relative paths).
    - Keep the `"use client";` directive as the literal first line -- required (RESEARCH.md: a
      probe component omitting it hit Next's Server-Component boundary error immediately).
    - Keep `TILE_BG`/`TILE_FG` as the exact same two Tailwind arbitrary-variant strings from the web
      source, byte-for-byte -- never convert them to `var(--pv-tile-bg)`/`var(--pv-tile-fg)`.
    - Fold in extension's delta A: add `const FAVICON_URL_PREFIX = "https://";` (matching the
      extension source's own const + comment explaining it dodges `server-config.test.ts`'s
      hard-coded-URL guard) and use it in the favicon `<img src={...}>` template literal in place
      of the web source's bare `https://` prefix.
    - Fold in extension's delta B: in `faviconHostnameFor()`'s login branch, replace the web
      source's bare `item.fields.urls.find(...)` with the extension source's defensive
      `const urls = Array.isArray(item.fields.urls) ? item.fields.urls : []; const url =
      urls.find((u) => u.trim() !== "");` guard.
    - Keep every other line -- SIZE map, `variant` prop/default, the favicon `<img>`'s
      `referrerPolicy="no-referrer"`/`loading="lazy"`/`onError` handling, `CardBrandTile` (all 4
      brand branches, pure CSS + inline SVG) -- byte-identical to the web source.
  </action>
  <acceptance_criteria>
    packages/pv-ui/components/ItemIconTile.tsx exists, starts with `"use client";`, imports only
    from `react`, `lucide-react`, `pv-ui/vault/types`, `pv-ui/vault/search`, `pv-ui/vault/cardBrand`
    (no relative `../` or `@/` imports), exports a default function component accepting
    `{ item: VaultItem; variant?: "row" | "header" }`, and contains both `FAVICON_URL_PREFIX` and
    the `Array.isArray(urls)` guard. `TILE_BG`/`TILE_FG` string literals are byte-identical to web's
    source. `referrerPolicy="no-referrer"` is present on the favicon `<img>`.
  </acceptance_criteria>
  <verify>
    <automated>[ -f packages/pv-ui/components/ItemIconTile.tsx ] && head -1 packages/pv-ui/components/ItemIconTile.tsx | grep -q '"use client"' && grep -q 'from "pv-ui/vault/types"' packages/pv-ui/components/ItemIconTile.tsx && grep -q 'from "pv-ui/vault/search"' packages/pv-ui/components/ItemIconTile.tsx && grep -q 'from "pv-ui/vault/cardBrand"' packages/pv-ui/components/ItemIconTile.tsx && grep -q 'FAVICON_URL_PREFIX = "https://"' packages/pv-ui/components/ItemIconTile.tsx && grep -q 'Array.isArray(item.fields.urls)' packages/pv-ui/components/ItemIconTile.tsx && grep -q 'referrerPolicy="no-referrer"' packages/pv-ui/components/ItemIconTile.tsx && [ -z "$(grep -E '^import' packages/pv-ui/components/ItemIconTile.tsx | grep -v -E 'from \"(react|lucide-react|pv-ui/vault/types|pv-ui/vault/search|pv-ui/vault/cardBrand)\"')" ] && (cd web && npx tsc --noEmit) && (cd extension && npx tsc --noEmit)</automated>
  </verify>
  <done>packages/pv-ui/components/ItemIconTile.tsx is the promoted superset with both extension deltas folded in, imports only from pv-ui subpaths + react/lucide-react, and both consumers' tsc --noEmit are green against it (even before the shims in Task 2 point at it, since neither consumer imports it yet -- this confirms the file itself is syntactically/type valid standing alone).</done>
</task>

<task type="auto">
  <name>Task 2: Collapse both consumer copies into shims + zero-duplication + zero-knowledge close-out</name>
  <files>web/src/components/vault/ItemIconTile.tsx, extension/entrypoints/popup/ItemIconTile.tsx</files>
  <read_first>
    web/src/lib/vault/cardBrand.ts (the exact Phase-16 `export *` shim comment/template this task's web shim follows, adapted for a default export)
    extension/lib/vault/cardBrand.ts (the same Phase-16 shim template, extension side)
    web/src/components/vault/ItemRow.tsx, DetailPanel.tsx (confirm both import `ItemIconTile` from `"./ItemIconTile"` -- this task must NOT touch either importer)
    extension/entrypoints/popup/ItemListView.tsx (confirm it imports `ItemIconTile` from `"./ItemIconTile"` -- must NOT be touched)
    web/src/components/vault/ItemRow.test.tsx, extension/entrypoints/popup/ItemIconTile.test.tsx (the two test files that must pass with ZERO edits after this task)
    17-UI-SPEC.md "## Component Contract" table, `web/`/`extension/` import-site rows (the exact target shim shapes)
  </read_first>
  <action>
    Replace web/src/components/vault/ItemIconTile.tsx's ENTIRE content with a short header comment
    (mirroring web/src/lib/vault/cardBrand.ts's own shim-comment style, citing DS-03/plan 17-03)
    followed by exactly one line: `export { default } from "pv-ui/components/ItemIconTile";` -- the
    default-export variant of the Phase 16 `export *` shim template.
    Replace extension/entrypoints/popup/ItemIconTile.tsx's ENTIRE content with a thin wrapper (not
    a bare re-export, since it must pin `variant="row"`): import the default `ItemIconTile` from
    `"pv-ui/components/ItemIconTile"`, import the `VaultItem` type from `"pv-ui/vault/types"` (NOT
    the popup's own `"../../lib/vault/types"` -- direct pv-ui import, per Phase-16 convention),
    and export a default function named `ItemIconTile` (same exported name -- so
    ItemListView.tsx's existing `import ItemIconTile from "./ItemIconTile"` line needs zero edits)
    accepting `{ item: VaultItem }` that renders `<ItemIconTile item={item} variant="row" />`.
    Do NOT modify web/src/components/vault/ItemRow.tsx, DetailPanel.tsx, or
    extension/entrypoints/popup/ItemListView.tsx -- all 3 importers keep their existing
    `./ItemIconTile` import path unchanged (zero churn). Do NOT modify
    web/src/components/vault/ItemRow.test.tsx or extension/entrypoints/popup/ItemIconTile.test.tsx
    -- both must pass unedited, which is this task's own proof that the shim is behavior-neutral.
  </action>
  <acceptance_criteria>
    web/src/components/vault/ItemIconTile.tsx is a single-line re-export shim. extension/entrypoints/popup/ItemIconTile.tsx
    is a thin wrapper pinning `variant="row"` and importing `VaultItem` from `pv-ui/vault/types`.
    Neither test file is modified (git diff empty for both). Both full test suites and both `tsc
    --noEmit`/build chains pass. A repo-wide grep finds zero `function ItemIconTile` /
    `export default function ItemIconTile` definitions anywhere outside
    packages/pv-ui/components/.
  </acceptance_criteria>
  <verify>
    <automated>(cd web && npx vitest run src/components/vault/ItemRow.test.tsx && npx tsc --noEmit && npx next build) && (cd extension && npx vitest run entrypoints/popup/ItemIconTile.test.tsx && npx tsc --noEmit && npx wxt build -b chrome) && [ -z "$(grep -rn 'function ItemIconTile' web/src extension/entrypoints --include=*.tsx | grep -v packages/pv-ui)" ] && grep -q 'referrerPolicy="no-referrer"' packages/pv-ui/components/ItemIconTile.tsx && [ -z "$(grep -iE 'google|gstatic|s2/favicons|duckduckgo|proxy' packages/pv-ui/components/ItemIconTile.tsx)" ] && [ -z "$(grep -iE '^import' packages/pv-ui/components/ItemIconTile.tsx | grep -iE 'wasm|argon2|chacha|hkdf|derive|decrypt|prf')" ]</automated>
  </verify>
  <done>web/'s and the popup's own ItemIconTile.tsx are both zero-behavior-change shims; both pre-existing test files pass unedited; a repo-wide grep confirms zero remaining second implementation; the zero-knowledge favicon rule and pv-ui's crypto-free boundary are both grep-confirmed on the new canonical component. DS-03's ROADMAP success criterion 1 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| packages/pv-ui/components (new, first React surface in pv-ui) vs. both consumer apps | The promoted component is consumed identically by web (Next.js/Turbopack, client component) and the extension popup (WXT/Vite, React) -- same trust level as the two pre-promotion implementations it replaces, no new privilege boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-17-07 | Information Disclosure | packages/pv-ui/components/ItemIconTile.tsx favicon fetch | high | mitigate | Task 2's automated verify greps the promoted component for `referrerPolicy="no-referrer"` presence and for the absence of any known third-party favicon-proxy domain substring (google/gstatic/s2/duckduckgo/proxy) -- the zero-knowledge invariant must survive the promotion byte-for-byte, not just by design intent |
| T-17-08 | Tampering | packages/pv-ui crypto-free boundary | high | mitigate | Task 2's automated verify greps every `^import` line of the new component for crypto-surface keywords (wasm/argon2/chacha/hkdf/derive/decrypt/prf) -- pv-ui's first React component must not be the one that quietly imports a crypto/derive module |
| T-17-09 | Tampering | web/extension shim files (ItemIconTile.tsx) | medium | mitigate | Both pre-existing test files (ItemRow.test.tsx, ItemIconTile.test.tsx) run unedited against the shims and must pass -- any behavior drift introduced by the shim indirection is caught by tests that predate this plan, not tests this plan wrote to match its own output |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan (Plan 17-01 already resolved the peer-dependency install) |
</threat_model>

<verification>
Both consumers' targeted test file + tsc + build chain are green; the repo-wide zero-duplication
grep and the two zero-knowledge/crypto-free grep gates all pass.
</verification>

<success_criteria>
ItemIconTile exists exactly once as a React component in pv-ui, imported by both the web app and
the extension popup -- no second implementation remains (ROADMAP Phase 17 success criterion 1,
DS-03).
</success_criteria>

## Artifacts This Phase Produces (this plan's slice)

**Created:**
- `packages/pv-ui/components/ItemIconTile.tsx` -- the promoted canonical component (~185 LOC), exports `default function ItemIconTile({ item, variant })`

**Modified:**
- `web/src/components/vault/ItemIconTile.tsx` -- collapsed to a 1-line re-export shim
- `extension/entrypoints/popup/ItemIconTile.tsx` -- collapsed to a thin wrapper pinning `variant="row"`

**Deleted:** none (both prior files are replaced in place, not removed -- their import paths stay
live for the 3 existing importers).

<output>
Create `.planning/phases/17-shared-component-visual-alignment/17-03-SUMMARY.md` when done
</output>
