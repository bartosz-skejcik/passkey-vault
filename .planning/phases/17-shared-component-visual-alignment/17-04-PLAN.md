---
phase: 17-shared-component-visual-alignment
plan: 04
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified:
  - extension/e2e-visual/capture-tile-parity.mjs
  - extension/package.json
autonomous: true
requirements: [DS-03, DS-04, UX-01]

must_haves:
  truths:
    - "Both consumers' full build+test+typecheck chains (web: vitest/tsc/next build; extension: vitest/tsc/wxt build/wxt build -b firefox) are green with zero regressions after all three prior plans land."
    - "A repo-wide grep confirms zero remaining ItemIconTile implementations outside packages/pv-ui/components, and zero crypto-surface import lines anywhere in packages/pv-ui/{vault,i18n,components,clipboard.ts} -- both closing proofs for DS-03/DS-04 in their strongest, most literal form (mirrors Phase 16's own closing-gate plan, 16-06-PLAN.md Task 2)."
    - "An automated computed-style comparison proves the in-page tile's rendered background color numerically matches the web/popup tile's rendered background color, in both vault-dark and vault-light -- not inferred from a screenshot alone. (UX-01 adjacency: all three surfaces read the SAME token values -- --pv-tile-bg/--pv-tile-fg from tokens.css -- grep-verifiable single source, plus this browser-level cross-check)"
    - "8 screenshots (4 surfaces x 2 themes: web ItemRow, web DetailPanel header, popup list row, in-page Surface A dropdown, in-page Surface B prompt -- 5 surfaces would be 10, but DetailPanel and ItemRow share one theme pass each per the capture script's own grouping; script decides exact count, floor is 8) exist under .planning/phases/17-shared-component-visual-alignment/uat-screenshots/ for Bartek's own visual-taste review at the end-of-phase UAT gate."
    - statement: "A missing/failed favicon still falls back to the neutral type-glyph on the same light tile on every surface -- existing fallback preserved, suites pass unchanged."
      verification: "backstop"
    - statement: "The in-page dropdown/prompt tiles visually match web's/popup's tile rendering pixel-for-pixel (not just numerically-equal computed background color) in both themes."
      verification: "backstop"
    - statement: "The .pv-list scroll-cap affordance (max-height 250px, ~4.5 rows) survives the tile CSS swap unchanged."
      verification: "backstop"
  artifacts:
    - "extension/e2e-visual/capture-tile-parity.mjs (new standalone Playwright screenshot + computed-style harness)"
    - "extension/package.json: +test:e2e:visual, +pretest:e2e:visual npm scripts"
    - ".planning/phases/17-shared-component-visual-alignment/uat-screenshots/*.png (8+ files, local evidence, not committed to git per this project's established 260718-0qi/11-* precedent)"
  key_links:
    - "packages/pv-ui/tokens.css's --pv-tile-bg/--pv-tile-fg (Plan 17-02) + packages/pv-ui/components/ItemIconTile.tsx's TILE_BG/TILE_FG Tailwind classes (Plan 17-03) -> this plan's own computed-style cross-check -- the SAME resolved color reaching two structurally different consumption mechanisms (a Tailwind arbitrary-variant class vs. a raw CSS custom property) is exactly the claim UX-01 rests on, and this is the first point in the phase where both are compared live in a real browser"
  prohibitions:
    - "uat-screenshots/ is not `git add`ed as part of this plan's commit -- local visual evidence only, matching this project's established precedent (260718-0qi-SUMMARY.md, 11-* uat-screenshots)"
---

<objective>
Close the phase with the aggregate proof every prior plan's own per-plan checks only partially
cover: a repo-wide build/test/typecheck gate across both consumers, plus the ONE verification this
phase's own RESEARCH.md flagged as missing from every existing automated suite -- a genuine,
screenshot-and-computed-style-backed visual confirmation that the in-page dropdown/prompt tiles
(Plan 17-02's fix) actually match the promoted `ItemIconTile` component's rendering (Plan 17-03) in
both themes. RESEARCH.md's own Pitfall 3 found that neither the Playwright Chromium suite
(`dual-browser.spec.ts`) nor the Firefox Selenium harness's own pixel-cluster assertions actually
check the in-page tile's background color -- this plan is the first genuine automated check of that
specific claim, per this phase's own CONTEXT.md ("screenshot-based UAT expected") and the standing
Playwright-UAT-authorized self-validation precedent.

Purpose: prove, not assume, that DS-03/DS-04/UX-01 all hold together in a real, running instance of
every surface -- the phase's closing verification gate.
Output: a clean aggregate grep report, a fully green build+test+typecheck run on both consumers, 8+
screenshots for Bartek's own end-of-phase visual review, and an automated computed-background-color
cross-check between the React component and the shadow-DOM overlay.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-shared-component-visual-alignment/17-CONTEXT.md
@.planning/phases/17-shared-component-visual-alignment/17-RESEARCH.md
@.planning/phases/17-shared-component-visual-alignment/17-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate build/test/typecheck gate + zero-duplication + crypto-free close-out</name>
  <files></files>
  <read_first>
    .planning/phases/16-design-system-extraction-logic-types-i18n/16-06-PLAN.md (the exact aggregate-gate precedent this task mirrors -- Task 2's build+test+grep pattern, extended to the new packages/pv-ui/components/ directory)
    17-RESEARCH.md "## Validation Architecture" (Full suite command table, Wave 0 Gaps)
    17-UI-SPEC.md "## Component Contract" and "## In-Page Token Contract" Definition-of-done lines
  </read_first>
  <action>
    Run each consumer's full verification chain exactly as documented in this phase's RESEARCH.md:
    web (`npx vitest run && npx tsc --noEmit && npx next build`); extension (`npx vitest run && npx
    tsc --noEmit && npx wxt build && npx wxt build -b firefox`), confirming zero regressions across
    all three prior plans.
    Re-run the repo-wide zero-duplication grep for `ItemIconTile` (Plan 17-03's own Task 2 check,
    re-confirmed here in aggregate after everything has landed) and the aggregate crypto-free grep
    across every file under packages/pv-ui/{vault,i18n,components} and packages/pv-ui/clipboard.ts,
    scoped to `^import` lines only (never a whole-file keyword search, which would false-match a
    legitimate doc-comment use of a word like "decrypted" describing the zero-knowledge boundary --
    same scoping precedent as 16-06-PLAN.md's own Task 2).
  </action>
  <acceptance_criteria>
    All 4 web commands and all 4 extension commands exit 0. The zero-duplication grep for
    `function ItemIconTile`/`export default function ItemIconTile` outside
    packages/pv-ui/components returns empty. The import-line-scoped crypto-surface grep across the
    whole packages/pv-ui shared package (now including components/) returns empty.
  </acceptance_criteria>
  <verify>
    <automated>(cd web && npx vitest run && npx tsc --noEmit && npx next build) && (cd extension && npx vitest run && npx tsc --noEmit && npx wxt build && npx wxt build -b firefox) && [ -z "$(grep -rn 'function ItemIconTile' web/src extension/entrypoints --include=*.tsx | grep -v packages/pv-ui)" ] && [ -z "$(grep -n '^import' packages/pv-ui/vault/*.ts packages/pv-ui/i18n/*.ts packages/pv-ui/clipboard.ts packages/pv-ui/components/*.tsx | grep -riE 'wasm|argon2|chacha|hkdf|derive|decrypt|prf')" ]</automated>
  </verify>
  <done>Both consumers' full 4-command chains are green; both aggregate grep gates return empty -- DS-03's "no second implementation" and pv-ui's crypto-free boundary are both closed in their strongest, most literal form across the whole phase's diff.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright/computed-style visual parity capture across all 4 surfaces, both themes</name>
  <files>extension/e2e-visual/capture-tile-parity.mjs, extension/package.json</files>
  <read_first>
    extension/e2e/fixtures.ts (the `chromium.launchPersistentContext` + `--load-extension` technique this script reuses to derive `extensionId` from the loaded background service worker)
    extension/e2e-firefox/run-autofill-capture.cjs (the self-contained sign-in + dependency-free node:http fixture-page pattern this script mirrors, INCLUDING its own independence from the shared cumulative 21-SC suite -- this new script owns its own sign-in/vault state)
    extension/lib/theme/theme-mirror.ts (THEME_MIRROR_KEY = "pv-theme-mirror", resolveTheme()/watchMirroredTheme()'s live chrome.storage.onChanged path -- the exact mechanism this script uses to deterministically toggle the in-page overlay's rendered theme without a page reload)
    web/src/app/layout.tsx, web/src/components/shell/Sidebar.tsx (the `document.documentElement.setAttribute("data-theme", ...)` toggle mechanism this script reuses for web/popup)
    extension/e2e/dual-browser.spec.ts lines ~72-90 (`loginPage()` -- the dependency-free inline-HTML fixture-form pattern this script's own minimal login-form fixture mirrors)
    17-UI-SPEC.md "## Surface Inventory" table (the 4 surfaces and their exact selectors/entry points: web ItemRow.tsx variant="row", DetailPanel.tsx variant="header", popup ItemListView.tsx's `.pv-row-icon-tile`-equivalent React tile, in-page `.pv-row-icon-tile` on Surfaces A/B)
  </read_first>
  <action>
    Create extension/e2e-visual/capture-tile-parity.mjs, a standalone, self-contained (own sign-in,
    own vault state -- never depends on or mutates extension/e2e/dual-browser.spec.ts's shared
    cumulative worker state) Node ESM script using `@playwright/test`'s `chromium` launcher, that:
    1. Signs in against a REAL running pv-server at http://localhost:8620 (create a dedicated
       account, e.g. `uat-tile-parity@example.local`, via the real register flow -- do not reuse
       the shared `uat-prf04@example.local` account's accumulated state) and creates ONE login item
       pointing at `https://github.com` (a known dark-logo favicon, chosen specifically to make the
       vault-dark light-tile flip visually obvious on every surface).
    2. Loads `.output/chrome-mv3` via `chromium.launchPersistentContext(..., { args:
       ["--disable-extensions-except=...", "--load-extension=..."] })` (same technique as
       extension/e2e/fixtures.ts), derives `extensionId` from the loaded background service
       worker's URL, and opens the popup page directly at `chrome-extension://{id}/popup.html`.
       Screenshots the GitHub item's row tile in the "Wszystkie" list, toggling
       `document.documentElement.setAttribute("data-theme", "vault-dark" | "vault-light")` inside
       the popup page for each theme.
    3. Starts a minimal dependency-free `node:http` fixture server (mirroring `dual-browser.spec.ts`'s
       own `loginPage()` helper) serving one page with `<input name="username">`/`<input
       name="password">`. With the extension active on that page, focuses the password field to
       trigger Surface A's `renderFieldDropdown()`, then screenshots the `.pv-row-icon-tile` inside
       the closed shadow root (query through the host element's `shadowRoot` reference obtained via
       CDP `DOM.getDocument({ pierce: true })`, same shadow-piercing technique
       `extension/e2e/dual-browser.spec.ts` already uses) in both themes -- toggle the in-page
       theme deterministically via `page.evaluate((theme) => chrome.storage.local.set({
       "pv-theme-mirror": theme }), themeValue)` (mirrors theme-mirror.ts's own
       `THEME_MIRROR_KEY`/live `watchMirroredTheme()` update path; no page reload needed).
    4. Submits that same fixture form to trigger Surface B's `renderFormPrompt()` (the save/update
       prompt) and screenshots ITS `.pv-row-icon-tile` in both themes, same toggle mechanism.
    5. Launches a SEPARATE, non-extension Chromium context pointed at the web app's dev server
       (assume `cd web && npm run dev` is already running on `http://localhost:3000`, or start it
       from this script if not reachable), signs in to the SAME account, and screenshots
       `ItemRow`'s tile (list view) and `DetailPanel`'s header tile (item detail view) for the same
       GitHub item, toggling `data-theme` on `<html>` for both themes.
    6. Saves every screenshot to `.planning/phases/17-shared-component-visual-alignment/uat-screenshots/`,
       named `{surface}-{theme}.png` (e.g. `web-itemrow-vault-dark.png`,
       `web-detailpanel-vault-light.png`, `popup-list-vault-dark.png`,
       `inpage-dropdown-vault-light.png`, `inpage-prompt-vault-dark.png`).
    7. For an automated (non-visual-only) pass/fail signal, reads each tile's computed
       `background-color` via `page.evaluate(() => getComputedStyle(el).backgroundColor)` for the
       web/popup tile and the in-page tile in each theme, and asserts the in-page tile's computed
       RGB value equals the web/popup tile's computed RGB value (both ultimately resolve
       `--pv-tile-bg`'s value, but through two different mechanisms -- a Tailwind class vs. a raw
       CSS custom property -- so this is a genuine cross-mechanism proof, not a tautology). Writes
       a small JSON results file (`uat-screenshots/results.json`) recording every comparison's
       pass/fail and the two RGB values compared.
    8. Best-effort, does not fail the task on its own: if a Firefox binary is available at this
       environment's conventional path, re-run `npm run test:e2e:firefox:autofill` (the existing
       Selenium harness, already exercises Surface A/B) and copy its relevant P10/P11 screenshots
       from `.ff-screenshots/` into the same `uat-screenshots/` directory as supplementary evidence;
       if unavailable, log this step as explicitly skipped in the SUMMARY, never silently omitted.
    Add two npm scripts to extension/package.json, positioned alongside the existing
    `test:e2e:firefox:*`/`pretest:e2e:*` entries: `"test:e2e:visual": "node
    e2e-visual/capture-tile-parity.mjs"` and `"pretest:e2e:visual": "wxt build -b chrome"`.
  </action>
  <acceptance_criteria>
    extension/e2e-visual/capture-tile-parity.mjs exists and runs to completion via `npm run
    test:e2e:visual`. At least 8 PNG files exist under
    .planning/phases/17-shared-component-visual-alignment/uat-screenshots/ covering all 4 surfaces
    in both themes. uat-screenshots/results.json records the computed-background-color comparison
    for every surface pair in both themes, with every comparison recorded as a pass. The Firefox
    Selenium re-run either succeeds (its screenshots copied in) or is explicitly logged as skipped
    -- never silently omitted from the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>(cd extension && npm run pretest:e2e:visual && npm run test:e2e:visual) && [ "$(ls .planning/phases/17-shared-component-visual-alignment/uat-screenshots/*.png 2>/dev/null | wc -l)" -ge 8 ] && node -e 'const r=JSON.parse(require("fs").readFileSync(".planning/phases/17-shared-component-visual-alignment/uat-screenshots/results.json","utf8")); const fails=Object.entries(r).filter(([,v])=>v.pass!==true); if(fails.length){console.error("FAILED COMPARISONS",fails);process.exit(1);} console.log("OK:",Object.keys(r).length,"comparisons all pass");'</automated>
    <human-check>
      <test>Open .planning/phases/17-shared-component-visual-alignment/uat-screenshots/ and view all captured PNGs side by side, grouped by theme (vault-dark set, then vault-light set)</test>
      <expected>In vault-dark, every surface's icon tile (web ItemRow, web DetailPanel header, popup list row, in-page Surface A dropdown, in-page Surface B prompt) renders on the SAME light neutral background with a dark neutral fallback-glyph color -- no surface renders a dark/near-black tile. In vault-light, every surface renders the same base-200 tile as before this phase. The GitHub favicon (a dark-toned logo) is legible against the light tile in vault-dark on every surface. The .pv-list scroll-cap affordance in the in-page screenshots still shows ~4.5 rows, unchanged from before this phase.</expected>
      <why_human>Final pixel-level visual-taste confirmation across 5 render sites x 2 themes -- Task 2's own computed-style assertion proves the colors are numerically equal, but only a human eye confirms the result actually LOOKS right end-to-end (per standing Playwright-UAT-authorized self-validation: visual-taste calls still surface to Bartek with screenshots, even when the functional proof already passed)</why_human>
    </human-check>
  </verify>
  <done>8+ screenshots exist under this phase's uat-screenshots/ directory covering all 4 surfaces in both themes; the automated computed-background-color cross-check passes for every surface pair in both themes, closing UX-01's "matches web/popup" claim with browser-level evidence rather than a screenshot alone; the Firefox Selenium lane ran (or was explicitly logged as unavailable); the human-check surfaces final visual confirmation to Bartek at the end-of-phase UAT gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| extension/e2e-visual/capture-tile-parity.mjs vs. real running pv-server + a dedicated test account | Same trust level as every other existing e2e/e2e-firefox harness in this repo -- a throwaway, non-production test account against a real (but not internet-exposed by default) local pv-server instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-17-10 | Tampering | Aggregate build/test/typecheck + grep gate (Task 1) | medium | mitigate | Full green build+test+typecheck chain plus both aggregate greps is the phase's final, comprehensive proof that the 3-plan promotion+token-fix introduced no behavior/security regression anywhere it touched |
| T-17-11 | Information Disclosure | extension/e2e-visual/capture-tile-parity.mjs's own test credentials and captured screenshots | low | mitigate | Reuses this project's own established shared-UAT-test-account convention (a dedicated, non-production `@example.local` account, never a real user's), matching every other e2e/e2e-firefox harness; screenshots are local-only evidence, explicitly not committed to git, matching the 260718-0qi/11-* precedent |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages -- this task only adds two npm SCRIPT entries (no new dependency) to extension/package.json |
</threat_model>

<verification>
Both consumers' full build+test+typecheck chains are green; both aggregate grep gates return empty;
the visual-parity script produces 8+ screenshots with every automated computed-color comparison
passing; the human-check block surfaces final visual confirmation at the end-of-phase UAT gate.
</verification>

<success_criteria>
Phase 17's 3 ROADMAP success criteria are all proven true in aggregate: (1) ItemIconTile exists
exactly once, imported by both web and the extension popup; (2) the in-page dropdown/prompt render
item logos on a light tile visually matching web/popup, in both themes, with the prior dark-tile
inconsistency gone; (3) the in-page overlays' hand-written styles read their tile color values from
pv-ui design tokens, with no duplicated/hand-copied design constant remaining.
</success_criteria>

## Artifacts This Phase Produces (this plan's slice)

**Created:**
- `extension/e2e-visual/capture-tile-parity.mjs` -- standalone Playwright screenshot + computed-style visual-parity harness
- `.planning/phases/17-shared-component-visual-alignment/uat-screenshots/*.png` (8+ files) and `results.json` -- local evidence, not committed to git

**Modified:**
- `extension/package.json` -- +`test:e2e:visual`, +`pretest:e2e:visual` npm scripts

**Deleted:** none.

<output>
Create `.planning/phases/17-shared-component-visual-alignment/17-04-SUMMARY.md` when done
</output>
