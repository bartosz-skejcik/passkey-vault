---
phase: 26-web-app-sharing-ui-family-management
plan: 09
type: execute
wave: 5
depends_on: ["26-06", "26-08"]
files_modified:
  - web/src/components/vault/ItemContextMenu.tsx
  - web/src/components/vault/ItemContextMenu.test.tsx
  - web/src/components/vault/DetailPanel.tsx
  - web/src/components/vault/DetailPanel.test.tsx
  - web/src/components/vault/ItemRow.tsx
  - web/src/components/vault/ItemRow.test.tsx
autonomous: true
requirements: [SHARE-02, UX-05]

must_haves:
  truths:
    - "Every personal, non-collection-scoped item (including a passkey item) has a real, working Share entry point in both the kebab menu and the detail panel header."
    - "An item already inside a shared folder shows an honest note instead of a Share button that would always fail server-side."
    - "A shared item's avatar stack renders in the vault list without blocking the row on a per-item network fetch."
  key_links:
    - "ItemContextMenu 'Share...' entry and DetailPanel's Share icon both open ShareDialog (Plan 26-08) in item variant, scope: {kind: 'item', item}."
    - "item.collectionId !== null -> share.itemSharedOnCollectionNote replaces the Share action entirely (WR-10's server-side 400 on a direct share against a collection-scoped item makes a clickable button here a UI lie)."
---

<objective>
Wire D-1's item-level Share entry point (E1) into the two existing places a member acts on an item
— the kebab context menu and the detail panel header — and render D-3's avatar stack in the list
row and detail panel wherever an item is shared.

Purpose: `ShareDialog` (Plan 26-08) is useless without a way to open it from the actual item a
member wants to share. This plan is that wiring, plus the E1 backstop that a collection-scoped
item must show an honest note instead of a button that would always fail.
Output: "Share..." in `ItemContextMenu`, a Share icon in `DetailPanel`'s header, `AvatarStack`
rendering in `ItemRow`/`DetailPanel` wherever an item is shared.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-web-app-sharing-ui-family-management/26-UI-SPEC.md

# Exact entry-point precedents to mirror
web/src/components/vault/ItemContextMenu.tsx:129-155 (the existing "Move" `<details>` entry) —
  "Share…" gets an equivalent `<li>` entry, same list position discipline, opens `ShareDialog`
  rather than a nested menu.
web/src/components/vault/ItemContextMenu.tsx:170-176 — the existing passkey/undecryptable Edit
  suppression pattern — Share does NOT follow the passkey suppression (SHARE-02 explicitly covers
  passkey items, E1's own backstop requires Share to render for a passkey item exactly like a
  login item), but DOES follow the `item.undecryptable` suppression (an item that failed integrity
  has nothing safe to share).
web/src/components/vault/DetailPanel.tsx:226,278-291 — the existing Edit/Delete
  `btn btn-ghost btn-square btn-sm` icon-button row in the header — the new Share2 icon button goes
  BEFORE Edit in that same row.

# WR-10's server-side guard this plan's UI note exists to avoid contradicting
crates/pv-server/src/routes/vault.rs:1236-1246 (create_share) — a direct item_shares grant on a
  collection-scoped item (`collection_id IS NOT NULL`) is REJECTED with 400 server-side. A clickable
  Share action on such an item would 400 on every click — replace it entirely with
  `share.itemSharedOnCollectionNote` (a plain-text note, no button), not merely disable it.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Share entry points (ItemContextMenu + DetailPanel) + itemSharedOnCollectionNote</name>
  <files>web/src/components/vault/ItemContextMenu.tsx, web/src/components/vault/ItemContextMenu.test.tsx, web/src/components/vault/DetailPanel.tsx, web/src/components/vault/DetailPanel.test.tsx</files>
  <behavior>
    Test: a personal (non-collection-scoped) item shows "Share…" in the kebab menu, opening
    `ShareDialog` with `scope: {kind: "item", item}`.
    Test: a passkey item shows "Share…" exactly like a login item (no suppression) — distinct from
    Edit's existing suppression for passkeys.
    Test: an `undecryptable` item does NOT show "Share…" (mirrors the existing Edit suppression's
    rationale).
    Test: an item with `collectionId !== null` shows `share.itemSharedOnCollectionNote`
    (interpolated with the owning folder's name) in place of any Share action, in BOTH the context
    menu and the detail panel — never a clickable button that would 400.
    Test: `DetailPanel`'s header renders a `Share2` icon button before Edit, opening the same
    `ShareDialog`.
  </behavior>
  <action>
    In `ItemContextMenu.tsx`, add a "Share…" `<li>` entry (mirrors the existing Move entry's list
    position and testid convention, `data-testid="context-menu-share"`) that opens `ShareDialog`
    (Plan 26-08) with `scope: {kind: "item", item}`; suppressed only for `item.undecryptable`, not
    for `item.fields.type === "passkey"`. When `item.collectionId !== null`, render
    `share.itemSharedOnCollectionNote` (interpolated `{folder}` — resolve the folder name from
    `useCollections()`, Plan 26-05) as inert text instead of the Share entry. In `DetailPanel.tsx`,
    add a `Share2`-icon `btn btn-ghost btn-square btn-sm` button in the header row, positioned
    before the existing Edit button, opening the same dialog; apply the identical
    `itemSharedOnCollectionNote` substitution for a collection-scoped item.
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault/web && npx vitest run src/components/vault/ItemContextMenu.test.tsx src/components/vault/DetailPanel.test.tsx</automated>
  </verify>
  <done>All behaviors above pass; `npx tsc --noEmit` is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AvatarStack wiring in ItemRow and DetailPanel</name>
  <files>web/src/components/vault/ItemRow.tsx, web/src/components/vault/ItemRow.test.tsx</files>
  <behavior>
    Test: a shared item's row renders `AvatarStack` (via Plan 26-06's `useShareRecipients`);
    a non-shared item's row renders no stack.
    Test: an item row whose share-recipient data has not yet resolved renders zero avatar circles
    — not a placeholder, not a spinner (E5's loading backstop, re-verified here at the real call
    site).
    Test: rendering a list of many items where only a few are shared does not trigger a
    per-item fetch for the non-shared items (spy-based call-count assertion).
  </behavior>
  <action>
    In `ItemRow.tsx`, render `<AvatarStack item={item} />` (default, full-circle variant) inline
    with the row's existing metadata area (near the relative-time/TOTP-ring slot), only when
    `item.isShared` is true. In `DetailPanel.tsx`, render the same full-circle `AvatarStack` in the
    header's metadata area for a shared item.
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault/web && npx vitest run src/components/vault/ItemRow.test.tsx</automated>
  </verify>
  <done>Avatar stacks render only for shared items, resolve without blocking the row, and never
  fetch for non-shared rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | UI wiring only, over already-authorized data sources built in earlier plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-20 | Tampering (UX-level, not security) | A clickable Share action on a collection-scoped item that would always 400 server-side | medium | mitigate | Replaced entirely with the honest `itemSharedOnCollectionNote`, never merely disabled — a disabled-but-present button would still imply the action is meaningful once re-enabled |
</threat_model>

<verification>
`cd web && npx tsc --noEmit && npx vitest run src/components/vault/ItemContextMenu.test.tsx src/components/vault/DetailPanel.test.tsx src/components/vault/ItemRow.test.tsx`
</verification>

<success_criteria>
Every personal item has a real Share entry point in both surfaces; a collection-scoped item shows
the honest note instead; shared items show the avatar stack without N+1 fetches.
</success_criteria>

<output>
Create `.planning/phases/26-web-app-sharing-ui-family-management/26-09-SUMMARY.md` when done,
with `## Threat Flags` populated.
</output>
