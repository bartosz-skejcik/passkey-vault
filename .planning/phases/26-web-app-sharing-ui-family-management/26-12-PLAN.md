---
phase: 26-web-app-sharing-ui-family-management
plan: 12
type: execute
wave: 5
depends_on: ["26-03", "26-06", "26-07"]
files_modified:
  - web/src/components/settings/FamilyTab.tsx
  - web/src/components/settings/FamilyTab.test.tsx
  - web/src/lib/i18n/dictionary.ts
autonomous: true
requirements: [SEC-05]

must_haves:
  truths:
    - "A member can see their OWN identity fingerprint as six words, prominently, so they can read it aloud for an out-of-band comparison."
    - "A member can reveal any other member's fingerprint the same way, with a mismatch-consequence warning next to every rendered word list, not only the self card."
    - "Collection-scoped invites are no longer permanently disabled with 'coming later' copy — the capability this phase built is actually reachable."
  key_links:
    - "getFamilyMembers() self row's fingerprint hex -> formatFingerprintWords (Plan 26-03) -> six-word card, own row prominent, others behind a reveal toggle."
    - "invite-scope-select's 'folder' option un-disabled -> CollectionPicker (Plan 26-07) mounts beneath it when chosen -> generateInviteLink's existing collection-scope branch (already shipped, was blocked only by no client-side collections capability)."
---

<objective>
Extend `FamilyTab.tsx`'s Members section with D-4/SEC-05's identity fingerprint card and per-row
reveal, and discharge the second inherited obligation from Phase 24: collection-scoped invites ship
API-complete but UI-disabled with "coming in a later version" copy — this phase built the real
client-side collections capability that copy was waiting on, so it must be enabled and the copy
retired.

Purpose: This is SEC-05's actual delivery surface, and it is also where E9's one visible
consequence of a KEY-01 publish failure shows up (`identity.fingerprintUnavailable` persisting past
when it should have resolved) — so this plan is the honest observable proof that Plan 26-02's
trigger is working.
Output: A "Your identity fingerprint" card + per-member reveal toggles in `FamilyTab.tsx`; the
collection-scope invite option enabled with `CollectionPicker` mounted beneath it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-web-app-sharing-ui-family-management/26-CONTEXT.md
@.planning/phases/26-web-app-sharing-ui-family-management/26-UI-SPEC.md

# Fingerprint display precedent to mirror the branch shape of (never the format — hex vs. words)
web/src/components/invite/InviteLandingView.tsx:259-293 — "available vs. unavailable" branching
  (never a spinner/error for "not yet published" — that is an honest, expected state per
  `identity.fingerprintUnavailable`'s own honesty constraint 3), the `invite.fingerprintHonesty`
  comparison-ritual copy precedent. Replace `formatFingerprint`'s hex-grouping with Plan 26-03's
  `formatFingerprintWords`; render `identity.fingerprintMismatchWarning` (Plan 26-06's dictionary
  addition) adjacent to EVERY rendered word list — self card AND every expanded member row alike
  (honesty constraint 5) — never collapsed into `invite.fingerprintHonesty`, which is a different
  string for a different purpose (the comparison ritual, not the mismatch consequence).
web/src/components/settings/FamilyTab.tsx:647-686 (member-row rendering) — the reveal-toggle row
  shape mirrors this file's own row shell, add a `ChevronDown`/`ChevronRight` toggle per row
  matching `Sidebar.tsx`'s existing section-expand chevron.

# The exact obsolete-copy retirement this plan owes (inherited obligation, STATE.md Phase 24 entry)
web/src/components/settings/FamilyTab.tsx:540-560 — the `invite-scope-select`'s `"folder"` option
  is currently unconditionally `disabled`, with `invite.scopeFolderComingSoon`/
  `invite.scopeFolderUnavailableNote` shown beneath it. Un-disable the option; mount
  `CollectionPicker` (Plan 26-07) beneath the select exactly when `"folder"` is chosen, in the SAME
  visual position the disabled note occupied (no layout shift). Remove BOTH keys from
  `dictionary.ts` in THIS SAME task (never leave them dead) and update
  `FamilyTab.test.tsx`'s existing assertions against the old disabled state accordingly.
web/src/lib/invite/crypto.ts:94-101 (generateInviteLink) — the collection-scope branch is ALREADY
  fully implemented server- and crypto-side (`getCollection`, `unsealCollectionKey`,
  `channel.wrapCollectionKey`) — this plan only needs to supply it a real `collectionId` from
  `CollectionPicker`, never touch this function's own logic.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Identity fingerprint card + per-member reveal</name>
  <files>web/src/components/settings/FamilyTab.tsx, web/src/components/settings/FamilyTab.test.tsx</files>
  <behavior>
    Test: own row — `identity.yourFingerprintHeading` card renders above the Members list, showing
    the six-word string in `font-mono` when `fingerprint !== null`, plus a non-auto-clearing copy
    button (deliberate deviation from `copyWithAutoClear`, per UI-SPEC Phase-Specific Notes §2 — a
    fingerprint is not a secret).
    Test: own row — `identity.fingerprintUnavailable` renders instead of a word list when
    `fingerprint === null`, and is never styled or worded as an error (honesty constraint 3).
    Test: other members — a `ChevronDown`/`ChevronRight` toggle per row (not expanded by default),
    `identity.fingerprintRevealAria` labels it; expanding shows the same word-list + copy-button
    treatment as the self card, or `identity.fingerprintUnavailable` if that member's key isn't
    published yet.
    Test: `identity.fingerprintMismatchWarning` renders adjacent to EVERY rendered word list — the
    self card AND every expanded member row — never only on the self card (honesty constraint 5).
    Test: the word-list format is exactly six words separated by the documented middot spacing,
    matching D-4's literal example format.
  </behavior>
  <action>
    Add a "Your identity fingerprint" card to `FamilyTab.tsx`, pinned above the Members list inside
    the same `family-members-section` container, deriving the caller's own row from
    `members.find(m => m.user_id === selfUserId)` (already fetched). Add a reveal toggle per
    member row (mirrors `Sidebar.tsx`'s chevron idiom) that expands to show the SAME word-list +
    copy-button + mismatch-warning treatment. Use `formatFingerprintWords` from
    `packages/pv-ui/identity/fingerprint.ts` (Plan 26-03) on the `fingerprint` hex field already
    present on `FamilyMemberRecord`. The copy button uses a plain, non-auto-clearing clipboard
    write (`identity.fingerprintCopyAria`), a `Check` icon swap on success mirroring
    `DetailPanel.tsx`'s existing field-copy micro-interaction.
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault/web && npx vitest run src/components/settings/FamilyTab.test.tsx</automated>
  </verify>
  <done>All behaviors above pass; the mismatch warning renders next to every word list, never only
  the self card.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enable collection-scope invite via CollectionPicker + retire obsolete copy</name>
  <files>web/src/components/settings/FamilyTab.tsx, web/src/components/settings/FamilyTab.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <behavior>
    Test: the `"folder"` `<option>` in `invite-scope-select` is no longer `disabled`.
    Test: choosing `"folder"` mounts `CollectionPicker` beneath the select, in the same visual
    position the old disabled-note occupied.
    Test: `invite.scopeFolderComingSoon`/`invite.scopeFolderUnavailableNote` no longer exist in
    `dictionary.ts` (a compile-time guarantee via `npx tsc --noEmit`, since any surviving reference
    would fail to typecheck against a removed `DICTIONARY` key).
    Test: generating a collection-scoped invite with a real picked collection id calls
    `generateInviteLink` with `scope: {kind: "collection", collectionId, accessLevel}` and succeeds
    end-to-end against `generateInviteLink`'s existing, unmodified implementation.
  </behavior>
  <action>
    Remove the `disabled` attribute from the `"folder"` `<option>`; when selected, mount
    `CollectionPicker` (Plan 26-07) in place of the old disabled-note paragraph, wiring its
    `onSelect` to the invite form's collection-id state and its `onCreateNew` to open `ShareDialog`
    (Plan 26-08) folder-create variant inline. Delete `invite.scopeFolderComingSoon` and
    `invite.scopeFolderUnavailableNote` from `dictionary.ts`. Update `FamilyTab.test.tsx`'s
    existing assertions against the old disabled state to assert the new enabled behavior instead
    (never leave a test asserting a now-false claim).
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault/web && npx tsc --noEmit && npx vitest run src/components/settings/FamilyTab.test.tsx</automated>
  </verify>
  <done>The folder-scope option is genuinely usable end-to-end; the two obsolete dictionary keys
  are gone, not dead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Copy button uses a non-auto-clearing clipboard write | Deliberate, documented deviation from this codebase's otherwise-universal secret-copy auto-clear rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-22 | Spoofing | TOFU trust gap — the server distributes public keys with no transparency log | medium | accept | Explicitly the honest, in-scope SEC-05 mitigation for v0.4 (fingerprint comparison out-of-band); a "key changed" banner and transparency log are formally deferred (SEC-F1/SEC-F2), not silently dropped |
| T-26-23 | Information Disclosure | Non-auto-clearing clipboard write of the fingerprint | low | accept | A fingerprint is a non-reversible, public derivation of an already-published public key, not a secret — auto-clearing would work against its own out-of-band-comparison purpose (UI-SPEC Phase-Specific Notes §2) |
</threat_model>

<verification>
`cd web && npx tsc --noEmit && npx vitest run src/components/settings/FamilyTab.test.tsx`
</verification>

<success_criteria>
The fingerprint card and per-member reveal render real six-word fingerprints with the mismatch
warning everywhere required; collection-scoped invites are genuinely enabled, obsolete copy is gone.
</success_criteria>

<output>
Create `.planning/phases/26-web-app-sharing-ui-family-management/26-12-SUMMARY.md` when done,
with `## Threat Flags` populated.
</output>
