---
phase: 26-web-app-sharing-ui-family-management
plan: 10
type: execute
wave: 5
depends_on: ["26-05", "26-06", "26-08", "26-11"]
files_modified:
  - web/src/components/shell/Sidebar.tsx
  - web/src/components/shell/Sidebar.test.tsx
autonomous: true
requirements: [SHARE-01, UX-05]

must_haves:
  truths:
    - "A member can start creating a shared folder, or convert an existing personal folder into a shared one, from the Sidebar — even before they have any shared folders yet."
    - "A member can reach the Sharing overview from the Sidebar's persistent account-area cluster."
  key_links:
    - "'+ New shared folder' trigger -> ShareDialog (Plan 26-08) folder-create variant, no seed."
    - "Existing personal folder row's new kebab -> 'Share this folder' -> ShareDialog folder-create variant, seeded with that folder's id."
    - "Sharing nav trigger -> SharingOverviewPanel (Plan 26-11)."
---

<objective>
Build D-1's folder-level Share entry point (E2): a new "Shared folders" Sidebar section parallel to
the existing personal "Foldery" section, plus a "Share this folder" action on an existing personal
folder row — the first context menu ever added to a personal-folder Sidebar row (today's rows are
plain buttons). Also adds the Sharing-overview nav trigger.

Purpose: Without this, `ShareDialog`'s folder-create variant (Plan 26-08) and `SharingOverviewPanel`
(Plan 26-11) have no entry point a real user would find.
Output: A "Shared folders" collapsible section in `Sidebar.tsx`, a "Share this folder" kebab on
personal folder rows, and a Sharing-overview trigger near Settings/Lock/Logout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-web-app-sharing-ui-family-management/26-UI-SPEC.md

# Existing section shape to mirror exactly (same expand/collapse idiom, same default-collapsed state)
web/src/components/shell/Sidebar.tsx:205-273 (the "Foldery" section) — the "Shared folders" section
  is a sibling of this, same `navItemClass`/chevron/collapse pattern, sourced from
  `useCollections()` (Plan 26-05) instead of `useFolders()`.
web/src/components/shell/Sidebar.tsx:339-418 (the account-area dropdown cluster) — the Sharing
  overview trigger (`Share2` icon, `sharing.navLabel`) belongs in this SAME cluster, alongside
  Lock/Logout/Settings, per UI-SPEC's component inventory ("positioned in the Sidebar's
  account-area cluster near the existing Settings/Lock/Logout entries — a persistent
  account-scoped surface, not a per-item context action").
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared folders section + Share-this-folder kebab</name>
  <files>web/src/components/shell/Sidebar.tsx</files>
  <behavior>
    Test: zero shared folders — the section still renders (collapsed by default, matching
    `foldersExpanded`'s own default), with only the "+ Nowy udostępniony folder" trigger inside —
    never hidden entirely.
    Test: the trigger opens `ShareDialog` (Plan 26-08) in folder-create variant with no seed.
    Test: a shared folder name >= 40 chars truncates without breaking row height (`truncate` +
    `title`, mirroring Phase 25's own email-truncation backstop precedent).
    Test: an existing personal folder row now exposes a kebab with exactly one action, "Udostępnij
    ten folder", opening `ShareDialog` folder-create variant SEEDED with that folder's id (asserted
    via the `existingFolderId` prop passed to `ShareDialog`).
  </behavior>
  <action>
    Add a "Shared folders" collapsible section to `Sidebar.tsx`, structurally mirroring the
    existing "Foldery" section (own `useState` expand flag, default collapsed), sourced from
    `useCollections()`. Zero-state still renders the section shell with only the create trigger.
    Icon-only `AvatarStack` (Plan 26-06's `variant="icon"`) renders next to each shared-folder row
    per UI-SPEC E5's narrow-column resolution. Add a kebab (first ever on a personal-folder row) to
    each existing personal folder row with one action opening `ShareDialog` seeded from that
    folder's id.
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault/web && npx vitest run src/components/shell/Sidebar.test.tsx</automated>
  </verify>
  <done>All behaviors above pass; the zero-state never hides the entry point; truncation backstop
  passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sharing-overview nav trigger</name>
  <files>web/src/components/shell/Sidebar.tsx, web/src/components/shell/Sidebar.test.tsx</files>
  <behavior>
    Test: a `Share2`-icon, `sharing.navLabel`-labelled trigger renders in the account-area
    dropdown cluster, alongside the existing Lock/Logout/Settings entries.
    Test: clicking it opens `SharingOverviewPanel` (Plan 26-11).
  </behavior>
  <action>
    Add the Sharing-overview trigger to the SAME `<ul>` dropdown-content cluster
    `Sidebar.tsx:369-417` already renders (Language/Lock/Logout/Settings), following that list's
    existing `<li><button>` shape exactly, mounting `SharingOverviewPanel` on click via a local
    `showSharingOverview` state flag (same pattern `showGenerator`/`GeneratorDialog` already uses
    in this same file).
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault/web && npx vitest run src/components/shell/Sidebar.test.tsx</automated>
  </verify>
  <done>The trigger renders in the correct cluster and opens the panel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Navigation wiring only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-21 | Information Disclosure | None — the shared-folders list is the caller's own `useCollections()` data, already server-authorized | n/a | accept | No new query, no new endpoint |
</threat_model>

<verification>
`cd web && npx tsc --noEmit && npx vitest run src/components/shell/Sidebar.test.tsx`
</verification>

<success_criteria>
A member can create a shared folder (fresh or seeded), and reach the Sharing overview, entirely
from the Sidebar.
</success_criteria>

<output>
Create `.planning/phases/26-web-app-sharing-ui-family-management/26-10-SUMMARY.md` when done,
with `## Threat Flags` populated.
</output>
