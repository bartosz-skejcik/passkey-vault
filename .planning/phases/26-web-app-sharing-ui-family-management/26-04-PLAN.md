---
phase: 26-web-app-sharing-ui-family-management
plan: 04
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - crates/pv-server/src/routes/vault.rs
  - crates/pv-server/src/routes/mod.rs
  - crates/pv-server/src/routes/collections.rs
  - crates/pv-server/tests/vault.rs
  - crates/pv-server/tests/collections.rs
  - web/src/lib/vault/api.ts
autonomous: true
requirements: [SHARE-02, UX-05]

must_haves:
  truths:
    - "A member can ask 'who is this personal item directly shared with' and get a real answer, including suspended recipients marked as such — the data genuinely does not exist client-side today."
    - "A non-member of an item gets 404 on this endpoint, never a data leak."
  artifacts:
    - "crates/pv-server/src/routes/vault.rs::list_item_shares — GET /api/vault/items/{id}/shares, Membership<Item, RequireRead>-gated."
    - "web/src/lib/vault/api.ts::listItemShares — client wrapper."
  key_links:
    - "Membership<Item, RequireRead> (mirrors collections::access_list's extractor family, never FamilyMembership<RequireEdit>/getMemberAccess's owner-only gate) -> item_shares JOIN users JOIN family_members (status) -> CoRecipientRecord[] with a suspended flag -> Plan 26-06's AvatarStack data source and Plan 26-11's Sharing overview."
---

<objective>
Add the missing read path Phase 26's UI-SPEC Phase-Specific Notes §4 calls "a real gap, not a
rendering branch to invent around": `POST`/`DELETE /api/vault/items/{id}/shares` already exist,
but nothing returns the current recipient set for a personal item's direct shares. Per A-7,
suspended recipients are included and flagged, not filtered out — mirroring
`collections::access_list`'s own no-status-filter precedent, extended with an explicit `suspended`
field on BOTH share-listing endpoints for avatar-stack consistency across collection-scoped and
direct shares.

Purpose: D-3's avatar stack and D-1's Sharing overview both need "who is this item shared with" for
a personal item's direct shares, and this data has never existed on the wire. Land it now, before
any UI plan needs to consume it.
Output: `GET /api/vault/items/{id}/shares`, wired into the existing `/api/vault/items/{id}/shares`
route entry (a new HTTP method on an already-registered path, not a new table row); `access_list`
gains a `suspended` field for the same reason.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-web-app-sharing-ui-family-management/26-CONTEXT.md
@.planning/phases/26-web-app-sharing-ui-family-management/26-RESEARCH.md

# Environment
export PATH="/opt/homebrew/opt/rustup/bin:$HOME/.cargo/bin:$PATH" before any cargo command.

# Exact analog to mirror (do not diverge from this extractor choice — RESEARCH.md Pitfall 4)
crates/pv-server/src/routes/collections.rs:539-580 (access_list) — full pattern: struct shape,
handler shape, error handling, the "Membership<Collection, RequireRead> — any member with ANY
access level authorizes the listing" contract. The new handler swaps `Membership<Collection,
RequireRead>` for `Membership<Item, RequireRead>`, and swaps `collection_keys`/`collection_id` for
`item_shares`/`item_id` in the query — same CoRecipientRecord shape (user_id, email, access_level,
created_at), never `sealed_key` (T-22-16's discipline).
crates/pv-server/src/routes/membership.rs:262-388 (Item::resolve_access) — the item_shares branch's
own JOIN shape (fm_o/fm join through the item OWNER's family) is the reference for how to scope
this new query's suspended-status join correctly; do not hand-roll a different ownership check.
crates/pv-server/src/routes/mod.rs:255-278 (membership_routes) — the `/api/vault/items/{id}/shares`
path is ALREADY registered for `post(vault::create_share)`. axum folds a `.get(...)` chained onto
the SAME tuple entry (`get(vault::list_item_shares).post(vault::create_share)`), matching how
`/api/vault/items/{id}` already chains `put(...).delete(...)` — this does NOT add a new vec entry,
so `membership_routes().len()` stays unchanged; do not bump the cardinality tripwire test in mod.rs.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/vault/items/{id}/shares + suspended flag on both share-listing endpoints</name>
  <files>crates/pv-server/src/routes/vault.rs, crates/pv-server/src/routes/mod.rs, crates/pv-server/src/routes/collections.rs, crates/pv-server/tests/vault.rs, crates/pv-server/tests/collections.rs</files>
  <behavior>
    Test: a member with an active `item_shares` grant appears in the response with `suspended:
    false`; a member whose `item_shares` grant is real but whose `family_members.status` is
    'suspended' appears in the response too, with `suspended: true` — per A-7, never omitted.
    Test: a caller with no relationship to the item (not owner, no item_shares row) gets 404.
    Test: the response never includes `sealed_key` in its JSON keys (T-22-16 discipline, asserted
    by key-absence, not merely "not checked").
    Test: `collections::access_list`'s existing response gains the same `suspended` field, correct
    for both an active and a suspended co-recipient (extends that endpoint's existing test suite
    rather than replacing it).
  </behavior>
  <action>
    Add `pub async fn list_item_shares` to `vault.rs`, mirroring `access_list`'s full shape
    (struct, handler, error handling) with `Membership<Item, RequireRead>` — per RESEARCH.md
    Pitfall 4, this is the ONLY correct extractor; never `FamilyMembership<RequireEdit>` (that is
    `families::member_access`'s owner-only gate, the wrong question for this endpoint's actual
    callers — any member listing shares on their OWN item or an item shared TO them). Query:
    `SELECT s.recipient_user_id, u.email, s.access_level, s.created_at, (fm.status = 'suspended')
    AS suspended FROM item_shares s JOIN users u ON u.id = s.recipient_user_id JOIN family_members
    fm ON fm.user_id = s.recipient_user_id WHERE s.item_id = ? ORDER BY s.created_at ASC,
    s.recipient_user_id ASC` — the `family_members` join here is scoped to the RECIPIENT's own
    membership row (same shape `Item::resolve_access`'s item_shares branch already uses at
    membership.rs:308-318, but without that branch's `fm.status = 'active'` filter — this endpoint
    is a LISTING, per A-7 it must not filter suspended rows out, only flag them). Extend
    `CoRecipientRecord` (reuse the SAME struct from collections.rs, do not define a parallel type)
    with `pub suspended: bool`, and update `collections::access_list`'s own query with an
    equivalent `family_members` join against `ck.recipient_user_id` for consistency (D-3's caveat:
    both share types need consistent suspended treatment in the avatar stack). Register
    `.get(vault::list_item_shares)` chained onto the EXISTING `/api/vault/items/{id}/shares` tuple
    in `membership_routes()` (mod.rs:269) — do not add a new vec entry.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/rustup/bin:$HOME/.cargo/bin:$PATH" && cd /Users/j5on/.work/projects/passkey-vault && cargo test --workspace -p pv-server list_item_shares access_list</automated>
  </verify>
  <done>All four behaviors pass; `cargo build --workspace` is clean; `membership_routes().len()`
  is unchanged from its pre-plan value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client wrapper — listItemShares</name>
  <files>web/src/lib/vault/api.ts</files>
  <behavior>
    Test (typecheck-level, via existing vitest suite for this file if one exists, else a minimal
    new test): `listItemShares(itemId)` returns `Promise<ItemShareEntry[]>` where `ItemShareEntry`
    has `user_id, email, access_level, created_at, suspended`.
  </behavior>
  <action>
    Add `listItemShares(itemId: string): Promise<ItemShareEntry[]>` to `web/src/lib/vault/api.ts`
    (GET `/api/vault/items/${encodeURIComponent(itemId)}/shares` — WR-16 path-encoding discipline)
    and an `ItemShareEntry` interface matching the new `CoRecipientRecord` shape field-for-field.
    Also add `suspended: boolean` to the existing `CollectionAccessEntry` interface, matching
    Task 1's `access_list` extension.
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault/web && npx tsc --noEmit</automated>
  </verify>
  <done>Both interfaces compile and match the server's extended wire shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → `GET /api/vault/items/{id}/shares` | New read endpoint — untrusted caller must be provably a member of the item |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-08 | Information Disclosure | `list_item_shares` leaking to a non-member, or leaking `sealed_key` | high | mitigate | `Membership<Item, RequireRead>` gates every request; response struct field list is a closed set that never includes `sealed_key`, asserted by a key-absence test |
| T-26-09 | Information Disclosure | A suspended recipient's continued visibility in the listing | low | accept | Deliberate per A-7 — the grant genuinely still exists and reinstating restores it; hiding the row would be the dishonest option this phase's whole posture rejects, and mirrors `access_list`'s existing precedent |
</threat_model>

<verification>
`export PATH="/opt/homebrew/opt/rustup/bin:$HOME/.cargo/bin:$PATH" && cargo build --workspace && cargo test --workspace -p pv-server`
`cd web && npx tsc --noEmit`
</verification>

<success_criteria>
`GET /api/vault/items/{id}/shares` exists, is correctly gated, includes suspended recipients
flagged, and both share-listing endpoints now agree on the `suspended` field shape.
</success_criteria>

<output>
Create `.planning/phases/26-web-app-sharing-ui-family-management/26-04-SUMMARY.md` when done,
with `## Threat Flags` populated.
</output>
