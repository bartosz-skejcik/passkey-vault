---
phase: 26-web-app-sharing-ui-family-management
plan: 06
type: execute
wave: 3
depends_on: ["26-04", "26-05"]
files_modified:
  - web/src/lib/families/accessLevel.ts
  - web/src/lib/families/accessLevel.test.ts
  - web/src/components/settings/RemoveMemberDialog.tsx
  - web/src/components/settings/RemoveMemberDialog.test.tsx
  - web/src/lib/i18n/dictionary.ts
  - web/src/components/vault/AvatarStack.tsx
  - web/src/components/vault/AvatarStack.test.tsx
  - web/src/lib/vault/shareRecipients.ts
  - web/src/lib/vault/shareRecipients.test.ts
autonomous: true
requirements: [UX-05, UX-03, SHARE-03]

must_haves:
  truths:
    - "Every plan after this one that needs share/sharing/identity/folder copy, the access-level vocabulary, or an avatar stack imports one shared source instead of redefining its own."
    - "An item row shows an overlapping avatar stack of exactly who it is shared with, with a +N overflow past 3, and shows nothing while that data hasn't resolved yet — never a wrong or empty-looking flash."
  artifacts:
    - "web/src/lib/families/accessLevel.ts — accessLevelKey/accessRank/higherAccess, extracted from RemoveMemberDialog.tsx, now imported by it rather than defined in it."
    - "web/src/components/vault/AvatarStack.tsx — D-3's circle-stack and icon-only variants."
    - "web/src/lib/vault/shareRecipients.ts — resolves a VaultItem's recipient list (collection access-list or direct item-shares) without a fetch per item on every render."
  key_links:
    - "dictionary.ts's D-2 hard honesty strings, copied verbatim from 26-UI-SPEC.md's Copywriting Contract -> consumed unmodified by Plan 26-08's ShareDialog and Plan 26-12's fingerprint card."
    - "AvatarStack(item) -> shareRecipients.ts resolves by item.collectionId (cached per-collection access_list) or item.isShared+no-collection (per-item listItemShares, cached) -> renders circles or +N, or nothing while unresolved."
---

<objective>
Land the shared building blocks every remaining UI plan in this phase depends on, in ONE plan, so
`dictionary.ts` (a single file every later plan would otherwise all need to touch) and the
access-level vocabulary are each edited exactly once this phase, not re-touched by five parallel
plans. Also builds D-3's `AvatarStack.tsx` — this codebase's first overlapping-circle/initials
component, no existing analog to extend — and the data source it needs, deliberately avoiding an
N+1 fetch per row against a list that can hold hundreds of items.

Purpose: Every subsequent plan (ShareDialog, CollectionPicker, Sidebar, SharingOverviewPanel,
FamilyTab, ItemRow/DetailPanel wiring) either renders share/sharing/identity/folder copy, needs the
`read`/`edit`/`hidden_password` vocabulary, or needs to show "who is this shared with" — building
each of those once here means those plans do not collide on `dictionary.ts`.
Output: `accessLevel.ts`, the full i18n dictionary pass for this phase, `AvatarStack.tsx`, and
`shareRecipients.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-web-app-sharing-ui-family-management/26-CONTEXT.md
@.planning/phases/26-web-app-sharing-ui-family-management/26-UI-SPEC.md

# Copy every key from 26-UI-SPEC.md's "## Copywriting Contract" table VERBATIM
Do not retype the Polish diacritics from memory or paraphrase — copy the exact PL+EN strings for
every key in that table (share.hiddenPasswordDisclosureTitle/Body/Ack, share.hiddenPasswordInlineNote,
identity.fingerprintMismatchWarning, and every representative key below it: share.itemDialogTitle,
share.folderDialogTitleExisting, share.folderDialogTitleNew, share.recipientsLabel,
share.noOtherMembers, share.accessLevelLabel, share.ctaFolder, share.ctaItem, share.sharing,
share.createFailed, share.newFolderNameLabel, share.itemSharedOnCollectionNote, sharing.navLabel,
sharing.overviewHeading, sharing.tabByFolder, sharing.tabByPerson, sharing.emptyHeading,
sharing.emptyBody, sharing.sharedWithLabel, identity.yourFingerprintHeading,
identity.fingerprintRevealAria, identity.fingerprintUnavailable, identity.fingerprintCopyAria,
folder.pickerLabel, folder.pickerNone, folder.pickerCreateNew, folder.pickerEmpty). Do NOT retire
`invite.scopeFolderComingSoon`/`invite.scopeFolderUnavailableNote` in this plan — Plan 26-12 removes
those in the SAME task that stops referencing them in FamilyTab.tsx (removing a still-referenced
key here would break `npx tsc --noEmit`).
Honesty constraints 1-5 (26-UI-SPEC.md, same section, bottom) are hard requirements the checker
re-verifies — read them before writing the two D-2/UX-03 strings.

# Existing precedent (extract from here, don't redefine)
web/src/components/settings/RemoveMemberDialog.tsx:46-73 — `ACCESS_LEVEL_KEY`/`accessLevelKey`
  (WR-13's fail-closed-to-unknown discipline)/`accessRank`/`higherAccess` — extract all three
  functions verbatim into the new shared module; this file becomes an IMPORTER, not the owner.
web/src/lib/i18n/dictionary.ts:1102-1112 — `access.readOnly`/`access.fullEdit`/
  `access.hiddenPassword`/`access.unknown` already exist — reuse verbatim, do not add a second set.
web/src/components/invite/InviteLandingView.tsx:259-293 — the "available vs. unavailable" branch
  shape and `invite.fingerprintHonesty` precedent this phase's NEW
  `identity.fingerprintMismatchWarning` sits alongside (never merged into it — different key,
  different failure-mode purpose, per honesty constraint 5).

# AvatarStack has no analog — build from 26-UI-SPEC.md's E5 contract directly
20px circles, -space-x-2 overlap, ring-2 ring-base-100 per circle, first char of the recipient's
email local-part uppercased as the initial (no display-name field exists anywhere in this schema),
1-3 recipients render that many circles, 4+ renders 3 circles + a 4th `+N` circle in
`bg-base-300 text-base-content/60`, ONE `aria-label` summarizing the whole set (never one label per
circle), icon-only variant (a single small Share2 icon in text-secondary, no circles) for narrow
contexts (Sidebar's shared-folder rows, wired by Plan 26-10).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: accessLevel.ts extraction + full i18n dictionary pass</name>
  <files>web/src/lib/families/accessLevel.ts, web/src/lib/families/accessLevel.test.ts, web/src/components/settings/RemoveMemberDialog.tsx, web/src/components/settings/RemoveMemberDialog.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <behavior>
    Test: `accessLevelKey("read")`/`("edit")`/`("hidden_password")` return the three existing
    `access.*` keys; an unrecognized value returns `access.unknown` (WR-13, never the most
    reassuring fallback).
    Test: `higherAccess("read", "edit")` returns `"edit"`; `accessRank` orders
    read < hidden_password < edit, matching `membership.rs::combine_access`'s server-side rank.
    Test: `RemoveMemberDialog`'s existing test suite passes unmodified in outcome (behavior-neutral
    refactor) after switching to the imported functions.
    Test: every new dictionary key has both a `pl` and an `en` value (no partial entries);
    `share.hiddenPasswordDisclosureBody` and `identity.fingerprintMismatchWarning` match the
    UI-SPEC's exact strings byte-for-byte (the two D-2/UX-03/honesty-constraint-5 hard strings).
  </behavior>
  <action>
    Create `web/src/lib/families/accessLevel.ts` exporting `accessLevelKey`, `accessRank`,
    `higherAccess` (moved verbatim from `RemoveMemberDialog.tsx:46-73`), with a dedicated
    `web/src/lib/families/accessLevel.test.ts` carrying this task's own unit tests for the three
    functions directly (not only indirectly through `RemoveMemberDialog.test.tsx`, which tests the
    dialog's behavior, not the module's own contract). Update `RemoveMemberDialog.tsx` to import
    all three from the new module and delete its own local copies — a behavior-neutral refactor,
    no functional change. Add every key listed in this
    plan's `<context>` "Copy every key" note to `web/src/lib/i18n/dictionary.ts`, sourced verbatim
    from `26-UI-SPEC.md`'s Copywriting Contract table (PL+EN for each), placed near the existing
    `access.*`/`family.*`/`invite.*` sections for locality. Do not remove or modify any existing
    key.
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault/web && npx vitest run src/lib/families/accessLevel.test.ts src/components/settings/RemoveMemberDialog.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>RemoveMemberDialog's tests pass unmodified in outcome; every new dictionary key has PL+EN;
  the two hard honesty strings match the UI-SPEC verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AvatarStack.tsx + shareRecipients.ts data source</name>
  <files>web/src/components/vault/AvatarStack.tsx, web/src/components/vault/AvatarStack.test.tsx, web/src/lib/vault/shareRecipients.ts, web/src/lib/vault/shareRecipients.test.ts</files>
  <behavior>
    Test: 1, 2, and 3 recipients each render that many circles with the correct initials; 4+
    recipients render 3 circles plus a `+N` circle where N is the true remaining count.
    Test: a suspended recipient renders with a distinct visible treatment (not merely present in
    the same aria-label as an active one — D-3/A-7's "visible suspended treatment").
    Test: the stack's single `aria-label` summarizes every recipient (built from
    `sharing.sharedWithLabel`'s interpolation shape); no circle carries an individual label.
    Test: while the recipient data hasn't resolved yet, the component renders zero circles — not a
    skeleton, not a placeholder (E5's loading backstop).
    Test (`shareRecipients.ts`): for a collection-scoped item, the module fetches that
    COLLECTION's access list via `getCollectionAccessList` ONCE and reuses it for every item in
    that same collection (asserted via a fetch-call-count spy across two items sharing one
    `collectionId`) — never once per item. For a directly-shared personal item (`isShared === true`,
    `collectionId` null/undefined), the module calls `listItemShares(item.id)` (Plan 26-04) and
    caches the result by item id.
    Test: the icon-only variant renders a single `Share2`-shaped element in the documented
    `text-secondary` treatment, no circles, same `aria-label` contract as the full stack.
  </behavior>
  <action>
    Create `web/src/lib/vault/shareRecipients.ts`: a small module-level two-tier cache (Map keyed
    by collection id for `getCollectionAccessList` results, Map keyed by item id for
    `listItemShares` results) plus a `useShareRecipients(item: VaultItem):
    {email: string; suspended: boolean}[] | null` hook (`null` = not yet resolved). Resolution
    rule: `item.collectionId` present → resolve via the collection-id cache (fetch once per
    collection, share across every item in it); else `item.isShared === true` → resolve via the
    item-id cache (fetch once per item, on first mount that needs it); else return `[]`
    immediately (not shared, no fetch). Create `web/src/components/vault/AvatarStack.tsx`
    implementing D-3/E5's contract exactly as specified in this plan's `<context>` note above,
    consuming `useShareRecipients` internally for its default variant, plus an explicit
    `variant="icon"` prop for the icon-only case (Plan 26-10 passes recipients it already has,
    since Sidebar rows are per-COLLECTION not per-item — accept an optional pre-resolved
    `recipients` prop so the icon variant does not need its own fetch).
  </action>
  <verify>
    <automated>cd /Users/j5on/.work/projects/passkey-vault/web && npx vitest run src/components/vault/AvatarStack.test.tsx src/lib/vault/shareRecipients.test.ts</automated>
  </verify>
  <done>All behaviors above pass; the collection-scoped N+1-avoidance test specifically proves
  one fetch per collection, not per item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Consumes only already-authorized Plan 26-04/pre-existing endpoints; no new network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-12 | Tampering | An unrecognized access-level string silently mapped to the least-alarming label | medium | mitigate | `accessLevelKey`'s WR-13 fail-closed-to-`access.unknown` discipline, now the ONE place this logic lives, imported everywhere rather than reimplemented |
| T-26-13 | Information Disclosure | A stale cached `shareRecipients` entry showing an outdated recipient set after a revocation | low | accept | `shareRecipients.ts`'s cache is refreshed on any store re-render triggered by Plan 26-05's `onSharedRevisions`/personal-snapshot re-merge, matching this codebase's existing "poll/WS-driven eventual consistency, never a hard real-time guarantee" posture for every other list in the app |
</threat_model>

<verification>
`cd web && npx tsc --noEmit && npx vitest run src/lib/families/accessLevel.test.ts src/components/settings/RemoveMemberDialog.test.tsx src/components/vault/AvatarStack.test.tsx src/lib/vault/shareRecipients.test.ts`
</verification>

<success_criteria>
Every downstream plan can import `accessLevel.ts`, the new dictionary keys, `AvatarStack`, and
`shareRecipients.ts` without touching `dictionary.ts` or redefining access-level vocabulary again.
</success_criteria>

<output>
Create `.planning/phases/26-web-app-sharing-ui-family-management/26-06-SUMMARY.md` when done,
with `## Threat Flags` populated.
</output>
