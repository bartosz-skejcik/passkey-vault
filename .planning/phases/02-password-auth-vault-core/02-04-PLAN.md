---
phase: 02-password-auth-vault-core
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - .gitignore
  - web/.env.local
  - web/src/lib/i18n/dictionary.ts
  - web/src/lib/i18n/LocaleContext.tsx
  - web/src/lib/generator/strength.ts
  - web/src/lib/auth/api.ts
  - web/src/lib/auth/session.ts
  - web/src/lib/auth/pendingUnlock.ts
  - web/src/lib/crypto/index.ts
  - web/src/lib/idle/useIdleTimer.ts
  - web/src/components/auth/AuthCard.tsx
  - web/src/components/auth/RegisterForm.tsx
  - web/src/components/auth/LoginForm.tsx
  - web/src/components/auth/UnlockOverlay.tsx
  - web/src/components/shell/Sidebar.tsx
  - web/src/components/self-test/SelfTestCard.tsx
  - web/src/app/layout.tsx
  - web/src/app/page.tsx
  - web/src/app/self-test/page.tsx
  - web/src/app/globals.css
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-08]

must_haves:
  truths:
    - "A visitor can register a new account (email + master password) and land, unlocked, inside the vault shell without re-typing their password"
    - "A returning user can log in and is routed to the shell with the unlock overlay ACTIVE — login succeeding does not, by itself, decrypt anything; a DOM inspector confirms no plaintext item data exists behind the blur"
    - "After the configured idle period (default 15 min) with the vault unlocked, the app auto-locks: the WASM UserKey handle is freed and the unlock overlay reappears, while the session token (and thus the logged-in state) survives"
    - "The UI text (buttons, labels, aria-labels) is switchable between Polish and English from the sidebar, persists across reload, with no flash of the wrong language on load"
  artifacts:
    - path: "web/src/lib/crypto/index.ts"
      provides: "unlocked-UserKey singleton (setUnlockedUserKey/getUnlockedUserKey/lockVault/isUnlocked) subscribable via useSyncExternalStore"
      contains: "subscribeLockState"
    - path: "web/src/components/auth/UnlockOverlay.tsx"
      provides: "the visibly-distinct unlock step over a blurred, data-free shell"
      contains: "backdrop-blur"
    - path: "web/src/lib/i18n/dictionary.ts"
      provides: "PL/EN copy for every string this phase introduces"
      contains: "pl"
  key_links:
    - "web/src/components/auth/RegisterForm.tsx and LoginForm.tsx both call web/src/lib/crypto/index.ts's deriveAuthMaterial/generateUserKey/wrapUserKey/unwrapUserKey (Plan 02-01's facade) — never import ./wasm directly"
    - "web/src/lib/auth/api.ts's request/response shapes match Plan 02-02's routes/auth.rs handlers exactly (email/kdf/salt/auth_hash/pw_wrapped_uk field names)"
    - "web/src/lib/idle/useIdleTimer.ts's onIdle callback calls web/src/lib/crypto/index.ts's lockVault(), which notifies useIsUnlocked() subscribers to re-render the UnlockOverlay"
---

<objective>
Deliver the browser half of AUTH-01/AUTH-02/AUTH-08: registration, login, the architecturally-distinct unlock overlay, and idle auto-lock — the point at which a real person can open the app in a browser, create an account, and see their (still-empty, per Plan 02-05) vault behind a real unlock gate. Also establishes i18n (PL/EN, client-side dictionary, no framework routing — required by `output: "export"`) as this phase's first UI work, since every component this phase adds needs translated copy from day one per CONTEXT.md's locked decision.

Purpose: Complete AUTH-01 (register UI) and AUTH-02 (login UI + the visibly-distinct unlock step) and AUTH-08 (auto-lock) at the point a real user experiences them; establish the i18n contract every later component in this phase consumes.
Output: Working Register → Login → Unlock → auto-lock journey in the browser, PL/EN switchable, wired to Plans 02-01/02-02's crypto and API contracts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-password-auth-vault-core/02-CONTEXT.md
@.planning/phases/02-password-auth-vault-core/02-RESEARCH.md
@.planning/phases/02-password-auth-vault-core/02-UI-SPEC.md
@.planning/phases/02-password-auth-vault-core/02-01-SUMMARY.md
@.planning/phases/02-password-auth-vault-core/02-02-SUMMARY.md
@docs/UI-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n dictionary + auth API client + strength util (contracts)</name>
  <files>.gitignore, web/.env.local, web/src/lib/i18n/dictionary.ts, web/src/lib/i18n/LocaleContext.tsx, web/src/lib/generator/strength.ts, web/src/lib/auth/api.ts, web/src/lib/auth/session.ts, web/src/lib/auth/pendingUnlock.ts</files>
  <read_first>
    web/src/app/layout.tsx (themeInitScript pre-hydration pattern — localeInitScript mirrors this exactly, added in Task 3)
    web/src/components/shell/Sidebar.tsx (localStorage read/write try/catch pattern to replicate for locale/session storage)
    .planning/phases/02-password-auth-vault-core/02-UI-SPEC.md (Copywriting Contract table AND Icon-only controls accessibility contract table — every PL/EN pair in BOTH tables must exist as a dictionary key; this is the full string inventory for this phase, not just what Task 2/3 of this plan consume)
    .planning/phases/02-password-auth-vault-core/02-01-SUMMARY.md (exact exported crypto facade function names from Plan 02-01)
    .planning/phases/02-password-auth-vault-core/02-02-SUMMARY.md (exact request/response JSON field names from Plan 02-02's auth routes)
  </read_first>
  <action>
    Append `web/.env.local` to the root `.gitignore` (below the existing `.env` line) — this file holds the dev-only API base URL override and must never be committed.

    Create `web/.env.local` with `NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8620` (matches `pv-server`'s default `PV_ADDR`) — this is the dev-time override; production (Phase 7's single-container deployment) leaves this unset, so `apiFetch` falls back to same-origin relative paths.

    Create `web/src/lib/i18n/dictionary.ts`: `export type Locale = "pl" | "en";` and `export const DICTIONARY: Record<string, { pl: string; en: string }>` with one entry per row of UI-SPEC.md's Copywriting Contract table AND its Icon-only controls accessibility contract table, keyed by short dot-path strings you choose (e.g. `"auth.loginSubmit"`, `"auth.registerSubmit"`, `"auth.toggleToRegister"`, `"auth.toggleToLogin"`, `"auth.irrecoverableWarning"`, `"unlock.heading"`, `"unlock.submit"`, `"autolock.label"`, `"aria.lockNow"`, `"aria.changeLanguage"`, etc.) — copy the PL/EN text VERBATIM from UI-SPEC.md, do not paraphrase. Add two entries this plan needs beyond UI-SPEC's tables (not specified there, since logout wasn't in the original design pass but Plan 02-02 added a real logout endpoint that needs a UI trigger): `"auth.logout": { pl: "Wyloguj", en: "Log out" }` and `"aria.logout": { pl: "Wyloguj się", en: "Log out" }`. Export `export function t(locale: Locale, key: keyof typeof DICTIONARY): string { return DICTIONARY[key][locale]; }`.

    Create `web/src/lib/i18n/LocaleContext.tsx` (`"use client"`): a React context provider `LocaleProvider({ children })` that reads the initial locale from `document.documentElement.getAttribute("lang")` in a `useEffect` (mirrors Sidebar's theme-read pattern — the inline pre-hydration script, added in Task 3, has already set this attribute before first paint), holds `[locale, setLocaleState]` state, and a `setLocale(next: Locale)` function that updates state, sets `document.documentElement.setAttribute("lang", next)`, and persists via `try { localStorage.setItem("pv-locale", next); } catch {}`. Export `useLocale(): { locale: Locale; setLocale: (l: Locale) => void; t: (key: keyof typeof DICTIONARY) => string }` consuming the context, with `t` bound to the current locale.

    Create `web/src/lib/generator/strength.ts`: `export type Strength = "weak" | "medium" | "strong";` and `export function scorePasswordStrength(password: string): Strength` — a heuristic combining length and character-class diversity (lowercase/uppercase/digit/symbol present), e.g. length < 8 → "weak" regardless of diversity; length >= 8 with only 1 character class → "weak"; length >= 12 with 2+ classes, or length >= 8 with 3+ classes → "medium"; length >= 16 with 3+ classes → "strong". This is created here (ahead of the full generator in a later plan) because RegisterForm's inline strength meter (UI-SPEC's locked requirement) needs it now — the later generator plan reuses this file rather than recreating it.

    Create `web/src/lib/auth/session.ts`: `getSessionToken()`/`setSessionToken(token: string)`/`clearSessionToken()` and `getStoredEmail()`/`setStoredEmail(email: string)`/`clearStoredEmail()`, all `localStorage`-backed (keys `"pv-session-token"`, `"pv-account-email"`) with the established `try { ... } catch {}` defensive wrapping — the session token is an auth credential (not vault-secret material), so ordinary `localStorage` is the CONTEXT.md-locked choice for v0.1 (httpOnly-cookie hardening is explicitly deferred pre-v1.0, do not implement it here).

    Create `web/src/lib/auth/pendingUnlock.ts`: a module-level (non-persisted, cleared-on-read) holder for the just-derived wrapping key + `pw_wrapped_uk` produced by a same-tab `LoginForm` submission, so `UnlockOverlay` (Task 3) can skip re-deriving Argon2id when unlocking immediately follows login in the same session: `export function setPendingUnlock(wrappingKey: WasmWrappingKey, pwWrappedUk: string): void` and `export function takePendingUnlock(): { wrappingKey: WasmWrappingKey; pwWrappedUk: string } | null` (returns and clears in one call — a second call returns `null`). Import `WasmWrappingKey` as a type only from `@/lib/crypto` (Plan 02-01's re-exported type), never from `./wasm`.

    Create `web/src/lib/auth/api.ts`: `const API_BASE = process.env.NEXT_PUBLIC_API_BASE_URL ?? "";` `export async function apiFetch(path: string, init?: RequestInit): Promise<Response>` — prefixes `API_BASE`, sets `Content-Type: application/json` when a body is present, adds `Authorization: Bearer ${token}` when `getSessionToken()` returns non-null. Export this function (not just the auth-specific wrappers below) — Plan 02-05's `lib/vault/api.ts` reuses it directly for `/api/vault/*` calls rather than duplicating the base-URL/auth-header logic. Export typed wrappers matching Plan 02-02's exact JSON shapes (read `02-02-SUMMARY.md` for the authoritative field names before writing these): `prelogin(email: string): Promise<{ kdf: object; salt: string }>`, `register(body: { email: string; kdf: object; salt: string; auth_hash: string; pw_wrapped_uk: string }): Promise<{ user_id: string }>`, `login(body: { email: string; auth_hash: string }): Promise<{ session_token: string; pw_wrapped_uk: string }>`, `logout(): Promise<void>`, `me(): Promise<{ user_id: string; email: string; pw_wrapped_uk: string }>`. Each throws a typed `ApiClientError` (with the parsed `{error: string}` body message) on a non-2xx response, so calling components can distinguish e.g. 409 (duplicate email) from 401 (bad credentials) via `error.status`.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes (no test regressions from these new, currently test-free utility modules; existing crypto facade tests still green).
    - `grep -c '"pl":\|pl:' web/src/lib/i18n/dictionary.ts` shows every UI-SPEC.md Copywriting Contract row and Icon-only aria-label row has a corresponding entry (spot-checked: `grep -c "auth.loginSubmit\|unlock.heading\|aria.lockNow\|aria.changeLanguage" web/src/lib/i18n/dictionary.ts` is at least 4).
    - `.gitignore` contains `web/.env.local`.
    - `grep -c "from ['\"]\./wasm" web/src/lib/auth/pendingUnlock.ts` is 0 (type-only import from `@/lib/crypto`, choke-point invariant preserved).
  </acceptance_criteria>
  <done>i18n dictionary/context, session/pending-unlock storage helpers, the auth API client, and the standalone strength scorer all exist and are ready for Task 2/3 to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register/Login forms + unauthenticated routing</name>
  <files>web/src/components/auth/AuthCard.tsx, web/src/components/auth/RegisterForm.tsx, web/src/components/auth/LoginForm.tsx, web/src/app/page.tsx, web/src/app/layout.tsx</files>
  <read_first>
    web/src/app/page.tsx (current Phase 1 placeholder — this task replaces its top-level routing logic)
    web/src/app/layout.tsx (current themeInitScript/html wiring — this task wraps children in LocaleProvider, do not touch localeInitScript here, that is Task 3's job)
    web/src/lib/auth/api.ts, web/src/lib/auth/session.ts, web/src/lib/auth/pendingUnlock.ts (this plan's Task 1)
    web/src/lib/crypto/index.ts (Plan 02-01 — deriveAuthMaterial/generateUserKey/wrapUserKey/randomSalt/defaultKdfParamsJson)
    web/src/lib/generator/strength.ts (this plan's Task 1)
    web/src/lib/i18n/LocaleContext.tsx (this plan's Task 1)
    .planning/phases/02-password-auth-vault-core/02-UI-SPEC.md (Auth screens section — exact field lists, 400px card vocabulary, irrecoverable-password alert, strength meter placement)
  </read_first>
  <behavior>
    - `RegisterForm`, on submit with matching passwords, derives auth material ONCE, POSTs register then immediately POSTs login with the SAME derived `auth_hash` (no second Argon2id pass, no second password prompt), stores the session token, and calls `setUnlockedUserKey` directly with the freshly-generated User Key (no unwrap needed — the key was just generated in this same flow).
    - `RegisterForm` shows a mismatch validation error (not a network call) when password !== confirm password.
    - `LoginForm`, on submit, calls `prelogin(email)` then derives auth material against the returned salt/kdf, POSTs login, stores the session token AND stashes the derived wrapping key + returned `pw_wrapped_uk` via `setPendingUnlock` — it does NOT call `unwrapUserKey` itself (that is `UnlockOverlay`'s job in Task 3, preserving the visibly-distinct unlock step).
    - A wrong-password login surfaces the server's 401 as a plain validation message, not a thrown/unhandled rejection.
  </behavior>
  <action>
    Create `web/src/components/auth/AuthCard.tsx`: a shared 400px-wide, `base-100` surface, 16px-radius, 1px-`base-300`-border, 24px-padding, vertically-centered card wrapper (per UI-SPEC's Auth screens spacing vocabulary) taking `children` and a `heading` prop (Heading-role, 20px/700) — no shell chrome (sidebar/top bar), since the user isn't authenticated yet.

    Create `web/src/components/auth/RegisterForm.tsx` (`"use client"`): fields email, master password, confirm master password (plain DM Sans, `ui-monospace` for the password inputs per UI-SPEC's font rules), an inline strength meter driven by `scorePasswordStrength(password)` (success/warning/error DaisyUI segment coloring per UI-SPEC's Color section), and a persistent (non-dismissible) `alert alert-warning` showing `t("auth.irrecoverableWarning")` above the submit button. On submit: validate passwords match (`t("validation.passwordMismatch")` inline error if not, no network call); `const salt = randomSalt(16); const passwordBytes = new TextEncoder().encode(password); const material = deriveAuthMaterial(passwordBytes, salt, defaultKdfParamsJson()); const authHash = material.takeAuthHash(); const wrappingKey = material.takeWrappingKey(); const uk = generateUserKey(); const pwWrappedUk = wrapUserKey(wrappingKey, uk);` → `await register({ email, kdf: JSON.parse(defaultKdfParamsJson()), salt: base64Encode(salt), auth_hash: base64Encode(authHash), pw_wrapped_uk: pwWrappedUk })` → on success, `await login({ email, auth_hash: base64Encode(authHash) })` (same `authHash` bytes, no re-derivation) → `setSessionToken(session_token); setStoredEmail(email); setUnlockedUserKey(uk);` — the user lands fully unlocked, having typed their password exactly once. Surface a 409 (duplicate email) as a field-level error under the email input, using the `t("auth.registerSubmit")`-adjacent error copy pattern already established by UI-SPEC's validation rows.

    Create `web/src/components/auth/LoginForm.tsx` (`"use client"`): fields email, master password. On submit: `const { kdf, salt } = await prelogin(email);` decode `salt` from base64, `const material = deriveAuthMaterial(new TextEncoder().encode(password), decodedSalt, JSON.stringify(kdf)); const authHash = material.takeAuthHash(); const wrappingKey = material.takeWrappingKey();` → `const { session_token, pw_wrapped_uk } = await login({ email, auth_hash: base64Encode(authHash) });` → on success: `setSessionToken(session_token); setStoredEmail(email); setPendingUnlock(wrappingKey, pw_wrapped_uk);` — do NOT call `unwrapUserKey`/`setUnlockedUserKey` here; a 401 response surfaces as a single generic inline error (`t` key covering both "wrong password" and "no such account" with identical wording, matching Plan 02-02's server-side indistinguishability).

    Both forms toggle via the Secondary-blue text link (`t("auth.toggleToRegister")`/`t("auth.toggleToLogin")`), not tabs, per UI-SPEC.

    Rewrite `web/src/app/page.tsx`'s top-level logic: on mount, check `getSessionToken()`. If absent → render `<AuthCard>` with `RegisterForm`/`LoginForm` toggled by local state (default: Login, since a returning self-hosted user is the more common case — new registration is reached via the toggle link). If present → render the existing shell (`Sidebar`/`TopBar`/`MainColumn`) — Task 3 adds the `UnlockOverlay` gating logic on top of this branch; for now, an authenticated session with no unlocked key renders the shell with its Phase-1-era empty-state content (Task 3 layers the overlay in without this task needing to know about lock state yet — keep this task's routing to exactly two branches: unauthenticated vs. authenticated).

    In `web/src/app/layout.tsx`, wrap `{children}` with `<LocaleProvider>{children}</LocaleProvider>` inside `<body>` (import from `@/lib/i18n/LocaleContext`) — do not modify `themeInitScript` or add `localeInitScript` here, that belongs to Task 3.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes, including new tests for `RegisterForm`'s password-mismatch validation (no network call fired) and `LoginForm`'s single-derivation-then-pending-unlock flow (mocked `deriveAuthMaterial`/`login`/`setPendingUnlock`).
    - `grep -c "unwrapUserKey" web/src/components/auth/LoginForm.tsx` is 0 (login never unwraps directly — that is Task 3's `UnlockOverlay` responsibility).
    - `grep -c "setUnlockedUserKey" web/src/components/auth/RegisterForm.tsx` is at least 1 (register unlocks immediately using its own freshly-generated key, no second password prompt).
  </acceptance_criteria>
  <done>A visitor can register (landing unlocked, one password entry) or log in (landing at an authenticated-but-not-yet-unlocked state) against the real Plan 02-02 API and Plan 02-01 crypto facade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unlock overlay, lock-state singleton, idle auto-lock, settings dropdown, self-test relocation</name>
  <files>web/src/lib/crypto/index.ts, web/src/lib/idle/useIdleTimer.ts, web/src/components/auth/UnlockOverlay.tsx, web/src/components/shell/Sidebar.tsx, web/src/components/self-test/SelfTestCard.tsx, web/src/app/layout.tsx, web/src/app/page.tsx, web/src/app/self-test/page.tsx, web/src/app/globals.css</files>
  <read_first>
    web/src/lib/crypto/index.ts (Plan 02-01's current exports — this task ADDS the lock-state singleton on top, does not restructure existing exports)
    web/src/lib/auth/pendingUnlock.ts, web/src/lib/auth/api.ts, web/src/lib/auth/session.ts (this plan's Task 1)
    web/src/components/auth/LoginForm.tsx (this plan's Task 2 — the pending-unlock producer)
    web/src/components/self-test/SelfTestCard.tsx (current fatal-branch early-return that is MISSING the retry button, and the "patrz błąd poniżej" copy needing the reword fix — both are carried-forward Phase 1 UI-REVIEW fixes)
    .planning/phases/02-password-auth-vault-core/02-UI-SPEC.md (Unlock overlay treatment — backdrop-blur-md/bg-base-300/70 scrim/z-50; Auto-lock & settings surface section — dropdown contents and idle-timeout options; Carried-forward fixes list)
  </read_first>
  <behavior>
    - `isUnlocked()` returns `false` initially; after `setUnlockedUserKey(uk)` it returns `true`; after `lockVault()` it returns `false` again, and the previously-set handle's `.free()` was called exactly once (not on every lock call if already locked).
    - A component using `useIsUnlocked()` re-renders when `setUnlockedUserKey`/`lockVault` are called (verified via a `renderHook`-style test with `act()`).
    - `useIdleTimer(timeoutMs, onIdle)` calls `onIdle` once after `timeoutMs` of no simulated DOM activity (fake timers), and resets the timer on a simulated activity event without calling `onIdle` early.
  </behavior>
  <action>
    Extend `web/src/lib/crypto/index.ts` with a lock-state singleton (module-level, alongside the existing `ready` promise singleton — same file, same conventions): `let currentUserKey: WasmUserKey | null = null; const lockListeners = new Set<() => void>();` `export function setUnlockedUserKey(uk: WasmUserKey): void` (frees any existing `currentUserKey` first, then assigns and notifies), `export function getUnlockedUserKey(): WasmUserKey | null`, `export function lockVault(): void` (frees `currentUserKey` if non-null, sets it to `null`, notifies — a no-op notify-free early-return if already `null`, so repeated idle-timer firings don't double-notify), `export function isUnlocked(): boolean`, `export function subscribeLockState(listener: () => void): () => void` (adds to `lockListeners`, returns an unsubscribe closure). Add `web/src/lib/crypto/useIsUnlocked.ts` (or export from the same `index.ts` if simpler — keep the choke-point file itself free of React imports if this project's convention separates pure logic from hooks; since Phase 1 has no such split established, adding the hook directly to `index.ts` alongside its data is acceptable): `export function useIsUnlocked(): boolean` implemented via React's `useSyncExternalStore(subscribeLockState, isUnlocked, () => false)` (third arg = a stable `false` snapshot for any non-browser render path, matching this app's client-components-only architecture).

    Create `web/src/lib/idle/useIdleTimer.ts` (`"use client"`): `export function useIdleTimer(timeoutMs: number, onIdle: () => void): void` — attaches listeners for `mousemove`, `keydown`, `click`, `scroll` (passive) on `window`/`document`, each resetting a `setTimeout` that calls `onIdle` after `timeoutMs`; cleans up all listeners and the pending timeout on unmount or when `timeoutMs`/`onIdle` change. Read the configured timeout from `localStorage.getItem("pv-autolock-minutes")` (default `"15"`) at the call site (Sidebar/page.tsx), not inside this hook — the hook itself is a pure timeout-minutes-agnostic primitive.

    Create `web/src/components/auth/UnlockOverlay.tsx` (`"use client"`): renders ONLY when `getSessionToken()` is present AND `useIsUnlocked()` is `false`. Layout per UI-SPEC: the real shell renders behind it (this component does not replace the shell — `page.tsx`, updated below, renders both, with the shell's data-bearing children only mounted when unlocked — see the hard DOM requirement below), `backdrop-blur-md` applied to the shell layer, `bg-base-300/70` scrim, a 400px/16px-radius/24px-padding unlock card at `z-50`, heading `t("unlock.heading")`, submit `t("unlock.submit")`. On mount, check `takePendingUnlock()`: if present, render a single button (no password field) that calls `unwrapUserKey(pending.wrappingKey, pending.pwWrappedUk)` then `setUnlockedUserKey(result)`. If absent (fresh reload / post-idle-lock — no in-memory material survives either of those), render a master-password field; on submit, call `me()` to (a) confirm the session token is still valid and (b) fetch the current `pw_wrapped_uk`, then `prelogin(storedEmail)` for `{kdf, salt}`, derive the wrapping key via `deriveAuthMaterial`, and `unwrapUserKey`/`setUnlockedUserKey` as above. If `me()` returns 401 (session actually expired), clear the stored session/email and force a re-render into the unauthenticated (Login) state — do not leave the user staring at a broken unlock form for a session that no longer exists.

    **Hard requirement, not cosmetic-only** (per UI-SPEC): whatever component tree renders the actual vault data (Plan 02-05's `ItemList`/`DetailPanel` — not yet built) must only be mounted, or must receive no decrypted item props/state, while `useIsUnlocked()` is `false`. Since Plan 02-05 hasn't landed yet, satisfy this NOW at the `page.tsx` level: `MainColumn`'s children (in the authenticated branch) are only rendered when `useIsUnlocked()` is `true`; when `false`, `MainColumn` renders with no children (Phase 1's built-in empty-state text still shows, blurred, which contains no per-user data — acceptable, since it is static copy, not vault contents).

    In `web/src/app/page.tsx`: import `UnlockOverlay` and `useIsUnlocked`; in the authenticated branch, render the shell as `<div className={!unlocked ? "blur-md" : undefined}>...</div>` (or apply the blur at the shell's outer wrapper) with `<UnlockOverlay />` rendered as a sibling positioned above it (per the z-50/scrim treatment) whenever `getSessionToken()` is present and `!unlocked`.

    Extend `web/src/components/shell/Sidebar.tsx`'s existing account block with a DaisyUI `dropdown` (trigger: the existing account row) containing, in order: an idle-timeout `<select>`/radio group (`t("autolock.label")`, options 1/5/15/30/60 minutes, default 15, persisted to `localStorage["pv-autolock-minutes"]`), a language switcher (`Languages` icon button, `aria-label={t("aria.changeLanguage")}`, cycles PL↔EN via `useLocale().setLocale`), a "Zablokuj teraz"/"Lock now" action (`Lock` icon, `aria-label={t("aria.lockNow")}`, calls `lockVault()` directly), and a "Wyloguj"/"Log out" action (`aria-label={t("aria.logout")}`, calls `await logout(); clearSessionToken(); clearStoredEmail(); lockVault();` then a full page reload or router-level reset back to the unauthenticated Login screen). Wire the idle-timeout `<select>`'s persisted value into a `useIdleTimer` call placed at the `page.tsx` level (only active while `useIsUnlocked()` is `true` — no point running the idle timer while already locked), with `onIdle={lockVault}`.

    In `web/src/app/layout.tsx`: add the `localeInitScript` (mirrors `themeInitScript`'s exact IIFE-in-`<script dangerouslySetInnerHTML>` shape — reads `localStorage['pv-locale']`, validates it's `'pl'`/`'en'`, sets `document.documentElement.setAttribute('lang', locale)`, falls back to `'pl'` in the catch branch) as a second `<script>` tag alongside the existing theme script; change `<html lang="pl">` to plain `<html>` (the script now sets it dynamically, matching how `data-theme` already has no hardcoded default).

    Fix the two carried-forward Phase 1 UI-REVIEW items in `web/src/components/self-test/SelfTestCard.tsx`: (1) the `fatal` branch's early-return is currently missing the "Uruchom ponownie" retry button entirely — add the same `<button onClick={run}>` present in the non-fatal render path to the fatal branch's JSX too, so a fatal `initCrypto()` failure is always recoverable without a full page reload; (2) reword the partial-failure summary line from `"${passedCount}/5 kroków przeszło — patrz błąd poniżej"` to `"${passedCount}/5 kroków przeszło — patrz błąd przy kroku powyżej"` (EN equivalent: `"...see the error at the step above"`) — both strings now sourced from the i18n dictionary (Task 1), not hardcoded.

    Move the self-test off the home route: create `web/src/app/self-test/page.tsx` composing `Sidebar`/`TopBar`/`MainColumn`/`SelfTestCard` exactly as `page.tsx` did in Phase 1; remove `SelfTestCard` from `web/src/app/page.tsx` entirely (its content is now the auth/vault routing this plan and Plan 02-05 own).

    Fix the light-theme `base-300` token in `web/src/app/globals.css`'s `vault-light` theme block: change `--color-base-300: oklch(98.86% 0.0017 67.80);` to `--color-base-300: oklch(93% 0.004 67.80);` — the current value duplicates `base-200` exactly, making every `border-base-300` divider invisible in light mode (Phase 1 UI-REVIEW Priority Fix #1, WARNING-level, folded into this phase's first light-mode-relevant shell touch).
  </action>
  <verify>
    <automated>cd web && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes, including the lock-state singleton tests and the idle-timer fake-timer tests from `<behavior>`.
    - `npm run build` (static export) exits 0.
    - `grep -c "backdrop-blur" web/src/components/auth/UnlockOverlay.tsx` is at least 1.
    - `grep -c "Uruchom ponownie\|t(\"self-test" web/src/components/self-test/SelfTestCard.tsx` shows the retry button now present in BOTH the fatal and non-fatal render branches (region-scoped: check the fatal-branch block specifically contains a button element, not just the file overall).
    - `grep -c "oklch(98.86% 0.0017 67.80)" web/src/app/globals.css` is exactly 1 (only `base-200`'s original value remains; `base-300` in the `vault-light` block now reads `oklch(93% 0.004 67.80)`).
    - `<verify><human-check>` Load the app, register a new account: confirm you land unlocked with no unlock overlay. Log out, log back in: confirm the unlock overlay appears blurred over the shell, and the browser DevTools Elements panel shows no item text behind the blur (there is none yet — Plan 02-05 adds real items). Set auto-lock to 1 minute, wait, confirm the overlay reappears without losing the logged-in state (no redirect to Login).</human-check>
  </acceptance_criteria>
  <done>The full register→login→unlock→auto-lock→lock-now→logout journey works in the browser; i18n switches without a flash of the wrong language; both Phase 1 UI-REVIEW carry-forward fixes are applied.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- **Files:** `web/.env.local` (new), `web/src/lib/i18n/{dictionary.ts,LocaleContext.tsx}` (new), `web/src/lib/generator/strength.ts` (new), `web/src/lib/auth/{api.ts,session.ts,pendingUnlock.ts}` (new), `web/src/lib/idle/useIdleTimer.ts` (new), `web/src/components/auth/{AuthCard,RegisterForm,LoginForm,UnlockOverlay}.tsx` (new), `web/src/app/self-test/page.tsx` (new), `web/src/lib/crypto/index.ts` (edited — lock-state singleton added), `web/src/components/shell/Sidebar.tsx` (edited — settings dropdown), `web/src/components/self-test/SelfTestCard.tsx` (edited — two fixes), `web/src/app/{layout.tsx,page.tsx,globals.css}` (edited)
- **TS exports:** `Locale`, `DICTIONARY`, `t()`, `LocaleProvider`, `useLocale()`, `scorePasswordStrength()`, `Strength`, `apiFetch`, `prelogin/register/login/logout/me`, `ApiClientError`, session/pendingUnlock getters+setters, `setUnlockedUserKey/getUnlockedUserKey/lockVault/isUnlocked/subscribeLockState/useIsUnlocked`, `useIdleTimer`
- **React components:** `AuthCard`, `RegisterForm`, `LoginForm`, `UnlockOverlay`

These are all newly-created symbols this phase introduces — later drift verification should not flag them as undocumented.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser DOM ↔ user-visible content while locked | The blurred-shell requirement exists precisely so a locked session never exposes plaintext vault data even cosmetically |
| `localStorage` (session token, email, locale, autolock minutes) | Persisted, non-httpOnly, readable by any script in the page — an accepted residual XSS risk per CONTEXT.md's explicit deferred decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Information Disclosure | Locked shell rendering vault data behind the blur | critical | mitigate | `MainColumn`'s data-bearing children are only mounted when `useIsUnlocked()` is `true`; blur is cosmetic reinforcement, not the actual protection — the protection is "no data in the render tree" |
| T-02-15 | Information Disclosure | Bearer token in `localStorage`, readable by any injected script (XSS) | medium | accept | Explicit, already-documented CONTEXT.md decision ("httpOnly-cookie approach revisited pre-v1.0"); mitigated elsewhere via React's default JSX escaping and no `dangerouslySetInnerHTML` outside the two pre-hydration scripts (theme + locale), not re-litigated here |
| T-02-16 | Repudiation / stale-session UX | `UnlockOverlay`'s full-reload path calling an actually-expired session | low | mitigate | `me()` call before attempting unwrap detects an expired/deleted session and forces a clean transition to the unauthenticated Login screen, rather than leaving a dead unlock form |
| T-02-17 | Tampering | Idle timer double-firing `lockVault` (e.g. tab visibility changes) | low | mitigate | `lockVault()` early-returns (no notify) if already locked, making repeated `onIdle` calls idempotent |

</threat_model>

<verification>
`cd web && npm test && npm run build` must both exit 0. The human-check step is required for the DOM-inspection and cross-reload lock-persistence claims that automated tests cannot fully observe in this plan (no real vault data exists yet to inspect against — Plan 02-05 sharpens this check once items exist).
</verification>

<success_criteria>
- Register lands the user unlocked in one password entry; login lands the user authenticated-but-locked.
- Idle timeout frees the WASM UK handle and re-shows the unlock overlay without logging the user out.
- i18n switches PL/EN with no FOUC, persisted across reload.
- Both Phase 1 UI-REVIEW carry-forward items (self-test retry button, "patrz błąd" copy) are fixed.
</success_criteria>

<output>
Create `.planning/phases/02-password-auth-vault-core/02-04-SUMMARY.md` when done, listing the final crypto-facade lock-state API and i18n dictionary key set for Plans 02-05/02-06 to read.
</output>
