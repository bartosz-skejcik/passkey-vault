---
phase: 02-password-auth-vault-core
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - web/src/lib/vault/types.ts
  - web/src/lib/vault/api.ts
  - web/src/lib/vault/store.ts
  - web/src/lib/vault/search.ts
  - web/src/components/vault/ItemList.tsx
  - web/src/components/vault/ItemRow.tsx
  - web/src/components/vault/DetailPanel.tsx
  - web/src/components/vault/TypePicker.tsx
  - web/src/components/vault/ItemForm.tsx
  - web/src/components/shell/TopBar.tsx
  - web/src/app/page.tsx
  - web/src/lib/i18n/dictionary.ts
autonomous: true
requirements: [VAULT-01, VAULT-02, VAULT-03, VAULT-04, UI-03]

must_haves:
  truths:
    - "Once unlocked, a user can click '+ Nowy item', pick a type (login/card/identity/note), fill in the form, save, and immediately see it in the list — the full create-and-view loop works end-to-end through real encryption and the real server API"
    - "Typing in the search box instantly filters the visible list by name/username/domain, entirely client-side (no network request per keystroke)"
    - "Every item's ciphertext is encrypted/decrypted using the SAME item id (client-generated) it was created with — the AD binding from Plan 02-01 is exercised for real, not just in unit tests"
    - "No third-party favicon fetch exists anywhere in the item list — only the neutral type-icon renders (privacy-by-default, per RESEARCH.md's explicit favicon-deferral finding)"
    - "A user can assign a folder and free-form tags to an item directly from the create/edit form (populated from useFolders()/useAllTags()), not only view them from a later sidebar filter — VAULT-03's organization model is reachable at the point of data entry, not just at browse time"
  artifacts:
    - path: "web/src/lib/vault/store.ts"
      provides: "client-side item state: fetch-decrypt-on-unlock, create/list, in-memory cache"
      contains: "decryptItem"
    - path: "web/src/lib/vault/store.ts"
      provides: "folder decrypt/create plumbing (useFolders/createVaultFolder) + tag derivation (useAllTags) that ItemForm's folder-select/tag-input consume (VAULT-03)"
      contains: "useFolders"
    - path: "web/src/lib/vault/search.ts"
      provides: "instant client-side search index over decrypted items"
      contains: "search"
    - path: "web/src/components/vault/ItemForm.tsx"
      provides: "type-specific create form for all four item types, including a shared folder-select + tag multi-select (VAULT-03)"
      contains: "passkeyPlaceholder"
  key_links:
    - "web/src/lib/vault/store.ts generates the item's id client-side (crypto.randomUUID()) BEFORE calling encryptItem, then sends that same id to POST /api/vault/items — the server (Plan 02-03) never generates item ids, because the AD binding (Plan 02-01) requires the id to exist before encryption"
    - "web/src/lib/vault/store.ts recombines the server's separate enc_key/enc_data fields into the single combined JSON string web/src/lib/crypto/index.ts's decryptItem expects, and splits encryptItem's combined output back into enc_key/enc_data before POSTing — the wire shape (two columns) and the crypto facade's shape (one combined EncryptedItem JSON) are deliberately different and must be bridged here"
    - "web/src/app/page.tsx's unlocked branch now renders web/src/components/vault/ItemList.tsx as MainColumn's children instead of Plan 02-04's empty placeholder"
---

<objective>
Deliver the first real, demoable vault capability: a user can create a login/card/identity/note item, see it appear in the list, open its details, and search across everything they've saved — the thinnest complete vertical slice through crypto (Plan 02-01) → API (Plan 02-03) → UI. Edit, delete, folders/tags, the password generator, and clipboard auto-clear are Plan 02-06's job (validation/error-states/polish layered on top of this working core), per the MVP task-order pattern (failing E2E path → thinnest real slice → polish).

Purpose: Complete VAULT-01's create+read path, VAULT-02's real (not just unit-tested) exercise of AD-bound encryption, VAULT-04's instant client-side search, and UI-03's list+detail+create UI.
Output: A working "add an item, see it, open it, find it" loop against the real server and real crypto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-password-auth-vault-core/02-CONTEXT.md
@.planning/phases/02-password-auth-vault-core/02-RESEARCH.md
@.planning/phases/02-password-auth-vault-core/02-UI-SPEC.md
@.planning/phases/02-password-auth-vault-core/02-03-SUMMARY.md
@.planning/phases/02-password-auth-vault-core/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vault types, API client, store (fetch-decrypt-on-unlock), search index</name>
  <files>web/src/lib/vault/types.ts, web/src/lib/vault/api.ts, web/src/lib/vault/store.ts, web/src/lib/vault/search.ts</files>
  <read_first>
    web/src/lib/auth/api.ts (Plan 02-04 — the exported `apiFetch` this file reuses for `/api/vault/*` calls)
    web/src/lib/crypto/index.ts (Plan 02-01/02-04 — `encryptItem`/`decryptItem`/`getUnlockedUserKey`/`subscribeLockState`/`isUnlocked`)
    .planning/phases/02-password-auth-vault-core/02-03-SUMMARY.md (exact `/api/vault/items`/`/api/vault/folders` request/response field names — `id`, `enc_key`, `enc_data`, `revision`, `expected_revision`)
    .planning/phases/02-password-auth-vault-core/02-UI-SPEC.md (Vault list + detail panel section — the four item types' field lists)
  </read_first>
  <behavior>
    - `store`'s decrypt path recombines a server row's separate `enc_key`/`enc_data` strings into the single JSON shape `decryptItem` expects (`{"enc_key": JSON.parse(enc_key), "enc_data": JSON.parse(enc_data)}`, stringified), calls `decryptItem(uk, combined, row.id, row.revision)`, and parses the result as the item's `ItemFields` JSON.
    - `store`'s create path generates a fresh id via `crypto.randomUUID()`, calls `encryptItem(uk, JSON.stringify(fields), id, 1)`, splits the combined result back into `enc_key`/`enc_data` before calling the API client's `createItem`.
    - A search query matching an item's `name` (case-insensitive, partial) returns it; a query matching neither name/username/domain across any item returns an empty array — no network call is made by the search function itself (it operates over an already-decrypted in-memory array).
    - When `subscribeLockState` fires with `isUnlocked() === true`, the store's list is (re-)fetched and decrypted; when it fires with `false` (lock), the store's in-memory item array is cleared to empty (never left populated with stale plaintext while locked).
    - `createVaultFolder(name)` encrypts the folder name via the same `encryptItem`/`decryptItem` pair item ciphertext uses (folder id as the AD `item_id`, revision fixed at 1, unsplit combined JSON stored in `enc_name`), and the new folder is immediately visible via `useFolders()` without a re-fetch.
    - `useAllTags()` returns the deduplicated union of every currently-loaded item's `fields.tags` array.
  </behavior>
  <action>
    Create `web/src/lib/vault/types.ts`: `export type ItemType = "login" | "card" | "identity" | "note";` a discriminated union `ItemFields` with a `type` tag and per-type fields exactly matching UI-SPEC.md's Vault list + detail panel section field lists (login: name, username, password, url, notes, folderId, tags, — plus the inert passkey-placeholder is UI-only, not a data field; card: name, cardholderName, number, expiry, cvv, notes, folderId, tags; identity: name, firstName, lastName, email, phone, address, notes, folderId, tags; note: name, body, folderId, tags). `export interface VaultItem { id: string; revision: number; fields: ItemFields; }` and `export interface Folder { id: string; name: string; }`.

    Create `web/src/lib/vault/api.ts`: import `apiFetch` from `@/lib/auth/api`. `export async function listItems(): Promise<{ id: string; enc_key: string; enc_data: string; revision: number }[]>` (GET `/api/vault/items`), `export async function createItem(id: string, encKey: string, encData: string): Promise<{ id: string; revision: number }>` (POST `/api/vault/items` with `{id, enc_key: encKey, enc_data: encData}`), `export async function updateItem(id: string, encKey: string, encData: string, expectedRevision: number): Promise<{ revision: number }>` (PUT `/api/vault/items/{id}`), `export async function deleteItem(id: string): Promise<void>` (DELETE `/api/vault/items/{id}`), `export async function listFolders(): Promise<{ id: string; enc_name: string }[]>`, `export async function createFolder(encName: string): Promise<{ id: string }>`, `export async function deleteFolder(id: string): Promise<void>` — mirror Plan 02-03's exact endpoint paths/field names (read `02-03-SUMMARY.md` before writing these, do not assume).

    Create `web/src/lib/vault/store.ts` (module-level singleton, same pattern family as `lib/crypto/index.ts`'s `ready` promise and lock-state singleton): `let items: VaultItem[] = []; const listeners = new Set<() => void>();` `export function getItems(): VaultItem[]`, `export function subscribeItems(listener: () => void): () => void`. `async function loadAndDecryptAll(): Promise<void>` — calls `listItems()`, for each row recombines `enc_key`/`enc_data` into the combined JSON `decryptItem` expects, calls `decryptItem(getUnlockedUserKey()!, combined, row.id, row.revision)`, `JSON.parse`s the plaintext into `ItemFields`, builds the `VaultItem[]` array, assigns to `items`, notifies listeners. `export async function createVaultItem(fields: ItemFields): Promise<VaultItem>` — `const id = crypto.randomUUID(); const plaintext = JSON.stringify(fields); const combined = encryptItem(getUnlockedUserKey()!, plaintext, id, 1); const { enc_key, enc_data } = splitCombinedEncryptedItem(combined);` (a small private helper doing the inverse of the recombine step — `JSON.parse(combined)` then `JSON.stringify` each of its `enc_key`/`enc_data` sub-objects back into their own strings) `await createItem(id, enc_key, enc_data);` then push `{id, revision: 1, fields}` into `items`, notify, return it. Wire the module's top-level side effect: `subscribeLockState(() => { if (isUnlocked()) { void loadAndDecryptAll(); } else { items = []; notifyListeners(); } });` (imported from `@/lib/crypto`) — this is what makes "unlock the vault" automatically populate the list and "lock the vault" automatically clear it from memory, satisfying Plan 02-04's hard DOM requirement now that real data exists. Export `export function useVaultItems(): VaultItem[]` via `useSyncExternalStore(subscribeItems, getItems, () => [])`.

    In the SAME `store.ts` module, add folder state and tag derivation NOW (not deferred to Plan 02-06) because Task 3's `ItemForm` folder-select/tag-input needs it to exist in this plan, and UI-SPEC's login-form field list (folder select, tag multi-select) requires it at create time, not just at browse time: `let folders: Folder[] = []; const folderListeners = new Set<() => void>();` a `subscribeFolders`/`getFolders` pair and `export function useFolders(): Folder[]` via `useSyncExternalStore(subscribeFolders, getFolders, () => [])`. Extend `loadAndDecryptAll` to also call `listFolders()`, decrypt each row's `enc_name` via `decryptItem(getUnlockedUserKey()!, row.enc_name, row.id, 1)` (no split/recombine needed — folders store one `enc_name` column matching `encryptItem`'s single combined-JSON output exactly, unlike items), `JSON.parse` into `{name}`, assign to `folders`, notify folder listeners. `export async function createVaultFolder(name: string): Promise<Folder>` — `const id = crypto.randomUUID(); const encName = encryptItem(getUnlockedUserKey()!, JSON.stringify({ name }), id, 1); await createFolder(encName);` push `{id, name}` into `folders`, notify, return it. Add `export function useAllTags(): string[]` computing the de-duplicated union of every item's `fields.tags` from the current `items` array — no server storage for tags, per CONTEXT.md's locked model. (Plan 02-06 adds `deleteVaultFolder` and the Sidebar folder/tag nav UI on top of this already-established plumbing — it does not recreate `useFolders`/`createVaultFolder`/`useAllTags`.)

    Create `web/src/lib/vault/search.ts`: `export function searchItems(items: VaultItem[], query: string): VaultItem[]` — case-insensitive partial match against `fields.name`, and (when present) `fields.username`/`fields.url`-derived domain for login items; returns `items` unchanged when `query` is empty/whitespace.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes, including new tests for the recombine/split round-trip (a value encrypted then immediately decrypted through the split→recombine path yields the original `ItemFields`), the lock/unlock subscription behavior (items populate on unlock, clear on lock), and `searchItems`'s case-insensitive partial matching.
    - `grep -c "crypto.randomUUID" web/src/lib/vault/store.ts` is at least 1 (client generates the item id, never the server).
    - `grep -c "fetch(" web/src/lib/vault/search.ts` is 0 (search is purely in-memory, no network call).
    - `grep -c "useFolders\|createVaultFolder\|useAllTags" web/src/lib/vault/store.ts` is at least 3 (folder decrypt/create plumbing and tag derivation are present in this plan, ready for Task 3's `ItemForm` to consume — not deferred to Plan 02-06).
  </acceptance_criteria>
  <done>The vault store fetches, decrypts, and caches items on unlock, clears them on lock, supports client-side creation with correct AD-bound encryption, search works entirely in-memory, and folder/tag plumbing (create + derive) is ready for the form layer.</done>
</task>

<task type="auto">
  <name>Task 2: List + detail view (read mode)</name>
  <files>web/src/components/vault/ItemList.tsx, web/src/components/vault/ItemRow.tsx, web/src/components/vault/DetailPanel.tsx, web/src/components/shell/TopBar.tsx, web/src/app/page.tsx</files>
  <read_first>
    web/src/lib/vault/store.ts, web/src/lib/vault/search.ts (this plan's Task 1 — `useVaultItems`, `searchItems`)
    web/src/components/shell/TopBar.tsx (current disabled search input and disabled "+ Nowy item" button — this task enables the search input only; the button's click handler is wired in Task 3, which owns the create flow)
    web/src/components/shell/MainColumn.tsx (current empty-state — Task keeps this component as-is, just supplies real children now)
    web/src/app/page.tsx (Plan 02-04's authenticated/unlocked branch — currently renders MainColumn with no children)
    .planning/phases/02-password-auth-vault-core/02-UI-SPEC.md (Vault list + detail panel section —64px row height, 32px type-icon, favicon/type-icon treatment, selected-row styling, detail panel view-mode layout)
  </read_first>
  <action>
    Create `web/src/components/vault/ItemRow.tsx`: 64px height, 16px horizontal padding, 8px internal gap row. Left: a 32px/8px-radius neutral type-icon (`Vault`=login, `CreditCard`=card, `IdCard`=identity, `StickyNote`=note — `lucide-react`, already a dependency). **Documented decision:** UI-SPEC's "favicon/Globe fallback" wording (UI-03) is deliberately NOT implemented this phase — no per-domain favicon fetch of any kind exists anywhere in `web/src/components/vault/`; the neutral type-icon alone satisfies UI-03's baseline visual-differentiator requirement, per RESEARCH.md's Pitfall 4 finding that third-party favicon services leak visited-site metadata to whichever service resolves the icon (also tracked as threat T-02-18 below). Favicon fetching (e.g. via a privacy-preserving same-origin proxy, so no third party learns which sites a user has saved) is out of scope for Phase 2 and deferred to a later phase — this is a scoped-out feature, not an oversight. Then `fields.name` (Body/400) + a subtitle (Label/400, `base-content/60`) showing `fields.username` for login items or the type label otherwise. Trailing: a neutral `badge-sm` (icon+label, no color-coding) and a static 8px muted-gray health-dot (`bg-base-content/20`, non-interactive — real health scoring is out of scope, HEALTH-01 is a v0.3 feature). Selected state: `bg-primary/8%` fill + 2px coral left border (per UI-SPEC's typography-substitute emphasis rule — no new font weight).

    Create `web/src/components/vault/ItemList.tsx` (`"use client"`): takes a `searchQuery` prop (lifted from `TopBar`, see below) and an `onSelect(item: VaultItem)` callback; reads `useVaultItems()`, filters via `searchItems`, renders one `ItemRow` per result (empty-results state: plain-DM-Sans text `t("search.emptyResults")` when `searchQuery` is non-empty and results are empty — distinct from the zero-items-ever-created Fuzzy-Bubbles empty state, which `MainColumn` already owns).

    Create `web/src/components/vault/DetailPanel.tsx` (`"use client"`, VIEW MODE ONLY this task — edit/create wiring is Task 3): 400px fixed width desktop / full-width overlay below `md`, `base-100` surface. Renders the selected item's name as Heading, then each field as a Label (field name) + value (Body, `ui-monospace` for password/card-number fields specifically, plain for everything else) row — no copy buttons yet (Plan 02-06 wires clipboard). Two fields are special-cased rather than fed through the generic Label+value loop: `fields.folderId` renders as a resolved folder name via a `useFolders()` lookup (`t("item.noFolder")` when null/absent) instead of a raw id, and `fields.tags` renders as a row of neutral `badge-sm` tag chips (same neutral treatment as the type badge) instead of a raw array — both surface VAULT-03's organization data meaningfully rather than as opaque values. For `type === "login"` items, render the inert passkey placeholder section (bordered sub-panel, `KeyRound` icon in teal `#00CDB7`, copy `t("item.passkeyPlaceholder")`) exactly as UI-SPEC specifies — no add-passkey action, this section does nothing but display.

    In `web/src/components/shell/TopBar.tsx`: remove `disabled` from the search `<input>`, lift its value up via an `onSearchChange` prop (TopBar becomes a small controlled component receiving `value`/`onChange` from its parent, `page.tsx`) so `ItemList` and `TopBar` share the same search state. Leave the "+ Nowy item" button `disabled` in this task — Task 3 wires it.

    In `web/src/app/page.tsx`'s authenticated+unlocked branch: hold `const [searchQuery, setSearchQuery] = useState("")` and `const [selectedItem, setSelectedItem] = useState<VaultItem | null>(null)`; pass `searchQuery`/`setSearchQuery` to `TopBar`; render `<MainColumn><ItemList searchQuery={searchQuery} onSelect={setSelectedItem} />{selectedItem && <DetailPanel item={selectedItem} onClose={() => setSelectedItem(null)} />}</MainColumn>` (or an equivalent list-plus-side-panel layout matching UI-SPEC's "list stays visible/scrollable alongside the panel on desktop" requirement).
  </action>
  <verify>
    <automated>cd web && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes (component-level tests for `ItemRow` type-icon selection and `ItemList` filtering-by-search-query).
    - `npm run build` exits 0.
    - `grep -rc "fetch(\`https://" web/src/components/vault/` across all files in the directory is 0 (no third-party favicon fetch anywhere).
    - `grep -c "KeyRound" web/src/components/vault/DetailPanel.tsx` is at least 1 (passkey placeholder section present for login items).
  </acceptance_criteria>
  <done>A user can see their (currently empty, until Task 3 adds create) vault as a list, search it instantly, and open a detail panel showing decrypted fields — no favicon fetch, no plaintext ever rendered without a real decrypt having happened first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Type picker + create forms (login/card/identity/note)</name>
  <files>web/src/components/vault/TypePicker.tsx, web/src/components/vault/ItemForm.tsx, web/src/components/shell/TopBar.tsx, web/src/app/page.tsx, web/src/lib/i18n/dictionary.ts</files>
  <read_first>
    web/src/lib/vault/store.ts (this plan's Task 1 — `createVaultItem`, and now also `useFolders()`/`createVaultFolder`/`useAllTags` for this task's folder-select/tag-input)
    web/src/lib/vault/types.ts (this plan's Task 1 — the four `ItemFields` shapes)
    web/src/components/vault/DetailPanel.tsx (this plan's Task 2 — the panel this create flow shares screen real estate with)
    web/src/lib/i18n/LocaleContext.tsx, web/src/lib/i18n/dictionary.ts (Plan 02-04 — `useLocale`, `DICTIONARY`; this task adds the item-type/field-label keys not already present)
    .planning/phases/02-password-auth-vault-core/02-UI-SPEC.md (Vault list + detail panel section — 4-tile type picker, per-type field lists, "plain DM Sans, no emoji" rule for forms)
  </read_first>
  <behavior>
    - Selecting a type in `TypePicker` renders that type's form with empty fields; submitting with a required `name` field empty shows a validation error and does not call `createVaultItem`.
    - Submitting a valid login form calls `createVaultItem` with `{type: "login", name, username, password, url, notes, folderId, tags}` and, on success, closes the create flow and the new item is visible in `ItemList` (already covered by Task 1's store test — this task's test asserts the FORM correctly shapes its submitted `ItemFields` object per type).
    - The folder `<select>` and tag input are present and functional identically across all four item types (not login-only); selecting a folder sets `fields.folderId`, adding a tag sets `fields.tags`, and both survive into the submitted `ItemFields` object.
  </behavior>
  <action>
    Add any item-type/field-label dictionary keys not already covered by Plan 02-04's Task 1 pass over UI-SPEC.md's Copywriting Contract table (re-check `web/src/lib/i18n/dictionary.ts` for gaps against UI-SPEC's "Item create — type picker", "Item save", "Item edit", "Passkey placeholder section" rows plus any per-field labels this form needs that aren't in that table already — field labels like "Nazwa"/"Name", "Użytkownik"/"Username" etc. are Claude's-discretion copy not explicitly enumerated in UI-SPEC's table; keep them short, plain, consistent with the rest of the app's tone). Explicitly add these keys, not in UI-SPEC's table but required by the folder-select/tag-input below: `"item.folderLabel"` ("Folder"/"Folder"), `"item.tagsLabel"` ("Tagi"/"Tags"), `"item.noFolder"` ("Bez folderu"/"No folder"), `"aria.newFolder"` ("Nowy folder"/"New folder" — reused verbatim by Plan 02-06's Sidebar "+ new folder" affordance per UI-SPEC's Icon-only controls table; do not create a duplicate key there).

    Create `web/src/components/vault/TypePicker.tsx`: 4 tiles (login/card/identity/note), icon (`Vault`/`CreditCard`/`IdCard`/`StickyNote`) + label per tile, no color-coding (same neutral treatment as `ItemRow`'s type badges), `onSelect(type: ItemType)` callback, heading `t("item.typePickerHeading")`.

    Create `web/src/components/vault/ItemForm.tsx` (`"use client"`, one component switching on `type` prop rather than four separate files — keeps the per-type field lists co-located and easy to compare against UI-SPEC's table in one place): required `name` field (validation error `t("validation.required")` if empty on submit, no network call); type-specific fields per UI-SPEC exactly — login: username, password (`ui-monospace`, `Eye`/`EyeOff` toggle — the trailing generator-popover trigger button is a Plan 02-06 addition, render the password field without it for now, not as a "TODO" but simply because the generator doesn't exist in this plan's scope), url, notes, and the inert passkey placeholder section (reuse `DetailPanel`'s exact placeholder markup/copy — extract it to a small shared `PasskeyPlaceholderSection.tsx` if that avoids duplicating the JSX, or duplicate the small block if extraction adds more indirection than it saves — planner's call, either is acceptable); card: cardholderName, number (`ui-monospace`, masked with reveal toggle), expiry, cvv (`ui-monospace`, masked); identity: firstName, lastName, email, phone, address; note: body (plain multi-line `<textarea>`, explicitly NOT `ui-monospace` — secure notes are prose). On submit: build the type-specific `ItemFields` object, call `await createVaultItem(fields)`, then call the `onCreated()` callback (closes the create flow, e.g. clearing `page.tsx`'s "creating" state).

    Add a shared (non-type-specific) folder/tag field block to `ItemForm`, rendered identically for all four types per UI-SPEC line 172 (folder select + tag multi-select apply to every item's organization, not just logins): a folder `<select>` (`t("item.folderLabel")`, a leading `t("item.noFolder")` empty option, then one `<option>` per `useFolders()` entry, writing to `fields.folderId`) with a small inline `Plus` affordance (`aria-label={t("aria.newFolder")}`) that opens a one-field name prompt calling `createVaultFolder(name)` and immediately selecting the newly-created folder in the same `<select>`; and a tag input (`t("item.tagsLabel")`) implemented as a simple comma-or-Enter-delimited tag-chip input, offering `useAllTags()`'s existing tags as autocomplete suggestions, writing the resulting array to `fields.tags: string[]` — no new tag-management screen; tags are freeform per CONTEXT.md's locked client-side-only model. This block satisfies VAULT-03's "assign a folder/tag while editing an item" requirement (Plan 02-06's Sidebar work adds *filtering* by folder/tag on top of this, reusing the same `useFolders()`/`useAllTags()` plumbing established in this plan's Task 1).

    In `web/src/components/shell/TopBar.tsx`: remove `disabled` from the "+ Nowy item" button, wire its `onClick` to a prop `onNewItem` (called by `page.tsx`).

    In `web/src/app/page.tsx`: add `const [creating, setCreating] = useState(false)` and `const [creatingType, setCreatingType] = useState<ItemType | null>(null)`; `onNewItem` sets `creating = true` (and clears `selectedItem`); when `creating` and `creatingType === null`, render `TypePicker` in the detail-panel slot; once a type is chosen, render `ItemForm` for that type in the same slot; `onCreated` resets both `creating`/`creatingType` back to their initial state.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes, including the required-`name`-validation test and a per-type "submits the correctly-shaped ItemFields object" test for at least the login and note types.
    - `grep -c "ui-monospace" web/src/components/vault/ItemForm.tsx` is present on password/number/cvv fields and ABSENT on the note body textarea (region-scoped check — grep the note-type JSX block specifically, not the whole file, since the file legitimately uses `ui-monospace` elsewhere).
    - `grep -c "useFolders\|useAllTags" web/src/components/vault/ItemForm.tsx` is at least 2 (folder-select and tag-input are wired to the store's folder/tag plumbing, not stubbed).
    - `<verify><human-check>` In a real browser: unlock the vault, click "+ Nowy item", pick "login", fill in a name+username+password, create a new folder via the folder select's "+" affordance and add a tag, save — confirm it appears in the list immediately, click it, confirm the detail panel shows the decrypted values, the resolved folder name, the tag chip, and the inert passkey section.</human-check>
  </acceptance_criteria>
  <done>A user can create any of the four item types — including assigning a folder and tags — and immediately see and open them; the full create-and-view vertical slice is real and working end to end.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- **Files:** `web/src/lib/vault/{types.ts,api.ts,store.ts,search.ts}` (new), `web/src/components/vault/{ItemList,ItemRow,DetailPanel,TypePicker,ItemForm}.tsx` (new), `web/src/components/shell/TopBar.tsx` (edited — search + new-item wiring), `web/src/app/page.tsx` (edited — vault content replaces Plan 02-04's placeholder), `web/src/lib/i18n/dictionary.ts` (edited — item-type/field-label keys added)
- **TS exports:** `ItemType`, `ItemFields`, `VaultItem`, `Folder`, `listItems/createItem/updateItem/deleteItem/listFolders/createFolder/deleteFolder`, `getItems/subscribeItems/useVaultItems/createVaultItem`, `getFolders/subscribeFolders/useFolders/createVaultFolder/useAllTags`, `searchItems`
- **React components:** `ItemList`, `ItemRow`, `DetailPanel`, `TypePicker`, `ItemForm`

These are all newly-created symbols this phase introduces — later drift verification should not flag them as undocumented.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Decrypted item plaintext, held only in the browser's in-memory store | Never touches the server; cleared on lock (Plan 02-04's hard requirement, now exercised with real data) |
| Client-generated item id → server storage → AD-bound decryption | The id must round-trip exactly; any mismatch anywhere in this chain fails decryption loudly (Plan 02-01's guarantee), not silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Information Disclosure | Third-party favicon fetch | medium | mitigate | `ItemRow` renders only the neutral type-icon; no `fetch()` call to any icon service exists in `web/src/components/vault/` (grep-audited in Task 2's acceptance criteria) — per RESEARCH.md's Bitwarden/Vaultwarden-documented favicon-privacy finding |
| T-02-19 | Information Disclosure | Stale decrypted items surviving a lock event | critical | mitigate | `store.ts`'s `subscribeLockState` callback clears the in-memory `items` array to empty the instant `isUnlocked()` becomes `false` — verified by Task 1's lock/unlock subscription test |
| T-02-20 | Tampering | Client-supplied item `id` colliding with an existing id (own or another user's) | low | mitigate | Plan 02-03's `ON CONFLICT(id) DO NOTHING` + `rows_affected()` check rejects a colliding id with 409 rather than silently overwriting; astronomically unlikely for client-generated UUIDv4s in practice |

</threat_model>

<verification>
`cd web && npm test && npm run build` must both exit 0 after all three tasks. The Task 3 human-check is required because it is the first point in this phase where real create→encrypt→store→fetch→decrypt→render happens in a live browser against the real server, not mocks.
</verification>

<success_criteria>
- Create → list → search → open works end-to-end for all four item types.
- No favicon fetch anywhere.
- Lock clears decrypted data from memory; unlock repopulates it.
</success_criteria>

<output>
Create `.planning/phases/02-password-auth-vault-core/02-05-SUMMARY.md` when done, listing the final vault store/API exports for Plan 02-06 to read.
</output>
