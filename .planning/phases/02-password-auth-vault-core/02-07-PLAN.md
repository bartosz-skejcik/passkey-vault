---
phase: 02-password-auth-vault-core
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pv-server/src/routes/vault.rs
  - crates/pv-server/tests/vault.rs
  - web/src/lib/vault/api.ts
  - web/src/lib/vault/store.ts
  - web/src/lib/vault/store.test.ts
  - web/src/lib/vault/types.ts
  - web/src/lib/vault/search.ts
  - web/src/lib/vault/search.test.ts
  - web/src/lib/format/relativeTime.ts
  - web/src/lib/format/relativeTime.test.ts
  - web/src/lib/i18n/dictionary.ts
  - web/src/components/shell/Sidebar.tsx
  - web/src/components/shell/Sidebar.test.tsx
  - web/src/components/generator/GeneratorDialog.tsx
  - web/src/components/generator/GeneratorDialog.test.tsx
  - web/src/components/vault/ItemRow.tsx
  - web/src/components/vault/ItemRow.test.tsx
  - web/src/components/vault/ItemContextMenu.tsx
  - web/src/components/vault/ItemContextMenu.test.tsx
autonomous: true
requirements: [UI-03, VAULT-04, VAULT-06]
gap_closure: true

must_haves:
  truths:
    - "The sidebar groups navigation into four labeled sections — Categories (All Items, Logins, Cards, Identities, Notes, plus a disabled Passkeys entry badged 'soon'), Folders, Tags, and Tools (Password Generator) — and selecting a category/folder/tag filters the visible item list by that criterion, entirely client-side"
    - "Each item row shows a relative last-updated time (e.g. '5m ago') derived from the server's real updated_at column, not a client-guessed or hardcoded value"
    - "A user can right-click an item row or click its kebab (More) button to open a menu offering type-appropriate copy actions, Move to folder, Edit, and Delete — every action reuses the existing safe clipboard-auto-clear, optimistic-concurrency, and delete-confirmation paths rather than a new hand-rolled one"
  artifacts:
    - path: "web/src/components/vault/ItemContextMenu.tsx"
      provides: "kebab/right-click item action menu (copy, move, edit, delete)"
      contains: "onDeleteRequest"
    - path: "web/src/lib/format/relativeTime.ts"
      provides: "server-timestamp-driven relative time formatting for item rows"
      contains: "formatRelativeTime"
    - path: "web/src/components/generator/GeneratorDialog.tsx"
      provides: "standalone Tools-menu password generator with a Copy action"
      contains: "onClose"
  key_links:
    - "crates/pv-server/src/routes/vault.rs's create/list/update handlers now surface the vault_items.updated_at column, which web/src/lib/vault/store.ts threads onto VaultItem.updatedAt for web/src/components/vault/ItemRow.tsx's relative-time display"
    - "web/src/components/vault/ItemContextMenu.tsx's Move action calls the SAME web/src/lib/vault/store.ts updateVaultItem(...) DetailPanel's edit flow already uses, inheriting its optimistic-concurrency/409 handling instead of a new direct API call"
    - "web/src/components/shell/Sidebar.tsx's new itemType category filters extend web/src/lib/vault/types.ts's VaultFilter union, consumed by the existing web/src/lib/vault/search.ts filterItems() already wired into ItemList — no new filtering mechanism"
---

<objective>
Close GAP-02-02 (sidebar Categories/Folders/Tags/Tools restructure), GAP-02-03 (item row relative last-updated time), and GAP-02-04 (item row kebab/right-click context menu) from 02-VERIFICATION.md's UAT gap list — three UX changes the user requested after seeing Phase 2's working vault, adapted from a Proton Pass reference to this project's item types and datafa.st aesthetic (not a Proton clone).

Purpose: Make the sidebar's navigation legible and complete (type filters + a working password-generator entry), give every item row a real, server-truthful "last touched" signal, and collapse the row-hover copy/delete affordances into one discoverable action menu reachable by mouse or right-click.
Output: A restructured Sidebar, a redesigned ItemRow trailing column, a new ItemContextMenu component, a new standalone GeneratorDialog, and a small pv-server change exposing `updated_at` on vault item responses.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-password-auth-vault-core/02-CONTEXT.md
@.planning/phases/02-password-auth-vault-core/02-UI-SPEC.md
@.planning/phases/02-password-auth-vault-core/02-VERIFICATION.md
@.planning/phases/02-password-auth-vault-core/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sidebar Categories/Folders/Tags/Tools restructure (GAP-02-02)</name>
  <files>web/src/components/shell/Sidebar.tsx, web/src/components/shell/Sidebar.test.tsx, web/src/lib/vault/types.ts, web/src/lib/vault/search.ts, web/src/lib/vault/search.test.ts, web/src/lib/i18n/dictionary.ts, web/src/components/generator/GeneratorDialog.tsx, web/src/components/generator/GeneratorDialog.test.tsx</files>
  <read_first>
    web/src/components/shell/Sidebar.tsx (current flat Wszystkie/Foldery/Tagi nav plus the existing settings dropdown at the bottom — this task restructures only the `<nav>` block; the account/settings dropdown is untouched)
    web/src/components/shell/Sidebar.test.tsx (existing testids `sidebar-nav-all`/`sidebar-nav-folders`/`sidebar-nav-tags`/`sidebar-folder-{id}`/`sidebar-tag-{tag}`/`sidebar-new-folder-*` and their assertions — every one of these must keep working unchanged)
    web/src/lib/vault/types.ts (VaultFilter union — extend, don't replace, the existing `all`/`folder`/`tag` variants)
    web/src/lib/vault/search.ts (matchesFilter/filterItems — add the new branch here, the only place filter logic lives)
    web/src/components/vault/ItemRow.tsx (TYPE_ICON map — mirror the same per-type icon choices for the new category buttons so a login's icon matches everywhere)
    web/src/lib/generator/password.ts, web/src/lib/generator/strength.ts (pure functions this task's new GeneratorDialog reuses as-is; do not modify them, do not modify web/src/components/generator/GeneratorPopover.tsx which is a different plan's file)
    web/src/components/vault/DeleteConfirmDialog.tsx (the fixed inset-0 centered-modal treatment this task's GeneratorDialog visually follows — AlertTriangle/plain-DM-Sans convention doesn't apply here, but the fixed/centered/backdrop structure does)
  </read_first>
  <behavior>
    - Clicking "All Items" still calls `onFilterChange({kind:"all"})` (unchanged from today).
    - Clicking a category button (Logins/Cards/Identities/Notes) calls `onFilterChange({kind:"itemType", itemType:"<type>"})`, and `filterItems(items, {kind:"itemType", itemType:"login"})` returns only items whose `fields.type === "login"`.
    - The Passkeys category entry is rendered as a genuinely `disabled` button and never invokes `onFilterChange` regardless of click.
    - Opening the Tools section and clicking "Generator haseł" mounts `GeneratorDialog`; clicking its Copy-password action writes the current preview to the clipboard through the existing auto-clearing clipboard helper (not a direct/bypassing write) and then closes the dialog.
    - `GeneratorDialog` regenerates a new preview when the mode toggle, length/word-count slider, or a character-set checkbox changes, exactly mirroring `generateCharacterPassword`/`generatePassphrase`'s existing contract.
  </behavior>
  <action>
    In `web/src/lib/vault/types.ts`: add a fourth `VaultFilter` member `{ kind: "itemType"; itemType: ItemType }` to the existing union (keep `all`/`folder`/`tag` exactly as-is).

    In `web/src/lib/vault/search.ts`: extend `matchesFilter` with an `itemType` branch returning `fields.type === filter.itemType`, checked before the final `tags.includes` fallback. `filterItems`'s `all`-kind short-circuit stays unchanged.

    In `web/src/lib/i18n/dictionary.ts`: add `sidebar.categories`, `sidebar.catLogins`, `sidebar.catCards`, `sidebar.catIdentities`, `sidebar.catNotes`, `sidebar.passkeys`, `sidebar.passkeysSoon`, `sidebar.tools`, and `sidebar.generator` (PL/EN pairs — "Kategorie"/"Categories", "Loginy"/"Logins", "Karty"/"Cards", "Tożsamości"/"Identities", "Notatki"/"Notes", "Passkeys"/"Passkeys", "wkrótce"/"soon", "Narzędzia"/"Tools", "Generator haseł"/"Password generator"). Also add `action.copyPassword` ("Kopiuj hasło"/"Copy password") here since `GeneratorDialog`'s copy button needs it and Task 3's context menu will reuse the same key rather than duplicating it.

    Restructure `web/src/components/shell/Sidebar.tsx`'s `<nav>` block into four sections, each with a clickable header toggling a local boolean (`categoriesExpanded` default `true`, `foldersExpanded`/`tagsExpanded` unchanged default `false`, `toolsExpanded` default `true`) and a `ChevronDown` affordance matching the existing Folders/Tags rotate-on-expand treatment:
    - Categories (header `data-testid="sidebar-section-categories"`, label `t("sidebar.categories")`): "All Items" (keep `data-testid="sidebar-nav-all"`, swap its icon from `Vault` to `LayoutGrid` so it's visually distinct from the Logins category below it), then one button per `ItemType` (`data-testid="sidebar-nav-type-login"` etc., icons `Vault`/`CreditCard`/`IdCard`/`StickyNote` matching `ItemRow.tsx`'s `TYPE_ICON` map, labels the four new `sidebar.cat*` keys, `navItemClass(activeFilter.kind==="itemType" && activeFilter.itemType===type)`, `onClick={() => selectFilter({kind:"itemType", itemType: type})}`), then a disabled Passkeys row (`data-testid="sidebar-nav-passkeys"`, `<button type="button" disabled>`, `KeyRound` icon in the accent/teal color already used by `PasskeyPlaceholderSection`, label `t("sidebar.passkeys")`, trailing `badge badge-sm` showing `t("sidebar.passkeysSoon")`).
    - Folders and Tags: keep the existing implementation byte-for-byte (same testids, same behavior) — only their position moves under the new section grouping; do not rename or restructure their internals.
    - Tools (header `data-testid="sidebar-section-tools"`, label `t("sidebar.tools")`): one row (`data-testid="sidebar-generator-trigger"`, `Wand2` icon, label `t("sidebar.generator")`) that sets a new `showGenerator` boolean state to `true` on click. Render `{showGenerator ? <GeneratorDialog onClose={() => setShowGenerator(false)} /> : null}` once, near the end of Sidebar's returned JSX (sibling to the existing account/settings dropdown block, same nesting depth `DeleteConfirmDialog`-style conditional components use elsewhere in this codebase).

    Create `web/src/components/generator/GeneratorDialog.tsx` (`"use client"`): a standalone, centered `fixed inset-0 z-50 flex items-center justify-center bg-base-300/70` modal (same structural pattern as `DeleteConfirmDialog.tsx`, not `GeneratorPopover.tsx`'s anchored dropdown — a centered modal has no viewport-edge anchoring to get wrong, which is why this task deliberately does not reuse or modify `GeneratorPopover.tsx`). Heading `t("sidebar.generator")`, an `X` close button (`aria-label={t("aria.closePanel")}`) calling the `onClose` prop, backdrop click also calls `onClose` (inner panel `stopPropagation`s). Body: the same mode segmented toggle (character/passphrase), live preview input (`ui-monospace`), length/word-count `range` control, character-set checkboxes (character mode only), and strength meter reused from `GeneratorPopover.tsx`'s existing pattern — call `generateCharacterPassword`/`generatePassphrase` from `@/lib/generator/password` and `scorePasswordMeter` from `@/lib/generator/strength` directly (both are pure, already-tested functions; this task only writes new presentational JSX around them, no new generation logic). Primary action button (`data-testid="generator-dialog-copy"`, label `t("action.copyPassword")`) reads the clipboard-clear duration via `readClipboardSeconds()` from `@/lib/clipboard`, calls `copyWithAutoClear(preview, seconds * 1000)`, calls `showCopyToast(t("field.password"), seconds * 1000)` from `@/lib/vault/copyToast`, then calls `onClose()`.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes, including new/updated tests: every pre-existing Sidebar test in `Sidebar.test.tsx` still passes unmodified; a new test confirms clicking `sidebar-nav-type-login` calls `onFilterChange` with `{kind:"itemType", itemType:"login"}`; a new test confirms `sidebar-nav-passkeys` is a `disabled` element and clicking it never calls `onFilterChange`; a new `search.test.ts` test confirms `filterItems` with an `itemType` filter returns only matching-type items; a new `GeneratorDialog.test.tsx` confirms clicking its copy action calls the clipboard helper with the current preview and then calls `onClose`.
    - `grep -c "itemType" web/src/lib/vault/types.ts` is at least 1.
    - `grep -c "sidebar-nav-passkeys" web/src/components/shell/Sidebar.tsx` is at least 1.
    - `grep -c "GeneratorPopover" web/src/components/generator/GeneratorDialog.tsx` is 0 (this new dialog is self-contained and does not import the other plan's popover component, keeping the two gap-closure plans file-disjoint).
  </acceptance_criteria>
  <done>The sidebar shows Categories/Folders/Tags/Tools sections; selecting a category filters the item list by type; Passkeys is visibly present but inert; Tools opens a working, self-contained password generator that copies to the clipboard through the existing auto-clear guarantee.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server-truthful relative "last updated" time on item rows (GAP-02-03)</name>
  <files>crates/pv-server/src/routes/vault.rs, crates/pv-server/tests/vault.rs, web/src/lib/vault/api.ts, web/src/lib/vault/store.ts, web/src/lib/vault/store.test.ts, web/src/lib/vault/types.ts, web/src/lib/format/relativeTime.ts, web/src/lib/format/relativeTime.test.ts, web/src/lib/i18n/dictionary.ts, web/src/components/vault/ItemRow.tsx, web/src/components/vault/ItemRow.test.tsx</files>
  <read_first>
    crates/pv-server/src/routes/vault.rs (create/list/update handlers — `vault_items.updated_at` already exists in the schema and is already written by `update()`'s `datetime('now')`, but no handler currently SELECTs or RETURNINGs it to the client)
    crates/pv-server/tests/vault.rs (existing `req`/`body_json`/`item_body` test helpers and the field-presence assertion style already used — e.g. `assert_eq!(body["revision"], 1)` — new tests follow the same style)
    web/src/lib/vault/api.ts, web/src/lib/vault/store.ts (ItemRow/VaultItem wire-to-domain mapping, `decryptItemRow`, `createVaultItem`, `updateVaultItem` — this task threads one new field through this existing pipeline, it does not restructure it)
    web/src/lib/vault/store.test.ts (mocked `ItemRow`/`updateItem`/`createItem` fixtures and the exact `toEqual` assertion on `updateVaultItem`'s return value that this task's new field will change)
    web/src/lib/i18n/dictionary.ts (flat-dictionary + `interpolate()` pattern this task's new `time.*` keys follow)
    web/src/components/vault/ItemRow.tsx (current trailing-edge badge + health-dot cluster this task replaces with the relative-time text)
  </read_first>
  <behavior>
    - `formatRelativeTime(undefined, t, locale)` returns `null`.
    - `formatRelativeTime(iso, t, locale, now)` where `now - iso` is under 60 seconds returns `t("time.justNow")`.
    - A difference in `[1 minute, 60 minutes)` returns the `time.minutesAgo` template with the correct whole-minute count substituted.
    - A difference in `[1 hour, 24 hours)` returns the `time.hoursAgo` template with the correct whole-hour count.
    - A difference in `[1 day, 30 days)` returns the `time.daysAgo` template with the correct whole-day count.
    - A difference of 30 days or more returns a short locale-formatted date string, not one of the templated relative strings above.
    - A malformed/unparseable timestamp string returns `null` rather than throwing or rendering "Invalid Date".
    - `POST /api/vault/items`'s response body includes a non-empty `updated_at` string; every row from `GET /api/vault/items` includes a non-empty `updated_at`; `PUT /api/vault/items/{id}`'s response includes a non-empty `updated_at` reflecting the just-completed update.
    - `ItemRow` renders the formatted relative time in its trailing column when `item.updatedAt` is set, and renders nothing there (no crash, no placeholder dash) when it is `undefined`.
  </behavior>
  <action>
    In `crates/pv-server/src/routes/vault.rs`: add `pub updated_at: String` to the `CreateItemResponse`, `VaultItem` (the list-response struct), and `UpdateItemResponse` structs. In `create()`, change the `INSERT ... ON CONFLICT(id) DO NOTHING` query to append `RETURNING updated_at` and switch from `.execute()` to `.fetch_optional()`; when the returned `Option<SqliteRow>` is `None` the conflict path is unchanged (still `ApiError::Conflict`), when `Some(row)` read `updated_at` via `row.try_get::<String, _>("updated_at")` and include it in the `201` response. In `list()`, add `updated_at` to the `SELECT` column list and to the constructed `VaultItem` struct. In `update()`, append `RETURNING updated_at` to the `UPDATE ... WHERE id = ? AND user_id = ? AND revision = ?` statement and switch from `.execute()` to `.fetch_optional()`; when `None`, the existing disambiguating follow-up `SELECT` (404 vs 409) is unchanged; when `Some(row)`, read `updated_at` and include it in the `200` response alongside `revision`. Every existing `WHERE user_id = ?` ownership scoping stays exactly as-is — this task only changes which columns come back on success, never which rows are reachable.

    In `crates/pv-server/tests/vault.rs`: add a test asserting a freshly-created item's response and its corresponding row in a subsequent `GET /api/vault/items` both carry a non-empty `updated_at` string, and a second test asserting a `PUT` update's response also carries a non-empty `updated_at`.

    In `web/src/lib/vault/api.ts`: add `updated_at: string` to the `ItemRow` interface, and widen `createItem`'s and `updateItem`'s return-type promises to include `updated_at: string` alongside their existing fields.

    In `web/src/lib/vault/types.ts`: add an optional `updatedAt?: string` field to `VaultItem` (optional, not required — several existing hand-built `VaultItem` test fixtures across the codebase outside this plan's file list construct this type without it, and this task does not touch those files).

    In `web/src/lib/vault/store.ts`: `decryptItemRow` sets `updatedAt: row.updated_at` on the returned `VaultItem`. `createVaultItem` captures `createItem(...)`'s resolved value (currently discarded) and sets `updatedAt` from its `updated_at` field on the constructed item. `updateVaultItem` captures `updateItem(...)`'s resolved value (currently discarded) and sets `updatedAt` from its `updated_at` field on the constructed item (the existing `revision: currentRevision + 1` computation is unchanged — only `updatedAt` is newly threaded through).

    In `web/src/lib/vault/store.test.ts`: update the mocked `mockUpdateItem`/`mockCreateItem` resolved values to include an `updated_at` string, and update the one existing assertion that does a full-object `toEqual` on `updateVaultItem`'s return value to also expect the new `updatedAt` field.

    Create `web/src/lib/format/relativeTime.ts` exporting `formatRelativeTime(updatedAt: string | undefined, t: (key: keyof typeof DICTIONARY) => string, locale: Locale, now: Date = new Date()): string | null`. SQLite's `datetime('now')` produces a space-separated `"YYYY-MM-DD HH:MM:SS"` string with no timezone designator (it is always UTC) — before parsing, normalize any such string by replacing the space with `T` and appending `Z` (an already-`T`-containing string is passed through as-is), then construct a `Date`; if parsing fails (`Number.isNaN` on `.getTime()`) or the input is `undefined`, return `null`. Bucket the `now - then` difference into under-a-minute (`time.justNow`), under-an-hour (`time.minutesAgo`, interpolated `{n}` = whole minutes), under-a-day (`time.hoursAgo`, whole hours), under-30-days (`time.daysAgo`, whole days), else format `then` via `Intl.DateTimeFormat(locale === "pl" ? "pl-PL" : "en-US", { day: "numeric", month: "short" })`. Use `interpolate` from `@/lib/i18n/dictionary` for the templated buckets.

    In `web/src/lib/i18n/dictionary.ts`: add `time.justNow`, `time.minutesAgo`, `time.hoursAgo`, `time.daysAgo` (PL/EN: "Przed chwilą"/"Just now", "{n} min temu"/"{n}m ago", "{n} godz. temu"/"{n}h ago", "{n} dni temu"/"{n}d ago").

    In `web/src/components/vault/ItemRow.tsx`: remove the trailing `badge` (type icon + label) and the static health-dot placeholder span — both are superseded by the relative-time column this task adds and the kebab menu Task 3 adds (the type is already communicated by the leading icon tile; the health-dot was an inert "coming soon" placeholder). In their place render `formatRelativeTime(item.updatedAt, t, locale)` inside a small `text-xs text-base-content/50 shrink-0 whitespace-nowrap` span, only when the result is non-null.
  </action>
  <verify>
    <automated>cargo test -p pv-server && cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pv-server` passes, including the two new `updated_at`-presence tests, and every pre-existing `tests/vault.rs` test (ownership scoping, 409/404 disambiguation, blob-unchanged-on-conflict) continues to pass unmodified.
    - `cd web && npm test` passes, including `relativeTime.test.ts`'s bucket-boundary tests (just-now/minutes/hours/days/date-fallback/undefined/malformed) and `ItemRow.test.tsx`'s new relative-time-rendering assertion.
    - `grep -c "updated_at" crates/pv-server/src/routes/vault.rs` is at least 4 (the three response structs plus the `list()` SELECT).
    - `grep -c "updatedAt" web/src/lib/vault/types.ts` is at least 1.
  </acceptance_criteria>
  <done>Every item row shows a real, server-derived "last updated" relative time; the old static type-badge/health-dot trailing cluster is gone; the backend now returns updated_at on every vault-item response without loosening any existing ownership/concurrency check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Item row context menu — kebab + right-click (GAP-02-04)</name>
  <files>web/src/components/vault/ItemContextMenu.tsx, web/src/components/vault/ItemContextMenu.test.tsx, web/src/components/vault/ItemRow.tsx, web/src/components/vault/ItemRow.test.tsx, web/src/lib/i18n/dictionary.ts</files>
  <read_first>
    web/src/components/vault/ItemRow.tsx (Task 2's output — the row this task adds a kebab trigger and right-click handler to)
    web/src/components/vault/DetailPanel.tsx (`handleCopy`/`renderCopyButton` and the `FIELD_ORDER`/`MONO_FIELDS` per-type field convention this task's copy actions mirror, and the exact `updateVaultItem`/`RevisionConflictError` import path this task's Move action reuses)
    web/src/lib/clipboard.ts (`copyWithAutoClear`, `readClipboardSeconds` — the only sanctioned clipboard-write path)
    web/src/lib/vault/copyToast.ts (`showCopyToast`)
    web/src/components/vault/DeleteConfirmDialog.tsx (unmodified — this task imports and reuses it exactly as `DetailPanel.tsx` already does, from a second call site)
    web/src/lib/vault/store.ts (`useFolders`, `updateVaultItem` — Task 2 already extended `updateVaultItem`'s return shape, no further change needed here)
    web/node_modules/daisyui — DaisyUI 5's `dropdown-open` class forces `.dropdown-content` visible regardless of CSS focus-within state (verified against the installed `daisyui@5.6.18` package's compiled CSS) — this task relies on that class rather than the focus-driven default, because a right-click trigger does not reliably move DOM focus the way a click does
  </read_first>
  <behavior>
    - Clicking the kebab button opens `ItemContextMenu` and does not call the row's `onClick`/selection callback.
    - Right-clicking (a `contextmenu` DOM event) anywhere on the row opens the same menu, prevents the native browser context menu, and also does not call the row's `onClick`.
    - For a login item with both `username` and `password` set, the menu offers Copy username and Copy password; for a card item with `number` set, Copy card number; for an identity item with `email` set, Copy email; for a note item, no copy actions are offered at all.
    - Every copy action writes through the existing auto-clearing clipboard helper — never a raw/direct clipboard write that bypasses the auto-clear guarantee.
    - Choosing a folder under Move calls `updateVaultItem(item.id, {...item.fields, folderId}, item.revision)` and then closes the menu; choosing "no folder" passes `folderId: null`.
    - Clicking Edit calls the same callback the row's own click already uses (opening the item, not a separate edit-mode entry point).
    - Clicking Delete closes the context menu and opens the existing `DeleteConfirmDialog` — it never deletes without that confirmation step.
  </behavior>
  <action>
    In `web/src/lib/i18n/dictionary.ts`: add `aria.itemMenu` (PL `` Więcej opcji dla „{name}" ``, EN `` More options for "{name}" ``, interpolated the same way `aria.deleteItem` already is) and `action.copyUsername`, `action.copyCardNumber`, `action.copyEmail`, `action.move`, `action.delete` (PL/EN: "Kopiuj nazwę użytkownika"/"Copy username", "Kopiuj numer karty"/"Copy card number", "Kopiuj email"/"Copy email", "Przenieś"/"Move", "Usuń"/"Delete"). `action.copyPassword` already exists from Task 1.

    Create `web/src/components/vault/ItemContextMenu.tsx` (`"use client"`): props `{ item: VaultItem; onClose: () => void; onEdit: () => void; onDeleteRequest: () => void }`. Renders a `<ul className="dropdown-content menu z-20 mt-2 w-56 rounded-box border border-base-300 bg-base-100 p-2 shadow">` (no outer `.dropdown` wrapper — `ItemRow.tsx` owns that, per below). Calls `useFolders()` from `@/lib/vault/store` for the Move submenu. Copy actions are built from `item.fields.type`: for `login`, a Copy-username item (only when `fields.username` is non-empty) and a Copy-password item (only when `fields.password` is non-empty); for `card`, a Copy-card-number item (only when `fields.number` is non-empty); for `identity`, a Copy-email item (only when `fields.email` is non-empty); for `note`, none. Each copy action's handler reads `readClipboardSeconds()` from `@/lib/clipboard`, calls `copyWithAutoClear(value, seconds * 1000)`, calls `showCopyToast(<field label>, seconds * 1000)` from `@/lib/vault/copyToast`, then calls `onClose()`. Below the copy actions, a `<li><details><summary>{t("action.move")}</summary><ul>...</ul></details></li>` nested-menu block (DaisyUI's native submenu pattern) listing `t("item.noFolder")` plus every entry from `useFolders()`; clicking one calls `updateVaultItem(item.id, { ...item.fields, folderId }, item.revision)` from `@/lib/vault/store` (fire-and-forget is acceptable here — `updateVaultItem`'s own `RevisionConflictError` handling already re-syncs the store on conflict) then `onClose()`. Below that, an Edit item calling `onEdit()` then `onClose()`, and a Delete item (styled `text-error`, matching `DeleteConfirmDialog`'s destructive-action color) calling `onDeleteRequest()`.

    In `web/src/components/vault/ItemRow.tsx`: change the root element from a native `<button>` to a `<div role="button" tabIndex={0} onClick={onClick} onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); onClick(); } }}>` — a native `<button>` cannot legally contain another interactive `<button>` (the kebab trigger this task adds), and browsers silently mangle that nesting, so this is a required structural fix, not a stylistic one; keep every existing className/data-testid on this root element exactly as before. Add a `group` class to the root's existing className string (needed for the kebab's hover-reveal below) and an `onContextMenu={(e) => { e.preventDefault(); e.stopPropagation(); setMenuOpen(true); }}` handler. Add local state `menuOpen`/`showDeleteDialog` (both default `false`). After the relative-time span (Task 2), add a `<div className={\`dropdown dropdown-end \${menuOpen ? "dropdown-open" : ""}\`}>` wrapping: a kebab trigger button (`data-testid={\`item-menu-trigger-\${item.id}\`}`, `aria-label={interpolate(t("aria.itemMenu"), {name: item.fields.name})}`, `MoreVertical` icon, classes `btn btn-ghost btn-square btn-sm shrink-0 opacity-0 transition-opacity group-hover:opacity-100 focus:opacity-100`, `onClick={(e) => { e.stopPropagation(); setMenuOpen((v) => !v); }}`), and, only while `menuOpen`, `<ItemContextMenu item={item} onClose={() => setMenuOpen(false)} onEdit={onClick} onDeleteRequest={() => { setMenuOpen(false); setShowDeleteDialog(true); }} />`. Add a `useEffect` that, only while `menuOpen` is true, listens for document-level `pointerdown` outside the menu's wrapper `ref` and closes the menu (cleaned up on unmount/toggle-off) — needed because this menu is explicitly state-driven (via `dropdown-open`) rather than relying on the CSS focus-within collapse the rest of the app's dropdowns use. After the context menu block, conditionally render `{showDeleteDialog ? <DeleteConfirmDialog item={item} onClose={() => setShowDeleteDialog(false)} onDeleted={() => setShowDeleteDialog(false)} /> : null}`.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes, including: kebab click opens the menu and does not fire the row's onClick; `fireEvent.contextMenu(...)` on the row opens the same menu and does not fire onClick; a login item's menu contains both copy actions and a card item's contains only Copy card number; clicking Delete opens `delete-confirm-dialog` (not an immediate delete); clicking a folder under Move calls the mocked `updateVaultItem` with the expected `folderId`.
    - `grep -c "copyWithAutoClear" web/src/components/vault/ItemContextMenu.tsx` is at least 1.
    - `grep -c 'navigator\.clipboard\.writeText' web/src/components/vault/ItemContextMenu.tsx` is 0 (every copy action goes through the shared auto-clearing helper, never a direct/bypassing clipboard write).
    - `grep -c "DeleteConfirmDialog" web/src/components/vault/ItemRow.tsx` is at least 1 (Delete reuses the existing confirmation dialog, no new/parallel delete path).
  </acceptance_criteria>
  <done>Every item row exposes a discoverable kebab button (hover/focus) and a right-click handler that open the same action menu, offering type-appropriate copy actions, folder move, edit, and confirmed delete — all routed through the existing safe primitives.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- **Files:** `web/src/components/vault/ItemContextMenu.tsx` (new), `web/src/components/generator/GeneratorDialog.tsx` (new), `web/src/lib/format/relativeTime.ts` (new), `crates/pv-server/src/routes/vault.rs` (edited — `updated_at` on create/list/update responses), `web/src/lib/vault/{api,store,types,search}.ts` (edited — `updatedAt` threading, `itemType` filter), `web/src/components/shell/Sidebar.tsx` (edited — Categories/Folders/Tags/Tools structure), `web/src/components/vault/ItemRow.tsx` (edited — relative time, kebab trigger, right-click), `web/src/lib/i18n/dictionary.ts` (edited — sidebar/time/action/aria keys)
- **TS exports:** `formatRelativeTime`, `ItemContextMenu` (default), `GeneratorDialog` (default) — new symbols this task introduces; later drift verification should not flag them as undocumented
- **Rust:** `CreateItemResponse`/`VaultItem`/`UpdateItemResponse` in `crates/pv-server/src/routes/vault.rs` gain an `updated_at: String` field each

These are all newly-created or newly-extended symbols this plan introduces.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Item-row copy actions (new) | A second, independent call site into the same clipboard-auto-clear guarantee Plan 02-06 established — must not silently regress to a raw write |
| Move-to-folder (new) | Re-encrypts and PUTs an item's ciphertext outside the DetailPanel edit flow — must not skip the AD-binding/optimistic-concurrency path that flow already enforces |
| `vault_items.updated_at` exposure | A previously-unreturned server column now included in every authenticated item response |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-25 | Information Disclosure | `ItemContextMenu.tsx` copy actions | medium | mitigate | Every copy action calls the existing `copyWithAutoClear`/`readClipboardSeconds` helpers from `lib/clipboard.ts`, never a direct `navigator.clipboard.writeText` — enforced by a negative grep in Task 3's acceptance criteria and covered by a unit test |
| T-02-26 | Tampering | `ItemContextMenu.tsx` Move action | high | mitigate | Move calls the same `updateVaultItem(id, fields, revision)` DetailPanel's edit flow uses, inheriting its AD-binding re-encryption and 409-on-stale-revision handling instead of a new hand-rolled PUT call |
| T-02-27 | Tampering / Elevation of Privilege | `crates/pv-server/src/routes/vault.rs` create/update `RETURNING` change | high | mitigate | The `WHERE user_id = ?` (and, for update, `AND revision = ?`) ownership/concurrency predicates are unchanged by switching `.execute()` to `.fetch_optional()` — only the returned columns differ; the full pre-existing cross-user 404 and stale-revision 409 test suite in `tests/vault.rs` continues to run unmodified alongside the two new tests |
| T-02-28 | Information Disclosure | `updated_at` now returned on every item | low | accept | `updated_at` is scoped to the session-authenticated owner exactly like every other item field already returned (`id`/`enc_key`/`enc_data`/`revision`) — no new recipient gains access, and the value itself (a modification timestamp) is not sensitive vault content |

</threat_model>

<verification>
`cargo test -p pv-server && cd web && npm test` must exit 0 after all three tasks. No human-check items are required — every behavior this plan changes (filter selection, relative-time formatting, menu open/close/action wiring) is deterministically assertable in the existing vitest/cargo-test harnesses already covering this phase.
</verification>

<success_criteria>
- Sidebar Categories/Folders/Tags/Tools sections exist, are collapsible, and category selection filters the list client-side.
- Item rows show a real server-derived relative last-updated time in place of the retired static badge/health-dot cluster.
- Every item row has a working kebab-button and right-click action menu offering type-appropriate copy, move, edit, and confirmed delete, all reusing Phase 2's existing safe primitives.
</success_criteria>

<output>
Create `.planning/phases/02-password-auth-vault-core/02-07-SUMMARY.md` when done.
</output>
