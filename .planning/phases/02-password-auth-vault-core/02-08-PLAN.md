---
phase: 02-password-auth-vault-core
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/vault/DetailPanel.tsx
  - web/src/components/vault/DetailPanel.test.tsx
  - web/src/components/generator/GeneratorPopover.tsx
  - web/src/components/generator/GeneratorPopover.test.tsx
autonomous: true
requirements: [UI-03]
gap_closure: true

must_haves:
  truths:
    - "Login passwords and card numbers render masked by default in the detail panel's view mode, with a reveal (eye) toggle next to the existing copy button that shows/hides the real value on demand; switching to a different item resets any revealed field back to masked"
    - "CVV stays masked with no reveal toggle in the detail panel, matching the no-reveal convention ItemForm's create/edit CVV field already established"
    - "The password-generator popover, opened from the login item form, stays fully within the viewport regardless of where its trigger sits on the page"
  artifacts:
    - path: "web/src/components/vault/DetailPanel.tsx"
      provides: "masked-by-default secret fields with a per-field reveal toggle"
      contains: "aria.showPassword"
    - path: "web/src/components/generator/GeneratorPopover.tsx"
      provides: "viewport-safe popover anchor positioning"
      contains: "dropdown-end"
  key_links:
    - "web/src/components/vault/DetailPanel.tsx reuses the aria.showPassword/aria.hidePassword dictionary keys ItemForm.tsx's existing reveal toggle already established, rather than introducing new copy"
---

<objective>
Close GAP-02-01 (password/card-number fields render in plaintext in the detail panel, with no reveal control) and GAP-02-05 (the password-generator popover overflows the viewport when opened from the new-login form) from 02-VERIFICATION.md's UAT gap list.

Purpose: Bring DetailPanel's view mode in line with the masked-by-default convention ItemForm's create/edit form already uses for the same fields, and fix a real layout bug where the generator popover renders partly off-screen.
Output: DetailPanel shows masked secrets with an explicit reveal toggle; GeneratorPopover's anchor positioning keeps its content inside the viewport.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-password-auth-vault-core/02-CONTEXT.md
@.planning/phases/02-password-auth-vault-core/02-UI-SPEC.md
@.planning/phases/02-password-auth-vault-core/02-VERIFICATION.md
@.planning/phases/02-password-auth-vault-core/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Password/card-number reveal toggle in the detail panel (GAP-02-01)</name>
  <files>web/src/components/vault/DetailPanel.tsx, web/src/components/vault/DetailPanel.test.tsx</files>
  <read_first>
    web/src/components/vault/DetailPanel.tsx (current view-mode field loop — `FIELD_ORDER`/`MONO_FIELDS` per-type field lists, `renderCopyButton`, the raw `fieldValues[key] || "—"` display this task changes to a masked display)
    web/src/components/vault/ItemForm.tsx (lines ~275-410 — the already-shipped `Eye`/`EyeOff` reveal toggle for the login password field and the card number field, and the explicit "CVV is masked-only, no reveal toggle" comment/pattern this task mirrors exactly rather than inventing a new convention)
    web/src/lib/i18n/dictionary.ts (`aria.showPassword`/`aria.hidePassword` — already defined by Plan 02-06 for ItemForm; this task reuses them verbatim, it does not add new dictionary keys)
    web/src/components/vault/DetailPanel.test.tsx (existing `item` fixture is a `note`-type item with no password/number/cvv field — new tests need their own login/card fixtures)
  </read_first>
  <behavior>
    - A login item's password field renders as a fixed-length mask, not the real value and not a value whose visible length reveals the real password's length.
    - Clicking the password field's reveal toggle shows the real value and flips the icon from `Eye` to `EyeOff`; clicking it again re-masks the value and flips the icon back.
    - A card item's `number` field behaves the same way (masked by default, independently toggleable from the password field).
    - A card item's `cvv` field always renders masked, with no reveal toggle rendered for it at all.
    - Re-rendering `DetailPanel` with a different `item` prop (same mounted component instance, matching how `page.tsx` actually uses it — no `key` prop forces a remount) resets every previously-revealed field back to masked.
    - Fields outside the reveal set (username, URL, notes, cardholder name, expiry, identity/note fields) are unaffected — displayed exactly as before.
  </behavior>
  <action>
    In `web/src/components/vault/DetailPanel.tsx`: import `Eye`, `EyeOff` from `lucide-react` alongside the existing icon imports. Add two module-level constants: `REVEALABLE_FIELDS = new Set(["password", "number"])` (gets a toggle) and a `MASK` placeholder string of ten bullet characters (`"•".repeat(10)`) — a fixed length so the mask never leaks the real value's character count; `cvv` stays masked via the existing `MONO_FIELDS` set but deliberately gets no entry in `REVEALABLE_FIELDS`, matching `ItemForm.tsx`'s explicit no-reveal-for-CVV convention.

    Add `const [revealedKeys, setRevealedKeys] = useState<Set<string>>(new Set())` and a `useEffect(() => setRevealedKeys(new Set()), [item.id])` so switching items always resets reveal state (the component is not remounted between item selections — `page.tsx` renders `<DetailPanel item={selectedItem} .../>` with no `key`). Add `isRevealed(key)` / `toggleReveal(key)` helpers (immutable `Set` copy-and-mutate, matching the existing `copiedKey` state's update style already in this file).

    Change the view-mode field loop's value display: introduce a small `displayValueFor(key, value)` helper — empty value renders `"—"` (unchanged); `MONO_FIELDS.has(key) && !REVEALABLE_FIELDS.has(key)` (i.e. `cvv`) always renders `MASK`; `REVEALABLE_FIELDS.has(key) && !isRevealed(key)` renders `MASK`; otherwise renders the real `value`. Immediately before the existing `renderCopyButton(key, fieldValues[key])` call (so the new toggle sits directly next to the copy button, per the gap's explicit "NEXT TO the existing copy button" placement), add a same-sized `btn btn-ghost btn-square btn-sm shrink-0` reveal-toggle button — rendered only when `fieldValues[key]` is non-empty AND `REVEALABLE_FIELDS.has(key)` — with `data-testid={\`reveal-${key}\`}`, `aria-label={isRevealed(key) ? t("aria.hidePassword") : t("aria.showPassword")}`, showing `EyeOff` when revealed and `Eye` when masked, `onClick={() => toggleReveal(key)}`.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes, including new tests: a login item's password renders masked by default and reveals the real value after clicking its reveal toggle; a card item's `number` field behaves the same; a card item's `cvv` field renders masked with no reveal-toggle element present for it; re-rendering `DetailPanel` with a different item resets a previously-revealed field back to masked.
    - `grep -c "EyeOff" web/src/components/vault/DetailPanel.tsx` is at least 1.
    - `grep -c 'reveal-cvv' web/src/components/vault/DetailPanel.tsx` is 0 (no reveal control is ever rendered for the CVV field, matching the established no-reveal-for-CVV convention).
  </acceptance_criteria>
  <done>Password and card-number fields in the detail panel are masked by default with a working reveal toggle beside the copy button; CVV stays masked with no toggle; switching items always re-masks.</done>
</task>

<task type="auto">
  <name>Task 2: Password-generator popover stays within the viewport (GAP-02-05)</name>
  <files>web/src/components/generator/GeneratorPopover.tsx, web/src/components/generator/GeneratorPopover.test.tsx</files>
  <read_first>
    web/src/components/generator/GeneratorPopover.tsx (the `dropdown`/`dropdown-content` structure this task adjusts — currently no alignment modifier, meaning content grows rightward from the trigger's left edge by DaisyUI's default)
    web/src/components/vault/ItemForm.tsx (the login password field row — `input` (`flex-1`) then the reveal-toggle button then this popover's trigger, all inside the fixed `right-0`-docked 400px detail/create panel from `page.tsx`; the trigger sits near the panel's right edge, which is why unmodified rightward growth pushes the popover's content off the right edge of the viewport)
    web/node_modules/daisyui/components/dropdown.css (confirms `dropdown-end` sets `--anchor-h:span-left`, anchoring the dropdown-content's right edge to the trigger instead of its left edge, so content grows leftward and stays inside a right-docked container — this is the correct DaisyUI 5 fix, not a custom positioning hack)
  </read_first>
  <action>
    In `web/src/components/generator/GeneratorPopover.tsx`: change the outer `<div className="dropdown">` to also include the `dropdown-end` modifier class, so the popover's content anchors to the trigger's right edge and grows leftward instead of rightward — this keeps it inside the right-docked panel it is opened from instead of overflowing past the viewport's right edge. Also replace the popover content's previous fixed-pixel Tailwind width with a viewport-clamped width using Tailwind's arbitrary-value `min()`/`calc()` syntax (`w-[min(320px,calc(100vw-2rem))]`), so on a very narrow viewport the popover can never exceed the available width even before considering its anchor side — this is a defensive addition beyond the specific reported bug, not a replacement for the `dropdown-end` fix. Every other class and all existing behavior (mode toggle, length/word-count controls, regenerate, apply) stay exactly as-is.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes, including every pre-existing `GeneratorPopover.test.tsx` test unmodified, plus a new test asserting the popover's container carries the viewport-safe alignment class once opened.
    - `grep -c "dropdown-end" web/src/components/generator/GeneratorPopover.tsx` is at least 1.
    - `grep -c 'w-\[320px\]' web/src/components/generator/GeneratorPopover.tsx` is 0 (the old fixed-pixel width is gone, replaced by the viewport-clamped one).
  </acceptance_criteria>
  <done>The generator popover, opened from the login password field inside the right-docked item form, renders fully within the viewport instead of overflowing past its right edge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Detail-panel secret fields, rendered without an explicit reveal action | Shoulder-surfing / incidental-observer exposure risk on a shared screen |
| Reveal state carried across a component-instance reuse (no remount between item selections) | A stale "revealed" flag from a previous item could otherwise leak into a freshly-selected different item's render |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-29 | Information Disclosure | `DetailPanel.tsx` view-mode password/card-number fields | medium | mitigate | Fields render as a fixed-length mask by default; the real value is only shown after an explicit per-field reveal click, and the mask length never varies with the real value's length |
| T-02-30 | Information Disclosure | `DetailPanel.tsx` reveal state across item switches | medium | mitigate | `revealedKeys` resets via a `useEffect` keyed on `item.id`, so a field revealed for one item can never render revealed for a different, freshly-selected item in the same mounted panel |

</threat_model>

<verification>
`cd web && npm test` must exit 0 after both tasks. No human-check items are required — masking/reveal state and the popover's alignment class are both deterministically assertable in the existing vitest/RTL harness already covering this phase; the popover's exact pixel-overflow behavior was already confirmed as a real bug during human UAT (source of GAP-02-05), and the automated class-presence check is the practical regression guard going forward.
</verification>

<success_criteria>
- Password and card-number fields in the detail panel are masked by default with a working, per-field reveal toggle beside the copy button; CVV stays masked with no toggle.
- The generator popover never renders outside the viewport, regardless of which form it is opened from.
</success_criteria>

<output>
Create `.planning/phases/02-password-auth-vault-core/02-08-SUMMARY.md` when done.
</output>
