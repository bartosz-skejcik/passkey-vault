---
phase: 08-extension-bootstrap-wasm-in-background-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [extension/wxt.config.ts, extension/package.json, extension/entrypoints/background.ts, scripts/build-wasm.sh, .gitignore]
autonomous: false
requirements: [EXT-01]

must_haves:
  truths:
    - "extension/ is a working WXT project (sibling to web/) that generates a manifest for both a Chrome and a Firefox target"
    - "The generated Chrome manifest explicitly declares 'wasm-unsafe-eval' in its CSP — not left to an implicit default (D-07)"
    - "Firefox's background target (MV2 persistent background page) is a deliberate, documented pin in wxt.config.ts, not an unstated WXT default (D-08)"
    - "scripts/build-wasm.sh remains the single source of the pv-wasm artifact, extended additively (not forked) to also emit an extension/ output pair (D-02/D-12)"
  artifacts:
    - path: "extension/wxt.config.ts"
      provides: "CSP (wasm-unsafe-eval), browser_specific_settings.gecko.id, and the documented Firefox MV2 rationale"
      contains: "wasm-unsafe-eval"
    - path: "extension/package.json"
      provides: "predev/prebuild hooks invoking the shared build-wasm.sh, mirroring web/package.json"
      contains: "build-wasm.sh"
    - path: "scripts/build-wasm.sh"
      provides: "additive second output target: extension/lib/crypto/wasm (JS glue) + extension/public/wasm (binary)"
      contains: "extension/lib/crypto/wasm"
  key_links:
    - from: "extension/package.json"
      to: "scripts/build-wasm.sh"
      via: "npm predev/prebuild script invocation, same convention as web/package.json"
      pattern: "build-wasm\\.sh"
---

<objective>
Scaffold the new `extension/` WXT project (sibling to `web/`), explicitly declare its MV3 CSP and Firefox manifest pin, and extend the existing `scripts/build-wasm.sh` so it produces a second copy of the unchanged `pv-wasm` artifact for `extension/` to consume.

Purpose: This is the foundation every later Phase 8 plan builds on — the manifest/CSP/Firefox-target decisions (D-07/D-08/D-09) and the build-artifact wiring (D-02/D-12) must exist and be verified before any crypto code is written.
Output: A buildable `extension/` WXT project (background entrypoint stub only, no crypto logic yet) and an extended `scripts/build-wasm.sh` that emits the same wasm-bindgen artifact into `extension/lib/crypto/wasm/` + `extension/public/wasm/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-CONTEXT.md
@.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-RESEARCH.md
@.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-PATTERNS.md
@scripts/build-wasm.sh
@web/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: verify wxt@0.20.27 / @wxt-dev/browser@0.2.2 package legitimacy before install</name>
  <action>N/A — this is a mandatory pre-install human verification gate for [SUS]-flagged packages, not an automatable code action. This gate always halts regardless of workflow.human_verify_mode or workflow.auto_advance (package legitimacy checkpoints are never auto-approvable) — see how-to-verify.</action>
  <what-built>Nothing yet — this gate runs BEFORE installing anything. The Phase 8 research's Package Legitimacy Audit flagged both `wxt@0.20.27` and `@wxt-dev/browser@0.2.2` as [SUS] (a "too-new" heuristic false-positive: both packages have a multi-year-old repo at github.com/wxt-dev/wxt and 700K+ weekly downloads, but WXT ships frequent point releases which trips the too-new heuristic). Per the package legitimacy gate protocol, [SUS] packages require a blocking human-verify checkpoint before install even when the research already assessed them as legitimate.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/wxt — confirm the maintainer/org, that the repository link points to github.com/wxt-dev/wxt, and the weekly download count is in the hundreds-of-thousands range.
    2. Visit https://www.npmjs.com/package/@wxt-dev/browser — confirm the same publishing org and a similar high download count.
    3. Optionally re-check current versions with `npm view wxt version` and `npm view @wxt-dev/browser version` (this session's research verified `0.20.27` / `0.2.2` live on 2026-07-14; if a newer patch exists now, that is an acceptable, low-risk bump per RESEARCH.md Assumptions Log A1).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installing wxt@0.20.27 and @wxt-dev/browser@0.2.2, or "reject" with a reason to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Scaffold extension/ and pin the MV3 manifest (CSP, Firefox target, gecko.id)</name>
  <files>extension/wxt.config.ts, extension/package.json, extension/entrypoints/background.ts</files>
  <read_first>.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-RESEARCH.md (Architecture Patterns: Pattern 2, Pattern 3, Anti-Patterns to Avoid), .planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-CONTEXT.md (D-01, D-04, D-07, D-08, D-09)</read_first>
  <action>
    From the repo root, run `npx wxt@0.20.27 init extension`. If the CLI prompts interactively, choose: package manager = npm, template = vanilla (framework-free — no React/Vue/Svelte; Phase 8 ships no UI beyond a later debug popup, per CONTEXT.md discretion). If this WXT version exposes non-interactive init flags (check `npx wxt@0.20.27 init --help`), prefer those over accepting interactive defaults, and never let the CLI default to a framework template.

    After scaffolding, delete any default content-script or popup entrypoints the vanilla template includes — Phase 8 (D-04) only needs `entrypoints/background.ts`; a debug popup is added later in plan 08-03, not here.

    Replace the generated `entrypoints/background.ts` with a minimal stub:
    a `defineBackground({ type: 'module', main() { ... } })` call where `main()` is empty except for a single `console.log` announcing the background context started. `type: 'module'` is required from this first commit onward (not added later) because plan 08-02 will add an `import` statement for the generated `pv_wasm.js` glue, and MV3 service workers only support `import` syntax when the manifest's background field is declared as a module — retrofitting this later would require touching the manifest config again. `main()` must stay synchronous with no top-level `await` (WXT imports this file under Node during the build step; async/browser-only side effects outside `main()` break the build, not just the runtime).

    Edit `extension/wxt.config.ts` to explicitly declare, as a plain object (not a per-browser function — nothing here diverges by browser):
    - `manifest.content_security_policy.extension_pages` set to exactly `"script-src 'self' 'wasm-unsafe-eval'; object-src 'self';"` (D-07 — the literal MV3 CSP string permitting WASM compilation).
    - `manifest.browser_specific_settings.gecko.id` set to a fixed literal string, e.g. `"passkey-vault@extension.local"` (D-09 — any stable value is acceptable; the requirement is that it is a deliberate literal, not left to an ephemeral dev-mode default that changes across sessions. `strict_min_version` is deliberately NOT set here — deferred to Phase 13).
    - A code comment directly above the config object documenting the Firefox MV2 decision (D-08): WXT's own per-browser default already produces Chrome→MV3 service worker, Firefox→MV2 persistent background page, and that split is exactly the deliberate choice this project makes — MV2 sidesteps the idle-kill/wake problem entirely on Firefox for this phase (per PITFALLS.md #8), rather than taking on an MV3 event-page implementation there too. Do NOT set a top-level `manifestVersion` override (that would force both browsers to the same version, defeating the point of proving Chrome's MV3 service-worker survival). State in the comment that this pin is verified in plan 08-03 by inspecting the generated Firefox manifest.json for `background.persistent === true` and a `background.scripts` array (Firefox's own field name — never `background.service_worker`, which is Chrome-only).

    Install `@wxt-dev/browser@0.2.2` as a dependency (`npm install @wxt-dev/browser@0.2.2` from inside `extension/`) — needed by plan 08-02 for a typed cross-browser `browser.*` global.
  </action>
  <verify>
    <automated>cd extension && npx wxt build -b chrome 2>&1 | tail -20 && npx wxt build -b firefox 2>&1 | tail -20 && find .output -name manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - extension/wxt.config.ts contains the exact string "wasm-unsafe-eval"
    - extension/wxt.config.ts contains a gecko.id literal (not a template placeholder)
    - extension/entrypoints/background.ts contains "type: 'module'"
    - Both `wxt build -b chrome` and `wxt build -b firefox` exit 0 and each produces a manifest.json under extension/.output/
  </acceptance_criteria>
  <done>extension/ builds cleanly for both browser targets with the CSP and gecko.id explicitly declared in wxt.config.ts, and a documented, deliberate Firefox MV2 rationale comment is present.</done>
</task>

<task type="auto">
  <name>Task 2: Extend scripts/build-wasm.sh additively for extension/ and wire extension/package.json</name>
  <files>scripts/build-wasm.sh, extension/package.json, .gitignore</files>
  <read_first>scripts/build-wasm.sh, web/package.json, .planning/phases/01-wasm-crypto-bridge-web-app-shell/01-01-SUMMARY.md</read_first>
  <action>
    Extend `scripts/build-wasm.sh` (do not fork or duplicate it — D-02/D-12) by adding new steps AFTER the existing web/ output steps (after "Move the compiled binary into the static-asset directory"), using the exact same `wasm-bindgen --target web` invocation and the same zero-arg-default-neutralizing `sed` step, targeting a second output pair:
    - `mkdir -p extension/lib/crypto/wasm extension/public/wasm`
    - `wasm-bindgen --target web --out-dir extension/lib/crypto/wasm target/wasm32-unknown-unknown/release/pv_wasm.wasm`
    - The same `sed -i.bak` neutralize-zero-arg-default replacement against `extension/lib/crypto/wasm/pv_wasm.js` (update the error message's file reference to `lib/crypto/wasm-loader.ts`, since that is where the extension's explicit-path `init()` call lives, created in plan 08-02) followed by `rm -f extension/lib/crypto/wasm/pv_wasm.js.bak`
    - `mv extension/lib/crypto/wasm/pv_wasm_bg.wasm extension/public/wasm/pv_wasm_bg.wasm`
    - Closing echo lines confirming both output paths, mirroring the existing script's style.

    Add to `extension/package.json`: `"predev": "bash ../scripts/build-wasm.sh"` and `"prebuild": "bash ../scripts/build-wasm.sh"` — identical convention and identical relative path to `web/package.json`'s existing scripts (both `web/` and `extension/` are sibling directories one level below the repo root).

    Append two new lines to the ROOT `.gitignore` (not a nested extension/.gitignore — mirror the existing web/ entries' location and style) directly after the existing "WASM build artifacts" block: `extension/lib/crypto/wasm/` and `extension/public/wasm/`.
  </action>
  <verify>
    <automated>bash scripts/build-wasm.sh && test -f extension/lib/crypto/wasm/pv_wasm.js && test -f extension/public/wasm/pv_wasm_bg.wasm && test ! -f extension/lib/crypto/wasm/pv_wasm_bg.wasm && test -f web/src/lib/crypto/wasm/pv_wasm.js && test -f web/public/wasm/pv_wasm_bg.wasm</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build-wasm.sh contains "extension/lib/crypto/wasm"
    - extension/package.json contains "build-wasm.sh"
    - Root .gitignore contains "extension/lib/crypto/wasm/" and "extension/public/wasm/"
    - Re-running scripts/build-wasm.sh still produces web/'s original output unchanged (idempotent, additive-only)
  </acceptance_criteria>
  <done>A single `bash scripts/build-wasm.sh` invocation produces both web/'s existing output AND extension/lib/crypto/wasm/pv_wasm.js + extension/public/wasm/pv_wasm_bg.wasm, with no separate/forked build script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM-in-MV3-service-worker | The browser platform's CSP enforcement around whatever loads/executes inside the background context — this plan only sets the manifest-level CSP declaration; plan 08-02 is what actually loads WASM through it |
| Build-artifact provenance | scripts/build-wasm.sh is the sole path by which compiled crypto code reaches either web/ or extension/ — an unaudited fork here would create a second, divergent crypto surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-SC | Tampering | npm installs (`wxt@0.20.27`, `@wxt-dev/browser@0.2.2`) | high | mitigate | Blocking human-verify checkpoint (Task 0 above) before either package is installed, cross-checked against npmjs.com — never auto-approvable |
| T-08-01 | Tampering | extension/wxt.config.ts CSP declaration | high | mitigate | D-07's exact CSP string is declared explicitly in wxt.config.ts (not left to a permissive default); plan 08-03 grep-verifies the *generated, packaged* manifest.json (not just `wxt dev` output) contains it unmodified |
| T-08-08 | Tampering | scripts/build-wasm.sh forked/diverged for extension/ | medium | mitigate | This plan extends the existing script additively (same wasm-bindgen invocation, same version pin) rather than creating a second script — verified by the acceptance criteria confirming web/'s original output is unchanged after the extension is added |

</threat_model>

<verification>
Before declaring this plan complete:
- [ ] `cd extension && npx wxt build -b chrome` and `npx wxt build -b firefox` both exit 0
- [ ] Generated Chrome manifest.json contains "wasm-unsafe-eval"
- [ ] `bash scripts/build-wasm.sh` produces both web/'s and extension/'s output in one run, web/'s output unchanged
- [ ] Package legitimacy checkpoint was approved before any install ran
</verification>

<success_criteria>
- All tasks completed
- extension/ builds for both Chrome and Firefox targets
- CSP, gecko.id, and the Firefox MV2 rationale are explicit in wxt.config.ts, not implicit defaults
- scripts/build-wasm.sh remains the single source of the pv-wasm artifact for both web/ and extension/
- No errors or warnings introduced in web/'s existing build
</success_criteria>

<output>
After completion, create `.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-01-SUMMARY.md` documenting the Firefox MV2 rationale prominently (per CONTEXT.md's open question — flag this choice for Bartek to veto if desired), the exact gecko.id value chosen, and the exact generated `.output/` directory names confirmed on this machine's installed WXT version.
</output>
