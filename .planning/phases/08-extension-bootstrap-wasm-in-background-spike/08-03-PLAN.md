---
phase: 08-extension-bootstrap-wasm-in-background-spike
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: [extension/entrypoints/popup/index.html, extension/entrypoints/popup/main.ts, extension/package.json]
autonomous: true
requirements: [EXT-01]

must_haves:
  truths:
    - "The extension loads unpacked/temporary in both Chrome and Firefox with zero console errors on install (SC #1)"
    - "The packaged (not wxt dev) Chrome build's generated manifest declares the CSP with wasm-unsafe-eval, and the background field uses type: module (SC #2, D-10)"
    - "The packaged Firefox build's generated manifest shows a deliberate MV2 persistent background page (background.persistent=true, background.scripts array), not an implicit default (SC #4)"
    - "The round-trip proof, exercised against the real packaged Chrome build, survives a genuine service-worker idle-kill/wake cycle (SC #3, D-10) — confirmed via end-of-phase human verification"
  artifacts:
    - path: "extension/entrypoints/popup/index.html"
      provides: "Minimal debug harness UI — a button and a result area"
    - path: "extension/entrypoints/popup/main.ts"
      provides: "sendMessage-only trigger for the background round-trip spike — no crypto imports"
  key_links:
    - from: "extension/entrypoints/popup/main.ts"
      to: "extension/entrypoints/background.ts"
      via: "browser.runtime.sendMessage({kind:'spike.roundtrip'}) round-trip, rendered into the popup's result area"
      pattern: "spike.roundtrip"
---

<objective>
Add a minimal debug harness (a popup button), build the real packaged/signed extension for both browsers, and automatically verify the generated manifests match the D-07/D-08/D-09 pins. The one genuinely unautomatable step — a real Chrome service-worker idle-kill/wake cycle against the packaged build — is recorded as a `<human-check>` for end-of-phase UAT rather than a mid-flight halt (`workflow.human_verify_mode = end-of-phase`).

Purpose: Everything up to this plan is inert until it is actually loaded and exercised inside real browsers. D-10 explicitly requires this against the packaged build, using real platform termination — not `wxt dev`, not a simulated kill.
Output: A working debug popup, both browsers' packaged builds verified via generated-manifest inspection, and explicit human-check items queued for end-of-phase UAT covering SC #1/#3/#4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-CONTEXT.md
@.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-RESEARCH.md
@extension/entrypoints/background.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minimal debug harness popup — trigger and display the round-trip result</name>
  <files>extension/entrypoints/popup/index.html, extension/entrypoints/popup/main.ts</files>
  <read_first>extension/entrypoints/background.ts</read_first>
  <action>
    Create `extension/entrypoints/popup/index.html` and `extension/entrypoints/popup/main.ts` as a minimal, framework-free popup (plain DOM APIs — no React/Vue, per CONTEXT.md discretion; this is a throwaway spike harness, not the real Phase 9 popup). Include:
    - A button labeled "Run round-trip spike" and a second button labeled "Check again" (same handler — lets a human trigger the message twice, before and after an idle-kill, without needing to reopen the popup from scratch).
    - A `<pre>` element that renders the JSON-stringified result of `browser.runtime.sendMessage({ kind: 'spike.roundtrip' })`, or the caught error's message on rejection.

    This popup must NEVER import `wasm-loader.ts`, `vault-session.ts`, or `pv_wasm.js` directly, and must never call any pv-wasm export itself (D-04) — it only relays a `runtime.sendMessage` call and displays whatever the background context returns. All derive/wrap/unwrap logic stays exclusively in the background (plan 08-02).
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - extension/entrypoints/popup/main.ts contains "sendMessage" and "spike.roundtrip"
    - extension/entrypoints/popup/main.ts does not import from "../background" or "../../lib/crypto/wasm-loader" or "../../lib/crypto/vault-session"
  </acceptance_criteria>
  <done>Opening the popup and clicking "Run round-trip spike" sends the message and renders whatever JSON the background returns, with zero crypto logic inlined in the popup itself.</done>
</task>

<task type="auto">
  <name>Task 2: Build packaged outputs for both browsers, verify generated manifests match D-07/D-08/D-09, queue the real-browser round-trip/idle-kill checks for end-of-phase UAT</name>
  <files>extension/package.json</files>
  <read_first>extension/wxt.config.ts</read_first>
  <action>
    Add `"build:chrome": "wxt build -b chrome"` and `"build:firefox": "wxt build -b firefox"` scripts to `extension/package.json`. Run both from inside `extension/`. Then locate the generated manifests with `find .output -name manifest.json` (confirm the exact directory names on this installed WXT version — do not assume `.output/chrome-mv3`/`.output/firefox-mv2` are correct without checking).

    Verify, by reading the generated Chrome manifest.json: it contains the exact CSP string with `wasm-unsafe-eval` (D-07), and its `background` key shows `"type":"module"` alongside `"service_worker"` (required for the `import` statements in background.ts/wasm-loader.ts/vault-session.ts to work at all — Pattern 2).

    Verify, by reading the generated Firefox manifest.json: its `background` key shows `"persistent":true` and a `"scripts"` array (Firefox's own field name — never `"service_worker"`, which is Chrome-only) — this is the concrete, generated-output proof that the D-08 Firefox MV2 pin from plan 08-01 actually took effect, not merely a comment in source. Also confirm `browser_specific_settings.gecko.id` matches the literal value chosen in plan 08-01 (D-09).

    This is the "packaged/signed build" D-10 requires testing against — explicitly not `wxt dev` output. If either manifest is missing an expected field, fix `extension/wxt.config.ts` (do not proceed until the manifests match D-07/D-08/D-09) and rebuild before continuing.

    The remaining checks (does the extension actually load with zero console errors, does the round-trip survive a real Chrome service-worker idle-kill) require a real browser GUI session that cannot be driven from this environment — record them as `<human-check>` entries in `<verify>` below rather than halting here, per `workflow.human_verify_mode = end-of-phase`. The end-of-phase verifier harvests these into `08-UAT.md` for one consolidated human review pass instead of a mid-flight halt.
  </action>
  <verify>
    <automated>cd extension && npx wxt build -b chrome && npx wxt build -b firefox && CHROME_MANIFEST=$(find .output -path "*chrome*" -name manifest.json | head -1) && FIREFOX_MANIFEST=$(find .output -path "*firefox*" -name manifest.json | head -1) && grep -c "wasm-unsafe-eval" "$CHROME_MANIFEST" && grep -c "persistent" "$FIREFOX_MANIFEST"</automated>
    <human-check>
      <test>Open chrome://extensions, enable Developer mode, "Load unpacked", select the Chrome output directory confirmed above. Open the popup and click "Run round-trip spike"; then, in the background service worker's DevTools (or chrome://serviceworker-internals), use the real "stop"/terminate control to force a genuine platform-level idle-kill — not a page reload, not disabling/re-enabling the extension. Re-open the popup and click "Check again".</test>
      <expected>Zero console errors on the extension's card or in the background console on load (SC #1). First click: {ok: true, survived: false}. Second click (post real termination): {ok: true, survived: true} — proving the chrome.storage.session envelope survived the real termination and unwrapped correctly on the fresh service-worker instance (SC #3, D-10).</expected>
      <why_human>D-10 explicitly forbids a simulated/mocked termination in a test harness — this requires the browser's actual platform-level service-worker termination mechanism, which has no CLI/headless equivalent available in this environment.</why_human>
    </human-check>
    <human-check>
      <test>Open about:debugging#/runtime/this-firefox, "Load Temporary Add-on", select the Firefox output directory's manifest.json confirmed above. Open the background page's persistent "Inspect" console (Firefox shows this for MV2 background pages — there is no service-worker entry, which is the expected difference from Chrome) and click the popup's "Run round-trip spike" button once.</test>
      <expected>Zero console errors on load (SC #1). {ok: true} with no console errors on a single click — Firefox is pinned to MV2 (D-08) specifically so it never needs a second post-idle-kill click; that guarantee is what the MV2 pin provides (SC #4).</expected>
      <why_human>Firefox's about:debugging temporary-add-on flow and its background-page console have no CLI-drivable equivalent in this environment.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Generated Chrome manifest.json contains "wasm-unsafe-eval" and "\"type\":\"module\""
    - Generated Firefox manifest.json contains "\"persistent\":true" and a "scripts" background field, and does not use "service_worker" for its background field
    - Both manifests contain the same, deliberate gecko.id literal chosen in plan 08-01
  </acceptance_criteria>
  <done>Both packaged builds exist under extension/.output/, their generated manifests are confirmed (by direct inspection) to match D-07/D-08/D-09, and the two human-check items above are queued for end-of-phase UAT covering SC #1/#3/#4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Debug harness (popup) <-> background | Message-passing boundary between the untrusted-by-convention popup UI and the crypto-executing background context |
| WASM-in-MV3-service-worker | This is the plan where the boundary is finally exercised against real, packaged, signed browser builds rather than in-process tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Elevation of Privilege | extension/entrypoints/popup/main.ts | high | mitigate | D-04 — popup only calls `runtime.sendMessage`; acceptance criteria in Task 1 explicitly grep-check it never imports the crypto modules directly |
| T-08-06 | Spoofing / Repudiation | extension/entrypoints/background.ts onMessage listener | low | accept | No sender-origin validation exists on the `spike.roundtrip` message handler — acceptable this phase because the only possible sender is this extension's own popup (no content script or page-injected script exists yet per D-04/D-11); revisit when Phases 10-12 introduce content-script- or page-originated messages that reach this listener's descendants |
| T-08-01 | Tampering | Generated Chrome/Firefox manifest.json (packaged build) | high | mitigate | Task 2's grep-verification runs directly against the generated, packaged output — not `wxt dev` — closing the exact gap PITFALLS.md #4 warns about (CSP/manifest differences between dev and packaged builds) |

</threat_model>

<verification>
Before declaring this plan complete:
- [ ] Both `wxt build -b chrome` and `wxt build -b firefox` succeed and their manifests match D-07/D-08/D-09 (Task 2, automated)
- [ ] Both `<human-check>` items are recorded and surfaced to `08-UAT.md` at end-of-phase verification (SC #1/#3/#4)
</verification>

<success_criteria>
- All tasks completed
- All four ROADMAP Phase 8 success criteria have either an automated check (SC #2, D-07/D-08/D-09 manifest fields) or a queued human-check (SC #1, #3, #4)
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-03-SUMMARY.md`
</output>
