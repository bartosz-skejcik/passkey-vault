---
phase: 21-crypto-foundation-asymmetric-identity-collection-keys
plan: 01
subsystem: crypto
tags: [crypto_box, x25519, sealed-box, backward-compat, decision-record, pv-core]

requires: []
provides:
  - "KEY-05 decision record (crypto_box =0.9.1 chosen over hpke/rsa/hand-rolled X25519-ECDH), committed as the phase's first commit in PROJECT.md + docs/ARCHITECTURE.md"
  - "Committed pre-v0.4 EncryptedItem fixture (crates/pv-core/tests/fixtures/pre_v0_4_item.json), generated by today's unmodified encrypt_item before any AAD change exists"
  - "Permanent backward-compatibility regression test (crates/pv-core/tests/backward_compat.rs) proving the fixture decrypts byte-identically"
affects: [21-02, 21-03, 21-04, 21-05]

tech-stack:
  added: []
  patterns:
    - "Committed test fixtures under crates/*/tests/fixtures/, generated by a deleted one-shot cargo example (never committed) — new pattern for this repo, first used here"
    - "Cross-cutting regression proof lives in its own crates/*/tests/*.rs file, not inside the module's #[cfg(test)] mod tests — mirrors pv-provider/tests/response_shape.rs"

key-files:
  created:
    - crates/pv-core/tests/fixtures/pre_v0_4_item.json
    - crates/pv-core/tests/backward_compat.rs
  modified:
    - .planning/PROJECT.md
    - docs/ARCHITECTURE.md

key-decisions:
  - "KEY-05: crypto_box crate, exact-pinned =0.9.1, features chacha20+alloc+rand_core — chosen over hpke 0.14.0, rsa 0.9.10, and hand-rolled X25519-ECDH via x25519-dalek. Full rationale + both disclosed limitations (no AAD at seal layer; crypto_box::SecretKey lacks Zeroize) recorded in PROJECT.md Key Decisions table and docs/ARCHITECTURE.md 'Decyzja D'."
  - "Task 3's TDD test intentionally passes on first run (no RED phase) — it exercises already-frozen, unmodified decrypt_item behavior; its value is as a committed regression tripwire for FUTURE AAD changes, not as a driver of new implementation. This matches the plan's own explicit design (21-RESEARCH.md 'Pre-v0.4 Fixture Strategy': 'this test is expected to pass trivially even without any of Phase 21's changes')."

patterns-established:
  - "Ordering-gate plans: when a phase's own success criteria mandate commit ORDER (not just content), each gate gets its own isolated commit containing only the files that gate requires, verified immediately after commit via git show --stat and git log --reverse."

requirements-completed: [KEY-05]

coverage:
  - id: D1
    description: "KEY-05 decision record exists in both PROJECT.md and docs/ARCHITECTURE.md, is the phase's first commit, states both disclosed crypto_box limitations (no AAD at seal layer, SecretKey lacks Zeroize)"
    requirement: "KEY-05"
    verification:
      - kind: other
        ref: "git show --stat --format=%s HEAD (commit 27735f5) — subject contains KEY-05, touches only .planning/PROJECT.md and docs/ARCHITECTURE.md; git log --oneline --reverse -- .planning/PROJECT.md docs/ARCHITECTURE.md confirms 27735f5 is the earliest Phase-21 commit for these files"
        status: pass
    human_judgment: false
  - id: D2
    description: "Pre-v0.4 EncryptedItem fixture committed, generated by the pre-change encrypt_item, generator deleted before commit"
    requirement: "KEY-01"
    verification:
      - kind: unit
        ref: "crates/pv-core/tests/backward_compat.rs#pre_v0_4_item_decrypts_unchanged"
        status: pass
      - kind: other
        ref: "git show --name-only --format= HEAD (commit 8c24514) lists only crates/pv-core/tests/fixtures/pre_v0_4_item.json; generate_fixture.rs absent from working tree"
        status: pass
    human_judgment: false
  - id: D3
    description: "Permanent backward-compat regression test passes against committed fixture; items.rs byte-for-byte unchanged"
    requirement: "KEY-01"
    verification:
      - kind: unit
        ref: "cargo test -p pv-core --test backward_compat -> test result: ok. 1 passed"
        status: pass
      - kind: other
        ref: "git diff --stat crates/pv-core/src/items.rs (empty for the whole plan)"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-07-30
status: complete
---

# Phase 21 Plan 01: Ordering Gates — KEY-05 Decision Record & Backward-Compat Fixture Summary

**Committed the phase's two hard ordering gates in the mandated sequence: the KEY-05 crypto_box decision record as the first Phase 21 commit, then a pre-v0.4 EncryptedItem fixture generated from today's unmodified `encrypt_item` plus its permanent regression test — both landing before any dependent code (crypto_box dependency, `identity.rs`, or AAD changes) exists anywhere in the tree.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-07-30T01:01Z
- **Tasks:** 3
- **Files modified:** 4 (2 docs, 1 fixture, 1 test — plus a generator that was created then deleted, never committed)

## Accomplishments
- KEY-05 decision record: `crypto_box =0.9.1` (chacha20+alloc+rand_core) chosen over `hpke` 0.14.0, `rsa` 0.9.10, and hand-rolled X25519-ECDH — recorded in `.planning/PROJECT.md`'s Key Decisions table and a new "Decyzja D" subsection in `docs/ARCHITECTURE.md` §4, including the extended key-hierarchy diagram branch (UK → identity secret key → Collection Key → collection-scoped items) and both disclosed limitations (no AAD support at the `ChaChaBox` seal layer; `crypto_box::SecretKey` does not implement `Zeroize`)
- Pre-v0.4 backward-compatibility fixture committed: `crates/pv-core/tests/fixtures/pre_v0_4_item.json`, generated once via a throwaway `cargo run -p pv-core --example generate_fixture` against today's unmodified `encrypt_item`, then the generator deleted so it can never silently regenerate
- Permanent regression test `crates/pv-core/tests/backward_compat.rs#pre_v0_4_item_decrypts_unchanged` decrypts the committed fixture and asserts the exact fixed plaintext — a tripwire for any future accidental perturbation of `build_item_aad`'s byte layout or the `AAD_ITEM_KEY_PREFIX`/`AAD_ITEM_DATA_PREFIX` constants

## Task Commits

Each task was committed atomically:

1. **Task 1: KEY-05 decision record** - `27735f5` (docs) — touches only `.planning/PROJECT.md` and `docs/ARCHITECTURE.md`, no `crates/pv-core/src/` files
2. **Task 2: Generate and commit the pre-v0.4 backward-compatibility fixture** - `8c24514` (test) — touches only `crates/pv-core/tests/fixtures/pre_v0_4_item.json`
3. **Task 3: Permanent backward-compatibility regression test** - `adb973a` (test) — touches only `crates/pv-core/tests/backward_compat.rs`; `crates/pv-core/src/items.rs` byte-for-byte unchanged

**Plan metadata:** committed separately by the orchestrator after wave completion (worktree mode — this agent does not write STATE.md/ROADMAP.md).

## Files Created/Modified
- `.planning/PROJECT.md` - New KEY-05 row in Key Decisions table
- `docs/ARCHITECTURE.md` - New "Decyzja D: sealed-box dla Collection Key (KEY-05)" subsection under §4, plus one appended branch on the existing "Hierarchia kluczy" ASCII diagram (no existing line altered)
- `crates/pv-core/tests/fixtures/pre_v0_4_item.json` - Committed pre-v0.4 `EncryptedItem` JSON, generated by the pre-change code path
- `crates/pv-core/tests/backward_compat.rs` - New integration test file, `pre_v0_4_item_decrypts_unchanged`

## Decisions Made
- KEY-05 sealed-box crypto choice (`crypto_box` =0.9.1) — full rationale and rejected alternatives recorded in both PROJECT.md and ARCHITECTURE.md as required by SC#1; see `key-decisions` above for the condensed version.
- Task 3's regression test was written to pass immediately rather than following a strict RED-then-GREEN cycle, since it deliberately exercises already-frozen, unmodified `decrypt_item` behavior (the plan's own `<done>` criterion requires `items.rs` be byte-for-byte unchanged by this task). This is the documented design in `21-RESEARCH.md`'s "Pre-v0.4 Fixture Strategy" section, not an ad hoc deviation from TDD.

## Deviations from Plan

None — plan executed exactly as written. No Rule 1-4 auto-fixes were needed; both tasks' acceptance criteria were met on first attempt.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. No new Cargo dependency, feature, or pv-core source module was added by this plan (by design — Plan 21-02 onward add the actual `crypto_box` dependency and `identity.rs` module).

## Next Phase Readiness

Both phase-level ordering gates (SC#1, SC#4) are now satisfied in git history and independently verifiable via `git log`/`git show`, before any other Phase 21 plan's code exists in this worktree:
- SC#1: `27735f5` is the earliest Phase 21 commit touching `.planning/PROJECT.md`/`docs/ARCHITECTURE.md`, contains only those two files, and the KEY-05 decision record states both disclosed crypto_box limitations plainly.
- SC#4: `8c24514` (fixture) predates any commit touching `crates/pv-core/src/items.rs` — none exists yet in this plan — and the permanent regression test in `adb973a` is green.

Plans 21-02 through 21-05 (identity keypair module, sealed Collection Key, scope-bound AAD, pv-wasm bridge) can now proceed — the `crypto_box` dependency, `identity.rs` module, and any AAD-touching change in `items.rs` are all unblocked by these two gates. No blockers identified for downstream plans.

---
*Phase: 21-crypto-foundation-asymmetric-identity-collection-keys*
*Completed: 2026-07-30*
