---
phase: 21-crypto-foundation-asymmetric-identity-collection-keys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - docs/ARCHITECTURE.md
  - crates/pv-core/tests/fixtures/pre_v0_4_item.json
  - crates/pv-core/tests/backward_compat.rs
autonomous: true
requirements: [KEY-05, KEY-01]

must_haves:
  truths:
    - "The KEY-05 decision record exists in both .planning/PROJECT.md's Key Decisions table and docs/ARCHITECTURE.md §4, is the phase's first commit, and predates every commit that adds crypto_box or identity.rs (git log --oneline --reverse confirms ordering)."
    - "crates/pv-core/tests/fixtures/pre_v0_4_item.json is a committed EncryptedItem JSON blob generated by the current, unmodified encrypt_item — not a same-run round trip — and crates/pv-core/tests/backward_compat.rs decrypts it byte-identically to the fixed plaintext the generator used."
    - statement: "KEY-05 decision record ordering (SC#1) and fixture-before-AAD-changes ordering (SC#4) are process/commit-sequencing properties, not runtime behaviors a unit test can assert directly — confirmed only by inspecting git history."
      verification: backstop
  artifacts:
    - "crates/pv-core/tests/fixtures/pre_v0_4_item.json"
    - "crates/pv-core/tests/backward_compat.rs"
  key_links:
    - "PROJECT.md Key Decisions row -> docs/ARCHITECTURE.md §4 narrative (same decision, two locations, cross-referenced)"
    - "backward_compat.rs -> pv_core::items::decrypt_item (frozen personal-scope path, unmodified by this plan)"
  prohibitions:
    - requirement_id: KEY-05
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT omit or downplay the known crypto_box limitations (ChaChaBox rejects non-empty AAD; crypto_box::SecretKey does not implement Zeroize) in the KEY-05 decision record — both must be stated plainly for a future reviewer, not silently worked around or hidden"
    - requirement_id: KEY-03
      category: n/a
      status: dismissed
      verification: null
      resolution: null
      reason: "Stage 2 kept zero bespoke prohibitions for KEY-03 (cross-scope AAD tampering) — the only surfaced candidate (a malicious/broken server substituting ciphertext across scopes) is canon Tampering, already covered by this phase's mandatory <threat_model> STRIDE register in Plan 21-03, not re-minted here."
    - requirement_id: KEY-04
      category: n/a
      status: dismissed
      verification: null
      resolution: null
      reason: "Stage 2 kept zero bespoke prohibitions for KEY-04 (domain-separation constants) — every surfaced candidate (constant reuse, missing version suffix) is routine architecture/correctness, already owned by the constant-distinctness test in Plan 21-02, not a values/safety/ethics concern."
---

<objective>
Lay the two prerequisite gates every other Phase 21 plan depends on, in the exact order the
phase's own success criteria mandate: (1) write and commit the KEY-05 sealed-box decision
record BEFORE any dependent crypto_box/identity.rs code exists anywhere in the tree (SC#1),
and (2) generate and commit a pre-v0.4 backward-compatibility fixture from TODAY's unmodified
`encrypt_item` code path, plus its permanent regression test, BEFORE any AAD-touching code
change lands (SC#4).

Purpose: every later Phase 21 plan (21-02 through 21-05) adds real crypto_box-dependent code
or touches items.rs's AAD scheme. Both gates in this plan must be satisfied first, or the
phase's own explicit success criteria fail on ordering alone regardless of code quality.

Output: a KEY-05 decision record in `.planning/PROJECT.md` + `docs/ARCHITECTURE.md`, a
committed pre-v0.4 fixture, and a permanent regression test proving it decrypts unchanged.
No pv-core source files are modified by this plan — `identity.rs` does not exist yet and
`items.rs` is untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-crypto-foundation-asymmetric-identity-collection-keys/21-CONTEXT.md
@.planning/phases/21-crypto-foundation-asymmetric-identity-collection-keys/21-RESEARCH.md
@docs/ARCHITECTURE.md
@crates/pv-core/src/items.rs
@crates/pv-core/src/keys.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: KEY-05 decision record — crypto_box vs. hand-rolled X25519-ECDH vs. hpke/rsa</name>
  <reversibility rating="costly">The sealed-box crate choice was already made under Claude's Discretion in 21-CONTEXT.md; this task only records it. Undo cost if ever revisited: every downstream phase's SealedKey wire format and pv-wasm binding shape would need to change together — costly, not one-way (no persisted data exists yet).</reversibility>
  <files>.planning/PROJECT.md, docs/ARCHITECTURE.md</files>
  <read_first>
    .planning/phases/21-crypto-foundation-asymmetric-identity-collection-keys/21-CONTEXT.md (section "Sealed-Box Construction (KEY-05)"), .planning/phases/21-crypto-foundation-asymmetric-identity-collection-keys/21-RESEARCH.md (sections "Verified Corrections", "KEY-05 Decision Record — Ready-to-Write Content", "Zeroize Gap", "AAD Binding — Where It Actually Lives"), .planning/PROJECT.md (Key Decisions table, note the XBR-03 row's format as the precedent to match), docs/ARCHITECTURE.md (section 4 "Kryptografia", especially the "Hierarchia kluczy" ASCII diagram and the "### Decyzja A/B/C" heading style under section 2)
  </read_first>
  <action>
    This is the FIRST task of the FIRST plan of the phase, and its commit must be the phase's
    first commit — no other Phase 21 task may run before this one lands.

    In `.planning/PROJECT.md`'s Key Decisions table, add one new row (matching the existing
    `| Decision | Rationale | Outcome |` column shape and the XBR-03 row's level of detail) for:
    Decision column: "KEY-05: sealed-box crypto — crypto_box crate (exact-pinned =0.9.1,
    features chacha20+alloc+rand_core) vs. hand-rolled X25519-ECDH vs. hpke 0.14.0 vs. rsa
    0.9.10". Rationale column must state: crypto_box is the only stable, Cure53-audited
    RustCrypto-org public-key-encryption crate already sharing this project's pinned
    chacha20poly1305/rand_core/aead dependency graph; its chacha20 feature yields
    XChaCha20-Poly1305, the exact AEAD `aead_seal` already uses; the "zero new
    rand_core/getrandom lines" claim was independently re-verified via `cargo tree` against
    both native and wasm32-unknown-unknown targets (cite this re-verification, not just the
    milestone research's original claim). Reject hpke 0.14.0 (forces bumping the pinned hkdf
    and chacha20poly1305 versions; the crate and its x25519-dalek 3.0.0 KEM were ~3 weeks old
    at research time; no independent audit). Reject rsa 0.9.10 (open unpatched
    RUSTSEC-2023-0071 Marvin-attack advisory; matches the Bitwarden RSA-layer pattern this
    project already rejected). Reject hand-rolled X25519-ECDH via x25519-dalek directly
    (x25519-dalek 3.0.0 pulls a rand_core major version that breaks the dependency-graph
    alignment crypto_box preserves; hand-composing a KEM is exactly what a security reviewer
    flags as rolled crypto). Record that crypto_box's own built-in seal/unseal (its optional
    `seal` cargo feature) was found and deliberately rejected, not overlooked: it hardcodes
    SalsaBox (XSalsa20Poly1305) rather than ChaChaBox, breaking the project's own
    cipher-consistency rationale for choosing crypto_box in the first place. State the two
    known limitations plainly, per this project's "security-critical decisions explained"
    documentation convention: ChaChaBox's encrypt/decrypt calls reject any non-empty
    associated data, so Collection-Key scope binding is enforced one layer down at the
    item-AEAD level instead of at the seal layer; and `crypto_box::SecretKey` does not
    implement the `zeroize::Zeroize` trait (its own hand-written Drop only zeroizes an
    internal scalar field, never the raw 32-byte array), so pv-core's opaque identity-key
    wrapper stores its own byte array with its own Zeroize/ZeroizeOnDrop rather than holding a
    long-lived crypto_box::SecretKey. Outcome column: "Decided (Phase 21)".

    In `docs/ARCHITECTURE.md` section 4 ("Kryptografia"), add a new subsection titled
    "### Decyzja D: sealed-box dla Collection Key (KEY-05)" immediately after the existing
    "Hierarchia kluczy" diagram and before "### Przepływ PRF unlock", carrying the same content
    as the PROJECT.md row but in fuller prose (Polish, matching the surrounding section's
    language), including the same two disclosed limitations. Also extend the existing ASCII
    "Hierarchia kluczy" diagram in that same section with one additional branch below the
    existing `UK -> wrapuje per-item Cipher Keys` line, showing conceptually: UK wraps an
    X25519 identity secret key (via the existing symmetric aead_seal, new domain-separation
    constant), and that identity key's public half seals per-recipient Collection Keys,
    which in turn key collection-scoped item encryption. Do not remove or alter any existing
    line of the diagram — only append.

    Commit this task's changes as the phase's first commit, containing only these two files
    (.planning/PROJECT.md and docs/ARCHITECTURE.md) — no other file changes.
  </action>
  <verify>
    <automated>git show --stat --format=%s HEAD | grep -q KEY-05 && test "$(git show --name-only --format= HEAD | grep -c 'crates/pv-core/src/')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md`'s Key Decisions table contains a row whose Decision column
      contains the substring "KEY-05" and whose Outcome column contains "Phase 21".
    - `docs/ARCHITECTURE.md` contains a heading matching `### Decyzja D` under section 4.
    - `docs/ARCHITECTURE.md`'s new subsection text contains both "Zeroize" and "AAD" (case
      matches the source), proving both disclosed limitations are present, not omitted.
    - This is the earliest commit in the repository's history that touches either
      `.planning/PROJECT.md` or `docs/ARCHITECTURE.md` with Phase 21 content — no
      `crates/pv-core/src/identity.rs` file and no `crypto_box` dependency exist in the tree
      at the time of this commit (`git show --stat HEAD` lists only the two docs files).
  </acceptance_criteria>
  <done>Decision record committed in its own commit, containing both the chosen path and both disclosed limitations, before any dependent code exists.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and commit the pre-v0.4 backward-compatibility fixture</name>
  <files>crates/pv-core/examples/generate_fixture.rs (created then deleted, never committed), crates/pv-core/tests/fixtures/pre_v0_4_item.json</files>
  <read_first>
    crates/pv-core/src/items.rs (the CURRENT, unmodified `encrypt_item`/`EncryptedItem` this fixture must be generated through), crates/pv-core/src/keys.rs (`UserKey::from_bytes`), .planning/phases/21-crypto-foundation-asymmetric-identity-collection-keys/21-RESEARCH.md (section "Pre-v0.4 Fixture Strategy" — follow its generation procedure exactly)
  </read_first>
  <action>
    Create `crates/pv-core/examples/generate_fixture.rs` containing a `fn main()` that: builds
    `let uk = pv_core::keys::UserKey::from_bytes([0x42u8; 32]);` (a fixed, non-random,
    explicitly-test-only key, never treated as a real secret); calls today's unmodified
    `pv_core::items::encrypt_item(&uk, br#"{"type":"login","username":"fixture-user","password":"fixture-pre-v0.4-password"}"#, "fixture-item-pre-v0.4", 1)`;
    serializes the resulting `EncryptedItem` via `serde_json::to_string_pretty`; and writes it
    to `crates/pv-core/tests/fixtures/pre_v0_4_item.json` via `std::fs::write` (this is
    executor-run example-binary I/O, not `pv-core` library code — the library itself stays
    I/O-free). `serde_json` is already available to examples as a pv-core dev-dependency; no
    `Cargo.toml` change is needed.

    Run it once: `mkdir -p crates/pv-core/tests/fixtures && cargo run -p pv-core --example
    generate_fixture`. Inspect the resulting JSON file to confirm it deserializes and contains
    `enc_key`/`enc_data` fields each with `nonce`/`ciphertext` arrays.

    Delete `crates/pv-core/examples/generate_fixture.rs` (it must never be committed — a
    generator left in the tree could be accidentally re-run on a future `cargo test`,
    defeating the entire "prove against the OLD code path" purpose). Confirm via `git status`
    that only `crates/pv-core/tests/fixtures/pre_v0_4_item.json` is untracked/new.

    Commit ONLY `crates/pv-core/tests/fixtures/pre_v0_4_item.json` in this commit, in its own
    commit, before any subsequent commit touches pv-core's `items.rs` module.
  </action>
  <verify>
    <automated>test -f crates/pv-core/tests/fixtures/pre_v0_4_item.json && ! test -f crates/pv-core/examples/generate_fixture.rs && test "$(git show --name-only --format= HEAD | grep -c 'crates/pv-core/src/')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pv-core/tests/fixtures/pre_v0_4_item.json` exists, is valid JSON, and contains
      keys `enc_key` and `enc_data`, each an object with `nonce` and `ciphertext` array fields.
    - `crates/pv-core/examples/generate_fixture.rs` does not exist in the working tree after
      this task completes.
    - The commit adding the fixture file touches no file under `crates/pv-core/src/`.
  </acceptance_criteria>
  <done>Fixture committed alone, generated from the current unmodified encrypt_item, generator deleted so it cannot regenerate on a future test run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Permanent backward-compatibility regression test</name>
  <behavior>
    - Test 1 (`pre_v0_4_item_decrypts_unchanged`): loading `crates/pv-core/tests/fixtures/pre_v0_4_item.json`, deserializing it as `EncryptedItem`, and calling `pv_core::items::decrypt_item` with the SAME fixed `UserKey::from_bytes([0x42u8; 32])`, `"fixture-item-pre-v0.4"`, and revision `1` the generator used, must return exactly the fixed plaintext bytes `{"type":"login","username":"fixture-user","password":"fixture-pre-v0.4-password"}`.
  </behavior>
  <files>crates/pv-core/tests/backward_compat.rs</files>
  <read_first>
    crates/pv-provider/tests/response_shape.rs (module-doc-comment style precedent for a standalone `tests/` regression-tripwire file — mirror its "why this file exists, what it's NOT a duplicate of" framing), crates/pv-core/src/items.rs
  </read_first>
  <action>
    Create `crates/pv-core/tests/backward_compat.rs`. Open with a module doc comment stating:
    this proves SC#4 ("an existing v0.3 account can be given an identity keypair without
    re-encrypting a single byte of its existing vault") via committed fixture data generated by
    the pre-change code path, not a same-run encrypt-then-decrypt round trip — a same-run round
    trip would always use whatever the *current* code produces on both sides and cannot catch a
    regression to `build_item_aad`'s byte layout or the `AAD_ITEM_KEY_PREFIX`/
    `AAD_ITEM_DATA_PREFIX` constants, which is exactly the failure mode this file exists to
    catch. Implement `pre_v0_4_item_decrypts_unchanged` per the Behavior block above, using
    `include_str!("fixtures/pre_v0_4_item.json")`. This is a new, separate test file — it does
    not modify pv-core's `items.rs`'s existing `#[cfg(test)] mod tests` in any way.
  </action>
  <verify>
    <automated>cargo test -p pv-core --test backward_compat | grep -E 'test result: ok\. [1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pv-core --test backward_compat` passes with exactly 1 test.
    - `crates/pv-core/src/items.rs` is byte-for-byte unchanged by this task (`git diff
      --stat crates/pv-core/src/items.rs` for this commit is empty).
  </acceptance_criteria>
  <done>Permanent regression test committed, passing against the committed fixture, proving no re-encryption occurred and standing as a tripwire for any future AAD regression.</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `crates/pv-core/tests/fixtures/pre_v0_4_item.json` — committed fixture, generated by the pre-change `encrypt_item` code path
- `crates/pv-core/tests/backward_compat.rs` — new integration test file (`pre_v0_4_item_decrypts_unchanged`)
- `.planning/PROJECT.md` — new Key Decisions row (KEY-05)
- `docs/ARCHITECTURE.md` — new "### Decyzja D" subsection + extended hierarchy diagram
- No new pv-core source module, no new Cargo dependency, no new Cargo feature (those land in Plan 21-02 onward)

**ASVS Scope (L1):** This plan's threat model applies OWASP ASVS V5 (Input Validation, for the
fixture/decision-record content checks) and V6 (Cryptography, for the frozen personal-scope AAD
guarantee this plan protects); V2 (Authentication), V3 (Session Management), and V4 (Access
Control) are out of scope for Phase 21 — no login/session surface exists and membership
authorization is Phase 22 — per 21-RESEARCH.md's "Security Domain -> Applicable ASVS Categories"
table. Blocking threshold: high severity.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation content -> future reviewer | The decision record is the sole source a future contributor reads to understand why crypto_box was chosen and what its known limitations are |
| Fixture generator (one-shot binary) -> committed fixture | The fixture must faithfully represent the pre-v0.4 on-disk format; a buggy generator would silently poison every future backward-compat check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01-01 | Information Disclosure | KEY-05 decision record (PROJECT.md/ARCHITECTURE.md) | medium | mitigate | Task 1's acceptance criteria hard-require both "Zeroize" and "AAD" substrings to be present, structurally preventing the record from silently omitting either disclosed crypto_box limitation |
| T-21-01-02 | Tampering | `crates/pv-core/tests/fixtures/pre_v0_4_item.json` (hardcoded test UserKey `[0x42; 32]`) | low | accept | Fixed test-only key, never treated as or resembling a real secret; documented inline in both the (deleted) generator and the permanent test as explicitly a fixture constant, not production key material |
| T-21-01-03 | Repudiation | Commit ordering (SC#1/SC#4 are process properties) | medium | mitigate | Task-level `git log` verification commands make the ordering claim auditable rather than merely asserted; acceptance criteria check `git show --stat` of the first commit |
</threat_model>

<verification>
`cargo test -p pv-core --test backward_compat` passes (1 test). `git log --oneline --reverse -- .planning/PROJECT.md docs/ARCHITECTURE.md` shows the KEY-05 decision commit as the earliest Phase-21-related commit. `git log --oneline --reverse -- crates/pv-core/tests/fixtures/pre_v0_4_item.json` shows the fixture commit predates any later commit touching `crates/pv-core/src/items.rs` (verified again at the phase gate once Plan 21-03 lands).
</verification>

<success_criteria>
Both phase-level ordering gates (SC#1, SC#4) are satisfied in the git history, verifiable by `git log`, before any other Phase 21 plan's code exists. The permanent backward-compat test is green.
</success_criteria>

<output>
Create `.planning/phases/21-crypto-foundation-asymmetric-identity-collection-keys/21-01-SUMMARY.md` when done
</output>
