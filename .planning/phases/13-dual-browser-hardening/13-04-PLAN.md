---
phase: 13-dual-browser-hardening
plan: 04
type: execute
wave: 3
depends_on: ["13-03"]
files_modified: [.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md, extension/e2e/dual-browser.spec.ts]
autonomous: true
requirements: [XBR-01]
must_haves:
  truths:
    - "The Firefox packaged/dev build has been walked through all 19 SCs, with results recorded in the checklist's Firefox column"
    - "D-05 (chrome.storage.session-only key, survives idle-kill/wake) is explicitly re-verified on both browsers, not assumed to hold on Firefox just because its event-page lifecycle is more lenient"
    - "D-08 (MAIN-world navigator.credentials patch injection via manual document.createElement('script') from an ISOLATED-world content script) is confirmed to behave identically on both browsers"
    - "Every divergence found during this pass has been fixed in the owning phase's source file and re-verified on BOTH browsers before being marked resolved — never fixed and tested on only one browser"
  artifacts:
    - path: .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md
      provides: "Final all-green (or explicitly-justified-exception) SC-by-SC Chrome/Firefox matrix"
  key_links:
    - from: ".planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md"
      to: "extension/ source files touched by divergence fixes"
      via: "any FAIL row's fix is traceable to a specific file change, re-tested on both browsers before being marked resolved"
      pattern: "FAIL.*(fixed|RESOLVED)"
---

<objective>
Complete the dual-browser hardening pass: walk all 19 Phase 9-12 success criteria on Firefox (using `web-ext run`, since Playwright cannot load extensions into Firefox — Plan 13-03's harness is Chromium-only by design), explicitly re-verify the two riskiest cross-browser invariants (D-05 session-storage discipline, D-08 MAIN-world injection parity), triage every divergence found, and fix each one in its owning phase's code — re-tested on BOTH browsers before being marked resolved, per the standing anti-pattern this project exists to avoid (a Chrome-only-tested fix silently re-breaking Firefox, or vice versa).

Purpose: D-01 / ROADMAP Phase 13 SC #1 and #3 — full systematic re-verification on both `wxt dev -b chrome` and `wxt dev -b firefox`, and this phase owning the *fix*, not just the finding (13-CONTEXT.md's explicit scope).
Output: A fully-populated, all-green (or explicitly-justified) `13-UAT-CHECKLIST.md`, and any divergence fixes committed to the phases that own them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dual-browser-hardening/13-CONTEXT.md
@.planning/phases/13-dual-browser-hardening/13-RESEARCH.md

# This plan's baseline — the Chrome-side results and the harness built in the prior wave:
.planning/phases/13-dual-browser-hardening/13-03-SUMMARY.md
.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md
extension/e2e/dual-browser.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-driven Firefox UAT walk — all 19 SCs plus D-05/D-08 explicit checks</name>
  <files>.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md</files>
  <read_first>13-UAT-CHECKLIST.md, .planning/phases/13-dual-browser-hardening/13-01-SUMMARY.md (Firefox MV2/MV3 target confirmed)</read_first>
  <action>
    Playwright does not support loading a WebExtension into its Firefox channel, so this pass uses
    a different, self-driven mechanism: run `cd extension && npx web-ext run -t firefox-desktop
    --verbose` (or `npm run dev:firefox` if Phase 8 wired an equivalent script) to launch a real
    Firefox instance with the extension loaded, and drive each of the 19 SCs manually against that
    running instance — using the same test fixtures/pages the Chrome Playwright suite exercises
    (Task 1 of 13-03 defines the 19 cases; reuse the same URLs/forms/test accounts here rather than
    inventing new ones). `web-ext run --verbose` streams the extension's console output to the
    terminal, so any crash/error surfaces automatically as you go — capture and record any such
    output alongside each SC's checklist row, not just a bare pass/fail.

    For each of the 19 SCs, update `13-UAT-CHECKLIST.md`'s Firefox column with `PASS` or `FAIL —
    <specific reason, e.g. "PRF banner absent" or "autofill dropdown did not open on gesture">`.

    Then perform the two explicit invariant checks as their own checklist rows (do not skip these —
    they are the pass's highest-risk items per 13-RESEARCH.md's Anti-Patterns and Known Threat
    Patterns):
    - **D-05**: With the extension unlocked in this Firefox instance, force the background context
      to terminate or go idle for 60+ seconds (Firefox's event-page model is more lenient than
      Chrome's service worker, so confirm the unlocked key still lives ONLY in
      `browser.storage.session` — inspect via the extension's own debug/storage-inspection surface,
      or `about:debugging` → Storage tab if available — and is neither in `storage.local` nor a
      still-warm module-level variable that happened to survive due to Firefox's laxer lifecycle).
      Record the actual observed storage location.
    - **D-08**: Confirm the MAIN-world `navigator.credentials` patch is present on a real third-party
      test page in this Firefox instance (inspect via the page's DevTools console —
      `navigator.credentials.create.toString()` should show the RPC-shim wrapper, not the native
      implementation) and that it was injected via the manual `document.createElement('script')`
      path from an ISOLATED-world content script (per D-08's decision — WXT's declarative
      `world: 'MAIN'` is not used, since Firefox doesn't support it reliably for MV3 content
      scripts). Confirm this matches exactly what Task 1/13-03 already confirmed for Chrome.
  </action>
  <verify>
    <automated>grep -c "PENDING" .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md | grep -qx '0' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `13-UAT-CHECKLIST.md` contains zero `PENDING` entries after this task
    - The D-05 row documents the actual observed storage API used (must read `browser.storage.session`)
    - The D-08 row documents the confirmed MAIN-world injection mechanism matches Chrome's
  </acceptance_criteria>
  <done>All 21 checklist rows have a Firefox result (PASS or FAIL with reason); D-05 and D-08 are explicitly confirmed, not assumed from Firefox's more lenient lifecycle.</done>
</task>

<task type="auto">
  <name>Task 2: Triage and fix every divergence — re-verify on BOTH browsers before marking resolved</name>
  <files>.planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md</files>
  <read_first>13-UAT-CHECKLIST.md</read_first>
  <action>
    For every row with a `FAIL` in either the Chrome or Firefox column, locate and fix the bug in
    the actual owning phase's source file (Phase 9/10/11/12's code — never patch around it inside
    this phase's own harness files). After each fix, re-run BOTH the Chrome Playwright test for that
    SC (`npx playwright test --project=chromium -g "<SC description>"`) AND the Firefox manual check
    for that SC (repeat the relevant step from Task 1) — per 13-RESEARCH.md's explicit anti-pattern
    warning, a fix verified on only the browser where the bug was originally found must not be
    trusted, since Firefox's more lenient event-page lifecycle can mask a bug that Chrome's stricter
    service-worker termination would immediately re-expose (and vice versa for any Firefox-specific
    manifest/CSP quirk).

    Update the checklist row to `RESOLVED — <what was fixed, file path, brief reason>` only after
    both browsers confirm green for that SC. If every row is already `PASS` with no `FAIL` entries
    from Task 1, state that explicitly in the SUMMARY rather than fabricating fixes.

    The automated verify below matches only cells whose content starts with `FAIL` immediately
    after a table-column pipe (`| FAIL`), not the bare substring anywhere in the row — so a
    `RESOLVED` cell's brief reason may freely narrate what used to be broken (e.g. "was failing on
    Firefox, fixed in popup.tsx") without tripping the gate. Just don't start a `Notes`/reason cell's
    text with the literal word `FAIL` once a row is resolved.

    Finally, update `.planning/phases/13-dual-browser-hardening/13-VALIDATION.md`'s frontmatter
    field `nyquist_compliant` to `true` once every row is `PASS` or `RESOLVED`.
  </action>
  <verify>
    <automated>grep -cE '\| *FAIL' .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md | grep -qx '0' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Zero rows remain `FAIL` in `13-UAT-CHECKLIST.md` — every row is `PASS` or `RESOLVED — <detail>`
    - Every `RESOLVED` row cites a specific file path and re-confirms both-browser re-testing
    - `13-VALIDATION.md` frontmatter `nyquist_compliant: true`
  </acceptance_criteria>
  <done>Every SC is green on both browsers; any divergence found has a documented, both-browser-re-verified fix; the phase's validation artifact is marked compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Chrome-verified fix ↔ Firefox behavior | A fix made and only tested on one browser is a trust boundary this phase exists to close |
| MAIN world (page) ↔ ISOLATED content script ↔ background | The passkey-provider RPC shim's injection mechanism, re-verified cross-browser here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-13-08 | Tampering / Elevation of Privilege | Any divergence fix touching background message handlers or session storage | high | mitigate | Task 2's mandatory both-browser re-verification before marking any row `RESOLVED` — no fix is trusted from a single-browser test run |
| T-13-09 | Elevation of Privilege | `browser.storage.session`'s `access_level` accidentally widened while fixing a divergence | high | mitigate | Task 1's D-05 check explicitly confirms storage.session access stays extension-only (never exposed to content scripts) after any background-code touch, per 13-RESEARCH.md's ASVS V4 mapping |
| T-13-10 | Spoofing | MAIN-world `navigator.credentials` patch injected/behaving differently across browsers, opening a gap where the RPC shim could be bypassed on one browser only | high | mitigate | Task 1's D-08 check confirms identical manual `document.createElement('script')`-from-ISOLATED-world injection behavior on both browsers, matching the Chrome-side confirmation from Plan 13-03 |

</threat_model>

<verification>
- [ ] `13-UAT-CHECKLIST.md` has zero `PENDING` and zero `FAIL` rows
- [ ] D-05 and D-08 rows explicitly documented with observed evidence, not assumptions
- [ ] Every `RESOLVED` row's fix was re-tested on both browsers
- [ ] `13-VALIDATION.md` frontmatter `nyquist_compliant: true`
- [ ] Commit atomically per fix: `git add <specific owning-phase file path>` per divergence fix, plus a final `git add .planning/phases/13-dual-browser-hardening/13-UAT-CHECKLIST.md .planning/phases/13-dual-browser-hardening/13-VALIDATION.md` — never `git add -A`
</verification>

<success_criteria>
- All tasks completed
- Full 21-row matrix green on both browsers
- D-05/D-08 invariants explicitly re-confirmed, not assumed
- No errors or warnings introduced
</success_criteria>

<output>
Create `.planning/phases/13-dual-browser-hardening/13-04-SUMMARY.md` when done — this is the phase's final closing summary; list every divergence found and fixed, or state explicitly that none were found.
</output>
