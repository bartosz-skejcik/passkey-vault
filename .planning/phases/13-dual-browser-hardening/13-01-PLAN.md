---
phase: 13-dual-browser-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [extension/wxt.config.ts, extension/package.json]
autonomous: true
requirements: [XBR-01]
must_haves:
  truths:
    - "extension/wxt.config.ts's already-shipped identical strictest-common-denominator CSP (script-src 'self' 'wasm-unsafe-eval'; object-src 'self';) for extension_pages on both browsers is re-confirmed unchanged, not re-declared"
    - "extension/wxt.config.ts's existing browser_specific_settings.gecko.id ('passkey-vault@extension.local', pinned since Phase 8) is left unchanged; strict_min_version is newly added beside it at '115.0' (the browser.storage.session floor) — never a WXT dev-mode auto-generated id, never a floor below 115"
    - "The packaged Firefox build (wxt build -b firefox) passes web-ext lint with zero manifest/CSP errors"
    - "A real crypto operation (vault unlock round-trip) succeeds when the packaged Firefox build is loaded via about:debugging, proving wasm-unsafe-eval is functionally sufficient, not just lint-clean"
  artifacts:
    - path: extension/wxt.config.ts
      provides: "Cross-browser manifest config: strict_min_version pin, Firefox-branch optional_permissions host-pattern fix; gecko.id and CSP re-confirmed unchanged"
    - path: extension/package.json
      provides: "web-ext devDependency + lint:firefox script (dev:firefox/build:firefox already existed pre-Phase-13)"
  key_links:
    - from: "extension/wxt.config.ts"
      to: "extension/package.json"
      via: "lint:firefox script's --source-dir target matches the .output/ directory wxt.config.ts's build actually produces"
      pattern: "lint:firefox.*--source-dir"
---

<objective>
Re-confirm the extension's already-shipped cross-browser manifest/CSP configuration and add the one genuinely missing hardening piece plus the tooling to prove it: `browser_specific_settings.gecko.id` is ALREADY pinned (Phase 8, `wxt.config.ts:130`, `passkey-vault@extension.local`) and MUST NOT change; this plan ADDS `strict_min_version` beside it (D-04). The strict, identical CSP (D-09) is ALREADY shipped as the WXT object shape and auto-converted correctly for Firefox's MV2 manifest — this plan re-confirms that survives the packaged build, it does not add or re-declare it. Also re-confirms (not re-decides) Phase 8's actual MV2-vs-MV3 Firefox target (D-07), fixes the Firefox-branch `optional_permissions` host-pattern gap, then verifies the packaged Firefox build passes `web-ext lint` (D-02) and actually instantiates WASM at runtime, not just in dev mode.

Purpose: D-02/D-04/D-07/D-09 are the phase's configuration-correctness bar (ROADMAP Phase 13 SC #2 and #4). Without a deliberately-pinned Firefox extension ID and version floor, `chrome.storage.session` state churns or the manifest asserts compatibility with versions the extension cannot actually run on (breaking D-05's invariant test in later plans); without a verified CSP, `pv-wasm` silently fails to instantiate only in the packaged build (Pitfall 2/4).
Output: A minimally-hardened `extension/wxt.config.ts` (strict_min_version + Firefox optional_permissions fix only — gecko.id and CSP untouched) + `extension/package.json` (web-ext devDependency + lint:firefox script), a clean `web-ext lint` pass on the packaged Firefox build. Firefox is already installed on this dev machine (152.0.6) — no install step needed. The moz-extension CORS server-side deliverable (D-10, EXT-05) is NOT this plan's job — see the note at the end of Task 2 pointing to NEW plan 13-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dual-browser-hardening/13-CONTEXT.md
@.planning/phases/13-dual-browser-hardening/13-RESEARCH.md
@.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-CONTEXT.md
@.planning/phases/08-extension-bootstrap-wasm-in-background-spike/08-RESEARCH.md

# Phase 8's actual scaffold — read whatever exists here at execution time (this plan only
# runs after Phase 8-12 have shipped, per GSD's numeric phase-execution order):
extension/wxt.config.ts
extension/package.json
.planning/phases/08-extension-bootstrap-wasm-in-background-spike/*-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add web-ext tooling (actual delta only)</name>
  <files>extension/package.json</files>
  <read_first>extension/package.json, 13-RESEARCH.md (Environment Availability, Package Legitimacy Audit)</read_first>
  <action>
    STALE PREMISE, DO NOT ACT ON IT: 13-RESEARCH.md's Environment Availability table claims no
    Firefox is installed on this dev machine. That is no longer true — `firefox --version` reports
    152.0.6 already installed. Do NOT run `brew install --cask firefox` or add any install step.

    Also STALE: `extension/package.json` already has `"dev:firefox": "wxt -b firefox"` and
    `"build:firefox": "wxt build -b firefox"` (and `"build:chrome": "wxt build -b chrome"`); the
    plain `"dev": "wxt"` already targets Chrome by default. Do NOT add `dev:chrome` or duplicate
    `dev:firefox`/`build:firefox` — they already exist and are equivalent to what would otherwise
    be newly added. (Optional, non-blocking: add a `"dev:chrome": "wxt -b chrome"` alias purely for
    naming symmetry with `dev:firefox` — skip if it adds no value.)

    The ONLY genuine gap: `web-ext` is not yet a devDependency, and there is no `lint:firefox`
    script. Add `web-ext@10.5.0` as a devDependency in `extension/package.json`
    (`npm i -D web-ext@10.5.0` from inside `extension/`). Before installing, run
    `npm view web-ext repository.url` and `npm view web-ext scripts.postinstall` as an automated
    sanity re-confirmation — 13-RESEARCH.md's Package Legitimacy Audit already vetted and approved
    this package (official Mozilla tool, 162k+ weekly downloads, no postinstall script; `10.5.0` is
    confirmed still the latest published version), so no blocking human checkpoint is required, per
    that audit's explicit discretion note; this is a fast automated re-check, not a new audit.

    Add exactly one new script: `"lint:firefox": "web-ext lint --source-dir
    ./.output/firefox-mv2"`. The real generated output directory is `firefox-mv2` (Firefox is on
    MV2 per Phase 8's D-07 pin, ground-truth-confirmed against the actual `.output/` tree) — never
    `firefox-mv3`, which does not exist.
  </action>
  <verify>
    <automated>cd extension && node -e "const p=require('./package.json'); if(!p.devDependencies['web-ext']) process.exit(1); if(!p.scripts['lint:firefox']) process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `extension/package.json` devDependencies contains `"web-ext"` pinned to `10.5.0`
    - `extension/package.json` scripts contains `lint:firefox` targeting `./.output/firefox-mv2`
    - `dev:firefox` and `build:firefox` are unchanged (already existed) — not duplicated or overwritten
  </acceptance_criteria>
  <done>`web-ext` is a devDependency; `lint:firefox` exists and points at the real `.output/firefox-mv2` directory; no redundant scripts added, no Firefox install step run (already present).</done>
</task>

<task type="auto">
  <name>Task 2: Add strict_min_version + Firefox optional_permissions fix; re-confirm gecko.id/CSP/MV2 unchanged (D-04, D-07, D-09)</name>
  <files>extension/wxt.config.ts</files>
  <read_first>extension/wxt.config.ts, .planning/phases/08-extension-bootstrap-wasm-in-background-spike/*-SUMMARY.md, 13-RESEARCH.md (Pattern 1, Pitfall 3)</read_first>
  <action>
    GROUND TRUTH (re-confirmed against the real file, do not re-derive): Phase 8 already pinned
    Firefox to MV2 persistent background (D-07 — confirmed, not changing). `wxt.config.ts:128-132`
    already ships:
    ```
    browser_specific_settings: {
      gecko: {
        id: 'passkey-vault@extension.local',
      },
    },
    ```
    This `gecko.id` is ALREADY pinned and deliberate (Phase 8, D-09 per that file's own comment) —
    it is NOT a WXT dev-mode placeholder. It MUST NOT change. Do not write
    `passkey-vault@paczesny.pl` or any other value anywhere in this task — changing the id would
    orphan any `chrome.storage.session` state keyed to the add-on's identity (exactly the churn
    D-04 exists to prevent). This task's ONLY edit inside that `gecko` block is to ADD a sibling
    `strict_min_version` key next to the existing `id`.

    Set `browser_specific_settings.gecko.strict_min_version: '115.0'`. Rationale (record verbatim in
    the SUMMARY): the extension reads/writes `browser.storage.session` in 9 non-test files (the D-05
    User-Key home) and `storage.session` did not ship in Firefox until version 115 — a lower floor
    (e.g. 91 ESR, previously considered) would assert compatibility with versions where the
    extension cannot function at all. 115 happens to also be an ESR release. NEVER set a floor below
    115.

    CSP is VERIFY-NOT-ADD. `wxt.config.ts:122-124` already ships the strict, identical CSP as the
    WXT object shape (`content_security_policy: { extension_pages: "script-src 'self'
    'wasm-unsafe-eval'; object-src 'self';" }`), and the pinned WXT 0.20.27 (`node_modules/wxt/dist/core/utils/manifest.mjs`
    `convertCspToMv2()`) already auto-converts this to the correct plain-string form in the
    generated Firefox MV2 manifest (confirmed: `.output/firefox-mv2/manifest.json` already carries
    `"content_security_policy":"script-src 'self' 'wasm-unsafe-eval'; object-src 'self';"`). Do NOT
    add, rewrite, or per-browser-branch the CSP in this task — a per-browser CSP branch would
    violate D-09 ("identical, not branched per browser"). This task's ONLY `wxt.config.ts` edits are
    (a) the `strict_min_version` addition above and (b) the Firefox-branch `optional_permissions`
    fix below.

    CARRIED-OVER BLOCKER from Phase 9 (09-03-SUMMARY.md flagged this "for Phase 13" — do not skip):
    Firefox MV2 STRIPS `optional_host_permissions` from the manifest entirely (WXT's manifest.mjs
    treats it as an MV3-only key), so EXT-05's runtime `browser.permissions.request()` grant for the
    user's self-hosted server origin has NO manifest pre-declaration on Firefox and the request will
    fail. Fix in the Firefox branch of the manifest function: declare the host match-patterns
    (`http://*/*`, `https://*/*`) under MV2's shared `optional_permissions` array instead (Firefox
    reads host patterns from there), keeping `optional_host_permissions` for the Chrome/MV3 branch
    untouched. Then verify IN REAL FIREFOX (web-ext run) that `browser.permissions.request({origins:[...]})`
    for a concrete origin actually prompts and grants. Record observed behavior in the SUMMARY.

    SCOPE OF THIS TASK'S ACCEPTANCE (do not overclaim): this task verifies the permission
    PROMPT/GRANT only — i.e. that Firefox actually shows the `browser.permissions.request()` dialog
    and grants it once the manifest pre-declares the host patterns via `optional_permissions`. It
    does NOT verify that `server-config.ts`'s `configureServer()` succeeds end-to-end on Firefox: the
    `/healthz` reachability probe that `configureServer()` runs happens BEFORE the permission is
    granted, and Firefox's own CORS enforcement against that pre-grant cross-origin fetch is a
    SEPARATE, server-side deliverable (moz-extension origin allowlisting) owned by NEW plan 13-05
    (D-10, EXT-05) — do not assert or attempt to fix that here.

    LINT WATCH-ITEM (resolve per actual output, do not pre-empt): once `strict_min_version` is set,
    Task 3's `web-ext lint` run may newly report a `content_scripts[].world` finding, since the
    generated firefox-mv2 manifest emits `"world":"ISOLATED"` (a key Firefox only recognizes from
    version 128). Do NOT bump `strict_min_version` to 128 pre-emptively on this assumption alone —
    wait for Task 3's real lint output and resolve based on what it actually reports.

    NOT THIS PLAN'S JOB: the moz-extension CORS server-side mechanism (D-10: pv-server gaining
    first-class support for a scheme-scoped `moz-extension://*` wildcard via a deliberate mechanism
    like `AllowOrigin::predicate`), its docs, and the ServerConfigView CORS-vs-unreachable UX (D-11)
    are owned entirely by NEW plan 13-05, authored in parallel. Do not fold any of that in here.
  </action>
  <verify>
    <automated>cd extension && grep -c "wasm-unsafe-eval" wxt.config.ts | grep -qv '^0$' && grep -c "strict_min_version" wxt.config.ts | grep -qv '^0$' && grep -c "passkey-vault@extension.local" wxt.config.ts | grep -qv '^0$' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `extension/wxt.config.ts` still contains `wasm-unsafe-eval` (re-confirmed unchanged, not re-added)
    - `extension/wxt.config.ts` still contains `browser_specific_settings.gecko.id: 'passkey-vault@extension.local'`, byte-for-byte unchanged from Phase 8
    - `extension/wxt.config.ts` newly contains `strict_min_version` set to `'115.0'`, never a value below 115
    - CSP shape (object) is unchanged — no per-browser CSP branch introduced
    - The generated firefox manifest declares the host match-patterns under `optional_permissions` (MV2) so `browser.permissions.request()` prompts and grants — verified against the real generated `.output/firefox-mv2/manifest.json` AND a live web-ext run exercising the permission dialog (NOT asserting `configureServer()`'s pre-grant healthz probe succeeds — that is 13-05's job, D-10)
  </acceptance_criteria>
  <done>wxt.config.ts gains `strict_min_version: '115.0'` and the Firefox-branch `optional_permissions` host-pattern fix; gecko.id, CSP, and the Firefox MV2 target are all re-confirmed unchanged from Phase 8's actual shipped decision.</done>
</task>

<task type="auto">
  <name>Task 3: Verify web-ext lint + runtime WASM smoke test on the packaged Firefox build (D-02)</name>
  <files>extension/wxt.config.ts</files>
  <read_first>13-RESEARCH.md (Pitfall 2, Anti-Patterns, Code Examples)</read_first>
  <action>
    Run `cd extension && npm run build:firefox` to produce the packaged Firefox output, then
    `npm run lint:firefox`. Fix any manifest/CSP/permission errors `web-ext lint` reports until it
    exits 0 — this is the literal D-02 acceptance bar (ROADMAP Phase 13 SC #2).

    A clean lint pass only proves manifest *syntax* validity, not that `wasm-unsafe-eval` is
    functionally sufficient at runtime (Pitfall 2's anti-pattern). Load the packaged Firefox build
    as a temporary add-on (Firefox → `about:debugging#/runtime/this-firefox` → "Load Temporary
    Add-on" → select the `.output/firefox-mv2/manifest.json`) and trigger ONE vault-unlock WASM
    round-trip — confirm no `CompileError`/`EvalError: Refused to compile` appears in the background
    console. Record the exact steps taken and the observed console output in this plan's SUMMARY.md
    so the check is reproducible, not just asserted.

    SCOPE (do not expand): this smoke test proves only that WASM instantiates under the packaged
    Firefox CSP for a single round-trip — it is NOT the full per-feature cross-browser parity UAT
    (that is 13-04's job per D-01, covering unlock/session, autofill, generate/capture, and passkey
    provider across both browsers). Do not attempt to exercise every feature here.
  </action>
  <verify>
    <automated>cd extension && npm run build:firefox && npm run lint:firefox</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint:firefox` exits 0 with no errors
    - SUMMARY.md documents the manual about:debugging load + the specific WASM vault-unlock round-trip triggered + confirmation of no CompileError/EvalError in console
  </acceptance_criteria>
  <done>`web-ext lint` passes clean on the packaged Firefox build, and a real WASM crypto operation has been confirmed to run without CSP-related errors when loaded as a temporary add-on; full per-feature parity UAT remains deferred to 13-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Extension build config → packaged browser runtime | `wxt.config.ts`'s declared manifest/CSP is the only thing standing between the packaged extension and a browser that might silently relax or reject it |
| npm registry → dev machine | Installing `web-ext` as a new devDependency pulls third-party code onto the build machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-13-01 | Tampering | `content_security_policy.extension_pages` in `wxt.config.ts` | high | mitigate | Explicit identical CSP string on both browsers (D-09); `web-ext lint` catches syntax regressions; manual runtime smoke test (Task 3) catches functional regressions lint alone can't see |
| T-13-02 | Tampering | Firefox extension identity (`browser_specific_settings.gecko.id`) | medium | mitigate | Already-pinned, permanent, non-auto-generated gecko.id (Phase 8, D-04) — this plan re-confirms it is left unchanged, preventing dev-mode ID rotation from invalidating `chrome.storage.session`-scoped secrets across sessions |
| T-13-03 | Information Disclosure | `strict_min_version` omission silently weakening lint's version-gated API-availability checks | medium | mitigate | `strict_min_version` set to a real, deliberately-chosen floor (Pitfall 3); Task 3 confirms lint output actually reports version-gated findings, not a suspiciously-clean trivial pass |
| T-13-SC1 | Tampering | `npm i -D web-ext` (new devDependency, supply-chain surface) | high | mitigate | Pre-vetted in 13-RESEARCH.md's Package Legitimacy Audit (Approved: official Mozilla tool, 162k+ weekly downloads, no postinstall script); Task 1 re-confirms via automated `npm view web-ext repository.url`/`scripts.postinstall` at install time — no blocking human checkpoint required per that audit's explicit discretion |

</threat_model>

<verification>
- [ ] `npm run build:firefox` succeeds with no build errors
- [ ] `npm run lint:firefox` exits 0
- [ ] `wxt.config.ts` CSP (unchanged) and gecko.id (unchanged) re-confirmed; `strict_min_version` newly present at `'115.0'`
- [ ] Firefox already installed on the dev machine (152.0.6) — no install step performed by this plan
- [ ] Commit atomically: `git add extension/wxt.config.ts extension/package.json` — never `git add -A`
</verification>

<success_criteria>
- All tasks completed
- `web-ext lint` clean on the packaged Firefox build
- Firefox available for this phase's remaining plans (already installed, pre-existing)
- No errors or warnings introduced
</success_criteria>

<output>
Create `.planning/phases/13-dual-browser-hardening/13-01-SUMMARY.md` when done — document the actual Firefox MV2 target re-confirmed, that gecko.id was left unchanged (`passkey-vault@extension.local`), the `strict_min_version` value chosen and why (115.0 / storage.session floor), the Firefox optional_permissions fix + observed permission-grant behavior, the web-ext lint outcome (including any content_scripts world:ISOLATED finding and how it was resolved), and the manual runtime smoke-test steps/output. Note the moz-extension CORS deliverable is deferred to NEW plan 13-05 (D-10).
</output>
