---
phase: 11-generate-capture
plan: 09
type: execute
wave: 7
depends_on: ["11-08"]
files_modified:
  - extension/entrypoints/popup/ItemListView.tsx
  - extension/entrypoints/popup/ItemListView.test.tsx
  - extension/entrypoints/popup/autofill/AutofillItemRow.tsx
  - extension/entrypoints/popup/autofill/OnThisPageSection.tsx
  - extension/entrypoints/popup/style.css
  - extension/lib/autofill/inpage-overlay.ts
  - extension/lib/autofill/inpage-overlay.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "The in-field dropdown and the prompt-window account lists SCROLL: with more than ~4 accounts for one site, every account is reachable (max-height + overflow-y:auto on the LIST area, header/footer stay pinned). Bartek live-bug 2026-07-16: 'mam więcej niż 4 konta do jednej witryny i nie widzę jednego albo 2 bo nie mogę scrollować'."
    - "Popup list items get a button-like HOVER affordance matching the web app's datafa.st button style: 1px border + pressed/depress effect, visible ONLY on hover — rows stay flat/borderless at rest (Bartek: 'taki sam styl jak dla guzików czyli ten border i efekt wciśnięcia tylko to na hoverze')."
    - "vault-light hover polarity is DARKER-on-hover: the hovered row/control background is darker than its rest state, never lighter (Bartek: 'zamień miejscami kolor hover i zwykły')."
    - "Zero behavioral change — fill/copy/navigation handlers, gating, and all existing tests stay green; this is presentation only."
  artifacts:
    - path: "extension/lib/autofill/inpage-overlay.ts"
      provides: "Scrollable list containers (dropdown + prompt) with pinned header, token-based scrollbar-friendly styles"
  key_links:
    - from: "extension/entrypoints/popup/ItemListView.tsx"
      to: "extension/entrypoints/popup/style.css"
      via: "shared row-hover utility (border + pressed effect) applied to both All-items rows and On-this-page rows"
      pattern: "pv-row-hover|hover:"
---

<objective>
Bartek's live popup/overlay review (2026-07-16, dark-theme screenshots + light-theme testing): item-row hover is nearly invisible, light-theme hover polarity is inverted, and the in-page account lists cannot scroll — accounts beyond ~4 are unreachable. Fix all three.

Output: scrollable in-page lists; button-like hover affordance on popup rows; correct light-theme hover polarity. Presentation-only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-generate-capture/11-CONTEXT.md   # D-12/D-13 — tokens, no literal colors outside inpage-theme.ts
@extension/lib/autofill/inpage-overlay.ts
@extension/entrypoints/popup/ItemListView.tsx
@web/src/components/vault/ItemRow.tsx                 # web row hover reference
@packages/pv-ui/tokens.css

# The web app's datafa.st button recipe (see web button styles / UI-DESIGN.md): 1px solid
# border (base-300/base-content-muted), small radius, subtle hard shadow; :active presses
# (translate down + shadow removed). Rows should get the HOVER-ONLY variant of this.
# IMPORTANT: another fixer is concurrently editing background/* + capture-handler/vault-store —
# do NOT touch any file outside this plan's files_modified list.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scroll for in-page account lists (dropdown + prompt window)</name>
  <files>extension/lib/autofill/inpage-overlay.ts, extension/lib/autofill/inpage-overlay.test.ts</files>
  <read_first>extension/lib/autofill/inpage-overlay.ts (both surfaces' DOM structure + CSS)</read_first>
  <behavior>
    - The in-field dropdown's row list gets max-height (~4.5 rows so cutoff is visibly "half a row" = scroll affordance) + overflow-y:auto; the dropdown's search/header (if any) stays pinned outside the scroll area.
    - The prompt window's account list likewise scrolls inside the panel (panel keeps its close/block header pinned).
    - Scrollbar styled unobtrusively via tokens (thin, base-content/20 thumb) — both themes.
    - Positioning/reposition-on-scroll logic (Bitwarden pattern from phase 10 round-2) unaffected.
    - Tests: list container carries the overflow/max-height styles; with N=6 rows all 6 exist in DOM (scroll reaches them).
  </behavior>
  <action>Wrap ONLY the row-list portion of each surface in a scroll container; do not restructure headers/footers.</action>
  <verify>
    <automated>cd extension && npx vitest run lib/autofill/inpage-overlay && npx tsc --noEmit</automated>
  </verify>
  <done>6+ accounts for one site are all reachable by scrolling in both in-page surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Popup row hover affordance + light-theme hover polarity</name>
  <files>extension/entrypoints/popup/ItemListView.tsx, extension/entrypoints/popup/ItemListView.test.tsx, extension/entrypoints/popup/autofill/AutofillItemRow.tsx, extension/entrypoints/popup/autofill/OnThisPageSection.tsx, extension/entrypoints/popup/style.css</files>
  <read_first>web/src/components/vault/ItemRow.tsx, extension/entrypoints/popup/style.css, extension/entrypoints/popup/ItemListView.tsx</read_first>
  <behavior>
    - Every clickable item row (All-items list AND On-this-page rows) at REST: flat, no border. On HOVER: 1px border (web button border token) + the datafa.st pressed-style affordance; on :active additionally the pressed translate. One shared utility class (style.css) so both row kinds stay identical.
    - vault-light polarity: hovered background DARKER than rest (e.g. rest base-100, hover base-200/300-mix per web ItemRow's own hover) — swap wherever the current styles make hover lighter on light. vault-dark keeps lighter-on-hover (correct today).
    - Audit the popup's other hover states (footer select, header buttons) for the same light-theme inversion and align them.
    - No layout shift on hover (border must not change row dimensions — use inset/transparent-border-at-rest technique).
    - Existing tests stay green; add/adjust a rendering test pinning the shared hover class presence on both row kinds.
  </behavior>
  <action>Match the web app's row/button hover recipe visually — read ItemRow.tsx and the web button styles first; keep DaisyUI/token classes, no literal colors.</action>
  <verify>
    <automated>cd extension && npx vitest run entrypoints/popup && npx tsc --noEmit && npx wxt build 2>&1 | tail -1</automated>
  </verify>
  <done>Rows visibly respond on hover with the button border+press style; light theme hover darkens.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-11-40 | Tampering | inpage-overlay scroll container | low | mitigate | Presentation-only; closed-shadow isolation, metadata-only rows, and fill gating untouched. |

</threat_model>

<verification>
- [ ] `cd extension && npx vitest run` passes; tsc clean; both builds green
- [ ] Packaged-build check: 6 accounts on one site → all reachable in dropdown and prompt; hover visible in both themes; light hover darkens
</verification>

<success_criteria>
- Scroll works in both in-page lists; popup rows have the button-style hover; light-theme hover polarity corrected
</success_criteria>

<output>
Create `.planning/phases/11-generate-capture/11-09-SUMMARY.md` when done.
</output>
