---
phase: 11-generate-capture
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - extension/entrypoints/content/ui/shadow-root-mount.ts
  - extension/entrypoints/content/ui/generate-popover.ts
  - extension/lib/i18n/dictionary.ts
  - extension/entrypoints/content-relay.content.ts
autonomous: true
requirements: [CAP-01]

must_haves:
  truths:
    - "On a signup/registration form, the user can request and apply a generated strong password in both character and passphrase modes, per ROADMAP Success Criterion 1 (CAP-01)."
    - "The suggestion UI renders inside a closed Shadow DOM root so the host page's CSS cannot leak in and the extension's injected DOM cannot be read/manipulated by the page's own script."
    - "The popover is click-triggered only, never auto-opening on focus, so it does not visually race the browser's own native 'suggest strong password' popover (Pitfall C)."
  artifacts:
    - path: "extension/entrypoints/content/ui/shadow-root-mount.ts"
      provides: "Lazy, tab-scoped createShadowRootUi mount reused by every Phase 11 UI surface"
    - path: "extension/entrypoints/content/ui/generate-popover.ts"
      provides: "40px click-triggered RefreshCw trigger + 320px suggestion popover (Surface 1 of 11-UI-SPEC.md)"
  key_links:
    - from: "extension/entrypoints/content/ui/generate-popover.ts"
      to: "extension/entrypoints/content/form-detector.ts"
      via: "findPasswordFieldPair() result determines which field(s) the trigger anchors to and which field(s) 'Use this password' fills"
      pattern: "findPasswordFieldPair"
    - from: "extension/entrypoints/content/ui/generate-popover.ts"
      to: "extension/lib/messaging/ext-protocol.ts"
      via: "sendMessage({kind:'generate-request', payload:{...}}) — never generates locally"
      pattern: "generate-request"
---

<objective>
Deliver the user-facing half of CAP-01: a closed-Shadow-DOM-mounted, click-triggered password-suggestion popover anchored to signup-form fields, backed exclusively by the background's `generate-request` handler (Plan 11-01) and the field pairing Plan 11-02's form-detector already computes.

Purpose: This is the first content-script-injected UI surface in the whole v0.2 extension — the shadow-root mount lifecycle built here is reused unmodified by Plan 11-05's save/update toast and origin-mismatch modal, so its isolation guarantees (closed root, self-hosted fonts, no page CSS leakage) must be correct before those surfaces build on it.
Output: A working generate-password suggestion the user can click, preview (both modes), regenerate, and apply into the paired signup fields.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-generate-capture/11-CONTEXT.md
@.planning/phases/11-generate-capture/11-RESEARCH.md
@.planning/phases/11-generate-capture/11-PATTERNS.md
@.planning/phases/11-generate-capture/11-UI-SPEC.md

# Uses 11-01's generate-request contract, 11-02's form-detector/findPasswordFieldPair, and
# 11-03's message-handling foundation (indirectly, via the shared router already wired).

@web/src/components/generator/GeneratorPopover.tsx
@web/src/lib/i18n/dictionary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shadow-root mount lifecycle + self-hosted font wiring</name>
  <files>extension/entrypoints/content/ui/shadow-root-mount.ts</files>
  <read_first>extension/wxt.config.ts, extension/entrypoints/content-relay.content.ts (Phase 10's actual current content-script entrypoint — read before adding any mount call)</read_first>
  <action>
    Implement `mountShadowUi()`: one `createShadowRootUi` instance per tab/frame, `mode:'closed'`
    (page JS must not be able to reach `element.shadowRoot`), `cssInjectionMode:'ui'`, mounted lazily on
    first need (first signup-field focus or first submit-capture success) — never eagerly on every page
    load. Export a single `getOrCreateShadowRoot()` accessor other UI modules (this plan's
    `generate-popover.ts`, Plan 11-05's toast/modal) call to obtain the SAME mounted root — never call
    `createShadowRootUi` a second time per tab. Bundle `DM Sans` and the `ui-monospace` stack as
    self-hosted `@font-face` declarations referencing `chrome-extension://`/`moz-extension://`
    web-accessible-resource URLs inside the injected stylesheet — verify no Google-Fonts or other
    third-party CDN URL appears anywhere in the generated CSS (a page-injected surface must not trigger
    an outbound font fetch to a host the user never consented to loading from, per 11-UI-SPEC.md's Font
    row). If `extension/wxt.config.ts` does not yet declare the font files as web-accessible resources,
    add that declaration here.
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit && grep -c 'fonts.googleapis\|fonts.gstatic' entrypoints/content/ui/shadow-root-mount.ts | grep -qx 0</automated>
  </verify>
  <done>getOrCreateShadowRoot() returns the same closed-mode shadow root instance across repeated calls in a tab; the injected stylesheet references only extension-scheme font URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Generate-password popover (Surface 1) wired to generate-request + field pairing</name>
  <files>extension/entrypoints/content/ui/generate-popover.ts, extension/lib/i18n/dictionary.ts, extension/entrypoints/content-relay.content.ts</files>
  <read_first>web/src/components/generator/GeneratorPopover.tsx, extension/lib/i18n/dictionary.ts (create it mirroring web/src/lib/i18n/dictionary.ts's DICTIONARY/interpolate() shape if it does not yet exist under extension/), extension/entrypoints/content-relay.content.ts</read_first>
  <action>
    Render a 40px-hit-area trigger (16-20px `RefreshCw` icon, absolutely positioned in the trailing
    padding of any field `form-detector.ts` classifies `signup`), click-triggered only — never opening on
    focus (11-UI-SPEC.md Morning review note 1 / Pitfall C: avoids racing the browser's own native
    "suggest strong password" popover, which fires on the same `autocomplete="new-password"` signal). On
    click, mount the 320px popover (via Task 1's `getOrCreateShadowRoot()`) below the field: title, a
    Characters/Passphrase segmented toggle, a `ui-monospace` preview with an Eye/EyeOff reveal toggle,
    a length range (8-64 characters / 3-10 words, matching the v0.1 bounds), character-set checkboxes
    (character mode only), a strength meter reusing `scorePasswordMeter` from 11-01's ported
    `lib/generator/strength.ts`, a `RefreshCw` regenerate control, and a coral "Use this password" apply
    button — mirror `GeneratorPopover.tsx`'s mode-dispatch/apply-callback state shape (generate into local
    preview state, apply only on explicit click), never auto-filling the field on generation. Every
    generated value comes from `sendMessage({kind:'generate-request', payload:{...}})` — this file must
    never call `generateCharacterPassword`/`generatePassphrase` directly, even though those functions have
    no WASM/key-material dependency (D-01/D-07's single-message-registration discipline). Applying fills
    the field `findPasswordFieldPair()` (from 11-02's `form-detector.ts`) identified — both the
    new-password and confirm-password fields when a real pair was found — then closes the popover. On a
    `{error}` response, show the `generate.failed` inline red text in place of the preview; the regenerate
    control stays enabled. Add these dictionary keys verbatim from 11-UI-SPEC.md's Copywriting Contract
    table (PL/EN): `generate.trigger`, `generate.title`, `generate.modeCharacter`, `generate.modePassphrase`,
    `generate.regenerate`, `generate.apply`, `generate.failed`, plus the `aria.showPassword`/
    `aria.hidePassword` entries if not already present. Wire the trigger's mount point into
    `content-relay.content.ts`'s existing per-field observation loop (read the real file first — attach
    to whatever hook Phase 10 already uses to notice new fields on the page, do not create a second,
    parallel DOM-scanning loop).
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit</automated>
    <human-check>Load the unpacked extension, open a fixture signup page with a new-password + confirm-password field pair, confirm the 40px trigger appears in the field's trailing padding, click it, confirm both modes render a preview, regenerate produces a new value, and "Use this password" fills BOTH password fields and closes the popover. Confirm the native browser suggestion and this popover never appear open simultaneously (Pitfall C).</human-check>
  </verify>
  <done>generate-popover.ts click-triggers per signup field; both modes work via generate-request round trips; applying fills the paired fields; dictionary entries present; deferred to end-of-phase UAT for final visual sign-off per project workflow config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| MAIN-world page DOM (untrusted, any script the page runs) <-> closed Shadow DOM root | The page's own JS shares the same document but must not be able to read into or write onto the extension's injected UI, and the extension's injected CSS must not leak onto the page. No message-passing crosses this boundary — it is a DOM/CSS isolation boundary, not a data boundary. |
| ISOLATED content script -> background service worker | `generate-popover.ts`'s only outbound call: `sendMessage({kind:'generate-request', ...})`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-11-11 | Tampering / Information Disclosure | `shadow-root-mount.ts` | medium | mitigate | `mode:'closed'` on `createShadowRootUi` prevents the host page's own script from reaching `element.shadowRoot` to read or manipulate the injected popover/preview (e.g. scraping a previewed password or spoofing the "Use this password" button). |
| T-11-12 | Information Disclosure | Injected stylesheet's font `@font-face` URLs | low | mitigate | Fonts are bundled as self-hosted, extension-scheme (`chrome-extension://`/`moz-extension://`) web-accessible resources — never a third-party CDN URL — so a page the extension injects content into never triggers an outbound request the user didn't consent to. |
| T-11-13 | Spoofing | Generate-trigger icon visual identity | low | accept | A malicious page could attempt to visually mimic the extension's own trigger icon inside its own DOM to phish a click. Accepted: the real popover only ever renders inside the closed shadow root, so clicking a page-forged lookalike opens nothing extension-controlled; this generic phishing-style risk is common to all password-manager extensions and is not closable within this phase's scope. |

</threat_model>

<verification>
- [ ] `cd extension && npx tsc --noEmit` passes
- [ ] Injected stylesheet contains no third-party font CDN URL
- [ ] Manual UAT: trigger/popover/apply flow verified on a fixture signup page (deferred to end-of-phase UAT per `human_verify_mode: end-of-phase`)
</verification>

<success_criteria>
- Both tasks completed
- CAP-01's generated-password suggestion works end-to-end (trigger -> popover -> generate-request -> apply)
- Shadow-root mount is reused (not re-created) by Plan 11-05's later surfaces
</success_criteria>

<output>
Create `.planning/phases/11-generate-capture/11-04-SUMMARY.md` when done.
</output>
