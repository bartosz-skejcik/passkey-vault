---
phase: 11-generate-capture
plan: 05
type: execute
wave: 4
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified:
  - extension/lib/autofill/save-update-toast.ts
  - extension/lib/autofill/mismatch-modal.ts
  - extension/lib/i18n/autofill-dictionary.ts
  - extension/entrypoints/content-relay.content.ts
  - extension/e2e-fixtures/adversarial-iframe/top.html
  - extension/e2e-fixtures/adversarial-iframe/attacker-frame.html
autonomous: true
requirements: [CAP-02, CAP-03]

must_haves:
  truths:
    - "After a successful submit, the user is prompted to save the new login, attributed to the correct origin (ROADMAP Success Criterion 2, CAP-02)."
    - "When the submitted password differs from an already-saved item's password for the same origin+username, the user is offered an update instead of a duplicate save (ROADMAP Success Criterion 3, CAP-03)."
    - "A password submitted from a cross-origin iframe never gets silently attributed to the top-level page — the user sees both origins explicitly and must take an extra, deliberate confirm step (ROADMAP Success Criterion 4, D-06)."
    - "An unchanged resubmit never shows any prompt at all (Pitfall B — no-op is silent, not a needless interruption)."
  artifacts:
    - path: "extension/lib/autofill/save-update-toast.ts"
      provides: "Save-new-login / update-existing-item toast (Surface 2 of 11-UI-SPEC.md), never auto-dismissing except the post-success flash"
    - path: "extension/lib/autofill/mismatch-modal.ts"
      provides: "Blocking origin-mismatch escalation modal (Surface 3), showing both frameOrigin and topOrigin in full"
    - path: "extension/e2e-fixtures/adversarial-iframe/top.html"
      provides: "EXTENDED existing Phase 10 adversarial two-origin fixture (Origin A, http://127.0.0.1:8791, embedding Origin B's attacker-frame.html at http://localhost:8792 — see e2e-fixtures/adversarial-iframe/README.md) so its existing #top-form/#frame-form login forms also reproduce the historical Bitwarden CVE-class cross-origin-iframe scenario for THIS phase's submit-capture/mismatch-modal flow, not a new, separate fixture pair"
  key_links:
    - from: "extension/entrypoints/content-relay.content.ts"
      to: "extension/lib/autofill/mismatch-modal.ts"
      via: "capture.propose response with mismatch:true routes to the modal, never the toast"
      pattern: "mismatch"
    - from: "extension/lib/autofill/save-update-toast.ts"
      to: "extension/lib/messaging/ext-protocol.ts"
      via: "confirm click re-sends the full field payload via sendMessage({kind:'capture.confirm', payload:{...}})"
      pattern: "capture\\.confirm"
---

<objective>
Complete CAP-02/CAP-03's user-facing surface: a save/update toast that never silently disappears, and a blocking origin-mismatch modal that gates both save and update whenever the form's real frame origin differs from the page the user is looking at — plus the adversarial cross-origin-iframe UAT fixture D-06 requires before this phase can be considered done.

Purpose: This is where the phase's headline security requirement (ROADMAP Success Criterion 4 / D-06 / the historical Bitwarden CVE-class bug) becomes visible to the user, not just correct in the background — Plan 11-03 already computes `mismatch` independently and correctly; this plan is responsible for never letting that signal be silently swallowed by a UI that shows the wrong surface.
Output: Working save/update toast and mismatch modal, wired into the content-relay's response handling for `capture.propose`, plus the extended two-origin fixture pair for the adversarial manual/Playwright check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-generate-capture/11-CONTEXT.md
@.planning/phases/11-generate-capture/11-RESEARCH.md
@.planning/phases/11-generate-capture/11-PATTERNS.md
@.planning/phases/11-generate-capture/11-UI-SPEC.md

# Reuses 11-04's mounted shadow root (getOrCreateShadowRoot(), extracted from
# lib/autofill/inpage-overlay.ts's imperative attachShadow pattern — NOT WXT's createShadowRootUi)
# and lib/i18n/autofill-dictionary.ts's dictionary conventions (X-2 — NOT lib/i18n/dictionary.ts,
# the separate popup dictionary); consumes 11-03's capture.propose/capture.confirm response shapes
# and 11-02's submit-capture success signal.
#
# X-4 (blocked-origins gating): both surfaces here are DOWNSTREAM of 11-02's submit-capture, which
# is already gated behind isConfiguredServerOrigin()/isOriginBlocked() before it ever fires
# capture.propose (11-02's Task 2). Neither the toast nor the mismatch modal needs its OWN
# origin-gating check — suppressing capture upstream already suffices; do not add a redundant gate
# here.
#
# Fixtures: extension/tests/uat-fixtures/ does not exist and this plan does NOT create it. Phase 10
# already built the adversarial two-origin fixture this scenario needs —
# extension/e2e-fixtures/adversarial-iframe/ (top.html at http://127.0.0.1:8791 embedding
# attacker-frame.html at http://localhost:8792 via serve.mjs, documented in that directory's
# README.md). Task 2 below EXTENDS those two existing files, it does not create a new fixture pair.

@extension/lib/autofill/inpage-overlay.ts
@extension/e2e-fixtures/adversarial-iframe/README.md
@extension/e2e-fixtures/adversarial-iframe/top.html
@extension/e2e-fixtures/adversarial-iframe/attacker-frame.html
@web/src/components/vault/CopyToast.tsx
@web/src/components/vault/ErrorToast.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Save-new-login / update-existing-item toast (Surface 2)</name>
  <files>extension/lib/autofill/save-update-toast.ts, extension/lib/i18n/autofill-dictionary.ts</files>
  <read_first>web/src/components/vault/CopyToast.tsx, web/src/components/vault/ErrorToast.tsx (shape reference only — these are React/Tailwind; port the STRUCTURE, not the framework, exactly like inpage-overlay.ts already does for its own surfaces), extension/lib/autofill/inpage-mount.ts (11-04's getOrCreateShadowRoot()), extension/lib/autofill/inpage-overlay.ts (the imperative, framework-free rendering pattern to follow — no React/Tailwind/DaisyUI in the shadow root)</read_first>
  <action>
    Implement the toast as a view rendered into 11-04's already-mounted shadow root (call
    `getOrCreateShadowRoot()` — never a second host/shadow mount), using the same imperative,
    framework-free DOM-building style `inpage-overlay.ts` already uses (no React, no Tailwind/DaisyUI
    classes — inlined styles/CSS only). Given a `capture.propose` response:
    `action:'no-op'` renders nothing at all (no toast, no flash — Pitfall B, resubmitting an unchanged
    password is a non-event); `action:'new'`/`'update'` with `mismatch:false` renders a 360px toast
    (capped `calc(100vw - 48px)`), fixed bottom-right with a 24px margin, that never auto-dismisses except
    a ~1.5s post-confirm success flash. Contents: a `Vault` icon, the `{origin} · {username}` line, a
    masked password preview with an Eye/EyeOff reveal toggle, a confirm button (coral, label "Save" for
    `new` / "Update" for `update`) plus a ghost "Not now", and a top-right `X` dismiss. On confirm click,
    send the FULL field payload fresh from this content script's own closure —
    `sendMessage({kind:'capture.confirm', payload:{action, frameOrigin, username, password, itemId?,
    currentRevision?}})` — never a reference to earlier background state (sidesteps the MV3
    idle-kill-between-propose-and-confirm gap per 11-RESEARCH.md's Open Question 2 recommendation and
    D-02). While the confirm round trip is in flight, show a small inline spinner in the confirm button
    and disable both buttons to prevent a double-submit race. On `{status:'ok'}`, swap to a `CircleCheck`
    success flash for ~1.5s then dismiss automatically (the one exception to "never auto-dismisses" — the
    decision is already resolved at that point). On `{status:'conflict'}`, show the update-conflict copy
    and keep the toast open. On `{status:'error'}`, show the generic save-failed copy with a Retry button
    and keep the toast open. Mount with `role="status"` / `aria-live="polite"` (a non-interrupting
    announcement). Add these dictionary keys verbatim from 11-UI-SPEC.md's Copywriting Contract table
    (PL/EN) to `extension/lib/i18n/autofill-dictionary.ts`'s `AUTOFILL_DICTIONARY` object (X-2 — NOT
    `lib/i18n/dictionary.ts`, the separate popup dictionary): `save.title`, `save.body`, `save.confirm`,
    `save.dismiss`, `save.saved`, `save.failed`, `save.retry`, `update.title`, `update.body`,
    `update.confirm`, `update.dismiss`, `update.updated`, `update.conflict`, plus the toast-dismiss aria
    label.
  </action>
  <verify>
    <automated>cd extension && npx tsc --noEmit</automated>
    <human-check>On a fixture login page (same-origin, no iframe), submit valid new credentials and confirm the save toast appears attributed to the fixture's own origin with the actual submitted username and a masked password preview; click Save and confirm the success flash then auto-dismiss, and that the item appears in the vault (via popup or web app). Resubmit the SAME password and confirm no toast appears at all (Pitfall B). Change the password and resubmit; confirm the UPDATE toast (not a second save prompt) appears.</human-check>
  </verify>
  <done>no-op renders nothing; new/update render the correct toast variant with correct copy; confirm re-sends the full payload; success/error/conflict states match the Copywriting Contract; the toast never silently auto-dismisses except the post-success flash.</done>
</task>

<task type="auto">
  <name>Task 2: Origin-mismatch escalation modal (Surface 3, D-06) + content-relay wiring + adversarial UAT fixture</name>
  <files>extension/lib/autofill/mismatch-modal.ts, extension/lib/i18n/autofill-dictionary.ts, extension/entrypoints/content-relay.content.ts, extension/e2e-fixtures/adversarial-iframe/top.html, extension/e2e-fixtures/adversarial-iframe/attacker-frame.html</files>
  <read_first>web/src/components/ (search for the project's existing sober confirm-dialog pattern — e.g. a delete-confirmation or passkey-enrollment dialog from Phase 2/3 — and read the actual file for its exact scrim/focus-trap/button-order shape; this is a SHAPE REFERENCE only — it's React/Tailwind, this modal is imperative/framework-free like inpage-overlay.ts), extension/lib/autofill/inpage-overlay.ts, extension/entrypoints/content-relay.content.ts, extension/e2e-fixtures/adversarial-iframe/README.md + top.html + attacker-frame.html (the EXISTING Phase 10 adversarial fixture this task extends — top.html already has a `#top-form` login form served from `http://127.0.0.1:8791` and embeds attacker-frame.html's `#frame-form` login form from `http://localhost:8792`; both forms currently only `preventDefault()` on submit with no DOM-removal/URL-change signal, so submit-capture's success heuristic (11-02) will need one added to actually fire against them — check before assuming they work as-is)</read_first>
  <action>
    Implement the 400px mismatch modal, imperative and framework-free (no React/Tailwind/DaisyUI in the
    shadow root — same style as `inpage-overlay.ts` and Task 1's toast): whenever a `capture.propose`
    response has `mismatch:true` (11-03's independently-verified flag), the content-relay routes to THIS
    modal instead of Task 1's toast — the toast path is skipped entirely for a mismatched origin. Shape:
    centered, fixed full-viewport overlay, a scrim behind it, `role="alertdialog"` with focus moved to it
    and trapped on mount, NOT dismissible via a click on the scrim or the Escape key — only the two
    explicit labeled buttons close it. Show BOTH the `frameOrigin` and `topOrigin` values in full, unelided
    plain text (D-06's literal requirement — never a vague "a different site" summary). Contents: an
    `AlertTriangle`-style icon in the error color, a heading, body copy interpolating
    `{frameOrigin}`/`{topOrigin}` via `autofill-dictionary.ts`'s `interpolate()` helper, an error-styled
    banner, a ghost "Cancel" button (visually first/left) and an error-styled "Save anyway" button (right).
    Confirming "Save anyway" re-runs the EXACT SAME `capture.confirm` call Task 1's toast uses — do not add
    a second persistence code path; this applies identically whether the underlying action is `new` or
    `update`. Wire `content-relay.content.ts`: when a `capture.propose` response arrives, branch on
    `response.mismatch` — `true` routes to this modal, `false` routes to Task 1's toast — this is the
    plan's one integration point tying 11-02's submit-capture output through 11-03's classification into
    the correct surface. Neither surface needs its own blocked-origins/own-vault check here (X-4): both
    are downstream of 11-02's submit-capture, which is already gated before it ever sends
    `capture.propose` — do not add a redundant `isConfiguredServerOrigin()`/`isOriginBlocked()` check in
    this file. Add dictionary keys verbatim from 11-UI-SPEC.md's Copywriting Contract table (PL/EN) to
    `extension/lib/i18n/autofill-dictionary.ts`'s `AUTOFILL_DICTIONARY` object (X-2 — NOT
    `lib/i18n/dictionary.ts`): `mismatch.title`, `mismatch.body`, `mismatch.confirm`, `mismatch.cancel`,
    plus the mismatch-warning-icon aria label. EXTEND the existing adversarial two-origin fixture at
    `extension/e2e-fixtures/adversarial-iframe/` (do NOT create a new fixture pair under a
    `tests/uat-fixtures/` directory, which does not exist in this repo) — `top.html` (Origin A,
    `http://127.0.0.1:8791`) already embeds `attacker-frame.html` (Origin B, `http://localhost:8792`) via
    an `<iframe>`, and both already carry a plain login form (`#top-form`/`#frame-form`). If their current
    submit handlers (`e.preventDefault()` plus a canary `input`-event write only) don't yet give
    submit-capture's success heuristic a DOM-removal or URL-change signal to detect, extend them minimally
    (e.g. remove the form or `history.pushState` on submit) so a real submit through
    `attacker-frame.html`'s form actually triggers `capture.propose` from that (cross-origin) frame — this
    reproduces the historical Bitwarden CVE-class scenario PITFALLS.md documents, for manual or
    Playwright-driven verification, reusing the existing fixture's serve.mjs/README rather than a parallel
    fixture.
  </action>
  <verify>
    <automated>test -f extension/e2e-fixtures/adversarial-iframe/top.html && test -f extension/e2e-fixtures/adversarial-iframe/attacker-frame.html</automated>
    <human-check>Load the unpacked extension in a real browser. Run `node extension/e2e-fixtures/adversarial-iframe/serve.mjs 8791 A` and `node extension/e2e-fixtures/adversarial-iframe/serve.mjs 8792 B` per that directory's README, then open `http://127.0.0.1:8791/top.html`. Submit the embedded iframe's (`#frame-form`, Origin B) login form and confirm the MISMATCH MODAL appears (not the plain save toast), showing both the iframe's actual origin (`http://localhost:8792`) and the top page's origin (`http://127.0.0.1:8791`) in full. Confirm the modal cannot be dismissed via Escape or a click outside it, and that "Save anyway" completes the same save as a normal same-origin flow via `#top-form`. This is D-06's required adversarial case and gates ROADMAP Success Criterion 4 for this phase.</human-check>
  </verify>
  <done>The modal renders whenever mismatch:true, showing both origins in full; it is not dismissible except via its two explicit buttons; "Save anyway" persists via the same capture.confirm path as Task 1's toast; content-relay.content.ts correctly branches between the toast and the modal; the existing adversarial-iframe fixture (extended, not duplicated) reproduces the scenario.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cross-origin iframe's MAIN-world page (untrusted, actively adversarial in the historical CVE-class case) <-> ISOLATED content script <-> background | The scenario this plan's headline mitigation exists for: a form embedded in a different origin than the top-level page must never have its captured credential silently attributed to the top-level page. |
| Closed Shadow DOM UI <-> host page | Reused unmodified from Plan 11-04 — the toast and modal render inside the same closed root, page JS cannot reach either. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-11-14 | Spoofing / Information Disclosure | `content-relay.content.ts` propose-response routing | critical | mitigate | This is the phase's headline requirement: whenever 11-03's independently-verified `mismatch:true` flag is set, routing goes to `mismatch-modal.ts` unconditionally — the toast path is never reachable in that case, closing the historical Bitwarden CVE-class "cross-origin iframe silently attributed to the top-level origin" bug (D-06 / ROADMAP Success Criterion 4). |
| T-11-15 | Tampering | `mismatch-modal.ts` dismissal behavior | high | mitigate | The modal is not dismissible via a scrim click or Escape — only the two explicit labeled buttons ("Cancel" / "Save anyway") close it, with focus trapped on mount (`role="alertdialog"`), so a user cannot accidentally proceed past the warning without deliberately reading and choosing. |
| T-11-16 | Repudiation / Information Disclosure | Save/update toast's password-reveal control | low | accept | A page-level clickjacking overlay could theoretically attempt to trick a user into revealing/confirming a save. Accepted: the closed shadow root's own stacking context means a page cannot dispatch a trusted synthetic click into elements it has no reference to; broader host-page clickjacking is a generic risk affecting every browser extension equally and is out of this phase's scope to close. |
| T-11-19 | Information Disclosure | Toast/modal surfacing on the user's own vault app / a blocked origin | low | accept | Neither surface has its own `isConfiguredServerOrigin()`/`isOriginBlocked()` check (X-4): both are strictly downstream of `capture.propose`, which 11-02's submit-capture never sends in the first place once its own gate suppresses activation on those origins — no capture.propose message means no toast/modal opportunity, so a second check here would be dead code. |

</threat_model>

<verification>
- [ ] `cd extension && npx tsc --noEmit` passes
- [ ] Both extended fixture files exist under `extension/e2e-fixtures/adversarial-iframe/`
- [ ] Manual UAT: same-origin save/update/no-op flow verified (Task 1)
- [ ] Manual UAT: cross-origin-iframe mismatch scenario verified against the extended adversarial-iframe fixture (Task 2, D-06) — deferred to end-of-phase UAT per `human_verify_mode: end-of-phase`
</verification>

<success_criteria>
- Both tasks completed
- CAP-02/CAP-03 are fully user-reachable: save, update, and no-op all behave per the Copywriting Contract
- The origin-mismatch modal gates 100% of save/update flows where frameOrigin !== topOrigin, verified against a real adversarial fixture, not just unit-level logic
</success_criteria>

<output>
Create `.planning/phases/11-generate-capture/11-05-SUMMARY.md` when done.
</output>
