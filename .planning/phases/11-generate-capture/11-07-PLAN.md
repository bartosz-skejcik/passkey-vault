---
phase: 11-generate-capture
plan: 07
type: execute
wave: 5
depends_on: ["11-04", "11-05", "11-06"]
files_modified:
  - extension/lib/theme/theme-mirror.ts
  - extension/lib/theme/theme-mirror.test.ts
  - extension/lib/autofill/inpage-theme.ts
  - extension/lib/autofill/inpage-theme.test.ts
  - extension/lib/autofill/inpage-mount.ts
  - extension/lib/autofill/inpage-overlay.ts
  - extension/lib/autofill/generate-popover.ts
  - extension/lib/autofill/generate-popover.test.ts
  - extension/lib/autofill/save-update-toast.ts
  - extension/lib/autofill/mismatch-modal.ts
  - extension/entrypoints/content-relay.content.ts
  - extension/entrypoints/popup/index.html
  - extension/entrypoints/popup/main.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every extension surface (generate popover, save/update toast, mismatch modal, phase-10 overlay prompt window, in-field dropdown, field icon, popup) renders with the web frontend's vault-light/vault-dark OKLCH tokens — no hand-rolled color values left in surface CSS (D-12, Bartek 2026-07-16: 'ma być taki sam jak frontend')."
    - "The active theme is MIRRORED from the web app: when the user visits the configured server origin, the content script reads html[data-theme] (on load and on change) and persists it to chrome.storage.local; all surfaces and the popup resolve theme as mirror → prefers-color-scheme → vault-dark."
    - "The in-page generate popover's layout, controls, and labels mirror web/src/components/generator/GeneratorPopover.tsx one-to-one (same control order, same copy keys, same visual hierarchy) — a user who knows the web generator recognizes the in-page one instantly."
    - "Security dialogs (mismatch modal) stay legible and serious in BOTH themes — the light variant must not soften the warning styling (project constraint: playfulness nigdy w dialogach bezpieczeństwa)."
  artifacts:
    - path: "extension/lib/theme/theme-mirror.ts"
      provides: "readMirroredTheme()/watchMirroredTheme() + captureThemeFromWebApp() (server-origin content-script side) + resolveTheme() fallback chain"
    - path: "extension/lib/autofill/inpage-theme.ts"
      provides: "vault-light/vault-dark OKLCH custom-property CSS (ported verbatim from web/src/app/globals.css) scoped per data-pv-theme attribute, consumed by every in-page surface stylesheet"
  key_links:
    - from: "extension/entrypoints/content-relay.content.ts"
      to: "extension/lib/theme/theme-mirror.ts"
      via: "on configured-server-origin pages: captureThemeFromWebApp() reads html[data-theme] + MutationObserver, writes chrome.storage.local"
      pattern: "captureThemeFromWebApp"
    - from: "extension/lib/autofill/inpage-mount.ts"
      to: "extension/lib/theme/theme-mirror.ts"
      via: "mount resolves theme and stamps data-pv-theme on the shadow panel root; storage onChanged re-stamps live"
      pattern: "resolveTheme|data-pv-theme"
    - from: "extension/entrypoints/popup/main.tsx"
      to: "extension/lib/theme/theme-mirror.ts"
      via: "popup sets document.body data-theme from resolveTheme() and live-updates on storage change"
      pattern: "resolveTheme"
---

<objective>
Deliver D-12 (11-CONTEXT.md addendum): visual and theme parity between every extension surface and the web frontend. Bartek reviewed the phase-11 UAT screenshots and rejected the hand-rolled dark styling — his frontend runs vault-light while the popup, toasts, and generator are hard-coded dark, and the in-page generator looks nothing like the web GeneratorPopover he explicitly asked to be replicated.

Purpose: one design system across web + extension. Theme is mirrored from the user's own web app (the product's source of truth for appearance), not guessed.
Output: shared token stylesheet + theme mirror module; all in-page surfaces and the popup restyled and theme-following; generator popover visually 1:1 with the web generator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-generate-capture/11-CONTEXT.md   # D-12
@web/src/app/globals.css                              # SOURCE OF TRUTH: vault-dark/vault-light OKLCH tokens
@web/src/components/generator/GeneratorPopover.tsx    # SOURCE OF TRUTH: generator layout/controls/copy
@extension/entrypoints/popup/style.css                # popup already carries BOTH themes ported — only the hardcoded body attribute blocks switching
@extension/lib/autofill/inpage-mount.ts
@extension/lib/autofill/inpage-overlay.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Theme mirror module + content-relay capture + popup un-hardcoding</name>
  <files>extension/lib/theme/theme-mirror.ts, extension/lib/theme/theme-mirror.test.ts, extension/entrypoints/content-relay.content.ts, extension/entrypoints/popup/index.html, extension/entrypoints/popup/main.tsx</files>
  <read_first>web/src/app/layout.tsx (themeInitScript — the web's own resolution order), extension/entrypoints/background/server-config.ts (how the configured origin is exposed; content-relay already has isConfiguredServerOrigin), extension/entrypoints/popup/style.css</read_first>
  <behavior>
    - lib/theme/theme-mirror.ts exports: `captureThemeFromWebApp(doc)` (reads html[data-theme], validates against the vault-light/vault-dark enum, writes chrome.storage.local key `pv-theme-mirror`, attaches a MutationObserver on the attribute so a toggle in the web app updates the mirror live); `resolveTheme()` (async: mirror → matchMedia('(prefers-color-scheme: light)') → 'vault-dark'); `watchMirroredTheme(cb)` (chrome.storage.onChanged subscription, returns detach).
    - content-relay: ONLY on pages where isConfiguredServerOrigin() is true, call captureThemeFromWebApp(document). (On the vault app the overlay is suppressed — this is the one thing the content script still does there.)
    - Popup: index.html keeps a neutral default; main.tsx sets document.body's data-theme from resolveTheme() before first render (top-level await or pre-render call) and re-stamps on watchMirroredTheme. A sub-100ms initial-flash from the async storage read is acceptable; minimize by resolving before ReactDOM render.
    - Invalid/malicious storage values (anything not in the enum) resolve to the fallback chain — the mirror is data, not code.
  </behavior>
  <action>
    TDD the module (jsdom: fake chrome.storage, fake matchMedia). Wire content-relay capture behind the existing
    isConfiguredServerOrigin() gate. Un-hardcode the popup. Verify popup theme flips live when the mirror value
    changes (test via watchMirroredTheme callback).
  </action>
  <verify>
    <automated>cd extension && npx vitest run lib/theme entrypoints/__tests__/content-relay && npx tsc --noEmit</automated>
  </verify>
  <done>Theme mirror captures from the web app, resolves with the documented fallback chain, popup follows it live.</done>
</task>

<task type="auto">
  <name>Task 2: Shared token stylesheet + restyle ALL in-page surfaces to frontend parity</name>
  <files>extension/lib/autofill/inpage-theme.ts, extension/lib/autofill/inpage-theme.test.ts, extension/lib/autofill/inpage-mount.ts, extension/lib/autofill/inpage-overlay.ts, extension/lib/autofill/generate-popover.ts, extension/lib/autofill/generate-popover.test.ts, extension/lib/autofill/save-update-toast.ts, extension/lib/autofill/mismatch-modal.ts</files>
  <read_first>web/src/app/globals.css (BOTH theme blocks — port token values verbatim), web/src/components/generator/GeneratorPopover.tsx (layout to replicate), .planning/phases/11-generate-capture/11-UI-SPEC.md</read_first>
  <behavior>
    - inpage-theme.ts exports a CSS string defining the vault-dark and vault-light custom-property sets (base-100/200/300, base-content, primary, secondary, accent, error, warning, radii, borders — copied VERBATIM from web/src/app/globals.css) scoped as `[data-pv-theme="vault-dark"]{...}` / `[data-pv-theme="vault-light"]{...}`, plus the shared font stack ("DM Sans", system-ui fallback — unchanged convention).
    - inpage-mount.ts: getOrCreateShadowRoot stamps `data-pv-theme` on the mount's panel container from resolveTheme() at mount time and re-stamps on watchMirroredTheme — every surface inside inherits the right tokens automatically.
    - EVERY surface stylesheet (inpage-overlay.ts prompt window + in-field dropdown + field icon; generate-popover.ts; save-update-toast.ts; mismatch-modal.ts) replaces hard-coded colors with var(--...) references to the shared tokens. No literal oklch()/hex color remains in surface CSS except inside inpage-theme.ts itself.
    - generate-popover.ts LAYOUT is rebuilt to mirror web GeneratorPopover.tsx one-to-one: same control order (mode tabs, preview row with regenerate + reveal, length slider/input with value, charset checkboxes / word-count + separator, strength meter if the web one has it, apply button styling and placement), same i18n copy keys (reuse web dictionary strings via autofill-dictionary — port any missing keys with pl+en), same spacing/radius scale. Imperative DOM (no React) stays — parity is visual, not framework.
    - Mismatch modal: light variant keeps the warning banner high-contrast (error tokens), full-origin monospace lines legible — verify against the project's "security UI always serious" constraint.
    - All existing behavior tests stay green — this task must not change any behavioral contract (message kinds, callbacks, gating), only presentation + theme stamping.
  </behavior>
  <action>
    Port tokens first (inpage-theme.ts + test asserting both theme blocks parse and expose the same variable set).
    Then convert surfaces one by one, running each surface's test file after conversion. Update inpage-mount to
    stamp/re-stamp the theme attribute. Add a test: mount with mirror=vault-light → panel has data-pv-theme="vault-light";
    storage change to vault-dark → attribute re-stamped.
    Screenshot verification (light AND dark, all surfaces) happens in the orchestrator's packaged-build UAT pass — note in SUMMARY that it is required before Bartek review.
  </action>
  <verify>
    <automated>cd extension && npx vitest run lib/autofill && npx tsc --noEmit && npx wxt build 2>&1 | tail -2</automated>
  </verify>
  <done>All in-page surfaces and the popup share the frontend's token system, follow the mirrored theme, and the generator popover is visually 1:1 with the web GeneratorPopover.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-11-30 | Tampering | `pv-theme-mirror` storage value | low | mitigate | Enum-validate on read AND write; any unexpected value falls through to prefers-color-scheme → vault-dark. The mirror never carries markup/CSS, only one of two constant strings. |
| T-11-31 | Spoofing | hostile page mimicking the server origin | low | accept | Capture is gated on isConfiguredServerOrigin() (exact origin match, same gate the overlay suppression trusts). Worst case a spoofed page could flip light/dark — cosmetic only. |

</threat_model>

<verification>
- [ ] `cd extension && npx vitest run lib/theme lib/autofill` passes
- [ ] `cd extension && npx tsc --noEmit` passes
- [ ] `cd extension && npx wxt build && npx wxt build -b firefox` both pass
- [ ] Packaged-build UAT screenshots: generator popover, toast, modal, overlay prompt, dropdown, popup — each in vault-light AND vault-dark
</verification>

<success_criteria>
- Both tasks completed
- With the web app set to vault-light, every extension surface renders light with the frontend's exact tokens; toggling the web app to vault-dark flips the extension surfaces without reloads
- Generator popover indistinguishable in layout from the web GeneratorPopover
</success_criteria>

<output>
Create `.planning/phases/11-generate-capture/11-07-SUMMARY.md` when done.
</output>
