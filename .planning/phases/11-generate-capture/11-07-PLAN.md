---
phase: 11-generate-capture
plan: 07
type: execute
wave: 5
depends_on: ["11-01", "11-04", "11-05", "11-06"]
files_modified:
  - package.json
  - packages/pv-ui/package.json
  - packages/pv-ui/tokens.css
  - packages/pv-ui/generator/password.ts
  - packages/pv-ui/generator/password.test.ts
  - packages/pv-ui/generator/strength.ts
  - packages/pv-ui/generator/wordlist.ts
  - web/package.json
  - web/next.config.ts
  - web/src/app/globals.css
  - web/src/lib/generator/password.ts
  - web/src/lib/generator/strength.ts
  - web/src/lib/generator/wordlist.ts
  - extension/package.json
  - extension/lib/generator/password.ts
  - extension/lib/generator/strength.ts
  - extension/lib/generator/wordlist.ts
  - extension/entrypoints/popup/style.css
  - extension/lib/theme/theme-mirror.ts
  - extension/lib/theme/theme-mirror.test.ts
  - extension/entrypoints/content-relay.content.ts
  - extension/entrypoints/popup/index.html
  - extension/entrypoints/popup/main.tsx
  - Dockerfile
autonomous: true
requirements: []

must_haves:
  truths:
    - "packages/pv-ui is the single source of truth for the vault-dark/vault-light token set and the generator logic — web and extension both consume it; no byte-for-byte copies of either remain (D-13, Bartek 2026-07-16: 'oddzielny package z którego korzysta i extension i frontend')."
    - "The web app's visual output is UNCHANGED in both themes after the extraction — this plan moves sources, it does not redesign anything web-side."
    - "The active theme is MIRRORED from the web app: content script on the configured server origin reads html[data-theme] (load + MutationObserver), persists to chrome.storage.local; popup and (in 11-08) in-page surfaces resolve mirror → prefers-color-scheme → vault-dark (D-12)."
    - "The Docker single-container build still succeeds — the Dockerfile's web build stage copies packages/ (phase-7 deployment contract must not silently break)."
  artifacts:
    - path: "packages/pv-ui/tokens.css"
      provides: "vault-dark + vault-light theme blocks (OKLCH custom properties + color-scheme), consumable by Tailwind/DaisyUI (web, popup) and as raw CSS text (in-page shadow)"
    - path: "packages/pv-ui/generator/password.ts"
      provides: "the ONE generateCharacterPassword/generatePassphrase implementation (moved from web/src/lib/generator)"
    - path: "extension/lib/theme/theme-mirror.ts"
      provides: "captureThemeFromWebApp()/resolveTheme()/watchMirroredTheme() — D-12 mirror chain"
  key_links:
    - from: "web/src/app/globals.css"
      to: "packages/pv-ui/tokens.css"
      via: "@import of the shared token file replacing the inline theme blocks"
      pattern: "pv-ui"
    - from: "extension/entrypoints/background/generate-handler.ts"
      to: "packages/pv-ui/generator/password.ts"
      via: "extension/lib/generator/* become re-exports (or imports rewired) to the package"
      pattern: "pv-ui"
    - from: "extension/entrypoints/content-relay.content.ts"
      to: "extension/lib/theme/theme-mirror.ts"
      via: "captureThemeFromWebApp() on isConfiguredServerOrigin() pages"
      pattern: "captureThemeFromWebApp"
---

<objective>
Deliver D-13 + the mirror half of D-12: extract the shared design tokens and generator logic out of the frontend into `packages/pv-ui` (npm workspace), rewire web and extension to consume it, and build the theme-mirror pipeline so the extension knows which theme the user's web app runs.

Purpose: one source of truth ends the token/generator drift (popup's style.css and extension's lib/generator are hand-synced copies today); the mirror makes "extension looks like MY frontend" mechanically true.
Output: workspace package consumed by both apps; theme mirror + popup un-hardcoded; all builds (web, extension both browsers, Docker) green. In-page surface restyling is plan 11-08.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-generate-capture/11-CONTEXT.md   # D-12 + D-13
@web/src/app/globals.css                              # theme blocks to extract
@web/src/app/layout.tsx                               # themeInitScript — web's own resolution order (mirror must match)
@extension/entrypoints/popup/style.css                # hand-synced token copy to replace
@web/src/lib/generator/password.ts                    # generator to move
@Dockerfile                                           # phase-7 web build stage — must COPY packages/

# There is no root package.json / npm workspace today — web/ and extension/ are independent
# npm projects. This plan introduces the workspace. Verify how each build resolves the new
# package (Next 15: transpilePackages; WXT/vite: plain TS import through the workspace symlink)
# with REAL builds, not assumptions. If root workspaces prove disruptive to either build,
# fall back to "file:../packages/pv-ui" deps in web/package.json + extension/package.json —
# outcome (single source) matters, mechanism is executor's discretion. Record the choice in SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: packages/pv-ui — tokens + generator extracted, web and extension rewired</name>
  <files>package.json, packages/pv-ui/package.json, packages/pv-ui/tokens.css, packages/pv-ui/generator/password.ts, packages/pv-ui/generator/password.test.ts, packages/pv-ui/generator/strength.ts, packages/pv-ui/generator/wordlist.ts, web/package.json, web/next.config.ts, web/src/app/globals.css, web/src/lib/generator/password.ts, web/src/lib/generator/strength.ts, web/src/lib/generator/wordlist.ts, extension/package.json, extension/lib/generator/password.ts, extension/lib/generator/strength.ts, extension/lib/generator/wordlist.ts, extension/entrypoints/popup/style.css, Dockerfile</files>
  <read_first>web/src/app/globals.css, extension/entrypoints/popup/style.css, web/src/components/generator/GeneratorPopover.tsx (imports to rewire), extension/entrypoints/background/generate-handler.ts (imports), Dockerfile, web/next.config.ts</read_first>
  <behavior>
    - packages/pv-ui ships: tokens.css (BOTH theme blocks moved from globals.css — DaisyUI 5 accepts raw `[data-theme=...]{--color-*: ...}` custom-property themes; keep color-scheme and every token DaisyUI generated before; verify visually via build) and generator/ (password/strength/wordlist moved from web/src/lib/generator, tests alongside).
    - web/src/app/globals.css imports the package tokens instead of its inline theme blocks; web visual output identical in both themes (build + spot-check pages).
    - web/src/lib/generator/* and extension/lib/generator/* become thin re-export shims (`export * from "pv-ui/generator/password"`) so every existing import path and test keeps working with ZERO churn in consumers — or imports are rewired and the local files deleted; pick ONE approach and apply consistently (shims are lower-risk; note the choice in SUMMARY). Test files stay green either way.
    - extension/entrypoints/popup/style.css consumes the package tokens instead of its hand-synced copy (its header comment about manual sync is deleted — that debt is what this plan kills).
    - Dockerfile web build stage COPYs packages/ before building web; `docker build` must still be buildable in principle (verify stage ordering by reading; actual docker run happens in UAT env if available).
    - Root workspace (or file: deps) wired; `npm install` + full test suites + builds green in BOTH projects.
  </behavior>
  <action>
    Extract, rewire, run everything: web (npx vitest run + npm run build with NEXT_PUBLIC_API_BASE_URL="") and
    extension (npx vitest run + npx tsc --noEmit + npx wxt build + npx wxt build -b firefox). The web build output
    web/out is REQUIRED fresh at the end (the running pv-server serves it — standing rule: every web change needs
    rebuild + server restart, orchestrator handles the restart).
  </action>
  <verify>
    <automated>cd web && npx vitest run 2>&1 | tail -2 && NEXT_PUBLIC_API_BASE_URL="" npm run build 2>&1 | tail -3 && cd ../extension && npx vitest run 2>&1 | tail -2 && npx tsc --noEmit && npx wxt build 2>&1 | tail -2</automated>
  </verify>
  <done>One token source, one generator source, both apps build green, Dockerfile carries packages/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Theme mirror (D-12) + popup un-hardcoding</name>
  <files>extension/lib/theme/theme-mirror.ts, extension/lib/theme/theme-mirror.test.ts, extension/entrypoints/content-relay.content.ts, extension/entrypoints/popup/index.html, extension/entrypoints/popup/main.tsx</files>
  <read_first>web/src/app/layout.tsx (themeInitScript), extension/entrypoints/content-relay.content.ts (isConfiguredServerOrigin gate)</read_first>
  <behavior>
    - lib/theme/theme-mirror.ts: captureThemeFromWebApp(doc) — reads html[data-theme], enum-validates (vault-light|vault-dark), writes chrome.storage.local key `pv-theme-mirror`, MutationObserver keeps it live on toggle; resolveTheme() — mirror → matchMedia('(prefers-color-scheme: light)') → 'vault-dark' (same order as the web's own themeInitScript); watchMirroredTheme(cb) — chrome.storage.onChanged subscription with detach.
    - content-relay: on isConfiguredServerOrigin() pages ONLY, call captureThemeFromWebApp(document) (the overlay stays suppressed there — capture is the one job the content script keeps on the vault app).
    - Popup: index.html loses the hardcoded data-theme; main.tsx resolves theme BEFORE first ReactDOM render and re-stamps on watchMirroredTheme. Invalid storage values fall through the chain (mirror is data, not code).
  </behavior>
  <action>
    TDD in jsdom with faked chrome.storage + matchMedia. Pin: enum validation, fallback order, observer updates
    mirror on attribute flip, popup re-stamps live.
  </action>
  <verify>
    <automated>cd extension && npx vitest run lib/theme entrypoints/__tests__/content-relay && npx tsc --noEmit</automated>
  </verify>
  <done>Popup follows the web app's actual theme after one visit to the vault; falls back to system preference before that.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-11-30 | Tampering | `pv-theme-mirror` storage value | low | mitigate | Enum-validated on read and write; anything else falls through to prefers-color-scheme → vault-dark. Carries one of two constant strings, never markup/CSS. |
| T-11-31 | Spoofing | hostile page posing as server origin | low | accept | Capture gated on isConfiguredServerOrigin() (exact-origin, same gate overlay suppression trusts). Worst case: cosmetic light/dark flip. |
| T-11-32 | Tampering | supply chain — new workspace package | low | mitigate | packages/pv-ui is FIRST-PARTY code moved within this repo; no new external dependencies are introduced by this plan. |

</threat_model>

<verification>
- [ ] web: vitest green, `NEXT_PUBLIC_API_BASE_URL="" npm run build` green, visual spot-check both themes unchanged
- [ ] extension: vitest green, tsc green, both browser builds green
- [ ] no remaining copy of theme tokens or generator logic outside packages/pv-ui (grep proves it)
- [ ] Dockerfile web stage copies packages/
</verification>

<success_criteria>
- Both tasks complete; single-source tokens + generator; theme mirror live; popup theme follows the frontend
</success_criteria>

<output>
Create `.planning/phases/11-generate-capture/11-07-SUMMARY.md` when done.
</output>
