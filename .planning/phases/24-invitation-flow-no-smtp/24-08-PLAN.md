---
phase: 24-invitation-flow-no-smtp
plan: 08
type: execute
wave: 5
depends_on: ["24-06", "24-07"]
files_modified:
  - web/e2e/invite-flow.spec.ts
autonomous: true
requirements: [FAM-05, FAM-06]

must_haves:
  truths:
    - "A real owner browser session can create a family-only invite through the actual Settings UI and a completely separate, previously-unauthenticated browser context can register inline through the actual invite landing page and land in the vault as a family member — proven through real UI interaction, not API calls."
    - "A real, already-logged-in second browser session can redeem the same class of invite link directly (no registration step) and land in the vault."
    - "The 'join as a different account' escape genuinely clears the visiting session and re-renders the register/login branch, observed live in a real browser, without a page reload interrupting the flow."
    - "An already-a-member browser session redeeming a second, different invite link lands in the vault without an error screen."
    - "A fabricated/unknown invite id renders the unified failure message in a real browser, with no family name or inviter text anywhere on the page."
  artifacts:
    - web/e2e/invite-flow.spec.ts
  key_links:
    - "This spec proves the FUNCTIONAL two-session flows live; it does NOT re-attempt SC 4's concurrency proof at the browser level — that proof already exists, authoritatively, as a genuinely concurrent Rust integration test in Plan 24-04, matching this project's own Phase 23 precedent for not duplicating a hard-to-reproduce race at the browser layer once a stronger proof exists underneath it."
---

<objective>
Prove the invitee-facing flows work end-to-end in a real, live, two-browser-context scenario — the
standing multi-session Playwright harness Phase 23 built specifically so Phases 24-27 would not have
to stand up their own.

Purpose: closes the loop between "the server is correct" (Plans 24-01/02/04) and "the UI is correct in
isolation" (Plans 24-06/07) with a real end-to-end proof.
Output: `web/e2e/invite-flow.spec.ts`.
</objective>

## Artifacts this phase produces

- `web/e2e/invite-flow.spec.ts` (new) — brand-new-invitee join, existing-account join, wrong-account
  escape, already-a-member no-op, invalid-link scenarios.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-invitation-flow-no-smtp/24-CONTEXT.md
@web/e2e/fixtures.ts
@web/e2e/shared-sync.spec.ts
@web/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand-new-invitee join + existing-account join + invalid-link scenarios</name>
  <files>web/e2e/invite-flow.spec.ts</files>
  <read_first>
    web/e2e/fixtures.ts (the `twoSessions` fixture and its `createSession` helper — both sessions it
    produces are ALREADY registered; the brand-new-invitee scenario needs a THIRD, still-unauthenticated
    context via the plain Playwright `browser` fixture directly, not `twoSessions`, since `twoSessions`
    always pre-registers both accounts before a test body ever runs); web/e2e/shared-sync.spec.ts (the
    established two-session-against-one-app usage idiom, dialog-guard rigor); Phase 20's standing
    "zero OS-level dialogs in automation" rule (memory: no-interactive-prompts-in-automation) — this
    spec must not trigger any `window.confirm`/`alert`.
  </read_first>
  <behavior>
    - Owner (one authenticated session) navigates Settings → Family tab, creates a family (if the test
      account doesn't already have one), generates a family-only invite, and reads the full link text
      out of the read-only link input's value.
    - A brand-new, separate, previously-unauthenticated browser context navigates directly to that
      link, registers inline, and ends up in the normal vault shell (proving `onDone`'s transition
      actually fires in a real browser).
    - A second, already-registered-and-logged-in session (via `twoSessions`) navigates to a second,
      freshly-generated invite link and joins directly via the Join button, landing in the vault with
      no registration step shown.
    - Navigating to `/invite/definitely-not-a-real-id` (no `#` fragment even needed for this case)
      renders the unified failure message and NO family/inviter text anywhere on the page.
  </behavior>
  <action>
    Create `web/e2e/invite-flow.spec.ts` importing `test`/`expect` from `./fixtures`. Write
    `owner_creates_invite_and_brand_new_user_joins_inline`: use `twoSessions` for the owner session
    only (ignore the second); through the owner's `page`, open Settings, click the Family tab, if the
    bootstrap form is showing fill in a family name and submit, then select whole-family scope, leave
    expiry at its 7-day default, click Generate, and read the link input's `value` attribute (Playwright
    `.inputValue()`). Separately, `const freshContext = await browser.newContext(); const freshPage =
    await freshContext.newPage();` (mirroring `createSession`'s own dialog-guard setup — add the same
    `beforeunload`/`dialog` guard this fixture applies, and the same `pv-locale=en`/
    `pv-onboarding-complete=true` `addInitScript` calls, so this ad hoc context behaves identically to
    a `twoSessions`-produced one). Navigate `freshPage` to the copied link, fill the inline
    `RegisterForm`'s email/password/confirm fields (unique email per test run), submit, and assert the
    page transitions to the normal vault shell (e.g. wait for a vault-shell-only test id/selector to
    appear — check `web/e2e/shared-sync.spec.ts` for the exact selector this codebase's other specs
    already use to detect "vault is showing"). Then, through the OWNER's page, navigate to the Family
    roster view (or re-open the invite-creation flow, whichever surfaces membership count in this
    phase's actual UI) and assert the member count increased by one.

    Write `existing_logged_in_session_joins_directly_no_registration_shown`: owner generates a SECOND
    fresh invite link; navigate `twoSessions`'s second (already-authenticated) session's `page` to it;
    assert the "signed in as {email}" notice shows THAT session's own email, click the Join button
    (waiting for it to become enabled if the vault needs an explicit unlock step first — reuse whatever
    unlock affordance/selector this codebase's other specs already target for `UnlockOverlay`), and
    assert the transition to the vault shell with no `RegisterForm`/`LoginForm` ever rendered during
    this scenario.

    Write `unknown_invite_id_renders_unified_failure_with_no_leaked_context`: navigate any fresh page
    to `/invite/this-id-does-not-exist#anything`, assert the rendered text contains the unified failure
    copy and does NOT contain any family name or email string from either test fixture used elsewhere
    in this file.
  </action>
  <verify>
    <automated>npx playwright test web/e2e/invite-flow.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    All three scenarios pass headed and headless, with zero `dialogFired()` triggers across every
    session used. Re-run the file 2 more times to confirm no flake (3 consecutive green runs total).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wrong-account escape + already-a-member scenario + CI glob confirmation</name>
  <files>web/e2e/invite-flow.spec.ts</files>
  <read_first>
    web/e2e/invite-flow.spec.ts (Task 1's own file, extended here); the repo's CI workflow file for the
    `web-e2e` job Phase 23 wired (grep for `playwright test` in `.github/workflows/`) — confirm its
    glob/command already picks up any `web/e2e/*.spec.ts` file with no per-file allowlist edit needed;
    if it DOES enumerate spec files explicitly, add this one.
  </read_first>
  <behavior>
    - Clicking "join as a different account" on the logged-in branch genuinely clears the session
      (subsequent `getSessionToken()`-dependent UI shows the register/login branch) without a full
      page navigation.
    - A session that is already a family member, redeeming a DIFFERENT invite, ends up in the vault
      with no error state ever shown.
    - The CI workflow runs this new spec file without additional configuration.
  </behavior>
  <action>
    Extend `web/e2e/invite-flow.spec.ts` with `join_as_different_account_clears_session_and_shows_register_branch`:
    reuse `twoSessions`'s second session, navigate to a fresh invite link, click "join as a different
    account", assert the view now shows the `RegisterForm`/`LoginForm` toggle (not the current-account
    notice) and that no browser navigation occurred (e.g. assert `page.url()` still carries the same
    `/invite/{id}#...` — a full reload would have re-resolved a fresh URL object but the path/hash
    should be unchanged).

    Add `already_a_member_redeeming_a_different_invite_lands_in_vault_without_error`: reuse the session
    that joined in Task 1's first scenario (already a member from that test, if test ordering/isolation
    allows reuse — otherwise add a small setup step within this test that makes a session a member
    first via a second invite redemption), have the owner generate a THIRD invite link, have that
    already-a-member session redeem it, and assert it lands in the vault with no error toast/dialog.

    Verify (do not necessarily need to EDIT) the CI workflow: confirm the existing `web-e2e` job's
    Playwright invocation is a glob (`playwright test` with no explicit file list) so this new spec
    file is picked up automatically; if the job DOES enumerate `.spec.ts` files by name, add this one
    to that list.
  </action>
  <verify>
    <automated>npx playwright test web/e2e/invite-flow.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    Both new scenarios pass, 3 consecutive green runs. `npx playwright test` (the FULL suite, every
    spec file) stays green — no regression to `shared-sync.spec.ts` or any other existing spec sharing
    the `twoSessions` fixture. If the CI workflow needed an explicit addition, it is committed as part
    of this task; otherwise this is confirmed and left unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness ↔ real browser automation | this spec is the only place Phase 24's guarantees are checked against genuine browser behavior (real DOM, real fetch, real WASM) rather than a mocked/unit-tested approximation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-20 | Repudiation (test-integrity) | web/e2e/invite-flow.spec.ts | medium | mitigate | every scenario is re-run 3 consecutive times during verification per this project's own flake-intolerance standard (13-03-SUMMARY.md precedent); zero `dialogFired()` triggers required across all sessions |
| T-24-SC | Tampering (supply chain) | web/e2e/invite-flow.spec.ts | n/a | accept | no new npm package — reuses the existing `@playwright/test` dependency and `fixtures.ts` |

</threat_model>

<verification>
```
npx playwright test web/e2e/invite-flow.spec.ts --project=chromium
npx playwright test
```
Run the new spec file 3 times in a row before considering this plan done.
</verification>

<success_criteria>
Every functionally-reachable invite flow (brand-new join, existing-account join, wrong-account escape,
already-a-member no-op, invalid link) is proven live in a real browser against the real server, closing
the loop this phase's server and UI plans opened separately.
</success_criteria>

<output>
Create `.planning/phases/24-invitation-flow-no-smtp/24-08-SUMMARY.md` when done, with `## Threat Flags`
populated.
</output>
