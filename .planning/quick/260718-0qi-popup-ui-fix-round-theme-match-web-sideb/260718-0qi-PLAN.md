---
phase: quick-260718-0qi
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/entrypoints/popup/ItemListView.tsx
  - extension/entrypoints/popup/ItemListView.test.tsx
  - extension/entrypoints/popup/style.css
  - extension/e2e/dual-browser.spec.ts
autonomous: true
requirements: [EXT-04, EXT-06]
must_haves:
  truths:
    - "The popup's top bar (title + search) and content card use the EXACT same background tokens as the web app's Sidebar/main content, in both vault-dark and vault-light."
    - "A floating '+' FAB sits bottom-right of the popup, independent of the footer; the footer holds only a comfortably-sized gear (left) and the auto-lock select (right)."
    - "The 'Full screen' pill renders in the top bar next to the title, not in the footer."
    - "Hovering an item row shows no drop-shadow, and the adjacent row separator does not visibly change on hover."
    - "The 'All items'/'Wszystkie' label and the sort select render on one line, sized consistent with the rest of the popup UI, with no overlap, in both locales."
    - "extension/e2e/dual-browser.spec.ts's Phase 9 group (P9-SC1..SC7) passes green against the corrected popup DOM."
  artifacts:
    - extension/entrypoints/popup/ItemListView.tsx (theme, FAB restore, footer/top-bar restructure, row-adjacent markup, label/sort sizing)
    - extension/entrypoints/popup/ItemListView.test.tsx (updated/added assertions for the corrected layout)
    - extension/entrypoints/popup/style.css (.pv-row-hover shadow removal)
    - extension/e2e/dual-browser.spec.ts (disambiguated Phase 9 selectors + any diagnosed drift fixes)
  key_links:
    - "ItemListView.tsx's top-bar bg-base-200/text-base-content <-> packages/pv-ui/tokens.css's --color-base-200/--color-base-content tokens <-> web/src/components/shell/Sidebar.tsx's own bg-base-200 wrapper and web/src/components/shell/TopBar.tsx's own bg-base-200 header (read-only parity reference)"
    - "ItemListView.tsx's restored floating '+' FAB <-> unchanged handleNewItemType()/openInNewTab() flow <-> the web app's /?action=new-item&type=<id> route (behavior untouched, only position/size changed)"
    - "dual-browser.spec.ts's disambiguated selectors <-> ItemListView.tsx's existing #pv-autolock id and data-testid=popup-sort-select/popup-footer/popup-fab attributes"
---

<objective>
Popup UI fix round: five independent, Bartek-decided (FINAL, live-tested) corrections to yesterday's popup sheet-look restyle, plus a root-caused repair of the Phase 9 Chromium e2e suite the restyle broke.

1. **Theme parity**: the top bar (title + search) currently uses `bg-neutral`/`text-primary-content` (too light) instead of matching the web app's Sidebar/TopBar `bg-base-200` token -- fix so the popup's dark strip is pixel-identical (same OKLCH token) to the web sidebar in both themes.
2. **FAB + footer + top bar restructure**: restore the "+" new-item control as a floating bottom-right FAB (it was wrongly folded into the footer); move the "Full screen" pill out of the footer into the top bar beside the title; bump the footer's remaining gear icon to a comfortable, FAB-consistent size.
3. **Row hover fix**: remove the drop-shadow from `.pv-row-hover:hover` (currently makes the adjacent divide-y separator look "lit up"); keep the existing inset-ring/press pattern.
4. **Label/sort sizing**: the "All items"/"Wszystkie" heading + sort select read too small and the heading wraps to two lines -- bump both to match the rest of the popup's body-text scale, on one line, no overlap.
5. **E2E repair**: root-cause and fix the 5 currently-failing Phase 9 Chromium e2e tests (P9-SC2/SC3/SC4/SC5/SC7) against the corrected DOM -- diagnose first, then fix selectors (or, if a real product regression is found, fix the product and say so).

Purpose: ship Bartek's live-UAT corrections exactly as specified (no scope reduction, no reinterpretation) and restore the Phase 9 e2e suite to green.
Output: corrected ItemListView.tsx/style.css, updated ItemListView.test.tsx coverage for the corrected layout, a green dual-browser.spec.ts Phase 9 group, and a final full-gate verification pass (vitest, compile, chrome e2e, firefox build).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@extension/entrypoints/popup/ItemListView.tsx
@extension/entrypoints/popup/ItemListView.test.tsx
@extension/entrypoints/popup/style.css
@extension/entrypoints/popup/App.tsx (lines 398-418, the item-list view's fixed h-[600px]/w-[380px] shell this task's markup renders inside)
@extension/e2e/dual-browser.spec.ts (lines 426-596, the Phase 9 describe block)
@web/src/components/shell/Sidebar.tsx (lines 156, `bg-base-200` wrapper -- theme parity reference, READ-ONLY)
@web/src/components/shell/TopBar.tsx (line 17, `bg-base-200` header -- second confirmation of the same token, READ-ONLY)
@web/src/components/vault/ItemRow.tsx (lines 115-119, the web app's own row hover recipe: `hover:bg-base-content/[0.06]`, no shadow -- READ-ONLY reference for Task 3)
@packages/pv-ui/tokens.css (READ-ONLY -- do not edit; `--color-base-100/200/300/content` are the tokens in play for Task 1)
@extension/lib/i18n/dictionary.ts (lines 125-168, search.placeholder/nav.fullScreen/sort.label/vault.allItemsHeading -- exact PL/EN copy, unchanged by this plan)

Diagnostic findings already confirmed during planning (do not re-derive, verify and build on them):
- `bg-neutral` is used in ItemListView.tsx in exactly ONE place (the top-bar wrapper div) -- confirmed via grep, safe scoped change for Task 1.
- The web app's Sidebar.tsx AND TopBar.tsx both use `bg-base-200` (not `bg-base-100`) -- this is the correct target token for Task 1's top bar, not a guess.
- The popup's content card already uses `bg-base-100`, which already matches the web app's own body background (daisyUI's base layer sets `body { background-color: var(--color-base-100) }`; `web/src/app/globals.css` has no override) -- Task 1 does NOT need to touch the content card's background, only confirm it via Read.
- daisyUI 5's `.btn` size scale (confirmed via `extension/node_modules/daisyui/components/button.css`): default/no-size-class = 40px (`--size-field * 10`), `btn-sm` = 32px, `btn-xs` = 24px. "Comfortable" in Task 2 means dropping `btn-sm` to the 40px default, not inventing a new size.
- The pre-restyle FAB (git history, commit 5495b10, before the sheet-look round) was `absolute bottom-16 right-3 z-50` wrapping a `btn btn-primary btn-square` -- a valid positioning precedent to restore from and adjust slightly (see Task 2).
- `extension/e2e/dual-browser.spec.ts` has exactly 4 bare `popup.locator("select")`/`popup.selectOption("select", ...)` occurrences in the whole file (lines ~448, 488, 509, 523), ALL inside the Phase 9 describe block, ALL now ambiguous because ItemListView.tsx renders a SECOND `<select>` (the sort control, `data-testid="popup-sort-select"`, introduced by the prior redesign commit) whenever the shared UAT vault (`uat-prf04@example.local`, reused unreset across e2e runs) has any items -- this is the near-certain root cause of P9-SC2/SC3/SC4. P9-SC5/SC7 need independent diagnosis (Task 5 below) -- do not assume they share this cause without checking.
- Local baseline before this plan: `npx vitest run` = 605 passing tests, 50 files, 1 pre-existing unrelated unhandled-rejection warning from ServerConfigView.tsx (not introduced by this plan -- ignore unless it starts failing tests).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme parity -- top bar matches web sidebar's bg-base-200</name>
  <files>extension/entrypoints/popup/ItemListView.tsx</files>
  <action>
Change the top-bar wrapper div's className (currently `flex shrink-0 flex-col gap-2 bg-neutral px-4 pb-3 pt-3 text-primary-content`) so its background/text token pairing is `bg-base-200`/`text-base-content` instead of `bg-neutral`/`text-primary-content` -- this is the SAME token pairing web/src/components/shell/Sidebar.tsx's `<aside>` wrapper and web/src/components/shell/TopBar.tsx's `<header>` both already use (confirmed in context above), giving the popup's dark strip pixel-parity with the web app's own sidebar/topbar in both vault-dark (base-200 is darker than base-100: 24.78% L vs 26.86% L) and vault-light (base-200 is a hair off pure white, matching the web sidebar's own barely-there light-theme tint: 98.86% L vs base-100's 100% L) -- both themes stay coherent automatically since this is a real theme-adaptive token pair, not a hand-picked literal. Rewrite the comment block directly above this div (currently explaining the old bg-neutral/text-primary-content rationale) to instead explain the corrected pairing and WHY (parity with web's Sidebar/TopBar, per Bartek's 2026-07-18 live-UAT correction) -- do not leave stale rationale describing a token pairing that is no longer used. Do not touch the content card's `bg-base-100` (the div directly below, holding the scrollable sections) -- it already matches the web app's own main-content/body background; leave it exactly as-is (Read to confirm, no edit needed).
  </action>
  <verify>
    <automated>cd extension && grep -c 'bg-base-200 px-4 pb-3 pt-3 text-base-content' entrypoints/popup/ItemListView.tsx</automated>
  </verify>
  <done>The top-bar div's className contains bg-base-200/text-base-content (not bg-neutral/text-primary-content); the grep above returns 1; the content card below is unchanged (still bg-base-100); npm run compile is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restore floating FAB, rebalance footer, move Full screen pill to top bar</name>
  <files>extension/entrypoints/popup/ItemListView.tsx, extension/entrypoints/popup/ItemListView.test.tsx</files>
  <action>
In `ItemListView.tsx`:

(a) Restore the "+" new-item control as a floating FAB. Move the `fabMenuRef`-wrapped div (currently the second child of the footer's left `<div className="flex items-center gap-1">` group) OUT of the footer entirely, to become a direct sibling of the top-bar div / content-card div / footer div (i.e. a direct child of the outermost `relative flex ... w-[380px] ...` wrapper). Position it `absolute bottom-16 right-4 z-50` (adapted from the pre-restyle historical precedent of `bottom-16 right-3`, moved to `right-4` for edge consistency with this popup's own `px-4` content padding elsewhere -- verify visually via screenshot that this gives clean clearance above the now-shorter footer, with no overlap). Give this wrapper div `data-testid="popup-fab"`. Restore the button's size to the daisyUI default (drop `btn-sm`): `btn btn-primary btn-square shadow-lg`, `<Plus size={20} aria-hidden="true" />` (was size 18). Keep `aria-label`, `aria-haspopup="menu"`, `aria-expanded`, the `onClick` toggle, and the entire type-menu `<ul>` (NEW_ITEM_TYPE_ORDER map, existing classes) completely unchanged -- only the wrapper's position and the button's size classes/icon size change.

(b) Footer: the left group (`<div className="flex items-center gap-1">`) now contains ONLY the gear button (the FAB div moved out per (a)). Give the OUTER footer div (currently `<div className="flex shrink-0 items-center justify-between gap-2 border-t border-base-300 px-4 py-2">`) a `data-testid="popup-footer"`. Bump the gear button to the same comfortable size as the FAB (drop `btn-sm`): `btn btn-ghost btn-square`, `<Settings size={20} aria-hidden="true" />` (was size 18), so the two icon controls read as visually consistent weight even though they now live in different parts of the popup.

(c) Move the "Full screen" pill (currently the second child of the footer's right group, after the auto-lock select) into the top bar, beside the title. Wrap the existing `<h1>` in a new `<div className="flex items-center justify-between gap-2">` alongside the pill button, so the top bar's first row reads title-left/pill-right, with the search input staying on its own row below (unchanged). Move the pill's ENTIRE existing JSX (`inline-flex h-9 items-center gap-1.5 rounded-[10px] bg-primary px-3 text-sm font-medium text-primary-content transition hover:opacity-90`, `ExternalLink` icon, `nav.fullScreen` label, `onClick={() => void openInNewTab("")}`) verbatim into this new title row -- do not duplicate it, do not change its handler or classes. The footer's right group now contains ONLY the auto-lock label + select (the pill moved out).

(d) Update this file's header comment block (near the top, describing the "sheet-look" layout) so it accurately reflects the corrected state: FAB restored as a floating bottom-right control (not folded into the footer), footer = gear only (left) / auto-lock only (right), top bar = title + Full-screen pill + search. Attribute this to Bartek's 2026-07-18 live-UAT correction round, superseding only the footer/top-bar PLACEMENT decisions from the immediately-prior sheet-look round (not its theme/hover/sizing choices, which are separately corrected by this plan's other tasks).

In `ItemListView.test.tsx`:

Add `within` to the existing `@testing-library/react` import. Add a new test asserting the corrected structure: render with an unlocked, empty-vault mock (same pattern as the existing Test 5/6 mocks); get `screen.getByTestId("popup-footer")` and `screen.getByTestId("popup-fab")`; assert `footer.contains(fab)` is false (FAB is not a footer descendant); assert `within(footer).queryByRole("button", { name: /new item|nowy element/i })` is null (the "+" control is no longer in the footer); assert `within(fab).getByRole("button", { name: /new item|nowy element/i })` exists (the "+" control lives in the FAB); assert `within(footer).getByRole("button", { name: /settings|ustawienia/i })` exists (gear stays in the footer); assert `within(footer).queryByRole("button", { name: "Full screen" })` is null (pill no longer in the footer) while `screen.getByRole("button", { name: "Full screen" })` still exists somewhere in the document (pill moved, not removed). Update Test 11's title/description (currently says "the footer's right-side pill") to describe its new top-bar location -- its assertion logic (role/name query, unaffected by DOM position) needs no functional change.
  </action>
  <verify>
    <automated>cd extension && npx vitest run entrypoints/popup/ItemListView.test.tsx</automated>
  </verify>
  <done>The "+" FAB is a floating, absolutely-positioned button that is NOT a descendant of the footer testid; the footer contains exactly one icon button (gear, comfortable 40px size) on the left and the auto-lock select on the right; the "Full screen" pill renders in the top bar next to the title; all ItemListView.test.tsx tests (existing + the new structural test) pass; npm run compile is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Row hover -- remove drop-shadow, keep border/press pattern</name>
  <files>extension/entrypoints/popup/style.css</files>
  <action>
In `.pv-row-hover:hover`, remove the two external drop-shadow `box-shadow` layers (`0 3px 2px -2px color-mix(...) 10%...` and `0 4px 3px -2px color-mix(...) 10%...`) -- these are what currently makes hover look like a lifted card and makes the adjacent divide-y row separator appear to "light up" (the shadow's blur visually overlaps the separator line beneath it). KEEP the inset-ring layer (`inset 0 0 0 1px color-mix(in oklch, var(--color-base-content) 15%, transparent)`) -- this reads as a border, not a shadow, and is what satisfies "keep the border/press hover pattern consistent with web ItemRow"'s spirit of a subtle interactive affordance without a floating-card look (web/src/components/vault/ItemRow.tsx's own hover is even flatter -- just a background tint, no border at all -- but Bartek's instruction is to remove the shadow specifically, not the whole affordance). Leave `.pv-row-hover:active`'s box-shadow/translate (the press state) completely untouched -- it is not part of this complaint. Update the comment block above `.pv-row-hover` (currently describing a "two-layer drop shadow" recipe borrowed from daisyUI's own `.btn` component) to describe the corrected, single-inset-ring recipe and why the drop-shadow layers were removed (Bartek 2026-07-18 live-UAT: shadow made hover look broken / collided with the row separator).

After the CSS change, build the extension and visually verify (via a screenshot in both vault-dark and vault-light, e.g. through the existing e2e Chromium harness or a quick dev-server screenshot) that hovering a row no longer shows a shadow and the adjacent divide-y separator does not visibly change appearance on hover. Only if the separator still visually collides with the hover state after the shadow removal, add a small amount of extra vertical breathing room between rows (e.g. bump each row button's `py-2` to `py-2.5` in ItemListView.tsx) as a documented fallback -- shadow removal is the primary, expected-sufficient fix; do not pre-emptively add spacing before confirming the shadow removal alone does not already resolve it.
  </action>
  <verify>
    <automated>cd extension && grep -c 'inset 0 0 0 1px color-mix(in oklch, var(--color-base-content) 15%, transparent);' entrypoints/popup/style.css</automated>
  </verify>
  <done>`.pv-row-hover:hover`'s box-shadow declares only the inset-ring layer (the grep above returns 1, meaning it is the SOLE value, not one of three comma-joined layers); `.pv-row-hover:active` is unchanged; a screenshot self-check (documented in the SUMMARY) confirms no shadow and no visible separator change on hover in both themes.</done>
</task>

<task type="auto">
  <name>Task 4: Label/sort sizing -- "All items" row on one line, matched to body-text scale</name>
  <files>extension/entrypoints/popup/ItemListView.tsx</files>
  <action>
In the "Wszystkie"/"All items" section header row (the `<div className="flex items-center justify-between gap-2">` immediately containing the `<h2>` heading and the `data-testid="popup-sort-select"` `<select>`): bump the `<h2>`'s className from `text-xs font-medium text-base-content/60` to `text-sm font-medium text-base-content/60 whitespace-nowrap` -- the size bump (12px -> 14px) matches Bartek's explicit "same font size as the surrounding UI" instruction, and `whitespace-nowrap` is what actually GUARANTEES the single-line rendering (a flex item's default `min-width:auto` otherwise permits wrapping at the space inside "All items" whenever the row is squeezed, which is the direct cause of the two-line wrap Bartek reported -- font size alone would not fix that). Bump the `<select>`'s daisyUI size class from `select-xs` to `select-sm` (matching the footer's OWN auto-lock `<select>`, which already uses `select-sm` -- this closes the internal size inconsistency between the popup's two selects) and add `shrink-0` so it can never be squeezed by the now-larger heading text. Do NOT modify `extension/entrypoints/popup/autofill/OnThisPageSection.tsx`'s "Na tej stronie" heading -- Bartek's decision names only the "'All items' + sort dropdown row," and this task is scoped to exactly that; note in the SUMMARY that this intentionally creates a size difference between the two section headings (12px "Na tej stronie" vs 14px "Wszystkie"), since that is the literal, scoped instruction, not an oversight.
  </action>
  <verify>
    <automated>cd extension && grep -c 'text-sm font-medium text-base-content/60 whitespace-nowrap' entrypoints/popup/ItemListView.tsx</automated>
  </verify>
  <done>The "Wszystkie"/"All items" h2 has text-sm + whitespace-nowrap (grep above returns 1); the sort select has select-sm + shrink-0 (not select-xs); rendering at the popup's default 380px width shows "All items" and "Wszystkie" each on one line with no overlap with the select; existing ItemListView.test.tsx Tests 12/13 (querying by data-testid, unaffected by class changes) still pass.</done>
</task>

<task type="auto">
  <name>Task 5: E2E repair -- root-cause and fix the 5 failing Phase 9 Chromium tests</name>
  <files>extension/e2e/dual-browser.spec.ts</files>
  <action>
Root-cause FIRST, against the ACTUAL rebuilt extension (Tasks 1-4 must already be applied) -- do not edit selectors from memory or assumption.

Step 1 -- build and observe: from `extension/`, run `npx wxt build -b chrome` (matching the `pretest:e2e:chrome` hook), then run ONLY the Phase 9 describe block with real output captured (e.g. `npx playwright test --project=chromium -g "Phase 9"`, optionally `--headed`/`--trace on` for a failing run if the default output is insufficient to diagnose). Read each failure's actual error text.

Step 2 -- apply the already-confirmed fix for P9-SC2/SC3/SC4 (lines ~448, 488, 509, 523 -- the ONLY 4 bare `popup.locator("select")`/`popup.selectOption("select", ...)` occurrences in the whole file, confirmed via grep during planning): ItemListView.tsx now renders a SECOND `<select>` (the sort control) whenever the shared UAT vault has items, which it does after many prior e2e runs against the same unreset `uat-prf04@example.local` account -- every bare `"select"` locator is now ambiguous (Playwright strict-mode violation: "resolved to 2 elements"). Replace each of these 4 occurrences with `popup.locator("#pv-autolock")` (the auto-lock select's existing, unchanged id) -- never a bare `"select"` anywhere the sort control could also be present. Re-run `-g "P9-SC2"`, `-g "P9-SC3"`, `-g "P9-SC4"` individually to confirm each is green after this fix, before moving on.

Step 3 -- diagnose P9-SC5 and P9-SC7 independently; do NOT assume they share Step 2's root cause. Read each test's actual failure output (timeout vs. strict-mode vs. assertion vs. navigation error) against the CURRENT DOM (search input now lives in the `bg-base-200` top bar, still `type="text"` with the unchanged `search.placeholder` translation; the "Full screen" pill now lives in the top bar per Task 2, same accessible role/name as before -- these facts are already confirmed, do not re-verify them, but DO verify whether either test's actual failure traces back to something else, e.g. cascading state left behind by SC2-4's pre-fix failures in a prior run, timing against the shared vault's now-larger item count, or a genuine locator/DOM mismatch). If a failure traces to selector drift, fix the LOCATOR precisely (prefer existing ids/data-testids already in ItemListView.tsx -- `#pv-autolock`, `[data-testid="popup-sort-select"]`, `[data-testid="popup-footer"]`, `[data-testid="popup-fab"]` -- over inventing new ones). If a failure instead reveals a REAL product regression (e.g. search genuinely broken, fullscreen genuinely unreachable), FIX THE PRODUCT CODE in ItemListView.tsx/App.tsx and state this explicitly, by name, in the SUMMARY -- do not silently patch the test to paper over a real regression. Never fix a failure by weakening an assertion, adding an arbitrary sleep, or swallowing an error.

Do not modify any Phase 10/11/12 test in this file -- scope is strictly the Phase 9 describe block (lines ~430-596) plus, only if Step 3's diagnosis genuinely requires it, the already-in-scope ItemListView.tsx/App.tsx product files.
  </action>
  <verify>
    <automated>cd extension && npx playwright test --project=chromium -g "Phase 9"</automated>
  </verify>
  <done>All 7 Phase 9 SCs (P9-SC1..SC7) pass in a single `--project=chromium -g "Phase 9"` run against the real rebuilt extension; any real product regression found and fixed is named explicitly in the SUMMARY; zero bare ambiguous "select" locators remain anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 6: Final verification -- full gate run</name>
  <files>extension/entrypoints/popup/ItemListView.tsx, extension/entrypoints/popup/ItemListView.test.tsx, extension/entrypoints/popup/style.css, extension/e2e/dual-browser.spec.ts</files>
  <action>
Run the full verification block in order, capturing real command output at each step (never assert success from memory):

(a) `cd extension && npx vitest run` -- expect fully green, count >= 605 (the confirmed pre-plan baseline), zero skips. Then `npm run compile` -- expect clean, no new type errors.

(b) Confirm pv-server is already reachable at `http://localhost:8620` (never `127.0.0.1`) via `curl -s http://localhost:8620/healthz` before running e2e -- do not start a second server instance if one is already running. Then `npm run test:e2e:chrome` (this runs `pretest:e2e:chrome` -> `wxt build -b chrome` automatically via npm's pretest hook, then `playwright test --project=chromium`). The Phase 9 group (P9-SC1..SC7) MUST be fully green -- this is a hard gate. Phase 10-12 flaky-recoveries attributable to machine memory pressure (per this project's own documented precedent) are acceptable; any OTHER hard fail must be triaged: if traceable to this plan's own changes, fix it in this same task (creating one additional targeted commit if code changes were needed) and re-run the failing spec to confirm green; if pre-existing/unrelated to this plan, document it as such in the SUMMARY without treating it as a plan failure. Do not click into, resize, or otherwise steal focus from any Playwright-opened browser window while headed tests run.

(c) `npm run build:firefox` -- expect a clean build, no errors.

Report the final vitest count, the Phase 9 per-SC pass/fail, any Phase 10-12 findings (flaky vs. hard-fail vs. pre-existing), and the Firefox build result in the SUMMARY. Never declare the plan done with any of (a)/(b)'s Phase-9-group/(c) red.
  </action>
  <verify>
    <automated>cd extension && npx vitest run && npm run compile && npm run test:e2e:chrome -- --project=chromium -g "Phase 9" && npm run build:firefox</automated>
  </verify>
  <done>npx vitest run is green (>=605 tests, 0 skips); npm run compile is clean; npm run test:e2e:chrome's Phase 9 group is fully green (7/7); npm run build:firefox completes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Popup UI (rendered DOM) -> Bartek's own eyes (visual UAT) | Purely presentational/layout change; no new data crosses any boundary, no new external calls introduced |
| dual-browser.spec.ts -> real Chromium + local pv-server (dev/test only) | Selector/test-harness fixes only, never shipped in the extension bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick0qi-01 | Tampering (UI) | ItemListView.tsx top-bar/footer/FAB restructure | low | accept | Pure presentational/layout change reusing existing, already-shipped tokens (bg-base-200/text-base-content from packages/pv-ui/tokens.css, read-only) and existing handlers (openInNewTab/handleNewItemType/handleAutoLockChange) verbatim -- no new data paths, no new network calls, no new package installs. |
| T-quick0qi-02 | Information Disclosure | Row hover / label sizing changes | low | accept | Touches only the presentation of data already rendered in the DOM (item names/subtitles, section labels) -- no new fields exposed, no new content rendered. |
| T-quick0qi-03 | Tampering (test harness) | dual-browser.spec.ts Phase 9 selector fixes | low | accept | Dev/test-only file, never shipped; fixes disambiguate existing selectors against the already-shipped DOM using existing ids/data-testids, no assertion is weakened or removed to force green. |
</threat_model>

<verification>
Run all tasks' `<verify>` steps in order (Tasks 1-4 are independent CSS/JSX edits gated by scoped grep + `npm run compile`; Task 5 depends on Tasks 1-4's DOM being in place before diagnosing; Task 6 is the aggregate final gate). The plan is not complete until Task 6's full command chain (`npx vitest run && npm run compile && npm run test:e2e:chrome -- --project=chromium -g "Phase 9" && npm run build:firefox`) exits green with real captured output.
</verification>

<success_criteria>
- Task 1: popup top bar uses bg-base-200/text-base-content (pixel-parity with web Sidebar/TopBar in both themes); content card unchanged (still bg-base-100).
- Task 2: "+" is a floating FAB outside the footer's DOM subtree, comfortable 40px size; footer holds exactly gear (left, 40px) + auto-lock select (right); "Full screen" pill lives in the top bar beside the title.
- Task 3: `.pv-row-hover:hover` has no drop-shadow layers; separator does not visibly change on hover (screenshot-confirmed).
- Task 4: "All items"/"Wszystkie" + sort select render on one line, sized consistent with the popup's body text, no overlap.
- Task 5: all 7 Phase 9 e2e SCs green; any real product regression found is named explicitly in the SUMMARY, not silently patched around.
- Task 6: full vitest suite (>=605), compile, Phase 9 e2e group, and Firefox build all green, with real captured output for each.
- Five atomic commits exist (theme; FAB+footer+topbar; hover; label/sort sizing; e2e repair), each staged with explicit file paths, never `git add -A`.
</success_criteria>

<output>
Create `.planning/quick/260718-0qi-popup-ui-fix-round-theme-match-web-sideb/260718-0qi-SUMMARY.md` when done, documenting: each commit (with hash), each task's actual verify output, the screenshot self-check result for Task 3, any real product regression fixed during Task 5 (named explicitly), the final vitest count vs. the 605 baseline, the Phase 9 per-SC e2e result, and the Firefox build result.
</output>
