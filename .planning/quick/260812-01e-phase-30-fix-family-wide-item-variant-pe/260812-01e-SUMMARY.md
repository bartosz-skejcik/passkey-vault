---
quick_id: 260812-01e
subsystem: sharing
tags: [rust, axum, sqlx, typescript, react, playwright, family-wide-sharing, access-control]
requires:
  - phase: 30-the-living-group-family-wide-sharing
    provides: family-wide FOLDER sharing (item variant was the one remaining gap)
provides:
  - per-level item_bucket schema (up to 3 buckets per family, one per access level)
  - server-side contributor-edit-claim mechanism on item_bucket destinations
  - two closed privilege-escalation side doors (revoke_access refusal, declared-level propagation bound)
  - client-side level-keyed bucket resolution and a defended 409-swallow
  - item_bucket excluded from the ordinary "What you're sharing" folder tab
  - honest ShareDialog copy disclosing the contributor-edit asymmetry
  - live, recipient-side, real-crypto Playwright proof of the item variant + Face 2
affects: [phase-30-re-verification, sharing-uat]
tech-stack:
  added: []
  patterns:
    - "three-state resolver (NotFamilyWide/Declared/LegacyUnknown) instead of Option<T>, to keep a legacy-NULL DB row structurally distinguishable from a genuine declared value"
    - "additive bound layered on an existing gate, never a rewrite of the gate's own exhaustive match"
key-files:
  created:
    - crates/pv-server/migrations/0021_item_bucket_per_level.sql
  modified:
    - crates/pv-server/src/routes/membership.rs
    - crates/pv-server/src/routes/vault.rs
    - crates/pv-server/src/routes/collections.rs
    - crates/pv-server/src/routes/invitations.rs
    - crates/pv-server/src/routes/families.rs
    - crates/pv-server/tests/family_wide_sharing.rs
    - crates/pv-server/tests/collections.rs
    - web/src/lib/families/api.ts
    - web/src/components/vault/ShareDialog.tsx
    - web/src/components/vault/ShareDialog.test.tsx
    - web/src/components/vault/SharingOverviewPanel.tsx
    - web/src/components/vault/SharingOverviewPanel.test.tsx
    - web/src/lib/i18n/dictionary.ts
    - web/e2e/family-wide-sharing.spec.ts
key-decisions:
  - "LOCKED decision 1's mechanism (contributor claims edit on write) is unchanged; its two undeclared side effects (revocation power, propagation-level ceiling) are closed as additive server-side bounds, never by narrowing the mechanism itself"
  - "The add_member/invite-fold-in declared-level bound is scoped to item_bucket collections only, matching revoke_access's own established scoping -- applying it to family-wide FOLDERS too would have silently tightened pre-existing, unrelated, deliberate folder-sharing behavior"
  - "A second family is structurally impossible in this codebase (FAM-01's idx_families_singleton, one row for the whole database) -- Task 8's live test reuses the existing singleton family via a fresh context on the same reconstructible owner identity, rather than the plan's literal 'fresh family' instruction"
requirements-completed: [FSH-01]
duration: ~3h
completed: 2026-08-12
status: complete
---

# Quick Task 260812-01e: Fix the family-wide ITEM variant Summary

**Per-level item_bucket schema (up to 3 buckets/family) + server-side contributor-edit-claim mechanism, two closed privilege-escalation side doors, client-side level-keying, honest ShareDialog copy, and a live Playwright proof — closing Phase 30 SC2's last gap.**

## Performance

- **Tasks:** 8/8 completed
- **Commits:** 8 task commits (no metadata/docs commit — this is a quick task, not a phase plan)
- **Files modified:** 14 (1 new migration, 13 existing files)

## Per-task status and falsification evidence (LOCKED decision 3)

### Task 1 — Server mechanism: a contributor claims edit on write

**Commit:** `6487895`

Added `is_item_bucket_collection`, `require_and_claim_item_bucket_edit`, and the three-state `FamilyWideDeclaredLevel`/`resolve_family_wide_declared_level` to `membership.rs`. Wired `vault.rs::move_item`'s Gate 2 to branch to the claim path for `item_bucket` destinations (family-wide FOLDER destinations unaffected). New test `task1_non_creator_contributor_claims_edit_on_read_declared_item_bucket` reproduces VERIFICATION.md's exact control probe reversed.

**Falsification — observed output (verbatim):**
```
thread 'task1_non_creator_contributor_claims_edit_on_read_declared_item_bucket' panicked at crates/pv-server/tests/family_wide_sharing.rs:1659:5:
assertion `left == right` failed: a non-creator member holding only 'read' on a family-wide item_bucket must be able to move their own item into it -- VERIFICATION.md's exact control probe, reversed
  left: 403
 right: 200
```
Restored; `cargo test --workspace --no-fail-fast` exit 0 (all 30 suites green).

### Task 2 — Close the edit-row's side doors

**Commit:** `1184b98`

`collections::revoke_access` refuses outright on any `item_bucket`-kind collection. `collections::add_member` and `invitations::create`'s `family_wide_keys` loop both gained an additional `requested_level == declared` bound, scoped to `item_bucket` collections only (see Deviations). Four new tests, each independently falsified.

**Falsification 1 — revoke_access refusal, observed output (verbatim):**
```
thread 'task2_self_escalated_contributor_cannot_revoke_the_creator' panicked at crates/pv-server/tests/family_wide_sharing.rs:1833:5:
assertion `left == right` failed: a self-escalated contributor must never be able to revoke the bucket's creator
  left: 204
 right: 403
```

**Falsification 2 — add_member declared-level bound, observed output (verbatim):**
```
thread 'task2_self_escalated_contributor_cannot_grant_more_than_the_declared_level' panicked at crates/pv-server/tests/family_wide_sharing.rs:1881:5:
assertion `left == right` failed: a self-escalated contributor must never be able to hand a third member more than the bucket's own declared level
  left: 201
 right: 403
```

**Falsification 3 — invite fold-in declared-level bound, observed output (verbatim):**
```
thread 'task2_self_escalated_family_owner_cannot_fold_edit_into_an_invite' panicked at crates/pv-server/tests/family_wide_sharing.rs:2049:5:
assertion `left == right` failed: a self-escalated family owner's invite folding in more than the bucket's own declared level must be refused
  left: 201
 right: 403
```
(See Deviations — this test's attacker had to be the family OWNER, not a plain member; a finding made while executing.)

**Falsification 4 — legacy-NULL row must NOT be bound (collapsing `LegacyUnknown` into `Declared(Read)`), observed output (verbatim):**
```
thread 'task2_legacy_null_level_family_wide_collection_invite_fold_in_still_succeeds' panicked at crates/pv-server/tests/family_wide_sharing.rs:2140:5:
assertion `left == right` failed: an edit-holding member's invite folding in a LEGACY NULL-level family-wide collection at 'edit' must still succeed -- LegacyUnknown applies no equality bound
  left: 403
 right: 201
```
All four restored; `cargo test --workspace --no-fail-fast` exit 0 (all 30 suites green, zero warnings).

### Task 3 — Per-level item_bucket schema + level-aware pending discovery

**Commit:** `58a413c`

New migration `0021_item_bucket_per_level.sql` re-scopes `idx_one_item_bucket_per_family` to `(family_id, COALESCE(family_wide_access_level, ''))`. `families.rs`'s `PendingGrant` gains `access_level: Option<String>`. Renamed and extended `tests/collections.rs`'s test with a third, different-level item_bucket creation attempt.

**Falsification — observed output (verbatim):**
```
thread 'second_item_bucket_at_the_same_level_for_same_family_is_409_but_a_different_level_succeeds' panicked at crates/pv-server/tests/collections.rs:2524:5:
assertion `left == right` failed: a THIRD item_bucket for the same family, declared at a DIFFERENT level, must succeed — LOCKED decision 1 permits up to three item_bucket collections per family, one per access level
  left: 409
 right: 201
```
Restored; `cargo test --workspace --no-fail-fast` exit 0, confirmed stable across repeated runs.

**Found and fixed while executing (in scope, caused directly by this task's own field addition):** adding `access_level` to `PendingGrant`'s JSON response broke `family_wide_pending_discovery_response_carries_only_ids_and_kinds` (renamed to `..._and_access_levels`), which asserts an exact key list and value allowlist. Updated `known_values` to include `"read"` and the exact-shape assertions to include `access_level`. A further subtlety: the JSON key ORDER assertion had to be pinned to `cargo test --workspace`'s actual observed order (struct-declaration order: `collection_id`, `kind`, `access_level`), not `-p pv-server`'s order (alphabetical) — `serde_json::Value::Object`'s re-parsed key order depends on whether the `preserve_order` feature is enabled, and Cargo's feature unification enables it workspace-wide (via `pv-provider`'s `passkey-authenticator` dependency chain) but not when building `pv-server` alone. This is exactly the class of trap LOCKED decision 4 warns about (narrower command passing while the wider command fails), just in the opposite direction from the usual case. Verified stable across two consecutive full `cargo test --workspace --no-fail-fast` runs.

### Task 4 — hidden_password variant and Face 2 cannot recur

**Commit:** `0280e6a`

Two new tests: the `hidden_password` variant of Task 1's proof, and "Face 2 cannot recur" (two item_buckets at different declared levels, an overlapping recipient resolving independently on each).

**Falsification 1 — hidden_password variant, observed output (verbatim):**
```
thread 'task4_non_creator_contributor_claims_edit_on_hidden_password_declared_item_bucket' panicked at crates/pv-server/tests/family_wide_sharing.rs:2262:5:
assertion `left == right` failed: a non-creator member holding only 'hidden_password' on a family-wide item_bucket must be able to move their own item into it
  left: 403
 right: 200
```

**Falsification 2 — Face 2 cannot recur (fixture mistake: requesting the first bucket's level on the second bucket's `add_member` call), observed output (verbatim):**
```
thread 'task4_face2_two_item_buckets_at_different_levels_resolve_independently' panicked at crates/pv-server/tests/family_wide_sharing.rs:2408:5:
assertion `left == right` failed: fanning the recipient out at edit on the SECOND bucket must succeed
  left: 403
 right: 201
```
This fails ONE step earlier than the test's own final level-comparison assertions — Task 2's own declared-level bound refuses the mismatched grant outright at the `add_member` call. Documented in the test's own doc comment as still-valid evidence (the mistake is caught, doubly). Both restored; `cargo test --workspace --no-fail-fast` exit 0 (14 tests in `family_wide_sharing.rs`, all 30 suites green).

### Task 5 — Client: level-keyed bucket resolution + defended 409-swallow

**Commit:** `ca250d4`

`familyItemBucketRow` now requires a `level` parameter, matching `family_wide_access_level` too. `grantCollectionToRecipients`'s 409 handling now verifies the recipient's actual persisted level (via `recipientAlreadyHoldsIntendedLevel`/`getCollectionAccessList`) instead of unconditionally treating a 409 as success. `BUCKET_ROW` fixture gained `family_wide_access_level`; two pre-existing tests updated to match; two new tests added (level-mismatch-creates-new-bucket, Face-2 409 defense in both directions).

**Falsification 1 — level-keying, observed output (verbatim):**
```
FAIL  src/components/vault/ShareDialog.test.tsx > ShareDialog > family-wide item share (FSH-01 submitItemVariant) > does NOT reuse an existing bucket declared at a DIFFERENT level -- creates a new, separate bucket instead (260812-01e Task 5)
AssertionError: expected "spy" to be called 1 times, but got 0 times
 ❯ src/components/vault/ShareDialog.test.tsx:1245:36
   expect(mockCreateCollection).toHaveBeenCalledTimes(1);
```

**Falsification 2 — Face-2 defense, observed output (verbatim):**
```
FAIL  src/components/vault/ShareDialog.test.tsx > ... > a 409 whose recipient holds a DIFFERENT (wrong) level IS reported as failed
→ Unable to find an element by: [data-testid="share-partial-error"]
```
(A `waitFor` timeout — the wrong-level failure was never reported with the pre-fix unconditional-409-is-success code, so the dialog never showed `share-partial-error` and the assertion could never become true.)
Both restored; `npm run compile` / `npm test` (50/50 in `ShareDialog.test.tsx`, 967/967 total) / `npm run build` all exit 0.

### Task 6 — Client: exclude item_bucket from the ordinary folder tab

**Commit:** `feb7234`

Added `c.family_wide_kind !== "item_bucket"` to `SharingOverviewPanel.tsx`'s `editableIds` filter. Two new tests: one extends the `useCollections()` fixture to also include a decrypted bucket entry (the gap that let this leak go uncaught); one adds a second item_bucket at a different level to prove multi-bucket resolution still works.

**Falsification — observed output (verbatim, DOM excerpt from the failing assertion):**
```
expect(screen.queryByTestId("sharing-overview-folder-fam-bucket-1")).not.toBeInTheDocument()
```
failed with the bucket's placeholder name rendering as a real folder row:
```html
<span class="min-w-0 flex-1 truncate text-sm font-bold" title="family-wide-items">
  family-wide-items
</span>
```
Restored; `npm run compile` / `npm test` (28/28 in `SharingOverviewPanel.test.tsx`, 969/969 total) / `npm run build` all exit 0.

### Task 7 — Honest copy: disclose the contributor-edit asymmetry

**Commit:** `94835d6`

Added `share.familyWideItemContributorEditNote` (PL + EN, carrying all three required facts per plan-check C-2: any member at will, read-only does not prevent editing, permanent). Rendered in `ShareDialog.tsx` immediately below the access-level radio group, testid `share-family-wide-item-contributor-note`, conditioned on `scope.kind === "item" && isFamilyWideSelected && accessLevel !== null && accessLevel !== "edit"`.

This task adds no new unit tests per the plan's own file list (dictionary.ts + ShareDialog.tsx only) — its falsification/live proof is Task 8's own e2e assertion against this exact note. `npm run compile` / `npm test` (969/969, unchanged count) / `npm run build` all exit 0.

### Task 8 — Live proof: item variant, recipient-side, real crypto, Face 2 falsified

**Commit:** `fde4b1b`

New, independent `test.describe` block in `family-wide-sharing.spec.ts`. Owner + a fresh member join a family; owner shares item X at "read" (baseline); the member (non-creator, "read"-only) shares item Y also at "read" — VERIFICATION.md's exact control probe; owner decrypts item Y (the recipient-side proof this defect's control probe was missing); owner shares item Z at "edit" (Face 2); member decrypts Z too; item X/Y's and Z's `collection_id`s are asserted distinct, and each bucket's own `family_wide_access_level` is asserted correct via `GET /api/vault/collections/{id}`.

**Falsification 1 (step 2, Task 1's server fix reverted) — observed output (verbatim):**
```
TimeoutError: locator.waitFor: Timeout 30000ms exceeded.
Call log:
  - waiting for getByTestId('share-dialog') to be detached
    63 × locator resolved to visible <div data-testid="share-dialog" class="fixed inset-0 z-50 flex items-center justify-center bg-base-300/70 p-4">…</div>
```
Run against the FULL spec file (matching the literal `npx playwright test e2e/family-wide-sharing.spec.ts --retries=0` command): 9 passed, 1 failed as above, in 1.8m.

**Falsification 2 (step 4/5, Task 5's `familyItemBucketRow` level filter reverted) — observed output (verbatim):**
```
TimeoutError: locator.waitFor: Timeout 30000ms exceeded.
Call log:
  - waiting for getByTestId('share-dialog') to be detached
    63 × locator resolved to visible <div data-testid="share-dialog" ...>
    at shareItemFamilyWide (family-wide-sharing.spec.ts:1667:44)
```
This fails ONE step earlier than the plan's literal prediction ("step 5's distinct-collection-id assertion now fails") — see Deviations for the full mechanism. Both restored; final live run (fresh build) — **10/10 passed in 1.1m.**

**Final CI-width verification, all five commands, exit codes recorded:**
```
cargo test --workspace --no-fail-fast   → exit 0 (30/30 suites)
cd web && npm run compile                → exit 0
cd web && npm test                       → exit 0 (969/969)
cd web && npm run build                  → exit 0
cd web && npx playwright test e2e/family-wide-sharing.spec.ts --retries=0 → exit 0 (10/10)
```

## Files Created/Modified

- `crates/pv-server/migrations/0021_item_bucket_per_level.sql` — new, re-scopes the per-family item_bucket unique index to per-(family, level)
- `crates/pv-server/src/routes/membership.rs` — `is_item_bucket_collection`, `require_and_claim_item_bucket_edit`, `FamilyWideDeclaredLevel`/`resolve_family_wide_declared_level`; removed the now-dead `is_family_wide_collection`
- `crates/pv-server/src/routes/vault.rs` — `move_item`'s Gate 2 branches to the claim path for item_bucket destinations
- `crates/pv-server/src/routes/collections.rs` — `revoke_access` refuses on item_bucket; `add_member` bounds a family-wide grant to the collection's own declared level (item_bucket only)
- `crates/pv-server/src/routes/invitations.rs` — the `family_wide_keys` fold-in loop gets the same declared-level bound (item_bucket only)
- `crates/pv-server/src/routes/families.rs` — `PendingGrant.access_level`
- `crates/pv-server/tests/family_wide_sharing.rs` — 10 new tests (Tasks 1, 2×4, 4×2, 8×1 live)
- `crates/pv-server/tests/collections.rs` — renamed/extended item_bucket creation test
- `web/src/lib/families/api.ts` — `PendingGrant.access_level`
- `web/src/components/vault/ShareDialog.tsx` — level-keyed `familyItemBucketRow`, defended `grantCollectionToRecipients`, the contributor-edit note
- `web/src/components/vault/ShareDialog.test.tsx` — 3 new tests, 3 fixture updates
- `web/src/components/vault/SharingOverviewPanel.tsx` — `editableIds` excludes item_bucket
- `web/src/components/vault/SharingOverviewPanel.test.tsx` — 2 new tests
- `web/src/lib/i18n/dictionary.ts` — `share.familyWideItemContributorEditNote` (PL+EN)
- `web/e2e/family-wide-sharing.spec.ts` — new independent `test.describe` block with 1 live test

## Decisions Made

- The `add_member`/invite-fold-in declared-level bound is scoped to `item_bucket` collections only (mirroring `revoke_access`'s own scoping), not to all family-wide collections — applying it broadly would have silently tightened pre-existing, deliberate family-wide FOLDER behavior this fix never intended to touch (found live: it broke `seed_family_wide_folder`, a pre-existing, unrelated fixture used by five other tests in this same file).
- Task 8's live test reuses the existing singleton family via a fresh browser context on the reconstructible `FAMILY_OWNER_EMAIL` identity, instead of creating a genuinely new family — see Deviations for why a second family is structurally impossible in this codebase.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] The add_member/invite-fold-in declared-level bound must be scoped to item_bucket collections only**
- **Found during:** Task 2
- **Issue:** My initial implementation applied the new `requested_level == declared` equality bound to ANY family-wide collection (kind-agnostic), matching the plan's literal wording. This broke `family_wide_sharing.rs`'s pre-existing `seed_family_wide_folder` fixture (used by 5 unrelated tests), which deliberately grants a member `"edit"` on a folder declared `"read"` — a legitimate, pre-existing, deliberate per-recipient choice by the folder's creator at share time, unrelated to Task 1's contributor-escalation mechanism (which only exists for `item_bucket` collections).
- **Fix:** Scoped the equality bound to `item_bucket` collections only, via an additional `membership::is_item_bucket_collection(...)` check at both call sites (`collections::add_member`, `invitations::create`'s fold-in loop), mirroring `revoke_access`'s own established item_bucket-only scoping and its stated rationale.
- **Files modified:** `crates/pv-server/src/routes/collections.rs`, `crates/pv-server/src/routes/invitations.rs`
- **Verification:** `cargo test --workspace --no-fail-fast` exit 0, all 30 suites green (previously 5 failures)
- **Committed in:** `1184b98` (Task 2 commit)

**2. [Rule 1 — Test correction] Task 2 test 3's attacker cannot be a plain family member**
- **Found during:** Task 2
- **Issue:** The plan's test 3 spec assumed a self-escalated CONTRIBUTOR (a plain family member) could generate an invite whose `family_wide_keys` entry attempts to fold in more than the bucket's declared level. Empirically confirmed via a throwaway probe test that `invitations::create` is gated by `ActiveFamilyMembership<RequireEdit>` — a FAMILY-ROLE check satisfied only by the family's `owner` role. A plain member 403s at this extractor, before the handler body (and thus this task's new bound) is ever reached, regardless of collection-level access.
- **Fix:** Rewrote the test's attacker to be a self-escalated FAMILY OWNER (fanned out at the bucket's declared level by a DIFFERENT member who is the bucket's actual creator, then self-escalating via Task 1's mechanism) — the one role that can ever reach this endpoint at all, and the scenario B-3's real finding actually describes.
- **Files modified:** `crates/pv-server/tests/family_wide_sharing.rs`
- **Verification:** falsification observed the expected 201→403 transition (see Task 2's falsification 3 above)
- **Committed in:** `1184b98` (Task 2 commit)

**3. [Rule 1 — Bug] JSON key-order assertion pinned to the wrong cargo invocation's behavior**
- **Found during:** Task 3
- **Issue:** `serde_json::Value::Object`'s RE-PARSED key order depends on whether serde_json's `preserve_order` feature is enabled in the current cargo invocation's dependency graph — enabled workspace-wide under `cargo test --workspace` (via `pv-provider`'s `passkey-authenticator` chain, Cargo's feature unification), but NOT under `cargo test -p pv-server` alone. An initial fix pinned the assertion to the `-p pv-server`-only (alphabetical) order, which passed standalone but failed under the actual required `--workspace` command.
- **Fix:** Re-pinned the assertion to the order actually observed under `cargo test --workspace --no-fail-fast` (struct-declaration/insertion order), matching LOCKED decision 4's literal acceptance command.
- **Files modified:** `crates/pv-server/tests/family_wide_sharing.rs`
- **Verification:** `cargo test --workspace --no-fail-fast` exit 0, confirmed stable across two consecutive full runs
- **Committed in:** `58a413c` (Task 3 commit)

**4. [Rule 4-adjacent — architectural finding, resolved without changing production behavior] A genuinely "fresh family" is impossible in this codebase**
- **Found during:** Task 8
- **Issue:** The plan's Task 8 instruction was "its own beforeAll/afterAll with fresh owner + member accounts and a fresh family". `idx_families_singleton` (migration `0014_family_sharing.sql`, FAM-01's LOCKED decision from an earlier phase) is a UNIQUE index over the constant expression `(1)` on the WHOLE `families` table — "exactly one family per INSTANCE" (that migration's own header comment), not one per owner. Confirmed live: a brand-new owner account's `POST /api/families` 409s ("family already exists") once the same spec file's OTHER `test.describe` block (which runs first, serially) has already created the one family this database will ever hold; the UI renders "Couldn't create the family. Try again."; the `beforeAll` hook then times out waiting for a state that can never arrive.
- **Fix:** Reused the SAME singleton family via `ensureFamilyOwnerSession` — a FRESH browser context authenticating as the SAME reconstructible `FAMILY_OWNER_EMAIL` identity `fixtures.ts` itself documents as reusable "by any file in this run" — rather than the outer describe's own long-lived session object. A genuinely NEW member account still joins fresh via the real invite UI. This preserves the achievable part of the plan's isolation concern (no shared session objects, no shared lock state) while dropping the literal "fresh family" wording that cannot exist in this codebase. Confirmed no collision risk: the outer describe block never creates an `item_bucket`-kind collection (only `folder`-kind family-wide shares), so this block's `item_bucket` creations are the family's first ever, at both declared levels used.
- **Files modified:** `web/e2e/family-wide-sharing.spec.ts`
- **Verification:** live run, full spec file, 10/10 passed, fresh build
- **Committed in:** `fde4b1b` (Task 8 commit)

**5. [Not a code fix — falsification methodology correction] Task 8's falsifications must run against the full spec file, not a `-g`-filtered subset**
- **Found during:** Task 8
- **Issue:** An initial attempt to isolate just the new `test.describe` block via `npx playwright test ... -g "260812-01e Task 8"` (to speed up iteration) surfaced a reproducible failure at step 3 (owner decrypting item Y) that reproduced IDENTICALLY in both the falsified AND the fully-fixed code — a standalone-execution timing artifact (server-side data was confirmed fully correct via a temporary debug probe: both items in the same bucket, both owner and member holding `edit`) that does not reproduce when the block runs as part of the full file (the actual, literal acceptance command).
- **Fix:** Re-ran both falsifications (and the final clean verification) against the full `family-wide-sharing.spec.ts` file, matching the literal `cd web && npx playwright test e2e/family-wide-sharing.spec.ts --retries=0` command exactly. Both falsifications then reproduced their expected failures cleanly and immediately (30s each, not 90s+ hangs).
- **Files modified:** none (methodology only; the debug probe added/removed during diagnosis left no trace in the committed file)
- **Verification:** all runs reported above are from full-file invocations
- **Committed in:** N/A (process correction, not a code change)

**6. [Rule 1 — Falsification-site finding, documented not silently accepted] Task 8 step 5's falsification fails one step earlier than predicted**
- **Found during:** Task 8
- **Issue:** The plan's specified falsification for step 5 — revert Task 5's `familyItemBucketRow` level filter, confirm the test's own distinct-collection-id assertion then fails — instead fails at the PRIOR `shareItemFamilyWide(ownerCtx.page, itemZId, "edit")` call itself (the dialog never detaches). Root cause: with the level filter disabled, item Z incorrectly reuses item X/Y's "read"-declared bucket (Face 2's exact original defect, reproduced) instead of creating a new "edit" one; Task 2's OWN, separate declared-level bound then refuses the resulting attempt to grant the family's other members "edit" on a bucket declared "read" (403), which `grantCollectionToRecipients` records as a partial failure, so the dialog never calls `onShared()` and never detaches.
- **Fix:** No code change — documented precisely, in the test's own code comment and here, that this is still valid evidence of the SAME underlying defect (item Z would otherwise land in the wrong bucket); Task 2's bound is a second, complementary defense the original plan-check B-5 finding predates, catching the mistake one step upstream of where B-5's own assertion would have caught it.
- **Files modified:** `web/e2e/family-wide-sharing.spec.ts` (comment only)
- **Verification:** falsification observed and documented (see Task 8's falsification 2 above); restored state re-confirmed green (10/10)
- **Committed in:** `fde4b1b` (Task 8 commit)

---

**Total deviations:** 6 (4 auto-fixed code/test corrections, 1 architectural finding resolved without touching production behavior, 1 falsification-methodology correction plus 1 falsification-site documentation)
**Impact on plan:** All deviations were necessary corrections to make the plan's own stated goals (a genuinely green `cargo test --workspace`, a genuinely falsifiable live proof) actually true, not scope creep. No LOCKED decision was violated; `may_grant_access_level`'s nine arms, `collections::create`'s hard-coded creator-edit INSERT, and `hidden_password`+family-wide's availability in ShareDialog all remain byte-for-byte unchanged (confirmed via `git diff` scoped to each, see Verification below).

## Issues Encountered

None beyond what is documented in Deviations above.

## Verification of LOCKED decision 2 (must-not-touch)

```
git diff f2d0bb2..HEAD -- crates/pv-server/src/routes/membership.rs | grep "AccessLevel::Read, AccessLevel::Read\|AccessLevel::Edit, AccessLevel::Edit"
→ no output (the nine match arms are untouched)

git diff f2d0bb2..HEAD -- crates/pv-server/src/routes/collections.rs | grep -B5 -A5 "INSERT INTO collection_keys"
→ no output (create()'s hard-coded 'edit' INSERT hunk is untouched)

git diff f2d0bb2..HEAD -- web/src/components/vault/ShareDialog.tsx | grep "hidden_password\|hiddenPassword"
→ one hit, a new comment referencing hidden_password in Task 7's contributor-edit note explanation;
  no hidden_password branch itself was modified, guarded, or narrowed
```

## Self-Check

- `crates/pv-server/migrations/0021_item_bucket_per_level.sql` — FOUND
- `crates/pv-server/src/routes/membership.rs` `is_item_bucket_collection`/`require_and_claim_item_bucket_edit`/`resolve_family_wide_declared_level` — FOUND (grep confirmed)
- `web/src/lib/i18n/dictionary.ts` `share.familyWideItemContributorEditNote` — FOUND
- `web/e2e/family-wide-sharing.spec.ts` new `test.describe` block — FOUND, live-passed
- Commit `6487895` — FOUND in `git log`
- Commit `1184b98` — FOUND in `git log`
- Commit `58a413c` — FOUND in `git log`
- Commit `0280e6a` — FOUND in `git log`
- Commit `ca250d4` — FOUND in `git log`
- Commit `feb7234` — FOUND in `git log`
- Commit `94835d6` — FOUND in `git log`
- Commit `fde4b1b` — FOUND in `git log`

## Self-Check: PASSED

## Next Phase Readiness

Phase 30's SC2 item-variant gap is closed and live-proven. A re-verification pass (`/gsd-secure-phase` or a fresh `30-VERIFICATION.md` re-run) should confirm SC2 moves from `failed` back to full `pass`, alongside the already-closed B1/B2/B3 blockers from the prior re-verify. No blockers for downstream work; T-30fix-06 (client-settable `family_wide_kind`) remains recorded, accepted debt, unrelated to this fix's scope.

---
*Quick task: 260812-01e*
*Completed: 2026-08-12*
